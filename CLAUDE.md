# FocusPet — 설계 스펙 (Claude Code 핸드오프용)

## 목적
작업/공부 중 컴퓨터 화면 한쪽에 떠 있다가, 정해진 방식으로 "집중하세요" 알림을 주는 개인용 데스크탑 펫.
포트폴리오용 아님 — 일상 사용 목적. 브라우저 안이 아니라 **다른 어떤 프로그램을 켜놔도** 위에 떠 있어야 해서 웹앱이 아닌 Electron 데스크탑 앱으로 선택함.

## 핵심 요구사항 (바뀌면 안 되는 것)
1. 다른 프로그램(전체화면 강의 영상 포함) 위에 항상 표시되어야 함
2. 캐릭터는 여러 종류 중 사용자가 설정에서 선택
3. 알림 트리거 방식이 설정 가능해야 함
   - 고정 간격 (강의 시청처럼 입력이 적은 상황용)
   - 활동(키보드/마우스) 감지 — 입력이 끊기면 알림 (문서 작업 중 딴짓 감지용)
   - 위 두 개 동시 사용도 가능해야 함

## 현재 구현 방식
- **메인 프로세스** (`main.js`): 트레이 아이콘, 펫 창 생성, 타이머/idle 폴링, 설정 파일 저장/로드
  - `petWindow.setAlwaysOnTop(true, 'screen-saver')` + `setVisibleOnAllWorkspaces({visibleOnFullScreen:true})`로 전체화면 위 표시 구현
  - 고정 간격: `setInterval`
  - 활동 감지: `powerMonitor.getSystemIdleTime()`을 5초마다 폴링, threshold 넘으면 1회 알림 (재알림은 다시 활동 후 idle 재진입 시)
  - `both` 모드는 두 타이머를 동시에 돌림
  - 이거와 별개로 `startAwayPoll()`이 항상(알림 모드 설정과 무관하게) 5초마다 idle time을 확인해서 3분(`AWAY_IDLE_THRESHOLD_SEC`) 이상 입력이 없으면 `pet-away-state` IPC로 펫 창에 알림 → 펫이 잠들었다가 활동 재개 시 깨어남 (순전히 연출용, 알림 로직과는 독립적)
  - `startCursorTrackPoll()`도 마찬가지로 항상 돌아감 — 100ms마다 `screen.getCursorScreenPoint()`와 `petWindow.getBounds()`를 읽어 커서를 이 창의 로컬 좌표로 환산한 값을 `'cursor-track'` IPC로 펫 창에 보냄. `pet.js`의 로컬 `mousemove`는 커서가 이 340x210 창 위에 있을 때만 발생하는데(드래그-따라가기가 겪는 것과 같은 사각지대), 커서 반응형 인터랙션(아래 "이동 정책 변경 3단계" 참고)은 창 밖에 있는 커서에 대해서도 "어느 쪽에 있는지"를 알아야 해서 추가함 - 드래그-따라가기의 16ms 폴링과 달리 물리적으로 커서에 붙어있을 필요가 없어 100ms로 충분히 가벼움.
  - 펫 창 크기 460x210 → 340x210으로 축소 (패트롤 가능 범위가 너무 넓다는 피드백 반영)
  - **항상 최상단 유지 (`startAlwaysOnTopPoll()`)**: `setAlwaysOnTop(true,'screen-saver')`는 창 생성 시 딱 한 번만 호출돼 있었음 — Windows에서는 다른 앱이 자기도 topmost/screen-saver 레벨을 요청하거나, 특정 전체화면 독점 앱/게임, UAC 프롬프트 등의 상황에서 이 창이 다른 창 뒤로 밀릴 수 있는데(Electron에 이런 상황을 알려주는 이벤트가 따로 없음), 한 번 밀리면 다시 앱을 재시작하기 전까진 계속 뒤에 숨어있게 됨. `startAwayPoll`/`startCursorTrackPoll`과 같은 패턴으로 `startAlwaysOnTopPoll()`을 추가 — 3초마다 `petWindow.setAlwaysOnTop(true,'screen-saver')`를 **무조건** 다시 호출(`isAlwaysOnTop()`으로 먼저 확인 후 필요할 때만 부르는 방식은 일부러 안 씀 — 플래그 자체는 살아있는데 다른 topmost 창이 나중에 그 위로 올라와서 순서만 밀린 경우는 `isAlwaysOnTop()`이 여전히 true를 반환하므로 그 케이스를 못 잡음, 무조건 다시 호출해야 topmost 그룹 안에서도 맨 앞으로 다시 끌어올림). 원인을 개별적으로 감지/분기하는 대신 몇 초마다 통째로 재적용해서 어떤 원인이든 자연 복구되게 하는, 기존 poll들과 같은 접근.
  - **클릭 통과(click-through)**: `createPetWindow()`에서 `petWindow.setIgnoreMouseEvents(true, {forward:true})`를 기본값으로 걸어둠 — 펫 창은 항상 340x210 사각형을 통째로 차지하므로, 이게 없으면 캐릭터가 그려지지 않은 투명한 나머지 영역까지 뒤에 있는 다른 프로그램 클릭을 다 막아버림. `{forward:true}`가 핵심: ignore 상태에서도 `mousemove`만은 렌더러로 계속 전달되게 해서, pet.js가 커서 위치의 실제 알파값을 보고 판단할 수 있게 함(아래 pet.js 항목 참고). 렌더러는 `set-ignore-mouse-events` IPC(`preload.js`의 `setIgnoreMouseEvents`)로 `ignore`를 켰다 껐다 전환 — 판정 로직 자체는 메인 프로세스가 아니라 pet.js에 있고, main.js는 그 결과를 그대로 `BrowserWindow.setIgnoreMouseEvents`에 전달하는 역할만 함.
  - **드래그로 펫 옮기기**: pet.js가 스프라이트 픽셀 위에서 `mousedown`을 받으면(클릭 통과 히트테스트가 이미 `hitState`로 판정해둔 걸 재사용, 다시 샘플링 안 함) `drag-start` IPC를 보내고, `mouseup`에서 `drag-end`를 보냄. 실제로 창을 옮기는 건 메인 프로세스 쪽 — 렌더러의 `mousemove`만으로는 커서가 이미 이 창의 (아직 안 움직인) 영역 밖으로 나가버리면 못 쫓아가므로, 대신 `drag-start`를 받으면 `setInterval`로 16ms마다 `screen.getCursorScreenPoint()`를 읽어서 grab offset(드래그 시작 시점에 커서가 창 안 어디를 잡았는지, `pet.js`가 `mousedown` 이벤트의 `clientX/clientY`로 계산해 IPC에 실어 보냄)만큼 뺀 자리로 창을 옮김. `drag-end`에서 `clearInterval`.
    - **크기가 계속 자라나는 버그와 그 우회**: 처음엔 `win.setPosition(x,y)`로 옮겼는데, 이 특정 창(투명+frameless+alwaysOnTop이면서 canvas가 매 프레임 다시 그려지는 중)에다 `screen.getCursorScreenPoint()`를 16ms마다 같이 호출하면서 `setPosition`을 반복 호출하면 창 높이가 폴링이 계속되는 동안 끝없이 커짐(재현 스크립트로 최소 조합을 좁혀봄 — `setPosition`만 반복 호출, 이 창인데 드래그 상태 아님, 빈 창에 커서+setPosition 조합, 이 창+드래그 상태인데 poll이 고정값을 쓰는 경우는 전부 안정적이었고, "이 창 + 실제로 mousedown이 눌린 상태 + `getCursorScreenPoint()`로 읽은 값으로 매 틱 반복 호출"이라는 조합에서만 재현됨 — 정확한 근본 원인까지는 못 밝혔지만 재현 스크립트로 조건은 확인함). `setPosition(x,y)` 대신 `setBounds({x,y,width,height})`로 바꾸고 `width`/`height`를 드래그 시작 시점의 `win.getBounds()`에서 딱 한 번만 읽어서 매 틱 명시적으로 다시 못박아주는 걸로 우회함 — 원인은 불명확해도 결과적으로 크기가 절대 안 변하는 걸 반복 확인함.
    - **클릭 통과와의 상호작용**: 드래그 중에는 `pet.js`가 알파 히트테스트를 아예 건너뛰고 `hitState`(=ignore 상태)를 드래그 시작 시점 값으로 고정 — 안 그러면 창이 커서를 쫓아가는 도중 한두 프레임 뒤처질 때 그 순간의 커서 좌표가 (아직 안 움직인) 창의 투명한 부분이나 창 밖으로 샘플링돼서 클릭 통과로 되돌아갈 수 있고, 그러면 이후 `mouseup`을 이 창이 아예 못 받아서 드래그가 영원히 안 끝남.
    - **patrol/걷기 애니메이션 일시정지**: 드래그 중엔 리마인더 말풍선이 떠 있을 때와 똑같이 `paused` 플래그로 처리 — `animal.update(dt, false, ...)`가 강제로 idle을 유지(완전히 얼어붙는 건 아니고 idle 애니메이션은 계속 돎, 기존 말풍선 동작과 동일한 컨벤션). 드래그 시작~끝 동안 `x`(창 안에서의 로컬 패트롤 좌표)는 안 건드리므로, `mouseup` 시점에 창만 새 위치로 옮겨진 채로 그 자리에서 그대로 patrol이 재개됨 — 원래 구석으로 돌아가는 로직이 따로 없어도 자연히 "놓은 자리에서 계속"이 됨.
    - **`mousedown`+`mouseup`이 같은 엘리먼트에서 끝나면 `click`도 같이 발동**해서 드래그를 끝낼 때마다 `poke()`가 한 번 더 불릴 뻔했음 — `mouseup` 핸들러에서 `suppressNextClick` 플래그를 세팅해 그 다음 `click` 한 번을 소비하도록 처리.
    - **"잡힌 느낌" 연출**: "그냥 순간이동하듯 옮겨지는" 느낌이 아니라 실제로 손에 집힌 것처럼 보이도록 4가지 후보(스퀴시, 매달림 포즈, 스프링 지연 추적, grab/grabbing 커서)를 전부 시도해보고 넷 다 자연스럽게 읽혀서 그대로 유지함 — 하나씩 제거해보는 대신 각각을 독립적으로 검증(스퀴시는 눈으로는 안 보일 만큼 미묘해서 수치 트레이스로, 스프링은 물리 시뮬레이션으로, 나머지는 스크린샷/`getComputedStyle`로)한 뒤 넷 다 통과해서 뺄 이유가 없었음.
      - **스퀴시 + 매달림 포즈**: `createAnimal()`에 `setHeld(held)`를 추가 — `mousedown`에서 `animal.setHeld(true)`, `mouseup`에서 `animal.setHeld(false)`를 호출(`pet.js`). 내부적으로는 `pinnedSleep`과 같은 우선순위 구조의 새 오버라이드 상태 `pinnedHeld`를 두고, `updateBehaviorState()`에서 `pinnedSleep`보다도 먼저 체크해서 `held`라는 새 idle behavior로 강제 진입시킴(잡혀 있는 동안은 걷기/기존 idle 전부 무시). `held.apply()`는 `Math.exp(-t*8)*0.5`로 감쇠하는 스퀴시 펄스(순간적으로 눌렸다가 `bodySquash 0.12`로 잦아듦), `dangle=1`(다리를 축 늘어뜨림 — 아래 참고), 머리/귀도 처짐 방향으로 오프셋을 준다. 스퀴시는 96px 화면에서 체고 변화가 약 4px(그리드 1.25유닛)뿐이라 스크린샷 두 장을 눈으로 비교해서는 차이를 못 느꼈음 — 매트릭스 추적 mock ctx로 몸통 사각형의 실제 world-space 높이를 프레임별로 찍어보고서야(6→4.75유닛으로 즉시 줄었다가 ~0.5초에 걸쳐 5.75로 이완) 실제로 동작한다는 걸 확인함(이전 턴의 "투명 배경이 흰색으로 보여서 안 그려진 줄 알았다"는 교훈과 같은 맥락 — 눈으로 안 보인다고 안 되는 게 아니라 수치로 확인해야 함).
      - **다리 축 늘어짐(dangle)**: `legJointAngles()` 맨 끝에서 `pose.dangle`이 0보다 크면 hip/knee 각도를 `pose.dangleSway`(잡힌 채로 살짝 좌우로 흔들리는 sine)와 고정된 무릎각(12°) 쪽으로 `lerp`해서 덮어씀 — 걷기/tuck 등 기존 다리 로직 위에 얹는 최종 오버라이드라 다른 종별 관절 파라미터를 하나도 안 건드리고 전 종에 동일하게 적용됨. 꼬리도 같은 문제(다른 idle이 세팅 안 하는 필드가 남는 문제, 위 "포즈 전환 글리치" 항목과 동일한 종류)를 피하려고, `updateTailFlavor()`가 매 프레임 종별 기본 흔들림으로 덮어쓰기 전에 `pose.tailOverride` 플래그를 체크해서 세팅돼 있으면 건너뛰게 만듦(`held`와 아래 `tailtwitch` 둘 다 이 플래그를 씀) — 처음엔 `held.apply()`에서 `tailPhase`/`tailAmp`를 직접 세팅했는데 `updateTailFlavor()`가 그 다음에 실행되면서 매번 종별 기본값으로 도로 덮어써버려서 꼬리만 안 늘어지는 버그가 있었음, 이 플래그로 고침.
      - **스프링 지연 추적**: `main.js`의 `drag-start` 핸들러를 "커서 - offset 자리로 즉시 `setBounds`" 방식에서 스프링-댐퍼 물리 시뮬레이션으로 교체 — 매 16ms 틱마다 `targetX/Y`(커서 기준 목표 위치)와 현재 위치 `curX/Y`의 차이에 `SPRING_STIFFNESS(180)`를 곱하고 현재 속도에 `SPRING_DAMPING(16)`을 곱해 뺀 걸 가속도로 삼아 `velX/Y`, `curX/Y`를 적분(오일러 적분, `dt`는 실제 틱 간격을 측정해서 사용, 최대 50ms로 clamp). 일부러 underdamped(진동이 완전히 안 죽고 살짝 오버슈트하는 영역)로 튜닝해서 "뻣뻣하게 따라온다"가 아니라 "느슨하게 매달려 흔들리며 따라온다"는 느낌을 냄 — 별도 스크립트로 150px 스텝 입력에 대한 반응을 시뮬레이션해서 목표치에 수렴은 하되(발산하지 않고) 과도한 오버슈트는 없는 걸 확인한 뒤 실제 값 채택. 이전 턴에 고친 "창 크기가 계속 자라나는 버그" 우회(=`setBounds`에 `width`/`height`를 매 틱 명시적으로 다시 넘기기)는 그대로 유지 — 물리 시뮬레이션으로 바뀐 건 x/y를 계산하는 방식뿐, `setBounds` 자체의 사용법은 안 건드림.
      - **grab/grabbing 커서**: `pet.css`의 `#pet-canvas` 기본 커서를 `pointer`에서 `grab`으로, `pet.js`가 `mousedown`/`mouseup`에서 `canvas.style.cursor`를 `grabbing`/`grab`으로 직접 토글.
- **설정 저장**: `electron-store` 같은 외부 라이브러리 없이 `app.getPath('userData')/settings.json`에 직접 JSON 읽기/쓰기 (`settingsStore.js`). `LEGACY_CHARACTER_MAP`이 예전 로스터의 캐릭터 키(`cat_b`, `rabbit_a`, `rabbit_c`, `panda`, 재분류된 `dog`, 삭제된 `dog_pug` 등)를 로드 시점에 살아있는 키로 자동 치환 — 이 매핑에도 없고 현재 `VALID_CHARACTERS`에도 없는 값이면 `DEFAULTS.character`(`cat_a`)로 조용히 폴백. 저장된 `settings.json`을 수동으로 마이그레이션할 필요 없이 로스터를 계속 정리/확장할 수 있게 하려는 장치.
- **펫 창** (`windows/pet/`): frameless, transparent, `focusable:false`. 캐릭터는 **픽셀아트(도트) 스프라이트**로 렌더링 — 한 차례 Three.js 기반 실제 3D 로우폴리로 시도했다가 다시 픽셀아트로 되돌림.
  - **왜 3D를 접고 픽셀아트로 돌아왔나**: 이 펫은 96x96px 정도의 작은 크기로 화면 구석에 떠 있으면서 캐릭터 10종이 실시간으로 서로 구분되어야 함. 이 크기에서는 3D의 "정확한 비율과 조명"보다 픽셀아트의 "과장된 실루엣과 평면 색상"이 훨씬 잘 통함 — Mac Pet 같은 실제로 성공한 메뉴바/노치 데스크탑 펫들이 전부 이 방식을 쓰는 이유이기도 함. 3D 버전은 목-몸통 이음새 문제는 깔끔히 해결했지만, 그 정교함 자체가 작은 크기에서는 잘 안 보이고 오히려 캐릭터 간 차별점(귀 모양 등)이 실루엣에 묻혀버리는 문제가 있었음.
  - **엔진** (`windows/shared/animal-engine.js`, pet/settings 두 창이 공유, Three.js 종속성 완전히 제거함 — `package.json`에 dependencies 없음, 순수 Canvas 2D): 24x24 논리 픽셀 그리드에 PX=4로 그려서 정확히 96x96px 캔버스가 됨(스케일 단계 없음, 별도 이미지 파일도 없음 — 매번 코드로 그림). 세로 그리드는 원래 20이었다가 24로 늘림 — 토끼 hop 애니메이션 피크에서 `bodyBob`이 크게 음수로 튀면서 귀(`earStyle:'long'`)까지 딸려 올라가는데, 20에서는 그 위로 남는 여백이 없어서 귀 끝이 캔버스 상단 경계에 그대로 잘려나갔음(아래 "토끼 귀 클리핑" 항목 참고). 늘어난 4줄은 전부 기존 콘텐츠 "위쪽"에 여백으로만 추가한 것 — 모든 종의 y좌표를 기존 값에서 +4 해서 authored했고(`GROUND_Y`도 18→22로 같이 이동), 바닥 기준 상대 위치/비율 자체는 그대로 유지됨. 몸통/머리/귀는 사각형(모서리 1px씩 깎은 `pxSoft`로 살짝 둥글게, 단 5px 미만인 작은 도형은 모서리를 깎으면 오히려 십자 모양으로 뾰족해 보여서 그냥 각지게 둠 — 처음에 판다 귀가 뾰족한 십자 모양으로 나온 버그를 이렇게 고침)로 그리는 방식.
    - **토끼 귀 클리핑 버그 고침**: 위 그리드 확장(20→24)에 더해 hop 궤적 자체의 정점 높이(`GAIT_PRESETS.hop`의 `height`)도 5→4로 살짝 줄여 여유를 더 뒀음 — 두 조치를 합쳐 hop 정점에서 귀 끝까지 캔버스 상단까지 최소 2그리드유닛(8px) 이상 남도록 확인함(계산으로 도출한 값이 아니라 실제로 200프레임을 그려서 화면에 찍힌 모든 도형의 최소 y좌표를 추적하는 스크립트로 검증 — 눈으로 스크린샷 몇 장 보고 "괜찮아 보인다"로 끝내지 않고, 한 사이클 전체를 훑어서 한 번도 0 밑으로 안 내려가는지 확인).
    - **관절 체인 (`pxJoint`/`drawLegChain`/`legJointAngles`)**: 다리는 더 이상 통짜 사각형이 아니라 어깨/고관절→무릎→발끝으로 이어지는 2본(upper+lower) 체인 — `pxJoint(ctx, hingeX, hingeY, angleDeg, length, width, color)`가 힌지에서 `angleDeg` 방향으로 `length`만큼 뻗은 사각형 하나를 그리고 그 끝점을 반환, `drawLegChain`이 이걸 두 번(hip→knee, knee→paw) 이어 붙임. 목도 같은 방식으로 몸통 상단 고정점과 머리 사이를 잇는 1본 `neck` — 각도는 매 프레임 `headDX/headDY`(기존 idle behavior들이 그대로 쓰는 필드)로부터 `atan2`로 역산해서 그리므로, groom/sleep처럼 머리가 크게 움직이는 idle을 새로 손대지 않아도 목이 따라 굽음(길이는 원래 종별 고정값이었다가 이후 실제 거리 기반 계산값으로 바뀜 — 아래 "sit 포즈가 몸통이 겹쳐 보이는" 항목 참고). 꼬리는 배열의 첫 세그먼트(몸통에 가장 가까운 것)만 고정 위치에 rigid하게 그리고, 그 다음 세그먼트부터는 이전 세그먼트 끝점에서 이어지는 체인 + 세그먼트별로 위상이 지연된(phase-lagged) sine 파동을 누적해서 마디마디 휘어지는 스윙을 만듦(진폭은 꼬리 끝으로 갈수록 커짐) — 정지 상태(파동 진폭 0)에서는 원래 하드코딩된 좌표를 그대로 복원하도록 세그먼트 간 각도/길이를 원래 좌표에서 역산해서 씀. 각도 컨벤션: 0°=아래, 양수=시계방향(즉 -x/꼬리쪽으로 스윙) — `pxJoint`의 회전행렬에서 직접 유도한 값이라 `legJointAngles`의 다른 계산도 전부 이 부호를 기준으로 맞춤. `drawLegChain`은 선택적으로 `pawColor`/`pawFraction`도 받음 — 아래 다리뼈를 `color`로 그린 뒤 그 끝쪽 일부(기본 35%)만 `pawColor`로 같은 각도/폭으로 겹쳐 그려서 발끝만 다른 색인 2톤 다리를 표현(예: 턱시도 고양이의 검정 다리+흰 양말 — 아래 관련 버그 항목 참고). 안 쓰는 종은 완전히 무해.
    - **종별 관절 차별화** (`SPECIES[...].joints`): 다른 파라미터들과 같은 철학으로(파일 상단 코멘트에 있는 "10종 정도뿐이니 도출 공식보다 종별로 손으로 명시하는 게 더 안전하다"는 원칙) 종마다 `front`/`back` 다리의 길이·스윙폭·무릎 굽힘폭·tuck(웅크림) 목표각을 전부 따로 명시. 특히 토끼(`rabbit_b`)는 뒷다리가 hock(발목)을 크게 접는 실제 토끼 해부 구조를 반영해 뒷다리 `tuckHip/tuckKnee`를 훨씬 크게(95°/130°) 잡고 앞다리는 거의 안 움직이게(`swingAmp` 작음) 해서 "뒷다리가 일을 다 하는" 실루엣을 냄 — 고양이/강아지는 앞뒤가 더 대칭적. 강아지 중 `floppyRollover:true`가 설정된 종(`dog_dachshund`)은 `rollover` 중 무릎 각도에 sine 흔들림을 얹어 헐렁한 다리 느낌을 추가. `dog_corgi`/다리 짧은 품종은 스윙폭/무릎폭을 작게 잡아 뭉툭하고 덜 굽는 다리로, 햄스터는 반대로 스윙폭을 크게 잡아 짧고 빠른 종종걸음으로 차별화.
    - **고양이 `sit` 포즈가 뭉개져 보이던 버그**: 원인은 `tuckHip`/`tuckKnee` 수치 자체 — 고양이 4종 전부 `tuckHip`이 55~60°(고관절이 크게 뒤로/옆으로 스윙)인데 `tuckKnee`는 75~85°(무릎은 상대적으로 덜 굽음)로 잡혀 있어서, 앉았을 때 발끝이 몸통 아래로 깔끔하게 접히는 게 아니라 다리 전체가 뒤로 크게 휙 젖혀지면서 발이 허공에 어정쩡하게 걸리는 모양이 나왔음(스크린샷을 직접 렌더링해서 확인 — 무릎 각도만 눈으로 훑어서는 안 보이던 문제라 실제로 sit 포즈를 그려서 눈으로 봐야 알 수 있었음). 실제 고양이가 식빵 자세로 앉을 때는 반대 — 고관절 스윙은 작게, 무릎을 크게 접어서 발끝이 몸통 바로 아래 붙게 되는 쪽. 그래서 고양이 4종의 `tuckHip`/`tuckKnee`를 앞다리 `15°/125°`, 뒷다리 `20°/135°`로(스윙 최소화, 무릎 폴딩 최대화) 다시 잡음 — 강아지/토끼/햄스터의 sit도 같이 렌더링해서 확인했는데 이쪽은 원래도 문제없었어서(하체 비율이 다르고 tuck 각도 자체도 덜 극단적) 건드리지 않음, 즉 이 버그는 고양이 4종 한정.
    - **sit 포즈가 "몸통이 겹쳐 보이는" 것처럼 남아있던 진짜 원인 (목 앵커/길이 버그)**: 위 tuckHip/tuckKnee 수정으로 다리는 고쳐졌지만, 이후에도 턱시도 등 일부 종은 sit 상태에서 목 부근에 몸통 색과 다른 덩어리가 붙어있는 것처럼 보이는 문제가 남아있었음. 각도 숫자를 또 조정하는 대신 목 관절의 앵커/회전 기준점 자체를 다시 짚어봄 — `neck`은 `pxJoint(ctx, neckBaseX, neckBaseY, neckAngle, neckLen, ...)`로 그리는데, `neckLen`이 `cfg.joints.neckLen`(종별 고정값)이었음. 고정 길이 뼈는 머리 "방향"만 가리킬 뿐 머리에 실제로 "닿지는" 않음 — 평소(걷기)엔 머리와 몸통이 authored 위치상 우연히 거의 붙어 있어서 이 차이가 안 보였지만, `sit`/`sleep`처럼 `bodyBob`이 커서 몸통 상단이 머리보다 훨씬 아래로 내려가는 포즈에서는 목 베이스와 머리 부착점 사이의 실제 거리가 `neckLen`보다 훨씬 커짐 — 그런데도 뼈는 여전히 `neckLen`만큼만 그려지니, 두 점을 잇지 못하는 짧고 두꺼운 대각선 막대가 허공에 떠 있는 것처럼 보였고, 이게 몸통색(`cfg.bodyColor`)이라 몸통과 안 붙어있으면 "몸통 조각이 삐져나온 것"처럼 읽혔음(같은 색 종은 우연히 안 보였고, 흰 패치와 인접한 턱시도/허스키 같은 종에서 두드러짐). **고침**: `neckLen`을 종별 고정값 대신 `Math.max(0.3, Math.hypot(headAttachX-neckBaseX, headAttachY-neckBaseY))`로 매 프레임 실제 거리를 계산해서 사용 — 이제 목은 항상 몸통과 머리를 정확히 잇는다. 이것만으론 부족해서(폭이 여전히 두꺼워 짧은 거리에서도 눈에 띔) 10종 전부 `neckW`도 대략 절반으로 줄임(예: `cat_a` 2.2→1.2, `dog_husky` 2.6→1.5). `cfg.joints.neckLen` 필드 자체는 이제 안 쓰이지만(길이는 항상 계산값), 종별 설정에는 그대로 남아있음 — 지우는 게 맞지만 이번엔 렌더링 로직만 고치는 데 집중함. 10종 전부 sit/sleep 포즈로 진입시켜 스크린샷으로 확인해서 해결 확인함.
    - **다리 4개 표현 (`drawLegPair`)**: 옆모습에서 앞다리1+뒷다리1(사실상 좌우 다리가 완전히 겹쳐 보임)로 뭉툭해 보이던 걸, 각 앞/뒤 다리 자리에 "먼 쪽"과 "가까운 쪽" 두 개를 살짝 어긋나게 그려서 4개 다리가 다 있는 것처럼 보이게 함 — `drawLegPair(ctx, cfg, pose, isFront, legY)`가 `legJointAngles()`로 구한 hip/knee 각도를 그대로 쓰되, "먼 쪽" 다리는 x를 `Math.max(0.9, w*0.7)`만큼 뒤(왼쪽)로 밀고 폭을 1px 얇게, hip 각도도 `*0.85`로 살짝 덜 벌어지게 그려서 원근감 있는 실루엣을 냄(각도까지 다르게 한 건 두 다리가 완전히 평행하게 겹쳐 보이면 "그냥 두꺼운 다리 하나"로 다시 읽힐 수 있어서). 처음엔 오프셋을 `Math.max(0.4, w*0.35)`로 작게 잡았는데 확대 스크린샷에서 "다리가 4개"가 아니라 "다리가 살짝 삐뚤빼뚤한 하나"로 보여서, 오프셋을 눈에 띄게 키운 지금 값으로 재조정 — 걷기/앉기 각 포즈에서 다시 확인한 뒤 이 방식을 그대로 채택함(사전에 사용자가 허락한 "지저분해 보이면 2다리 방식으로" 대안은 필요 없었음).
    - **몸통에서 안 떨어지게 하기**: 목/꼬리 앵커는 몸통의 실제 그려지는 상단 가장자리(`bodyBob`뿐 아니라 `bodySquash`로 인한 이동분까지)를 그대로 따라감 — 처음엔 꼬리가 `-bodyBob*0.4`만 반영해서 걷기 중 살짝 처지는 정도로만 튜닝돼 있었는데, `sleep`/`sit`처럼 `bodyBob`이 크고 양수인 데다 `bodySquash`까지 겹치는 포즈에서는 몸통 상단이 훨씬 크게 내려가는데 꼬리는 그만큼 안 따라가서 몸통과 꼬리 사이에 눈에 띄는 빈 사각형 틈이 생기는 걸 스크린샷으로 확인하고 고침(아래 "포즈 전환 글리치" 항목 참고) — 이제는 몸통 rect가 쓰는 것과 동일한 `bodyBob + squashY*0.5` 오프셋을 목/꼬리 둘 다에 그대로 적용.
    - **캐릭터 구분 원칙**: 사실적 비율보다 "실루엣과 색을 확실히 다르게"가 우선. `assets/reference/`의 cat1·cat2·dog1·rabbit1·2·3 사진에서 "그 이미지를 봤을 때 떠오르는 핵심 특징"(귀 모양, 털 색, 무늬)만 뽑아 단순화한 것이지 사실적 재현이 목표가 아님. `fox.jpg`는 참고용으로만 있고 목록엔 없음.
    - **2차 로스터 개편**: 초기 8종(고양이 A/B, 강아지, 토끼 A/B/C, 판다, 햄스터) 중 고양이 B(회색+흰 패치), 토끼 A(보라), 토끼 C(분홍, 귀 벌어짐), 판다 4종을 완전히 삭제하고(`SPECIES`에서 키 자체를 제거 — `CHARACTERS`가 `Object.keys(SPECIES)`로 자동 도출되므로 설정 화면 목록도 같이 빠짐) 고양이 3종·강아지 4종을 실제 품종/무늬 특징을 반영해 새로 추가함. 기존 `dog`(탄 색+처진 귀) 항목은 별도로 남기지 않고 `dog_dachshund`로 재분류(귀 스타일은 그대로 `floppy` 유지, 몸통만 길고 낮게 재작업) — 그래서 강아지가 5종이 아니라 4종. 최종 로스터 10종:
      - **고양이 A** (유지): 주황 태비(세로 줄무늬)
      - **턱시도** (`cat_tuxedo`): `assets/reference/tuxedo-cat-reference.jpg`(정면 사진) 기준으로 재설계 — 이마/귀/눈 주변은 검정, 눈 사이~코~입 주변으로 흰 블레이즈가 세로로 내려오는 좌우대칭 무늬, 넓은 흰 가슴, 발끝만 흰색, 분홍 코, 초록빛 눈. 정면 사진의 "얼굴 중앙 세로 블레이즈"를 옆모습 스프라이트로 옮기면 "머리 앞쪽 가장자리를 따라 눈높이~턱까지 밝은 패치"가 되는 셈이라(이 종은 항상 옆에서 본 모습만 그리므로), 기존에 하나의 사각형이던 `snout`을 배열로 받을 수 있게 확장(위쪽은 좁고 아래쪽은 넓어지는 패치 2개를 쌓음 — `drawCreature`가 `Array.isArray(cfg.snout)`이면 순서대로 다 그림, 기존처럼 객체 하나만 주는 종들은 그대로 동작). 코 색도 새로 추가한 `nose.color`(없으면 기존처럼 `darkColor`)로 분홍으로 오버라이드. 꼬리는 마지막 세그먼트를 다른 것보다 크게 authored해서 끝이 풍성해 보이게 함(별도 렌더링 기법 없이 세그먼트 크기만으로). *(디버깅 메모: 스크린샷을 처음 봤을 때 흰 블레이즈가 안 보여서 렌더링이 깨진 줄 알았는데, `getImageData`로 좌표를 직접 찍어보니 정확히 흰색(255,255,255,255)으로 그려지고 있었음 — 캔버스 투명 배경이 스크린샷 뷰어에서 흰색으로 보이는 바람에 흰 블레이즈가 배경과 구분이 안 됐던 것뿐이었음. 캔버스에 임의의 진한 배경색을 깔고 다시 스크린샷을 떠서 확인함. 흰색/밝은 색 패치를 새로 디자인할 때는 이 함정을 기억해둘 것 — 투명 배경 스크린샷만 보고 "안 그려진다"고 판단하면 안 됨.)*
      - **삼색이** (`cat_calico`): 흰 바탕에 검정+주황 패치. 실제 칼리코 특유의 "패치가 좌우 비대칭"을 살리려고 양쪽 귀 색을 다르게 줌(`ear.color`/`ear.color2` — 이 종만 쓰는, `drawEarPointy`에 새로 추가한 필드). 초록 눈.
      - **샴** (`cat_siamese`): 몸통은 크림색인데 얼굴·귀·다리·꼬리만 진한 seal-brown "포인트" 컬러. 얼굴이 몸통과 아예 다른 색이어야 해서 `headColor`(새 필드, 없으면 기존처럼 `bodyColor`를 그대로 씀 — `drawCreature`의 머리 그리는 줄만 수정) 도입 계기가 된 종. 파란 눈.
      - **닥스훈트** (`dog_dachshund`, 기존 `dog` 재활용): 몸통을 grid 폭의 80%(19/24유닛)까지 늘리고 높이는 5유닛으로 낮춤 — 이 로스터에서 가장 극단적인 비율. 적갈색, 처진 귀.
      - **코기** (`dog_corgi`): 다리가 아주 짧고(관절 `upperLen`/`lowerLen` 0.6) 몸통이 김. 귀는 `pointy` 스타일을 다른 종보다 크게 키워서 사용(같은 그리기 함수를 재사용하되 파라미터만 다르게 하는 원칙 그대로). **꼬리 없음**(`hasTail:false`) — 실제 펨브록 웰시코기가 선천적 단미인 특징을 그대로 반영, 다른 강아지들과 구분되는 포인트.
      - **허스키** (`dog_husky`): 회색 몸통 + 흰 얼굴(`headColor`)/가슴, 파란 눈, 선 귀. 꼬리를 몸통 뒤가 아니라 등 위로 말아 올라가는 모양으로 authored — 꼬리 체인은 세그먼트 rest 좌표를 어떻게 잡느냐의 문제일 뿐이라(아래 tail-chain 설명 참고) 코드 변경 없이 좌표만으로 표현됨. `swishTail:false`라 거의 고정된 채로 살짝만 흔들림.
      - **포메라니안** (`dog_pomeranian`, 퍼그 대체 — 아래 "3차 로스터 조정" 참고): 허스키와 같은 어휘(선 귀 + 등 위로 말린 꼬리)를 쓰면서도 한눈에 갈리도록, 허스키는 "날렵한 스피츠" 느낌인 반면 이쪽은 몸통 폭/높이를 거의 1:1로 잡아서(다른 강아지는 전부 옆으로 긴 비율) "동그란 털뭉치" 실루엣으로 차별화. 크림/골드 색 — 기존 오렌지 계열(고양이A, 닥스훈트)과도 채도/톤을 다르게 잡음. 등 쪽에 dither 하이라이트(아래 "그라데이션/디더링" 항목)를 얹어 "복슬복슬함"을 텍스처로 흉내.
      - **토끼** (`rabbit_b`, 라벨에서 A/B/C 구분 뗌 — 이제 한 종뿐이라): 크림+분홍 귀끝, 유지.
      - **햄스터**: 더 동글동글하게 재설계 — 아래 "3차 로스터 조정" 참고.
    - **3차 로스터 조정**: 퍼그(`dog_pug`)를 삭제하고 **포메라니안**(`dog_pomeranian`)으로 교체 — 허스키 추가 이후로 "쫑긋한 귀 + 등 위로 말린 꼬리" 어휘를 이미 쓰고 있어서, 치와와(초소형+큰 귀)와 포메라니안(동그란 털뭉치) 중 실루엣이 기존 로스터(특히 몸통이 짧고 낮은 코기/퍼그 계열)와 가장 안 겹치는 쪽으로 포메라니안을 골랐음 — 자세한 차별화 근거는 위 포메라니안 항목 참고. 햄스터도 재설계 — 기존 버전은 몸통이 머리보다 별로 안 커서 "그냥 작은 설치류" 정도로 밖에 안 읽혔는데, 실제 햄스터 특징(거의 목이 안 보일 정도로 동글동글한 몸통, 짧고 통통한 다리, 작은 귀, 꼬리 없음, 볼주머니)을 살려 몸통을 더 크고 둥글게(`bodyW`/`bodyH` 확대), 다리 관절 길이를 더 줄이고(`upperLen`/`lowerLen` 0.5~0.6), 볼주머니(`cheeks`)와 배 무늬를 더 키움.
    - **그라데이션/디더링 실험** (`pxDither`, `SPECIES[...].shade`): 픽셀아트 느낌을 유지하면서 입체감을 줄 수 있는지 시도 — 매끈한 알파 블렌딩 대신 물리 픽셀 단위 체커보드 디더링(`pxDither(ctx,x,y,w,h,colorA,colorB)`, 논리 그리드 유닛이 아니라 실제 화면 픽셀 하나하나를 번갈아 칠함)으로 두 색 사이 중간 톤을 흉내내는, 레트로 픽셀아트에서 실제로 쓰이던 기법. 몸통을 그린 직후 상단에 얇은 띠(1.5~2유닛) 하나만 밝은 색으로 디더링해서 "위에서 빛을 받는 둥근 몸통" 느낌을 냄 — `cfg.shade = {highlight, bandH}`가 있는 종만 적용, 없으면 기존처럼 단색 그대로. 96x96px 실제 크기로 스크린샷을 떠서 확인해보니(확대한 스크린샷만 보면 체커판이 그대로 보여서 안 어울려 보일 수 있음 — 실제 크기에서 판단해야 함) 거슬리는 노이즈가 아니라 은은한 하이라이트 정도로 읽혀서, 샴 고양이 하나만 시도해보고 끝내지 않고 **닥스훈트**·**포메라니안**에도 같은 걸 적용함(닥스훈트는 매끈한 긴 몸통이라 하이라이트가 자연스럽게 어울리고, 포메라니안은 "복슬복슬함"을 텍스처로 흉내내는 용도를 겸함). 나머지 7종은 안 건드림 — 전부 다 입히면 효과가 특별해 보이지 않고, 무늬가 있는 종(고양이A 줄무늬, 삼색이 패치)은 패턴과 디더링이 겹쳐서 지저분해질 위험이 있다고 판단.
    - **샴 고양이 그라데이션 위치/방향 수정 (몸통 → 얼굴, 평평한 밴드 → 방사형)**: 위 `shade`는 몸통에 넣은 것이었는데, 실제로 원했던 건 샴 특유의 콜러포인트(눈코입 주변 진하고 이마/뺨 바깥쪽으로 갈수록 밝아짐) — 그래서 `cat_siamese`에서 `shade`를 떼고 얼굴 전용 `faceShade`를 새로 만듦(`headColor`가 이미 있는 종에서 얼굴을 몸과 다른 색으로 다루는 것과 같은 맥락, `faceShade`는 그 `headColor` 위에 한 겹 더 얹는 하이라이트). **1차 시도**: `pxDither`를 그대로 재사용해서 머리 상단 가장자리(이마 쪽)에 가로 밴드 하나, 머리 왼쪽(뒤통수/귀 쪽) 가장자리에 세로 밴드 하나, 이렇게 두 개의 평평한 50/50 체커 밴드를 겹쳐 발랐음 — 렌더링 자체는 됐지만(`getImageData`로도 확인) 크롭 스크린샷으로 다시 보니 "그라데이션"이 아니라 머리의 절반 정도를 뒤덮는 진한 체커보드 블록과 나머지 절반의 단색 영역이 딱 붙어있는 L자 모양 이색 분할로 읽혔음 — 두 밴드가 각자 독립적으로 하드 엣지에서 뚝 끊기다 보니 서로 안 섞이고, 폭(2.5유닛, 머리 폭 7유닛의 1/3 이상)도 너무 넓어서 "은은한 하이라이트"가 아니라 "칠하다 만 것"처럼 보였음. **2차 수정(현재)**: `pxDither` 대신 새로 만든 `pxDitherRadial(ctx,x,y,w,h,colorA,colorB,focusX,focusY,radius)`로 교체 — 4x4 Bayer 행렬(`BAYER4`, 정렬 디더링에서 쓰는 표준 임계값 행렬)을 이용해 각 픽셀의 임계값과, 그 픽셀이 `focusX/focusY`(기본값: 눈 중심)로부터 얼마나 떨어져 있는지(`radius`로 정규화한 0~1 density)를 비교해서 색을 결정 — focus에 가까울수록(density→0) 진한 `headColor`가, 멀수록(density→1) 밝은 `highlight`가 우세해지는 진짜 방사형 낙차를 만듦. 두 개의 독립된 사각 밴드 대신 머리 전체 영역에 하나의 연속된 함수를 적용하는 방식이라 하드 엣지 없이 자연스럽게 이어짐. 눈으로 보는 판단이 이전 턴들에서 몇 번 틀렸던 전례가 있어서(투명 배경/미묘한 스퀴시 등) 이번에도 시각 확인에만 기대지 않고, 머리 영역을 focus로부터의 거리 3구간(가까움/중간/멈)으로 나눠 각 구간의 평균 RGB를 스크립트로 뽑아 `[115,88,66] → [128,98,73] → [149,114,84]`로(다크 headColor `[92,70,54]`에서 라이트 highlight `[169,130,95]` 쪽으로) 세 채널 모두 단조증가하는 걸 수치로 확인 — 진짜 "중심에서 바깥으로 밝아지는" 낙차가 있는 걸 확인 후 확정. `focusX/focusY`는 기본적으로 `cfg.eye` 위치(대부분의 종에게 자연스러운 기본값)를 쓰고, `radius`는 `cat_siamese`가 `6.5`로 오버라이드 — 다른 종이 `faceShade`를 쓰고 싶으면 `highlight`만 지정해도 되고 필요하면 `focusX/focusY/radius`로 세밀 조정 가능.
  - **행동 상태머신** (3D 버전에서 그대로 가져옴 — 렌더링 방식과 무관하게 잘 만든 부분이라 로직은 유지하고 "포즈를 어떻게 그리는가"만 바꿈): 각 동물은 `walk`(또는 hop 등 자기 gait) ↔ `idle`(종별 행동 하나) 사이를 자동으로 오감 — 원래는 `walkMin~walkMax`초 걷다가 랜덤하게 하나의 idle 행동을 골라 재생하고, 그 행동의 지속시간이 끝나면 곧장 다시 걷기로 복귀하는 식이어서 사실상 항상 patrol 중이었음. 이후 "대부분 제자리, 가끔 짧게 이동"으로 한 차례 완화했다가, 지금은 아래 "커서 반응형 인터랙션으로 전환" 항목에서 자율 이동 자체를 완전히 없앰 — `walk` 상태/`enterWalk()`/gait 관련 코드는 지워지지 않고 그대로 남아있지만(이하 서술 참고) 현재는 아무도 `enterWalk()`를 호출하지 않아 사실상 항상 `idle`. idle 행동 풀: `groom`(그루밍), `stretch`(기지개), `sit`(앉기), `sleep`(수면, 다리 접고 눈 감김+숨쉬기), `earflick`(귀 실룩), `rollover`(옆으로 뒹굴기 — 픽셀아트에서는 캔버스 자체를 몸통 중심으로 회전시켜서 구현), `cheekpuff`(볼주머니 부풀리기, 햄스터), 그리고 아래 "종별 모션 리서치" 항목에서 새로 추가한 `tailtwitch`/`earalert`/`nosetwitch`(+ 그랩 전용 `held`, 위 드래그 항목 참고). 3D의 THREE.js transform 대신, 각 idle behavior의 `apply(pose,t,duration)`이 `pose` 객체(다리 위상, 몸통 bob/squash, 머리 오프셋, 귀 flick, 꼬리 swing, 눈감김, 볼펌프, roll각도 등 순수 숫자 필드)를 채워 넣고, 매 프레임 이 `pose`를 읽어서 사각형을 다시 그림 — 2D/3D 어느 쪽이든 상태머신 코드 구조 자체는 거의 그대로.
    - **종별 모션 리서치**: 기존 가중치 기반 랜덤 idle 풀 구조(`idlePool: [{name, weight}, ...]`, `pickIdleBehavior`)는 그대로 두고, 각 동물이 실제로 하는 특징적 행동을 조사해서 idle 풀 구성/가중치와 새 idle behavior 몇 개로 반영함. 완전히 새로운 애니메이션 시스템을 만들지 않고 기존 `apply(pose,t,duration)` 패턴 안에서 3개만 추가한 이유: 이미 있는 `earflick`(귀 실룩) 자체가 "쫑긋한 귀가 반응하듯 움찔거림" 요구를 그대로 충족해서 재사용하고 가중치만 올리면 됐고, `sit`/`groom`/`stretch`도 이미 종별로 tuck 각도가 달라 "식빵굽기"/"그루밍"/"몸 늘리기"를 이미 표현하고 있었음.
      - **`tailtwitch`(고양이 전용, "꼬리 끝만 까딱거리기")**: `tailPhase = t*14`(빠른 주기), `tailAmp = Math.exp(-t*2.2)*1.3`(시간이 지날수록 감쇠)를 직접 세팅하고 `pose.tailOverride = true`로 `updateTailFlavor()`의 매 프레임 기본 흔들림 덮어쓰기를 막음(위 "다리 축 늘어짐" 항목의 `tailOverride` 메커니즘 재사용) — 걷기 중의 크고 느린 스윙과 뚜렷이 다른, 짧고 빠르게 튕겼다 잦아드는 움직임이라 "몸은 가만히 있는데 꼬리 끝만 신경질적으로 까딱거림"으로 읽힘. 고양이 4종의 idlePool에 비교적 높은 가중치(2~2.5)로 추가.
      - **`earalert`(토끼 전용, "귀 방향 바꾸기/경계")**: `earFlick`을 0→1로 올렸다가(rise) 잠깐 유지(hold)하고 다시 0으로 내리는(fall) 3단계 구간을 `clamp01`로 나눠 만듦 — 기존 `earflick`(짧은 sine-decay 움찔)과 달리 "귀를 세우고 한동안 그 자세를 유지"하는 형태라 별도 behavior로 분리. 토끼 idlePool에서 가장 높은 가중치(3)로, 토끼의 대표 행동으로 설정.
      - **`nosetwitch`(토끼 전용, "코 씰룩거리기")**: `pose.noseTwitch = clamp01(0.5 + Math.sin(t*13)*0.5)`로 빠른 진동을 만들고, 코 렌더링 쪽(`drawCreature`의 nose 그리는 부분)에서 `noseScale = 1 + pose.noseTwitch*0.6`로 코 사각형을 자기 중심 기준으로 살짝씩 키웠다 줄였다 함(좌상단 기준으로 그냥 키우면 코가 옆으로 밀리는 것처럼 보여서, `x/y`에서 `(noseW-cfg.nose.w)/2`만큼 빼서 중심을 고정) — 은은한 귀 떨림도 곁들임. `noseTwitch` 필드는 `defaultPose()`에 새로 추가.
      - **강아지 품종별 gait 차별화 (`gaitOpts`, `SPECIES[...].gaitTuning`)**: "코기는 짧은 다리로 종종거림"은 다리 관절 파라미터만으론 부족해서(관절이 짧아도 걸음 자체의 리듬이 다른 개와 같으면 "그냥 다리 짧은 개"로만 보임) gait 프리셋에 `gaitOpts(anim, defaults) => ({...defaults, ...(anim.spec.gaitTuning||{}), speed: anim.spec.speed})`를 얹어 종별로 `bob`(상하 바운스 폭) 등을 오버라이드할 수 있게 함. `dog_corgi`는 gait 자체를 `trot`에서 (햄스터가 쓰던) `scamper`로 바꿔서 종종걸음 리듬을 냄. `dog_dachshund`는 `gaitTuning:{bob:0.35}`로 바운스를 확 줄여 "낮은 자세로 스윽 이동"하는 글라이드 느낌을. `dog_pomeranian`은 반대로 `gaitTuning:{bob:2.2}`로 바운스를 키워 "통통 튀듯 걷기"를 표현.
      - **허스키 idlePool 재정렬**: 시바/허스키/진돗개류의 "쫑긋한 귀가 소리에 반응하듯 움찔거림"은 이미 있는 `earflick`이 정확히 그 모양이라 새로 안 만들고, `dog_husky`의 `earflick` 가중치를 3.5(다른 idle보다 확연히 높게)로 올려서 이 종의 시그니처 행동으로 만듦.
      - **햄스터**: "볼주머니 채우기/비우기"는 기존 `cheekpuff`를 유지하되 `pose.cheekPuff = Math.sin(clamp01(t/duration)*Math.PI)*0.9`로 한 번 부풀었다 가라앉는 아치형 곡선으로 다듬음(비대칭적으로 훅 부풀었다 뚝 끊기던 것보다 채우고-비우는 왕복이 더 자연스럽게 읽힘). "짧은 다리로 종종걸음"은 이미 `gait:'scamper'` + 짧은 관절 길이로 반영돼 있어서 추가 변경 없음.
    - **포즈 전환 시 리셋 버그 고침**: 예전엔 `idle→walk` 전환 때만 `anim.pose`를 새로 초기화했고, 그 외 모든 전환(walk→idle, idle→idle 재추첨, `poke()`/`forceSleep()`/`wakeUp()`으로 인한 강제 중단)은 이전 pose 객체를 그대로 재사용 — 새 idle behavior의 `apply()`는 자기가 쓰는 필드만 세팅하므로, 직전 behavior가 건드려놓고 자기는 더 이상 안 건드리는 필드(대표적으로 `rollover`의 `rollAngle`, `sit`/`sleep`의 `legsTucked`)가 그대로 남아 다음 behavior와 뒤섞여 그려짐. 캔버스가 96x96px로 여백이 거의 없다 보니(당시엔 96x80px), 예를 들어 `rollover` 도중 3분 idle로 `forceSleep()`이 걸리면 몸 전체가 기울어진 각도(`rollAngle`)가 리셋 안 된 채 `sleep`(다리 접힘+눈 감김) 포즈와 겹쳐져서, 꼬리가 캔버스 위쪽 경계 밖으로 회전해 잘리거나(`legsTucked` 잔존값 때문에 다리 높이가 0 이하로 계산돼 사라지는 것도 같은 원인) 하는 식으로 보였을 것 — `enterIdle()`/`enterWalk()` 헬퍼로 모든 전환 지점을 통일하고 매번 `anim.pose = defaultPose()`로 완전히 초기화하도록 고침. (`assets/reference/bug-tail-ear-glitch.png`가 저장돼 있다고 했는데 실제로는 폴더에 없어서 스크린샷 자체는 못 봤음 — 위 진단은 코드 분석 + 직접 재현한 케이스로 검증한 것.)
    - **클릭 반응 / 잠들기**: 그대로 유지 — 펫을 클릭하면 귀 실룩임 + 짧은 반응 말풍선(`animal.poke()`), 시스템 3분 이상 idle이면 `sleep`에 고정되어 잠들고 활동 재개 시 기지개(`animal.forceSleep()`/`wakeUp()`, main.js의 `startAwayPoll()`과 연동).
    - **"산만하다" 피드백 — 가만히 숨쉬는 `breathe` idle 추가 + idle 리듬 차분화**: `pickIdleBehavior`는 원래 항상 idlePool에서 이름 있는 행동(groom/stretch/sit/...) 하나를 뽑아 재생하는 구조라 — 진짜 "아무것도 안 하고 가만히 서있는" 상태 자체가 없었음, 매 순간 뭔가는 하고 있었던 셈. `breathe`라는 가장 단순한 idle behavior를 새로 추가 — `pose.bodyBob`/`headDY`에 아주 낮은 진폭(0.3/0.15유닛)의 느린 사인(1.1rad/s, 서로 살짝 위상차)만 줘서 "가슴이 오르내리는 숨쉬기"만 표현하고 그 외엔 아무것도 안 건드림. 지속시간을 다른 idle(대부분 0.8~4초)보다 훨씬 길게(7~13초) 잡고, 10종 전부의 idlePool에 가중치 12(다른 항목들 합계가 종별로 8~11.5 정도이므로 대략 절반 이상의 뽑힐 확률)로 추가 — 뽑힐 확률에 더해 지속시간 자체도 훨씬 기니 시간 기준 점유율은 그보다도 높아짐.
      - **수치 검증**: `test/sim-idle-rhythm.mjs`(`npm run test:idle-rhythm`)를 새로 만들어 종별 20분 시뮬레이션에서 `breathe`의 시간 점유율과 분당 전환 횟수를 측정 — 전 종 77~86% 시간을 `breathe`가 차지하고, 분당 전환 횟수는 4.9~7.35회(약 8~12초에 한 번 꼴)로 나옴. `breathe` 추가 전이었다면 개별 idle 지속시간이 1~4초대였으니 분당 전환이 20~40회 이상이었을 것 — 수치로 "훨씬 차분해짐"을 확인. 회귀 테스트로 남겨둠(`test:movement-stats`와 같은 성격 — 나중에 idlePool 가중치를 또 조정하다가 이 리듬이 도로 산만해지면 잡아낼 수 있게).
    - **포즈 전환이 뚝뚝 끊기는 문제 — 크로스페이드 레이어 추가**: `enterIdle`/`enterWalk`가 호출하는 `resetPose()`는 매번 `anim.pose = defaultPose()`로 전체를 즉시 초기화해왔음(위 "포즈 전환 시 리셋 버그 고침" 항목 참고 — 이 자체는 잔존 필드가 다음 behavior와 섞이는 걸 막기 위한 올바른 설계였음) — 문제는 이 초기화가 순간적이라, 예를 들어 `sit`(bodyBob=1.6)에서 `earflick`(bodyBob 안 건드림, 그래서 0)으로 전환되는 순간 화면에 그려지는 값이 한 프레임 만에 1.6→0으로 뚝 떨어져 보였을 것. **고침**: `resetPose()`가 초기화 직전의 pose를 `anim.transitionFrom`으로 스냅샷해두고 `anim.transitionElapsed=0`으로 크로스페이드 타이머를 리셋 — `update()` 맨 마지막(`applyCursorLookEasing` 다음)에서 `applyPoseCrossfade(anim,dt)`가 그 프레임에 새로 계산된 목표 pose 값들을 스냅샷 값에서부터 0.25초(`POSE_TRANSITION_DURATION`)에 걸쳐 숫자 필드마다 `lerp`로 블렌딩. 전환이 0.25초 안에 또 일어나도(가장 짧은 idle인 earflick조차 0.8초는 되므로 실제로는 거의 안 겹치지만) 그 시점의 "지금 블렌딩 중인" pose를 다시 스냅샷하고 타이머만 리셋하는 방식이라 자연히 이어짐 — 별도의 "이미 전환 중이면 무시" 같은 분기 없이도 안전함. `tailOverride` 같은 boolean 필드는 `typeof`체크로 건너뛰고, 숫자 필드만 블렌딩.
      - **수치 검증**: `sit`(bodyBob=1.6) 도중 `poke()`로 강제 전환시켜 이후 20프레임의 `bodyBob`을 추적 — 전환된 첫 프레임은 직전 값(1.6)과 완전히 동일(블렌딩 비율 0에서 시작하므로 팝 없음), 이후 서서히 감소해 최종적으로 목표값(earflick은 bodyBob을 안 건드리므로 0)에 수렴. 크로스페이드가 공식 종료된 뒤에도 값이 계속 줄어드는 게 보였는데 — 이건 버그가 아니라 `updateContinuousGait`의 idle 분기(`else { anim.pose.bodyBob *= 0.8; ... }`, 걷기 관성이 남아있을 때 idle로 전환되면 서서히 감쇠시키던, 이번 작업 전부터 있던 기존 메커니즘)가 매 프레임 `gaitFn(anim,dt,false)`으로 항상 같이 돌고 있어서 크로스페이드와 겹쳐 이중으로 감쇠되는 것 — 결과적으로 팝 없이 더 매끄럽게 수렴하는 방향이라 문제 삼지 않음. `npm run test:invariants`(120만 프레임) 재실행 — 위반 0건.
    - **`sit`을 종별로 분화 (식빵/하체앉기/토끼로프/햄스터정착)**: 예전엔 전 종이 똑같은 `sit`(다리 전부 tuck + 몸통 스쿼시 + 머리 좌우 흔들림)을 썼는데, 실제 동물마다 앉는 자세가 다름. `legJointAngles`가 쓰던 단일 `pose.legsTucked` 스칼라를 `legsTuckedFront`/`legsTuckedBack` 두 필드로 쪼갬(`legJointAngles(cfg,pose,isFront)`가 `isFront`에 따라 둘 중 하나를 읽음) — 앞뒤 다리가 독립적으로 접혀야(강아지는 뒷다리만) 하는데 스칼라 하나로는 둘을 구분할 수 없었기 때문. `sleep`/`rollover`/걷기·hop gait의 리셋 분기는 그대로 앞뒤에 같은 값을 넣도록만 고쳐서(대칭적으로 웅크리는 건 여전히 대칭) 기존 동작을 안 건드림. `sit.apply(pose,t,duration,cfg)`에 4번째 인자로 종 config를 넘기도록 시그니처를 넓히고(다른 idle behavior는 다 무시, `sit`만 읽음), `cfg.sitStyle`(SPECIES 테이블에 종별로 명시 — "10종뿐이니 공식보다 손으로" 원칙 그대로)로 분기:
      - **`loaf`(고양이 4종, 그대로 유지)**: 기존과 동일 — 앞뒤 다리 전부 tuck, 몸통 bob 크게, 머리 좌우로 살짝 흔들림.
      - **`haunches`(강아지 4종)**: 실제 "앉아"는 뒷다리만 접고 엉덩이를 바닥에 붙이고 앞다리는 세운 채 상체를 드는 자세라, `legsTuckedBack`만 크게 올리고(0.95) `legsTuckedFront`는 0으로 둬서(다른 tuck 안 하는 idle들이 이미 렌더링하는 "서 있을 때의 기본 무릎 각"을 그대로 씀) 앞다리가 안 접히게 함. **1차 시도가 화면으로는 거의 안 보였음**: 다리 각도 수치 자체는 정확했지만(`legAngles:back hip=47.5° knee=72.2°` vs `legAngles:front hip=0° knee=16°` — 손으로 확인), 다리가 몸통보다 먼저 그려지고(z-order) 접힌 뒷다리의 발끝이 `legY`(몸통 아랫변) 바로 위/근처에서 끝나 몸통 사각형에 거의 다 가려지는 데다, 코기/닥스훈트처럼 다리 자체가 짧은 종은 세그먼트 길이(`upperLen/lowerLen` 0.6~0.7)가 작아서 각도 차이가 나도 실제 픽셀 이동량이 작아 `test/render-poses.mjs` 스크린샷으로 그냥 서 있는 것처럼 보였음(수치는 맞는데 육안 판정과 어긋난 사례 — 이전 턴들의 교훈대로 스크린샷을 직접 떠서 확인했기 때문에 잡을 수 있었음). **수정**: 원래 아무 종도 안 쓰던(죽은 필드였던) `pose.waddleTilt`(각도, `drawCreature`가 `rollAngle`과 합쳐서 캔버스 전체를 회전시키는 데 씀)를 재활용해 `-s*9`도(음수 — `waddleTilt`가 양수면 머리 쪽(고 x좌표)이 아래로 돌아가므로, 앉을 때 원하는 "가슴 쪽이 들리고 엉덩이가 내려가는" 방향은 반대) 만큼 살짝 뒤로 기울임, `bodyBob`도 0.7→1.0으로 소폭 키움 — 다리 각도 비대칭은 그대로 두고 위에 시각적으로 더 뚜렷한 신호를 얹은 것. 재렌더링해서 4종(코기/닥스훈트/허스키/포메라니안) 전부 확인 — 몸 전체가 기울고 앞다리 쪽만 바닥에 닿아 보이는 게 뚜렷해짐. (부수 효과: 그림자(`drawShadow`)는 `pose`를 전혀 안 읽는 고정 타원이라 이 기울임을 따라가지 않아 살짝 어긋나 보이는데, `rollover`가 이미 최대 70°까지 훨씬 크게 같은 회전을 써왔고 그 정도 어긋남은 지금까지 문제로 지적된 적이 없어서 — 이번 9°는 그보다 한참 작음 — 같은 기준으로 안 고치고 남겨둠, 아래 알려진 한계 참고.)
      - **`loafNoLegs`(토끼)**: 뒷다리(이미 가장 크게 접히는 종 — `tuckHip:95/tuckKnee:130`)는 `legsTuckedBack=1`(거의 완전히 접어 몸통 아래로 숨김), 앞다리(원래 작음)는 `0.7`만 접어 완전히 안 사라지게. 몸통 bob은 1.8로 고양이보다 살짝 큼. 좌표 확인 결과 뒷다리 발끝이 `legY` 근처(몸통 바로 아래)로 접혀 들어가 시각적으로 몸통에 가려지는 것 확인.
      - **`roundPuff`(햄스터)**: 다리가 이미 아주 짧아 tuck을 크게 해도 티가 잘 안 나서 앞뒤 각각 0.5/0.6로 적당히만, 대신 `cheekPuff`를 0.25만큼 같이 올려서 "자리 잡고 앉으며 볼도 살짝 부푼" 느낌을 냄 — 기존 `cheekpuff` idle과 겹치지 않게(그쪽은 훨씬 크게 0→0.9 왕복) 은은한 수준으로만.
      - **검증**: `test/render-poses.mjs`가 10종 중 8종(강아지 4 + 고양이 2 + 토끼 + 햄스터)을 실제로 `sit`에 진입시켜 스크린샷으로 저장 — 강아지 4종 모두 기울어진 자세 확인, 고양이는 기존과 동일한 식빵 자세 유지 확인.
    - **`bodySquash` 중 pattern/belly/cheeks 장식이 몸통 밖으로 튀어나오는 문제 재확인 + 이번엔 실제로 고침**: 지난 턴에 "알려진 한계"로 남겨뒀던 항목("풀 스쿼시일 때 패턴/배 무늬가 튀어나와 보임")을 이번 sit 분화 작업 후 다시 확인해달라는 요청에 따라 재점검 — 참고로 이 문제는 `sleep`(모든 종이 `bodySquash`를 쓰는 유일한 idle)에서만 발생하고 `sit`(이번에 손댄 것)은 애초에 `bodySquash`를 전혀 안 쓰므로 sit 분화 자체와는 무관하지만, 요청대로 재확인 후 그대로 있길래 이번엔 제대로 고침. **원인**: `pattern`은 `y`에 `pose.bodyBob`만 더하고 스쿼시 오프셋이 아예 없었고(몸통 윗변은 스쿼시로 내려가는데 패턴은 안 따라가 위로 튀어나옴), `belly`는 `+squashY*0.3`/`-squashY*0.5`라는, 몸통 자체가 쓰는 비율(윗변 `+squashY*0.5`, 높이 `-squashY`)과 다른 임의의 상수를 쓰고 있었음 — 둘 다 "그때그때 봐서 튜닝한" 근사치였지 몸통의 실제 축소 비율과 연동된 게 아니었음. **고침**: `bodyScaleY = bodyH/cfg.bodyH`(몸통이 스�쿼시로 줄어든 실제 비율)를 구해서, `pattern`/`belly` rect를 "몸통의 authored 좌표계 기준 상대 위치"를 그대로 유지한 채 몸통과 동일한 비율로 이 함수(`toBodySquashed`)를 통해 리맵 — 스쿼시가 0이면 정확히 기존과 같은 결과(`bodyScaleY=1`)가 나오고, 스쿼시가 커질수록 몸통이 줄어드는 만큼 패턴/배 무늬도 같이 줄어들어 몸통 경계를 벗어나지 않음. 같은 자리에서 `cheeks`(햄스터 볼주머니)도 점검해보니 `pose.bodyBob`을 아예 안 더하고 있었던 걸 발견(머리는 `headY`에 `+pose.bodyBob`을 더하는데 볼주머니는 `headDX/headDY`만 썼음) — 걷기 bounce나 이번에 손댄 `sit`/`roundPuff`처럼 `bodyBob`이 있는 모든 상황에서 볼주머니가 머리에서 미세하게 떨어져 보였을 문제라 `+pose.bodyBob`을 추가해 같이 고침. 패턴/벨리와 달리 정확한 비례 관계가 필요한 게 아니라 그냥 빠진 오프셋 하나를 더한 것이라 별도 스케일 로직 없이 바로 해결.
    - **이동 정책 변경 1단계 (상시 patrol → 대부분 제자리, 가끔 짧게 이동, 이후 2단계에서 완전히 대체됨)**: 예전엔 `walk`가 끝나면 곧장 idle 하나 재생 후 곧장 다시 `walk`로 돌아가는 구조라, idle 지속시간(대략 1~9초)과 walk 지속시간(`walkMin~walkMax`, 종별로 2~7.5초)이 거의 반반씩 번갈아 — 즉 펫이 깨어있는 시간의 절반 가까이를 항상 좌우로 걷고 있었음. 한 차례는 `updateBehaviorState`의 idle 분기에 idle 누적 시간과 다음 이동까지의 랜덤 목표치를 추가해 "평소엔 제자리, 가끔 짧게 이동"으로 완화(이동 길이는 `spec.walkMin/walkMax`에 축소 계수를 곱해 재사용, 종별 상대적 보폭감 유지)했었음 — `test/sim-movement-stats.mjs`(`npm run test:movement-stats`)로 walk 비율이 종별 추정 약 43~65%(원래) → 실측 약 2~5%(1단계)로 떨어진 걸 수치로 확인했었음. 아래 2단계에서 이 누적 타이머/burst 트리거 자체를 완전히 걷어내고 커서 반응형으로 대체했으므로, `test:movement-stats`를 다시 돌리면 이제 전 종 walk 비율이 정확히 0.0%로 나옴(더 이상 자율 이동이 전혀 없다는 걸 보여주는 회귀 확인용으로 남겨둠).
    - **이동 정책 변경 2단계 (자율 이동 완전 제거 → 커서 반응형 인터랙션)**: 사용자가 연속으로 두 번째 "더 차분하게" 요청 — 이번엔 "가끔 짧게 이동"조차 빼고 자체적으로는 아예 안 움직이되, 커서 움직임에 반응하도록 바꿔달라는 것. 두 파일에 걸쳐 구현:
      - **엔진 쪽 (`animal-engine.js`)**: `updateBehaviorState`의 idle 분기에서 "다음 이동까지 누적 시간을 세다가 `enterWalk()` 호출" 로직 자체를 제거하고 원래의 "idle 하나 끝나면 다음 idle 하나 더" 단순 반복으로 되돌림 — `MOVE_INTERVAL_MIN/MAX`/`MOVE_BURST_SCALE`/`idleElapsed`/`nextMoveDelay`는 완전히 삭제. 다만 `enterWalk()` 함수 자체와 `walk` 상태 분기, `GAIT_PRESETS`/걷기 애니메이션, 종별 `gait`/`speed`/`walkMin`/`walkMax` 필드, `pet.js`의 `x`/`direction`/`bounds()` 좌표 로직은 지우지 않고 그대로 남겨둠 — 지금은 아무도 `enterWalk()`를 호출하지 않아 사실상 죽은 코드지만, 완전히 잘 작동하고 검증까지 끝난 걷기 렌더링 하나를 "지금 아무도 안 부른다"는 이유만으로 통째로 뜯어내는 건 이번 요청(자율 이동을 멈춰달라는 것이지 걷기 렌더링 자체를 없애달라는 게 아님) 대비 과한 변경이라 판단 — `cfg.joints.neckLen`을 지오메트리용으로는 안 쓰지만 지우지 않고 남겨둔 것과 같은 판단 기준. 나중에 "가끔 커서 쪽으로 몇 걸음 다가가기" 같은 걸 다시 넣는다면 이 자리에 그대로 다시 연결하면 됨.
      - **커서 신호 처리**: `createAnimal()`이 반환하는 객체에 `setCursorHint(hint)`를 추가 — `hint`는 `null`(커서가 멀리 있거나 없음) 또는 `{dx, dy, close}`(펫 중심 기준 커서 방향과, 더 가까운 "경계" 반경 안인지 여부 — 실제 화면/윈도우 좌표 계산은 엔진이 모르는 영역이라 호출자인 `pet.js`가 계산해서 넘김). 매 프레임 두 함수가 이걸 소비: `updateCursorAlertTrigger`(커서가 "멀리→가까이"로 바뀌는 순간에만, 종의 idlePool에 있는 것 중 가장 "경계 반응"다운 행동 하나로 갈아탐 — `ALERT_BEHAVIOR_PRIORITY = ['earalert','tailtwitch','nosetwitch','earflick']` 순서로 종의 idlePool을 뒤져서 있는 것 채택, 전부 없으면 모든 종이 가진 `earflick`으로 폴백 — "품종별 idle 행동 풀에 이미 있는 반응 활용"이라는 요청을 그대로 반영)와 `applyCursorLookEasing`(고개/눈이 커서 쪽을 향하도록 `pose.lookDX/lookDY`를 목표치 쪽으로 매 프레임 부드럽게 보간, `LOOK_MAX=0.8`그리드유닛/`LOOK_EASE_RATE=6`). `lookDX/lookDY`는 `headDX/headDY`와 별개 필드로 둬서 groom/sleep/sit 등 idle behavior 자체의 머리 오프셋과 안 겹치고 그 위에 얹히도록 함(`drawCreature`가 머리/눈/귀/코/주둥이/볼주머니를 그릴 때 전부 `pose.headDX+pose.lookDX`, `pose.headDY+pose.lookDY`를 합친 `hdx`/`hdy`를 씀 — 목도 `headX`/`headY`를 통해 자동으로 따라 굽음). `pinnedSleep`/`pinnedHeld`(자는 중/드래그 중)일 땐 완전히 무시.
      - **호출 순서 버그와 수정**: 처음엔 `updateCursorAlertTrigger`와 이징 로직을 하나의 `updateCursorLook()` 함수로 합쳐서 `updateBehaviorState`보다 *먼저* 불렀는데, 렌더링 스크린샷 확인 중(아래 검증 항목 참고) 같은 커서 힌트를 줘도 실행마다 `lookDX`가 다르게 수렴하는 걸 발견 — 원인은 `enterIdle()`/`enterWalk()`가 전환마다 `anim.pose`를 통째로 `defaultPose()`로 리셋하는데(포즈 전환 리셋 컨벤션, 위 항목 참고) 이게 `lookDX/lookDY`도 매번 0으로 되돌려버려서, 흔한 idle→idle 재추첨(수초마다 랜덤하게 발생)이 일어날 때마다 커서를 향한 시선이 다시 0부터 수렴해야 했던 것 — `updateTailFlavor`가 `tailPhase`/`tailAmp`를 매 프레임 자기가 다시 세팅해서 이 문제를 안 겪는 것과 같은 이치인데, 커서-룩 로직은 반대 순서(리셋되기 전에 세팅)로 불러서 이 이점을 못 받고 있었던 것. `updateCursorAlertTrigger`(전환을 일으킬 수 있는 부분, 여전히 `updateBehaviorState` 이전)와 `applyCursorLookEasing`(그냥 보간만, `updateTailFlavor`와 같은 자리인 `updateBehaviorState`/idle `apply()` *이후*)로 분리해서 고침 — 이제 어떤 전환이 일어나든 그 프레임 안에서 바로 보정됨. 5번의 반복 실행으로 `lookDX`가 매번 동일하게(0.78~0.80) 수렴하는 걸 확인.
      - **`pet.js` 쪽**: 기존 클릭 통과 히트테스트용 `pendingMouse`(샘플링마다 `null`로 비워짐)와는 별개로 `lastMouseClient`/`lastMouseMoveAt`을 새로 둬서 "커서가 가만히 멈춰있는 동안에도" 마지막 위치를 유지 — 매 프레임 `updateCursorHint()`가 펫-랩의 화면상 중심(`x + WRAP_WIDTH/2`, `stageHeight - 14 - WRAP_WIDTH/2`)과 마지막 커서 위치의 거리를 계산해 `CURSOR_NEAR_RADIUS`(130px, 이 안이면 시선이 따라감) / `CURSOR_CLOSE_RADIUS`(55px, 이 안이면 경계 반응 트리거) 두 반경과 비교해 `animal.setCursorHint(...)`를 호출. `mouseleave`는 클릭 통과 창의 `setIgnoreMouseEvents({forward:true})` 오버라이드를 통해 안정적으로 전달된다는 보장이 없어서(`mousemove`는 명시적으로 forward되지만 `mouseleave`도 그런지는 불확실), 대신 `CURSOR_STALE_MS`(400ms) 동안 `mousemove`가 안 왔으면 "커서가 없어짐"으로 간주. `direction`/`x`/`bounds()`는 위에서 설명한 대로 죽은 코드가 아니라 "지금은 항상 0인 advance를 소비하는 자리"로 그대로 둠 — 실제 이동이 없으니 `x`는 초기값(`MARGIN`)에서 절대 안 바뀜.
      - **선택 사항으로 남겨둔 것**: 요청에 "커서가 계속 가까이 머무르면 아주 가끔 한두 걸음 다가가는 정도는 판단해서 넣어도 된다"는 여지가 있었지만, 이번이 "더 차분하게" 요청의 두 번째 연속(1단계 때 이미 상시 patrol을 줄였는데 이번엔 그 마지막 남은 이동까지 빼달라는 것)이라 "자체적으로는 전혀 안 움직임"을 무조건적인 제약으로 지키는 쪽을 택함 — 접근 이동은 넣지 않음. 이미 있는 `enterWalk()`/gait 서브시스템이 그대로 남아있으므로 나중에 필요하면 붙이기만 하면 됨.
      - **검증**: `test/render-poses.mjs`(`npm run test:render-poses` — 직접 만든 헤드리스 Canvas2D 서브셋 구현체 `test/mini-canvas.mjs` 위에서 실제 `drawCreature`/`drawShadow`를 그대로 실행해 PNG로 저장, node-canvas 같은 네이티브 의존성 없이 Node 내장 zlib만으로 PNG를 직접 인코딩)로 커서 힌트별 스크린샷(`out-cat-look-none/right/left-down.png`)을 뜨고, 상태 전이는 스크립트 로그로 확인: `rabbit_b`는 idlePool에 있는 `earalert`로, `cat_a`는 `tailtwitch`로, `earalert`/`tailtwitch`/`nosetwitch`가 전부 없는 `dog_corgi`는 폴백인 `earflick`으로 정확히 반응(모두 기대값과 일치). 커서가 사라진 뒤 5초 넘게 지나도 정상적인 idle 순환으로 돌아와 있는 것도 확인(멈춰있지 않음). `test/sim-invariants.mjs`(120만 프레임)도 이 변경 전후로 재실행해 위반 0건 유지 확인.
    - **이동 정책 변경 3단계 (근접 트리거 → 상시 방향 추적)**: 2단계는 "커서가 `CURSOR_NEAR_RADIUS`(130px) 안에 들어와야만" 시선이 반응했는데, 원했던 건 "화면 어디에 있든 커서 쪽을 상시로" — 즉 트리거가 아니라 항상 켜져 있는 추적. 두 가지를 바꿈:
      - **화면 전체 커서 위치 확보**: `pet.js`의 로컬 `mousemove`는 커서가 이 340x210 창 위에 있을 때만 발생(드래그-따라가기가 같은 문제를 이미 겪었던 것과 동일한 사각지대 — `main.js`의 `drag-start` 주석 참고)하므로, 창 밖 커서 위치는 알 방법이 없었음. `main.js`에 `startCursorTrackPoll()`을 추가 — `setInterval`로 100ms마다(드래그-따라가기의 16ms보다 훨씬 낮은 빈도로 충분 — "어느 쪽에 있는지"만 필요하지 커서에 물리적으로 붙어있어야 하는 게 아님) `screen.getCursorScreenPoint()`와 `petWindow.getBounds()`를 읽어 이미 창 로컬 좌표로 변환한 값(`cursor.x-bounds.x, cursor.y-bounds.y` — 실제 `mousemove` 이벤트의 `clientX/clientY`와 같은 좌표계)을 `'cursor-track'` IPC로 렌더러에 보냄. `preload.js`에 `onCursorTrack` 추가.
      - **`pet.js`**: 기존 로컬 `mousemove`와 새 `onCursorTrack` 둘 다 같은 `lastMouseClient`를 갱신(마지막에 온 쪽이 이김 — 창 안에 있을 때는 `mousemove`가 더 정밀/고빈도라 자연히 우세하고, 창 밖에서는 poll이 유일한 소스라 문제 없음). `updateCursorHint()`에서 "`CURSOR_NEAR_RADIUS`보다 멀면 `null`" 컷오프를 제거 — 이제 `dx`/`dy`는 항상 계산되고 clamp만 됨(상수 이름도 `CURSOR_DIRECTION_RANGE`로 바꿔서 "이 거리부터는 완전히 그쪽으로 봄"이라는, 컷오프가 아니라 포화 거리라는 새 의미를 반영). `close`(근접 alert 트리거)는 그대로 `CURSOR_CLOSE_RADIUS`(55px) 기준 유지 — 상시 추적은 "추가"이지 근접 반응을 대체하는 게 아니라는 요청 그대로.
      - **부수로 발견한 버그: lookDX/lookDY가 idle 전환마다 리셋됨**: 스크린샷 검증 중 같은 커서 위치를 몇 초간 유지시켰는데 `lookDX`가 매번 다른 값(-0.13 ~ -0.80 사이 아무 값)으로 멈추는 걸 발견 — 지난 턴에 "같은 프레임 안에서 보정되도록 순서를 바꿔서" 고쳤다고 생각한 버그가 실은 절반만 고쳐져 있었음. 원인: `enterIdle`/`enterWalk`는 전환마다 `anim.pose`를 통째로 `defaultPose()`로 초기화하는데(포즈 전환 리셋 컨벤션, 위 항목 참고) 이게 `lookDX`/`lookDY`도 0으로 되돌림 — 순서를 고쳐서 "리셋된 그 프레임 안에서" 다시 목표치로 보간을 시작하게는 됐지만, `LOOK_EASE_RATE=6`이면 95% 수렴에 ~0.5초가 걸리는데 idle 지속시간이 짧은 종(`earflick` 혼자만 있어도 0.8~1.3초)은 그 사이 또 전환이 일어나 처음부터 다시 시작하는 일이 잦았음 — 결과적으로 "항상 부드럽게 커서를 향함"이 아니라 idle이 바뀔 때마다 살짝 흔들리다 다시 감기는 톱니파 패턴이었던 것. **고침**: `lookDX`/`lookDY`는애초에 어떤 idle behavior의 `apply()`도 건드리지 않는, 완전히 별개 시스템(`updateCursorAlertTrigger`/`applyCursorLookEasing`) 소유인데 `defaultPose()` 리셋 대상에 왜 포함돼 있었는지가 진짜 문제 — `resetPose(anim)` 헬퍼를 새로 만들어 `enterIdle`/`enterWalk`가 pose를 초기화할 때 `lookDX`/`lookDY`만은 이전 값을 그대로 옮겨 보존하도록 고침(다른 필드는 여전히 전부 리셋 — `rollAngle`/`legsTucked` 등이 새는 걸 막던 원래 컨벤션은 그대로 유지). 재검증: 같은 커서 힌트로 5번 반복 실행 결과 `lookDX`가 매번 정확히 동일한 값(-0.702/0.714)으로 수렴 — 전환 타이밍에 더 이상 안 흔들림.
      - **검증**: `test/render-cursor-sweep.mjs`/`test/render-cursor-sequence.mjs`로 `pet.js`의 `updateCursorHint()`와 동일한 수식을 그대로 재현해 커서를 화면 좌측 멀리(오프스크린, clientX=-600 — 로컬 `mousemove`만으로는 절대 못 얻는 값) → 좌측 창 안 → 중앙 → 우측 창 안 → 우측 멀리(오프스크린)로 쓸어가며 각 지점의 `hint.dx`/수렴한 `lookDX` 값을 숫자로 확인(오프스크린 좌우 모두 정확히 ±1.0/±0.80으로 포화, 창 안 좌우는 각각 0.877/-0.877, 0.892/0.892 등 방향에 맞게 부호 일치) 하고, `cat_a`/`dog_husky` 양쪽에 대해 스크린샷도 저장(`sweep-cat-*.png`, `sequence-*.png`) — 머리/눈 부위가 커서 방향에 따라 실제로 이동한 걸 육안으로도 확인. `test/sim-invariants.mjs`(120만 프레임)도 재실행해 위반 0건 유지 확인.
    - **squash 중 다리 힌지(legY)가 몸통에서 떨어지는 버그 발견 + invariant 자동 탐지 도입**: "간헐적으로, 특정 종/상태에 안 묶여서 몸이 찌그러지거나 뭉개져 보인다"는 리포트에 대해, 스크린샷으로 순간을 잡아 눈으로 판단하는 대신(이전 sit 버그 때도 4000프레임 회귀 테스트로 못 잡았던 전례가 있어 이번엔 접근을 바꿈) `windows/shared/animal-engine.js`에 기하 계측 훅을 추가함 — `px`/`pxSoft`/`pxJoint`가 그리기 직전에 자기가 그릴 정확한 논리 그리드 좌표(회전/PX 배율 적용 *전*, 즉 SPECIES 테이블이 authored된 것과 같은 좌표계)를 `reportPart(tag, ...)`로 보고(`setGeometrySink()`로 등록한 콜백이 있을 때만 — 실제 앱에서는 아무도 등록 안 하므로 `if` 한 줄 비용만 남는 순수 테스트 훅), 몸통/머리/목/다리 8세그먼트/꼬리 N세그먼트에 태그를 답. 이 데이터를 받아 프레임 하나의 유효성을 판정하는 `checkPoseInvariants(cfg, pose, geometry)`를 export하고, `test/sim-invariants.mjs`(`npm run test:invariants`)가 10종 × 6개 시드 구성(시드 4개는 갓 켠 상태, 2개는 각각 앱이 27.8시간/57.9일째 켜져 있던 것처럼 `elapsed` 오프셋을 줘서 오래 켜둔 경우의 오실레이터 누적치도 커버) × 종당 20,000프레임을 돌리면서 `poke()`/`forceSleep()`/`wakeUp()`/`setHeld(true/false)`/말풍선 일시정지를 서로 겹치게(자는 중에 드래그, 말풍선 뜬 채로 잠듦 등) 랜덤하게 섞어 매 프레임 invariant를 체크 — 걸리면 문제 종류/프레임/직전 40프레임의 상태 전환 이력을 `test/invariant-violations.log`에 남김.
      - **1차 시도는 허탕**: 처음엔 각 pose 필드가 finite인지/대략적인 범위 안인지, 몸통·머리 rect 크기가 authored 값 근처인지 정도의 느슨한 sanity check만 넣고 120만 프레임을 돌렸는데 위반 0건 — 이전 sit 버그의 교훈("각도 문제인 줄 알았는데 사실 다른 동적 계산값 문제") 그대로, 헐거운 범위 체크로는 안 걸리는 종류였다는 뜻이라 판단하고 직접 수식을 다시 짚어봄.
      - **손 계산으로 찾은 것**: `drawCreature`에서 `bodyY = cfg.bodyY + pose.bodyBob + squashY*0.5`, `bodyH = cfg.bodyH - squashY`인데(`squashY = pose.bodySquash * cfg.bodyH * 0.35`) 다리 힌지 `legY`는 `cfg.legY + pose.bodyBob`뿐 — 몸통의 실제 렌더링된 아랫변(`bodyY+bodyH = cfg.bodyY+cfg.bodyH+pose.bodyBob - squashY*0.5`, 스쿼시가 몸통을 세로 중심 쪽으로 대칭 압축하므로 아랫변은 `squashY*0.5`만큼 위로 올라옴)을 `legY`가 전혀 따라가지 않고 있었음 — 정확히 목/꼬리를 몸통에 붙게 고쳤던(위 "몸통에서 안 떨어지게 하기" 항목) 것과 같은 종류의 버그가 다리에는 적용 안 돼 있었던 것. `bodySquash`는 `sleep`(전 종의 idlePool에 존재)과 드래그 `held` 둘 다에서 0보다 커지므로, 이 버그는 특정 종/특정 idle 하나에 묶이지 않고 "언제든 잠들거나 집힐 때마다"라 사용자 입장에서는 "아무 때나 랜덤하게"로 보였을 만함.
      - **invariant를 정밀화해서 실제로 잡음**: 위 손 계산을 바탕으로 `checkPoseInvariants`에 "`leg:*:*:upper` 힌지의 y좌표는 `body` 태그로 보고된 렌더링된 아랫변(`body.y+body.h`)과 (오차 0.15유닛 이내로) 정확히 같아야 한다"는 하드 invariant를 추가 — 이건 느슨한 sanity bound가 아니라 "legY는 bodyY+bodyH와 정확히 같아야 한다"는 SPECIES 테이블 위 코멘트에 이미 명시돼 있던 authored-time 컨벤션을 프레임 단위로도 그대로 검증한 것. 재실행하자 즉시 10종 전부, 120만 프레임 중 316,438프레임(약 26%)에서 위반 검출 — `sleep`/`held`로 `bodySquash`가 조금이라도 올라간 프레임마다 100% 재현.
      - **첫 수정은 부호가 틀림**: `legY`에 `+ squashY*0.5`를 더하는 걸로 처음 고쳤는데 재실행하니 위반이 316,438 → 329,834건으로 오히려 늘어남 — 목/꼬리가 따라가는 건 몸통 *윗변*(스쿼시로 아래로 내려감, `+squashY*0.5`가 맞음)인데 `legY`는 몸통 *아랫변*(스쿼시로 위로 올라감, `-squashY*0.5`가 맞음)을 따라가야 하는 거라 부호가 반대였던 것 — 검증 없이 "그럴듯해 보이는 첫 수정"을 그대로 믿었으면 놓쳤을 실수라, invariant 스위트를 고치고 나서도 매번 재실행해 확인하는 이 절차 자체가 그대로 값어치를 함. `legY = cfg.legY + pose.bodyBob - squashY*0.5`로 정정.
      - **최종 확인**: 같은 10종 × 6시드 × 20,000프레임(총 120만 프레임)을 동일하게 재실행 — 위반 0건. `test/sim-invariants.mjs`/`checkPoseInvariants`는 회귀 테스트로 남겨둠(`npm run test:invariants`), 앞으로 pose 계산을 건드릴 때마다 같은 방식으로 재검증 가능.
    - **귀-머리 경계 "베어 문 자국" 노치 버그 (좌표 invariant는 통과해도 실제 렌더링은 깨져 보이는 별개 문제)**: 사용자가 "여전히 웅크릴 때 뭉개져 보인다"며 `assets/reference/glitch-ear-head-seam.png`를 언급했는데 실제로는 폴더에 저장돼 있지 않았음(`bug-tail-ear-glitch.png`가 같은 식으로 없었던 전례와 동일 — 텍스트 설명만으로 재현/진단 진행). 지난 turn의 `checkPoseInvariants`는 "각 파츠가 올바른 좌표에 있는지"만 검증하지 "실제로 그려진 픽셀이 매끈한지"는 전혀 다른 질문이라 이런 종류의 버그는 애초에 잡을 수 없는 범위 밖이었음 — 이번엔 실제 렌더링 결과를 봐야 해서, Node에서 실제 `drawCreature`를 그대로 실행해 PNG로 뽑는 헤드리스 Canvas2D 서브셋(`test/mini-canvas.mjs` — `fillRect`/`save`/`translate`/`rotate`/경로 fill을 자체 구현, PNG 인코딩은 Node 내장 `zlib`만 사용, node-canvas 등 네이티브 의존성 없음)을 새로 만듦. 여기서 처음에 좌표 변환을 잘못해서(`animal-engine.js`의 `px()`/`pxJoint()`가 이미 자체적으로 `*PX(4)`를 하는데 그 위에 또 `ctx.scale()`을 얹어서 이중 배율) 크롭한 이미지가 전부 배경색으로만 나오는 삽질이 있었음 — `test/render-zoom.mjs`의 주석에 원인을 남겨둠, 실제 배율은 `PX*SCALE`이어야 함.
      - **원인**: `drawEarPointy`(고양이 4종 + 코기/허스키/포메라니안, 7종이 씀)는 삼각형이고, 머리는 `pxSoft`로 그려져서 네 모서리가 1유닛씩 깎여 있음(둥글게 보이려고 — `pxSoft` 주석 참고). 귀의 밑동이 머리 위쪽 모서리 바로 근처에 붙는데, 삼각형은 밑동에서 끝(tip)으로 갈수록 좁아지므로 머리의 모서리-컷이 있는 바로 그 행(`y=headY`)에서는 아직 밑동 폭(100%)에 도달하지 못한 상태(약 75% 폭) — 그 결과 머리의 깎인 모서리와 귀의 아직 안 넓어진 가장자리 사이에 어느 쪽도 안 채우는 작은 삼각형 틈이 남음. 배경이 반대쪽에서 그대로 보여서 지그재그(계단형) 노치로 읽힘 — 정확히 사용자가 설명한 "한 입 베어 문" 모양. 직접 좌표 계산(`legJointAngles`류 함수를 손으로 재현)으로 종별 부족량을 구해봄: `cat_a`/`cat_tuxedo`/`cat_calico`/`cat_siamese` 각 0.63유닛, `dog_husky` 0.70유닛, `dog_pomeranian` 1.00유닛(가장 심함) 부족, `dog_corgi`만 우연히 귀 폭/각도가 넉넉해서 커버됨(7종 중 6종에서 재현). 고정밀 줌 크롭(`test/render-zoom.mjs`, `PX*SCALE`=40배)과 한 픽셀씩 배경/전경을 스캔하는 스크립트로 실제 존재를 확인 — `귀-머리 경계`는 육안 확인 자체도 처음엔 96x96 전체 그림에서는 너무 작아 못 알아챘고(초반 크롭 좌표도 여러 번 재조정), 종 좌표를 정확히 읽어 크롭 범위를 계산한 뒤에야 뚜렷하게 보임(다른 종은 육안으로 안 보인다고 결론 내리지 말고 숫자로 재확인하라는 이전 턴들의 교훈 그대로).
      - **고침**: 머리를 그리는 줄을 `pxSoft(...)`에서 `px(...)`(모서리 안 깎는 일반 사각형)로 교체 — 귀 하나하나의 폭/각도를 종별로 다시 튜닝해서 모서리-컷을 커버하게 만드는 대신, 애초에 모서리-컷 자체를 없애서 6종을 한 번에 해결(귀 스타일이 바뀌어도 다시 안 깨짐). 대가는 머리 모서리가 아주 살짝(1유닛) 덜 둥글어 보이는 것뿐인데, 어차피 대부분 귀에 가려지는 부분이라 손실이 작다고 판단.
      - **부수 확인 (사용자 체크리스트 나머지 항목)**: (1) z-order — 노치의 진짜 원인은 순서 문제가 아니라 좌표 부족이었음(귀가 머리보다 나중에/위에 그려지는 건 원래도 맞는 순서). (2) 무늬(턱시도 흰 배/양말)가 웅크린 몸통 경계를 넘는지 — 지난 턴에 이미 고친 `bodyScaleY` 비례 리맵이 여전히 유효한지 `sit`/`sleep` 양쪽으로 재렌더링해 재확인, 문제 없음. (3) 다른 종 — 위에서 이미 7종 전부(귀가 삼각형인 종) 확인함; 토끼(`long`, 직사각형이라 밑동까지 폭이 안 좁아짐)·햄스터(`round`, `pxSoft`도 5유닛 미만이라 애초에 통짜로 그려짐)는 귀 모양 자체가 다르므로 같은 버그가 구조적으로 발생할 수 없음, 확인차 같이 렌더링해봤지만 문제 없었음.
      - **검증**: 고치기 전 상태로 잠깐 되돌려 `test/BEFORE-seam-{tuxedo,husky,pomeranian}.png`를 뜨고, 고친 뒤 같은 좌표로 `test/AFTER-seam-{tuxedo,husky,pomeranian}.png`를 떠서 나란히 비교 — BEFORE는 머리 경계선이 계단형으로 끊기고 배경이 삐져나오는 게 보이고, AFTER는 귀의 대각선 가장자리가 머리의 수평 가장자리로 매끄럽게 이어짐(끊김 없음). 한 픽셀씩 스캔하는 스크립트로도 BEFORE에서 보이던 "배경 열 개수"가 AFTER에서 0으로 떨어지는 것 확인. `test/sim-invariants.mjs`(120만 프레임)도 재실행해 위반 0건 유지 확인(머리를 `px`로 바꿔도 보고되는 geometry 자체는 동일 — `pxSoft`도 작은 도형이 아니면 전체 rect를 한 번에 report하므로 invariant 체크와는 무관).
    - **강아지가 가끔 물구나무 서듯 뒤집히는 버그 — 원인은 `rollover`, 상태 전환 로직은 무관함**: `anim.behaviorState`/`anim.currentIdle`을 직접 대입하는 곳이 `enterIdle`/`enterWalk` 두 곳뿐임을 확인(grep으로 전수 확인) — 즉 모든 전환이 pose 리셋을 거치므로 `rollAngle`이 전환 사이에 새서 쌓이는 종류의 버그는 아님. 진짜 원인을 찾으려고 `test/render-rollover.mjs`로 `rollover` idle의 실제 회전 곡선(0→70°→0, `Math.sin` 아치)을 프레임별로 렌더링해서 직접 봄 — `dog_dachshund`(bodyW:19/bodyH:5, 로스터에서 가장 극단적으로 길고 낮은 비율)가 `rollAngle`이 최대치(70°) 근처일 때 옆으로 구르는 게 아니라 몸 전체가 거의 수직으로 서서 판자를 세워놓은 것처럼 보임 — 회전 피벗이 몸통 중앙 근처에 고정돼 있어서, 몸이 짧고 통통하면(고양이 등) 그 각도가 "옆으로 살짝 구르기"로 읽히지만 몸이 길고 낮으면 같은 각도가 "긴 축이 거의 수직이 되는" 결과를 냄 — `dog_husky`(bodyW:14/bodyH:8)·`dog_pomeranian`(bodyW:14/bodyH:10)도 정도는 약하지만 같은 방향으로 어색함. 상태머신 버그가 아니라 "의도된 행동(장난스럽게 옆으로 구르기)이 특정 체형에서 과하게 나타나는" 케이스로 판단 — 사용자 지침대로 삭제 대신 자연스럽게 보강함.
      - **고침**: `rollover.apply(pose,t,duration,cfg)`(다른 idle과 마찬가지로 4번째 인자로 종 config를 받도록 확장)에서 회전 최대각을 고정 70° 대신 `cfg.rolloverMaxAngle ?? 70`으로 — `rollover`를 실제로 쓰는 3종(`dog_dachshund`/`dog_husky`/`dog_pomeranian`, 코기는 `rollover` 자체가 idlePool에 없음)에만 몸통 비율에 맞춰 손으로 값을 낮춤: 닥스훈트 22°(가장 극단적이라 가장 크게 낮춤), 허스키 42°, 포메라니안 58°(제일 정사각형에 가까워 원래 각도에서 덜 깎음) — 10종 전부에 공식을 적용하는 대신 이 3종만 손튜닝(이 파일 전반의 "종 몇 개뿐이니 공식보다 손으로" 원칙 그대로).
      - **검증**: `test/render-rollover.mjs`로 3종 모두 회전 곡선을 다시 렌더링 — 닥스훈트는 최대각에서도 몸이 완만하게 기울어진 "장난스러운 구르기"로 보이고(물구나무 서던 것과 확연히 다름), 허스키/포메라니안도 덜 극단적으로 개선. `test/sim-invariants.mjs` 재실행, 위반 0건(수치만 바뀐 것이라 애초에 영향 없을 걸로 예상했고 확인도 그렇게 나옴).
    - **턱시도 고양이 다리 전체가 흰색으로 보이는 버그 (사용자가 직접 코드에서 원인 특정)**: `cat_tuxedo`의 `legColor:'#ffffff'`가 다리 전체(허벅지+종아리)를 흰색으로 칠하고 있었는데, 위 tuxedo 항목의 참고 사진 설명 자체가 "white only at the very end of each leg (paws)"라고 이미 명시하고 있었음 — 발끝만 흰 양말이어야 하는데 종 재설계 당시 다리 전체를 흰색 하나로 퉁친 채 넘어갔던 것. `drawLegChain`/`drawLegPair`가 다리 하나당 색을 하나만 받는 구조라 애초에 2톤 다리를 표현할 방법이 없었음(사용자가 코드까지 읽고 구조적 원인을 짚어줌).
      - **고침**: `drawLegChain(ctx,...,color,tagPrefix,pawColor,pawFraction)`에 선택 인자 두 개를 추가 — 아래 다리뼈(무릎→발끝)를 `color`로 그린 다음, `pawColor`가 있으면 그 위에 같은 각도/폭으로 아래 다리뼈 끝부분(길이의 `pawFraction`만큼, 기본값 `PAW_FRACTION_DEFAULT=0.35`)만 한 번 더 겹쳐 그림 — 같은 각도/폭이라 이음매 없이 딱 맞물림. `pawColor`/`pawFraction`을 안 주는 종은 이 블록 자체가 완전히 건너뛰어져 기존 동작과 100% 동일. `cat_tuxedo`는 `legColor`를 몸통색(`#161616`)으로, `pawColor`를 흰색(`#ffffff`)으로 설정.
      - **다른 종 점검**: 요청대로 `cat_calico`도 확인 — `legColor:'#faf3e6'`가 `bodyColor:'#faf3e6'`와 정확히 같아서 애초에 문제 없음(원래도 몸 색 그대로인 크림색 다리, 단색 의도 그대로 맞음). 샴(`legColor:'#5c4636'`)은 몸통이 아니라 포인트 색과 일치 — 실제 샴 고양이가 다리/발끝까지 진한 포인트 색인 게 맞는 특징이라(위 샴 항목 코멘트 "dark seal-brown points on ears/face/paws/tail" 참고) 이건 흰 양말이 아니라 의도된 단색이므로 안 건드림. 허스키(`legColor:'#e8e6e0'`)는 `headColor`(흰 얼굴/가슴)와 같은 색 — 이미 확립된 "흰 얼굴+가슴" 마킹의 연장으로 다리도 통짜 흰색인 게 자연스러워 안 건드림. 발끝만 다른 색이어야 하는 종은 턱시도 하나뿐이었음.
      - **검증**: `cat_tuxedo`를 실제 `drawCreature`로 렌더링 — 고치기 전(`test/BEFORE-legs-tuxedo.png`, 다리 전체가 흰색)과 고친 뒤(`test/AFTER-legs-tuxedo.png`, 다리는 검정이고 발끝에만 작은 흰 양말)를 나란히 비교해 눈으로 확인. `test/sim-invariants.mjs` 재실행 — 위반 0건(`leg:*:*:paw` 태그는 길이 검사 정규식 `^leg:(front|back):(near|far):(upper|lower)$`에 안 걸려 그 특정 체크는 건너뛰지만, finite/캔버스 밖 이탈 등 일반 geometry 체크는 다른 모든 파츠와 동일하게 받음).
    - **닥스훈트 처진 귀가 머리보다 커서 목 아래로 늘어지는 버그 (사용자가 직접 원인 특정)**: `dog_dachshund`의 `ear:{h:7}`인데 `headH:6`이라 귀가 머리보다 크고, `drawEarFloppy`가 머리 위쪽에서 그 길이만큼 그대로 아래로 그리다 보니(`headY+1`에서 시작해 `e.h`만큼 아래로) 실제로 몸통/앞다리 영역(`bodyY:16`~`legY:21`)까지 침범해서 머리에서 뚝 떨어진 진한 색 블록처럼 보였음(사용자가 좌표까지 대조해서 정확히 짚어줌).
      - **고침**: 종별 수치를 손으로 낮추는 대신 `drawEarFloppy` 자체에 상한을 걸어서 앞으로 추가될 floppy 귀 종에도 똑같이 적용되게 함 — `effectiveH = Math.min(e.h, cfg.headH - 0.5)`로 그리는 높이를 제한(머리 아랫변에서 최대 0.5유닛까지만 처짐 허용, 그 이상은 잘림). 닥스훈트는 `e.h=7`이 `effectiveH≈5.5`로 잘려서 귀 아랫변이 머리 아랫변(`grid y=19`)에서 `19.47`(약 0.5유닛 처짐)로 끝남 — 고치기 전엔 `20.97`(약 2유닛 처짐, 몸통/다리 영역까지 침범)이었던 것과 비교해 숫자로 확인.
      - **검증**: `test/BEFORE-ear-dachshund.png`/`AFTER-ear-dachshund.png`(전신)과 더 확대한 `BEFORE/AFTER-ear-dachshund-crop.png`(귀-몸통 경계만 크게)를 나란히 비교 — AFTER에서는 귀 아래로 몸통/다리 쪽 실루엣의 원래 계단형 윤곽이 다시 드러나 보이는 반면(귀가 그 위를 안 덮음), BEFORE는 그 계단을 귀 색이 그대로 덮어버려 하나의 이어진 블록처럼 보였음. 픽셀 스캔으로 귀 색(`#8a3f1f`)이 나타나는 가장 아래쪽 행의 grid y좌표를 직접 재서 20.97 → 19.47로 줄어든 것 확인. `test/sim-invariants.mjs` 재실행 — 위반 0건(귀는 애초에 invariant가 태깅/검사하는 대상이 아니라 이 변경과는 무관 — 눈으로 보는 렌더링 검증이 유일한 확인 수단).
    - **sleep 포즈에서 귀-머리 사이에 배경색 틈이 생기는 버그 (사용자가 직접 원인 특정)**: `drawCreature`에서 머리 사각형은 `pose.bodySquash`가 클 때(`sleep`/`held`) `headY + (cfg.headH - headH)`로 윗변이 아래로 밀려 그려지는데(스쿼시가 헤드 위쪽만 깎아내는 방식이라 — 위 "귀-머리 경계 노치" 항목의 헤드 렌더링 코멘트 참고), 귀를 그릴 때 넘기는 `earCfg`는 밀리기 전의 `headY`를 그대로 쓰고 있었음 — `cat_a`로 확인해보니 `bodySquash=1`(sleep 완전히 진입)일 때 `cfg.headH - headH_actual = 1.75`유닛만큼 귀 밑동이 머리 실제 윗변보다 위에 붕 떠서, 그 사이로 배경색이 드러나 보임(사용자가 코드에서 정확히 짚어줌).
      - **고침**: 머리의 실제로 그려지는 윗변을 `headTopY = headY + (cfg.headH - headH)`라는 이름으로 명시적인 변수로 뽑아내고, 머리 `px()` 호출과 `earCfg.headY` 둘 다 이걸 쓰도록 통일. `bodySquash=0`인 다른 모든 포즈(sit/걷기/기타 idle)에서는 `cfg.headH-headH = cfg.headH-cfg.headH = 0`이라 `headTopY`가 `headY`와 정확히 같아지므로(대수적으로 완전히 동일), 이 변경이 그 포즈들의 렌더링 결과를 단 1픽셀도 바꿀 수 없음 — 그래도 사용자 요청대로 실제로 렌더링해서 확인함(아래 검증 항목).
      - **부수 발견 (faceShade도 같은 버그)**: 같은 자리를 고치다가 `faceShade`(샴 고양이의 콜러포인트 그라데이션)도 `pxDitherRadial(ctx, headX, headY, ...)`로 밀리기 전 `headY`를 그대로 쓰고 있는 걸 발견 — 요청받은 범위(귀)는 아니지만 정확히 같은 원인(헤드 실제 윗변과 안 맞는 좌표)이라 같이 고침(`headTopY`로 교체). 시각적 증상은 귀만큼 뚜렷하진 않았음(배경색 대신 그라데이션이 살짝 위로 치우치고 아래쪽이 짧아지는 정도) — 샴 sleep 렌더링(`test/BEFORE/AFTER-faceshade-sleep-siamese.png`)으로 그라데이션이 머리 아래쪽까지 더 고르게 이어지는 걸 확인. `headAttachY`(목 부착점 계산에 쓰는, 머리 내부 55% 지점)는 손대지 않음 — 목 길이가 매 프레임 실제 거리로 재계산돼 어차피 시각적 끊김이 안 생기고, 이번에 보고된 범위도 아니라서 더 깊이 들어가지 않음.
      - **검증**: `cat_a` sleep 포즈를 고치기 전/후로 렌더링해 확대 크롭 비교(`test/BEFORE-earhead-sleep-cat.png` vs `AFTER-earhead-sleep-cat.png`) — BEFORE는 귀 삼각형과 머리 사각형 사이에 뚜렷한 초록(배경) 띠가 있었는데 AFTER는 완전히 사라지고 귀가 머리에 매끈하게 이어짐. 요청대로 다른 포즈 영향 없음도 확인: `test/render-poses.mjs`로 만든 `out-cat-sit.png` 등 sit 포즈 스크린샷이 이전과 시각적으로 동일(애초에 `bodySquash=0`이라 대수적으로 변경 자체가 불가능함을 위에서 확인했지만 눈으로도 재확인). `test/sim-invariants.mjs` 재실행 — 위반 0건.
    - **강아지 "물구나무" 재현 시도 - 실제 임계값 초과 프레임을 못 찾음 (정직한 보고, 숫자 조정 없음)**: 사용자가 `rolloverMaxAngle`(22°)과 `waddleTilt`(-9°)를 정적으로 렌더링해서는 handstand가 안 나온다고 확인하고, 실행 중 순간을 잡는 계측을 요청 — `animal-engine.js`에 `setRotationDebug(sink, thresholdDeg=25)`를 새로 추가(기존 `setGeometrySink`와 동일한 옵트인 패턴, 아무도 안 부르면 완전 무비용). `createAnimal()`의 `update()` 맨 끝에서 매 프레임 `recordRotationDebug(anim, elapsed)` 호출 — `|pose.rollAngle+pose.waddleTilt|`(정확히 `drawCreature`가 `ctx.rotate()`에 넘기는 그 합)가 임계값을 넘으면 pose 전체값 + 직전 20개 상태 전환(behaviorState/currentIdleName이 실제로 바뀐 시점만 기록, 값 변화 없는 매 프레임을 다 안 남겨서 로그가 안 부풀어오름) 이력을 sink로 보고.
      - **대규모 시뮬레이션**: `test/sim-rotation-debug.mjs`가 `test/sim-invariants.mjs`와 같은 방식(dt 지터 + poke/forceSleep/wakeUp/setHeld/pause를 서로 겹치게 랜덤 주입, 이번엔 커서 힌트 랜덤 주입도 추가 — cursor-alert 트리거가 `rollover` 도중에도 끼어들 수 있는 경로라 빠뜨리면 커버리지 구멍이었음)으로 `rollover`나 `sit`(`haunches`)이 있는 종(닥스훈트/허스키/포메라니안/코기) + 대조군으로 고양이 2종을, 종당 8개 시드 × 300,000프레임(약 83분 상당) 총 1,440만 프레임 실행. 결과: 25° 초과 243,299프레임 전부 `currentIdleName:'rollover'`에서만 발생, `rollAngle`/`waddleTilt`가 동시에 유의미하게 0이 아닌 경우·60°(로스터에서 설정된 가장 큰 `rolloverMaxAngle`인 포메라니안 58°보다도 큰 값) 초과·`rollover`/`sit` 외 다른 idle에서 발생, 이 셋 중 어느 것도 0건 — 즉 전부 이미 튜닝하고 스크린샷으로 확인해둔 정상적인 rollover 피크였고 "이상 현상"은 하나도 안 잡힘.
      - **실제 Electron 앱으로도 확인**: `FOCUSPET_DEBUG_ROTATION=1`로 실행하면 main.js가 `did-finish-load` 이후 IPC로 렌더러에 계측을 켜라고 알리고, 렌더러는 `setRotationDebug`의 sink에서 다시 IPC로 main 프로세스에 이벤트를 보내 파일로 기록(콘솔이 아니라 파일인 이유: 이 창은 frameless+click-through라 devtools를 붙이기 까다로움, main.js는 진짜 Node 프로세스라 `fs` 접근이 그냥 됨). **삽질 기록**: (1) 처음엔 `loadFile(file, {query:{...}})`로 플래그를 넘기려 했는데 렌더러에서 전혀 안 잡혀서(원인은 깊이 안 팜 — IPC push가 이미 이 코드베이스에서 검증된 패턴이라 그쪽으로 바로 전환) `did-finish-load` 후 IPC push로 바꿈. (2) `Start-Process -FilePath "npm"`으로 백그라운드 실행했는데 프로세스 자체가 안 뜬 적이 있었음 — `npm`이 Windows에서 `.cmd` 셸 래퍼라 `-RedirectStandardOutput`을 쓰면 "%1 is not a valid Win32 application" 에러로 즉시 실패(리다이렉션 없이 부를 땐 방식이 달라서 되긴 했었음) — `cmd.exe /c npm start > log 2>&1`로 감싸서 해결, 이걸로 실제 콘솔 출력을 처음 볼 수 있었음. (3) 그 콘솔 출력에서 `settingsStore.js`가 `JSON.parse` 실패로 기본값(`cat_a`)에 폴백하고 있는 걸 발견 — 캐릭터를 `dog_husky`로 바꾸려고 PowerShell `ConvertTo-Json | Set-Content -Encoding utf8`로 유저의 실제 `settings.json`(`%APPDATA%\focus-pet\settings.json`)을 수정했는데, 그 방식이 UTF-8 BOM을 넣고 메시지 문자열의 한글을 깨뜨려버려서 `JSON.parse`가 터지고 있었음 — **유저의 실제 데이터 파일을 손상시킨 것**이라 Node `fs.writeFileSync(...,'utf8')`(BOM 없음)로 원래 한글 메시지를 복원하고 캐릭터를 `dog_husky`로 다시 설정, 계측이 끝난 뒤엔 원래 값(`dog_dachshund`)으로 되돌려놓음.
      - **결과**: `dog_husky`로 약 4~5분 실제 구동 — `rollover`가 1회 발생, `combinedAngle`이 25.07°~42.00°(허스키의 `rolloverMaxAngle`과 정확히 일치) 범위에서 매끄럽게 오르내렸고 출처는 전부 `rollover`, 이상 케이스 0건 — 시뮬레이션 결과와 완전히 일치.
      - **결론 (정직하게 보고)**: 라이브 실행(실제 앱 ~5분) + 대규모 시뮬레이션(1,440만 프레임, 커서/포크/수면/드래그/일시정지 상호 중첩 포함) 양쪽 다 25°를 넘는 프레임은 전부 이미 알고 있고 스크린샷으로 "장난스러운 기울임"으로 확인해둔 정상적인 `rollover` 피크였음 — `rollAngle`/`waddleTilt` 조합이나 예상 밖 출처에서 나오는 진짜 "물구나무"에 해당하는 프레임은 못 찾음. 코드를 하나도 안 고쳤음(숫자 조정 없음) — 이 현재 코드베이스에서는 재현 실패로 결론. 재발한다면 이번에 만든 계측(아래 참고)을 다시 켜서 그 시점의 `history`를 보는 게 다음 단서가 될 것.
      - **계측 중 남긴 것/치운 것**: `animal-engine.js`의 `setRotationDebug`/`recordRotationDebug`와 `test/sim-rotation-debug.mjs`(`npm run test:rotation-debug`)는 `setGeometrySink`/`test/sim-invariants.mjs`와 같은 성격(옵트인, 무비용, 재사용 가능한 회귀 도구)이라 그대로 남겨둠 — 이 조사에서 이미 한 번 실제로 값어치를 증명했으니 다음에 비슷한 리포트가 오면 바로 재사용 가능. 반면 `main.js`/`preload.js`/`pet.js`의 실제 앱 전용 배선(env var 릴레이, IPC 핸들러, `reportRotationDebug`/`onEnableRotationDebug`)은 이번 조사 한정 용도라 전부 제거함 — 원인 진단용 `env-check.log` 남긴 것도 삭제. `test/rotation-debug.log`(시뮬레이션 쪽 요약)만 남아있고 실앱 로그(`rotation-debug-live.log`)는 정리함.
    - **"물구나무" 진짜 원인 발견 — 회전각이 아니라 다리가 몸통과 같이 뻣뻣하게 도는 것 (사용자가 화면 녹화를 프레임 단위로 분석해서 특정)**: 위 조사는 "`rollAngle`/`waddleTilt` 합이 튀는 순간"을 찾는 가설이었고 그건 못 찾았음(정확한 결론이었음, 아래 내용이 그걸 틀렸다고 뒤집는 게 아님) — 사용자가 실제 리포트된 녹화 396프레임을 직접 프레임별로 분석해 다른 각도로 짚어줌: 실루엣의 가로:세로 비율이 전체 영상에서 한 번도 1.7:1 밑으로(수직에 가깝게) 안 떨어짐 — 즉 `rolloverMaxAngle`로 이미 깎아둔 회전각 자체는 튄 적이 없다는 뜻. 대신 `legJointAngles`를 다시 읽어보니: `rollover.apply()`는 `pose.rollAngle`(몸통 전체를 `drawCreature` 맨 위 `ctx.rotate()`로 회전시키는 값)과 `legsTuckedFront/Back`(최대 0.4까지만 tuck 방향으로 블렌드)만 세팅하고, 다리의 hip/knee 각도 자체는 그 tuck 블렌드 외엔 평소 서 있을 때 계산(`phase*swingAmp` 등, idle 중엔 `legPhase=0`이라 사실상 각 종의 기본 서 있는 각도)에서 별로 안 움직임 — 다리가 몸통 로컬 좌표계 안에서 "힘이 빠진" 각도로 바뀌는 게 전혀 없이 몸통과 같은 `ctx.rotate()` 안에서 그대로 같이 돔. 그 결과 다리의 **월드(화면) 공간 각도 = 로컬 각도 + rollAngle**로 그냥 더해지는데, 로컬 각도가 원래 서 있는 각도(수직에서 15~20° 정도)라 rollAngle이 커질수록 월드 각도가 더 커져서(예: 포메라니안 피크에서 15.9°+57.6°=73.5°, 거의 수평) 몸통이 기울어질수록 다리가 오히려 더 "뻗대며 버티는" 것처럼 보였던 것 — 각도 자체는 정상 범위인데 다리 렌더링이 그 회전을 안 따라가는 게 진짜 문제였음. (닥스훈트에 이미 있던 `cfg.joints.floppyRollover` 플래그+`knee += Math.sin(...)*18` 블록은 무릎에 코스메틱 웨이브만 얹는 것뿐이라 — 자기 주석에도 "purely cosmetic extra wiggle on top of the tuck already driven by legsTucked"라고 명시돼 있었음 — hip 각도의 기본값 자체를 안 바꾸므로 이 문제를 해결 못 했고, 애초에 허스키/포메라니안엔 설정도 안 돼 있었음.)
      - **고침**: `held`(드래그로 집었을 때)가 이미 쓰던 `pose.dangle`/`pose.dangleSway` 메커니즘(`legJointAngles` 맨 끝, hip/knee를 무조건 덮어씀 — 위 "잡힌 느낌" 항목 참고)을 `rollover.apply()`에서도 같이 세팅하도록 확장 — `rollAngle`과 같은 `s`(0→1→0 사인 아치) 엔벨로프로 `pose.dangle = s`, 그리고 `pose.dangleSway = -pose.rollAngle`. `pxJoint`의 자체 `ctx.rotate(rad)`와 `drawCreature` 바깥쪽의 `ctx.rotate(angle)`가 같은 Canvas2D 회전이라 그냥 더해진다는 걸 이용 — 다리 로컬 각도를 `-rollAngle`로 주면 바깥쪽 `+rollAngle`과 정확히 상쇄돼서, 몸통이 얼마나 기울든 다리의 최종 월드 각도가 항상 (거의) 0°(수직 아래, 중력 방향)로 유지됨 — "몸은 굴러도 다리는 중력 따라 축 늘어진 채 그대로"라는, `held`와 정확히 같은 물리적 직관. `legAngle` 자체는 안 건드리고 렌더링(다리가 몸통 회전을 따라가는 방식)만 바꿨으므로 `rolloverMaxAngle`(22°/42°/58°) 수치는 요청대로 그대로 둠. 닥스훈트 전용이던 기존 `floppyRollover` 플래그+무릎 웨이브 블록은 이제 3종 모두를 포괄하는 이 메커니즘이 완전히 대체하므로(부분적으로만 잔존해봐야 dangle 블렌드에 일부 섞여 들어가는 것 말고는 득이 없음) 제거 — `legJointAngles`에서 그 블록과 `dog_dachshund.joints.floppyRollover:true`를 삭제.
      - **수치 검증 (육안보다 우선 — 이전 턴들의 교훈 그대로)**: `setGeometrySink`로 `leg:front:near:upper` 태그의 `hingeX/Y`·`tipX/Y`를 읽어 로컬 hip 각도를 역산(`atan2`)하고, 고치기 전 코드(임시 사본)와 나란히 각 종 rollover 피크 프레임에서 비교 — **로컬 hip각+rollAngle(=월드 각)**이 고치기 전엔 닥스훈트 41.7°/허스키 60.8°/포메라니안 73.5°(전부 수직에서 크게 벗어남, 회전이 클수록 더 벌어짐)였던 게, 고친 뒤엔 닥스훈트 0.4°/허스키 0.5°/포메라니안 0.6°(3종 모두 수직에서 1° 안쪽)로 수렴 — 의도한 "몸이 얼마나 기울든 다리는 중력 방향으로 늘어짐"이 수치로 정확히 확인됨. 스크린샷 비교(96x96 전체 뷰, 확대 크롭 둘 다 시도)는 포메라니안·허스키처럼 다리 세그먼트 길이(`upperLen`/`lowerLen`)가 짧은 종에서는 각도 변화폭(60~70°)에 비해 실제 픽셀 이동량이 크지 않아 육안으로는 뚜렷이 안 갈렸음(이전 턴의 스퀴시 펄스가 "체고 변화 4px라 눈으론 안 보였다"던 것과 같은 종류 — 수치 트레이스로만 확실히 확인됨). `npm run test:invariants`(120만 프레임) 재실행 — 위반 0건.
      - **다음에 참고할 것**: `test/render-rollover.mjs`(이미 있던 스크립트, 그대로 재사용 — 각 종 rollover 사이클을 5개 지점으로 나눠 렌더링)는 이번에도 1차 육안 확인용으로 다시 돌려서 최신 스프라이트로 갱신해둠(`test/rollover-{dachshund,husky,pomeranian}-f{15,30,45,60,75}.png`). 진단에 썼던 "before 엔진 사본 + 수치 트레이스 스크립트"는 이번 조사 전용이라 정리함 — 비슷한 회전/포즈 버그가 다시 리포트되면 `setGeometrySink` + `atan2`로 로컬 각도를 역산해 before/after를 수치로 비교하는 이 방법이 육안 판단보다 먼저 믿을 수 있는 절차라는 걸 기억해둘 것.
    - **rollover 애니메이션 재설계 — dangle 수정으로도 안 고쳐졌던 진짜 원인 (사용자가 화면 녹화를 다시 프레임 단위로 분석해서 특정)**: 위 dangle 수정(다리를 world-vertical로 늘어뜨림) 이후에도 "앞다리로 버티고 뒷다리만 붕 뜬" 모양이 그대로 남아있다는 리포트 — 사용자가 이번엔 회전 피벗 자체(`drawCreature` 맨 위 `cx = (cfg.bodyX + cfg.headX + cfg.headW) / 2`)가 몸통 정중앙이 아니라 머리 쪽에 가깝다는 가설을 제시. 좌표로 직접 검산해봄: 닥스훈트는 `cx=12`인데 꼬리(`tail[0].x=0`)까지 포함한 전체 실루엣의 시각적 경계는 대략 x=0(꼬리)~x=24(코끝)라 중점은 12 — 즉 피벗이 "머리 쪽으로 치우쳤다"기보다는 이미 전체 실루엣의 기하학적 중점에 상당히 가까움(허스키도 마찬가지로 계산상 1 grid unit 이내 오차). **그런데도 사용자가 관찰한 증상 자체는 100% 정확함** — 진짜 원인은 피벗의 위치가 아니라 **애초에 "긴 막대를 하나의 피벗으로 강체 회전시키는" 접근 자체**였음: 몸통 폭(닥스훈트 19유닛)에 비해 몸높이(5유닛)가 극단적으로 얇은 형태에서는, 피벗을 아무리 정확히 중앙에 맞춰도 다리가 붙어있는 피벗 근처 영역은 회전각과 무관하게 화면상 이동량이 작고(반지름이 작으므로), 피벗에서 먼 꼬리 끝은 반지름이 커서 같은 각도에도 훨씬 큰 호를 그리며 휙 움직임 — 이게 바로 "다리는 거의 안 움직이는데(=버티고 서 있는 것처럼 보임) 꼬리만 붕 뜬다"는 정확히 그 인상을 만듦, 피벗을 어디로 옮겨도 한쪽 끝은 항상 이렇게 큰 호를 그릴 수밖에 없는 구조적 문제. 사용자도 "회전축 미세조정 대신 다른 방식으로" 요청했는데, 위 검산이 바로 그 판단이 옳았던 이유를 뒷받침함 — 회전축을 어디로 옮겨도 근본적으로 안 고쳐졌을 것.
      - **재설계 방향 결정 (스크린샷 실측 비교)**: 사용자가 제시한 두 방향 — A) 큰 회전 대신 `bodySquash`를 크게 줘서 옆으로 누운 듯 납작하게 만들고 거기에 짧은 주기로 여러 번 오가는 작은 흔들림을 얹는 "누운 채로 좌우로 뒹구는" 방식, B) rollover 자체를 다른(더 차분한) 행동으로 교체 — 둘 다 실제로 구현해서(B는 `test/_candidateB-engine.mjs`라는 임시 사본에, A는 실제 엔진에) 닥스훈트/포메라니안 rollover 사이클을 스크린샷으로 렌더링해 비교함. **B(차분한 lean, 오실레이션 없이 한 번만 살짝 기울고 유지)** 결과: 닥스훈트는 몸높이(5유닛)가 워낙 얇아서 `bodySquash` 공식(`squashY = bodySquash * bodyH * 0.35`)이 절대 픽셀 변화량으로 환산되면 거의 안 보이는 수준(스크린샷상 거의 평소 서 있는 자세와 구분이 안 감) — 반대로 포메라니안(몸높이 10유닛, 로스터에서 가장 정사각형에 가까움)은 같은 정규화 수치인데도 절대 변화량이 훨씬 커서 과하게 기울어 보임 — 즉 B는 종별 체형에 따라 효과 크기가 들쭉날쭉하고, 대표적으로 가장 문제였던 닥스훈트에서는 사실상 "뒹구는" 느낌이 거의 안 남. **A(적극적 wallow)**는 `bodySquash` 상한을 훨씬 높게(0.9) 잡아 닥스훈트에서도 눈에 띄게 납작해지고, 다리도 명확히 다른 위치로 벌어져 보여 "뒹굴거리는" 인상이 뚜렷함 — 4종 전부(코기 포함, 아래 참고) 스크린샷으로 확인해 채택.
      - **최종 구현 (A 채택)**: `rollover.apply(pose, t, duration)`을 전면 재작성 — 더 이상 `cfg`(`rolloverMaxAngle`)를 안 받음. `envelope = Math.sin(u*Math.PI)`(behavior 진행률 0→1→0의 부드러운 인/아웃)로 `bodySquash`를 `0.15~0.9`까지 올려(옆으로 누운 듯 납작하게 — `sleep`이 이미 전 종에서 문제없이 쓰는 것과 같은 메커니즘, 회전이 전혀 없어서 이 버그 자체가 구조적으로 발생할 수 없음), `bodyBob`을 `envelope*1.6`만큼 내려서(`sleep`과 같은 부호 — 스쿼시되며 바닥 쪽으로 가라앉는 방향) 땅에 눕는 느낌을 보강. 큰 단일 회전 대신 `pose.rollAngle = Math.sin(t*5.5)*14*envelope` — 진폭을 예전 최소치(닥스훈트 22°)보다도 작은 ±14°로, 대신 behavior 지속시간(2.6~3.4초) 동안 여러 번(약 2~3사이클) 오가게 해서 "한 번 크게 구른다"가 아니라 "누운 채로 좌우로 흔들거린다"는 인상을 냄 — 이미 `bodySquash`로 납작해진 실루엣이라 이 정도 진폭으로는 절대 "서 있는 것처럼" 보일 수 없음(아래 검증 참고). 다리는 `held`가 쓰는 `pose.dangle`/`dangleSway` 메커니즘을 재사용하되(위 dangle 수정 항목 참고) `dangleSway = Math.sin(t*7+1.3)*22`로 `rollAngle`과 **독립된 위상**을 줌 — 몸의 흔들림과 다리가 1:1로 같이 움직이면 "그냥 뻣뻣하게 딸려간다"로 보이는데, 위상이 다르면 "몸은 몸대로 흔들리고 다리는 허공에서 따로 파닥거린다"로 읽혀 뒹구는 느낌이 강해짐. `cfg.rolloverMaxAngle`은 이 설계에서 의미가 없어져서 3종(`dog_dachshund`/`dog_husky`/`dog_pomeranian`) 전부에서 필드와 관련 주석을 삭제.
      - **코기에 처음으로 rollover 추가**: 기존엔 코기(`hasTail:false`)의 idlePool에 `rollover` 자체가 없었음(옛 설계가 꼬리 유무와 무관하게도 시도된 적이 없었을 뿐) — 새 설계는 `bodySquash`/`rollAngle`(작은 흔들림)/`dangle`(다리 파닥임) 중 어느 것도 꼬리를 참조하지 않으므로 꼬리 없는 종에도 구조적으로 아무 문제 없이 적용됨, 실제로 스크린샷 확인 결과도 문제 없어서 `dog_corgi`의 `idlePool`에 `{ name: 'rollover', weight: 1 }`을 다른 3종과 같은 가중치로 추가 — 이제 강아지 4종 전부가 rollover를 가짐.
      - **수치 검증 (사용자의 원래 진단 방법을 그대로 재사용)**: 사용자가 최초 버그를 "396프레임 동안 실루엣 가로:세로 비율이 1.7:1 밑으로 안 떨어짐(=한 번도 수직에 가깝게 안 섬)"으로 검증했던 것과 같은 방법을 재검증 스크립트(`test/render-rollover.mjs`)에 그대로 프로그램화 — `setGeometrySink`로 매 프레임 몸통/머리/다리/꼬리 파츠의 bounding box를 모아 가로:세로 비율을 계산하고, rollover 사이클 전체(약 150~190프레임)에서 최솟값을 추적. 결과: 닥스훈트 2.42, 코기 1.72, 허스키 1.54, 포메라니안 1.45 — 4종 전부 1.0을 여유 있게 웃돌아(가로가 세로보다 항상 더 큼) 한 번도 "수직에 가깝게 서는" 순간이 없음을 확인. `npm run test:invariants`(120만 프레임) 재실행 — 위반 0건.
      - **최종 스크린샷 검증**: `test/render-rollover.mjs`로 4종 전부 rollover 사이클을 촘촘히(12프레임 간격) 렌더링해 육안으로 훑고, 대표 5프레임(`f000/036/072/108/144`)을 큐레이션해 `test/rollover-dog_{dachshund,corgi,husky,pomeranian}-f*.png`로 남김 — 전 종에서 몸이 납작하게 눌린 채 좌우로 흔들리고 다리가 각 프레임마다 다른 각도로 벌어져 있는 게 보이며, 예전처럼 몸이 좁고 길게 세워지거나 앞다리만 뻗대고 있는 프레임은 4종 어디에도 없음. 정리: candidate B 비교용 임시 사본(`test/_candidateB-engine.mjs`, `test/_render-candidateB.mjs`, `candB-*.png`)과 예전 단일-arc 설계 전용이었던 구 `render-rollover.mjs`(5프레임 고정 샘플링, 새 설계의 여러 흔들림 사이클을 못 잡음)는 정리하고, 촘촘한 샘플링+수치 검증을 겸하는 새 버전으로 대체.
    - **rollover 재설계 2차 — "시소처럼 보인다" 피드백, rollAngle 오실레이션 자체를 포기하고 "등 대고 누워 다리 버둥거림"으로 전환**: 위 재설계(스쿼시+`rollAngle` 소진폭 다회 흔들림)는 "서 있는 것처럼 보이는" 문제는 확실히 없앴지만, 사용자가 실제로 보니 몸 전체가 규칙적으로 좌우로 왔다갔다하는 게 시소/그네처럼 보인다는 새 피드백 — `Math.sin(t*5.5)*14`가 진폭은 작아도 "한 주파수로 깨끗하게 오가는 단일 진동"이라는 형태 자체가 문제였음(진폭을 더 줄이는 미세조정이 아니라 사용자가 아예 "rollAngle 오가는 접근 자체를 그만두자"고 명시). 사용자 진단: 실제로 뒹구는 강아지의 핵심은 "몸이 기울어짐"이 아니라 "등을 바닥에 대고 누워서 다리를 든 채 버둥거림" — 몸통 회전은 완전히 부차적이어야 하고 다리 자체의 자세/움직임이 주가 되어야 함.
      - **몸통**: `rollAngle`을 몸 전체를 흔드는 용도에서 빼고, 대신 두 개의 서로 다른(맞아떨어지지 않는) 주파수 사인을 합친 미세한 "불규칙한 씰룩거림"으로 교체 — `(Math.sin(t*11) + Math.sin(t*17.3+0.8)*0.6) * 2.2 * envelope`(피크 진폭 약 ±3.5°, 사용자가 제시한 "정 필요하면 ±5도 이내" 안에 여유 있게 들어옴) — 단일 주기가 아니라 서로 안 맞물리는 두 주기를 더해서 "일정한 리듬으로 오간다"는 인상 자체가 안 생기게 함(시소는 규칙적인 왕복이라 시소로 보였던 것이므로, 규칙성을 깨는 게 핵심이라 판단). `bodySquash`(최대 0.9)와 `bodyBob`(가라앉음)은 이전 설계 그대로 유지 — 납작하게 눕는 부분 자체는 문제 없었음.
      - **다리 뒤집기 (`legJointAngles`의 새 `legsUp` 오버라이드)**: 기존 `dangle`(hip/knee를 `dangleSway`/12°로 고정 — "축 늘어짐", `held`에서 쓰는 늘어진-아래-방향 다리)와는 반대로 다리가 **위쪽**(pxJoint 각도 컨벤션상 0°=아래이므로 180°=위)을 향하게 하는 완전히 새 메커니즘을 추가 — `pose.legsUp`(0..1 블렌드 팩터, `dangle`과 같은 자리에서 마지막에 적용되어 항상 우선함)과 `pose.paddleHipFront/Back`·`paddleKneeFront/Back`(앞/뒤 다리 쌍이 서로 다른 주파수·위상으로 `180°` 중심으로 오가는, 매 프레임 완전히 계산된 목표각). `dangle`과 `legsUp`을 별도 필드로 분리한 이유: 하나는 "무게가 안 실려 축 늘어짐"(느린 진자), 하나는 "뒤집혀서 허공에 버둥거림"(빠르고 진폭 큰 파닥임)으로 물리적 의도 자체가 달라 같은 메커니즘을 억지로 겹쳐 쓰면 나중에 헷갈릴 여지가 있다고 판단(이 파일 전반의 "의미가 다르면 필드도 분리" 컨벤션 그대로). `rollover.apply()`는 이제 `dangle`/`dangleSway`를 전혀 안 건드리므로(오직 `held`만 여전히 씀) 실질적 충돌은 없음.
      - **z-order 문제 발견 + 고침 (렌더링해보기 전엔 몰랐을 것)**: 다리를 위로 향하게만 하고 처음 렌더링해보니 다리가 전혀 안 보이거나 몸통에 파묻혀 보임 — 원인은 `drawCreature`가 원래 다리를 몸통보다 **먼저**(뒤에) 그리는 순서였기 때문(서 있는/걷는 다리는 힌지가 몸통 아랫변에 있고 아래로 뻗으므로, 몸통에 가려지는 건 원래 의도된 정상 동작 — 벨리 밑에서 다리가 시작되는 것처럼 보이려는 것). 다리가 **위로** 향하면 같은 힌지(`legY`, 몸통 아랫변)에서 몸통의 세로 범위(스쿼시된 높이) 안쪽으로 뻗어 들어가는 셈이라, 뒤에 그려지면 몸통에 완전히 덮여버림. **고침**: `pose.legsUp>0`일 때는 다리를 원래 자리(몸통보다 먼저)에서 안 그리고, `drawCreature` 맨 끝(머리/눈/코까지 다 그린 뒤, `ctx.restore()` 직전)으로 미뤄서 다시 그리게 함 — "등 대고 누운 개는 버둥거리는 다리가 몸보다 화면에 더 가까운(앞에 있는) 요소"라는 실제 원근과도 맞아떨어져서 단순 z-order 땜빵이 아니라 타당한 순서.
      - **다리 길이가 몸에 파묻히는 문제 (두 번째 렌더링에서 발견) + 종별 파생 공식으로 고침**: z-order를 고쳐도 다리가 짧아서 스쿼시된 몸통 자체의 세로 범위를 못 벗어나 여전히 몸통 위에 얹힌 작은 표식 정도로만 보임(`setGeometrySink`로 다리 끝 y좌표와 몸통 윗변 y좌표를 직접 비교하는 진단 스크립트로 확인 — 최초 시도는 4종 중 3종에서 다리 끝이 몸통보다 여전히 아래(=몸통에 파묻힘)였음). 다리 길이(`j.upperLen+j.lowerLen`, 종별 고정값)에 `reachScale`을 곱해 늘리는 방식으로 대응하되, **고정 배율 하나로는 안 됨** — 포메라니안(몸높이 10, 로스터에서 가장 크면서 다리는 가장 짧은 0.6+0.6=1.2)과 허스키(몸높이 8, 다리 1.1+1.1=2.2)는 몸-다리 비율이 몇 배씩 차이나서, 허스키 기준으로 맞춘 배율을 포메라니안에 그대로 쓰면 전혀 안 벗어나고, 포메라니안 기준으로 맞추면 허스키 다리가 막대기처럼 과하게 길어짐(실제로 배율 6까지 시도해봤다가 스크린샷에서 다리가 몸통 절반 길이만 한 긴 사선 막대로 튀어나와 보여 되돌림). **고침**: `drawLegPair`에서 종별로 "이 종이 실제로 필요한 만큼만" 유도 — `neededReach = cfg.bodyH * 0.685 + 2`(0.685 = rollover의 최대 `bodySquash`인 0.9에서 살아남는 몸높이 비율 `1-0.9*0.35`, +2는 몸통 경계를 눈에 띄게 벗어나라는 여유분), `maxReachScale = min(4.2, max(1, neededReach / (j.upperLen+j.lowerLen)))`(상한 4.2는 포메라니안처럼 극단적으로 짧은 다리에 억지로 큰 배율을 줘서 막대기처럼 보이는 걸 막는 안전판), `reachScale = 1 + pose.legsUp * (maxReachScale - 1)`. `checkPoseInvariants`의 다리 길이 검증(`leg:*:*:upper/lower` 길이가 종별 authored 값과 맞는지 보는 하드 invariant, 앞선 세션에서 만든 것)도 같은 공식으로 업데이트해서 이 의도된 변화를 오탐하지 않게 함.
      - **수치로 확인한 최종 상태 (정직한 보고 — 4종 전부 완전히 몸통을 벗어나지는 않음)**: 위 공식으로 튜닝한 뒤, 다리 끝 y좌표가 몸통 윗변보다 위로 올라가는 정도("clearance", 양수면 몸통을 벗어남)를 rollover 사이클 전체(약 150~200프레임)에서 추적: 닥스훈트 최대 +1.96유닛(172프레임 중 84프레임에서 벗어남), 코기 최대 +0.81유닛(155개 중 24개), 허스키 최대 +1.95유닛(170개 중 61개), **포메라니안은 사이클 전체에서 한 번도 몸통 윗변을 넘지 못함(최대 -1.89, 즉 항상 몸통 안쪽)** — 포메라니안은 몸높이 대비 다리가 로스터에서 가장 짧고(0.6+0.6) 몸통은 가장 큰(10) 조합이라, 위 상한(4.2배)까지 늘려도 물리적으로 부족함. 상한을 6까지 올려서 시험해봤을 때는 수치상 아주 살짝 벗어나긴 했지만(+0.24, 200프레임 중 9프레임만) 실제 스크린샷에서는 다리 각도가 수직에서 먼 순간에 몸 절반 길이만 한 얇고 긴 대각선 막대로 튀어나와 보여 다른 프레임들에서 오히려 더 부자연스러웠음 — 그래서 "가끔 아주 살짝 벗어나지만 나머지 프레임에서 막대기처럼 안 보이는" 6배 대신 "전혀 안 벗어나지만 비율은 자연스러운" 4.2배 상한을 최종으로 선택함(스크린샷으로 둘 다 직접 비교해서 판단 — 수치 하나만 보고 정하지 않음). 포메라니안은 다리가 몸통 실루엣을 완전히 벗어나 허공에 뜨는 것처럼 보이진 않지만, 다리 방향이 위쪽으로 뒤집혀 있고 다른 세 다리와 함께 제각각 다른 각도로 벌어져 있는 건(옅은 다리색이 몸통색과 가까운 것도 겹쳐 눈에 덜 띄는 요인 — `legColor`가 `bodyColor`와 동일) 뚜렷이 보임 — "완벽한 클리어런스" 대신 "비율이 안 깨지는 선에서 최대한"으로 타협한 것을 정직하게 기록. 이후 이 종을 더 다듬으려면 다리 길이보다(이미 한계까지 늘림) `legColor`를 몸통과 다르게 주는 쪽이 다음으로 시도해볼 만한 방향.
      - **꼬리**: 사용자 요청대로 `pose.tailOverride=true` + `tailPhase=t*8`/`tailAmp=1.3*envelope`로 `held`/`tailtwitch`와 같은 메커니즘을 재사용해 꼬리도 같이 흔들리게 함 — 꼬리 없는 코기(`hasTail:false`)는 이 필드들이 설정돼도 애초에 그릴 꼬리 자체가 없어 완전히 무해.
      - **회귀 검증**: `npm run test:invariants`(120만 프레임, 다리 길이 invariant 공식 갱신 포함) 위반 0건, `npm run test:movement-stats` 전 종 walk 0.0% 유지(무관한 변경이라 예상대로). `test/render-rollover.mjs`로 4종 전부 재렌더링해 대표 5프레임(`f000/036/072/108/144`)으로 교체 확인 — 몸 전체가 좌우로 왕복하는 시소 형태의 움직임은 어디에도 없고(작은 불규칙 씰룩임만 있음), 다리가 위쪽을 향한 채 프레임마다 다른 각도로 벌어져 있어 "버둥거림"으로 읽힘(허스키·코기·닥스훈트는 다리가 몸통 윤곽 밖으로 뚜렷이 보이는 프레임이 있고, 포메라니안은 위 항목대로 부분적). 진단에 썼던 스크래치 스크립트(`test/_diag-legreach.mjs` 등)는 정리.
    - **rollover 최종 수정 — "노 젓는 것처럼 보인다" 피드백, 다리 움직임을 파닥임에서 떨림으로 축소 (마지막 시도, 실패하면 삭제하기로 사전 합의)**: 위 paddle 설계(`paddleHipFront/Back`가 180°를 중심으로 ±42~48°, `paddleKneeFront/Back`가 서로 다른 위상/주파수로 오가는 것)에 대해 "다리가 360도로 계속 돌아가며 노 젓는 것처럼 보인다"는 피드백 — 원인은 hip과 knee가 같은 다리 안에서 서로 다른 주파수(예: 앞다리 hip 6.3rad/s, knee도 6.3rad/s인데 위상이 1.9만큼 어긋남)로 움직이면 두 관절이 합쳐진 발끝 궤적이 리사주 도형처럼 루프를 그리게 되고, 진폭도 커서(±35~48°) 이 루프가 눈에 확 띄는 크기로 나왔던 것 — "떨림"이 아니라 실제로 "휘젓는" 움직임을 그리고 있었던 셈. **고침**: 사용자가 제시한 "마지막으로 한 번 더 시도, 안 되면 삭제"에 따라 hip/knee 진폭을 대폭 줄이고(180±9°, 무릎도 14±8°) **같은 다리 안에서는 hip과 knee가 완전히 같은 위상/주파수**를 쓰도록 통일(앞다리 `t*15`, 뒷다리는 `t*13.5+1.1`로 앞/뒤 쌍끼리는 여전히 다르게 — "제각각 움직이는 느낌"은 앞/뒤 쌍 사이에서만 남기고, 한 다리 안에서의 루프는 없앰) — 두 관절이 항상 같은 위상으로 같이 움직이므로 발끝이 그리는 궤적이 루프가 아니라 거의 직선에 가까운 왕복이 되어 "짧게 떨림"으로 읽힘. `bodySquash`/`bodyBob`/몸통의 미세한 불규칙 흔들림(`rollAngle`)은 이전 설계에서 이미 검증됐던 부분이라 손대지 않음.
      - **검증**: 정적 스크린샷 비교로는 "떨림 vs 회전"을 구분하기 어려워서(진폭이 작아 한 프레임만 봐서는 잘 안 보임) `test/_diag-tremble-seq.mjs`(스크래치, 검증 후 삭제)로 rollover 피크 구간에서 3프레임(0.05s) 간격의 연속 스크린샷 6장을 4종 전부에 대해 렌더링 — 다리가 매 프레임 조금씩만 위치를 바꾸며 위쪽을 향한 자세를 유지하는 걸 눈으로 확인(허스키/코기는 특히 또렷하게 다리 모양이 몸통 위로 드러남, 닥스훈트/포메라니안은 이전과 같은 이유로 덜 두드러지지만 회전/루프처럼 보이는 프레임은 없음). `npm run test:invariants`(120만 프레임) 재실행 — 위반 0건. 결과가 충분히 만족스러워서(더 이상 "노 젓기"로 안 보임) 삭제 대신 유지하기로 결정 — `rollover`는 강아지 4종 idlePool에 그대로 남음.
    - **rollover 완전 삭제 + `bellyUp`(배 하늘로 향한 채 가만히 쉬기)으로 교체 — 계속 붙잡지 않기로 결정**: 위 "떨림" 수정 이후에도 "어정쩡하게 엎드린 채 눈이 감긴 것도 뜬 것도 아닌 애매한 자세로 멈추는 것 같다"는 새 피드백 — 사용자가 "전환 크로스페이드나 눈 감김 상태가 중간에 꼬였을 가능성"을 직접 지목. 이번엔 사용자가 명시적으로 "더 붙잡지 말고 완전히 삭제, 대신 훨씬 단순한 정적 포즈로 교체"를 지시 — rollover는 이번까지 포함해 총 4번의 서로 다른 설계(①큰 단일 회전 → 핸드스탠드처럼 보임, ②스쿼시+`rollAngle` 소진폭 다회 흔들림 → 시소처럼 보임, ③다리를 `legsUp`/`paddleHip·Knee`로 파닥이게 → 노 젓는 것처럼 보임, ④파닥임을 떨림으로 축소 → 이번 리포트의 "어중간한 정지 자세")를 거쳤고, 매번 겉으로 드러난 증상은 달랐지만 공통점은 전부 **시간에 따라 변하는 값(회전각, 흔들림, 파닥임/떨림)이 있었다는 것** — 사용자 지침대로 근본 원인을 더 파고드는 대신, "동적인 요소를 아예 없애면 이 종류의 버그 자체가 구조적으로 생길 수 없다"는 판단으로 완전 삭제 + 무(無)-애니메이션 재설계를 선택.
      - **삭제한 것**: `IDLE_BEHAVIORS.rollover` 전체, `defaultPose()`의 `legsUp`/`paddleHipFront`/`paddleKneeFront`/`paddleHipBack`/`paddleKneeBack` 필드, `POSE_UNIT_FIELDS`의 `legsUp` 항목, `POSE_FIELD_BOUNDS`의 `paddleHipFront/Back`·`paddleKneeFront/Back` 항목, `legJointAngles`의 `pose.legsUp>0` 오버라이드 블록, 강아지 4종(`dog_dachshund`/`dog_corgi`/`dog_husky`/`dog_pomeranian`) idlePool의 `rollover` 항목, `test/render-rollover.mjs`와 `test/rollover-dog_*.png`(더 이상 존재하지 않는 behavior를 테스트하던 스크립트라 그대로 두면 무한루프/에러만 남음).
      - **유지한 것 (버그와 무관했던 부분)**: 다리가 몸통에 가려지지 않게 몸통보다 나중에 그리는 z-order 우회(`drawCreature`의 `bellyUp` 분기)와, 종별 몸높이/다리길이 비율에서 필요한 만큼만 다리 길이를 늘리는 `reachScale` 유도 공식(`drawLegPair`) — 둘 다 "다리를 위로 향하게 해서 보이게 하는" 정적인 기하 문제이지, 지금까지 문제였던 "다리가 시간에 따라 어떻게 움직이는가"와는 무관한 부분이라 그대로 재사용(단, 필드명은 `legsUp`→`bellyUp`, 곱셈 상수는 새 `bodySquash` 피크(0.9→0.85)에 맞춰 0.685→0.15로 갱신).
      - **새 `bellyUp`**: `duration: randRange(4,7)`, `apply(pose,t)`가 `s=clamp01(t/0.6)`(그 뒤로는 안 바뀌고 유지 — `sleep`이 이미 오래 검증해온 것과 똑같이 "한 번 램프하고 계속 유지"하는 패턴, `rollover`가 매번 새로 만들었던 `sin(u*Math.PI)` 형태의 "다시 0으로 돌아오는" 엔벨로프 자체를 안 씀)로 `bodySquash=s*0.85`, `bodyBob=s*1.6`, `bellyUp=s`(다리가 위를 향하는 정도), `headDY=s*0.5`, `headDX=-s*0.3`, 그리고 **`eyesClosed=0`을 명시적으로 세팅**. 다리 목표각 자체도 `legJointAngles`에서 `hip=lerp(hip,180,pose.bellyUp)`, `knee=lerp(knee,15,pose.bellyUp)`로 **시간(`t`)이 전혀 안 들어가는 상수** — 예전 `paddleHip/KneeFront/Back`처럼 매 프레임 다시 계산되는 오실레이터가 아예 없음. 꼬리도 `tailOverride`를 안 건드려서 종별 평소 idle 흔들림이 그대로 나감(예전 rollover처럼 별도 흔들기 로직 없음). 몸통 흔들림(`rollAngle`)도 아예 안 건드림(0으로 유지) — "동적인 움직임을 최소화"라는 요청을 문자 그대로, 렌더링에 필요한 최소한(스쿼시+다리각+약간의 머리 처짐, 전부 한 번 램프하고 고정)만 남김.
      - **`eyesClosed=0`을 명시한 이유**: 리포트된 "눈이 감긴 것도 뜬 것도 아닌" 증상의 유력한 후보가 크로스페이드였음 — `sleep`(eyesClosed를 1까지 올림)에서 곧장 전환되면, 옛 rollover의 `apply()`는 `eyesClosed`를 아예 안 건드려서 `defaultPose()`의 기본값(0)에 맡기고 있었는데, 이 경우 크로스페이드가 "직전 값(최대 1) → 이번 프레임의 목표값(0)"을 0.25초에 걸쳐 블렌딩하는 것 자체는 정상 동작이지만, 그 사이(전환 직후 몇 프레임)에 화면을 캡처하면 눈이 부분적으로만 감긴 어중간한 순간을 그대로 보게 됨 — 이게 사용자가 본 "애매한 자세"였을 가능성이 있음(정확한 재현은 못 했지만, 개연성 있는 메커니즘). 새 `bellyUp`은 매 프레임 `pose.eyesClosed=0`을 **명시적으로** 세팅하므로, 크로스페이드가 매 프레임 블렌딩하는 목표값 자체가 항상 정확히 0으로 고정 — 크로스페이드 도중에는 여전히 잠깐 부분적으로 감긴 값을 거치겠지만(0.25초 동안 1→0으로 자연스럽게 줄어드는 것 자체는 의도된 동작), 그 이후로는 다시는 안 올라가고 계속 0을 유지 — "눈 상태가 어중간한 데서 멈춰버리는" 것과 "잠깐 스쳐가는 자연스러운 전환"의 차이.
      - **수치 검증 (반복 렌더링으로 안정성 확인)**: `test/_diag-bellyup-stability.mjs`(스크래치, 검증 후 삭제)로 강아지 4종 각각 90분 시뮬레이션(닥스훈트 42회, 코기 55회, 허스키 39회, 포메라니안 26회 `bellyUp` 진입 — 무작위 `poke()`/`forceSleep()`/`wakeUp()`을 자주 섞어 특히 `sleep`(eyesClosed=1)에서 곧장 전환되는 경우를 의도적으로 많이 만듦) — `bellyUp`이 충분히 정착한 뒤(진입 후 1초 이상 지난 프레임만 집계, 즉 0.6초 램프업+0.25초 크로스페이드 둘 다 끝난 시점) `eyesClosed`가 정확히 0이 아닌 경우, `bodySquash`/`bellyUp` 값이 인접 프레임끼리 0.01 이상 차이 나는(=아직도 드리프트 중인) 경우를 각각 셌음 — **4종 전부 세 가지 지표 모두 0건**, 즉 정착 후에는 눈도 다리도 완전히 고정된 채 안 흔들림을 확인.
      - **스크린샷 검증**: `test/render-bellyup.mjs`(npm 스크립트는 아니고 `node test/render-bellyup.mjs`로 직접 실행 — 매번 새 `createAnimal()`로 독립된 인스턴스를 만들어 서로 다른 난수 상태에서 종별 3번씩 렌더링)로 4종 × 3회 반복 렌더링, 정착된 프레임(`behaviorTimer>1.2초`)에서 `eyesClosed`/`bodySquash`/`bellyUp` 값을 매번 출력 — 12번의 렌더링 전부 정확히 동일한 값(`eyesClosed=0, bodySquash=0.85, bellyUp=1.00`)으로 수렴, 스크린샷으로도 눈이 뚜렷이 뜬 채(감긴 가로선 아님) 납작하게 누워 다리가 몸통 위로 드러난 일관된 포즈를 4종 전부에서 확인(`test/bellyup-dog_{dachshund,corgi,husky,pomeranian}-v0.png`로 대표 1장씩 남김). `npm run test:invariants`(120만 프레임) 재실행 — 위반 0건.
  - **pet.js**: 3D 카메라/투영 다 걷어내고 원래(도트 시절) 방식으로 복귀 — `#pet-wrap`(96x96 div)을 `translateX`로 좌우 이동시키고, 그 안의 `<canvas>`에 `image-rendering:pixelated`로 크리스프하게 그림. 방향 전환은 canvas에 `scaleX(-1)`(래퍼 전체가 아니라 canvas만 뒤집어야 말풍선 글자가 안 뒤집힘). 말풍선은 pet-wrap의 자식 DOM이라 좌표 투영 없이 그냥 따라다님. **좌표 안전장치**: x가 매 프레임 `Number.isFinite` 체크를 통과하지 못하면 안전값으로 폴백한 뒤 clamp — `Math.min/max`만으로는 NaN을 못 거르므로(NaN과의 비교는 항상 false) 이 체크를 따로 둠. 걸음 속도(`spec.speed`)는 이제 3D의 world-unit이 아니라 실제 CSS px/sec. 패트롤 가능 범위는 여전히 좁은 창(340px) 기준 그대로 유지. 효과음은 외부 mp3 없이 Web Audio API 오실레이터로 생성(구체적인 소리 자체는 아래 "알림 소리를 부드럽게" 항목에서 교체됨).
    - **커서 방향에 따라 몸 전체가 좌우로 방향을 바꿈**: 기존엔 머리/눈(`lookDX/lookDY`, animal-engine.js)만 커서를 향했고 몸 전체가 보는 방향(`direction`, canvas의 `scaleX(±1)`을 좌우하는 값)은 그대로였음 — patrol이 아직 있던 시절 걷다가 경계에 부딪히면 뒤집던 것 말고는 `direction`을 바꾸는 경로가 없었음(patrol 자체가 이동 정책 변경으로 완전히 제거된 뒤로는 그 경계-반사 코드조차 죽은 코드). `updateBodyDirection()`을 새로 추가 — `lastMouseClient`(이미 커서 추적에 쓰는 값)의 `dx` 부호로 `direction`을 정하되, 정확히 중앙 근처(±`DIRECTION_FLIP_HYSTERESIS`=20px)에서는 안 바꾸는 데드존을 둬서 커서가 펫 바로 위/아래를 지나갈 때 좌우로 파르르 떨리듯 뒤집히는 걸 막음. 눈동자를 따로 움직이는 건(사용자가 명시적으로 "과할 것 같다"며 제외 요청) 넣지 않음 — 머리 전체가 커서 쪽으로 기우는 기존 `lookDX`만으로 시선 반응은 이미 충분하다고 판단. *(4차 QA 라운드에서 이 이진 좌/우 방식이 각도 기반 연속 회전으로 교체됨 — "실제 사용 후 버그 리포트 2건 수정 + 신기능 1건" 섹션의 2번 항목 참고.)*
      - **호출 순서 문제 (죽은 코드였던 patrol 경계-반사 로직과의 충돌)**: `updateCursorHint()`(머리/눈 커서 추적, `tick()` 맨 앞에서 호출) 안에 `direction` 설정도 같이 넣으려 했는데, `tick()` 뒷부분의 이동/경계 처리 블록(`if(x<=min){x=min;direction=1;}...`)이 그 뒤에 또 실행됨 — `advance`가 항상 0이라(자율 이동 완전 제거 이후) `x`가 영원히 `MARGIN`(=`bounds().min`)에 고정돼 있어서 `x<=min`이 매 프레임 항상 참이 되고, 그 결과 이 블록이 `direction=1`로 매 프레임 강제로 되돌려버려서 커서 기반 설정이 그 프레임 안에서 바로 무효화됐음. 이 patrol 경계-반사 코드 자체는(향후 "커서에게 다가가기" 같은 자율 이동 기능이 돌아올 때를 대비해 일부러 안 지운 죽은 코드 — 위 이동 정책 히스토리 참고) 지우지 않고, 대신 `updateBodyDirection()` 호출을 그 이동/경계 블록 **뒤**(즉 `render()` 직전)로 옮겨서 매 프레임 최종 결정권을 갖게 함 — 죽은 코드를 안 건드리면서 문제만 해결.
      - **검증**: 실제 Electron 앱을 띄워 `FOCUSPET_DEBUG_FINAL` 임시 훅(스크린샷 캡처용, 검증 후 제거)으로 콘솔에 관련 에러 없이 정상 렌더링되는 것 확인(코드 경로가 실행되는지 확인하는 용도 — 실제 커서를 이 자동화된 캡처 흐름 안에서 움직여보는 것까지는 안 됨, 아래 "알려진 한계" 참고).
    - **말풍선이 왼쪽 끝에서 잘리는 버그 — 실제로는 클램프 자체가 없었음**: "전에 클램프를 넣었다"는 기억과 달리, `pet.js`/`pet.css` 전체를 검색해도 말풍선 위치에 클램프 로직은 전혀 없었음(있던 건 펫 자기 자신의 `x` 좌표를 창 안에 두는 `bounds()`뿐, 말풍선 전용 로직은 처음부터 없었음) — `#bubble`이 `#pet-wrap`(96px)의 자식으로 `left:50%`인 채 그 부모가 340px 스테이지의 어디에 있는지는 전혀 신경 안 쓰는 순수 CSS 중앙정렬이었던 것. 펫이 왼쪽 끝(`x=MARGIN=16`)에 있으면 펫 중심(`x+48=64`)을 기준으로 말풍선(최대 190px 너비, 중심에서 최대 95px)이 좌우로 뻗어나가는데, 64-95=-31이라 스테이지 왼쪽 경계(x=0)를 31px 넘어가 `overflow:hidden`에 잘림 — 정확히 리포트된 증상. **실제 재현**: `main.js`에 `FOCUSPET_DEBUG_BUBBLE` 임시 env 훅(검증 후 제거)을 추가해 실제 앱을 띄우고 진짜 알림을 띄운 뒤 `petWindow.webContents.capturePage()`로 실제 렌더링을 캡처 — 고치기 전 스크린샷(`test/BEFORE-AFTER-bubble-clamp-before.png`)에서 메시지 앞부분("물")이 실제로 잘려 나가 "...를 한 잔 마시고 와요"로 보이는 걸 확인, 가설이 아니라 실물로 재현함.
      - **고침**: `positionBubble()`을 새로 추가 — 펫 중심(`x+WRAP_WIDTH/2`)과 말풍선의 실제 레이아웃 폭(`bubbleEl.offsetWidth`, 메시지 길이에 따라 매번 다름 — `hidden` 클래스를 뗀 직후에 읽어야 정확한 값이 나옴)으로 말풍선이 허용 가능한 중심 범위(`[반폭+4px, 스테이지폭-반폭-4px]`)를 계산하고, 펫 중심이 그 범위를 벗어나면 딱 그만큼만 `bubbleEl.style.left`(인라인 스타일로 CSS의 `left:50%` 오버라이드)를 밀어서 화면 안에 붙잡아둠. 말풍선 꼬리(`#bubble::after`)는 그대로 두면 밀려난 말풍선의 새 중심을 가리키게 되므로, `--tail-shift` CSS 변수(`calc(50% + var(--tail-shift,0px))`)를 반대 부호로 걸어서 꼬리는 계속 펫의 실제 위치를 가리키게 보정. `showBubble()`에서 `hidden` 클래스를 뗀 직후, `show` 클래스를 붙이기 전에 호출.
      - **검증**: 고친 뒤 같은 훅으로 다시 캡처(`test/BEFORE-AFTER-bubble-clamp-after.png`) — 메시지 전체("허리 좀 펴볼까요?")가 잘림 없이 온전히 보이고 창 왼쪽 경계에서 적절한 여백을 두고 있음을 확인. 클램프 공식 자체도 순수 수치 스크립트로 좌/우 극단(x=16, x=228) 양쪽 다 재확인 — 양쪽 다 정확히 4px(`BUBBLE_SAFE_MARGIN`) 여유를 두고 딱 맞게 계산됨.
      - **왼쪽에 그림자 같은 아티팩트 — 사용자 가설(꼬리 동기화 실패)은 틀렸고, 진짜 원인은 여백 부족이었음**: 클램프는 잘 되는데 밀려날 때 왼쪽에 그림자처럼 보이는 게 남는다는 리포트 — 사용자는 "꼬리가 여전히 예전 CSS 위치 기준으로 그려지는 것 같다"고 추측. **먼저 그 가설부터 직접 확인**: `#bubble::after`의 `border-color`를 일시적으로 빨간색으로 바꾸고 실제 앱을 띄워 `capturePage()`+`nativeImage.crop()/resize()`로 꼬리 부분만 4배 확대 캡처 — 빨간 꼬리가 정확히 말풍선 박스 아래 중앙 근처(펫을 향해)에 있는 걸 확인, **꼬리 동기화는 이미 정상이었음**(지난 세션에 만든 `--tail-shift` 메커니즘이 실제로 잘 작동하고 있었다는 뜻 — 사용자의 가설을 그대로 믿고 꼬리 쪽만 고쳤다면 헛수고했을 뻔함). 흰 꼬리로 되돌리고 같은 위치를 다시 확대 캡처해보니, 꼬리와는 별개로 말풍선 박스의 **둥근 왼쪽 아래 모서리**에 회색 얼룩이 있는 걸 발견 — 이게 진짜 리포트된 아티팩트. 원인 규명: `#bubble`의 `box-shadow: 0 6px 16px rgba(0,0,0,0.18)`는 박스 경계선에서 blur-radius(16px)만큼 시각적으로 더 퍼져나가는데, 기존 `BUBBLE_SAFE_MARGIN=4`는 말풍선 **박스 자체**만 스테이지 안에 들어오게 여유를 뒀을 뿐 이 그림자의 퍼짐까지는 고려 안 함 — 펫이 왼쪽 끝에 있어 말풍선이 최대로 밀려나면 박스 왼쪽 경계가 스테이지 x=4에 딱 붙는데, 그림자는 거기서 최대 16px 더 왼쪽(x=-12)까지 퍼지려다가 스테이지의 `overflow:hidden`(x=0)에서 뚝 잘림 — 3면은 자연스럽게 흐려지는데 왼쪽 한 면만 칼로 자른 듯 뚝 끊기니 "그림자 같은 아티팩트"로 보였던 것.
      - **검증 (margin=4 vs 20 스크린샷 비교)**: `BUBBLE_SAFE_MARGIN`을 20(박스+그림자 blur-radius를 합쳐도 여유 있게 들어오는 값)으로 올리고 같은 위치를 다시 확대 캡처 — 회색 얼룩이 완전히 사라지고 모서리가 깨끗하게 둥글게 마감됨을 확인(`test/BEFORE-AFTER-bubble-shadow-fix-{before,after}.png`). 클램프 계산식 자체(펫 중심/말풍선 폭 기반 로직)는 안 건드리고 상수 하나(4→20)만 바꿈 — 좌우 극단에서 재계산해보면 말풍선 박스 경계가 스테이지 경계에서 20px 여유를 두게 됨(이전엔 4px). `positionBubble()`/`--tail-shift` 메커니즘 자체는 이미 정상이었으므로 그대로 유지.
    - **알림 소리를 부드럽게**: 기존 `playBeep()`은 880Hz 순수 사인파에 공격적인 exponential 엔벨로프(20ms 만에 피크, 그 뒤 급격히 감쇠)라 전자음처럼 쨍하게 들린다는 피드백 — 마림바/우드블록 같은 "도록" 소리를 요청받음. `pluckNote(ctxA, freq, startAt, duration, peak)`를 새로 만들어 기본음(사인) 하나에 4배음(조용한 배음, gain 0.18 — 실제 마림바 음판의 1차 배음이 기본음의 대략 4배 근처인 것에서 착안)을 살짝 섞고, 공격을 `linearRampToValueAtTime`으로 12ms에 걸쳐 부드럽게, 감쇠는 `exponentialRampToValueAtTime`으로 자연스럽게(320ms) — 급격한 attack/cutoff 대신 "튕기고 서서히 잦아드는" pluck 느낌을 냄. `playBeep()`은 이 `pluckNote`를 C5(523.25Hz)와 E5(659.25Hz, 0.13초 뒤) 두 번 연속으로 낮은음→높은음 순서로 호출해 "도록" 하는 두 음 리듬을 만듦. Web Audio API 특성상 Node에서 직접 소리를 들어보며 검증할 수는 없어서(마이크/스피커 없는 headless 환경) 엔벨로프 수식 자체(공격/감쇠 값이 유효 범위인지, `exponentialRampToValueAtTime`이 0이 아닌 양수 시작/끝값을 받는지 등)를 검토하고, 실제 Electron 앱을 띄워 알림을 실제로 트리거해 콘솔에 오디오 관련 에러 없이 재생 경로가 정상 실행되는 것까지 확인 — 소리 자체의 "느낌"은 사용자가 직접 들어보고 판단해야 함(알려진 한계 참고).
      - **더 묵직하게 — "아이폰 무음 스위치 소리 같다"는 후속 피드백**: 위 마림바 pluck이 여전히 가볍고 딸깍거리게 들린다는 피드백 — "손등으로 나무 상자를 두드리는" 묵직한 "도독" 소리를 요청받음. 원인으로 짚이는 두 가지를 손봄: (1) 음역대 자체가 너무 높았음(C5/E5, 523/659Hz는 작고 밝은 타악기 음역) — 기본음을 두 옥타브 가까이 낮춰 185Hz(F#3 근처)로. (2) 배음 구성이 저음 보강 없이 밝은 배음(4배음)만 있었음 — `pluckNote`를 `knockNote`로 교체하면서 배음을 4배에서 2배로 낮추고(더 둥글고 덜 밝게) gain도 낮춤(0.18→0.14), 그리고 **기본음의 절반 주파수(한 옥타브 아래)에 새 서브 오실레이터를 추가**(gain 0.55, 세 소스 중 가장 비중 있게) — "무게감"의 실체는 기본음을 낮추는 것보다 이 저음 보강 자체에서 나온다고 보고 추가함("배음을 더 낮은 쪽으로 옮기고... 저음 성분 비중을 높이는 식으로"라는 요청을 그대로 반영). 어택은 여전히 부드럽게(`linearRampToValueAtTime`, 12ms→18ms로 살짝 더 늘림), 디케이는 0.32초→0.5초로 늘려서 "치고 바로 끊기는" 느낌 대신 "치고 나서 여운이 남는" 느낌으로. 두 번의 노크도 기존엔 낮은음→높은음으로 올라가는 작은 멜로디(C5→E5)였는데, 이번엔 **완전히 같은 낮은 음(185Hz)을 두 번** — 사용자가 비유한 "두드리는" 동작 자체가 오르내리는 가락이 아니라 같은 강도로 반복되는 물리적 타격이라 판단해서, 멜로디 대신 반복 타격으로 성격을 바꿈.
      - **검증**: 이전과 마찬가지로 이 headless 환경에서 직접 들어볼 수는 없어서(알려진 한계 참고) 실제 Electron 앱을 띄워 알림을 트리거해 콘솔에 오디오 에러 없이 정상 실행되는 것까지 확인. 엔벨로프/주파수 값 자체도 코드 리뷰로 재검토(서브 오실레이터 gain 0.55가 기본음 대비 과하게 커서 클리핑 위험은 없는지 — `masterGain`의 피크 자체가 0.17로 이미 작아서 세 소스를 합쳐도 Web Audio의 정상 진폭 범위 안에 여유 있게 들어옴).
    - **클릭 통과 히트테스트**: `window`의 `mousemove`마다 좌표를 저장해뒀다가, 매 프레임(`tick()`의 `render()` 직후) `processPendingMouse()`가 한 번만 처리 — mousemove가 프레임보다 훨씬 자주 오므로 rAF 주기로 코얼레싱. canvas의 `getBoundingClientRect()`로 커서를 캔버스 백킹 픽셀 좌표로 환산하고(96x96 backing == 96x96 CSS라 배율 계산 불필요, `scaleX(-1)` 뒤집힘만 `canvas.width-1-x`로 보정) `ctx.getImageData(x,y,1,1).data[3]`로 그 지점의 실제 알파값을 읽어 임계치(128, 그림자의 ~46/255보다는 높고 스프라이트의 불투명 채움 255보다는 낮게)와 비교. 히트 여부가 바뀔 때만(`hitState`와 다를 때만) `focusPetAPI.setIgnoreMouseEvents(!hit, {forward:true})`를 호출해서 불필요한 IPC 왕복을 줄임. 바운딩박스 근사 대신 알파 샘플링을 택한 이유: 스프라이트가 매 프레임 자세가 바뀌는 작고 불규칙한 실루엣이라 고정 사각형으로는 정확도가 안 나오고, 1x1 `getImageData` 한 번은 mousemove당 비용으로 무시할 만함 (canvas는 `{willReadFrequently:true}`로 얻어서 반복 readback을 브라우저가 최적화하게 함). `main.js`가 창 생성 시 기본으로 `setIgnoreMouseEvents(true,{forward:true})`를 걸어두므로, 첫 mousemove가 오기 전까지는 안전하게 클릭 통과 상태. (예전엔 여기에 "기어 버튼 영역은 알파값과 무관하게 무조건 히트" 분기가 있었는데, 기어 버튼 자체가 없어져서 — 바로 아래 항목 — 지금은 전부 알파 샘플링 한 경로로만 판정함.)
    - **설정 버튼 제거 / 더블클릭으로 대체**: 펫 창의 톱니바퀴(⚙️) 버튼(`#gear`)을 완전히 삭제 — `index.html`에서 엘리먼트 자체를 뺐고, `pet.css`의 `#gear` 규칙과 `pet.js`의 관련 코드(클릭 리스너, 히트테스트 특례)도 같이 정리함. 설정을 여는 경로는 이제 두 가지: (1) 트레이 아이콘 메뉴의 "설정 열기"(기존 그대로, `main.js`의 `open-settings` IPC), (2) 펫을 더블클릭.
    - **더블클릭이 실제로는 안 열리던 버그**: 처음엔 `canvas`에 네이티브 `dblclick` 리스너를 달아뒀는데, 이 창이 `focusable:false`라 그런지 실제로는 잘 안 잡혔음(자동화 테스트에서 `dispatchEvent(new MouseEvent('dblclick'))`로 직접 합성 이벤트를 쏴서 확인했을 때는 통과했지만, 이건 브라우저의 실제 더블클릭 판정 로직 — 두 번의 real mousedown/mouseup을 시간差 보고 하나의 dblclick으로 묶어주는 것 — 을 건너뛰고 dblclick 이벤트 자체를 직접 만들어 쏜 것이었어서 가짜 통과였음). 네이티브 이벤트에 의존하지 않고 직접 타임스탬프를 재는 방식으로 교체 — 이미 있던 `click` 리스너(`poke()` 처리하는 곳)에서 `performance.now() - lastClickAt < 300`이면 더블클릭으로 판정해 `openSettings()`를 부르고 `poke()`는 건너뜀(리셋을 위해 `lastClickAt`을 0으로 되돌려서, 짧은 시간 안에 세 번째 클릭이 들어와도 그건 또 다른 더블클릭의 시작으로 안 잡히게 함). 테스트도 이제 실제 `click` 이벤트를 두 번 시간差 두고 보내는 식으로 재검증(합성 `dblclick` 한 방으로 넘어가지 않도록).
- **설정 창** (`windows/settings/`): 캐릭터 선택 그리드(4열 × 3행, 10종) — 각 칸의 작은 `<canvas>`에 동일 엔진으로 정지 포즈를 그려서 보여줌(정적 이미지 파일 아님, 실제 스프라이트 그대로). `.char-btn canvas`의 CSS `aspect-ratio`가 예전엔 그리드 비율(24x20 → 6:5)에 맞춰 하드코딩돼 있었는데, 그리드가 24x24(1:1)로 바뀌면서 이걸 안 고쳤으면 썸네일이 가로로 눌린 채 렌더링될 뻔했음 — `aspect-ratio: 1/1`로 같이 수정.

## 3D 복셀 공간 실험 (2025년 하반기부터 `main`에 병합됨 — 원래 `feature/3d-space`에서 실험되던 내용)
**갱신**: 아래 섹션은 원래 "이 내용은 `feature/3d-space` 브랜치에만 존재하고 `main`은 안정된 2D 버전 그대로 유지된다"는 전제로 쓰였음(각 항목에 남아있는 그 서술은 작성 당시엔 정확했던 기록이라 그대로 둠) — 이후 사용자가 3D 버전을 정식으로 채택하기로 결정해 `feature/3d-space`가 `main`으로 fast-forward 병합됨, 두 브랜치가 같은 커밋을 가리킴. **지금은 `main`이 곧 3D 버전**이고, 이 섹션에서 설명하는 voxel 렌더링(`windows/shared/voxel-engine.js`)이 `windows/pet/pet.js`가 실제로 쓰는 렌더러임 — 이 문서의 나머지 QA 라운드들(6차 이후 대부분)도 전부 이 3D 버전을 다루는 내용이고, 전부 `main` 기준으로 유효함.

**안전장치**: 이 브랜치를 만들기 전, 그동안(이번 대화 전체 + 이전 세션들) 쌓인 모든 픽셀아트 개선 작업이 실제로는 `main`에 커밋된 적이 없다는 걸 발견함(전부 uncommitted 상태로만 존재) — 이 상태로 바로 브랜치를 만들면 "언제든 main으로 돌아갈 수 있어야 한다"는 요청의 전제 자체가 깨짐(uncommitted 변경사항은 브랜치를 만들어도 그대로 따라오고, main은 아주 예전 초기 커밋 상태에 머물러 있어서 실제로 돌아갈 "안정된 main"이 없었음). 사용자에게 확인 후, 지금까지의 안정화된 2D 버전을 먼저 `main`에 커밋(`11afdc2`)한 다음, 그 지점에서 `feature/3d-space`를 분기함 — 이제 `git checkout main`이 실제로 안전한 폴백이 됨.

**접근 방식**: 예전 3D 시도(위 "왜 3D를 접고 픽셀아트로 돌아왔나" 항목 참고)가 실패한 이유는 "매끈한 로우폴리 지오메트리"가 96px 크기에서 실루엣/캐릭터 구분을 뭉갰기 때문이지 3D 좌표계 자체가 문제는 아니었다는 게 이번 실험의 전제 — 그래서 이번엔 지금의 2D 픽셀 그리드(SPECIES 테이블의 각 rect)를 마인크래프트 몹처럼 "각진 큐브로 압출(extrude)"하는 방식으로 시도. 매끈한 셰이딩/조명 없이 `MeshBasicMaterial`(빛에 전혀 반응 안 하고 각 면을 단색 그대로 렌더링)과 `antialias:false`로 지금 픽셀아트의 "평면 색상 + 딱딱 끊기는 경계" 느낌을 최대한 유지.

**기술 스택**: `three`(^0.185.1)를 `npm install --save`로 설치(이 프로젝트가 지금까지 지켜온 "dependencies 없음" 원칙에서 벗어나는 의도적 예외 — 사용자가 먼저 "Three.js를 다시 들여와야 할 것"이라고 인정한 부분). 번들러 없이 브라우저 네이티브 ES 모듈 + import map(`windows/pet3d/index.html`의 `<script type="importmap">`)으로 `three` bare specifier를 `node_modules/three/build/three.module.js`에 직접 매핑 — 이 프로젝트의 "빌드 스텝 없음" 관례를 그대로 유지.

**새 파일 (전부 프로토타입, 아직 실제 앱에 안 붙임)**:
- `windows/shared/animal-engine.js`: `SPECIES`를 새로 `export`(딱 이 한 줄만 기존 2D 엔진에 영향 — 기존 내부 사용법은 전혀 안 건드림, `test:invariants` 재실행해서 회귀 없음 확인). 3D 프로토타입이 종별 지오메트리/색상 데이터를 중복 정의하지 않고 기존 SPECIES 테이블을 그대로 재사용하기 위함 — 나중에 stage 3(전체 로스터 이식)를 하게 되면 데이터 소스가 이미 하나로 통일돼 있어 유리함.
- `windows/shared/voxel-engine.js`: `buildVoxelCreature(speciesKey)` — SPECIES 항목 하나를 읽어 기존 2D rect(body/head/snout/belly/pattern/eye/nose/ear/leg/tail) 각각을 `THREE.BoxGeometry` 하나로 압출한 `THREE.Group`을 반환. 다리/꼬리는 아직 관절 체인이 아니라 정적인 통짜 박스(옛 2D 엔진의 관절 체인 도입 이전 방식과 동일한 수준의 단순화) — 이번 프로토타입 범위가 "정적 실루엣 + 회전"까지이지 idle 행동 이식까지는 아니라서 의도적으로 최소화함.
- `windows/pet3d/`(`index.html`, `pet3d.js`): 프로토타입 전용 렌더링 창 — 실제 pet 창과 무관, `?species=`/`?spin=` 쿼리로 어떤 캐릭터를 정적으로 보여줄지 회전시킬지 결정. `window.focusPet3D`(`ready`/`frameCount`/`setAngle()`)를 노출해서 외부 테스트 스크립트가 폴링/제어 가능하게 함.
- `test/launch-voxel-proto.js`, `test/launch-voxel-sequence.js`, `test/launch-voxel-fps.js`: 실제 앱(main.js)과 완전히 분리된 독립 Electron 진입점(`npx electron test/launch-voxel-*.js ...`로 직접 실행) — `capturePage()`로 스크린샷을 뜨거나(정적 1장 / 각도별 시퀀스) `requestAnimationFrame` 프레임 카운트를 폴링해 실측 FPS를 잼. 아직 실험 단계라 실제 앱 코드(main.js/pet.js)는 전혀 안 건드림.

**1단계 검증 (정적 실루엣, cat_a + dog_husky)**: 결과가 예상보다 훨씬 좋았음 — 옆모습 정적 렌더링을 기존 2D 스크린샷과 나란히 비교(`test/voxel-{cat_a,dog_husky}-static.png` vs `test/voxel-compare-2d-{cat_a,dog_husky}.png`)했을 때, 색상/줄무늬 패턴/비율/종 구분 요소(귀 위치, headColor 차별화, 파란 눈 등)가 전부 놀라울 만큼 그대로 유지됨 — 매끈한 지오메트리가 아니라 각진 큐브라서 실루엣이 뭉개지지 않는다는 가설이 실제로 확인됨. (다리는 아직 앞/뒤 각 1개씩만 있어 옛 2D 엔진의 관절 체인 도입 이전 모습과 비슷하고, 그림자·눈 하이라이트 등 디테일은 아직 없음 — 예상된 단순화, 문제로 보지 않음.)
  - **첫 렌더링에서 발견한 버그 (프레이밍 잘림)**: 처음엔 머리/귀/눈 부분이 카메라 프레임 오른쪽 밖으로 잘려 나옴 — 원인은 `group.position.x`를 몸통(`bodyX+bodyW/2`)만 기준으로 재배치했는데, 머리(`headX`)가 몸통 중심보다 더 오른쪽으로 뻗어있어서(이 로스터의 모든 사족보행 종이 그러함) 재배치 기준이 실제 전신(코끝~꼬리끝) 중심과 어긋났던 것. `THREE.Box3().setFromObject()`로 전체 바운딩박스를 구해서 그 중심을 기준으로 재배치하도록 고침.
**2단계 검증 (회전)**: 두 가지를 확인 — (1) 회전이 실제로 부드러운지(모션 자체), (2) 회전 중 다른 각도에서도 실루엣이 여전히 알아볼 만한지(생김새).
  - **회전 피벗 버그 (스크린샷으로 발견)**: 바운딩박스 중심으로 `group.position`을 옮기는 위 수정을 그대로 회전에도 써도 될 줄 알았는데, 실제로 90°/180°/270° 시퀀스를 찍어보니 캐릭터가 화면 밖으로 거의 다 나가버림 — Three.js의 `group.rotation`은 항상 그 그룹 자신의 로컬 원점(0,0,0)을 기준으로 회전하는데, `position`을 옮기는 건 "그 그룹이 월드 공간 어디에 나타나는가"만 바꿀 뿐 "그 그룹 내부에서 회전축이 어디인가"는 전혀 안 바꿈 — 즉 바운딩박스 중심이 회전축이 되도록 하려면 자식(박스들)의 로컬 좌표 자체를 옮겨야 함. **고침**: 안쪽 `inner` 그룹에 모든 박스를 넣고 `inner.position.x = -center.x`로 자식들을 재배치한 뒤, 그 `inner`를 감싸는 바깥쪽 `outer` 그룹을 반환하도록 구조 변경 — 이제 `outer.rotation.y`가 정확히 바운딩박스 중심을 축으로 돎. 재검증 결과 8단계(0~315°, 45° 간격) 전부 프레임 안에 안정적으로 유지됨.
  - **회전 중 발견한 디테일 버그 (덤으로 고침)**: 눈/코/배 무늬/줄무늬를 몸통·머리 표면에 살짝 띄우려던 코드가 실제로는 `zOffset` 인자가 아니라 `depth` 인자에 값을 넣고 있어서, "표면 위로 살짝 나옴"이 아니라 "그 박스 자체가 조금 더 두꺼워짐"으로 동작하고 있었음(정면에서 보면 결과가 비슷해서 안 드러났지만, 회전 시 시야각이 바뀌면 차이가 남) — 회전 피벗을 고치는 김에 `zOffset`을 제대로 된 위치(표면 depth의 절반 + 약간의 여유)에 전달하도록 정정.
  - **실측 FPS (스크린샷만으론 "부드러운지" 알 수 없어서 수치로 확인)**: `test/launch-voxel-fps.js`로 `requestAnimationFrame` 프레임 카운트를 3초간 폴링 — 처음엔 `show:false`(숨김) 창에서 측정했더니 1.3fps라는 비정상적으로 낮은 값이 나왔는데, `backgroundThrottling:false`를 줘도 그대로였음 — 원인을 더 파보니 Chromium이 "한 번도 실제로 표시된 적 없는" 창 자체를 컴포지팅 안 해서 rAF 콜백 빈도 자체가 억제되는 것으로 보임(`backgroundThrottling`은 백그라운드로 밀린 "이미 보이던" 창에 적용되는 것이지, 애초에 안 보인 창엔 다른 메커니즘이 적용되는 듯). `show:true`로 바꿔서 재측정하니 60.1fps로 정상 — 실제 pet 창은 항상 보이는 상태이므로 이 값이 실제 상황을 정확히 반영함. **결론: 회전 자체는 문제없이 부드러움(60fps).**
  - **각도별 실루엣 가독성 (정직한 한계 발견)**: 0°/45°는 옆모습과 거의 동일하게 잘 읽히지만, 90°(정면·후면 뷰)에서는 실루엣이 상당히 밋밋한 사각 기둥으로 무너짐(`test/voxel-seq-{cat_a,dog_husky}-090.png`) — 원인은 몸통 폭(13~19유닛)에 비해 depth를 균일하게 6유닛(대략 몸높이 수준)으로만 줬기 때문에, 옆에서 볼 땐 몸높이×몸길이 실루엣이 나오지만 정면에서 볼 땐 몸높이×depth(둘 다 비슷한 6유닛대)만 보여서 "그 종만의 특징"이 거의 안 남음(귀도 머리 폭 방향으로 나란히 배치돼 있어서 90° 회전하면 두 귀가 앞뒤로 겹쳐 보임). 이건 버그라기보다 "옆모습 실루엣을 그대로 압출"하는 이번 접근 자체의 구조적 트레이드오프 — 실제 마인크래프트 몹도 얼굴 방향 텍스처가 따로 있어서 정면에서도 특징이 보이는 것이지, 단색 큐브 압출만으로는 정면 디테일이 거저 생기지 않음. **회전 애니메이션의 "실용적" 함의**: 제자리에서 완전히 한 바퀴 도는 걸 계속 보여주기보다는, 옆모습 근처 각도(예: ±30~45° 이내)로 살짝만 갸웃거리듯 흔드는 정도라면 이 트레이드오프가 거의 안 드러남 — 완전한 360도 스핀을 자주 보여줄 계획이라면 부위별 depth를 종별로 손튜닝하거나 정면 전용 디테일을 추가하는 작업이 필요함.

**종합 판단 (사용자에게 보고, 자동으로 다음 단계 진행 안 함)**: 1단계(정적 실루엣)는 가설을 명확히 입증 — 각진 큐브 압출이 실루엣/색 구분을 그대로 보존함. 2단계(회전)는 모션 자체는 확실히 합격(60fps, 피벗 고침 후 프레임 안에 안정적으로 유지)이지만, "완전 정면/후면에서도 잘 보이는지"는 이번 균일 depth 방식만으로는 한계가 있음을 발견 — 이게 실사용에 문제가 되는지(스핀을 자주/크게 보여줄 건지, 아니면 살짝 갸웃거리는 정도로 충분한지)는 제품 판단의 영역이라 사용자 결정이 필요. 3단계(전체 로스터 + 관절/idle 시스템 이식)는 이 판단이 선 뒤에 진행하기로 함 — 지금 시점에 바로 진행하지 않음.

### 정면/후면 디테일 추가 (완전 360도 회전으로 가기로 결정한 후속 작업)
사용자가 위 판단에서 "완전 360도 회전"으로 가기로 결정 — 그럼 정면/후면에서 밋밋한 덩어리로 보이던 문제부터 해결해야 stage 3(전체 로스터+관절/idle 이식)로 넘어갈 수 있음. `voxel-engine.js`를 전면 재작성.

**핵심 설계 아이디어 — Z축(깊이)을 좌우(lateral) 대칭축으로 재해석**: 기존엔 Z축이 그냥 "옆에서 봤을 때의 두께"였을 뿐이라 눈/귀/다리처럼 원래 좌우 한 쌍인 부위가 애초에 "하나"로만 존재했음(옆모습 스프라이트엔 원래도 한쪽만 보이니까). 이번 핵심 아이디어: 눈/귀/다리처럼 좌우로 쌍을 이루는 부위는 Z=0을 기준으로 **거울 대칭인 두 개의 박스**로 만듦 — 0°/180°(옆모습)에서 보면 두 박스가 화면상 같은 위치에 겹쳐 보여서(가까운 쪽만 보이고 먼 쪽은 몸통에 가려짐) 기존 2D 스프라이트와 똑같이 "한쪽만 보임"이 재현되고, 90°/270°(정면 근처)로 회전하면 Z축이 화면의 새로운 좌우 축이 되면서 두 박스가 자연스럽게 좌우로 벌어져 보임 — 카메라나 로직을 따로 안 만들어도 회전만으로 "옆에서는 하나, 정면에서는 둘"이 저절로 나옴. 코/주둥이/꼬리처럼 원래 하나뿐인 정중선 부위는 그대로 Z=0에 중앙 고정.

**부위별 처리**:
- **눈 (`eye`)**: `(eye.x, eye.y)`는 2D 데이터 그대로 쓰고, Z=±`EYE_Z`로 거울 배치. **처음에 낸 버그**: `EYE_Z`를 머리 자체의 반두께(`HALF_DEPTH`=3)보다 **작게**(2.46) 잡았더니 눈 박스가 머리 박스 속에 통째로 파묻혀서 **어느 각도에서도 안 보이는** 상태가 됐었음(z-fighting이 아니라 아예 다른 불투명 박스 속에 완전히 갇힌 것) — 렌더링해보고서야 발견(0°에서도 눈이 사라져 있었음). `EYE_Z`가 머리의 반두께를 **넘어서야**(3.35, `HALF_DEPTH+0.35`) 머리 표면 밖으로 살짝 튀어나와 어느 각도에서든 보임 — 이 "표면보다 얕으면 숨는다"는 원리를 그제서야 명확히 정리하고 나머지 부위에도 같은 기준을 적용함.
- **귀 (`ear`)**: 마찬가지로 Z=±`EAR_Z`로 거울 배치, X는 머리 폭 중앙 한 곳으로 통일(기존 2D의 `ear.gap`은 옆모습에서 "앞귀/뒤귀"를 흉내내던 원근 트릭이었는데, 진짜 좌우 대칭 정면 뷰에는 그대로 못 씀 — 버려짐). 귀는 머리보다 위(다른 Y 구간)에 있어서 머리 표면을 뚫고 나올 필요가 없으므로(그 높이엔 가릴 다른 박스가 없음) `EAR_Z`는 `HALF_DEPTH`보다 작아도(0.7배) 문제없이 다 보임 — 눈과 다른 제약.
- **코/주둥이 (`nose`/`snout`)**: 정중선(Z=0) 단일 구조 유지, 대신 머리 앞쪽으로 튀어나오게(+X 방향) 살짝 밀어냄. **처음에 낸 버그**: 코와 주둥이 둘 다 거의 같은 X 근처에 있어서 코를 살짝만(+0.1) 밀어냈더니 주둥이 앞면과 코가 사실상 같은 위치에 겹쳐 z-fighting 여지가 있었음 — 코 위치를 "주둥이가 실제로 계산한 앞쪽 끝(snoutFrontX)"보다 확실히 더 앞으로 고정하도록 고침.
- **다리 (`legFrontX`/`legBackX`)**: 앞다리·뒷다리 각각 Z=±`LEG_Z`(머리보다 훨씬 안쪽 — 실제 동물도 다리가 몸통 맨 가장자리에 안 붙어있음)로 거울 배치, 4개 다리 전부 표현됨. 사용자가 명시한 "다리 4개가 정면에서 좌우로 벌어져 보이게"를 그대로 구현.
- **배/무늬 (`belly`/`pattern`)**: 기존엔 몸통 표면 한쪽에만 살짝 튀어나온 얇은 데칼이라 그 튀어나온 방향(한쪽 면)에서만 보였음 — 이번엔 몸통 깊이의 대부분(94%)을 차지하는 폭으로 넓히고, X/Z 방향으로 아주 살짝(`SKIN`=0.15) 부풀려서 몸통을 감싸듯 어느 각도에서 봐도 몸통보다 겉에 있게 함(한 방향으로만 띄우던 예전 `zOffset` 트릭은 여러 각도에 안 맞아서 폐기).

**검증 (cat_a, dog_husky 둘 다 0°/45°/90°/135°/180°/270° 스크린샷 비교)**: 두 종 모두 예상대로 나옴 —
- 0°/45°/135°/180°: 옆모습이 기존 2D와 거의 동일하게 유지됨(180°는 "반대쪽 옆모습"이라 눈/귀가 똑같이 하나씩 보이는 게 맞음 — 실제 동물도 좌우 어느 쪽에서 봐도 눈 하나, 귀 하나씩 보이는 게 정상이므로 버그 아님).
- 90°/270°(회전축 관계상 실제로는 90°가 "머리 뒷면 느낌"/270°가 "정면 느낌"으로 나뉨, 정확히 카메라 정반대인 지점 두 곳): 눈 2개·귀 2개가 명확히 좌우 대칭으로 벌어져 보임, 270°(주둥이가 카메라를 향하는 쪽)에서는 주둥이/코까지 중앙에 제대로 자리잡아 진짜 "정면 얼굴"처럼 보임 — cat_a는 크림색 주둥이+어두운 코, dog_husky는 흰 주둥이+검정 코+파란 눈 2개가 전부 올바르게 나타남(`test/voxel-seq-{cat_a,dog_husky}-{000,045,090,135,180,270}.png`).
- **다리 좌우 분리 확인**: 96px 렌더링 자체로는 다리 간격이 좁아 잘 안 보여서, `test/launch-voxel-crop.js`(새 스크래치성 도구 — capturePage 후 `nativeImage.crop()/resize()`로 특정 영역만 확대, 이전 세션의 말풍선 그림자 조사에서 썼던 기법과 동일)로 다리 부분만 3배 확대 캡처(`test/voxel-legcrop-{cat_a,dog_husky}-270.png`) — 두 종 모두 다리 두 개가 뚜렷한 간격을 두고 갈라져 있는 게 확인됨(앞다리 쌍/뒷다리 쌍 중 카메라에 가까운 한 쌍만 보이고 나머지 한 쌍은 뒤에 가려짐 — 실제로 정면에서 봐도 뒷다리는 몸통에 가려 잘 안 보이는 것과 같은 이치라 자연스러움).

**결론**: 이번 라운드 목표(정면/후면 디테일)는 cat_a·dog_husky 둘 다에서 확인됨 — stage 3(나머지 8종 + 관절/idle 시스템 이식)로 진행.

### 나머지 8종 지오메트리 커버리지 확인 (stage 3의 첫 부분)
cat_a·dog_husky 2종에서 확인된 위 설계를 나머지 8종(cat_tuxedo, cat_calico, cat_siamese, dog_dachshund, dog_corgi, dog_pomeranian, rabbit_b, hamster)에도 그대로 적용해 `test/launch-voxel-batch.js`(새 스크래치 도구 — 한 BrowserWindow를 재사용하며 여러 종을 0°/270°로 순차 렌더링)로 렌더링. `hamster`의 `cheeks`(볼주머니, 다른 종엔 없는 필드)는 눈과 동일한 거울 배치+`EYE_Z` 기준을 그대로 적용해 `voxel-engine.js`에 새로 추가.

**캡처 자체가 가끔 오래된 프레임을 반환하는 버그 (배치 렌더링 중 발견)**: 첫 배치 실행 결과 10종 중 3종(cat_tuxedo, dog_dachshund, dog_pomeranian — 재실행 시 다른 3종으로 바뀜, 즉 종과 무관한 우연)에서 0°/270° 스크린샷이 **바이트 단위로 완전히 동일**하게 나옴 — `setAngle()`이 실패한 줄 알았지만, `document.getElementById('stage').toDataURL()`로 직접 캔버스 내용을 찍어보니 회전이 실제로는 정상 반영되고 있었음(캔버스 자체는 매번 다름). 즉 WebGL 렌더링(`renderer.render()`, 동기 호출)은 즉시 갱신되는데, `capturePage()`가 읽는 OS 컴포지터 쪽 프레임이 그보다 뒤처져서 가끔 직전 프레임을 반환한 것 — 위 2단계에서 발견한 "`show:false` 창은 실제 화면 갱신과 무관하게 rAF/컴포지팅 자체가 억제된다"는 것과 같은 근본 원인의 다른 증상. `show:true`로만 바꿔도 매 실행마다 실패하는 종이 달라지는(비결정적) 걸 재확인해서 그것만으론 불충분하다고 판단, `setAngle()` 호출 후 `capturePage()` 전에 120ms 여유를 주는 것으로 실제 해결(재실행 결과 10종 전부 0°/270°가 서로 다르게 나옴을 확인). `test/launch-voxel-batch.js`/`launch-voxel-sequence.js` 둘 다 이 패턴(`show:true` + 캡처 전 딜레이)으로 고쳐둠 — 앞으로 이 창에서 `capturePage()`를 새로 쓰는 스크립트는 전부 이 패턴을 따라야 함.

**진짜 버그 발견: belly/pattern/snout(얼굴 마킹)이 사실 전 종에서 안 보이고 있었음**: 위 배치 렌더링을 실제로 눈으로 확인하던 중 cat_tuxedo의 얼굴 블레이즈가 완전히 안 보이는 걸 발견(검은 머리 그대로, 흰 블레이즈 없음) — 조사해보니 `addSkinBox`가 만드는 배/무늬 데칼의 Z 깊이(`depth*0.94+SKIN`)가 그 데칼이 얹히는 몸통/머리 자체의 깊이(`DEPTH`, 같은 Z 중심)보다 **더 얕아서**, 데칼 전체가 몸통/머리라는 불투명한 박스 속에 통째로 파묻혀 **어느 각도에서도 안 보이는** 상태였음 — 위 "눈(`EYE_Z`)이 머리 반두께보다 얕으면 숨는다"던 것과 정확히 같은 종류의 버그를 배/무늬 쪽엔 아직 안 고친 채로 두고 있었던 것. cat_a의 태비 줄무늬·배 무늬, dog_husky의 가슴 무늬 등 다른 종에서도 전부 같은 이유로 안 보이고 있었는데, 1단계/2단계 검증 스크린샷에서는 (a) 애초에 "실루엣과 색이 유지되는가"만 눈여겨봤고 무늬 자체의 유무는 세심히 안 봤고 (b) 배/무늬 X 폭이 몸통 가장자리와 우연히 겹치는 부분에서 아주 얇게 삐져나온 조각(SKIN의 X방향 팽창분)만 보여서 "그런가 보다" 하고 넘어갔던 것으로 보임 — 이번에 이 라운드의 요구사항("몸통 무늬가 정면에서도 자연스럽게 이어지게")을 검증하려고 실제로 자세히 들여다보다가 처음 발견함. `cat_tuxedo`의 `snout` 배열(블레이즈용, 얼굴 마킹이라 머리 앞으로 튀어나오는 진짜 주둥이와 달리 머리와 같은 X 범위에 위치)도 같은 depth 계산(`DEPTH*0.75`, 역시 머리 depth보다 얕음)을 쓰고 있어서 완전히 파묻혀 있었음.

**고침**: `addSkinBox`가 이제 부위별로 다른 얕은 depth를 받는 대신, 항상 그 밑에 깔린 몸통/머리의 전체 `DEPTH`를 SKIN만큼 **넘어서는** 고정 깊이(`DEPTH + SKIN*2`, Z 중심 0)를 쓰도록 통일 — 눈/치크와 같은 원리("표면을 덮는 밑변 depth보다 얕으면 숨는다")를 배/무늬/얼굴마킹에도 그대로 적용. `snout`도 `addBox`(고정 얕은 depth) 대신 `addSkinBox`를 쓰도록 변경 — 머리 앞으로 튀어나오는 진짜 주둥이(cat_a 등, 이미 X로 머리를 벗어나 있어 원래도 안 파묻혀 있었음)는 이 변경으로 결과가 달라지지 않고, 얼굴에 얹히는 평평한 마킹(cat_tuxedo 블레이즈)만 비로소 보이게 됨. 부수 효과로 이 수정이 "배/무늬가 정면에서도 자연스럽게 이어져야 한다"는 이번 라운드 요구사항도 그대로 만족시킴 — Z로 완전히 감싸듯 튀어나오게 만들었으니 옆에서도, 정면에서도 끊기지 않고 보임.

**재검증 (10종 전부, 0°/270°, `test/voxel-batch-*.png`)**: 고친 뒤 cat_a부터 다시 렌더링 — 태비 줄무늬·배 무늬가 몸통에 뚜렷이 나타나고 정면에서도 가슴 쪽에 무늬 띠가 이어져 보임. cat_tuxedo는 흰 블레이즈(얼굴)와 넓은 흰 가슴이 드디어 나타나 "검은 고양이+흰 블레이즈/가슴+분홍 코+초록눈"이라는 원래 디자인 의도가 옆모습/정면 둘 다에서 확인됨. 나머지 8종도 각 종 고유 필드까지 포함해 전부 확인:
- cat_calico: 좌우 다른 색 귀(`ear.color2`, 검정/주황)가 정면에서 뚜렷이 분리돼 보임, 몸통의 검정+주황 패치도 보임.
- cat_siamese: `headColor`(포인트 컬러) 얼굴만 진한 seal-brown, 몸통은 크림색으로 분리돼 보임, 파란 눈 2개 정면에서 확인.
- dog_dachshund: 극단적으로 길고 낮은 몸통 + 배 무늬 띠 확인, 진한 갈색 눈 정면 확인.
- dog_corgi: `hasTail:false`가 정확히 꼬리 없음으로 렌더링됨(다른 강아지들과 나란히 비교해 꼬리 박스 자체가 없다는 것 확인), 가슴/배 무늬 확인.
- dog_pomeranian: 통통한 몸통 비율 + 무늬 확인.
- rabbit_b: `earStyle:'long'`(다른 종보다 훨씬 긴 귀 박스)이 정면에서 좌우로 벌어져 보임, 분홍 주둥이 확인.
- hamster: 새로 추가한 `cheeks`(볼주머니)가 정면에서 얼굴 양옆에 뚜렷한 크림색 패치로 보임, `hasTail:false`/`snout:null` 둘 다 올바르게 반영(주둥이 없이 바로 코).
`test/voxel-seq-{cat_a,dog_husky}-{000,045,090,135,180,225,270,315}.png`(수정된 엔진으로 재생성)로 8단계 전체 회전에서도 포핑/깨짐 없이 매끄럽게 이어짐을 재확인.

**결론**: stage 3의 지오메트리 커버리지 부분(10종 전부 + 정면/후면 디테일)은 완료 및 확인됨. stage 3의 나머지 절반(관절 체인/idle 행동 시스템을 3D로 이식하는 것 — 다리가 실제로 굽고, sit/sleep/groom 등 idle이 재생되는 것)은 아직 손대지 않은 훨씬 큰 별도 작업 — 2D 엔진은 매 프레임 `legJointAngles()`로 계산한 각도를 `pxJoint`로 그리는 방식인데, 3D에서 이에 대응하는 방식(박스 체인을 실제로 회전시킬지, 다른 접근을 쓸지)은 아직 설계하지 않음.

### 관절/idle 시스템 3D 이식 — 다리 관절 소규모 프로토타입 (cat_a)
stage 3의 나머지 절반(관절/idle 이식)을 어떻게 진행할지 사용자에게 물었더니 "알아서 판단"으로 위임 — 지금까지 이 브랜치 전체가 따라온 "작게 프로토타입 → 확인 → 확장" 패턴 그대로, 다리 관절 하나부터(idle 포즈 전환이나 꼬리 웨이브보다 먼저) cat_a 1종으로 작게 시작하기로 함.

**설계**: 2D의 `legJointAngles(cfg, pose, isFront)`(hip/knee 각도 계산 — pxJoint로 그리기 직전 단계)를 `animal-engine.js`에서 새로 `export`(기존 `SPECIES` export와 같은 성격 — 내부 사용법은 그대로, 순수 함수라 재사용에 안전)해서 3D에서 그대로 재사용. 2D는 본(bone) 하나하나를 매번 절대 각도로 독립적으로 그리는 방식(`pxJoint`, "본이 몇 개 안 되니 트랜스폼을 누적하는 것보다 절대 좌표가 더 단순하다"는 이유)인데, 3D에서는 `THREE.Group` 중첩(hip 피벗 그룹 → 그 자식으로 knee 피벗 그룹 → 그 자식으로 종아리 박스)으로 같은 걸 구현 — 무릎이 고관절의 로컬 좌표계 안에 실제로 자식으로 붙어있으므로 "고관절이 어떻게 돌든 무릎이 같이 따라간다"가 삼각함수 계산의 정확성에 기대는 게 아니라 구조적으로 보장됨(2D 쪽은 매 프레임 삼각함수로 직접 계산하다 보니 원리적으로는 버그가 나면 본이 떨어져 보일 수 있는 반면, 3D 중첩 그룹은 그 종류의 버그 자체가 발생할 수 없음).

**회전축/부호**: 다리의 앞뒤 스윙은 X(전후)-Y(상하) 평면에서 일어나므로 회전축은 Z(이번에 새로 추가된 좌우축) — `pxJoint`의 각도 컨벤션(0°=아래로 곧장, 양수=-X/꼬리 쪽으로 스윙)을 회전행렬로 직접 유도해본 결과 Three.js의 `rotation.z = +각도`는 반대 방향(+X 쪽)으로 스윙하는 것으로 나와서, `rotation.z = -각도`(hip/knee 둘 다)로 부호를 뒤집어야 2D와 동일한 스윙 방향이 나옴 — 무릎 회전은 고관절 그룹의 자식이므로 부모 회전 위에 자연히 누적되어(둘 다 같은 Z축 회전이라 각도가 그대로 더해짐) 2D의 `lowerAngle = hipAngle + kneeAngle`(절대각) 계산과 정확히 같은 결과가 됨 — 별도로 두 각도를 합산하는 코드 없이 그냥 계층 구조만으로 됨.

**구현**: `voxel-engine.js`에 `addLegChain(parent, cfg, isFront, zSide)`(hip/knee 피벗 그룹 + 위/아래 다리 박스 생성, `pawColor` 있으면 발끝 색 박스도 겹쳐 추가 — 2D의 `pawFraction` 캡 방식과 동일한 원리) + `applyLegPose(legs, cfg, pose)`(pose 객체 하나 받아서 `legJointAngles`로 계산한 각도를 4쌍의 피벗에 실제로 적용) 추가. `buildVoxelCreature`가 반환하는 그룹에 `.userData.legs`/`.userData.cfg`를 실어서 외부(테스트 하네스)가 `applyLegPose`를 호출할 수 있게 함. 기존 정적 다리 박스(구 프로토타입, 관절 없음)는 완전히 이 관절 체인으로 교체 — 하위 호환 코드 없음.

**검증 (cat_a, `test/launch-voxel-legtest.js`)**: 눈으로만 판단하지 않고 두 단계로 확인 —
1. **수치 확인**: `pet3d.js`에 임시가 아니라 재사용 가능한 디버그 접근자 `getLegDebug()`를 추가해 실제 적용된 피벗 회전값을 도(degree)로 읽어옴 — `legPhase:0.85` → `front:{hip:-15.3,knee:-2.1}`처럼 2D의 `legJointAngles`가 손계산으로 내놓는 값(`phase*swingAmp`, `(1-|phase|)*kneeSwingAmp` 등)과 부호까지 정확히 일치함을 확인(`legsTuckedFront/Back:1`→ 무릎 -125°/-135°까지 극단적으로 접히는 것도 `tuckHip/tuckKnee` 설정값과 일치). 회전이 매 pose마다 실제로 갱신되고 있다는 것 자체가 이 수치로 이미 증명됨.
2. **시각 확인**: 96px 렌더는 다리 세그먼트 자체가 작아서(cat_a 총 다리 길이 ~3그리드유닛) 스크린샷을 그냥 눈으로 봐서는 stand/swing/tuck 포즈가 거의 똑같아 보임 — 이전 턴들의 "육안 판단이 몇 번 틀렸다"는 교훈대로 곧이곧대로 "괜찮아 보인다"고 넘기지 않고, (a) 픽셀 diff 스크립트로 포즈 간 실제로 몇 픽셀이 달라지는지 정량화(stand vs sit-tuck 1533px, stand vs swing-fwd 602px 등 — 전부 0이 아님, 다리 영역에 국한된 bbox) (b) `nativeImage.crop()+resize()`로 다리 부분만 3배 확대해서 재확인 — 확대해서 보니 발 부분의 실루엣이 포즈마다 계단형으로 다르게 잘려나가는 게(회전된 사각형이 픽셀 그리드에 투영되는 방식이 각도마다 달라짐) 뚜렷이 보여서 실제 회전이 일어나고 있음을 눈으로도 재확인. **tuck(-125°~-140°) 시 다리가 몸통 뒤로 거의 다 가려져서 겉보기 변화가 작은 것**도 버그가 아니라 의도한 결과로 확인함 — 그 각도면 다리가 거의 위/뒤(꼬리 쪽)를 향하게 되어 원래도 몸통 박스 뒤에 가려지는 위치로 이동하는 게 2D의 "다리가 몸통 밑으로 숨어들어간다"는 tuck 컨벤션과 같은 방향의 결과.

**결론**: 다리 관절 체인 메커니즘 자체는 cat_a에서 확인됨(각도 계산 재사용 정확, 구조적으로 이탈 불가능, 회전이 실제로 그려짐). 아직 안 한 것: (1) 나머지 9종으로 확장 — 데이터 기반이라 종별 특수 코드는 필요 없을 걸로 예상되지만 미검증, (2) 걷기/tuck 전환 애니메이션(현재는 정지된 pose 스냅샷만 테스트 — 시간에 따라 부드럽게 보간되는지는 미검증), (3) 꼬리 웨이브·머리/목·귀 flick 등 다리 외 나머지 idle 시스템 전부, (4) 실제 pet 창(main.js/pet.js)에 이 3D 엔진을 연결하는 것 자체(지금도 여전히 `windows/pet3d/`라는 독립 테스트 창에서만 동작).

**voxel-viewer로 사용자 직접 확인 후 계속 진행 결정** — 사용자가 `test/serve-voxel-viewer.js`+`test/voxel-viewer.html`로 직접 회전/다리 관절을 눈으로 확인하고 만족, 나머지 9종 관절 확장 + idle/꼬리/머리 시스템 포팅 + 실제 pet 창 연결까지 끝까지 진행하기로 함(중간중간 체크인하되 자동으로 계속 진행).

### 다리 관절 나머지 9종 확장 (검증만, 코드 변경 없음)
`addLegChain`/`applyLegPose`는 애초에 종별 특수 분기가 전혀 없는 데이터 기반 구현이라(cfg.joints.front/back, cfg.legFrontX/Y/W 등을 그대로 읽음), "나머지 9종으로 확장"은 실제로는 코드를 더 안 짜고 전 종에 대해 검증만 하면 되는 작업이었음. `test/launch-voxel-legtest.js`를 인자 없이 실행하면 10종 전부를 돌리도록 확장해서 재실행 — stand/swing-fwd/swing-back/sit-tuck/dangle/bellyup 6개 포즈 × 10종 = 60프레임을 `getLegDebug()`로 수치까지 함께 기록. 결과: 전 종의 hip/knee 각도가 SPECIES 테이블에 손으로 적어둔 `tuckHip`/`tuckKnee`/`swingAmp`/`kneeSwingAmp` 값과 정확히 일치(예: 닥스훈트 tuckHip:50/70(front)·55/80(back), 코기 45/65·50/75, 포메라니안 40/100·45/110, 토끼 25/35·95/130 — 전부 SPECIES 테이블 원본값과 소수점까지 일치). 닥스훈트(극단적으로 길고 낮은 몸통+짧은 다리)·코기(매우 짧은 다리)·햄스터(가장 짧은 관절 길이)처럼 극단적인 비율의 종도 스크린샷상 깨짐/분리 없음 확인. 결론: 다리 관절 시스템은 10종 전부에 그대로 적용됨, 추가 튜닝 불필요.

### 몸통/머리/꼬리/idle 시스템 3D 포팅 (stage 2/3 핵심 작업)
**핵심 아키텍처 결정**: `createAnimal()`(idle 행동 풀 추첨, 상태머신, 커서 추적, 포즈 크로스페이드 전부 포함)이 순수하게 캔버스와 무관한 데이터 계산 계층이라는 걸 전체 소스를 다시 읽으며 확인 — 반환 객체의 `draw(ctx)` 메서드 단 하나만 2D 전용이고, `update(dt, allowedToMove, elapsed)`/`poke()`/`forceSleep()`/`wakeUp()`/`setHeld()`/`setCursorHint()`는 전부 `anim` 내부 상태(순수 객체)만 다룸. 즉 **idle 시스템 자체를 3D용으로 다시 만들 필요가 전혀 없음** — `createAnimal()`을 그대로 재사용하고, 매 프레임 계산되는 `pose` 객체를 3D 트랜스폼으로 바꿔주는 "3D판 `drawCreature`"만 새로 만들면 됨. 이를 위해 `animal-engine.js`에 `getPose()`를 추가(기존 `SPECIES`/`legJointAngles` export와 같은 성격 — 순수 추가, `anim.pose`의 라이브 참조를 반환. `inspect()`는 매번 새 객체를 복사해서 반환하므로 매 프레임 호출하기엔 부적합해 별도로 뺌). 추가 후 `test:invariants`(120만 프레임) 재실행 — 위반 0건, 2D 쪽 회귀 없음 확인.

**`voxel-engine.js` 전면 재작성 — 계층 구조**: 2D의 `drawCreature`가 매 프레임 각 파츠 좌표를 수식으로 다시 계산해 그리는 것과 달리, 3D는 씬그래프(부모-자식 트랜스폼 합성)를 활용해 "2D가 손으로 계산하던 관계"를 구조적으로 대신 표현하도록 설계함:
- `outer`(뷰 회전, Y축) → `rollPivot`(rollAngle+waddleTilt, Z축 회전, 2D의 `ctx.rotate()`와 동일한 `(cx,cyWorld)` 피벗) → `bobGroup`(bodyBob, 몸통 전체에 걸리는 단일 Y시프트) → `bodyGroup`(몸통+배+무늬, 자기 중심 기준 Y스케일로 bodySquash 표현) / `headOffsetGroup`(headDX/DY+lookDX/DY, 그 안에 `headSquashGroup`(머리, **바닥 기준** Y스케일 — 2D의 `headTopY`가 "턱은 고정, 정수리만 눌린다"인 것과 동일) + `headParts`(귀/눈/코/주둥이/볼, 스쿼시 영향 안 받음 — 2D도 이 파츠들은 `headTopY`를 안 씀)) / `tailParent`(관절 체인) / `legParent`(기존 다리 체인).
- **몸통 스쿼시가 "제 중심 기준 스케일"이라는 걸 손계산으로 먼저 증명**: 2D의 `bodyY = cfg.bodyY+bodyBob+squashY*0.5`, `bodyH = cfg.bodyH-squashY`를 대수적으로 풀어보면 몸통의 **중심 Y좌표는 squashY와 무관하게 그대로 유지됨**(위아래 가장자리가 대칭으로 안쪽으로 줄어드는 것)이 증명됨 — 이 덕분에 3D에서는 몸통을 담은 그룹을 몸통 중심에 위치시키고 `scale.y`만 프레임마다 바꾸면 Three.js가 자식(몸통 박스+배+무늬)까지 전부 같은 비율로 중심 기준 스케일해줘서, 2D의 `toBodySquashed()`(무늬/배가 몸통과 같은 비율로 줄어들게 만드는 별도 리매핑 함수)를 손으로 다시 구현할 필요가 없어짐 — 대수적으로 두 접근이 완전히 동치임을 먼저 증명한 뒤 코드로 옮김.
- **머리 스쿼시는 반대로 "바닥 기준"임을 확인**: 2D의 `headTopY = headY + (cfg.headH-headH)`를 보면 머리 아랫변(턱)은 스쿼시와 무관하게 고정, 정수리만 눌림 — 몸통과 반대 앵커. 3D에서는 `headSquashGroup`을 머리의 **아랫변** 좌표에 위치시키고(`headBottomY2D = cfg.headY+cfg.headH`), 머리 박스는 그 안에서 아랫변 기준 상대좌표(`addBoxRel`)로 배치 — `scale.y`가 줄어들수록 박스 중심이 자동으로 아랫변 쪽으로 딸려오면서 동시에 높이도 줄어, "턱 고정+정수리만 압축"이 스케일 하나로 재현됨(수식으로 검증: 로컬 y=3.5(중심)에 scale=0.75를 곱하면 2.625, 로컬 반높이도 3.5×0.75=2.625로 같이 줄어서 로컬 y=0(아랫변)은 항상 고정, 위쪽 끝(=7×scale)만 줄어듦 — 정확히 원하는 동작).
- **다리/꼬리는 반대 부호의 스쿼시 보정이 필요**: 2D의 `legY = cfg.legY+bodyBob-squashY*0.5`(다리는 몸통 **아랫변**을 따라가는데 그 아랫변은 스쿼시로 위로 올라옴) vs 꼬리의 `bodyShift = bodyBob+squashY*0.5`(꼬리는 몸통 **윗변**을 따라가는데 그 윗변은 스쿼시로 아래로 내려옴) — 정확히 반대 부호. `bobGroup`이 `bodyBob` 공통분만 처리하고, 다리 힌지(`applyLegPose`의 `legYDelta`)와 꼬리 원점(`applyTailChain`의 `tailShiftWorld`)에 각각 남은 스쿼시 보정만 반대 부호로 더해주도록 구현.
- **꼬리는 다리와 달리 중첩 회전을 안 씀**: 다리는 hip/knee 각도가 원래 "무릎각은 고관절각 위에 얹는 상대각"으로 설계돼 있어(`lowerAngle=hipAngle+kneeAngle`) `THREE.Group` 중첩(무릎 그룹이 고관절 그룹의 자식)만으로 각도가 자동으로 누적되지만, 꼬리는 각 세그먼트의 restAngle이 그 세그먼트 고유의 authored 좌표에서 독립적으로 계산되는 **절대각**이라(2D의 `pxJoint` 자체 코멘트가 "본이 몇 개 안 되니 절대각이 낫다"고 명시) 중첩 구조로 옮기면 매 단계 "부모가 이미 돈 만큼을 상쇄하고 다시 세팅"하는 항이 필요해짐 — 대신 `pxJoint`와 똑같은 방식으로 매 프레임 2D 좌표계에서 직접 하나씩 계산(원점→각도→끝점, 다음 세그먼트의 원점으로 체이닝)한 뒤 월드 좌표로 변환하는 방식을 그대로 채택.

**버그 발견 + 고침 — 귀가 머리 스쿼시를 안 따라가서 잠잘 때 머리가 통째로 떨어져 보임**: `test/voxel-viewer.html`로 실제 idle 사이클을 눈으로 보다가 `sleep` 진입 시 머리가 눈/코/귀 무리와 완전히 분리된 별도의 사각형처럼 붕 떠 보이는 걸 발견 — 육안 판단에 안 기대고 `pet3d.js`에 재사용 가능한 디버그 접근자 `setBodyPose(pose)`/`getBodyDebug()`(주요 파츠의 실제 월드 좌표를 반환)를 추가해 정지 포즈로 수치 확인. `head`/`nose` 좌표는 손으로 유도한 기대값과 소수점까지 정확히 일치했는데 `earLeft/Right`의 Y좌표만 기대값(8.35)과 다르게(10.1) 나옴 — 차이(1.75)가 정확히 `cfg.headH×bodySquash×0.25`(머리가 스쿼시로 줄어든 만큼)와 일치해서, 귀가 2D의 `headTopY`(스쿼시로 내려온 머리 윗변)를 전혀 안 따라가고 스쿼시 이전의 고정 위치에만 머물러 있다는 게 확인됨(2D는 "ear-head gap" 버그를 고치면서 이미 `headTopY`를 앵커로 쓰도록 고쳐뒀던 부분인데, 3D 이식 때 이 보정을 빠뜨렸던 것 — 설계 단계에서는 계획했지만 실제 구현에서 누락). **고침**: `buildEars`가 빌드 시점 Y좌표(`baseWorldY`)를 저장해두고, `applyEars`가 매 프레임 `-(cfg.headH×bodySquash×0.25)`만큼(2D의 headTopY 시프트와 동일 부호) 추가로 내려주도록 수정. 재검증 결과 귀 Y좌표가 정확히 기대값(8.35)으로 일치, `test/voxel-viewer.html`에서도 sleep 진입 시 귀-머리가 매끈하게 붙어서 움직이는 걸 재확인.

**idle 행동 풀 전종 스윕 검증**: `test/launch-voxel-idlesweep.js`(재사용 가능한 새 테스트 도구) — `createAnimal()`의 실제 상태머신을 30fps 상당으로 빠르게 시뮬레이션하면서, 각 idle 행동이 시작 후 0.3초(포즈 크로스페이드 0.25초보다 길게 — 전환 중간이 아니라 정착된 포즈를 캡처하기 위해) 지난 시점의 포즈를 캡처, 그 종의 idlePool에 있는 모든 행동을 최소 1번씩 볼 때까지 반복. cat_a/dog_corgi/dog_husky/rabbit_b/hamster 5종에 대해 실행 — **10종 전부의 idlePool 전체 행동이 3만 프레임 이내에 전부 관측됨(누락 없음)**, 캡처된 스크린샷 확인 결과 그루밍/앉기(로프+헌치스+루프노렉스+라운드퍼프 스타일 전부)/졸기/귀씰룩임/뒹굴기(bellyUp)/볼주머니/귀경계(earalert)/코씰룩임(nosetwitch)/꼬리까닥임(tailtwitch) 전부 깨짐이나 파츠 분리 없이 그럴듯하게 렌더링됨. 특히 코기의 `sit`(haunches 스타일, `waddleTilt`로 몸 전체가 뒤로 기우는 연출)이 3D에서도 몸 전체가 뚜렷하게 기울어진 모습으로 확인되어 `rollPivot`의 Z축 회전이 실제로 올바르게 작동함을 실사용 포즈로도 재확인.

**알려진 단순화 (의도적, 이번 라운드에서 새로 확인)**:
- 귀는 여전히 종 무관 단순 박스(위 정면/후면 라운드에서 이미 받아들인 단순화) — `earFlick`(귀 씰룩임/귀경계)은 X축 위치 이동으로만 근사, 2D의 삼각형/둥근/처진 모양별 "귀 끝만 움직임" 같은 세부 동작은 표현 안 됨. 토끼 `earalert` 스크린샷에서 귀가 "쫑긋 선다"기보다 "옆으로 살짝 이동한 색 조각"처럼 보이는 게 이 단순화의 직접적 결과 — 기능적으로는 정상 작동(수치로 확인됨), 시각적 임팩트만 2D 대비 약함.
- `faceShade`(샴 고양이의 콜러포인트 방사형 그라데이션)는 이번 포팅에서 아직 구현 안 함 — 순전히 캔버스 디더링 기법이라 3D 셰이더 없이는 동치가 없음, 일단 이 포즈 포팅 라운드의 범위 밖으로 미룸(샴은 faceShade 없이도 headColor 자체로 얼굴/몸 색 구분은 유지됨). *(2차 QA 라운드에서 실제로 구현됨 — 아래 "샴 고양이 얼굴 그라데이션 구현 + 그루밍 깜빡임 원인 규명" 항목 참고.)*
- 그림자(`drawShadow`)는 아직 3D에 이식 안 함 — 2D에서도 `pose`를 안 읽는 고정 타원이라 우선순위 낮음.

### 실제 pet 창에 연결 (stage 4 — `windows/pet/index.html`+`pet.js`)
`windows/pet/pet.js`의 Canvas 2D 렌더링(`animal.draw(ctx)`)을 통째로 3D voxel 렌더링으로 교체. 클릭 통과·드래그 이동·말풍선·알림 트리거·커서 추적 등 기존 기능은 전부 그대로 유지하면서 렌더링 계층만 갈아끼우는 걸 목표로 함 — `settings.js`/`windows/settings/`는 이번에 전혀 안 건드림(캐릭터 미리보기가 여전히 2D 엔진으로 그려짐, 정적 아이콘 하나만 필요한 용도라 3D로 바꿀 필요가 없다고 판단 — "설정 창 캐릭터 선택이 그대로 동작해야 한다"는 요구사항은 이 파일들을 안 건드리는 것 자체로 이미 충족됨).

**`windows/pet/index.html`**: `windows/pet3d/index.html`과 동일한 `<script type="importmap">`로 `three` bare specifier를 `../../node_modules/three/build/three.module.js`에 매핑하는 한 줄만 추가 — 나머지(`#pet-canvas` 엘리먼트, `pet.js`를 `type="module"`로 로드하는 것)는 기존 그대로(`pet.js`가 이미 `createAnimal`/`CHARACTERS`를 ES 모듈로 import하고 있었으므로 `type="module"` 자체는 이미 걸려 있었음).

**`windows/pet/pet.js` 핵심 교체**:
- 기존 `ctx = canvas.getContext('2d', {willReadFrequently:true})` 대신 `windows/pet3d/pet3d.js`와 같은 카메라/프러스텀(FRUSTUM=26, CENTER_Y=11, 직교 카메라)으로 `THREE.WebGLRenderer`/`Scene`을 셋업 — 다만 두 가지는 프로토타입 뷰어와 다르게 설정: (1) `alpha:true`+`scene.background`를 아예 안 세팅(프로토타입은 디버그용 초록 배경을 씀) — 클릭 통과가 성립하려면 캔버스 자체가 진짜 투명해야 함, 기존 2D 캔버스가 이미 투명했던 것과 동일한 요구사항. (2) `preserveDrawingBuffer:true` — 렌더링 직후에도 프레임버퍼를 읽을 수 있어야 아래 클릭 통과 히트테스트가 동작함.
- **클릭 통과 히트테스트**: 기존 `ctx.getImageData(cx,cy,1,1).data[3]`(2D 컨텍스트의 알파 채널 읽기)를 WebGL 버전으로 교체 — `gl.readPixels(cx, canvas.height-1-cy, 1,1, gl.RGBA, gl.UNSIGNED_BYTE, pixelBuf)`로 같은 지점의 알파를 읽음. **주의할 차이점**: WebGL의 `readPixels`는 원점이 좌하단(bottom-left)이라, 기존 좌상단(top-left) 기준으로 계산해오던 `cy`를 `canvas.height-1-cy`로 뒤집어야 함 — 이것만 빼면 판정 로직(`ALPHA_HIT_THRESHOLD=128` 등) 자체는 완전히 동일.
- **방향 전환(direction)**: 기존엔 `canvas.style.transform = direction===1?'scaleX(1)':'scaleX(-1)'`(CSS 좌우 반전)이었는데, 이제 `group.rotation.y = direction===1?0:Math.PI`(실제 3D Y축 180도 회전)로 교체 — 직교 카메라로 정면에서 보는 한 두 방식은 시각적으로 동치(모델을 Y축으로 180도 돌리고 같은 카메라로 보는 것과 화면을 수평으로 뒤집는 것은 같은 실루엣을 만듦)이면서, 진짜 3D 회전이라 삼색이 좌우 비대칭 귀 색처럼 "몸의 어느 쪽이 지금 카메라를 보고 있는지"까지 물리적으로 올바르게 반영됨(실제 동물이 방향을 틀면 좌우가 진짜로 바뀌는 것과 같은 이치) — CSS 반전은 그냥 이미지를 거울처럼 뒤집을 뿐이라 이 구분이 없었음. 이 교체 덕분에 `sampleHit`의 "CSS 반전을 다시 풀어서 백킹 픽셀로 매핑"하던 보정 로직(`if(direction===-1) cx=canvas.width-1-cx`)도 통째로 필요 없어짐 — 3D 회전이 이미 렌더링된 픽셀 자체에 반영돼 있으므로 그냥 있는 그대로 읽으면 됨.
- **캐릭터 전환**: 기존 `loadCharacter(key)`가 `animal = createAnimal(character)`만 하던 걸, `group`도 같이 재생성(`scene.remove(group); group = buildVoxelCreature(character); scene.add(group);`)하도록 확장 — 초기 로드(`getSettings().then(loadCharacter)`)와 설정 변경(`onSettingsUpdated`) 두 경로 모두 이 하나의 함수를 거치므로 별도 분기 없이 자동으로 커버됨.
- **`render()`**: `ctx.clearRect+animal.draw(ctx)` 대신 `group.userData.applyPose(animal.getPose())`(포즈 반영) + `renderer.render(scene,camera)`(렌더) 두 줄로 교체. 드래그 이동(`petWrapEl.style.transform=translateX(x)`)·말풍선·사운드·이중클릭 등 캔버스 렌더링과 무관한 나머지 로직은 전부 그대로.

**실제 앱 실행 검증 (`npx electron .`, `npm start`와 동일)**:
1. **렌더링/투명도**: `main.js`에 임시 env-var 훅(`FOCUSPET_DEBUG_CAPTURE`, 검증 후 완전히 제거 — 이전 세션들의 `FOCUSPET_DEBUG_ROTATION`/`FOCUSPET_DEBUG_BUBBLE`과 같은 1회성 패턴)을 걸어 로드 3초 뒤 `petWindow.webContents.capturePage()`로 실제 창을 캡처 — 렌더러 콘솔에 에러 없음(`main.js`가 이미 갖고 있던 `console-message` 포워딩으로 확인, 이 리스너 자체는 이번 세션 이전부터 있던 것 — 첫 3D 실험 때 추가됐다가 2D로 되돌아갈 때 지우는 걸 깜빡 잊고 남아있던 코드였는데, 이번에 다시 3D가 되면서 우연히 다시 정확한 설명이 됨). PNG를 직접 파싱해 알파 채널 확인 — 모서리(배경) `RGBA=(0,0,0,0)`(완전 투명, 기존 2D 캔버스의 투명 배경과 동일한 속성 확인됨), 불투명 픽셀(스프라이트) bbox가 `pet.css`의 `#pet-wrap{bottom:14px;left:0}` 배치와 일치하는 화면 좌하단에 정확히 위치.
2. **클릭 통과 히트테스트**: 같은 훅에서 `petWindow.webContents.sendInputEvent({type:'mouseMove',x,y})`로 실제 마우스 이벤트를 여러 좌표에 합성 주입하고, `pet.js`에 임시로 추가한 디버그 로그(검증 후 제거)로 `hitState` 전환을 관찰 — 스프라이트 위 좌표에서 `hitState -> true`, 빈 공간 좌표에서 `-> false`가 실제로 관측됨(`gl.readPixels` 경로가 실제 앱에서 정상 작동함을 확인) — 정확한 좌표 매핑(캡처 이미지의 스케일과 `sendInputEvent`가 기대하는 좌표계가 정확히 일치하는지)까지는 여러 후보 좌표를 훑어서 간접 확인한 수준(이전 세션들의 "합성 이벤트로 실제 커서 위치를 흉내낼 수 없다"는 드래그 스프링 물리 검증과 같은 종류의 한계 — 메커니즘 자체는 확인됐지만 실사용 시의 정밀도는 사용자 확인이 최종 검증).
3. **2D 회귀**: `npm run test:invariants`(120만 프레임) 재실행 — 위반 0건, `getPose()` 추가나 이번 라운드의 다른 `animal-engine.js` 변경이 기존 2D 엔진 자체에 영향 없음 재확인.

**검증 못 한 것 (정직하게 기록)**: 드래그(실제 마우스 버튼을 누른 채 이동), 이중클릭으로 설정 창 열기, 말풍선 표시, 알림 소리 재생, 설정에서 캐릭터를 바꿔서 실제로 3D 모델이 바뀌는지는 코드 경로상 로직 변경이 없거나(말풍선/사운드/드래그 물리) 이미 검증된 패턴을 그대로 재사용(`loadCharacter` 확장)하는 부분이라 별도 자동 검증은 안 함 — 이 중 어느 것도 렌더링 방식(2D→3D) 자체에 의존하는 로직이 아니라서 위험도가 낮다고 판단했지만, 실사용 중 문제가 보이면 알려주면 됨.

### `test/voxel-viewer.html` 뷰어 갱신
프로토타입 확인용 독립 뷰어(`test/serve-voxel-viewer.js`+`test/voxel-viewer.html`)를 다리 스윙 시뮬레이션(사인파) 대신 실제 `createAnimal()` 상태머신으로 교체 — 종 선택/자동회전/드래그 회전은 그대로, "다리 스윙" 체크박스+"앉기 토글" 버튼 대신 쿡 찌르기(poke)/재우기(sleep)/잡기(hold) 버튼으로 실제 idle 풀 전체를 인터랙티브하게 확인 가능. 캔버스 위 마우스 움직임을 커서 힌트로 변환해 시선 추적/근접 경계 반응도 함께 확인 가능. 계속 유지 — 앞으로 렌더링을 더 손볼 때 제일 먼저 열어볼 도구.

### 실제 사용 후 버그 리포트 4건 수정 (stage 4 이후 1차 QA 라운드)
사용자가 실제 앱을 띄워 써보고 4가지 문제를 리포트 — 전부 원인을 실제로 특정해서 고침(추측으로 고치지 않음).

**1. 더블클릭으로 설정창이 안 열림**: `main.js`에 임시 로깅 훅(`FOCUSPET_DEBUG_DBLCLICK`, 검증 후 제거)을 걸고 `sendInputEvent`로 실제 더블클릭 시퀀스를 재현해 `hitState`/`suppressNextClick`/`click` 이벤트 타임스탬프를 직접 추적 — **진짜 원인은 렌더링 방식(2D→3D)과 무관한 기존 로직 버그**였음: `mousedown`이 `hitState`가 true이기만 하면(실제 이동 여부와 무관하게) 무조건 `dragging=true`를 세팅하고, `mouseup`이 `dragging`이었으면 무조건 `suppressNextClick=true`를 세팅하는 구조라, **커서가 이미 스프라이트 위에 정지해 있는 상태에서 일어나는 모든 클릭**(더블클릭의 두 번째 클릭이 전형적 — 첫 클릭으로 이미 hitState가 true로 확정된 채 커서가 그 자리에 그대로 있으므로)이 "드래그 종료"로 오판돼 뒤따르는 `click` 이벤트가 통째로 삼켜지고 있었음 — 진단 스크립트로 실제 클릭 1의 `click` 이벤트조차 `suppressNextClick=true`로 삼켜지는 걸 직접 확인. **고침**: `mousedown` 시점의 커서 좌표를 저장해두고, `mouseup` 시점에 실제 이동 거리(`DRAG_MOVE_THRESHOLD=4px`)를 넘었을 때만 `suppressNextClick`을 세팅하도록 수정 — 진짜 드래그와 제자리 클릭을 구분. 재검증 결과 더블클릭 시퀀스가 정상적으로 `open-settings`를 발생시킴.
  - **부수 발견 + 추가 방어 (알파 플리커)**: 같은 진단 중, 정지된 커서로 두 번째 클릭을 보내는 사이(120ms) 순전히 breathe idle의 미세한 자세 변화만으로 `hitState`가 `true→false`로 잠깐 흔들리는 걸 관측 — 이 순간에 **실제** OS 클릭이 왔다면(합성 이벤트와 달리 실제 클릭은 OS가 라우팅 자체를 뒤 프로그램으로 넘겨버리므로) 펫 창에 아예 도달을 못 했을 것. 원인은 단일 픽셀 알파 샘플링이 3D 복셀 모델(박스가 여럿이라 미세한 자세 변화에도 경계 픽셀 값이 프레임마다 바뀔 수 있음)에는 2D 평면 실루엣보다 더 취약하다는 것 — `sampleHit`를 1픽셀 샘플 대신 5x5(`HIT_RADIUS=2`) 이웃 블록 중 하나라도 불투명하면 히트로 판정하도록 강화(빈 공간 클릭 통과는 그대로 정상 동작 확인).
- **2. 알림 소리 볼륨이 작음**: `knockNote`의 `peak` 기본값을 0.17→0.42로(~2.5배) — 헤드룸 계산: 세 오시레이터가 완전히 동위상으로 겹쳐도 `peak*(1+0.55+0.14)=peak*1.69`이므로 0.42*1.69≈0.71, 디지털 클리핑(1.0) 여유 있게 안쪽.
- **3. 회전이 3D처럼 안 느껴짐(좌우반전처럼 보임)**: 근본 원인은 `render()`가 매 프레임 `group.rotation.y = direction===1?0:Math.PI`를 **그대로 대입**하고 있었다는 것 — 즉 방향이 바뀌는 순간 중간 각도를 전혀 안 거치고 단 한 프레임 만에 0°→180°로 순간이동함. 실제 3D 지오메트리를 쓰고 있어도, 화면에 도달하는 결과물은 옛 CSS `scaleX(-1)` 즉시 반전과 **시각적으로 완전히 동일**(그 사이 각도를 눈으로 볼 기회 자체가 없음) — 이 세션 내내 회전을 검증했던 voxel-viewer(드래그/자동회전, 항상 연속적 회전)는 이 버그를 애초에 노출할 수 없는 사용 방식이었음. **고침**: `direction` 목표값으로 즉시 대입하는 대신 `currentRotationY`를 지수 감쇠(`1-exp(-dt*8)`, 이 코드베이스의 다른 이징과 동일한 형태)로 목표까지 매 프레임 부드럽게 근접시키도록 변경(~0.3초 소요) — 실제 창에서 방향 전환을 강제로 트리거해(커서 추적을 우회하는 임시 테스트 훅 사용, 검증 후 제거) 프레임 시퀀스를 캡처해보니 중간 지점(t≈60ms)에서 실루엣이 뚜렷하게 좁아지는 원근 압축이 보임 — 회전이 실제로 일어나고 있다는 걸 눈으로 확인(`test/voxel-rotsweep-*.png`).
  - **원근(perspective) 카메라 조사 (사용자 요청)**: `windows/pet3d/pet3d.js`에 `?perspective=1&camDist=NN` 옵션(실험용, pet.js에는 미적용)을 추가해 직교/원근을 실제로 비교 — **원근이 확실히 추가적인 입체감을 줌**을 확인(45도 각도에서 각 박스 윗면 모서리가 카메라 쪽으로 수렴하는 게 뚜렷이 보임, `test/voxel-camcompare-*.png`) — 직교 투영은 원리적으로 평행선이 거리와 무관하게 항상 평행이라 이런 단서를 절대 못 줌. 다만 **가까운 거리(camDist=20~35)에서 로스터 중 가장 긴 종(닥스훈트, 코-꼬리 총 길이 ~23유닛, FRUSTUM=26과 거의 맞먹음)이 여러 각도에서 화면 잘림**을 확인 — 90도 회전 시 몸 전체 길이가 카메라 쪽 깊이(Z)로 분산되면서 카메라에 가까운 쪽 끝이 계산했던 것보다 훨씬 크게 보여(월드 좌표 분석: 90도에서 근접단이 카메라와 23.5유닛 거리로, 계산 기준 거리 35유닛보다 훨씬 가까워져 약 1.5배 확대돼 보임) 잘림 발생, camDist를 늘리면 잘림은 없어지지만 원근 효과 자체가 옅어짐(로스터 10종 전부를 안전하게 커버하려면 상당한 추가 튜닝 필요). **결론**: 회전 애니메이션 수정만으로 "3D처럼 안 보인다"는 핵심 문제가 이미 확실히 해결됐다고 판단(스크린샷으로 원근 압축이 뚜렷이 확인됨) — 원근 카메라는 실제 이득이 있는 게 확인됐지만 안전하게 배포하려면 종별 프레이밍 튜닝이 추가로 필요해서, 이번엔 정직하게 "확인은 했지만 아직 적용 안 함"으로 남겨두고 `pet.js`는 직교 카메라 유지. 프로토타입 토글(`?perspective=1`)은 남겨둠 — 나중에 튜닝해서 도입하고 싶으면 바로 재사용 가능.
- **4. 그루밍 시 앞발이 얼굴색과 같아서 안 보임**: 실제로는 색 문제보다 **훨씬 근본적인 버그**였음 — `getBBoxDebug()`(신규 디버그 접근자)로 그루밍 포즈의 앞다리 월드 바운딩박스를 머리 바운딩박스와 비교해보니 **X/Y/Z 세 축 전부 완전히 겹침**을 확인. 원인: hip/knee 회전은 로컬 X-Y 평면에서만 일어나고, 다리의 Z위치(`LEG_Z`, 좌우 쌍을 가르는 고정 오프셋)는 빌드 시점에 한 번 정해진 뒤 포즈와 무관하게 절대 안 바뀌는데, 이 값(≈1.65)이 머리의 Z 범위(`[-HALF_DEPTH,+HALF_DEPTH]`=`[-3,3]`) 안쪽에 있어서 — "얼굴로 들어올린" 자세에서도 다리가 머리 앞면(Z=+3, 카메라에 더 가까움)보다 항상 뒤에 위치해 머리의 불투명 지오메트리에 완전히 가려짐. **색이 같아서 안 보이는 게 아니라 애초에 안 그려진 것과 마찬가지**로 가려져 있었던 것 — 이전 라운드의 EYE_Z/배·무늬 임베딩 버그와 정확히 같은 종류("표면보다 얕으면 안 보인다")의 새 사례였는데, 서 있는 다리는 이 문제가 생길 이유가 없어서(머리 앞으로 뻗을 일이 없으므로) 지금까지 안 걸렸던 것. **고침 (3단 처리)**: (1) *가려짐 해소*: `frontLegRaise`가 올라갈 때 앞다리의 Z위치를 자기 부호 방향으로(근접 쪽 다리는 카메라 쪽으로, 반대쪽 다리는 반대로) `HALF_DEPTH+1.2`까지 밀어내 머리 표면 밖으로 나오게 함(EYE_Z와 같은 "표면을 넘어서야 보인다" 마진 원칙 재사용) — 이게 진짜 핵심 수정. (2) *음영*: 그래도 색이 비슷해 헷갈릴 수 있으니 들어올린 다리(위/아래 다리 + paw)의 재질 색을 흰색 쪽으로 최대 40%(`GROOM_HIGHLIGHT_MAX`) 블렌드, `frontLegRaise`에 비례해 자연스럽게 페이드인/아웃. (3) *움직임*: 들어올려진 동안 무릎 각도에 작은 반복 진동(`GROOM_WOBBLE_DEG=16°`, `9rad/s`)을 얹어 비비는 듯한 동작을 표현. 세 가지 다 `pose.frontLegRaise`에 비례해서 켜지므로 groom 진입/이탈 크로스페이드와 자연스럽게 맞물림. **검증**: 고치기 전/후 바운딩박스 비교(고친 후 Z축이 완전히 분리됨, `[3.2,5.2]` vs 머리 `[-3,3]` — 더 이상 안 겹침) + 실제 idle 상태머신을 통해 그루밍에 진입시킨 스크린샷(`test/voxel-idlesweep-cat_a-groom.png`)에서 턱 밑에 뚜렷한 밝은 색 앞발이 보임 확인. 45°/180°에서도 확인 — 정상. 90°(정면 근접)에서는 근접/반대쪽 다리가 좌우 대칭으로 튀어나와 약간 부자연스러워 보이지만(양쪽 목 옆에 밝은 돌기 두 개), 그루밍이 방향 전환과 정확히 겹치는 드문 순간에만 잠깐 스치듯 보이는 경우라 감수하기로 함(버그라기보다 기하학적으로 불가피한 트레이드오프).

### 실제 사용 후 버그 리포트 5건 수정 (stage 4 이후 2차 QA 라운드)
1차 QA 라운드(위 4건)를 고친 뒤 다시 실사용하고 나온 후속 리포트 5건 — 이번에도 전부 원인을 실제로 특정하거나(볼륨/회전/그루밍/턱시도) 수치로 재현 확인(샴 그라데이션/깜빡임)한 뒤 고침.

**1. 알림 소리 볼륨을 설정 가능하게**: `settingsStore.js`의 `DEFAULTS`에 `soundVolume: 0.5`(0..1, 기본 50%) 추가 — `load()`의 `{...DEFAULTS, ...parsed}` 스프레드 패턴이 이미 있어서 필드 추가만으로 기존 `settings.json`과의 하위호환이 자동으로 됨(마이그레이션 코드 불필요). `windows/settings/`에 슬라이더(`<input type="range" id="soundVolume" min="0" max="100">`) 추가 — 저장은 `/100`으로 0..1 정규화, 로드는 `Math.round(value*100)`으로 퍼센트 표시, 둘 다 `settings.js`. `main.js`의 `reminder` IPC 페이로드에 `soundVolume` 필드 추가, `pet.js`의 `playBeep(volume=0.5)`가 `peak = 0.42 * safeVolume`(0.42는 기존에 "100% 볼륨" 기준으로 튜닝해뒀던 그 값 — 즉 100% 슬라이더는 기존 소리와 정확히 동일, 50%면 그 절반)로 반영. `safeVolume`은 `Number.isFinite` 체크 후 0..1로 clamp — 오디오 gain에 직접 먹이는 값이라(수동 편집된 `settings.json`이 들어와도) 방어적으로 처리.
- **검증**: `settingsStore.js`/`settings.js`/`main.js`/`pet.js` 코드 리뷰로 값 흐름(슬라이더→저장→IPC→gain 계산) 전체를 추적 확인. 헤드리스 환경이라 실제 소리 크기 변화를 귀로 들어볼 수는 없음(기존과 같은 한계) — 0%/50%/100%가 각각 gain 0/0.21/0.42로 정확히 이어지는 계산 자체는 코드로 확정적.

**2. 좌우 회전 시 뒤통수가 카메라를 향하며 도는 문제**: 1차 라운드에서 회전을 "즉시 스냅"에서 "지수 감쇠로 부드럽게"로 고쳤을 때(`currentRotationY`), 목표각을 `direction===1?0:Math.PI`(양수 180도)로 잡아뒀었는데 — 표준 Y축 회전행렬(`x'=x·cosθ+z·sinθ, z'=-x·sinθ+z·cosθ`)을 손으로 유도해보면 θ가 양수 쪽으로 커질 때 코(로컬 +X, Z=0)는 **음의 Z 쪽**으로 이동함 — 카메라가 +Z에서 -Z 방향을 보고 있으므로 음의 Z는 카메라에서 **멀어지는** 방향, 즉 θ=90° 근방(회전 중간)에서 얼굴이 카메라 반대쪽으로 돌아가고 등이 카메라를 향하게 됨. 최종 정지 각도(0° 또는 180°, mod 2π로 결과는 동일)는 안 바뀌니 회전이 다 끝난 뒤의 모습은 문제없었지만, **회전 도중 지나가는 중간 각도**가 잘못된 방향이었던 것 — 정확히 사용자가 보고한 증상("회전 중간에 뒤통수가 보임")과 일치. **고침**: `direction===-1`일 때 목표각을 `+Math.PI` 대신 `-Math.PI`로 — 최종 정지 각도는 동일(2π 모듈러니 시각적으로 같은 자세)하지만, 이번엔 θ가 **음수 쪽**으로 커지면서 코가 **양의 Z**(카메라 쪽)로 이동 — 회전 내내 얼굴이 카메라 쪽으로 리드하며 돎. 복귀 방향(왼쪽→오른쪽)도 `currentRotationY`가 -π에서 0으로 다시 이징되는 동안 똑같이 음수 구간을 지나가므로, 부호 하나만 바꿔서 양쪽 회전 방향 전부 "얼굴이 리드"가 됨(왕복별로 따로 처리할 필요 없음).
- **검증**: 독립 Electron 런처(`test/launch-rotation-test.js`, 검증 후 삭제)로 `windows/pet/index.html`을 `main.js` 없이 단독 로드(→ `startCursorTrackPoll`이 안 돌아서, `direction`을 강제로 바꾸는 임시 테스트 훅(`window.__setDirectionForTest__`, 검증 후 `pet.js`에서 제거)이 실제 커서 추적에 덮어써질 걱정 없음) — 방향 전환 중 프레임 시퀀스를 캡처(`test/crop-rotation-frames.js`로 스테이지 안 96x96 펫 영역만 4배 확대 크롭, 검증 후 삭제)해서 확인. 회전 중간 프레임(θ≈-74.5°)에서 파란 눈 두 개와 검은 코가 카메라를 향해 뚜렷이 보임(뒤통수가 아니라 얼굴이 리드) — 왕복(오른쪽→왼쪽, 왼쪽→오른쪽) 양쪽 다 확인. **부수 발견**: 테스트 훅으로 `direction`을 세팅해도 매 프레임 무조건 `direction=1`로 덮어써지는 걸 발견 — `tick()`의 "예전 patrol 경계-반사" 죽은 코드(`if(x<=min) direction=1`, 위 이동 정책 히스토리 참고)가 `x`가 항상 `min`에 고정돼 있어서(자율 이동이 없으므로) 매 프레임 계속 실행되고 있었던 것 — 실제 앱에서는 `updateBodyDirection()`이 매 프레임 그 다음에 다시 커서 기준으로 덮어쓰므로 문제가 안 됐지만, 테스트 훅은 그 이후 로직을 안 타서 정면으로 부딪힘. 테스트 스크립트에서 `x`를 경계 밖(중앙)으로도 같이 옮기는 것으로 우회(앱 코드 자체는 안 건드림 — 이 죽은 코드를 지우는 게 이번 요청 범위가 아님).

**3. 그루밍 동작을 "와이퍼" 스타일로 완전히 재설계**: 1차 QA에서 고친 그루밍(`GROOM_PROTRUDE_Z`로 앞발을 머리 표면 밖으로 밀어내고, `GROOM_WOBBLE`로 무릎에 작은 진동을 얹은 버전)이 여전히 "그루밍처럼 안 보인다"는 재리포트 — 이번엔 "한 번 들고 마는 게 아니라 좌우로 반복해서 훑는" 명시적 요청. 무릎 진동 대신 **고관절(hip) 자체가 좌우로 오가는 진짜 와이퍼**로 재설계 — 2D/3D가 공유하는 `legJointAngles`(다리 각도 계산의 단일 소스) 쪽에서 고침:
  - `animal-engine.js`의 `IDLE_BEHAVIORS.groom`에 `pose.groomWipe = Math.sin(t*GROOM_WIPE_FREQ)*GROOM_WIPE_AMPLITUDE`(진폭 30°, 주파수 7rad/s — groom 지속시간을 1.6~2.4초에서 2.2~3.2초로 늘려 한 사이클에 왕복 2회 이상 들어가게 함) 추가.
  - `legJointAngles`의 `frontLegRaise` 분기: 앞발을 들어올리는 목표 고관절각을 기존 고정값 `-160`에서 `-150+groomWipe`로 — 기준각을 150으로 살짝 낮춘 건 ±30도 스윙이 각도 wrap 지점(±180)에 너무 가까워지지 않게 여유를 두기 위함. 무릎각은 `-50`으로 고정(오시레이션 없음) — 무릎까지 같이 흔들면 "여러 관절이 제각각 흐느적대는" 느낌이 나서, 힌지 하나(고관절)만 흔드는 "뻣뻣한 팔이 정해진 축을 중심으로 좌우로 훑는" 진짜 와이퍼 느낌을 위해 의도적으로 무릎은 안 건드림.
  - `voxel-engine.js`에서 `GROOM_WOBBLE`(무릎에 `performance.now()` 기반 진동을 얹던 옛 메커니즘) 완전히 삭제 — 이제 와이퍼 동작 자체가 `legJointAngles`(2D/3D 공유)에서 나오므로 3D 전용 보강이 더 이상 필요 없어짐. `GROOM_PROTRUDE_Z`(머리 표면 밖으로 Z를 밀어내는 것, 진짜 원인 수정이었던 부분)와 `GROOM_HIGHLIGHT`(흰색 쪽으로 블렌드)는 그대로 유지 — 둘 다 모션이 아니라 가려짐/색 문제 해결책이라 이번 재설계와 무관.
  - `defaultPose()`/`POSE_FIELD_BOUNDS`에 `groomWipe` 필드 추가(0 기본값, 상한 45 — 실제 진폭 30보다 여유 있게).
- **검증**: `test/launch-groom-wiper-test.js`(재사용 가능한 도구로 남겨둠)로 cat_a/dog_husky 각각 한 사이클(8프레임, `groomWipe=30·sin(phase)`)을 `setBodyPose()`로 직접 구동해 스크린샷+`getLegDebug()`로 실제 적용된 고관절각을 같이 기록 — `groomWipe`가 0→30→0→-30→0으로 오갈 때 실제 렌더링된 hip 각도가 150°→120°→150°→180°→150°로 정확히 대응(수치로 스윙 확인). 스크린샷으로도 앞발이 "위/앞(얼굴 쪽)"과 "아래/뒤(가슴 쪽)" 두 자리를 오가며 뚜렷이 다른 위치에 그려지는 것 확인 — 이전의 "그 자리에 고정된 채 무릎만 미세하게 떪"과는 확연히 다른, 실제로 왔다갔다하는 궤적. `npm run test:invariants`(120만 프레임, 공유 로직을 건드렸으므로 재실행) 위반 0건.

**4. 샴 고양이 얼굴 그라데이션 구현 + 그루밍 깜빡임 원인 규명**: 두 증상("그라데이션이 지지직거림"과 "얼굴 그라데이션이 사라짐")을 각각 조사.
  - **그라데이션이 애초에 없었음**: `voxel-engine.js` 전체를 grep해봐도 `cfg.faceShade`를 참조하는 코드가 어디에도 없음 — 3D 포팅 당시(stage 2/3, 위 "알려진 단순화" 참고) 명시적으로 범위 밖으로 미뤄뒀던 항목이 그 뒤로 한 번도 구현되지 않았던 것뿐, "됐다가 깨진" 회귀가 아니었음.
  - **깜빡임의 진짜 원인 (수치로 확인)**: 임시 진단 스크립트(`test/debug-siamese-flicker.js`, 검증 후 삭제 — 인접한 두 프레임을 캡처해 픽셀이 토글되는지 비교)로 확인한 결과, 가만히 있을 때(0°/45°/90°)는 토글 0건인데 **그루밍 포즈에서만** 448개의 토글 프레임 발견 — 원인은 정확히 위 3번 항목에서 삭제한 `GROOM_WOBBLE`: `performance.now()`(실제 시계 시각)를 직접 읽어 무릎 각도를 흔들던 방식이라, 포즈 계산의 나머지 전부가 쓰는 "이 behavior가 시작된 이후 경과 시간(`t`, dt를 누적한 값)"과 전혀 동기화가 안 돼 있었음 — 크로스페이드/이징 등 다른 모든 시스템이 `t`/`dt` 기반인데 이것만 실시간 시계 기반이라, 렌더링 파이프라인 어디와도 안 맞물리는 진짜 "지지직"이 났던 것. 3번 항목에서 `GROOM_WOBBLE`을 지운 것 자체가 이 깜빡임의 실질적 수정 — `groomWipe`는 다른 모든 pose 필드처럼 `t`로부터 매 프레임 결정론적으로 계산되므로 이 문제가 구조적으로 재발할 수 없음.
  - **그라데이션 구현 (`addFaceShadedHeadBox`, `voxel-engine.js`)**: 2D의 `pxDitherRadial`(Bayer 디더링으로 두 색 사이 체커보드 낙차를 흉내내는 기법 — 캔버스 픽셀에 보간이 없어서 필요했던 것)을 그대로 재현하는 대신, `BoxGeometry`의 24개 정점 각각에 실제 보간되는 색을 부여하는 **진짜 스무스 그라데이션**으로 구현 — Three.js는 vertex color를 각 면에 걸쳐 자동으로 Gouraud 보간하므로 디더링 패턴을 흉내낼 필요가 없고, 결과적으로 2D의 픽셀아트 디더보다 오히려 실제 콜러포인트 고양이 털 결에 더 가까움(이 파일의 다른 모든 파츠가 지키는 "단색 평면"이라는 미학에서 벗어나는 유일한, 의도적인 예외 — `cfg.faceShade`가 있는 종에만 적용, 현재는 샴 한정). 각 정점의 로컬 (vx,vy)를 authored 2D 좌표(x2d,y2d)로 역변환(`x2d=headX+headW/2+vx`, `y2d=headY+headH/2-vy` — Y축은 2D가 아래로 증가, 로컬/월드는 위로 증가하는 차이를 그대로 반영)한 뒤 focus(눈 중심)로부터의 거리/radius로 `t`를 구해 `headColor`(어두움, t=0)에서 `highlight`(밝음, t=1)로 `THREE.Color.lerp`. Z(깊이)는 계산에 안 들어감 — 박스의 6개 면 중 어느 면에 속한 정점이든 같은 (x,y)면 같은 색이 나오게 해서, 모서리에서 색이 뚝 끊기는 이음매가 애초에 생기지 않게 함(2D도 원래 depth 개념이 없는 (x,y)만의 함수라 이 성질을 그대로 물려받은 것). *(7차 QA 라운드에서 이 24-정점(면당 4개 corner뿐) 접근 자체가 진짜 방사형 그라데이션을 표현하기엔 정점이 근본적으로 부족하다는 게 밝혀져 캔버스 텍스처 방식으로 전면 재작성됨 — "실제 사용 후 버그 리포트... 5차 QA 라운드" 섹션 참고.)*
  - **검증 (수치, 육안 둘 다)**: `pet3d.js`에 `getFaceShadeVertexColors()`(정점별 원본 색상 attribute를 직접 덤프, 렌더링/가려짐과 무관하게 검증 가능 — 남겨둠) 추가해 초점(눈)에 가까운 정점 vs 먼 정점의 색을 비교 — 먼 정점(t≈1, 코너)이 `highlight`의 **선형(linear) 색공간** 변환값과 소수점까지 정확히 일치, 중간 정점도 손으로 계산한 선형 보간값과 정확히 일치(Three.js의 최신 ColorManagement가 hex를 sRGB로 해석해 내부적으로 선형 색공간에 저장하기 때문에, 0..255 그대로 읽으면 sRGB 16진수 값과 안 맞아 보이는 게 정상 — 색 자체가 잘못된 게 아니라 표현 공간이 다른 것뿐임을 확인). 처음엔 스크린샷만 보고 "그라데이션이 반대 방향(눈 근처가 밝고 귀 근처가 어두움)인 것 같다"고 오판할 뻔했는데, 귀(같은 어두운 `headColor`)가 그라데이션의 밝은 쪽(귀 근처) 바로 옆에 붙어있어서 육안으로는 "그 근처가 전부 어둡다"는 착시가 생겼던 것 — 정점 좌표를 직접 대조하고 나서야 착시였음을 확인(이전 세션들의 "육안 판단이 몇 번 틀렸다"는 교훈이 이번에도 유효했음).
  - **깜빡임 재검증 (전체 idle 사이클)**: `pet3d.js`에 `live=1` 모드(`stepIdle(dt)`/`getIdleDebug()`) 추가 — 실제 `createAnimal()`/`update()` 상태머신을 그대로 구동해서(수동으로 특정 포즈를 주입하는 게 아니라 진짜 idle 풀 추첨을 통과시켜서) 시뮬레이션 시간을 빠르게 흘려보낼 수 있게 함. `test/launch-idle-cycle-flicker.js`(재사용 가능한 도구로 남겨둠)로 시뮬레이션 400초 상당(200개 체크포인트)을 돌리며 breathe/sit/sleep/tailtwitch/earflick/stretch/**groom**(10회 이상 등장 확인 — 낮은 가중치 idle까지 실제로 커버됐음을 확인) 전 idle을 거치게 하고, 매 체크포인트마다 **상태 변화 없이 스크린샷을 두 번 연속 캡처해서 바이트 비교** — 두 캡처 사이에 아무것도 안 바뀌었는데 픽셀이 다르면 그게 곧 깜빡임의 정의. **1차 시도에서 위양성 발견**: 캡처 사이 지연 없이 곧바로 두 번 캡처하니 가끔(200번 중 1~2번) 실제로 픽셀이 다르게 나왔는데, `groom`뿐 아니라 오시레이션이 전혀 없는 `breathe`에서도 나와서 — 렌더링 로직 문제가 아니라 이 프로젝트에 이미 기록돼 있던 `capturePage()` 자체의 알려진 타이밍 이슈(컴포지터가 아직 최신 프레임을 반영하기 전에 캡처될 수 있음, 위 "3D 복셀 공간 실험"의 batch 캡처 조사 항목 참고)로 의심 — 각 캡처 전에 60ms 여유를 주니 **200개 체크포인트 전부(3회 반복 실행 모두) 0건**으로 안정적으로 수렴, 캡처 하네스의 타이밍 아티팩트였다는 결론을 뒷받침. `npm run test:invariants` 재실행 — 위반 0건.

**5. 턱시도 고양이 등 색을 검정으로**: `cat_tuxedo`의 `pattern: [{x:12,y:13,w:5,h:3,color:'#ffffff'}]`(등/상체 위쪽에 흰 패치)를 필드 자체 삭제 — `bodyColor`가 이미 `#161616`(거의 검정)이라 검정 패치를 새로 얹으나 그냥 몸통색이 그대로 보이나 결과가 동일해서, 같은 색 패치를 덧그리는 대신 필드를 아예 없앰(가슴/배의 `belly`, 발끝의 `pawColor`는 요청 범위 밖이라 안 건드림 — 그대로 흰색 유지). `pattern`을 참조하는 2D(`drawCreature`)/3D(`buildVoxelCreature`) 둘 다 `if(cfg.pattern)` 가드가 있어서 필드 삭제만으로 양쪽 다 자동으로 반영됨, 별도 분기 수정 불필요.
- **검증**: `test/launch-tuxedo-check.js`(재사용 가능한 도구로 남겨둠)로 0°/45° 스크린샷 — 등 전체가 몸통과 이어진 단일 검정으로 보이고, 흰 가슴/배와 발끝 양말은 그대로 유지됨을 확인.

**전체 회귀 확인**: `npm run test:invariants`(120만 프레임, 공유 pose 로직을 여러 곳 건드렸으므로) 위반 0건. `main.js`/`preload.js`에 이번 라운드에서 추가한 임시 디버그 훅이 하나도 안 남아있음을 `git diff`로 확인(전부 독립 실행 스크립트로 검증하고, `pet.js`에 잠깐 추가했던 테스트 전용 훅 두 개(`__setDirectionForTest__`/`__getRotationDebug__`)도 검증 직후 제거). 실제 앱(`npm start`)을 띄워 몇 초간 정상 구동(크래시 없이 다중 Electron 프로세스가 안정적으로 유지됨) 확인 후 종료.

### 실제 사용 후 버그 리포트 5건 수정 (stage 4 이후 3차 QA 라운드)
2차 QA 라운드(위 5건, 사운드 볼륨/회전 방향/그루밍 와이퍼/샴 그라데이션/턱시도 색)를 고친 뒤 다시 실사용하고 나온 후속 리포트 5건 — 이번 라운드에서 가장 컸던 발견은 두 건(1번, 5번) 모두 "이 필드/이 케이스만" 고치는 патч가 아니라, 여러 증상의 진짜 공통 원인이 되는 구조적 버그였다는 것. 둘 다 사용자가 원인을 추측(1번: "리셋 로직에 groomWipe가 빠진 듯", 5번: "z-fighting으로 추정")했는데, 그 추측이 맞는 방향이었지만 실제 스코프는 사용자가 짐작한 것보다 훨씬 넓었음 — 조사 과정과 함께 정직하게 기록.

**1. 그루밍 후 발이 든 채로 고정됨 — "리셋 로직에 필드 하나가 빠짐"이 아니라 크로스페이드 자체의 구조적 버그**: 사용자 가설(`groomWipe`가 `enterIdle`/`enterWalk`의 리셋 로직에서 빠졌다)부터 확인 — `defaultPose()`/`POSE_FIELD_BOUNDS`엔 이미 `groomWipe: 0`/`groomWipe: 45`가 있었고, `resetPose()`도 `anim.pose = defaultPose()`로 매 전환마다 확실히 리셋하고 있었음 — 즉 사용자가 지목한 그 자리엔 실제로 버그가 없었음. 대신 `test/diag-groom-reset.js`(신규, 재사용 가능한 도구로 남김 — `live=1` 모드로 실제 상태머신을 구동하며 매 전환마다 `frontLegRaise`/`groomWipe`/`legsTuckedFront`/`legsTuckedBack`을 로깅)로 직접 재현해보니 **`frontLegRaise`가 첫 groom 이후 20+번의 후속 전환을 거치는 내내 정확히 1.000에 고정**됨을 확인 — 리셋 자체는 되는데 그 리셋이 어딘가에서 도로 덮어써지고 있다는 뜻.
  - **진짜 원인 (`applyPoseCrossfade`의 자기 참조 버그)**: `applyPoseCrossfade`가 매 프레임 `anim.pose[key] = lerp(from[key], anim.pose[key], t)`로 **`anim.pose`를 제자리에서(in place)** 블렌딩하고 있었음 — 문제는 `target = anim.pose[key]`가 "이번 프레임에 idle이 실제로 원하는 값"이 아니라 "**직전 프레임에 크로스페이드 자신이 써놓은 값**"을 읽는다는 것. 전환 첫 프레임엔 `resetPose()`가 막 `anim.pose`를 `defaultPose()`로 초기화했으니 `target`이 진짜 0으로 깨끗하지만(`lerp(1,0,0)=1`로 정상 크로스페이드 시작), 그 다음 프레임부터는 `resetPose()`가 다시 안 불리므로 `anim.pose.frontLegRaise`엔 **직전 프레임의 크로스페이드 출력(=1, 아직 안 줄어든 값)**이 그대로 남아있는데, 현재 idle(예: breathe)의 `apply()`는 애초에 `frontLegRaise`를 안 건드리는 필드라 그 값을 그대로 방치 — 결과적으로 crossfade의 `target`도 `start`(=1)도 똑같이 1이 되어 `lerp(1,1,t)=1`이 영원히 반복됨. `POSE_TRANSITION_DURATION`(0.25초)이 지나면 이 함수는 아예 실행을 멈추므로, 그 시점의 값(=1)에 **영구히 얼어붙음** — "일정 시간 뒤 멈추고 그 상태로 고정된다"는 증상과 정확히 일치. **매 프레임 idle이 명시적으로 설정하는 필드(bodyBob, tailPhase 등)에선 이 버그가 전혀 안 보였던 이유**: `updateContinuousGait`(걷기/트롯/왜들/스캠퍼 gait)의 idle 분기가 `anim.pose.bodyBob *= 0.8`처럼 **매 프레임 무조건 다시 쓰기** 때문에 크로스페이드의 오염된 값을 매번 덮어써서 우연히 자기 교정됨 — `frontLegRaise`/`groomWipe`는 groom 외 어떤 idle도, 어떤 gait 함수도 건드리지 않는 필드라 이 우연한 보호를 못 받았을 뿐. **직접 확인한 2차 파급 효과**: 같은 진단 스크립트로 `legsTuckedFront`/`legsTuckedBack`도 검사해보니 **`sit` 이후 0.8에 영구 고정**되는 걸 발견 — walk 계열 gait(`updateContinuousGait`)는 idle 분기에서 이 두 필드를 전혀 안 건드리기 때문(오직 hop gait의 `updateHopGait`만 `*=0.7`로 감쇠시킴). 즉 **고양이/강아지 등 walk 계열 종은 최초 1회 sit 이후로 계속 살짝 웅크린 채로 있었다는 뜻** — 사용자가 리포트한 그루밍 버그보다 실은 더 광범위하고 눈에 덜 띄는(다리가 완전히 안 접힌 애매한 각도라 "그냥 그런 자세인가" 싶었을) 버그가 같이 있었음.
  - **고침 (근본 구조 수정, 필드별 패치 아님)**: `update()`가 매 프레임 `raw = defaultPose()`로 **완전히 새 객체**를 만들고, 이 프레임에 실제로 연속성이 필요한 필드(아래)만 `anim.pose`(현재 화면에 보이는 값)에서 시딩한 뒤, gait 함수/현재 idle의 `apply()`/`updateTailFlavor`/`applyCursorLookEasing`이 전부 이 `raw` 위에 값을 씀 — `applyPoseCrossfade(anim, raw, dt)`가 `anim.transitionFrom`(전환 시점 스냅샷) → `raw`(이번 프레임의 진짜 의도값, 절대 오염 안 됨)를 블렌딩해서 그 결과를 `anim.pose`(표시용)에 **새 객체로 대입** — 크로스페이드 자신의 출력이 다음 프레임의 "의도값"으로 다시 읽히는 경로 자체가 원천 차단됨. 어떤 idle도 안 건드리는 필드는 매 프레임 `raw`에서 0으로 시작하므로 "이 behavior가 이 필드를 안 건드림 = 0"이라는 이 엔진의 기존 컨벤션이 이제 진짜로, 프레임 단위로 보장됨.
    - **연속성이 필요한 필드는 gait별로 다름 — 하나의 리스트로 뭉치면 또 버그가 남**: 처음엔 `legPhase`/`bodyBob`/`waddleTilt`(walk 계열 감쇠)와 `hopY`/`legsTuckedFront`/`legsTuckedBack`(hop 감쇠)를 **하나의 리스트**로 합쳐 무조건 시딩했는데, 재검증하니 `legsTuckedFront`/`legsTuckedBack`가 **여전히** sit 이후 0.8에 고정됨 — walk 계열 gait는 이 두 필드를 전혀 안 읽지도 감쇠시키지도 않는데 시딩만 해놨으니, 아무도 0으로 되돌려주지 않는 채로 계속 캐리오버된 것(정확히 같은 종류의 실수를 새 필드에 대해 반복한 것). **고침**: `LOOK_CONTINUITY_FIELDS`(`lookDX`/`lookDY`, 모든 종 공통 - 커서 시선 이징용)와 `CONTINUOUS_GAIT_FIELDS`(walk 계열)/`HOP_GAIT_FIELDS`(hop) 두 리스트로 분리하고, `update()`가 `anim.spec.gait === 'hop'` 여부로 **그 종의 실제 gait 함수가 정말 읽는 리스트만** 시딩하도록 수정.
  - **검증**: `test/diag-groom-reset.js`(cat_a, walk gait)와 그 변형(rabbit_b, hop gait)으로 재확인 — cat_a는 groom 종료 후 20프레임 이내 `frontLegRaise`/`groomWipe`가 정확히 0.000으로 돌아오고(수십 번의 후속 전환 내내 재확인), `sit` 이후 다음 전환에서 `legsTuckedFront`/`legsTuckedBack`도 정확히 0.000으로 돌아옴(전환 직후 크로스페이드 중인 프레임엔 여전히 0.8 근처 값이 보이는 게 정상 — 크로스페이드 자체는 살아있음, "고정" 문제만 해결). rabbit_b(hop gait)는 기존 설계대로 `legsTuckedFront`/`legsTuckedBack`가 hop 감쇠를 통해 **서서히** 줄어드는(즉시 0이 아니라 여러 프레임에 걸쳐) 기존 동작이 그대로 유지됨(의도적으로 안 건드림). `npm run test:invariants`(120만 프레임)/`test:movement-stats`/`test:idle-rhythm` 전부 재실행 — 위반 0건, walk% 0.0%/breathe 점유율·전환 빈도 전부 기존 베이스라인과 일치(회귀 없음). 실제 idle 상태머신을 구동해 groom→sleep/sit 전환을 3회 반복 촬영(`test/launch-groom-transition-final.js`, 재사용 가능한 도구로 남김) — 전환 직후 다리가 접히기 시작해 20프레임 뒤엔 완전히 정상 자세(다리 내려옴)로 돌아온 걸 스크린샷으로 확인.

**2. 발을 들 때 발 색깔이 회색으로 변함**: `applyGroomHighlight`(발을 들었을 때 흰색 쪽으로 최대 40% 블렌드해서 눈에 띄게 하는 장치)가 `pair.upperMesh`/`pair.lowerMesh`(다리 전체, 즉 위/아래 다리뼈)에도 **`cfg.pawColor` 유무와 무관하게 무조건** 호출되고 있었음 — 턱시도(`legColor:'#161616'`, 거의 검정)나 샴(`legColor:'#5c4636'`, 어두운 갈색)처럼 원래 다리색이 어두운 종은 이 40% 흰색 블렌드가 그대로 회색으로 보임. 애초에 이 하이라이트는 "발끝에 흰 양말처럼 밝은 마킹이 있는데 얼굴색과 비슷해서 헷갈릴 수 있다"는 상황(턱시도 하나만 해당 - `pawColor`가 있는 유일한 종)을 위한 보정이었는데, `pawMesh`(발끝, `cfg.pawColor`가 있는 종만 존재)뿐 아니라 다리뼈 전체에도 무차별 적용된 게 버그.
  - **고침**: `upperMesh`/`lowerMesh`에 대한 `applyGroomHighlight` 호출 두 줄을 완전히 제거 — 다리뼈는 어떤 포즈에서도 항상 `cfg.legColor` 그대로(다른 모든 포즈와 동일). `pawMesh`에 대한 호출만 유지(`if (pair.pawMesh)` 가드가 이미 있어 `pawColor` 없는 종은 애초에 `pawMesh` 자체가 없으므로 자동으로 아무 효과 없음). 로스터 중 `pawColor`가 있는 종은 턱시도 하나뿐(grep으로 확인) - 즉 이 수정 이후로는 턱시도의 이미 흰 발끝(흰→흰 블렌드라 사실상 무효과)을 빼면 어떤 종도 그루밍 중 다리 색이 안 바뀜, 가시성은 전적으로 `GROOM_PROTRUDE_Z`(Z축으로 얼굴 표면 밖까지 밀어내는 실질적 수정, 안 건드림)와 `groomWipe` 스윕 동작(2차 라운드에서 구현, 안 건드림)에 맡김.
  - **검증**: `test/launch-groom-paw-color-check.js`(재사용 가능한 도구로 남김) - `getLegDebug()`로 발을 든 상태의 `upperColor`/`lowerColor`를 직접 읽어서 턱시도는 정확히 `#161616`, 샴은 정확히 `#5c4636`(둘 다 각 종의 원래 `legColor`와 소수점 없이 완전 일치)임을 수치로 확인, 스크린샷으로도 회색기 없이 원래 색 그대로 보임을 확인.

**3/4. 샴 얼굴 그라데이션 "여전히 안 보임" + 깜빡임 재조사**: 사용자가 "지난번 검증했다던 것과 달리 실제로 보면 안 보인다"고 재확인을 요청 — 지난 라운드의 검증(정점 색상 attribute 덤프, 카메라 투영을 통한 픽셀 샘플 2점)을 신뢰하지 않고 **처음부터** 다시 확인.
  - **파이프라인 자체는 정상 연결돼 있음을 재확인**: `cfg.faceShade` 조건 분기(`buildVoxelCreature`의 `cfg.faceShade ? addFaceShadedHeadBox(...) : addBoxRel(...)`), 샴의 `faceShade: {highlight:'#a9825f', radius:6.5}` 설정, `MeshBasicMaterial({vertexColors:true})` 세팅 전부 코드상 문제없음을 재확인(사용자가 제시한 두 가설 - 머티리얼 미설정, 조건 분기 누락 - 둘 다 아니었음).
  - **실제 렌더링된 raw 프레임버퍼를 직접 스캔 (지난 라운드보다 훨씬 엄밀하게)**: 지난번엔 카메라 투영으로 계산한 2개 샘플 지점만 읽었는데, 이번엔 `pet3d.js`에 `getRawPixels()`(신규, 재사용 가능한 디버그 접근자 - WebGL 프레임버퍼 전체를 `gl.readPixels`로 그대로 덤프)를 추가해 머리 영역 전체를 가로줄 단위로 스캔(`test/diag-siamese-gradient-scan.js`, 신규) — **실제로 강한 그라데이션이 존재함을 확인**: 귀 근처(초점인 눈에서 먼 쪽)는 `(168,129,94)`(highlight `#a9825f`=169,130,95에 거의 정확히 도달), 눈 근처(초점 자체)는 `(92,70,54)`(headColor 그대로) — R채널 기준 76단위의 실질적인 스윙. **동일한 스캔을 실제 `windows/pet/pet.js` 창(프로토타입이 아니라 진짜 앱 렌더링 경로)에도 그대로 실행**(`test/diag-real-pet-siamese.js`, 신규) — 프로토타입과 완전히 동일한 수치, 렌더링 경로 차이로 인한 문제도 아님을 확인.
  - **그런데도 "안 보인다"고 느껴진 이유**: 스크린샷을 직접 봐도(96px 실제 크기) 그라데이션이 아주 미묘하게는 보이지만, 귀(딱딱한 단색 어두운 사각형)가 그라데이션의 가장 밝은 지점 바로 옆에 붙어있어서 "귀 옆이 밝다"는 인상이 "그라데이션이 있다"보다는 "우연히 그런가 보다"로 읽히기 쉬움 — 각도별 렌더링(`test/diag-siamese-angles.js`, 0/20/45/70/90/135/180도)을 비교해보니 **측면(0도)보다 45도·특히 90도(정면에 가까운 각도)에서 그라데이션이 훨씬 뚜렷하게 "얼굴 중앙이 어둡고 바깥이 밝은" 콜러포인트 형태로 보임**(초점인 눈이 화면 중앙에 투영되면서 방사형 낙차가 대칭적으로 펼쳐지기 때문) — 즉 구현 자체는 처음부터 정상이었고, 옆모습 각도에서 상대적으로 덜 두드러져 보였던 것뿐. **결론**: 코드/렌더링 버그는 발견되지 않음 — 실사용 중 가장 뚜렷하게 보이는 각도(정면에 가까운 회전)에서 다시 확인해달라고 안내할 수 있는 사안이었지, "연결이 안 됐다"는 가설은 기각.
  - **깜빡임은 여전히 재현됨 → 진짜 원인은 그라데이션이 아니라 5번(z-fighting)이었음**: `test/launch-idle-cycle-flicker.js`(지난 라운드 도구)를 이 라운드 초반(z-fighting 수정 전)에 재실행했을 때도 200개 체크포인트에서 0건으로 나왔었는데, 이건 이 테스트가 "완전히 같은 논리 상태를 두 번 캡처"만 확인하는 것이라 z-fighting처럼 **아주 작은 포즈 변화(예: breathe의 미세한 bodyBob 진동)에도 반응하는 종류의 불안정성은 애초에 못 잡는 종류**의 검증이었음(포즈를 고정해놓고 두 번 찍으면 z-fighting도 매번 "같은" 결과를 낼 수 있음 - 완전히 동일한 입력엔 GPU가 결정론적이므로). 실제로 "회전/미세 포즈 변화 중에 지지직거린다"는 사용자 표현과 맞아떨어지는 건 5번 항목에서 확인한 z-fighting 쪽이었고, 그 수정(`HEAD_Z_NUDGE`) 이후 재검증(`test/diag-zfight-final-check.js`)에서 각도를 아주 조금씩(0.1도 단위) 바꿔가며 비교했을 때도 머리/몸통 접합부의 색이 더 이상 안 뒤바뀜을 확인 — 3/4번 리포트가 사실상 하나(그라데이션 자체는 처음부터 정상, 깜빡임은 5번과 동일 원인)였다는 사용자의 추측이 맞았음, 단 "그라데이션이 매 프레임 재계산되며 깜빡인다"는 세부 메커니즘 가설은 틀림 - `addFaceShadedHeadBox`는 `buildVoxelCreature`가 캐릭터 로드 시 **딱 한 번만** 호출하고(정점 색상은 빌드 타임에 고정되는 `BufferAttribute`), 매 프레임 다시 계산되는 부분이 아예 없음을 grep으로 재확인.

**5. 회전할 때 목 주변이 지지직거림 (z-fighting) — 실제로는 목 자체가 아니라 "목이 있어야 할 자리에 아무 지오메트리도 없다"는 게 원인**: `voxel-engine.js` 전체를 grep해도 목(neck) 관련 코드가 단 한 줄도 없음 — 2D 엔진(`pxJoint` 기반 목 관절, "몸통과 머리 사이를 실제로 잇는" 지오메트리)과 달리, 3D는 머리 박스와 몸통 박스를 그냥 각자의 authored 2D 좌표대로 따로따로 배치할 뿐, 그 사이를 이어주는 어떤 다리 역할의 지오메트리도 없음. 문제는 이 둘의 authored 좌표가 **실제로 겹친다는 것**(예: cat_a는 bodyX/W=4/13→X:[4,17], headX/W=15/7→X:[15,22] - X축 2유닛 겹침, bodyY/H=13/6→Y:[13,19], headY/H=9/7→Y:[9,16] - Y축 3유닛 겹침) — 게다가 몸통·머리 둘 다 같은 `DEPTH`(6)로 Z=0 중심이라 **앞면이 정확히 같은 Z값(+HALF_DEPTH)에서 완전히 포개짐** — 이게 바로 z-fighting의 교과서적 조건(깊이값이 우연히 비슷한 게 아니라 정확히 동일한 두 불투명 폴리곤)임.
  - **왜 지금까지 안 걸렸나 (색이 같으면 z-fighting도 안 보임)**: `headColor`는 지정 안 하면 `bodyColor`로 폴백되므로, 로스터 10종 중 `headColor`가 `bodyColor`와 실제로 다른 종은 **샴**(`#5c4636` vs `#e8d9bb`)과 **허스키**(`#e8e6e0` vs `#5c6670`) 단 2종뿐 — 나머지 8종은 머리/몸통이 z-fight 하고 있어도 두 표면이 완전히 같은 색이라 어느 쪽이 "이겨도" 육안으로 차이가 없었을 뿐, 버그 자체는 로스터 전체에 걸쳐 항상 있었음. 정확히 사용자가 실사용 중 계속 마주쳤을 샴(2/4/5번 리포트 전부 샴 기준)에서 가장 먼저/뚜렷하게 드러난 이유.
  - **실측으로 확인**: `test/diag-neck-zfight.js`(신규) - 완전히 같은 정적 포즈를 **0.1도 단위**로 아주 조금씩만 회전시키며 연속 프레임을 비교, 실루엣 경계가 아닌(즉 배경도 아니고 진짜 가장자리도 아닌) 접합부 내부 픽셀이 큰 폭으로 색이 바뀌는지 검사 — 수정 전엔 0.1~0.6도 사이의 미세한 회전만으로도 샴·허스키 둘 다 접합부 픽셀 10여 개가 서로 다른 두 색(정확히 headColor ↔ bodyColor) 사이를 오가는 걸 확인, 회전을 전혀 안 시키고 완전히 같은 각도를 두 번 렌더링해도(`test/diag-zfight-final-check.js`) 우연히 같은 프레임 안에서도 재현 가능함을 확인.
  - **고침**: 목 지오메트리를 새로 만드는 대신(2D의 관절形 목을 3D에 옮기는 건 훨씬 큰 작업), 표준적인 z-fighting 대응인 "폴리곤 오프셋"을 적용 — 머리 박스를 카메라 쪽(+Z)으로 아주 살짝(`HEAD_Z_NUDGE = 0.08` 유닛, `HALF_DEPTH`=3의 3% 미만이라 육안으로는 안 보이는 수준) 밀어내서, 겹치는 영역에서 항상 머리 쪽이 명확히 더 가깝게(깊이값이 확정적으로 우세하게) 만듦 - 깊이버퍼가 매번 같은 결정을 내리게 되어 흔들림 자체가 구조적으로 불가능해짐. 일반 머리 박스(`addBoxRel` 호출, zOffset 인자 추가)와 샴의 `addFaceShadedHeadBox`(그라데이션 머리, 별도로 `mesh.position.set(...)`에 같은 오프셋 추가) 둘 다에 적용 - 후자를 빠뜨리면 하필 z-fighting이 가장 크게 보이던 샴만 여전히 안 고쳐지는 상황이 될 뻔했음.
  - **검증**: `test/diag-neck-zfight.js` 재실행 - 0.1~0.6도 구간의 흔들림 완전히 사라짐(0건), 0.6도 이상에서 남아있던 소수의 "큰 변화" 픽셀은 실제로 확인해보니 전부 배경↔실루엣 경계(정상적인 회전에 따른 윤곽선 이동)였지 headColor↔bodyColor 뒤바뀜이 아니었음(샘플 좌표/색상을 직접 출력해서 확인). `test/diag-zfight-final-check.js`로 4종(샴/허스키/일반 대조군 2종) × 9개 각도 × 완전 동일 상태 2회 렌더링 비교 - 합계 0바이트 차이. `test/launch-neck-rotation-final.js`(신규, 재사용 가능한 도구)로 샴/허스키 각각 0~60도 회전 구간을 목 부위만 확대(3배)해서 스크린샷 - 접합부 경계가 깨끗한 직선/직각으로 보이고 어떤 얼룩/톱니 패턴도 없음을 육안으로도 확인.

**전체 회귀 확인**: `npm run test:invariants`(120만 프레임)/`test:movement-stats`/`test:idle-rhythm` 전부 재실행 - 위반 0건, 기존 베이스라인과 일치. `main.js`/`preload.js`/`windows/pet/pet.js`에 이번 라운드에서 추가한 임시 디버그 훅 없음(이번 라운드는 전부 `pet3d.js` 프로토타입 뷰어의 재사용 가능한 디버그 접근자(`getRawPixels`, `stepIdle`/`getIdleDebug`는 지난 라운드부터 존재)와 독립 실행 스크립트만으로 검증, 실제 앱 파일은 안 건드림 - `git diff --stat`으로 확인). 실제 앱(`npx electron .`)을 띄워 정상 구동(콘솔 에러 없음, 프로세스 안정적으로 유지) 확인 후 종료.

### 실제 사용 후 버그 리포트 2건 수정 + 신기능 1건 (stage 4 이후 4차 QA 라운드)
3차 QA 라운드(그루밍 리셋/발 회색/샴 그라데이션/목 z-fighting 5건)를 고친 뒤 다시 실사용하고 나온 후속 리포트 1건 + 신규 기능 요청 1건.

**1. 샴 고양이 얼굴 그라데이션이 실제로는 여전히 안 보임 — 검증 방법 자체를 바꿔서 재확인 (버그는 없었음, 대비가 너무 약했음)**: 사용자가 이번엔 실제 스크린샷(`assets/reference/siamese-face-still-flat.png`)까지 첨부하며 "지난번 검증(코드 상 vertex color 배열 확인)과 실제 렌더링 결과가 어긋난 것 같다"고 재확인 요청 — 지난 라운드까지는 카메라 투영으로 계산한 2~3개 샘플 지점만 읽었는데, 이번엔 사용자 요청대로 아예 검증 방법 자체를 바꿈.
  - **머티리얼/메시 중복 여부부터 재확인**: `buildVoxelCreature`의 머리 생성 코드를 처음부터 다시 읽어 `headMesh`가 `cfg.faceShade` 유무에 따라 `addFaceShadedHeadBox`(vertexColors 머티리얼) **아니면** `addBoxRel`(단색 머티리얼) 둘 중 **하나만** 생성하는 걸 재확인 — 두 메시가 동시에 만들어지거나 다른 메시가 겹쳐 그려지는 경로는 코드상 존재하지 않음. 직전 라운드의 `HEAD_Z_NUDGE`(z-fighting 수정) 커밋도 다시 검토 — 일반 머리(`addBoxRel`)와 그라데이션 머리(`addFaceShadedHeadBox`) **양쪽 다** 오프셋이 적용돼 있고 후자가 빠진 것도 아님 확인.
  - **실제 화면 픽셀을 직접 읽어서 확인 (사용자가 요청한 방식 그대로)**: `test/diag-siamese-real-pixel-check.js`(신규) — 실제 `windows/pet/` 창을 띄워 `#pet-canvas`의 `gl.readPixels`로 **캔버스 전체**를 스캔하고, 나오는 모든 불투명 색상을 픽셀 수와 함께 집계 — 결과: 서로 다른 헤드컬러↔하이라이트 사이 중간색이 **54종류, 440픽셀** 존재(플랫한 렌더링이라면 이 사이 색이 거의 없어야 함) — 즉 그라데이션 자체는 파이프라인에 정상 연결돼 있고 실제로 렌더링되고 있음을 재확인(사용자의 "혹시 다른 메시가 덮어 그리는지" 가설과 "머티리얼 미설정" 가설 둘 다 기각). 사용자가 첨부한 실제 스크린샷도 직접 크롭/픽셀 샘플링해서 대조 — 거기서도 동일하게 어두운 단색 구간(귀+눈 주변, headColor 그대로)과 중간톤 그라데이션 구간(140~168 범위, 점진적)이 둘 다 존재함을 확인.
  - **그런데 왜 "안 보이는 게" 맞았나 — 버그가 아니라 대비가 실제로 부족했음**: 54개의 서로 다른 색이 존재해도 인접한 두 색끼리는 RGB 1~3단위 차이뿐이라(구 설정 `radius:6.5`가 머리 자체 대각선(~9.9유닛)보다 좁긴 해도 초점에서 가장 먼 실제 모서리(~7.2유닛)보다는 넓어서, falloff가 머리 전체에 걸쳐 "끝까지 도달하지 못한 채" 아주 완만하게만 퍼져 있었음) 사람 눈에는 사실상 "그냥 좀 칙칙한 갈색 한 덩이"로 읽혔던 것 — 세 번째로 같은 리포트가 들어온 걸 감안해 이번엔 "기술적으로 맞다"는 재확인에서 멈추지 않고 실제로 대비를 강화함: `radius`를 6.5→4(초점에서 4유닛만 지나면 완전히 하이라이트색에 도달 — 머리의 상당 부분이 "확실히 밝은" 영역으로 남도록), `highlight`를 `#a9825f`(169,130,95, headColor 대비 겨우 77/60/41 밝음)에서 `#cdac7c`(205,172,124, 113/102/70 밝음)로 상승 — 둘 다 `faceShade`가 2D/3D 공유 설정이라 양쪽 렌더러에 동시 반영됨.
  - **재검증**: 같은 픽셀 스캔 재실행 — 이제 헤드컬러(92,70,54, 440픽셀)와 하이라이트 근방(200,168,122 부근, 300+픽셀)이 뚜렷이 분리된 두 덩어리로 나뉘어 나타남(중간색 자체는 오히려 줄었는데, 이는 falloff가 더 좁은 반경 안에서 빨리 끝나 "완전히 밝은" 영역이 넓어졌기 때문 — 의도한 결과). 스크린샷으로도 귀 옆의 밝은 크림색 패치와 눈 주변의 짙은 seal-brown 마스크가 한눈에 구분됨(`test/launch-siamese-gradient-final.js`, 신규) — 90도 근접 각도에서는 실제 샴 고양이의 "다크 마스크" 마킹과 거의 흡사한 형태로 보임. 2D 디더링 버전도 같은 설정을 공유하므로 같이 재렌더링해 확인(`test/render-siamese-2d-check.mjs`, 신규) — 눈 주변에 눈에 띄게 조밀한 디더 패턴이 생겨 대비가 강해진 게 그대로 반영됨, 깨짐 없음. `npm run test:invariants`(120만 프레임) 재실행 — 위반 0건(색상 값만 바뀐 순수 데이터 변경이라 지오메트리 invariant엔 영향 없음, 예상대로).

**2. (신규 기능) 커서 방향 회전을 좌/우 이분법에서 각도 기반 연속 회전으로**: 기존엔 `direction`(±1)이라는 이진 값이 `updateBodyDirection()`(커서 dx에 히스테리시스 데드존을 적용해 부호만 결정)을 거쳐 `render()`에서 딱 두 개의 목표각(`0` 또는 `FACING_LEFT_Y`=-π) 중 하나로만 이어졌음 — 그 사이를 부드럽게 잇는 이징(`ROTATION_EASE_RATE`)은 이미 있었지만 도착지 자체는 항상 완전 측면 둘 중 하나뿐이었음.
  - **연속 각도 매핑 설계**: 커서 hint 계산에 이미 쓰이던 것과 동일한 `clampedDx`(펫 중심 기준 커서 dx를 `CURSOR_DIRECTION_RANGE`로 정규화해 -1..1로 클램프한 값)를 몸통 회전에도 그대로 재사용 — `targetRotationY = (clampedDx - 1) * (Math.PI/2)`라는 단순 선형 매핑 하나로: `clampedDx=+1`(커서가 오른쪽으로 완전히 벗어남) → `0`(완전 오른쪽 측면, 기존 `direction=1`과 정확히 같은 도착지), `clampedDx=-1`(완전 왼쪽) → `-π`(완전 왼쪽 측면, 기존 `direction=-1`과 동일), `clampedDx=0`(커서가 펫 정확히 위/아래) → `-π/2`(카메라를 정면으로 바라봄) — 그 사이 모든 값은 실제 대각선 중간각. 목표각이 항상 `[FACING_LEFT_Y, 0]=[-π,0]` 범위 안에만 머물기 때문에, 직전 라운드에 고친 "회전 중간에 뒤통수가 보이는" 버그(부호를 -π로 잡아야 얼굴이 회전을 리드하는 이유)의 전제가 연속 버전에서도 그대로 성립 — 별도의 wraparound 처리 없이 기존 부호 선택 하나로 전 구간 커버됨.
  - **`updateBodyDirection()` → `updateBodyRotationTarget()`로 교체**: 커서 dx로부터 `direction`(이진)을 정하던 로직을, 같은 dx로부터 `bodyRotationTargetY`(연속값, 새 모듈 스코프 변수)를 정하는 로직으로 완전히 교체 — `direction` 자체는 건드리지 않고 그대로 남겨둠(여전히 dormant patrol-bounce 코드가 매 프레임 `direction=1`로 되돌리는 채로 방치 — 이 변수를 실제로 읽는 곳은 이제 그 dormant 코드뿐이고, `advance`가 항상 0이라 어떤 값이어도 시각적 차이가 없으므로 안전). `DIRECTION_FLIP_HYSTERESIS`(구 이진 로직의 데드존 상수)는 삭제 — 연속값 사이에는 "튈" 이산 상태 자체가 없고, 목표값의 미세한 흔들림은 이미 있는 `ROTATION_EASE_RATE` 지수 이징이 그대로 저역통과 필터 역할을 하므로 별도 데드존이 불필요해짐.
  - **검증**: `test/launch-continuous-rotation-test.js`(신규, 재사용 가능한 도구) — `main.js` 없이 `windows/pet/` 창을 단독 로드하고, 실제 `mousemove` DOM 이벤트를 여러 `clientX`(dx=+1.0/+0.6/+0.3/0/-0.3/-0.6/-1.0에 해당하는 좌표)로 직접 디스패치(합성 테스트 훅 불필요 - pet.js의 기존 `window` 레벨 리스너가 그대로 받음), 각 값이 충분히 정착할 때까지(900ms, `ROTATION_EASE_RATE=8` 기준 95% 수렴 시간의 2배 이상) 기다린 뒤 임시 디버그 훅(`__getRotationDebug2__`, 검증 후 제거)으로 `bodyRotationTargetY`/`currentRotationY`를 직접 읽음 — 결과가 설계값과 **소수점 단위까지 정확히 일치**(dx=+0.6→-36.00°, dx=+0.3→-63.00°, dx=0→-90.00°, dx=-0.3→-117.00°, dx=-0.6→-144.00°, 전부 선형). 같은 시퀀스로 캡처한 스크린샷에서도 완전 측면(오른쪽)→대각선(3/4 view, 몸 폭이 줄고 깊이감이 보임)→정면(머리와 몸통이 거의 대칭으로 카메라를 향함)→반대쪽 대각선→완전 측면(왼쪽) 순으로 실루엣이 매끄럽게 이어지는 것을 육안으로도 확인 — 예전처럼 "완전 측면 아니면 반대쪽 완전 측면" 두 상태만 존재하던 것과 확연히 다름. 사용자 요구사항대로 머리/시선(`lookDX`/`lookDY`, `updateCursorHint`/`applyCursorLookEasing`)은 이번 라운드에서 전혀 안 건드림 - `updateBodyRotationTarget()`은 `animal.setCursorHint()`를 호출하는 `updateCursorHint()`와 완전히 분리된 별도 함수.

**전체 회귀 확인**: `npm run test:invariants`(120만 프레임) 위반 0건. `main.js`/`preload.js`에 이번 라운드 임시 디버그 훅 없음(`git diff --stat`으로 확인), `pet.js`에 잠깐 추가했던 `__getRotationDebug2__`도 검증 직후 제거 확인. 실제 앱(`npx electron .`)을 띄워 정상 구동(GPU 캐시 디렉토리 관련 Chromium 경고 몇 줄 외 콘솔 에러 없음 - 이 세션에서 동시에 여러 인스턴스를 반복 실행한 부작용일 뿐 애플리케이션 코드와 무관, 프로세스 자체는 끝까지 안정적으로 유지) 확인 후 종료.

### 샴 고양이 그라데이션, 근본 원인부터 재조사 (stage 4 이후 5차 QA 라운드)
4차 QA 라운드까지 세 번(4차/5차/6차 라운드에 걸쳐 실질적으로 세 번) "코드상 그라데이션이 있다"고 검증했는데도 실제 화면에서 계속 "안 보인다"는 리포트가 반복됨 — 이번엔 사용자가 직접 근본 원인 가설(정점 부족)을 제시하며 4단계 조사/수정 순서를 지정, 실제 사진(`assets/reference/siamese-real-photo.webp`)도 첨부. 순서대로 진행하고 매 단계 실제 렌더링 스크린샷으로 확인.

**1단계 — 머리 지오메트리의 정점 개수/배치 확인**: `pet3d.js`의 기존 `getFaceShadeVertexColors()` 디버그 접근자로 직접 덤프 — `new THREE.BoxGeometry(headW,headH,DEPTH)`(세그먼트 인자 없음, 기본값 1)가 만드는 24개 정점 엔트리(6면×4개) 중 **고유한 (x,y) 위치는 단 4개뿐**((22,9),(22,16),(15,9),(15,16) — 머리 사각형의 네 모서리)임을 확인 — 사용자의 가설이 정확했음. per-vertex color가 아무리 "정확한" 값을 각 모서리에 계산해 넣어도, 그 사이는 GPU가 그냥 4개 모서리 사이를 이중선형(bilinear) 보간할 뿐이라 — 그동안 "코드는 맞는데 화면에서 안 보인다"던 불일치의 진짜 정체.

**2단계 — 세분화 실험**: `BoxGeometry(...,20,20,1)`로 width/height 세그먼트를 20씩 늘려 재확인 — 정점이 1,050개(고유 위치 441개, 21×21 격자)로 늘어남. 렌더링해서 확인한 결과:
  - **0°(기본 휴식 자세, 옆모습)에서는 확실히 개선됨**: 6배 확대 크롭에서 눈 쪽으로 갈수록 어두워지는 게 실제로 부드럽게 보임 — BoxGeometry가 만드는 6개 면 중 폭×높이(headW×headH) 전체를 그대로 담당하는 **+z/-z(앞/뒤) 면**은 20×20 세그먼트를 온전히 받기 때문.
  - **90°(정면에 가까운 각도)에서는 여전히 납작한 블록으로 보임**: 이 각도에서 카메라를 향하는 건 **+x(코쪽 끝) 면**인데, 이 면은 폭방향 세그먼트가 아니라 `depthSegments`(1로 고정) × `heightSegments`(20)를 받음 — 게다가 이 면 위의 모든 정점은 x2d가 **딱 한 값(headX+headW, 눈 위치와 거의 같음)으로 고정**되어 있어서, 아무리 세분화해도 y2d 방향 1차원 변화만 표현 가능하고 진짜 2D 방사형 패턴 자체를 절대 담을 수 없음 — 세분화로 고칠 수 있는 문제가 아니라 박스라는 도형 자체의 구조적 한계. 이 90°는 이 파일 자체 설계 의도상(모듈 코멘트의 "Z=LEFT-RIGHT... 90°/270°에서 좌우로 분리되는" 부분) 우연한 각도가 아니라 의도된 정규 시야각이고, 4차 라운드에서 도입된 연속 회전 기능 때문에 실사용 중 자주 지나가게 됨 — 그냥 넘어갈 수 없는 문제로 판단.

**3/4단계 — 텍스처 방식으로 전면 재작성**: 2단계 결과(주요 각도인 0°는 개선, 마찬가지로 자주 보이는 90°는 구조적으로 못 고침) + 이마 줄무늬처럼 정점 보간으로는 표현하기 어려운 날카로운 모양이 추가로 필요하다는 점을 근거로 사용자 지침대로 텍스처 방식으로 전환. **하이브리드 설계** (완전히 새 지오메트리를 만드는 대신 기존 구조 재사용): `addFaceShadedHeadBox`가 이제 캔버스에 방사형 그라데이션 + 이마 줄무늬를 직접 그려 `THREE.CanvasTexture`로 만들고, `THREE.Mesh`에 **면별로 다른 머티리얼 배열**(`[vertexColorMat,vertexColorMat,vertexColorMat,vertexColorMat,texturedMat,texturedMat]`, BoxGeometry의 6면 그룹 순서 +x,-x,+y,-y,+z,-z에 대응)을 넘겨 — 주 시야각인 +z/-z(0°/180°)엔 픽셀 단위로 정확한 캔버스 텍스처를, 나머지 4면(+x/-x/+y/-y, 90°/270°와 위/아래 — 훨씬 드묾)엔 2단계에서 이미 확인한 세분화된 vertex color 폴백을 그대로 남김. UV는 BoxGeometry의 기본값을 신뢰하는 대신 `(x2d,y2d)`로부터 직접 계산해서 수동으로 설정(`uv.setXY(i, (x2d-headX)/headW, (y2d-headY)/headH)`) — THREE 내부 UV 컨벤션을 추측할 필요 없이 텍스처를 그리는 함수의 좌표 공식과 정확히 짝이 맞도록. 이마 줄무늬(`cfg.faceShade.stripe`, 신규 필드 — 색/너비/높이)는 참고 사진 기준으로 authored, 귀 사이 정중앙 최상단에 크림색 사각형 하나로 표현(귀 자체는 여전히 별도의 단색 박스라 그라데이션과 완전히 무관 — 요청사항 그대로 유지됨).
  - **버그 발견 (색 공간) — 처음엔 오히려 더 흐리게 나옴**: 텍스처를 처음 적용했을 때 오히려 색이 밝게 씻겨나간(washed-out) 것처럼 보임 — 픽셀 스캔으로 확인해보니 "밝은" 영역의 색이 (232,214,185) 근처로, headColor(92,70,54)와 highlight(205,172,124) 둘 중 어느 쪽으로도 단순 보간해서는 절대 나올 수 없는, 두 끝점보다도 더 밝은 값이었음 — 2-색 그라데이션의 산술적으로 불가능한 결과. 원인: **`THREE.CanvasTexture`의 기본 `colorSpace`가 `NoColorSpace`("이미 선형 색공간이라고 가정, 변환 안 함")인데, 캔버스 2D가 실제로 그리는 픽셀 데이터는 sRGB 인코딩**이라 — 렌더러의 최종 sRGB 출력 변환이 이미 sRGB인 데이터 위에 한 번 더 적용되면서 전체적으로 밝아짐(정확히 이전 라운드에 `THREE.Color`의 내부 선형 저장 때문에 겪었던 것과 같은 종류의 "이 숫자가 대체 어느 색공간이냐" 함정 — 그때는 vertex color 속성이었고 이번엔 텍스처 속성이라는 차이뿐). **고침**: `texture.colorSpace = THREE.SRGBColorSpace`를 명시적으로 설정. 재검증 결과 "밝은" 영역이 정확히 하이라이트 색(205,172,124, 설정값과 완전히 일치)에, 진짜 어두운 중간톤(123~154 범위 다수)도 실제로 등장 — 83개의 서로 다른 중간색이 204픽셀에 걸쳐 존재(색공간 버그가 있던 상태에서는 중간색이 아예 0개였음).
  - **검증**: 고친 뒤 6배 확대 크롭에서 눈 근처의 짙은 seal-brown이 귀 쪽으로 갈수록 부드럽게 크림색으로 옅어지는 게 뚜렷이 보이고, 이마 줄무늬도 귀 바로 아래 크림색 띠로 확인됨. 0°(옆모습)와 90°(정면에 가까운 각도) 둘 다 스크린샷으로 확인 — 90°는 여전히 폴백(vertex color)이지만 2단계에서 이미 확인했듯 눈/코 부위를 중심으로 어두운 마스크가 잘 형성돼 있어 실사용에 문제없는 수준. 참고 사진과 3분할 비교 이미지(`test/build-comparison.js`→`test/siamese-comparison.png`, 재사용 가능한 도구로 남김)를 직접 만들어 나란히 놓고 확인 — 사진 특유의 "눈코입 주변 짙은 마스크 + 뺨/이마로 갈수록 밝아짐 + 귀 사이 크림색 줄무늬 + 짙은 단색 귀 + 파란 눈" 패턴이 이제 육안으로도 뚜렷하게 대응됨(이전까지의 단색 진갈색 블록과는 확연히 다름). `npm run test:invariants`(120만 프레임) 재실행 — 위반 0건, 2D 렌더링(`test/render-siamese-2d-check.mjs`, 신규 — `cfg.faceShade.stripe` 필드가 추가돼도 2D의 `pxDitherRadial` 호출은 `highlight`/`radius`만 읽으므로 영향 없음 재확인)도 정상.

**전체 회귀 확인**: `npm run test:invariants`(120만 프레임) 위반 0건. `main.js`/`preload.js`/`pet.js` 전부 이번 라운드에서 안 건드림(`git diff --stat`으로 확인) — 전부 `voxel-engine.js`/`animal-engine.js`(SPECIES 테이블에 `stripe` 필드 추가)와 `pet3d.js`의 기존 디버그 접근자만으로 검증. 실제 앱(`npx electron .`)을 띄워 정상 구동 확인 후 종료.

### Z축 활용 + 정면 디테일 보강 + 설정 실시간 미리보기 (stage 4 이후 6차 QA 라운드 겸 신기능 3건)
5차 QA 라운드(샴 그라데이션 텍스처 재작성) 이후, 사용자가 버그 리포트가 아니라 신규 기능 3건을 한 번에 요청 — 순서대로 처리.

**3. 설정 화면 실시간 미리보기 (가장 단순)**: 캐릭터 그리드를 클릭하면 [저장]을 누르기 전에도 실제 펫 창에 즉시 반영되고, 저장 없이 설정창을 닫으면 원래 캐릭터로 되돌아가야 함. `preload.js`에 `previewCharacter(key)`(설정→메인)/`onPreviewCharacter(cb)`(메인→펫) 채널 추가, `main.js`에 `ipcMain.on('preview-character', ...)`가 그대로 펫 창에 릴레이. `settings.js`의 캐릭터 버튼 클릭 핸들러와 "기본값으로" 버튼 둘 다 `window.focusPetAPI.previewCharacter(key)`를 호출(저장 없이 임시 적용). 되돌리기는 이미 있던 `settingsWindow.on('closed', ...)` 핸들러(설정창이 어떻게 닫히든 — 저장/취소/OS 닫기 버튼 상관없이 — 항상 실행됨)가 `settings-updated` 이벤트를 펫 창에 다시 보내는 것을 그대로 재사용 — `settings` 변수 자체는 `save-settings` 핸들러만 변경하므로, 저장 없이 닫히면 `settings`는 여전히 이전 값 그대로라 이 핸들러가 자동으로 "원래 값으로" 되돌리는 효과를 냄(미리보기 전용 로직을 따로 안 만들어도 됨). `windows/pet/pet.js`엔 `onPreviewCharacter` 핸들러 한 줄만 추가(`key !== character`일 때만 `loadCharacter(key)` — 이미 있는 캐릭터 전환 함수 재사용, 초기 로드/저장 후 갱신 두 경로 모두 같은 함수를 거치므로 별도 분기 불필요).
  - **검증**: `test/launch-preview-test.js`(신규, 재사용 가능한 도구) — `main.js` 없이 pet+settings 두 창을 직접 띄우고 동일한 IPC 핸들러를 재구현해 실제 `preload.js`/`pet.js`/`settings.js` 코드를 그대로 구동. `dog_husky` 버튼을 실제 DOM 클릭으로 눌러 펫 창 캔버스의 불투명 픽셀 평균 색(`gl.readPixels` 지문)이 `cat_a`(주황)에서 바뀌는지 확인 → 저장 없이 `settingsWindow.close()` → 다시 지문을 찍어 정확히 원래 `cat_a` 값으로 복귀하는지 확인(둘 다 성공) → 이어서 `rabbit_b`를 미리보기 후 실제로 [저장] 버튼까지 눌러 계속 `rabbit_b`로 남아있는지(정상 저장 경로가 안 깨졌는지)도 같은 세션에서 확인. **테스트 하네스 자체의 버그**: 첫 시도에서 "미리보기는 되는데 복귀가 안 된다"는 결과가 나왔는데, 원인은 앱 코드가 아니라 테스트 하네스가 `main.js`의 `settingsWindow.on('closed', ...)` 핸들러를 안 옮겨왔던 것 — 그대로 복사해서 고침(실제 앱 코드는 처음부터 정상이었음).

**2. 정면 디테일 보강**: 귀/눈은 정면에서 입체감이 살도록, 고양이는 수염, 다른 종은 실제 동물 특징(강아지 콧구멍, 토끼 인중, 햄스터 볼주머니)을 반영. `voxel-engine.js`에 전부 구현, `animal-engine.js`의 SPECIES 테이블에 종별 플래그만 추가(`whiskers:true` 고양이 4종, `noseDetail:'nostrils'` 강아지 4종, `noseDetail:'philtrum'` 토끼).
  - **눈동자(pupil)**: 기존 iris(eyeColor)/하이라이트 사이에 작은 pupil 박스 추가. cat_a로 처음 확인했을 때 눈에 띄는 링이 안 보여서 버그인 줄 알았는데, `getRawPixels` 계열 디버그로 직접 픽셀을 찍어보니 cat_a는 애초에 `eyeColor`와 `darkColor`가 완전히 같은 값(`#2a1e16`)이라 iris와 pupil이 같은 색으로 그려지는 게 **디자인상 원래 그런 것**(이번에 생긴 문제 아님) — `dog_husky`(`eyeColor:'#7ec8e8'` vs `darkColor:'#1c2024'`, 확실히 다른 색)로 재확인해 pupil 자체 구현은 정상 작동함을 확인.
  - **귀 안쪽 음영**: `cfg.ear.inner`를 읽어 귀 메시의 자식으로 작은 안쪽 색 박스를 추가 — 이 필드는 사실 예전 라운드에 포메라니안/허스키/토끼 3종에 이미 authored돼 있었는데 그 어떤 렌더러(2D도 3D도)도 안 읽던 죽은 데이터였음, 이번에 그 기존 데이터를 그대로 재활용. `ear.inner`가 없는 나머지 7종은 새로 만든 `lightenColor(hex, amount)`(THREE.Color를 흰색 쪽으로 lerp)로 귀 색보다 밝은 값을 계산해 폴백.
  - **수염(고양이 4종) — 축을 잘못 잡아서 정면에서 안 보이던 버그**: 1차 구현은 수염 막대의 긴 축을 로컬 X(코-꼬리 방향)로 잡음 — 이 파일의 "Z축이 좌우 대칭축, 90°/270° 회전에서 화면 가로폭이 됨" 컨벤션과 정확히 반대라, 옆모습(0°)에서는 그럴듯하게 보였지만 **요청의 핵심인 정면(270°)에서는 수염 전체가 카메라를 향해 그대로 찌그러들어(foreshorten) 완전히 사라짐** — 실제 렌더링해서 발견(정면 스크린샷에 수염 자체가 없음). 막대의 긴 축을 로컬 X에서 Z로 바꾸고(`makeBox(THICKNESS,THICKNESS,LENGTH,...)`), 위/아래로 펼치는 `WHISKER_TILTS_DEG`는 회전축을 Y→X로, 코 쪽으로 살짝 모으는 `WHISKER_SPLAY_DEG`는 Z→Y로 맞바꿔 재배선 — 실제 고양이 수염이 좌우로 뻗는 것과도 맞고, 정면에서 가장 잘 보여야 한다는 요청과도 맞음. 재검증 결과 정면(270°)에서 양쪽 뺨에서 부채꼴로 뻗어나가는 게 뚜렷이 보이고, 옆모습(0°)에서는 반대로 끝쪽만 짧게 보임(실제 고양이를 옆에서 봐도 수염이 거의 안 보이는 것과 같은 원리 — 버그가 아니라 자연스러운 결과).
  - **강아지 콧구멍 — 두 가지 서로 다른 "왜 안 보이지" 버그를 순서대로 발견**: 코 박스 옆에 작은 점 2개를 좌우 대칭(눈/치크와 같은 "Z=±오프셋" 패턴)으로 추가하는 설계 자체는 처음부터 맞았음.
    1. **색이 코와 완전히 같아서 안 보임**: 점 색을 `cfg.darkColor`로 뒀는데, 코 색도 지정 안 하면 `cfg.nose.color || cfg.darkColor`로 폴백돼 대부분 종에서 똑같은 `darkColor`가 됨 — MeshBasicMaterial은 조명 반사가 전혀 없어 같은 색끼리는 어떤 각도에서도 이음매가 안 보임(그림자/하이라이트가 없으므로). `darkenColor(hex, amount)`(`lightenColor`의 반대, 검정 쪽으로 lerp)를 새로 만들어 `darkenColor(baseNoseColor, 0.55)`로 코 색 자체보다 확실히 어두운 색을 씀.
    2. **색을 고쳐도(디버그 마젠타로 바꿔도) 여전히 안 보임 — 진짜 원인은 두께가 얇은 축을 잘못 골랐던 것**: `getRawPixels`로 96x96 캔버스 전체를 스캔해도 마젠타 픽셀이 정확히 0개 — 색 문제가 아니라 렌더링 자체가 안 됨. 좌표를 손으로 역추적해보니, 점의 지오메트리를 `makeBox(dotSize,dotSize,0.12,...)`(로컬 Z만 얇은 납작한 판)로 만들었는데, 정면(270°) 회전에서는 로컬 Z가 화면 가로폭이 되는 바로 그 축이라 — 옆에서 보면 정상 크기(dotSize×dotSize)인 점이 정면에서는 두께 0.12칸(96px 캔버스에서 1픽셀 미만)로 짜부라져 사실상 렌더링될 픽셀이 없었음 — 위 수염 버그와 겉보기 증상은 다르지만(수염은 "축 방향이 반대", 콧구멍은 "얇은 쪽이 하필 그 축") 근본 원인은 같은 종류("이 축이 정면에서 화면 가로폭이 된다"는 걸 깜빡함). 점을 얇은 판 대신 `dotSize` 세 축 모두 동일한 정육면체로 바꿔서 고침(코 표면을 벗어나야 한다는 기존 Z-오프셋 공식도 정육면체의 커진 반두께에 맞춰 재계산).
    3. **토끼 인중(philtrum)은 우연히 이 버그를 안 겪음**: 처음부터 "코 뒤 스킨랩(주둥이) 표면을 앞뒤 양쪽에서 다 벗어나야 한다"는 다른 이유로 깊이(Z)를 `2*(DEPTH*0.25+0.15)`(=3.3, 상당히 두꺼움)로 잡아뒀는데, 이게 결과적으로 "정면에서도 화면 가로폭이 충분히 두꺼운" 조건을 우연히 만족시켜서 별도 수정 없이 1차 시도부터 정상 렌더링됨 — 즉 같은 실수를 두 번 안 한 게 아니라, 다른 이유로 세운 설계가 이 버그 자체를 원천적으로 피해간 경우.
  - **햄스터 볼주머니**: 요청에 언급된 "볼주머니가 정면에서도 보이게"는 별도 코드 없이 이미 확인됨 — 볼주머니는 이전 정면/후면 디테일 라운드에서부터 이미 눈/코와 같은 "Z=±오프셋 좌우 대칭" 패턴으로 만들어져 있었어서, 이번 라운드에선 최종 정면 스크린샷으로 재확인만 하고 코드는 안 건드림.
  - **검증**: `test/launch-final-frontcheck.js`(신규, 재사용 가능한 도구)로 10종 전부 정면(270°) 스크린샷(`test/frontdetail-check/final-*.png`) 확보 — 고양이 4종 전부 수염+눈동자+귀 음영, 강아지 4종 전부 콧구멍, 토끼 인중, 햄스터 볼주머니가 한 화면에서 동시에 확인됨. 콧구멍은 추가로 `pet3d.js`에 새로 추가한 `getNoseDetailDebug()`(코 그룹 자식들의 실제 월드 좌표/크기/색을 덤프)로 수치까지 재확인, 실제 화면 픽셀에 마젠타 디버그 컬러가 어디에도 없다는 것도 `gl.readPixels` 전체 스캔(`test/diag-nostril-pixels.js`, 신규)으로 재확인.

**1. Z축 활용 idle 행동 5종 추가**: 지금까지 펫이 쓰는 좌표는 사실상 X(좌우)/Y(위아래)뿐이었고 Z(카메라 앞뒤 깊이)는 회전에만 관여할 뿐 움직임에는 전혀 안 쓰이고 있었음 — 이걸 실제로 쓰는 idle 행동을 종별로 하나씩 추가.
  - **새 pose 필드 4개** (`animal-engine.js`의 `defaultPose()`/`POSE_FIELD_BOUNDS`): `depthZ`/`lateralX`(그리드 유닛, 월드 좌표 — 2D `drawCreature`는 카메라/깊이 개념이 없어 그냥 안 읽음, 다른 어떤 pose 필드가 특정 렌더러에서만 의미 있는 것과 같은 취급), `spinOverride`(boolean)/`spinAngle`(라디안) — idle 행동이 커서 추적 회전 대신 몸 전체 방향을 직접 결정하고 싶을 때 쓰는 오버라이드 쌍, `tailOverride`와 같은 구조. `spinAngle`은 여러 바퀴를 돌아도 절대 wrap(모듈로)하지 않음 — wrap하면 pet.js의 `FACING_LEFT_Y=-π` 부호 선택(회전 도중 뒤통수 대신 얼굴이 리드하게 만든 4차 라운드의 그 수정)이 지키던 전제가 깨져서 "먼 길로 돌아가는" 글리치가 재발할 수 있음.
  - **`voxel-engine.js`의 `applyPose`**: 매 프레임 `outer.position.set(lateralX,0,depthZ)`(월드 공간 오프셋 — `outer`는 pet.js가 `.rotation.y`만 세팅하는 바로 그 객체지만, Three.js의 TRS 합성 순서상 회전 다음에 위치가 적용되므로 몸이 어느 쪽을 보고 있든 항상 "카메라 기준" 앞뒤/좌우로 드리프트함)과 `outer.scale.set(s,s,s)`(`s=1+depthZ*DEPTH_SCALE_FACTOR`, 신규 상수 0.12)로 "가까우면 커 보이는" 가짜 원근 효과를 냄 — 진짜 원근 카메라 대신 이 방식을 쓴 이유는 이미 "알려진 한계"에 있던 실험(원근 카메라가 근접 거리에서 닥스훈트 같은 긴 종을 잘라먹는 문제)을 다시 안 건드리기 위함, 직교 카메라는 그대로 두고 스케일만 손으로 흉내.
  - **`pet.js`의 `render()`**: `pose.spinOverride`가 true면 커서 추적 이징(`bodyRotationTargetY`를 향한 지수 감쇠)을 건너뛰고 `currentRotationY = pose.spinAngle`을 직접 대입 — override가 꺼지는 순간(불리언이라 크로스페이드 없이 그 프레임에 즉시 스냅, 4차 라운드에서 확인한 `applyPoseCrossfade`의 불리언 처리와 동일 원리) `currentRotationY`가 스핀이 마지막으로 남긴 값에 그대로 있으므로, 그 다음 프레임부터 커서 추적 이징이 거기서부터 자연스럽게 이어받아 팝 없이 넘어감 — `spinAngle` 자체가 크로스페이드로 이상한 값으로 블렌딩되더라도(예: 여러 바퀴 돈 값에서 갑자기 0으로) override가 꺼진 순간부터는 아예 안 읽으므로 무해함.
  - **5개 idle**: `approach`(전종 공통, 낮은 가중치) — 온전한 사인파(0→1→0이 아니라 -1↔+1) 한 번으로 다가오기/멀어지기 둘 다 한 사이클 안에서 보여줌. `prowlcircle`(고양이 4종) — depthZ/lateralX가 함께 원(반지름 2.6, 1.25바퀴)을 그리며 사냥감 주위를 도는 느낌, `spinAngle`이 궤적의 접선 방향(이동 방향)을 향하도록 계산 — "빙 도는" 느낌을 내려면 몸이 궤적 중심을 향해 고정된 채 옆으로 미끄러지면 안 되고 실제로 도는 방향을 봐야 한다는 점을 반영. `chasetail`(강아지 4종) — prowlcircle보다 훨씬 빠른 스핀(≈5.2 rad/s)에 아주 작은 궤적(반지름 0.9)만 얹어 "꼬리 쫓아 제자리에서 빙글빙글"을 표현. `hopspin`(토끼) — 원래 walk gait 전용이던 `hopY`/`legsTuckedFront/Back` 필드를 idle이 직접 세팅(idle은 자기 스코프 밖 필드도 자유롭게 쓸 수 있다는 이 파일의 기존 컨벤션 그대로 재사용)해 폴짝 뛰면서 공중에서 회전하는 "비니(binky)"를 표현. `wheelrun`(햄스터) — `legPhase`를 빠르게 직접 오버라이드해 챗바퀴 뛰듯 다리만 바쁘게 움직이고, `spinOverride`는 의도적으로 안 켬(챗바퀴는 방향을 안 바꾸므로).
  - **검증 (수치 트레이스 + 스크린샷 시퀀스, 둘 다)**: `pet3d.js`의 `?live=1` 모드(실제 `createAnimal()` 상태머신을 그대로 구동)를 확장해 `stepIdle()`이 `pose.spinOverride`를 pet.js의 `render()`와 똑같이 반영하도록 고침(원래는 회전을 순수 수동 `setAngle()`에만 맡기고 있어서, 그대로 두면 depthZ/lateralX 이동은 보이는데 스핀은 하나도 안 보였을 것). `test/launch-zaxis-check.js`(신규) — 가중치 1짜리라 드물게 뽑히는 각 idle을(다른 라운드들의 "될 때까지 반복 stepIdle" 패턴 재사용) 랜덤으로 뽑힐 때까지 폴링한 뒤, 그 behavior가 끝날 때까지 매 스텝 `depthZ`/`lateralX`/`spinAngle`/`hopY`/`legPhase`를 JSON으로 남기고 몇 프레임마다 스크린샷도 저장. **햄스터 wheelrun이 첫 시도(1000초 시뮬레이션)에서 한 번도 안 뽑혔던 일**: 버그로 의심하는 대신 먼저 확률을 확인 — 별도 진단 스크립트로 1500초를 돌려보니 8번 정상적으로 뽑힘(1000초 기준 기대값 약 5.3회, 0번 나올 확률 약 0.5%) — 실제로 드문 경우에 그냥 걸린 것으로 확인 후 재실행해서 정상적으로 검증 완료(코드는 전혀 안 고침).
    - **수치 확인**: `prowlcircle`은 t=2.95(대략 중간)에서 depthZ=-1.93/lateralX=-1.74로 `sqrt(1.93²+1.74²)≈2.6`(설계한 반지름과 일치), `chasetail`은 spinAngle이 2.15초에 -11.18rad(≈5.2rad/s와 일치), `hopspin`의 hopY가 0↔2.7을 오르내림, `wheelrun`은 전 구간 `spinOverride:false` 유지 — 5개 전부 설계 수식과 소수점 단위로 일치.
    - **시각 확인**: `approach`는 f03(중간 크기)→f06(눈에 띄게 확대, "다가옴")→f19(눈에 띄게 축소, "멀어짐")으로 실제 스프라이트 크기가 커졌다 작아지는 게 스크린샷으로 뚜렷이 보임. `prowlcircle`은 f05(측면)/f13(정면 근처, 수염이 부채꼴로 펼쳐짐)/f20(3/4뷰)에서 서로 다른 각도가 나와 실제로 궤적을 따라 도는 게 확인됨. `chasetail`은 f02→f06 사이 짧은 시간에 완전히 다른 각도로 바뀌어 빠른 스핀이 확인됨. `hopspin`은 공중에 뜬 자세(f04)와 정면을 향한 자세(f09)가 섞여 나와 폴짝임+회전이 같이 보임. `wheelrun`은 f03/f08 둘 다 같은 측면 각도를 유지한 채(스핀 없음, 설계대로) 다리 위치만 바뀌는 게 확인됨.

**전체 회귀 확인**: `npm run test:invariants`(120만 프레임) — Feature 2/3 반영 직후 1회, Feature 1(신규 pose 필드/idle) 반영 후 다시 1회, 둘 다 위반 0건(voxel-engine.js/pet3d.js는 이 스위트의 검증 대상이 아니라 별도로 화면/수치 확인). `main.js`/`preload.js`에 이번 라운드에서 영구적으로 남긴 변화는 `preview-character` IPC 채널뿐(디버그 훅 없음, `git diff`로 확인), `voxel-engine.js`의 콧구멍 디버그용 마젠타 색은 검증 직후 원래 색으로 되돌림. 실제 앱(`npx electron .`)을 12초간 띄워 확인 — Chromium GPU 디스크 캐시 권한 경고(이 환경에서 반복적으로 나타나는 잡음, 이전 라운드들에서도 이미 확인된 것과 동일, 애플리케이션 코드와 무관) 외 콘솔 에러 없이 안정적으로 구동 후 종료.

### 부위 경계 실루엣 보강 + Z축 원형 경로 걷기로 재설계 (stage 4 이후 7차 QA 라운드 겸 신기능 2건)
6차 QA 라운드(Z축 idle 5종/정면 디테일/설정 미리보기) 이후 신규 요청 2건 — 둘 다 6차 라운드에서 만든 기능을 다시 다듬는 성격.

**1. 얼굴색=몸통색인 종의 목 경계가 안 보이는 문제 — 두 번의 서로 다른 기법을 시도, 첫 번째는 이론적으로 그럴듯했지만 실제로는 작동 불가능한 접근이었음**: 태비 고양이·허스키처럼(실제로는 로스터 10종 중 8종이 `headColor` 미지정으로 `bodyColor`를 그대로 씀 — 값이 다른 건 샴·허스키 2종뿐) 머리와 몸통이 같은 색인 종은 `MeshBasicMaterial`이 조명 반사를 전혀 안 하는 이 파일의 특성상 정면에서 목 경계가 완전히 안 보여 실루엣이 뭉개짐. "색상 조합별로 따로 대응하지 말고 범용적인 방법으로" 요청.
  - **1차 시도 (실패) — inverted hull 툰 외곽선**: 각 파츠(몸통/머리/귀/다리/꼬리)의 지오메트리를 살짝 키우고 `side:THREE.BackSide`로 뒤집어 자식으로 붙이는 표준 카툰 렌더링 기법 — 파츠 자신의 **바깥쪽 실루엣**(배경과 맞닿는 가장자리)에는 완벽하게 작동해서 렌더링해보면 또렷한 어두운 테두리가 보임(0.22 → 안 보임, 0.6까지 키워도 안 보임 — 처음엔 "너무 얇아서 그런가" 생각하고 계속 키웠지만 원인이 아니었음). 하지만 실제로 필요한 **내부 경계**(머리가 몸통 위에 얹혀 서로 다른 두 개의 불투명 파츠가 맞닿는 곳)에는 GROW 값을 아무리 키워도 절대 안 보임 — `test/diag-*-pixels.js`류 스크립트로 목 이음매 지점의 정확한 픽셀 색을 직접 스캔해서 확인(스크린샷을 보는 대신, 컷 위치를 `getBBoxDebug()`로 정확히 계산한 뒤 그 행의 모든 픽셀 색을 나열) — 외곽선 색이 파츠 바깥쪽에는 항상 나타나고 내부 이음매에는 단 한 번도 안 나타남. **근본 원인**: `BackSide`는 shell의 카메라 쪽 면을 컬링하고 반대쪽(카메라에서 먼) 면만 그리는데, 이는 배경(아무것도 없음)과 비교할 땐 항상 이기지만, 이웃 파츠의 **일반적으로 카메라를 향한(앞면) 솔리드 지오메트리**와 비교하면 그 이웃의 앞면이 shell의 뒷면보다 항상 카메라에 더 가까워서 무조건 짐 — `HEAD_Z_NUDGE` 같은 기존 Z-우선권 트릭도 이 shell의 뒷면이 아니라 파츠 "자신의" 앞면이 이기게 해주는 것이지 자식 shell의 뒷면엔 아무 도움이 안 됨. 즉 이 기법은 도형 확대량과 무관하게 **구조적으로** 내부 경계에서 작동할 수 없음 — 알아낸 뒤 이 기법 자체(`addOutline`/`OUTLINE_GROW` 등)를 코드에서 완전히 제거.
  - **2차 시도 (성공) — 명시적 "칼라(collar)" 밴드**: 파츠 자체를 부풀리는 대신, 경계 자리에 직접 얇은 평범한(BackSide 아님, 일반 정면 렌더링) 띠 지오메트리를 심음 — `addSeamCollar(parentMesh, w2d, localY, tintColor, depthSpan, zNudge)`. 이 칼라는 "이미 이웃보다 이기는" 파츠의 **자식**으로 붙어서 그 파츠의 위치/스쿼시/Z-우선권을 그대로 상속받고(목 칼라는 `headMesh`의 자식, `HEAD_Z_NUDGE`를 상속), 칼라 자신은 **일반 앞면 렌더링**이라 이웃의 앞면과 "동일한 종류의" 깊이 경쟁을 함 — `BackSide` shell의 근본적 한계를 피해감. 색은 `darkenColor(그 파츠 색, 0.55)`로 계산해 어느 색 조합이든 "이 부위의 살짝 어두운 버전"으로 자연스럽게 읽히게 함(고정 검정 대신).
    - **목(머리-몸통)**: `headMesh`의 자식, 머리 자신의 바닥 경계(local Y=-headH/2)에 위치, `SEAM_Z_NUDGE_INHERITED`(0.05)만으로 충분 — 이미 검증된 `HEAD_Z_NUDGE`의 우선권을 그대로 물려받으므로 칼라 자신은 "머리 자신의 솔리드 면과 z-fighting만 안 나면" 됨. `test/launch-neckseam-check.js`(신규, 재사용 가능한 도구)로 `getBBoxDebug()`를 통해 이음매의 정확한 세계좌표를 구하고 그 행의 픽셀을 스캔 — 이음매 행에 칼라의 딱 계산된 색(`darkenColor` 결과와 소수점까지 일치)이 넓은 띠로 나타나고, 그 아래 행(칼라보다 낮은 위치)엔 몸통 자신의 원래 색(줄무늬/배 무늬 패턴까지)이 계속 이어지는 걸 확인 — "칼라가 진짜로 몸통 위에 얹힌 것"이지 우연히 바깥 실루엣과 겹친 게 아님을 이렇게 구분해서 증명(처음엔 바깥 실루엣과 혼동해서 "작동한다"고 오판할 뻔했음 — 칼라 바로 아래 행까지 스캔해서 그 아래에 진짜 몸통 내용이 있는지 재확인하고서야 확정).
    - **어깨/엉덩이(다리-몸통)**: `upperMesh`(허벅지 뼈)의 자식, 다리 자신의 위쪽 경계(local Y=+upperLen/2)에 위치. 다리는 목과 달리 몸통에 대한 **기존에 검증된 Z-우선권이 전혀 없음**(이런 z-fight 자체가 이전에 보고/수정된 적이 없었음) — 처음엔 목과 같은 작은 nudge(0.05)를 썼는데 렌더링해보니 `dog_husky`는 잘 보이는데 `cat_a`는 완전히 안 보임(픽셀 스캔으로 "희미함"이 아니라 "완전 부재"를 확인) — 종마다 다리-몸통의 실제 기하학적 배치가 달라서 작은 마진으로는 안정적으로 안 이기는 경우가 있다는 뜻. `SEAM_Z_NUDGE_STANDALONE`(0.7, 이런 "상속된 우선권이 없는" 경우 전용의 훨씬 큰 값)으로 올려서 고침 — 재검증 결과 `cat_a`에서도 칼라 색(`152,89,49`, `darkenColor('#d9824a',0.55)`와 일치)이 정확한 행에 나타남, 스크린샷으로도 양쪽 앞다리/뒷다리 위에 뚜렷한 띠가 보임(0°/45°/270° 전부 확인).
    - **범위 밖으로 남긴 것**: 귀-머리, 꼬리-몸통 경계는 이번 라운드에서 안 건드림 — 사용자가 예시로 든 "목/어깨"에 집중하고 각각을 실제로 화면에서 확인하는 데 시간을 다 씀; 귀는 이미 대부분 종에서 색이 이미 다르고, 꼬리는 몸통과 달리 사전에 검증된 Z-우선권이 없어(다리와 같은 문제) 안전한 nudge 값을 새로 찾아야 하는 별도 작업이라 다음으로 미룸(정직하게 "알려진 한계"에 기록).
  - **검증**: `npm run test:invariants`(120만 프레임, `voxel-engine.js`는 2D 스위트 대상이 아니지만 애니메이션 로직 자체는 안 건드렸으므로 회귀 없음 예상대로 확인) 위반 0건. `test/launch-final-frontcheck.js`로 10종 전부 0°/45°/270°에서 목/어깨 칼라가 자연스럽고 은은하게(너무 두껍지 않게) 보이는 것 확인.

**2. Z축 idle 4종 — "제자리 회전+스케일"에서 "진짜 원형 경로로 걷기"로 재설계**: 6차 라운드의 `prowlcircle`/`chasetail`/`hopspin`/`wheelrun`은 실제로는 `lateralX`/`depthZ`가 원을 그리긴 했지만 다리(`legPhase`/`hopY`)는 전부 손으로 흉내낸 애니메이션(sin 파형 등)이었지 실제 걷기 gait가 아니었음 — "다리는 기존 걷기 gait를 그대로 쓰고, 몸 방향은 항상 원의 접선을 향하고, 원근 스케일은 유지"하는 진짜 걷기로 재설계 요청.
  - **설계**: `update()`의 idle 분기가 원래 `gaitFn(anim, raw, dt, false)`(다리를 가만히 멈추는 감쇠 모드)만 호출했는데, 새 `WALKING_IDLE_NAMES` Set(`prowlcircle`/`chasetail`/`hopspin`/`wheelrun` — `approach`는 원형 경로가 아니라 단순 전후 이동이라 이번 요청 범위 밖으로 두고 그대로 둠)에 현재 idle 이름이 있으면 `gaitFn(anim, raw, dt, true)`(진짜 걷기 모드)를 호출하도록 분기 추가 — `gaitFn`은 이미 `GAIT_PRESETS[spec.gait]`로 그 종이 실제 쓰는 걸음걸이(고양이 walk/닥스훈트·코기·햄스터 scamper/허스키·포메라니안 trot/토끼 hop)를 그대로 resolve하므로, 종마다 다른 코드를 안 짜도 각자의 실제 gait로 다리가 움직임. 반환되는 `advance`(이동거리)는 의도적으로 버림 — 예전 2D patrol의 `x` 좌표를 건드리는 값이라 이 Z축 원형 이동과는 무관.
  - **각 idle의 `apply()`는 이제 다리/hopY/bodyBob을 전혀 안 건드림** — gaitFn이 이미 채워놓은 값을 건드리면(apply가 gaitFn *다음*에 실행되므로) 덮어써서 무효화하게 됨. 대신 새 공유 헬퍼 `circleStep(pose, t, radius, angularSpeed, env)`(모듈 최상단, `IDLE_BEHAVIORS` 정의 전) 하나로 원 위 위치(`lateralX/depthZ = cos/sin(각도)*반지름*env`)와 접선 방향(`spinAngle`)만 계산 — `env`(0→1→0 엔벨로프)는 반지름에만 곱하고 각속도엔 안 곱함(경로 시작/끝에서 방향 전환이 굼떠 보이지 않게, 6차 라운드에서 이미 검증된 설계 그대로 유지).
  - **종별 반지름/속도**: "챗바퀴 도는 햄스터는 반경 작고 빠르게, 고양이 prowl은 반경 크고 천천히" 등 사용자 제안대로 손으로 튜닝 — `prowlcircle`(고양이) 반지름 2.6·각속도 0.9rad/s(느긋하게 넓게 돎), `chasetail`(강아지) 반지름 1.5·각속도 3.4rad/s(좁고 빠르게), `hopspin`(토끼) 반지름 1.8·각속도 2.3rad/s(hop gait 자체의 통통 튀는 리듬과 같이), `wheelrun`(햄스터) 반지름 0.8·각속도 4.4rad/s(가장 작고 가장 빠름 - 챗바퀴 느낌 유지, 다만 이젠 방향도 실제로 도는 "제자리 아닌" 원형 경로). `wheelrun`은 6차 라운드엔 "휠은 방향을 안 바꾸니까"라며 `spinOverride`를 일부러 안 켰었는데, 이번엔 "제자리 회전이 아니라 실제로 이동" 요청 자체가 그 전제(고정 장치로서의 휠)를 뒤집으므로 다른 세 종과 같은 원형-걷기 방식으로 통일.
  - **검증 (수치 + 스크린샷)**: `test/launch-zaxis-check.js`(6차 라운드 도구 재사용, 그대로 재활용 가능했음)로 4종 전부 재확인 — `legPhase`가 이제 진짜 sin 파형처럼 프레임마다 큰 폭으로 오르내림(예: `chasetail`은 0.969→0.998→0.930→0.771처럼 빠르게 진동, 예전엔 손으로 만든 단순 sin이었던 것과 값 자체는 비슷해 보여도 이젠 진짜 `anim.clock` 기반 gait 위상에서 나옴), `hopspin`은 `legPhase=0`(hop gait는 애초에 legPhase를 안 씀, 대신 `hopY`가 -0.06→0→0→-1.69처럼 실제 hop 사이클로 오르내림) — 모두 기대와 일치. `lateralX`/`depthZ`는 여전히 반지름-엔벨로프를 따르는 원 궤적(예: `prowlcircle` t=1.05에서 `sqrt(0.84²+1.16²)≈1.43`, t=2.05에서 `sqrt(0.64²+2.28²)≈2.37`로 반지름이 엔벨로프를 따라 커지는 것 확인). 스크린샷 시퀀스(`test/zaxis-check/*-f*.png`)로 같은 behavior의 서로 다른 프레임을 비교 — `prowlcircle`의 f10과 f20이 서로 다른 다리 스탠스(진짜 걸음걸이가 진행 중이라는 뜻)와 서로 다른 화면 크기/위치(원 위의 다른 지점, 원근 스케일 적용)를 동시에 보여줌; `chasetail`도 프레임마다 다리 자세가 확실히 바뀜. 여러 시점을 한 이미지로 이어붙이는 몽타주 스크립트를 시도했으나(`<canvas>`+`drawImage`) 5번째 이미지부터 로드가 실패하는 버그가 있어 완성하지 못함 — 대신 개별 프레임을 직접 확인하는 방식으로 검증을 마무리(몽타주 스크립트 자체는 정리해서 남기지 않음).
  - **회귀 확인**: `npm run test:invariants`(120만 프레임) 위반 0건, `test:movement-stats`(구 2D patrol 지표) 전 종 0.0% 그대로(이번 변경이 그 시스템을 다시 깨운 게 아님을 재확인), `test:idle-rhythm` breathe 점유율 70~83%로 기존 베이스라인과 같은 범위 유지(신규 behavior들의 duration이 다소 늘었지만 "차분함" 자체는 안 깨짐).

**전체 회귀 확인**: `npm run test:invariants`/`test:movement-stats`/`test:idle-rhythm` 전부 재실행 — 위반 0건, 기존 베이스라인과 일치. `main.js`/`preload.js`/`windows/pet/pet.js`는 이번 라운드에서 전혀 안 건드림(`git diff --stat`으로 확인) — 전부 `windows/shared/voxel-engine.js`(칼라 구현)와 `windows/shared/animal-engine.js`(gait 통합+circleStep)만 수정. 실제 앱(`npx electron .`)을 12초간 띄워 확인 — GPU 디스크 캐시 권한 경고 외 콘솔 에러 없이 정상 구동 후 종료.

### 고양이 prowlcircle 재튜닝 + 패턴 z-fighting + 수염 자동 대비 (stage 4 이후 8차 QA 라운드)
7차 QA 라운드(부위 경계 칼라 + Z축 원형 걷기 재설계) 이후 실사용 리포트 3건 — 전부 "코드 경로 자체는 맞는데 수치/색상 튜닝이 부족했다" 종류로, 이전 라운드들의 "그냥 재확인해보니 실제로는 다른 원인이었다"는 패턴과는 결이 다름(설계 결함이 아니라 파라미터 문제였음을 매번 직접 계측해서 확인).

**1. 고양이 prowlcircle이 여전히 "제자리에서 스케일만 변함"처럼 보임**: 사용자는 강아지 `chasetail`은 잘 고쳐졌는데 고양이만 dispatch가 빠졌거나 잘못 연결된 것 같다고 추측 — 실제로는 `WALKING_IDLE_NAMES` Set과 `circleStep()` 호출 둘 다 4종 전부에 종 구분 없이 동일하게 연결돼 있었고(코드 확인 + `legPhase`가 진짜 sin 파형처럼 진동하는 것도 트레이스로 재확인), dispatch 자체는 버그가 아니었음.
  - **진짜 원인 (수치로 특정)**: 온전한 렌더링 파이프라인 검증 대신 `getRawPixels()`로 실제 화면상 실루엣 bbox를 직접 측정하는 진단 스크립트(`test/diag-prowlcircle-render.js`)를 만들어 `lateralX`(직교 투영이라 화면에 그대로 반영되는 진짜 좌우 이동)와 `depthZ`(가짜 원근 스케일 트릭으로만 화면에 반영, 진짜 위치 이동은 전혀 없음)가 각각 화면에 미치는 영향을 따로 측정 — 4종 전부에서 **스케일 변화폭이 좌우 이동폭보다 2~2.9배 더 크게** 나타났고(그리고 근접 시 캔버스 경계에 잘려나가기까지 함), 고양이만 특별히 나쁜 비율은 아니었음(강아지도 거의 같은 2.88배). 진짜 차이는 **한 바퀴를 도는 데 걸리는 시간**이었음 — `chasetail`(반지름 1.5·각속도 3.4rad/s·지속시간 2.5~3.5초)은 짧은 지속시간 안에 약 1.6바퀴를 도는 반면, `prowlcircle`(반지름 2.6·각속도 겨우 0.9rad/s·지속시간 5~7.5초)은 훨씬 긴 시간 동안 겨우 0.9바퀴 — 즉 활성화 한 번에 "한 바퀴도 다 못 도는" 수준이라, 사용자가 보는 짧은 순간에는 "커졌다 작아졌다"라는 한쪽 구간만 보이고 반대편(좌우 이동)까지 이어붙여서 "도는 중"이라고 인식하기엔 반복 횟수가 너무 적었음.
  - **고침**: 반지름 2.6→2.2, 각속도 0.9→1.8rad/s, 지속시간 5~7.5초→4~5.5초로 재조정 — 새 파라미터로는 평균 활성화 한 번에 ~1.36바퀴(재계산: 1.8×4.75/(2π)) 완주, `chasetail`과 비슷한 수준의 반복성 확보. "고양이답게 강아지보다는 느긋하게"라는 원래 의도는 반지름을 강아지(1.5)보다 여전히 크게, 각속도는 강아지(3.4)보다 훨씬 느리게 유지하는 것으로 계속 지킴 — "한 바퀴 도는 데 너무 오래 걸려서 도는 것 자체가 안 보이는" 실패 지점만 피함.
  - **검증**: 재조정 후 실제 트레이스 확인 — 활성화 한 번(약 4초) 동안 `(lateralX,depthZ)`가 `(0,0)→(0.5,0.5)→(-1.1,1.4)→(-2.1,0.4)→(-1.9,-1.2)→(-0.6,-2.0)→(0.7,-1.6)→(1.2,-0.5)→(0,0)`로 완전한 한 바퀴를 그리는 것 확인. 스크린샷 시퀀스로도 화면상 크기·위치·회전 방향이 프레임마다 뚜렷이 달라지는 것 확인. 사용자가 같이 점검해달라고 한 토끼(`hopspin`)/햄스터(`wheelrun`)는 같은 방식으로 바퀴 수를 계산해보니 각각 ~1.19바퀴, ~2.28바퀴로 처음부터 충분히 역동적이었고(트레이스에서도 lateralX/depthZ가 양쪽 부호를 다 오가며 완전한 루프를 그리는 것 확인), 실제로 문제가 있는 건 고양이뿐이었음 — 나머지 둘은 코드를 안 건드림.

**2. 태비(치즈) 고양이, 커서가 가까이 오면 줄무늬가 지지직거림**: 목-머리 z-fighting을 고쳤던 5차 라운드와 같은 기법(`test/diag-neck-zfight.js`를 변형한 `test/diag-tabby-zfight.js` — 0.1도 미만의 아주 작은 회전 델타 사이에서 배경이 아닌 "내부" 픽셀이 완전히 다른 두 색 사이를 오가는지 검사)으로 재현·확진.
  - **1차 진단은 부분적으로 틀림**: 처음엔 "alert 반응(커서 근접 시 트리거되는 tailtwitch)이 원인"이라는 사용자 가설을 따라가려 했지만, cat_a의 `tailtwitch.apply()`는 꼬리 관련 필드만 건드리고 몸통/패턴과는 전혀 무관함을 코드로 확인 — 실제 원인은 alert 반응이 아니라 **커서를 따라가는 몸 전체 회전 자체**였음(회전할 때만 z-fighting이 드러나는 건 5차 라운드의 목-머리 버그와 동일한 메커니즘), "alert이나 자세 변화"라는 사용자의 두 가설 중 후자(자세=회전)가 맞았음.
  - **진짜 원인은 하나가 아니라 둘**: 0°~315° 8개 지점을 전수 조사한 결과, (a) **배(belly)와 줄무늬(pattern)가 서로 완전히 같은 깊이**(`addSkinBoxRel`로 동일한 `DEPTH+SKIN*2` 계산, Z 오프셋도 둘 다 0)로 겹치는 영역(cat_a의 belly x:6-15가 세 줄무늬의 x구간을 전부 포함)에서 서로 우선순위 없이 경합 — 2D 엔진(`drawCreature`)은 pattern을 먼저, belly를 나중에 그려서 belly가 항상 이기는데(자연스러운 페인터 알고리즘), 3D는 그 순서를 흉내낼 방법이 애초에 없어서 매 프레임 무작위로 뒤집혔음. (b) 이 문제를 고친 뒤에도 **줄무늬-몸통 사이의 z-fighting**이 하나의 특정 좌표(0도 근방)에서 계속 남아있었는데, 여러 번 마진을 키워봐도(0.06→0.3, 5배) 변화가 전혀 없어서 — 이건 z-fighting이 아니라 이 렌더러가 `antialias:false`로 설정돼 있어(선명한 픽셀아트 유지 목적) 아주 얇은 줄무늬의 경계 픽셀이 회전에 따라 래스터라이즈되는 픽셀이 살짝 바뀌는, 원래 있을 수 있는 하드엣지 지터라고 결론(z-fighting이라면 마진을 5배 키웠을 때 최소한 줄어들었어야 하는데 완전히 그대로였음 — 이 "마진 불변" 자체가 판별 기준이 됨).
  - **고침**: `addSkinBoxRel`/`addSkinBox`에 `extraZNudge` 매개변수 추가(기본값 0, 기존 snout 등 다른 모든 스킨 데칼은 영향 없음). `PATTERN_OVER_BODY_NUDGE`(0.3)를 pattern에, `BELLY_OVER_PATTERN_NUDGE`(=PATTERN_OVER_BODY_NUDGE+0.06)를 belly에 적용 — 2D 엔진의 그리기 순서(pattern 먼저, belly 나중 = belly가 이김)와 동일한 우선순위가 3D에서도 항상 재현되도록 함. `HEAD_Z_NUDGE`와 같은 "폴리곤 오프셋" 방식 재사용.
  - **검증**: `git stash`류의 임시 되돌리기 대신 상수를 직접 0으로 바꿔가며 진짜 "고치기 전/후" 스크린샷 확보(`test/diag-tabby-diff-visual.js` — 0°/0.1° 두 각도를 렌더링해서 바뀐 픽셀만 마젠타로 표시하는 diff 이미지). 고치기 전: 줄무늬 하나와 배-다리 경계에 마젠타 선 2줄, 고치기 후: 배 관련 선은 완전히 사라지고 줄무늬 쪽 1줄만 남음(위에서 설명한, 마진과 무관한 하드엣지 지터로 판단한 잔여분) — 전체 8개 체크포인트 합산으로도 대부분 대폭 감소(예: 180도 지점은 23→0으로 완전히 해소). `npm run test:invariants`(120만 프레임) 위반 0건.

**3. 콧수염 색을 얼굴 색 명암에 따라 자동으로 대비되게**: 기존엔 `WHISKER_COLOR`(크림색 `#f5f0e8`) 고정값 하나만 있었음 — 턱시도/샴처럼 어두운 얼굴에서는 우연히 눈에 띄었지만, `cat_calico`(거의 흰색 `#faf3e6`, 크림 수염과 거의 구분 안 됨)에서는 사실상 안 보이는 상태였음(직접 렌더링해서 확인 — before 스크린샷에서 수염이 실루엣에 거의 녹아 있음).
  - **구현**: Rec.709 상대 밝기 공식(`0.2126R+0.7152G+0.0722B`, 0-255 sRGB 채널에 직접 적용)으로 `relativeLuminance(hex)` 헬퍼 추가, 얼굴색(`cfg.headColor||cfg.bodyColor`, 얼굴 그라데이션 등 이 파일 다른 곳에서 이미 쓰는 것과 동일한 "얼굴색" 관례)의 밝기가 임계값(0.5) 이상이면 어두운 수염(`#241a12`), 미만이면 밝은 수염(기존 `#f5f0e8` 재사용)을 선택. 임계값은 실제 고양이 4종의 얼굴색으로 역산해서 검증: cat_a(주황) 0.63·calico(크림) 0.96 → 밝은 얼굴(어두운 수염 배정), tuxedo(검정) 0.09·siamese(seal-brown 마스크) 0.29 → 어두운 얼굴(밝은 수염 배정) — 0.5 하나의 임계값으로 사용자가 제시한 두 그룹("턱시도/샴 마스크는 진한 색 → 흰 수염", "태비/크림색은 밝은 색 → 검정 수염")과 정확히 일치.
  - **검증**: 4종 전부 정면(270°) 렌더링 — cat_a/calico는 이제 어두운 수염이 뚜렷이 보이고, tuxedo/siamese는 기존처럼 밝은 수염이 잘 보임. 가장 극적인 케이스인 calico는 고치기 전/후 확대 비교 스크린샷도 남김(`test/frontdetail-check/whisker-{before,after}-cat_calico.png`) — before는 수염이 거의 안 보이는 수준(크림 얼굴과 크림 수염이 거의 같은 톤), after는 어두운 수염이 확실하게 대비되어 보임.

**전체 회귀 확인**: `npm run test:invariants`(120만 프레임)/`test:movement-stats`/`test:idle-rhythm` 전부 재실행 — 위반 0건, 기존 베이스라인과 일치. `main.js`/`preload.js`/`windows/pet/pet.js`는 이번 라운드에서 전혀 안 건드림(전부 `windows/shared/animal-engine.js`의 `prowlcircle` 파라미터와 `windows/shared/voxel-engine.js`의 z-nudge/수염 로직만 수정). 실제 앱(`npx electron .`)을 12초간 띄워 확인 — GPU 디스크 캐시 권한 경고 외 콘솔 에러 없이 정상 구동 후 종료.

### 8차 QA 라운드 자체 수정이 낳은 회귀 2건 (stage 4 이후 9차 QA 라운드)
8차 QA 라운드의 두 수정(패턴 z-fighting `extraZNudge`, Z축 idle의 `spinAngle`)이 각각 새 회귀를 하나씩 낳았다는 리포트 — 사용자가 두 회귀 모두 원인 가설을 제시(1번: 넛지가 비대칭 적용됐거나 좌우 지오메트리 법선 방향이 반대, 2번: 각도 감싸기가 최단 경로를 안 씀)했고, 둘 다 방향은 정확했지만 실제 스코프는 가설보다 넓었음(2번은 두 군데 중 한 군데만 고쳐서는 안 끝났음) — 조사 과정 그대로 기록.

**1. 태비 고양이 왼쪽 몸통 줄무늬가 사라짐**: `addSkinBoxRel`/`addSkinBox`(8차 라운드에서 belly/pattern z-fighting을 고치려고 추가한 `extraZNudge` 매개변수)의 실제 구현을 다시 읽어보니 `extraZNudge`를 `addBoxRel`의 `zOffset` 인자로 그대로 넘기고 있었음 — `zOffset`은 이미 Z=0에 중심이 맞춰진 박스를 그 중심 **위치** 자체를 밀어내는 값이지, 크기를 키우는 값이 아님. `PATTERN_OVER_BODY_NUDGE=0.3`만큼 중심을 +Z로 밀면 +Z쪽 면은 0.3만큼 더 멀리(마진 증가, 이겼던 쪽은 계속 이김) 나가지만 **-Z쪽 면은 정확히 같은 0.3만큼 몸통 쪽으로 후퇴**(마진 감소)함 — 8차 라운드 이전부터 이미 있던 SKIN 기반 기본 마진이 딱 0.15였으므로, 이 넛지 하나가 -Z쪽 마진을 +0.15(안전)에서 -0.15(패배 — 줄무늬 면이 몸통 면보다 안쪽으로 들어가버려 그 시야각에서는 아예 안 그려진 것과 같음)로 뒤집어버린 것. 실측(`test/diag-pattern-visibility.js`, 8개 체크포인트에서 화면상 패턴 색 픽셀 수를 직접 셈)으로 정확히 확인 — 고치기 전엔 135°/180°/225° 세 지점에서 패턴 픽셀이 정확히 0개(그 외 지점은 195/195/60/180개 등 정상), 고친 뒤엔 8개 지점 전부 0이 아니고 대칭 지점끼리(0°=180°=195, 45°=135°=195, 225°=315°=180) 값도 일치. **8차 라운드 자신의 z-fighting 진단(`diag-tabby-zfight.js`)이 왜 이걸 못 잡았는지**: 그 진단은 "아주 작은 회전 차이에서 색이 흔들리는지"(불안정성)만 검사하도록 설계됐는데, 이 버그는 매 프레임 항상 똑같이 안 보이는 **안정적으로 틀린 상태**라 그 정의상 흔들림(flicker)이 0으로 나옴 — 안정적으로 틀린 결과는 불안정성 탐지로는 원리적으로 못 잡는다는 걸 보여준 사례.
  - **고침**: `addSkinBox`/`addSkinBoxRel`가 `extraZNudge`를 `zOffset`(위치 이동)이 아니라 `depth`에 `extraZNudge*2`를 더하는 식(크기를 양쪽으로 균등하게 키움, 박스 중심은 그대로 Z=0)으로 바꿈 — 이제 `extraZNudge`만큼의 마진이 +Z/-Z 양쪽 면에 똑같이 붙어서, 기존에 이기고 있던 쪽(+Z)의 결과는 그대로 유지하면서 지고 있던 쪽(-Z)도 같이 구제됨. `HEAD_Z_NUDGE`(머리 전용, `addBoxRel`의 `zOffset`을 직접 씀)와 `SEAM_Z_NUDGE_STANDALONE`/`SEAM_Z_NUDGE_INHERITED`(부위 경계 칼라)는 이 함수를 안 거치고 여전히 원래의 위치-이동 방식 그대로라 이번 수정과 무관 — 이론상 같은 종류의 비대칭 마진 문제를 안고 있을 수 있지만(넛지가 작아서 지금까지 안 걸렸을 뿐일 가능성), 이번에 리포트된 범위가 아니라 안 건드림, 아래 "알려진 한계"에 남겨둠.
  - **검증**: 고치기 전/후를 실제로 토글해가며(파일을 임시로 되돌렸다 복원) `diag-pattern-visibility.js` 재실행으로 회귀를 직접 재현 후 수정 확인. 좌우 비교 스크린샷(`test/launch-tabby-stripe-lr-check.js`, 180°="왼쪽"과 0°="오른쪽")도 남겨 눈으로도 양쪽 다 줄무늬가 뚜렷이 보이는 것 확인. 오른쪽(이미 잘 되던 쪽)에 대한 회귀가 없는지도 8차 라운드가 쓰던 바로 그 z-fighting 진단(`diag-tabby-zfight.js`)으로 재확인 — 원래 이기고 있던 0°/45°/90°/270°/315° 체크포인트는 플립 수가 고치기 전과 **바이트 단위로 동일**(53/23/23/6/7), 이전엔 완전히 안 보이던 135°/180°/225°에서만 새로 플립이 생겼는데(8→38, 0→15, 8→23), 이건 "전에 없던 새 버그"가 아니라 "전에는 안 그려져서 흔들릴 대상 자체가 없었는데 이제 그려지니까 다른 지점들과 똑같은 수준의 하드엣지 지터가 드러난 것"임을 8차 라운드가 확립한 판별법(넛지 크기를 5배 이상 키워도 흔들림이 줄지 않으면 z-fighting이 아니라 안티앨리어싱-off 렌더러의 정상적인 픽셀 경계 반올림)을 그대로 재적용해서 확인(0.3→1.0으로 키워도 단조 감소하지 않고 늘었다 줄었다 함 — margin 문제가 아니라는 뜻). `npm run test:invariants` 위반 0건.

**2. 회전(360도 등)이 끝나고 원래 자세로 돌아올 때 부자연스럽게 튕기듯 돌아옴**: `pose.spinAngle`(Z축 원형 걷기 idle이 몸 전체 방향을 직접 구동하는 필드)은 의도적으로 랩(wrap)하지 않고 계속 누적되는 값 — `chasetail`처럼 빠른 스핀은 한 번 재생에 15라디안 이상 쌓일 수 있음(defaultPose 자체 주석에 이미 "deliberately not wrapped"라고 명시돼 있었음, 그 자체는 의도된 설계). 문제는 이 큰 값을 **소비하는 두 곳** 모두 각도의 순환성(2π마다 같은 방향)을 모르고 raw 숫자 차이 그대로 보간하고 있었다는 것 — 사용자가 지목한 세 곳(`circleStep()`/커서 추적 회전/idle 전환 크로스페이드) 중 `circleStep()` 자체는 spinAngle을 "생성"만 할 뿐 소비하지 않아 문제 없었고, 실제 버그는 나머지 두 소비처에 각각 하나씩, 서로 다른 증상으로 있었음.
  - **곳 1 — `pet.js`의 커서 추적 회전 (`render()`)**: Z축 idle이 끝나 `pose.spinOverride`가 `true→false`로 바뀌는 순간, `currentRotationY`는 그 idle이 마지막으로 남긴(수십 라디안일 수 있는) `spinAngle` 값에 그대로 멈춰있는데, 그 다음 프레임부터 `else` 분기가 `(bodyRotationTargetY-currentRotationY)*ease`로 커서 쪽 목표(`[-π,0]` 범위)를 향해 이징하기 시작 — 두 값의 raw 차이가 수십 라디안이면 첫 프레임의 순간 각속도가 그만큼 커져서 "튕기는" 것처럼 보임. 실측: 고치기 전 코드로 시뮬레이션(`test/launch-rotation-snap-test.js` — 실제 pet 창을 단독 로드하고, 검증 후 제거한 임시 훅으로 `currentRotationY=16.5`/`bodyRotationTargetY=-1.2`를 직접 주입해 handoff 순간을 결정론적으로 재현)한 결과 프레임당 최대 각도 변화가 **1.92라디안(≈110°)** — 60fps 기준 순간 각속도 약 115rad/s. **고침**: `windows/shared/animal-engine.js`에 `normalizeAngleDelta(delta)`(2π 배수를 빼서 `(-π,π]` 범위로 접는, `export`된 공용 헬퍼)를 추가하고 `currentRotationY += normalizeAngleDelta(bodyRotationTargetY-currentRotationY)*ease`로 교체 — 매 프레임 최단 경로만 남기므로 raw 값이 아무리 여러 바퀴 쌓여 있어도 오차가 항상 `(-π,π]` 안으로 접혀서 순간 각속도가 `π*ease_rate` 이내로 묶임, 그리고 매 프레임 이렇게 재적용되므로 `currentRotationY` 자체도 자연히 target의 한 바퀴 이내로 수렴해 무한정 드리프트하지 않음. 재검증 결과 프레임당 최대 변화 **0.126라디안**(약 7°)으로 줄었고, 목표의 "가장 가까운 각도 동등값"(원 예시: target=-1.2 → 실제 수렴점 -1.2+2π×3≈17.65, 16.5에서 겨우 1.15만큼만 이동하면 되는 진짜 최단경로)에 오차 0.0001 이내로 정확히 수렴 확인.
  - **곳 2 — `animal-engine.js`의 `applyPoseCrossfade`**: 모든 숫자 pose 필드를 `lerp(start,target,t)`로 균일하게 블렌딩하는 범용 크로스페이드 함수 — spinAngle도 예외 없이 여기 걸리는데, 두 스핀 idle 사이의 전환(같은 idle이 곧바로 재추첨되는 "재시작" 포함, 이 로스터에선 종별로 circleStep 기반 idle이 하나뿐이라 재시작이 유일한 "spin→spin" 케이스)에서 `from.spinAngle`(직전 활성화가 쌓아둔 큰 값)과 `raw.spinAngle`(새 활성화의 t=0 기준 작은 값)을 그대로 lerp하면 고정된 0.25초 크로스페이드 창 안에 수십 라디안을 다 쓸어버림 — RNG 기반 실사용 시뮬레이션(`test/diag-crossfade-spinangle.js`, `dog_husky`로 900초까지 돌려봤지만 종별로 원형 idle이 하나뿐이라 "그 idle이 연달아 두 번 뽑히는" 조건 자체가 낮은 확률이라 재현 실패 — INCONCLUSIVE로 남김, 재사용 가능한 도구로 유지) 대신, `applyPoseCrossfade`를 이번 검증을 위해 `export`해서 직접·결정론적으로 호출하는 방식(`test/diag-crossfade-spinangle-direct.mjs`)으로 전환해 실제로 재현·확인함.
    - **1차 수정은 절반만 고쳐짐**: 블렌딩 루프 안에서만 `spinAngle`을 `start + normalizeAngleDelta(target-start)*t`로 특별 취급했더니, 0.25초 동안의 블렌딩 자체는 매끄러워졌지만(직접 테스트로 프레임당 델타가 정상 범위였음을 1차로 확인) — **크로스페이드가 끝나는 바로 그 경계 프레임**에서 여전히 큰 점프가 남아있었음: `applyPoseCrossfade`는 `transitionElapsed >= POSE_TRANSITION_DURATION`이 되면 블렌딩 루프 자체를 건너뛰고 `anim.pose = raw`를 그대로 대입하는 얼리 리턴 경로를 갖고 있는데, 이 경로는 내가 고친 특별 취급 로직을 아예 거치지 않고 raw(정렬 안 된, 작은 값)를 그대로 씀 — 즉 "0.25초에 걸쳐 스윕하던 큰 점프"를 "0.25초 시점 한 프레임에 몰린 큰 점프"로 위치만 옮긴 것이었음. `diag-crossfade-spinangle-direct.mjs`가 매 프레임을 전부 추적했기 때문에(스크린샷 몇 장이 아니라) 이 경계 프레임의 점프를 놓치지 않고 잡음 — "몇 프레임만 확인"이 아니라 "전환 구간 전체를 촘촘히 추적"해야 하는 이유를 보여준 사례.
    - **최종 수정**: 블렌딩 루프 안의 특별 취급을 걷어내고, 대신 전환이 **시작되는 바로 그 순간**(`transitionElapsed===0`인 첫 호출) `anim.transitionFrom.spinOverride && raw.spinOverride`(양쪽 다 스핀 idle일 때만, 즉 정말 두 spinAngle 연속체를 이어붙여야 하는 경우에만)를 조건으로 `anim.spinAngleLapOffset = 2π × round((from.spinAngle - raw.spinAngle)/2π)`(새 활성화의 값을 직전 값에 가장 가까운 2π 배수만큼 밀어서 "같은 연속체 위"로 재배치하는 오프셋)를 **한 번만** 계산해 `anim`에 영구 저장하고, 매 프레임(`transitionElapsed`가 얼마든, 블렌딩 중이든 얼리 리턴 경로든 상관없이) `raw.spinAngle += anim.spinAngleLapOffset`를 적용 — 블렌딩 루프와 얼리 리턴 경로 둘 다 이 보정이 이미 반영된 `raw`를 그대로 쓰므로 경계 프레임에서 새는 구멍이 없어짐. 스핀이 아닌 쪽으로의 전환에서는 오프셋을 0으로 리셋(오래된 오프셋이 훨씬 나중의 무관한 스핀 idle에 잘못 새는 것 방지). 이 방식이 자리잡은 뒤로는 블렌딩 루프 안의 `spinAngle` 특별 취급 자체가 불필요해짐(이미 정렬된 `raw.spinAngle`과 `from.spinAngle`은 항상 서로 π 이내라 평범한 `lerp`로도 자동으로 최단경로가 됨) — 코드 한 곳(오프셋)으로 두 소비 경로를 동시에 커버.
    - **검증**: 재작성한 `diag-crossfade-spinangle-direct.mjs`로 chasetail의 실제 각속도(3.4rad/s)를 반영해 매 프레임 `raw.spinAngle`을 진짜로 전진시키며(이전처럼 고정값이 아니라) 전체 시퀀스를 추적 — 경계 프레임(0.25초 지점) 전후로 22.78→22.64→22.54처럼 매끄럽게 감소, 프레임당 최대 변화 0.188라디안(기대 정상 속도 0.102rad/frame과 같은 자릿수)으로 안정. **일부러 다시 되돌려서(1차 수정 상태로) 같은 테스트를 돌려 이 스크립트가 실제로 그 버그를 잡아내는지도 재확인**(25.27라디안짜리 점프 검출, FAIL로 정확히 보고) — 그 후 다시 최종 수정으로 복원. `npm run test:invariants`(120만 프레임)/`test:movement-stats`/`test:idle-rhythm` 재실행 — 위반 0건, 기존 베이스라인과 일치.
  - **검증 중 발견한 테스트 스크립트 자체의 버그 (참고용 기록)**: `diag-crossfade-spinangle-direct.mjs` 초안이 `while (anim.transitionElapsed < 0.25+DT)`로 루프 종료 조건을 잡았다가 Node 힙 아웃오브메모리로 죽음 — `applyPoseCrossfade`의 얼리 리턴 경로가 `transitionElapsed>=0.25`가 되는 순간부터 그 값을 더 이상 증가시키지 않는다는(자기 문서화된 의도된 동작) 걸 놓쳐서, `transitionElapsed`가 0.25~0.28 사이 어딘가에 영원히 멈춘 채 반복문이 안 끝났던 것 — 고정 반복 횟수로 교체해 해결. 별개로 초안이 `raw`/`transitionFrom` 객체에 `spinOverride: true`를 안 넣어서 오프셋 계산 조건 자체가 항상 거짓으로 평가돼 "고쳐지지 않은 것처럼" 보였던 실수도 있었음 — 두 삽질 모두 실제 프로덕션 코드가 아니라 검증 스크립트 자체의 버그였고, 최종 스크립트에서 둘 다 바로잡고 나서야 진짜 before/after 대비가 나옴.

**전체 회귀 확인**: `npm run test:invariants`(120만 프레임)/`test:movement-stats`/`test:idle-rhythm` 전부 재실행 — 위반 0건, 기존 베이스라인과 일치. `pet.js`에 검증용으로 잠깐 추가했던 임시 훅(`__rotationSnapTestHook__`)은 검증 직후 제거 확인(`git diff`로 재확인). `applyPoseCrossfade`는 `test/diag-crossfade-spinangle-direct.mjs`가 앞으로도 재사용할 수 있게 `export` 상태로 남겨둠(다른 순수 함수들 - `legJointAngles`/`checkPoseInvariants`/`SPECIES` - 과 같은 "테스트를 위해 export된 공용 헬퍼" 성격, 실제 앱 동작에는 영향 없음). 실제 앱(`npx electron .`)을 10초간 띄워 확인 — 기존에 이미 알려진 GPU 디스크 캐시 권한 경고 외 콘솔 에러 없이 정상 구동 후 종료.

### 전체 종 z-nudge 전수 스윕 + prowlcircle 실제 앱 재검증 + 시작 위치 중앙 정렬 (stage 4 이후 10차 QA 라운드)
9차 QA 라운드(줄무늬 회귀, 회전 스냅 회귀)를 고친 뒤 사용자가 3가지를 요청 — (1) 태비 케이스 하나만 고치고 끝내지 말고 전체 종 x 회전/이동 상태를 스윕해서 같은 종류의 버그가 더 있는지 전수 점검, (2) prowlcircle이 "지난 두 번 고쳤다고 했는데도" 실제 사용에서 여전히 안 움직이는 것 같다며 이번엔 반드시 실제 Electron 앱으로(시뮬레이션 말고) 검증, (3) 신규 - 시작 위치를 창 중앙으로. 1번과 2번 둘 다 이번 라운드에서 "처음 세운 가설이 틀렸고, 실제로 파고드니 완전히 다른/더 큰 원인이 있었다"는 패턴을 다시 확인함 — 아래 그대로 기록.

**1. 전체 종 z-nudge 스윕 — 목-머리 z-nudge와 부위 경계 칼라에서 같은 버그 클래스의 새 사례 2건 발견**: `voxel-engine.js`의 `addBox`/`addBoxRel`/`addSkinBox`/`addSkinBoxRel`/`addSeamCollar` 등 Z-offset을 쓰는 모든 호출부를 전수 조사(귀/눈/다리/수염/코 디테일/볼주머니는 전부 "좌우 대칭 쌍, 각자 일관된 부호로 오프셋" 또는 "Z=0 중심 대칭 성장" 패턴이라 안전함을 코드로 확인 — 9차 라운드가 고친 pattern/belly의 원래 버그 클래스가 아예 발생할 수 없는 구조).

- **1-1. `HEAD_Z_NUDGE`(머리-몸통, 5차 라운드부터 있던 원조 z-fighting 수정)도 같은 위치-이동 버그였음**: `addFaceShadedHeadBox`(샴 전용)와 일반 머리 경로(`addBoxRel`) 둘 다 `HEAD_Z_NUDGE`를 `zOffset`(위치 이동)으로 쓰고 있었음 — 9차 라운드에서 발견한 pattern/belly 버그와 똑같은 메커니즘, 이번엔 pattern/belly보다 5개 라운드나 먼저 있었던 원조 수정 자체에 있었던 것. +Z쪽은 이겼지만(마진 +0.08) -Z쪽은 정확히 그만큼 짐(마진 -0.08) — pattern 때와 다른 점은, 머리-몸통은 원래 SKIN 마진 같은 사전 여유가 전혀 없는 완전한 무승부(0)에서 시작했다는 것: 즉 +Z쪽이 "이기게" 된 것도, -Z쪽이 "지게" 된 것도 전부 이 수정 자체가 만든 결과 — 고치기 전엔 랜덤하게 지지직거리던 게, 고치기 후엔 한쪽은 항상 이기고 반대쪽은 항상 지는(그래서 지지직 대신 "안정적으로 틀림") 결과로 바뀐 셈. 헤드컬러가 몸통과 다른 종(샴/허스키)에서만 육안으로 드러나는 문제라 지금까지 안 걸렸을 뿐 — 8개 각도 전수 스캔으로 재확인.
  - **고침**: `addSkinBox`/`addSkinBoxRel`와 동일한 원칙 — `HEAD_Z_NUDGE`를 `depth`에 양쪽으로 더해서(`DEPTH+HEAD_Z_NUDGE*2`, `zOffset=0`) 대칭 성장시킴. `addFaceShadedHeadBox`는 `BoxGeometry`의 depth 인자와 `mesh.position.z`를 각각 고침 — 정점 색/UV 계산은 X/Y만 쓰므로(이 함수 자신의 기존 주석에 명시) 이 변경과 무관.

- **1-2. 부위 경계 칼라(`addSeamCollar`, 7차 라운드) — 이번 라운드에서 가장 큰 발견, 첫 가설이 틀렸던 사례**: 목/어깨 칼라도 `zNudge`를 위치 이동으로 쓰고 있었고, 특히 어깨 칼라는 다리 두 벌(zSide=+LEG_Z/-LEG_Z, 완전한 거울상)에 **같은 부호로 고정된** 이동을 적용하고 있었음 — 손으로 계산하면 +LEG_Z쪽 다리는 여유 있게 이기는데(절대 Z=LEG_Z+0.7=2.35, body의 +3 면과 비교해 이득) -LEG_Z쪽 다리는 절대 Z=-LEG_Z+0.7=-0.95로 body의 -3 면에서 한참(1유닛 이상) 모자람 — 이건 "약간 손해"가 아니라 "커서를 따라 몸이 180도 돌 때마다 한쪽 어깨 칼라가 통째로 안 보이는" 수준의 버그.
  - **1차 가설(틀림)**: pattern/belly 때 배운 대로 "너비(w2d)를 양쪽으로 대칭 성장시키면 되겠다"고 판단 — 목 칼라의 w2d(=headW)가 머리 박스 자신의 X-extent와 정확히 같아서(무승부) 90도 회전 시 이 축이 가려짐 판정 축이 되는데 대칭 성장이 없었다는 게 근거였음. 실제로 적용해보니(w2d를 아무리 크게 키워도, headW+10처럼 극단적인 값까지 시도) 90도에서 여전히 목 칼라가 전혀 안 보임 — 너비를 독립적으로 늘려도, 깊이를 독립적으로 늘려도(6배까지) 각각 따로는 안 고쳐지고 **둘 다 큰 값으로 동시에** 줘야만 겨우 나타나는 걸 발견하고서야 가설 자체가 틀렸다는 걸 인정.
  - **진짜 원인(이분 탐색으로 축을 하나씩 고정해서 찾음)**: 깊이(depth)만 크게, 너비는 원래 작은 값으로 고정한 채 재시도 — **깊이만으로 고쳐짐**(0.58에서는 안 됨, 0.88부터 됨, 즉 임계값이 0.58~0.88 사이). 메커니즘은 처음 가정한 "가려짐 판정 축에서 이긴다"가 아니라 다른 것: 90도에서는 로컬 Z(깊이)가 이제 **화면에 보이는 폭** 축이 되는데(로컬 X가 가려짐 축이 되는 것과 반대), 깊이를 키우면 칼라가 화면상 더 넓어져서 — 중앙부는 여전히 못 이겨도(가려짐 축에서 못 이기니까) — **가장자리가 가리고 있는 대상(머리)의 화면상 폭보다 더 넓게 삐져나와서 그 삐져나온 부분만 보이게 됨**. 이게 가능했던 이유는 목/어깨 칼라의 진짜 경쟁 상대가 "Z=0 중심의 대칭 몸통"(pattern/belly가 이겨야 했던 그 몸통)이 아니라, **머리와 몸통이 애초에 코-꼬리 축에서 서로 동심이 아니라는 사실** 그 자체였음 — 예를 들어 닥스훈트는 몸통이 로컬X [1,20]에 걸쳐 있고 머리 자신의 중심이 X=20이라, 몸통의 코쪽 끝이 머리 중심과 거의 겹칠 정도로 깊이 파고듦(전체 로스터로 재확인) — 이런 큰 비대칭 이웃을 상대로 "작은 마진을 대칭으로 얹는" 접근 자체가 원리적으로 안 통했던 것.
  - **수치로 확정한 값**: `SEAM_Z_NUDGE_INHERITED`(목, 기존 0.05) → `HEAD_Z_NUDGE+1.2`(≈1.28, 발견한 임계값 0.88보다 여유 있게), `SEAM_Z_NUDGE_STANDALONE`(어깨, 기존 0.7) → 재검증 결과 cat_a의 어깨 칼라도 같은 문제로 90도에서 빠짐을 확인(1.9로 올려서 해결, 나머지 종은 원래도 통과). 너비(`w2d`)는 아주 작은 고정값(`WIDTH_MARGIN=0.05`, 양쪽 도합 0.1)만 유지 — 애초에 필요했던 건 "무승부 깨기" 정도였지 큰 비대칭 상대를 이기는 용도가 아니었음(1차 가설이 틀렸던 지점).
  - **왜 깊이를 키워도 0/180도에선 안 보이는지(부작용 없음 확인)**: 로컬 Z(깊이)는 0/180도에서 카메라 쪽으로 완전히 축소투영되는 축이라, 아무리 키워도 옆모습 실루엣엔 전혀 안 드러남(스크린샷으로 cat_a/husky/dachshund 0도·45도·90도 재확인 — 옆모습은 그대로, 90도에서만 칼라가 나타남).
  - **검증**: 종별로 예측한 색을 손으로 다시 계산하는 대신 `getSeamCollarColors()`(신규 디버그 접근자, `mesh.userData.isSeamCollar` 마커로 씬그래프를 순회해 실제 `material.color`를 그대로 읽음)를 추가해 매 종의 진짜 칼라 색을 코드에서 직접 가져와 검증 스크립트에 씀 — 처음엔 Node의 `require('three')`로 `darkenColor`를 재현해서 예측했는데 결과적으로 그 예측 자체는 정확했음(별개로 확인된 사실), 이 접근자는 그 값을 매번 재예측할 필요 없이 항상 정확하게 가져오기 위해 추가. `test/diag-seam-sweep-all-species.js`(10종 × 8각도, 목/어깨 칼라 + 헤드컬러 유무를 전부 검사)로 **10종 × 8각도 전체 통과** 확인. `npm run test:invariants`(120만 프레임)/`test:movement-stats`/`test:idle-rhythm` 재실행 — 위반 0건, 기존 베이스라인과 일치.

**2. prowlcircle 실제 앱 재검증 — 버그를 못 찾음(정직한 보고), 대신 검증 방법론 자체의 결함 두 가지를 발견**: 사용자가 "지난 두 번의 검증이 실제 사용에서는 안 맞았다"고 명시적으로 지적한 대로, 이번엔 `pet3d.js` 시뮬레이션이 아니라 **실제 `windows/pet/` 창**(main.js 없이 단독 로드, 기존 확립된 패턴)을 실시간으로 띄워 화면 스크린샷 시퀀스로 검증.
  - **1차 시도의 방법론 결함 (병렬 창 리소스 경합)**: 대기 시간을 줄이려고 고양이 4종을 한 Electron 프로세스에서 **동시에** 4개 창으로 띄웠는데, cat_a/cat_tuxedo가 90초 내내 `currentIdleName=null`(로딩조차 안 끝난 상태)에 멈춰 있는 걸 발견 — 4개의 WebGL 컨텍스트/ES 모듈을 동시에 초기화하는 부하 때문으로 보이는 테스트 하네스 자체의 아티팩트였고, 실제 앱은 펫 창을 하나만 띄우므로 사용자가 절대 마주칠 상황이 아님. cat_a 하나만 단독으로 띄워보니 정상적으로(sit→stretch→breathe→approach→sleep→tailtwitch→breathe, 60초 안에) 순환하는 걸 확인 — **순차 실행**(한 번에 한 종씩)으로 재설계.
  - **순차 재검증 결과: 4종 전부 실제 화면에서 진짜 원형 이동 확인**: `getRawPixels()`가 아니라 실제 `capturePage()` PNG의 **알파 채널 기반 바운딩박스**(투명 배경이므로 실루엣 자체를 직접 측정)로 화면상 중심 좌표를 프레임마다 추적 — cat_a(X스윙 32px, Y스윙 16px, 스케일스윙 59px), cat_tuxedo(22px/18px/73px), cat_calico(19px/14.5px/54px), cat_siamese(32px/13px/61px) 전부 X/Y/스케일 스윙이 비슷한 크기로 나타남(스케일만 크고 위치는 거의 안 변하는 "제자리에서 커졌다 작아짐"이 아니라, 실제로 화면 위치가 함께 움직인다는 뜻) — **4종 전부 실제 원형 이동이 화면에 정상적으로 나타남을 실제 앱 스크린샷으로 확인**, 코드 수정 없음(고칠 버그를 못 찾음, 8차/9차 라운드의 수정이 이미 올바르게 작동하고 있었다는 뜻).
  - **탐색 중 발견한, 재현 안 되는 간헐 현상 (정직하게 기록, 미해결)**: 순차 검증 과정에서 cat_calico를 격리해서 60초간 관찰했을 때 딱 한 번, `stretch@0.0s → tailtwitch@1.2s`로 전환된 뒤 남은 약 59초 내내 전혀 안 바뀌는 걸 목격 — `tailtwitch` 자체의 지속시간은 1.2~2초로 코드에 명시돼 있어 정상이라면 훨씬 일찍 다음 상태로 넘어가야 함. 페이지에 `window.onerror`/`unhandledrejection` 리스너를 심어 재현을 시도했지만 잡히는 에러 없음, 같은 종을 같은 방식으로 다시 두 번 띄워봐도 둘 다 정상적으로 순환(`pet3d.js`의 `stepIdle()` 구동 시뮬레이션으로도 calico가 100초 시뮬레이션 동안 프롤서클→그루밍→테일트위치→싯→스트레치→브리드→...로 문제없이 순환하는 것도 별도로 확인 — 공유 상태머신 로직 자체는 정상이라는 뜻). 3번 중 1번만 재현된 것이라 우연일 가능성이 높지만, 정확한 원인은 못 찾음 — 앞으로 비슷한 "특정 idle에서 멈춘 것 같다"는 리포트가 실사용 중 반복적으로 들어오면 이번에 추가한 도구들(`test/diag-calico-crash-capture.js`의 `window.onerror` 훅 패턴, `test/diag-single-window-idle-check.js`의 격리된 단일 창 순환 관찰)을 재사용해 더 깊이 볼 것.
  - **디버그 인프라**: `pet3d.js`에 `getCfgColors()`/`getSeamCollarColors()` 접근자 추가(재사용 가능한 디버그 도구, 위 1번 항목에서도 사용). `pet.js`에 이번 검증을 위해 잠깐 추가했던 `window.__idleReadHook__`(읽기 전용, `animal.getPose()`+`animal.inspect()`를 그대로 노출)은 검증 완료 후 제거.

**3. 앱 시작 시 펫 위치를 창 중앙으로**: 참고 스크린샷(`assets/reference/window-capture-initial-position.png`)이 실제로는 폴더에 없어서(이전 세션들의 "bug-tail-ear-glitch.png"/"glitch-ear-head-seam.png"와 같은 패턴 — 스크린샷 자체는 못 봄) 텍스트 설명(창 왼쪽 아래 구석)과 코드 분석만으로 진행. `windows/pet/pet.js`의 `let x = MARGIN`(=16, 패트롤이 있던 시절 왼쪽 끝에서 시작하던 값이 패트롤 제거 이후에도 안 바뀐 채 남아있던 것)을 `let x = ((stageEl.clientWidth||340) - WRAP_WIDTH) / 2`로 교체 — `bounds()`가 반환하는 `[min,max]` 범위의 정중앙과 대수적으로 동일한 값(MARGIN이 상쇄됨).
  - **세로는 의도적으로 안 건드림**: `#pet-wrap`의 `bottom:14px` CSS 앵커(땅 위에 서 있는 포즈/그림자/다리 접힘 전부 이 고정된 바닥선을 전제로 authored됨)는 그대로 유지 — 세로까지 창 정중앙으로 옮기면 펫이 허공에 붕 뜬 것처럼 보일 것이므로, "구석에 박혀 시작하는" 문제의 핵심은 가로 위치라고 판단(사용자가 "정중앙"이라고 표현했지만 세로까지 포함한 요구인지는 확인 안 된 판단 호출 — 필요하면 언제든 조정 가능).
  - **검증**: `test/launch-startup-position-check.js`로 실제 pet 창을 띄워 로드 직후 스크린샷 캡처, 알파 채널 바운딩박스로 캐릭터 중심의 X좌표가 스테이지 폭의 정확히 50.5%(기존엔 약 19%)에 위치함을 확인 — 스크린샷으로도 고양이가 창 중앙, 바닥에 선 채로 시작하는 것을 눈으로 확인.

**전체 회귀 확인**: `npm run test:invariants`(120만 프레임)/`test:movement-stats`/`test:idle-rhythm` 전부 재실행 — 위반 0건, 기존 베이스라인과 일치. `main.js`/`preload.js`는 이번 라운드에서 전혀 안 건드림. `pet.js`의 임시 훅(`__idleReadHook__`)은 제거 확인. 실제 앱(`npx electron .`)을 10초간 띄워 확인 — 기존에 이미 알려진 GPU 디스크 캐시 권한 경고 외 콘솔 에러 없이 정상 구동 후 종료.

### 정면 각도 부위-경계 선 삐져나옴 + 턱시도 발 지지직 + "제자리 스케일만" approach 삭제 (stage 4 이후 11차 QA 라운드)
10차 QA 라운드(전체 종 z-nudge 스윕 + prowlcircle 재검증 + 시작 위치 중앙 정렬) 이후 스크린샷(`assets/reference/front-seam-lines-bug.png`)을 첨부한 리포트 2건 + 신규 삭제 요청 1건. 사용자가 1번의 원인을 "접합부 깊이(depth)를 키운 게 회귀 원인"이라고 정확히 짚었는데, 실제로 파고들어보니 "깊이를 더 세밀하게 조정"하는 방향으로는 애초에 답이 없는 구조적 트레이드오프였음이 드러나서 — 튜닝이 아니라 메커니즘 자체를 바꾸는 쪽으로 감.

**1. 정면 각도에서 다리-몸통/몸통-얼굴 경계에 선이 삐져나와 보임**: 첨부 스크린샷은 정면을 보는 고양이의 목과 어깨 높이에 몸통 폭보다 넓은 어두운 가로줄이 양옆으로 튀어나온 모습 — 10차 라운드가 `addSeamCollar`(부위 경계에 색이 다른 접합부를 표시하는 얇은 띠, 머리-몸통이 같은 색인 종에서 실루엣이 뭉개지는 걸 막기 위한 장치)의 `depthSpan`(접합부 띠의 로컬 Z 두께)을 `SEAM_Z_NUDGE_INHERITED`(≈1.28)/`SEAM_Z_NUDGE_STANDALONE`(1.9)만큼 크게 부풀려서 몸통 뒤에 깊이 파고든 비대칭 이웃(예: 닥스훈트는 몸통이 머리 중심 X좌표까지 거의 닿음)을 상대로 z-테스트를 이기게 만들었던 바로 그 수정이 원인이었음.
  - **왜 "깊이를 더 세밀히 조정"으로는 못 고치는지 (수학적으로 불가능함을 먼저 확인)**: 이 파일의 좌표 컨벤션상 로컬 Z(접합부 띠의 "두께")는 0°/180°(옆모습)에서는 화면 깊이축(안 보임)이지만 90°/270°(정면)에서는 화면 **가로폭** 축이 됨(모듈 코멘트 참고) — 즉 10차 라운드가 "z-테스트를 이기기 위해 깊이를 키운 것"과 이번에 리포트된 "정면에서 몸통보다 넓게 튀어나와 보이는 것"은 **완전히 같은 수치**임(몸통/머리가 전부 같은 `DEPTH` 상수를 공유하므로, 접합부 띠의 깊이가 그 `DEPTH`를 넘어서야 z-테스트를 이길 수 있는데, 정면에서는 그 초과분이 그대로 "몸통보다 넓다"는 뜻이 됨). 10차 라운드 자신의 코멘트("the collar's own edges poke out past whatever's occluding its center")가 이미 이 메커니즘을 정확히 설명하고 있었음 — z-테스트를 이기는 방식 자체가 곧 튀어나오는 방식이었던 것이라, 어떤 깊이 값을 골라도 "이기면서 안 튀어나오는" 값은 존재할 수 없음.
  - **고침 (메커니즘 교체, 값 조정 아님)**: 접합부 띠는 애초에 z-버퍼 경쟁에서 이길 필요가 없는, 순수 장식용 경계선일 뿐이라는 데 착안 — `mesh.material.depthTest = false` + `mesh.material.depthWrite = false` + 높은 `renderOrder`(999)로 항상 다른 모든 불투명 지오메트리 위에 그려지게 바꿈. z-버퍼 경쟁 자체가 사라지므로 `depthSpan`을 더 이상 부풀릴 필요가 없어져서, 이제는 그 접합부가 실제로 얹히는 부위(머리는 `DEPTH+HEAD_Z_NUDGE*2`, 다리는 `legW`)와 정확히 같은 값을 씀 — 정면에서 몸통/다리와 정확히 같은 화면 폭이 되어 더 이상 튀어나오지 않음. `SEAM_Z_NUDGE_INHERITED`/`SEAM_Z_NUDGE_STANDALONE` 상수는 완전히 삭제.
  - **검증**: `test/diag-front-seam-poke.js`(신규, 재사용 가능한 도구) — 10종 × 8각도(0~315°, 45° 간격) 전부에서 (a) 접합부 색이 화면에 여전히 존재하는지(10차 라운드가 확립한 가시성 회귀 확인, `test/diag-seam-sweep-all-species.js`로도 별도 재확인 — 전부 OK), (b) 접합부 색 픽셀의 X범위가 나머지 몸체(접합부 색 제외) 픽셀의 X범위를 2px 이상 벗어나는지(튀어나옴 판정) 검사 — **80개 조합 전부 튀어나옴 0px로 통과**. 고치기 전 코드로 잠깐 되돌려 같은 각도(cat_a/dog_husky 270°)를 다시 렌더링해보니 목/어깨 높이에 첨부된 버그 스크린샷과 시각적으로 동일한 튀어나온 가로줄이 그대로 재현됨(`test/seam-fix-check/BEFORE-{cat_a,dog_husky}-270.png`) — 고친 뒤 같은 조건(`test/seam-fix-check/{cat_a,dog_husky}-270.png`)에서는 완전히 사라짐. `npm run test:invariants`(120만 프레임) 재실행 — 위반 0건(이 애니메이션 로직 자체는 안 건드렸으므로 예상대로).

**2. 턱시도 고양이 발쪽이 아직 살짝 지지직거림**: 1번과 같은 "접합부" 계열 문제일 거라는 사용자 추측과 달리, 원인은 전혀 다른 곳(부위 경계 칼라가 아니라 다리 관절 체인 자체)에 있었음 — `addLegChain`에서 발끝 색(`cfg.pawColor`, 로스터 중 턱시도 하나만 사용 — grep으로 확인)이 있는 종은 `lowerMesh`(종아리 전체, `lowerLen` 높이)를 먼저 그린 뒤 `pawMesh`(발끝, `lowerLen*pawFraction` 높이)를 그 **위에 겹쳐서** 세웠는데, 두 박스가 X/Y/Z 세 축 전부 정확히 같은 폭(`lowerW`)이라 발끝 구간에서는 종아리와 발끝 박스가 **완전히 같은 3D 공간을 통째로 공유**하는 상태였음 — 이전에 있었던 belly/pattern z-fighting(작은 마진 다툼)보다 훨씬 심한, "부분적으로 겹침"이 아니라 "그 구간 전체가 완전히 포개짐"이라 어느 색이 이길지가 프레임마다 정의되지 않는 전형적 z-fighting.
  - **고침 (겹쳐 쌓기 → 이어 붙이기)**: `lowerMesh`가 종아리 전체 대신 **발끝 아닌 부분만**(`boneLen = lowerLen*(1-pawFraction)`, `pawColor`가 없는 나머지 9종은 `pawFraction=0`이라 기존과 100% 동일) 차지하도록 높이를 줄이고, `pawMesh`는 그 나머지(`boneLen`~`lowerLen`)를 정확히 이어서 채우도록 위치를 조정 — 이제 두 박스는 경계선 하나만 공유할 뿐 부피를 전혀 공유하지 않아, z-버퍼가 뭘 골라야 할지 애매한 상황 자체가 구조적으로 사라짐(2D 엔진은 캔버스에 겹쳐 그리기만 하면 됐지만 — z-fighting 개념이 없으므로 — 3D는 겹치는 대신 나눠 붙이는 쪽으로 대응).
  - **검증 (기하 + 픽셀 둘 다)**: (a) 기하 — `getLegDebug()`에 `lowerLocalYRange`/`pawLocalYRange`(신규 필드, 두 메시의 **로컬** Y 범위)를 추가해 겹침을 직접 계산 — 고치기 전 0.525유닛 겹침 → 고친 뒤 정확히 0. (다리가 회전된 상태에서 WORLD 공간 `Box3`로 겹침을 재려 했던 1차 시도는 회전된 박스를 감싸는 축정렬 바운딩박스가 실제보다 부풀려져서 겹침이 있는 것처럼 오판하는 함정에 빠졌음 — 같은 부모 아래 회전이 서로 안 걸린 두 메시는 **로컬** Y 범위로 비교해야 정확하다는 걸 이번에 확인, `getLegDebug`의 자체 코멘트로 남겨둠.) (b) 픽셀 — `test/diag-neck-zfight.js`/`diag-tabby-zfight.js`가 확립한 "0.1도 미만 회전차로 두 프레임을 비교해 픽셀이 완전히 다른 색으로 뒤바뀌는지" 기법을 재사용(`test/diag-tuxedo-paw-zfight.js`, 신규) — 고치기 전 발 부근(화면 하단, raw 버퍼 기준 y≤20)에서 다리색(#161616)↔발색(#ffffff) 간 뒤바뀜이 8건 발생, 고친 뒤 0건(다른 이유로 존재하는, 이 수정과 무관한 잔여 흔들림 — 아래 참고 — 은 그대로 남아있지만 발끝 특유의 색 조합·위치는 완전히 사라짐). 확대 스크린샷(`test/seam-fix-check/tuxedo-paw-crop-{000,270}.png` vs `BEFORE-tuxedo-paw-crop-cat_tuxedo.png`)으로도 발끝 흰 양말과 검정 다리 사이 경계가 깨끗한 직선인지 눈으로 재확인.
  - **잔여 흔들림 분리 (오탐 방지)**: 위 픽셀 진단에서 화면 중간 높이(raw y≈30~46)에 다리색/발색과 우연히 똑같은 헥스값(턱시도는 `bodyColor`도 `#161616`, `belly`도 `#ffffff`라 다리/발과 정확히 같은 색)을 쓰는 몸통-배 경계가 있어서, 색만으로 판정하면 이 무관한 부위까지 "발 z-fight"로 오판될 뻔했음 — 고치기 전 코드로 되돌려 같은 진단을 다시 돌려본 결과 이 중간 높이의 흔들림은 **고치기 전/후 완전히 동일**(바이트 단위로 같은 좌표·같은 색)해서, 발끝 수정과는 무관한 이 렌더러의 기존 잔여 지터(안티앨리어싱을 껐을 때 서브 픽셀 회전에 따라 경계가 다른 픽셀에 랜딩하는 현상, 8차 라운드의 `PATTERN_OVER_BODY_NUDGE` 계열과 같은 부류)임을 확인 — 진단 스크립트의 판정 조건에 "발 위치(raw y≤20) **그리고** 다리↔발 색 조합"을 모두 요구하도록 반영해 이 오탐을 걸러냄.

**3. "제자리에서 커졌다 작아졌다"만 하는 `approach` 삭제**: 8차 QA 라운드 이전(round 8 issue 1)에 Z축을 처음 활용하며 추가했던 5개 idle 중 하나 — 카메라 쪽으로 다가왔다 멀어지는 것만 반복하고(`pose.depthZ`만 sine으로 오르내림) 실제 걸음도, 방향 전환도 없는 가장 단순한 버전이었음. 이후 7차 라운드에서 나머지 4개(`prowlcircle`/`chasetail`/`hopspin`/`wheelrun`)는 "이 종의 실제 gait로 걸으며 원을 도는" 방식으로 재설계됐지만 `approach`는 그 재설계 대상에서 의도적으로 빠진 채(원형 경로가 아니라는 이유로) 옛 방식 그대로 남아있었음 — 사용자 표현대로 "원형 걷기랑 겹치는 느낌이고 밋밋"해서 이번엔 재설계 없이 완전히 삭제하기로 함.
  - **삭제 범위**: `IDLE_BEHAVIORS.approach` 정의 전체와, 10종 전부의 `idlePool` 배열에서 `{ name: 'approach', weight: 1 }` 항목(전부 동일한 패턴이라 `replace_all`로 일괄 제거). `pose.depthZ`/`pose.lateralX`/`pose.spinOverride`/`pose.spinAngle` 필드 자체와 `voxel-engine.js`의 `applyPose`(이 필드들을 읽어 `outer.position`/`scale`/`rotation.y`에 반영하는 로직)는 그대로 유지 — `prowlcircle` 등 나머지 4개 idle이 여전히 이 필드들을 쓰므로, `approach`만의 전용 코드(그 idle의 `apply()` 함수 본문)만 걷어내고 공유 인프라는 안 건드림. `test/launch-zaxis-check.js`(round 8 검증 도구, 재사용 목적으로 유지 중)의 job 목록에서도 `approach` 항목 제거 — 더는 존재하지 않는 idle을 기다리며 무한 폴링하는 걸 방지.
  - **검증**: `grep -rn "'approach'"` 전체 재확인 결과 `windows/`/`test/`(오래된 스크린샷 산출물 JSON 제외) 어디에도 매치 없음 — idlePool·`IDLE_BEHAVIORS` 양쪽에서 완전히 제거됐음을 확인. `npm run test:invariants`(120만 프레임)/`test:movement-stats`(전 종 walk 0.0% 유지)/`test:idle-rhythm`(breathe 73~85%, 분당 전환 5.0~7.6회 — 기존 베이스라인과 같은 범위) 재실행 — 위반 0건, approach 삭제가 나머지 idle 풀의 확률 분포·리듬에 부작용을 안 남겼음을 확인(가중치 합이 줄어든 만큼 나머지 항목들의 상대 확률이 소폭 올라가는 게 정상이고 실제로 그렇게 나옴).

**전체 회귀 확인**: `npm run test:invariants`/`test:movement-stats`/`test:idle-rhythm` 전부 재실행 — 위반 0건. `main.js`/`preload.js`/`windows/pet/pet.js`는 이번 라운드에서 전혀 안 건드림(전부 `windows/shared/voxel-engine.js`의 접합부 칼라·다리 체인, `windows/shared/animal-engine.js`의 idle 정의, `windows/pet3d/pet3d.js`의 디버그 접근자 확장만 수정) — `git status`로 확인. 실제 앱(`npx electron .`)을 12초간 띄워 확인 — 강제 종료 시점의 타이머 경합으로 인한 통상적인 셧다운 로그(`Render frame was disposed`, GPU 프로세스 재시작 — 이전 라운드들에서도 나타났던, 앱 코드와 무관한 종료 아티팩트) 외 별다른 에러 없이 정상 구동 후 종료.

### 종별 idlePool 현황 (10종 전체, `animal-engine.js`의 `SPECIES[...].idlePool` 그대로)
숫자는 가중치(`pickIdleBehavior`가 이 가중치에 비례해 뽑음, 값이 클수록 자주 뽑힘) — `breathe`(가만히 숨쉬기, 전 종 공통 가중치 12)가 절대적으로 우세해서 실제 시간 점유율은 위 "breathe idle 추가" 항목 기준 종별 73~86% 수준(표의 숫자는 뽑힐 확률 가중치일 뿐, 지속시간까지 반영한 실제 점유율이 아님에 유의). `–`는 그 idle이 이 종의 `idlePool`에 아예 없다는 뜻. `approach`는 11차 라운드에서 전 종에서 삭제됨(표에서 뺌).

| 종 | groom(그루밍) | sit(앉기)\* | tailtwitch(꼬리까닥임) | stretch(기지개) | earflick(귀실룩임) | sleep(졸기) | bellyUp(뒹굴기) | earalert(귀경계) | nosetwitch(코씰룩임) | cheekpuff(볼주머니) | 원형 걷기\*\* | breathe |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 고양이 A (`cat_a`) | 3 | 2.5 | 2.5 | 2 | 1 | 1 | – | – | – | – | prowlcircle 1 | 12 |
| 턱시도 (`cat_tuxedo`) | 3 | 2.5 | 2 | 2 | 1 | 1 | – | – | – | – | prowlcircle 1 | 12 |
| 삼색이 (`cat_calico`) | 2 | 2.5 | 2.5 | 3 | 1 | 1 | – | – | – | – | prowlcircle 1 | 12 |
| 샴 (`cat_siamese`) | 2.5 | 3 | 2.5 | 2 | 1.5 | 1 | – | – | – | – | prowlcircle 1 | 12 |
| 닥스훈트 (`dog_dachshund`) | – | 3 | – | 1.5 | 2 | 1.5 | 1 | – | – | – | chasetail 1 | 12 |
| 코기 (`dog_corgi`) | – | 3 | – | 2 | 2.5 | 1.5 | 1 | – | – | – | chasetail 1 | 12 |
| 허스키 (`dog_husky`) | – | 2.5 | – | 2 | 3.5 | 1 | 1 | – | – | – | chasetail 1 | 12 |
| 포메라니안 (`dog_pomeranian`) | – | 3 | – | 2 | 2.5 | 1.5 | 1 | – | – | – | chasetail 1 | 12 |
| 토끼 (`rabbit_b`) | – | 2 | – | – | 1 | 1 | – | 3 | 2.5 | – | hopspin 1 | 12 |
| 햄스터 (`hamster`) | – | 2 | – | – | 2 | 1 | – | – | – | 3 | wheelrun 1 | 12 |

\* `sit`은 전 종에 있지만 자세 자체는 `cfg.sitStyle`에 따라 다름 — 고양이 4종은 `loaf`(식빵굽기), 강아지 4종은 `haunches`(뒷다리만 접고 앞다리로 버팀), 토끼는 `loafNoLegs`(뒷다리 거의 완전히 숨김), 햄스터는 `roundPuff`(짧은 다리+볼주머니 살짝 부풀림) — 위 "`sit`을 종별로 분화" 항목 참고.
\*\* "원형 걷기"는 이 종의 실제 gait(걷기/트롯/스캠퍼/홉)로 걸으며 Z축 원을 그리는 4개 idle(`prowlcircle`/`chasetail`/`hopspin`/`wheelrun`, 종마다 이름과 반지름/각속도가 다름 — 위 "Z축 원형 경로 걷기로 재설계"·"고양이 prowlcircle 재튜닝" 항목 참고)을 한 칸에 합친 것. 강아지 4종은 `hasTail:false`인 코기를 포함해 전부 `chasetail`을 가짐 — 코기는 원래 이 idle 자체가 없다가 7차 라운드에서 "꼬리 유무와 무관하게 적용 가능"이 확인돼 추가됨.

### 접합부 칼라 색이 완전히 새까맣게 보임 (stage 4 이후 12차 QA 라운드)
11차 QA 라운드(정면 포크아웃 + 턱시도 발 z-fight + approach 삭제)가 끝난 뒤 스크린샷(`assets/reference/seam-collar-too-black.png`)을 첨부한 리포트 — 목/다리 접합부 칼라가 "은은한 그림자 선"이 아니라 완전히 검은 만화 테두리처럼 보인다는 것. 사용자가 원인을 "지난 라운드에서 `depthTest:false`로 바꾸며 색 계산이 깨졌거나, 색 자체가 고정 검정값일 수 있다"고 추측했는데 — 실제로 확인해보니 색 계산 로직은 정상 동작 중이었고(고정 검정값도 아니었음), 진짜 원인은 그보다 단순한 곳(파라미터 하나)에 있었으면서, 고치는 과정에서 첫 시도 자체에서 새 버그를 하나 만들어냄.

**원인 진단**: `getSeamCollarColors()`(라이브 씬그래프에서 실제 렌더링 중인 머티리얼 색을 그대로 읽어오는, 기존에 이미 확립된 디버그 접근자)로 `dog_pomeranian`(첨부 스크린샷과 같은 종으로 추정)의 목 칼라 색을 직접 읽어보니 정확히 `darkenColor('#e8c896', 0.55)` = `#a28b68`였음 — 즉 **고정 검정값이 아니라 실제 부위 색(`bodyColor`)을 정확히 참조해서 계산되고 있었고**, `depthTest:false` 전환도 색 계산 자체와는 무관(그 변경은 렌더링 순서만 건드림, `material.color`엔 손을 안 댐)했다는 게 확인됨 — 두 가설 다 기각. 진짜 원인은 그보다 훨씬 단순한 `SEAM_COLOR_DARKEN = 0.55`(검정 쪽으로 55% 이동) 자체 — 9차 라운드에서 이 상수가 처음 정해질 때는 "머리-몸통이 완전히 같은 색이라 실루엣이 뭉개지는 걸 막을 만큼 확실히 구분되는 선"이 목표였어서 상당히 공격적인 값으로 튜닝됐던 것인데, 이번 요청("1px 정도 살짝 어두운 선, 은은하게")은 그보다 훨씬 약한 대비를 원하는 것이라 값 자체가 안 맞았던 것.

**1차 시도의 함정 — "예: 15~20%"를 문자 그대로 0.18로 넣었다가 색상 역전 버그 발생**: 요청 문구의 "예: 밝기를 15~20% 정도만 낮추는 정도"와 "그 접합부의 두 부위 색을 참고해서"를 같이 반영하려고, 먼저 두 부위 색을 50/50으로 평균(`blendColors`)한 뒤 그 평균을 0.18만큼 어둡게 만드는 방식을 시도 — 8/10종(머리-몸통이 같은 색)에서는 자기 자신과 평균 내는 셈이라 문제가 없었지만, **cat_siamese에서 색이 원래 부위보다 오히려 밝아지는 역전 버그**가 생김: siamese는 머리(짙은 seal-brown `#5c4636`)와 몸통(밝은 크림 `#e8d9bb`)이 극단적으로 다른데, 이 둘을 평균 내면 중간톤이 나오고, 거기서 18%만 어둡게 해도 원래 머리색 자체보다 여전히 밝은 값이 됨 — `getSeamCollarColors()`로 직접 읽어서 밝기(Rec.709 상대휘도)를 비교해보니 머리색 대비 **-107%**("어두워짐"이 아니라 "더 밝아짐")로 나와서 실제로 발견함(스크린샷만 봤으면 "그냥 살짝 다른 색이네" 정도로 넘어갔을 수도 있는 걸, 수치로 재보고서야 방향 자체가 반대라는 걸 확인). "두 색을 평균 내는" 방식 자체가, 두 부위의 밝기 차이가 큰 경우엔 원천적으로 "둘 다보다 어둡다"를 보장 못 한다는 게 문제였음.
  - **고침 (평균 대신 "더 어두운 쪽" 선택)**: `blendColors`(평균)를 버리고 `seamBaseColor(colorA, colorB)`(기존 수염 자동대비 기능에 이미 있던 `relativeLuminance`를 재사용해 둘 중 상대휘도가 더 낮은, 즉 더 어두운 색을 그대로 선택)로 교체 — 그 결과를 `darkenColor`로 한 번 더 어둡게 만듦. 이러면 결과가 항상 "더 어두운 쪽보다도 어두운" 값이 되는 게 구조적으로 보장됨(두 부위가 같은 색인 8/10종에서는 그 색 자체가 그대로 나와 기존과 동일). siamese 재검증 결과 머리색(`#5c4636`) 대비 정확히 **+17.3%** 어두워진 `#4c3a2c` — 방향이 정상으로 돌아옴.
  - **darken 값 재보정 (0.18 → 0.32, 색공간 문제를 먼저 확인하고 조정)**: "15~20%"를 그대로 `darkenColor`의 `amount` 인자에 넣었던 0.18은 실제로 렌더링해보니 지나치게 옅어서(라이브 색을 상대휘도로 재보니 대부분 종에서 **8~9%**밖에 안 어두워짐 — 요청한 15~20%의 절반 수준), 값이 왜 어긋나는지 먼저 확인함: `darkenColor`의 `lerp`는 THREE.Color의 내부 **선형(linear)** 색공간에서 계산되는데, "몇 % 어두워 보이는지"는 사람 눈이 판단하는 **감마(sRGB)** 공간 기준이라 이 둘이 1:1로 대응하지 않음(선형 공간에서 18% 줄이는 것과 sRGB에서 지각적으로 18% 어두워 보이는 것은 다른 수치) — 실제로 필요한 `amount` 값을 여러 후보(0.15/0.18/0.20/0.25/0.28/0.30/0.32/0.34/0.35)로 직접 계산해서 상대휘도 하락폭이 정확히 15~20% 구간에 들어오는 지점을 찾음, `0.32`에서 로스터 대표 색상들이 **15~17%** 하락으로 요청 구간에 안착함을 확인 후 채택.
  - **왜 55%가 "완전 검정"처럼 보였는지 재확인**: 0.55는 선형 공간에서도 절반 이상을 검정 쪽으로 이동시키는 값이라 상대휘도 하락폭이 대부분 종에서 30% 이상(예: 포메라니안 기준 실측 약 45%대)이었고, 여기에 더해 원래 몸 색 자체가 밝은 파스텔톤(포메라니안 크림, 토끼/삼색이 거의 흰색)인 종일수록 "밝은 배경 위의 진한 선"이라는 대비가 실제 수치 이상으로 도드라져 보였을 것 — 스크린샷으로 재확인.
  - **검증**: `test/diag-seam-color-subtlety.js`(신규, 재사용 가능한 도구) — 10종 전부의 목/어깨 칼라를 `getSeamCollarColors()`로 라이브로 읽어 그 부위의 실제 색 대비 상대휘도 하락폭을 계산 — 같은 색끼리 접하는 8종은 전부 **15~17%**(턱시도만 원래 색 자체가 이미 거의 검정이라 22.7%, 그래도 색 자체 변화폭이 작아 육안으로는 거의 안 보임), 색이 다른 두 부위가 만나는 husky/corgi의 다른 한쪽은(더 밝은 쪽 기준으로 비교하면) 40~64%로 크게 나오지만 이는 "그 접합부가 원래 더 어두운 쪽 색을 기준으로 32%만큼 더 어두워진 것"을 밝은 쪽과 비교했을 때 생기는 정상적인 결과(콜러포인트/투톤 종에서 두 색 경계가 자연스러운 음영처럼 보이는 것과 같은 이치)이지 버그가 아님 — 실제 스크린샷으로도 husky/corgi 둘 다 자연스러운 음영 선으로 보임을 확인(아래).
  - **스크린샷 확인 (10종 전부, 정면 270°/측면 0°)**: `test/launch-seam-fix-check.js`(11차 라운드 도구, 재사용)로 재렌더링 — 10종 전부 목/어깨 선이 더 이상 검정 띠가 아니라 그 부위 색과 거의 같은 톤의 은은한 그림자 선으로 보임 확인(`test/seam-fix-check/{종}-000.png`/`{종}-270.png`). 첨부된 버그 스크린샷과 가장 닮은 `dog_pomeranian-270.png`로 직접 비교 — 이전엔 뚜렷한 회색-검정 띠였던 자리가 이제 몸통과 거의 구분 안 될 정도로 옅은 탠(tan) 선으로 바뀜.
  - **회귀 확인**: 11차 라운드의 두 수정(정면 포크아웃 방지, 턱시도 발 z-fight)은 `addSeamCollar`의 `depthTest`/`renderOrder`/`depthSpan` 로직을 안 건드리고 색 계산 부분만 바꿨으므로 영향 없을 것으로 예상 — `test/diag-front-seam-poke.js`(10종×8각도 전부 여전히 통과, `visible=true`/poke 0px 유지 - 라이브 색을 그대로 읽어와 비교하는 방식이라 색이 바뀌어도 자동으로 맞춰 검증됨)와 `test/diag-tuxedo-paw-zfight.js`(여전히 PASSED) 재실행으로 확인. `npm run test:invariants`(120만 프레임) 위반 0건(이 로직은 애초에 색 계산과 무관).

**전체 회귀 확인**: `npm run test:invariants` 재실행 — 위반 0건. `main.js`/`preload.js`/`windows/pet/pet.js`/`animal-engine.js`는 이번 라운드에서 전혀 안 건드림(전부 `windows/shared/voxel-engine.js`의 색 계산 헬퍼(`blendColors` 도입 후 바로 `seamBaseColor`로 교체)와 `SEAM_COLOR_DARKEN` 상수만 수정) — `git diff`로 확인. 실제 앱(`npx electron .`)을 10초간 띄워 확인 — 기존에 이미 알려진 GPU 디스크 캐시 권한 경고·강제 종료 시점의 타이머 경합 로그 외 별다른 에러 없이 정상 구동 후 종료.

### 12차 라운드 수정이 "실제 npm start 앱"에서는 반영 안 된 것처럼 보인 문제 재조사 (stage 4 이후 13차 QA 라운드) — 코드 버그 없음, 원인은 눈/코를 칼라로 오인
12차 QA 라운드(`SEAM_COLOR_DARKEN` 재보정) 직후 "diagnostic 스크립트에서는 맞다고 했는데 실제 `npm start` 앱(껐다 켜도 동일)에서는 여전히 목/다리 경계가 새까맣다"는 리포트(`assets/reference/seam-still-black-real-app.png`) — 사용자가 과거 `setBodyPose` 라운드처럼 "진단 스크립트/`pet3d.js` 프로토타입 경로와 실제 `pet.js` 프로덕션 경로가 갈라져 있는 것 아니냐"는, 근거 있는 가설을 제시함(캐싱/빌드 stale 가능성도 함께). 순서대로 전부 직접 확인.

**사전 점검 (코드 레벨)**: (1) 코드 경로 분기 — `windows/pet/pet.js`와 `windows/pet3d/pet3d.js` 둘 다 `import { buildVoxelCreature } from '../shared/voxel-engine.js'`로 **같은 파일**을 가져다 씀(복제본 없음, `grep`으로 `addSeamCollar`/`SEAM_COLOR_DARKEN` 정의가 정확히 한 곳뿐임을 재확인). 렌더러 셋업(`WebGLRenderer` 옵션, 라이트 유무, tone mapping/colorSpace 오버라이드 여부)도 대조해봤지만 색 계산에 영향을 줄 만한 차이 없음(`pet.js`는 `alpha:true`, `pet3d.js`는 `alpha:false`일 뿐 — 둘 다 opaque 지오메트리라 알파 프리멀티플라이가 RGB에 영향을 못 줌). (2) 캐싱 — `buildVoxelCreature`는 `loadCharacter()`가 호출될 때마다 새 `THREE.Group`/새 `MeshBasicMaterial`을 만드는 순수 함수, 종별 캐시/메모이제이션 없음. (3) 빌드/번들 — `package.json`에 번들러 없음(`"start": "electron ."`), `dist`/`build` 산출물 자체가 존재하지 않음(`find`로 확인) — `main.js`가 `windows/pet/index.html`을 소스 그대로 `loadFile`. 세 가설 다 코드상으로는 근거를 못 찾음 — 그래도 사용자가 명시적으로 "diagnostic 결과 말고 진짜 실행 중인 앱 스크린샷"을 요구했으므로 여기서 멈추지 않고 실제로 확인.

**실제 앱 재현 (임시 훅, 검증 후 제거)**: `main.js`/`windows/pet/pet.js`에 `FOCUSPET_DEBUG_SEAM` 게이트 임시 훅을 걸어 — `main.js`가 실제로 생성하는 그 `petWindow`(설정 파일의 실제 `character: "dog_pomeranian"` 반영)에 프로덕션 IPC 채널(`preview-character`/`cursor-track`, `pet.js`가 실제로 구독하는 바로 그 채널)만으로 캐릭터 전환·회전을 구동하고 `capturePage()`로 스크린샷을 뜸. 추가로 `pet.js`에 `window.__seamDebugHook__`(pet3d.js의 `getSeamCollarColors()`와 동일한 기법 — `group.traverse`로 `userData.isSeamCollar` 메시의 `material.color`를 직접 읽음, 단 pet3d.js가 아니라 **이 창의 실제 프로덕션 `group`** 대상)과 `window.__gridDumpHook__`(GL `readPixels`로 96×96 캔버스 전체를 픽셀 단위 텍스트 그리드로 덤프)을 임시로 추가해 실제 렌더링 결과를 직접 읽음.

**결과 — 콜라 자체는 정상, "검게 보이는 것"의 정체는 눈+코였음**: `dog_pomeranian` 목 칼라의 실제 `material.color`가 정확히 `#c3a87e`(12차 라운드가 의도한 그 값)로 5개 칼라(목 1 + 다리 4) 전부 일치, `cat_a`/`dog_husky`/`cat_siamese`/`dog_corgi`도 각각 종별 기대값과 일치 — **프로덕션 코드 경로에 코드 버그 없음**(setBodyPose 라운드 때와 달리 이번엔 진짜로 경로가 갈라져 있지 않았음). GL 픽셀 그리드로 목 부위를 실제로 스캔해보니, 목 칼라는 실측 `#c9a06a`(재질색과 소수점 오차 이내로 일치, relLum 0.646)로 렌더링되고 있었고, 사용자가 "새까맣다"고 지목한 자리에 실제로 존재하는 훨씬 어두운 색(`#2a1c14` relLum 0.119, `#4a3527` relLum 0.221)은 **칼라가 아니라 `cfg.eyeColor`/`cfg.darkColor`** — `dog_pomeranian`의 SPECIES 테이블에 `eyeColor:'#2a1c14', darkColor:'#4a3527'`로 그대로 authored돼 있는, 6차 QA 라운드("정면 디테일 보강")부터 있던 완전히 별개의 얼굴 디테일(눈동자/코)이었음 — `SEAM_COLOR_DARKEN`/`addSeamCollar`와는 아무 관련 없는 코드 경로.
  - **사용자 스크린샷과 직접 대조**: `assets/reference/seam-collar-too-black.png`(12차 라운드 원본 리포트 사진)와 이번 `seam-still-black-real-app.png`를 8배 확대해서 재조사 재현 스크린샷과 나란히 비교 — 둘 다 정확히 같은 구도(눈 + 코가 목 칼라 바로 위/근처에 몰려있는 3/4 각도)에서 눈(작은 검은 사각형, 흰 하이라이트 1px 포함)과 코(눈 아래 진한 갈색 사각형)가 서로 붙어있고, 그 바로 아래로 옅은 목 칼라 선이 지나가는 걸 확인 — 사용자가 빨간 펜으로 두른 영역이 정확히 이 눈+코 뭉치와 겹침. 96×96이라는 작은 해상도에서 눈(코 위 ~2~4px)·코(그 아래 ~4px)·목 칼라(그 아래 얇은 1px 띠)가 세로로 촘촘히 붙어있다 보니, 실제로는 밝기가 뚜렷이 다른 세 요소(눈 relLum 0.12, 코 relLum 0.22, 칼라 relLum 0.65)가 사람 눈에는 "얼굴 아래쪽이 통째로 거뭇하다"는 하나의 인상으로 뭉쳐 보였을 가능성이 큼 — 12차 라운드의 `diag-seam-color-subtlety.js`/`launch-seam-fix-check.js`는 칼라의 **재질 색**과 정면(270°)/측면(0°) 위주로만 검증했지, 이 3/4 각도에서 눈·코와 나란히 놓였을 때의 "종합적 인상"까지는 스크린샷으로 재확인하지 않았던 게 이번에 놓쳤던 지점.
  - **다른 4종도 동일 패턴**: `cat_a`(눈 relLum 0.11, 수염이 아니라 눈이 어두움)/`dog_husky`(눈 relLum 0.07~0.12)/`cat_siamese`(눈 relLum 0.12, 얼굴 자체가 이미 어두운 seal-brown이라 칼라와의 대비가 원래 낮음)/`dog_corgi`(눈 relLum 0.10~0.16) 전부, 캔버스에서 가장 어두운 5개 색 중 진짜 칼라 색(relLum 0.5 이상)은 하나도 없고 전부 눈/코였음 — 5종 × 3각도(정면/좌우 3/4) 전부 동일한 결론.
  - **결론**: 코드 수정 없음 — 12차 라운드의 `SEAM_COLOR_DARKEN=0.32`/`seamBaseColor` 로직은 프로덕션 경로에서도 정확히 의도대로 동작 중. 눈/코가 "칼라보다 훨씬 어두운 게 당연한, 완전히 별개의 얼굴 디테일"이라는 사실 자체는 실제 문제 삼을 근거가 없어 보여, 사용자에게 이 구분을 스크린샷 대조로 설명 — 눈/코 자체를 옅게 하고 싶다는 별도 요청이 온다면 그건 6차 라운드의 얼굴 디테일 기능(수염 자동대비와 같은 자리)을 손보는, `SEAM_COLOR_DARKEN`과는 다른 작업이 될 것.
  - **정리한 것**: 임시 훅(`main.js`의 `FOCUSPET_DEBUG_SEAM` 블록, `pet.js`의 `__seamDebugHook__`/`__gridDumpHook__`/`__rowDumpHook__`)은 검증 직후 전부 제거(`git diff`로 프로덕션 파일에 잔여물 없음 확인) — 대신 같은 조사를 pet.js 소스를 전혀 안 건드리고 재현할 수 있는 `test/launch-seam-realapp-check.js`(신규, 재사용 가능)를 남김: 실제 `windows/pet/index.html`을 `preview-character`/`cursor-track` IPC만으로 구동하고, `#pet-canvas`의 공개 WebGL 컨텍스트로 `readPixels`해 캔버스에서 가장 어두운 색 5개와 각각의 상대휘도를 출력 — pet.js 내부에 debug 훅을 심지 않고도 "지금 화면에서 제일 어두운 게 진짜 칼라인지 아닌지"를 바로 재현/재확인할 수 있음(앞으로 비슷한 "여전히 검다" 리포트가 오면 이 스크립트부터 돌려볼 것). `test/seam-realapp-check/`에 5종×2~3각도 스크린샷을 남겨둠.

### 접합부 칼라 밝기 재조정 + 데칼(흰 무늬) 인식 색 계산 (stage 4 이후 14차 QA 라운드)
13차 QA 라운드(눈/코 오인 조사) 이후 신규 요청 2건 — 둘 다 접합부 칼라(`addSeamCollar`, 9/11/12차 라운드에서 다듬어온 것) 관련.

**1. `SEAM_COLOR_DARKEN`을 지금(0.32)보다 더 연하게**: 요청 자체는 파라미터 하나 낮추는 단순한 일이지만, 12차 라운드가 이미 "linear-space `amount`와 perceived sRGB 밝기 하락률이 1:1로 안 움직인다"(예: `amount=0.18`이 겨우 8~9%의 인지 하락만 냄, `amount=0.32`가 15~17%를 냄)는 걸 숫자로 확인해뒀으므로, 이번에도 감으로 값을 낮추는 대신 같은 측정 방법(`getSeamCollarColors()`로 라이브 머티리얼 색을 읽어 `relativeLuminance` 하락률 계산)을 재사용해 후보값을 실측. `test/diag-seam-color-subtlety.js`(12차 라운드 도구, 아래 issue 2 대응을 위해 이번에 `seamPart` 필터링 방식으로 갱신 — 뒤에서 설명)로 여러 값을 실측한 결과 `0.2`가 로스터 대부분(같은 색끼리 붙는 8종)에서 **9~14%** 하락(예: cat_a 9.9%, cat_siamese 10.0%, hamster 9.4%)으로, 0.32의 15~17%보다 뚜렷이 더 옅으면서도(요청한 "지금보다 덜 눈에 띄게") 0%에 가깝게 사라지지도 않는(요청한 "너무 낮춰서 안 보이게는 하지 말고") 지점에 안착 — 채택. 색이 서로 다른 두 부위가 만나는 husky/corgi 어깨는 여전히 큰 퍼센트(35.6%/60.5%)로 나오지만 이건 12차 라운드가 이미 "밝은 쪽 기준으로 비교하면 원래 크게 나오는 게 정상"이라고 확립해둔 종류의 수치라 이번에도 문제 삼지 않음.
  - **검증**: `test/launch-seam-round14-check.js`(신규, 재사용 가능한 도구)로 `SEAM_COLOR_DARKEN`을 잠깐 0.32로 되돌린 뒤(`windows/shared/voxel-engine.js`를 임시로 되돌렸다 복원 — 이 파일이 아직 git에 커밋된 적 없는 새 파일이라 `git stash`로는 되돌릴 수 없어서, 수정 전 내용을 `/tmp`에 백업해두고 되돌렸다 복원하는 방식을 씀) 8종(issue 2 대상 5종 + 대조군 3종)을 0°/45°로 렌더링해 "before" 스크린샷을 뜨고, 수정된 파일을 복원해 같은 조건으로 "after"를 뜸 — 두 세트를 픽셀 단위로 비교하는 `test/diff-seam14-tuxedo.js`/`diff-seam14-others.js`(신규)로 실제로 달라진 영역만 확대 크롭해서 확인(아래 issue 2 검증에서 재사용). `npm run test:invariants`(120만 프레임) 재실행 — 위반 0건(이 로직은 2D 엔진과 무관하므로 예상대로).

**2. 목 칼라가 흰 가슴/배 무늬(데칼) 위에서 튀어나와 보임 (턱시도 고양이)**: 스크린샷(`assets/reference/tuxedo-seam-on-white-patch.png`)을 8배 확대해서 확인 — 흰 가슴/배 패치(`cfg.belly`) 바로 위 경계에 진회색 가로줄이 흰 배경 위에 도드라져 보임. 원인을 추측이 아니라 실측으로 확인: `windows/pet3d/pet3d.js`에 새 디버그 접근자 `getMeshesByColor(hex)`(주어진 색과 정확히 일치하는 모든 메시의 월드 AABB를 반환 — `getBBoxDebug`/`getSeamCollarColors`와 같은 성격의 재사용 가능한 도구)를 추가하고 `test/diag-tuxedo-seam-belly-overlap.js`(신규)로 확인 — 목 칼라의 실제 AABB(x:[3.4,10.5], y:[5.75,6.25])와 흰 belly 데칼의 AABB(x:[-5.6,4.5], y:[3,6])가 x:[3.4,4.5]·y:[5.75,6] 구간에서 실제로 겹침을 수치로 확인(추측이 아니라 정확한 좌표로). 원인: 목 칼라의 틴트가 `seamBaseColor(headColor||bodyColor, bodyColor)`(9/12차 라운드 로직)만 참조 — 턱시도는 `bodyColor`가 거의 검정(`#161616`)이라, 칼라가 실제로 흰 데칼과 겹치는 자리에서도 "몸통은 검다"는 사실만 반영해 검정을 어둡게 만든 색을 그대로 썼던 것.
  - **전 종 스캔 (턱시도 하나만의 문제가 아니었음)**: 요청이 턱시도 사례 하나였지만, 고치기 전에 10종 전체의 `headBottomY2D`(목 칼라가 위치하는 2D-Y 좌표)와 `cfg.belly`/`cfg.pattern`의 X/Y 범위를 대조해본 결과 **cat_calico(주황 패치)·dog_corgi·dog_husky·dog_pomeranian(전부 크림/흰 패치)도 목 칼라 자리에 데칼이 실제로 겹침**을 발견 — 턱시도만큼 극적이진 않지만(이 4종은 `bodyColor` 자체가 이미 밝은 편이라 불일치가 작았을 뿐, 0은 아니었음) 같은 종류의 버그였음. 그래서 턱시도 전용 패치가 아니라 전 종 대응 가능한 범용 로직으로 구현.
  - **고침**: `decalColorAt(cfg, x2d, y2d)`(신규) — 그 2D 좌표에 실제로 `cfg.belly` 또는 `cfg.pattern`(배열, belly가 먼저 — 이 파일의 기존 belly-over-pattern Z-우선순위, `BELLY_OVER_PATTERN_NUDGE > PATTERN_OVER_BODY_NUDGE`,와 일치)이 그려지고 있으면 그 색을 반환. `addNeckSeamCollar(headMesh, cfg, headBottomY2D, depthSpan)`(신규, 기존 `addSeamCollar` 단일 박스 호출을 대체) — 목 칼라의 가로 폭(`headX`~`headX+headW`)을 `NECK_COLLAR_SAMPLE_STEP=0.25`유닛 간격으로 스캔해서 각 지점에 데칼이 있으면 그 데칼 색을, 없으면 기존 `seamBaseColor` fallback 색을 틴트로 채택하고, 인접한 동일 틴트 샘플을 하나의 박스로 병합(데칼이 하나도 없는 5종은 전부 같은 fallback 색으로 병합돼 정확히 기존과 동일한 단일 박스 하나로 귀결 — 회귀 없음). 각 세그먼트는 `darkenColor(그 자리의 실제 색, SEAM_COLOR_DARKEN)`로 어둡게 — "검정 위에선 검정보다 살짝 어둡게, 흰 무늬 위에선 흰색보다 살짝 어둡게"라는 요청을 문자 그대로 구현.
  - **부수 정리**: `addSeamCollar`/`addNeckSeamCollar` 둘 다 쓰는 `depthTest`/`renderOrder`/`userData` 설정 코드를 `finalizeSeamCollarMesh(mesh, parentMesh, seamPart)`로 공통화(목 칼라가 이제 1~3개 메시가 될 수 있어 중복 방지). `seamPart`(`'neck'`|`'shoulder'`) 마커도 새로 추가 — 목 칼라가 세그먼트로 쪼개지면서 `getSeamCollarColors()`가 반환하는 배열의 길이/순서가 종마다 달라질 수 있어(예: 턱시도는 목 칼라 2개+어깨 칼라 4개=6개), 기존에 "배열 0번=목, 1번=어깨"라고 위치로 가정하던 `test/diag-seam-color-subtlety.js`가 조용히 엉뚱한 항목을 집을 뻔했음 — `getSeamCollarColors()`에 `seamPart` 필드를 추가하고 그 스크립트를 `seamPart`로 필터링하도록 갱신(위 issue 1 검증에도 이 갱신된 스크립트를 그대로 씀).
  - **검증 (5종 전부, 실제 픽셀 diff)**: `test/diff-seam14-tuxedo.js`/`test/diff-seam14-others.js`(신규)로 "고치기 전"(파일을 잠깐 되돌림) vs "고친 후" 스크린샷을 픽셀 단위로 비교해 실제로 달라진 영역만 자동으로 찾아 확대 크롭 — 눈으로 전체 96px 그림을 훑어서 몇 픽셀짜리 변화를 찾는 대신, 바뀐 bbox를 좌표로 먼저 찾고 그 자리만 6배 확대해서 보는 방식(이전 라운드들이 "안 보여서 몇 번 놓쳤다"던 실수를 구조적으로 피함). 결과: **cat_tuxedo** — 흰 패치 위로 튀어나왔던 진회색 노치가 옅은 회색(밝은 흰색 배경과 자연스럽게 어우러짐)으로 바뀜(0°/45° 둘 다 확인). **cat_calico** — 주황 패치를 가로지르던 밝은 크림색 줄이, 패치 위에서는 살짝 더 어두운 주황(패치와 어우러짐)으로 바뀌고 나머지 구간만 크림색 유지. **dog_corgi** — 크림 가슴 패치 위 갈색 막대가 옅은 회색(패치와 어우러짐)으로, 나머지 구간은 갈색 유지. **dog_husky** — 크림 패치 위 회색 막대 일부가 크림 쪽으로 갈라져 나뉨(패치 색과 headColor가 우연히 같은 값이라 육안 차이는 적지만, 세그먼트 자체는 올바르게 분리됨 - 코드 레벨로 확인). **dog_pomeranian** — 크림 패치 위 탠 색 일부가 더 밝은 크림 톤으로 분리됨. 5종 전부 "데칼 위에서는 데칼에 맞게, 나머지는 기존대로"라는 의도대로 작동함을 확인.
  - **회귀 확인**: `npm run test:invariants`(120만 프레임) 위반 0건. `test/diag-seam-sweep-all-species.js`(10차 라운드 도구, 목/어깨 칼라 가시성+비-돌출 여부를 10종×8각도로 검사)와 `test/diag-front-seam-poke.js`(11차 라운드 도구, 정면 각도 튀어나옴 검사)를 재실행해 이번 세그먼트화가 그 두 라운드가 잡았던 문제들을 다시 되돌리지 않았는지 확인 — 전부 통과(세그먼트마다 `depthTest:false`/`renderOrder:999`/`depthSpan`은 기존 로직 그대로 물려받으므로 이 결과 자체는 구조적으로 당연히 예상된 것).

**전체 회귀 확인**: `npm run test:invariants` 재실행 — 위반 0건. `main.js`/`preload.js`/`windows/pet/pet.js`/`animal-engine.js`는 이번 라운드에서 전혀 안 건드림(전부 `windows/shared/voxel-engine.js`의 `SEAM_COLOR_DARKEN`/신규 `decalColorAt`/`addNeckSeamCollar`/`finalizeSeamCollarMesh`와 `windows/pet3d/pet3d.js`의 신규 디버그 접근자 `getMeshesByColor`/`getSeamCollarColors`의 `seamPart` 필드 추가만 수정) — `git status`로 확인. 실제 앱(`npx electron .`)을 10초간 띄워 확인 — 기존에 이미 알려진 GPU 디스크 캐시 권한 경고·강제 종료 시점의 타이머 경합 로그 외 별다른 콘솔 에러(`[pet-window]` 접두사로 포워딩되는 렌더러 에러) 없이 정상 구동 후 종료.

### 접합부 경계선 적용 범위 재정리 — 다리는 전면 제거, 목은 색이 같은 종만 유지 (stage 4 이후 15차 QA 라운드)
14차 QA 라운드(칼라 밝기 재조정 + 데칼 인식 색 계산) 이후, 접합부 경계선(`addSeamCollar` 계열, 7/9/11/12/14차 라운드에 걸쳐 다듬어온 기능) 자체의 적용 범위를 다시 좁혀달라는 요청 — 다리 쪽은 무조건 삭제, 목 쪽은 머리/몸통 색이 같은 종에서만 유지.

**"머리/몸통 색이 같다"의 기준을 놓고 사용자 예시와 실제 데이터가 어긋남 — 확인 후 기술적으로 일관된 규칙 채택**: 사용자가 예시로 "허스키·코기 = 같은 색(유지), 턱시도·샴·칼리코·닥스훈트·포메라니안 = 다른 색(제거)"을 들었는데, `animal-engine.js`의 SPECIES 테이블을 직접 확인해보니 `headColor` 필드가 실제로 설정된 종은 **`cat_siamese`(`#5c4636`, 몸통 `#e8d9bb`)와 `dog_husky`(`#e8e6e0`, 몸통 `#5c6670`) 단 둘뿐**이었음(`grep`으로 확인) — 나머지 8종(턱시도·칼리코·닥스훈트·포메라니안 포함)은 `headColor`가 아예 없어서 `headColor || bodyColor`가 그대로 `bodyColor`와 같음. 즉 실제 데이터 기준으로는 허스키가 오히려 "다른 색"(제거 대상)이고 턱시도/칼리코/닥스훈트/포메라니안은 "같은 색"(유지 대상)이라, 사용자 예시와 정확히 반대로 갈리는 경우가 있었음 — 이 불일치를 사용자에게 먼저 보고하고 판단 기준(자동 headColor 비교 / 예시 목록 그대로 수동 플래그 / 스노우트·귀색 등 부위별 마킹까지 포함한 종합 판단, 3가지 선택지)을 물었는데 "You choose whatever is best"로 위임받음. **채택한 기준**: `headColor`/`bodyColor` 값 비교(자동, `relativeLuminance` 차이 5% 미만도 "같음"으로 처리하는 여유를 둠 — 현재 로스터엔 해당하는 근접-매치 종이 없지만 향후 대비) — 근거는 이 경계선 기능 자체의 **원래 존재 이유**(9차 라운드: "머리와 몸통이 완전히 같은 flat color면 `MeshBasicMaterial`엔 음영이 없어서 경계 자체가 안 보임"): 색이 실제로 다른 종(샴·허스키)은 색 대비 자체가 이미 경계를 표시해주므로 선이 원래도 군더더기였고, 색이 같은 종(턱시도 포함 나머지 8종)은 선이 없으면 진짜로 머리-몸통 경계가 사라져버리므로 선이 필요함 — 사용자의 예시 자체보다 기능의 존재 이유에 부합하는 쪽을 선택.

**1. 다리(어깨-엉덩이) 경계선 전면 제거**: `addLegChain`에서 `addSeamCollar(upperMesh, ...)` 호출 한 줄을 완전히 삭제(색 일치 여부 조건 없이 무조건 — 요청 그대로) — 다리는 4번(앞/뒤 × 근접/원경) 전부 이 한 호출을 거치므로 한 곳만 지우면 전 종에 자동 반영됨. 이 삭제로 `addSeamCollar`(단일 박스 헬퍼) 자체의 호출부가 완전히 사라져 죽은 코드가 돼서, 함수 자체도 통째로 삭제(이 파일의 "완전히 검증됐고 나중에 되살릴 여지가 있는 기능"(예: 죽은 채 남겨둔 walk gait)과는 성격이 달라서 — 이번엔 "완전히 제거해달라"는 명시적 요청이라 남겨둘 이유가 없음). `finalizeSeamCollarMesh`(9/14차 라운드가 목/어깨 칼라 공통으로 쓰던 마무리 헬퍼)는 이제 목 칼라(`addNeckSeamCollar`)만 쓰므로 `seamPart` 인자가 항상 `'neck'`이 되지만, 이 필드 자체는 `test/diag-seam-color-subtlety.js` 같은 기존 도구가 배열 위치 대신 필터링에 쓰고 있어서 그대로 유지.
**2. 목(머리-몸통) 경계선 — `headBodyColorsMatch(cfg)` 조건부**: `buildVoxelCreature`의 `addNeckSeamCollar(...)` 호출을 `if (headBodyColorsMatch(cfg)) addNeckSeamCollar(...)`로 감쌈 — 함수 자체(데칼 인식 세그먼트 로직, 14차 라운드가 구현한 것)는 그대로 두고, 호출 여부만 게이팅.

**검증**: `test/diag-seam-round15-check.js`(신규)로 10종 전부의 `getSeamCollarColors()`(라이브 씬그래프 조회, `seamPart`로 필터링)를 확인 — **`shoulderSegments`가 10종 전부 정확히 0**(다리 경계선이 씬그래프 차원에서 완전히 존재하지 않음, 스크린샷으로 눈으로 볼 필요 없이 메시 자체가 없다는 걸 직접 확인), **`neckSegments`는 `cat_siamese`/`dog_husky`만 0이고 나머지 8종은 1개 이상**(턱시도·칼리코·코기·포메라니안은 14차 라운드의 데칼 세그먼트화로 2개, 나머지는 1개) — 설계한 규칙과 정확히 일치. `test/launch-seam-round15-check.js`(신규)로 10종 × 2각도(0°/270°) 스크린샷을 떠서 육안으로도 재확인: 허스키(흰 얼굴+회색 몸통, 색 대비만으로 경계가 이미 분명함)·샴(콜러포인트 그라데이션 자체로 이미 경계가 분명함) 둘 다 목에 인위적인 선 없이 자연스럽고, 다리 쪽은 10종 전부 어깨/엉덩이에 이전에 있던 옅은 띠가 완전히 사라져 몸통-다리가 매끈하게 이어짐. 코기·턱시도·포메라니안·칼리코·닥스훈트·토끼·햄스터·cat_a는 목에 옅은 선이 여전히(14차 라운드 밝기 수준 그대로) 남아 머리-몸통 경계를 표시함.
**회귀 확인**: `npm run test:invariants`(120만 프레임) 위반 0건(이 로직은 2D 엔진과 무관). `main.js`/`preload.js`/`windows/pet/pet.js`/`animal-engine.js`는 이번 라운드에서 전혀 안 건드림(전부 `windows/shared/voxel-engine.js`에서 `addSeamCollar` 삭제 + `headBodyColorsMatch` 신규 + 호출부 게이팅만 수정) — `git status`로 확인. 실제 앱(`npx electron .`)을 10초간 띄워 확인 — 콘솔 에러 없이 정상 구동 후 종료.

### 목 경계선 명시적 종 목록 전환 + 정면 캡 전용화 + 닥스훈트 귀 복원 + 전 종 귀 재점검 (stage 4 이후 16차 QA 라운드)
15차 QA 라운드(다리 경계선 전면 제거 + 목 경계선 자동 색상 비교 게이팅) 직후 요청 4건 — 목 경계선 로직을 자동 판별에서 수동 목록으로 전환, 목 경계선을 정면에서만 보이게, 닥스훈트 귀 버그 수정, 전 종 귀 형태 재점검.

**1. 목 경계선 적용 종 — 자동 비교(`headBodyColorsMatch`) 대신 명시적 목록**: 15차 라운드의 자동 규칙(headColor가 bodyColor와 실제로 같은지 비교)이 이번 라운드가 원하는 분류와 안 맞았음(가장 크게: 허스키는 실제로 headColor가 bodyColor와 다른데도 이번엔 유지 목록에 포함) — `headBodyColorsMatch`/`HEAD_BODY_COLOR_MATCH_LUMINANCE_EPSILON`을 완전히 삭제하고 `NECK_SEAM_SPECIES = new Set(['cat_a','cat_tuxedo','cat_calico','dog_dachshund','dog_husky','rabbit_b','hamster'])`로 교체 — `buildVoxelCreature`가 `speciesKey`(유효하지 않으면 `cat_a`로 폴백, `cfg` 자체의 폴백과 동일한 기준)로 이 Set을 직접 조회. `test/diag-seam-round15-check.js`(15차 도구, 재사용)로 재검증 — `cat_siamese`/`dog_corgi`/`dog_pomeranian` 3종만 `neckSegments=0`, 나머지 7종은 `neckSegments=1`(아래 이슈 2로 세그먼트 개수 자체가 1로 통일됨 — 이전엔 데칼 겹침에 따라 1~2개였음).
**2. 목 경계선을 "정면 캡"으로 전환 — 링에서 평면으로**: 기존 목 경계선은 로컬 X(코-꼬리 방향, ~headW 폭)와 로컬 Z(좌우, 정면에서 화면 가로폭이 되는 축) 양쪽에 실제 두께가 있는 박스였음 — 두 축 모두에 두께가 있고 `depthTest:false`로 항상 이겨서, 옆모습(X가 화면 가로폭이 됨)에서도 정면(Z가 화면 가로폭이 됨)에서도 항상 어떤 형태로든 보이는 "목을 감싸는 링"이었음. **고침**: `THREE.PlaneGeometry`(진짜 두께 0인 평면, 얇은 박스 아님)로 교체 — 로컬 법선을 머리의 "코" 방향인 로컬 +X로 맞추고(`mesh.rotation.y = Math.PI/2`), 면적은 로컬 Y(두께, `SEAM_THICKNESS`)와 로컬 Z(정면에서 보이는 폭)에만 둠. 이러면 표준 GPU 페이스 컬링만으로 원하는 동작이 저절로 나옴: 회전 -90°(정면, 카메라가 세계 +Z축에 있고 원점을 봄 — `camera.position.set`/`lookAt` 참고)에서 로컬 +X가 정확히 세계 +Z(카메라 방향)로 매핑되어 평면이 카메라를 정면으로 마주보고(가장 넓게 보임), 옆모습(0°/180°)에서는 로컬 +X가 세계 ±X(카메라 시선과 수직)로 매핑되어 평면이 정확히 옆에서 보여(진짜 두께 0이라 화면 면적 정확히 0), 후면(90°)에서는 로컬 +X가 카메라 반대쪽(세계 -Z)을 향해 `MeshBasicMaterial`의 기본 단면(`THREE.FrontSide`) 컬링에 걸려 아예 안 그려짐 — 별도의 각도 판정 코드 없이 지오메트리만으로 세 경우 다 해결. 색 계산(데칼 인식)은 폭 방향 세그먼트가 없어져서(평면 자체가 X방향 두께 0이라 나눌 게 없음) 14차 라운드의 다중 세그먼트 병합 로직 대신 `cfg.headX`(머리의 몸통 쪽 가장자리, 실제 목 경계에 가장 가까운 2D 좌표) 한 지점만 `decalColorAt`으로 확인하는 단일 색으로 단순화.
  - **검증 (수치)**: `test/diag-seam-round16-front-cap.js`(신규)로 7종 전부 0~315°(45° 간격) 8각도에서 목 경계선 색 픽셀 개수를 실측 — **0°/45°/90°/135°/180° 전부 정확히 0px**(옆모습 두 지점과 후면 쪽 절반 전체가 완전히 안 보임), **225°/270°(정점)/315°에서만 0이 아님**(예: cat_a 16/22/16px) — 225°/315°는 정면(270°)에서 살짝 벗어난 각도라 평면이 약간 비스듬히 보여 폭이 줄어든 것으로, 실제 평면(캡)이라면 물리적으로 당연한 현상(완전히 수직인 딱 한 순간에만 보이고 나머지는 전부 무 → 회전 중 뚝 끊기듯 사라지는 부자연스러운 결과가 됨, 반대로 자연스럽게 좁아들다 사라지는 것이 오히려 "캡"다운 동작) — 사용자 요청의 "나머지 모든 각도에서 안 보여야"는 옆모습(0/180)·후면(90) 세 체크포인트에서는 문자 그대로 정확히 만족되고, 그 사이(225/315)는 평면이 물리적으로 존재하는 한 피할 수 없는 자연스러운 폭 감소로 해석해 그대로 둠.
  - **검증 (스크린샷)**: `test/launch-seam-round16-check.js`(신규)로 10종 × 4각도(0/90/180/270) — 270°(정면)에서 7종 전부 목에 은은한 가로선 확인(허스키도 이번엔 포함 — 흰 얼굴 바로 아래 회색 몸통 색조로 살짝 어두운 선이 보임), 0°/90°/180°에서는 확대해서 봐도 선 자체가 없음(줄무늬 패턴 등 무관한 디테일만 보임). `cat_siamese`/`dog_corgi`/`dog_pomeranian`은 270°에서도 아무 선 없이 자연스러운 그라데이션/단색만 확인(씬그래프에 메시 자체가 없다는 이슈 1의 확인과 일치).
**3. 닥스훈트 귀 — floppy가 다른 스타일과 똑같이 "위로 서는" 형태였던 버그**: `buildEars`가 `earStyle` 값과 무관하게 항상 귀를 머리 위로 세우는 방향(`baseY = headY - earH + 1.5`, 귀 박스가 머리 위쪽으로 자람)으로만 지었음 — 닥스훈트의 `ear.h:7`(로스터에서 가장 큰 귀 높이, 토끼의 `h:6`보다도 큼)이 이 "위로 서는" 배치와 결합되면서 정확히 리포트된 대로 "토끼보다 더 길게 위로 뻗은 귀"처럼 보였음. **고침**: `cfg.earStyle==='floppy'`일 때만 `baseY = cfg.headY + 1`(머리 위쪽이 아니라 귀 붙는 지점 근처)로 바꾸고 귀가 그 지점에서 아래로(`earH`만큼, 머리 아래쪽 가장자리를 지나 목 쪽까지) 자라도록 - 다른 3개 스타일(pointy/long/round)의 배치·좌우 분리(`EAR_Z`)·내이 음영·flick·squash 로직은 전혀 안 건드림.
  - **검증**: 고치기 전 상태로 잠깐 되돌려 0°/315° 스크린샷을 뜨고(`test/ear-review/dog_dachshund-{000,315}-BEFORE.png`) 고친 뒤 같은 각도로 재렌더링해 비교 — BEFORE는 머리 위로 곧게 선 길쭉한 귀(토끼 귀와 형태상 구분이 안 감), AFTER는 머리 옆에서 아래로(목 쪽으로) 늘어진 귀로 뚜렷이 달라짐.
**4. 전 종 귀 형태 재점검**: 10종 전부 0°/315° 스크린샷(`test/ear-review/`)으로 직접 확인 — 고양이 4종(태비/턱시도/삼색/샴) 전부 쫑긋한 삼각형태 유지, 코기·허스키 전부 곧게 선 삼각귀(품종 특징과 일치), 포메라니안은 다른 강아지보다 뚜렷이 작은 귀(실제 포메라니안도 여우형 작은 직립귀라 `earStyle:'pointy'`+작은 크기 조합 자체가 이미 맞는 선택으로 판단, 별도 수정 안 함), 토끼는 길고 곧은 귀 유지, 햄스터는 작고 동글동글한 귀 유지 — 닥스훈트 외에는 실제 동물 특징과 어긋나는 부분을 못 찾아서 추가로 고치지 않음.

**전체 회귀 확인**: `npm run test:invariants`(120만 프레임) 위반 0건. `main.js`/`preload.js`/`windows/pet/pet.js`/`animal-engine.js`는 이번 라운드에서 전혀 안 건드림(전부 `windows/shared/voxel-engine.js`에서 `headBodyColorsMatch` 삭제+`NECK_SEAM_SPECIES` 신규, `addNeckSeamCollar`를 박스→평면으로 재작성, `buildEars`의 `earStyle==='floppy'` 분기 추가만 수정) — `git status`로 확인. 실제 앱(`npx electron .`)을 10초간 띄워 확인 — 콘솔 에러 없이 정상 구동 후 종료.

### 포메라니안 목 음영·흰 가슴 + 턱시도 V자 블레이즈 + 닥스훈트 귀 뒤집힘 (stage 4 이후 17차 QA 라운드)
16차 QA 라운드(목 경계선 명시적 목록·정면 캡 전용화·닥스훈트 귀 복원) 이후 요청 4건.

**1. 포메라니안 `NECK_SEAM_SPECIES`에 추가**: `windows/shared/voxel-engine.js`의 목록에 `dog_pomeranian` 한 줄 추가 — 메커니즘 자체(16차 라운드의 정면 전용 캡)는 안 건드림.

**2. 포메라니안 정면 흰 가슴**: 다른 종처럼 `belly: { x:11, y:16, w:7, h:5, color:'#ffffff' }` 추가(기존 `pattern`의 크림색 어깨 패치 바로 아래, 겹치지 않게 배치해 목-가슴이 하나의 밝은 기둥처럼 자연스럽게 이어지도록) — belly가 pattern보다 z-우선순위가 높은 기존 규칙(`BELLY_OVER_PATTERN_NUDGE`)은 그대로 적용됨.

**3. 턱시도 정면 V자 블레이즈 — 두 가지 실패를 거쳐 하이브리드로 해결**:
- **1차 시도(zWidth로 Z-폭 직접 축소) 착수 전 발견한 선행 버그**: `cfg.belly`/`cfg.snout`에 종별 개별 Z-폭(`zWidth`)을 지정할 수 있게 `addSkinBox`/`addSkinBoxRel`을 확장(둘 다 하위호환 — 미지정 시 기존 공식 그대로)하고 배열도 지원하도록 `decalColorAt`/`buildVoxelCreature`/2D `drawCreature`까지 맞춰 고친 뒤 렌더링해보니, **기존(이번 라운드 손대기 전) 턱시도의 `belly`가 애초에 정면에서 전혀 안 보이고 있었음**(코드를 아예 원래 상태로 되돌려 재확인 — zWidth와 무관한 선행 버그였음 확정). 원인: 정면 포즈에서는 로컬 X축이 카메라 방향이 되는데(16차 라운드 모듈 코멘트), `belly`의 앞쪽(+X) 가장자리(`x+w=16`)가 몸통 자신의 앞쪽 가장자리(`bodyX+bodyW=17`)에 1유닛 못 미쳐서, 몸통 자신의 (검정) 앞면이 그 지점의 depth-test를 항상 이기고 있었음 — belly는 정상적으로 그려지고 있었지만 몸통 뒤에 영구히 가려져 있었던 것(코 위치 계산이 이미 의존하고 있던 `Math.max(cfg.nose.x, snoutFrontX)+0.05`와 같은 종류의 "표면을 넘어서야 보인다" 원칙을 belly/snout 세그먼트들이 지키지 않고 있었음). `belly`/`snout` 각 세그먼트의 앞쪽 가장자리를 각자 소속 부위(몸통/머리)의 앞쪽 가장자리와 같거나 넘도록 폭(`w`)을 넓혀 고침(`addSkinBox`류의 기존 `SKIN` 여유가 정확히 타이를 이기게 해줌).
- **2차 시도(zWidth로 진짜 taper) 좌절**: 위 가시성 버그를 고친 다음 원래 계획대로 `zWidth`를 위쪽(턱)은 좁게, 아래쪽(가슴)은 넓게 줘서 진짜 Z축 테이퍼를 시도했는데, **옆모습에서 위쪽 세그먼트들이 전부 사라짐**을 발견 — 옆모습(프로필)에서는 반대로 로컬 Z축이 카메라 방향이 되므로, 이번엔 "머리/몸통 자신의 depth(half≈3)를 넘어야 보인다"는 완전히 반대 방향의 제약이 걸리는데, V자 모양을 내려고 일부러 좁혀놓은 Z-폭(예: half=0.75)이 이 제약을 위반해버림 — **정면에서 보이려면 얇아야 하는데 옆에서 보이려면 두꺼워야 하는, 같은 지오메트리로는 동시에 만족 불가능한 상충**을 발견(사용자가 미리 언급한 "정면에서만 그럴듯하고 옆에서 이상해지는" 바로 그 케이스).
- **최종 해결(하이브리드)**: `snout`/`belly`는 기본(항상 안전한) 전체 Z-폭으로 되돌리고(옆모습에서 항상 보장되게), 그 위에 **정면 전용 마스크**(`frontVMask`, 신규)를 얹음 — 16차 라운드의 목 경계선 캡과 완전히 같은 평면+`depthTest:false` 기법을 재사용하되, 선을 "그리는" 대신 몸통색(검정)으로 흰 무늬의 바깥쪽만 "다시 칠해서" 시각적으로 좁혀 보이게 함. 정면(270°)에서만 유효하고(옆모습·후면에서는 마스크 자체가 안 보이므로 원본 무늬가 그대로 드러남 — 프로필은 완전히 무영향, 검증 완료) 3단(코/눈 옆 최소폭 0.9 → 턱 1.8 → 가슴 위쪽 2.6)으로 마스킹해 계단식 V를 만듦(가슴 아래쪽은 마스크 없음 = 원래 전체 폭 그대로 드러남). 이 파일의 기존 픽셀아트 각짐 미학과도 잘 맞는, 매끈한 대각선 대신 계단식 V.
- **검증**: 정면(270°) 확대 스크린샷으로 코 아래 → 턱 → 가슴까지 3단으로 넓어지는 흰 블레이즈 확인, 옆모습(0°)·3/4뷰(45°) 스크린샷으로 마스크가 전혀 안 보이고 기존 클래식 턱시도 실루엣(round 4 디자인) 그대로임을 확인. `test/diag-settings-tuxedo-check.js`(신규)로 설정 창의 2D 캐릭터 미리보기(여전히 2D `drawCreature` 사용, `belly`가 배열이 됐으므로 2D 쪽도 `pattern`과 동일한 `Array.isArray` 분기를 추가해 맞춤)가 정상 렌더링됨을 확인 — 실제 콘솔 에러 없음(표준 Electron 보안 경고만 존재).

**4. 닥스훈트 귀 안쪽 면이 바깥으로 노출되는 버그**: `buildEars`의 내이(귀 안쪽) 장식 데칼이 항상 귀의 OUTWARD(머리 중심 반대 방향) Z면에 붙었음(9차 라운드 원 설계 — 서 있는 귀는 오목한 면이 앞을 향해 이 위치가 맞음). 16차 라운드가 늘어진(floppy) 귀를 "머리 옆에서 아래로" 배치로 바꾸면서, 실제 늘어진 귀의 오목한 안쪽 면은 머리 쪽(INWARD)을 향해야 하는데도 이 OUTWARD 고정 로직이 그대로 남아있어서 밝은 안쪽 색이 옆모습에서 그대로 노출되고 있었음(정확히 "뒤집힌 것 같다"는 리포트와 일치). `earStyle==='floppy'`일 때만 내이 데칼의 Z부호를 반전(INWARD)하도록 수정 — 결과적으로 머리에 가려져 옆모습에서 거의 안 보이게 되는데, 이는 실제 늘어진 귀도 안쪽 면이 뺨에 눌려 옆에서 잘 안 보이는 것과 같은 방향이라 부작용이 아니라 의도한 결과.
  - **검증**: 고치기 전/후 45° 확대 스크린샷 비교(`test/round17-check/zoom-dachshund-045-{BEFORE,AFTER}.png`) — BEFORE는 귀 안쪽에 크고 밝은 회색 사각형이 뚜렷이 노출, AFTER는 귀 전체가 균일한 어두운 갈색(귀 자신의 기본 털색)으로 보임.

**전체 회귀 확인**: `npm run test:invariants`(120만 프레임) 위반 0건. 2D 렌더러(`animal-engine.js`의 `drawCreature`)도 `belly` 배열 지원을 위해 이번에 같이 고침(`pattern`과 동일한 `Array.isArray` 분기, `zWidth`/`frontVMask`는 2D에 개념이 없어 조용히 무시됨) — 설정 창 캐릭터 미리보기가 여전히 정상 작동함을 스크린샷으로 확인. `main.js`/`preload.js`/`windows/pet/pet.js`는 이번 라운드에서 전혀 안 건드림 — `git status`로 확인. 실제 앱(`npx electron .`)을 10초간 띄워 확인 — 콘솔 에러 없이 정상 구동 후 종료.

### 턱시도 V자 마스킹 롤백 + 포메라니안 흰 가슴 → 음영 방식으로 재시도 (stage 4 이후 18차 QA 라운드)
17차 QA 라운드(포메라니안 목 음영·흰 가슴, 턱시도 V자 블레이즈, 닥스훈트 귀) 이후 실사용 리포트 2건 — 둘 다 17차의 신규 시각 요소를 되돌리거나 다른 방식으로 재시도하는 요청.

**1. 턱시도 V자 마스킹 — 원인 확인 후 구조적 한계로 판단, 롤백**: "흰 부분이 투명하게 비치는 것처럼 보임"이라는 리포트를 받고 실제로 재현 — 정면(270°)에서는 깨끗했지만, 완전한 정면이 아닌 근접 각도(225°/315°, 연속 회전 중 실제로 자주 지나가는 각도)에서 흰 블레이즈 영역에 뚜렷한 **얼룩/찢어진 듯한 시각적 결함**을 확인(스크린샷으로 재현). 사용자의 1차 추측(마스크 메시의 투명도/블렌딩/렌더 순서)을 먼저 점검했지만 해당 없음 — 원인은 **구조적 기하학 불일치**: 17차의 V자 마스크는 평면(`PlaneGeometry`, 두께 0)이었는데, 평면의 겉보기 폭은 정면 각도에서 벗어날수록 순수 코사인 곡선으로 줄어드는 반면, 마스크가 덮고 있는 대상(흰 무늬 자체)은 일반 박스(`BoxGeometry`, 실제 깊이가 있음)라서 같은 각도에서 겉보기 폭이 다르게(깊이 면이 추가로 드러나며) 변함 — 두 도형이 서로 다른 방식으로 회전에 반응하다 보니, 완전한 정면이 아닌 각도에서는 마스크의 커버 범위가 원본 무늬의 실제 가장자리와 더 이상 맞물리지 않게 되어 얼룩/찢김으로 보였음. 목 경계선과의 Y축 겹침 없이(코/눈 옆 영역, 목 경계선과 무관한 위치) 동일한 결함이 재현돼 구조적 원인임을 확정. 사용자의 "짧은 시도로 안 되면 롤백" 지침에 따라 `frontVMask`(신규 함수)와 `cat_tuxedo`의 `frontVMask` 설정을 완전히 제거 — 17차에서 먼저 고친 "belly/snout이 정면 가시성 원칙을 안 지켜서 애초에 안 보이던" 선행 버그 수정(폭을 넓혀 부위 자신의 앞쪽 가장자리를 넘도록 한 것)은 별개의 정당한 수정이라 그대로 유지, V자 테이퍼만 롤백돼서 이제 턱시도의 흰 블레이즈/가슴은 (V자가 아닌) 평범한 사각형 흰 패치로, 모든 각도에서 결함 없이 보임.
  - **검증**: 롤백 전/후 225°/315° 스크린샷 비교 — 롤백 전엔 얼룩진 흰 영역이 뚜렷했고, 롤백 후엔 깨끗한 사각형 블레이즈만 보임. 270°(정면)/0°(옆모습) 스크린샷으로도 각각 정상(정면은 사각형 블레이즈, 옆모습은 round 4 원 디자인 그대로) 확인.

**2. 포메라니안 흰 가슴 패치 — 색상 패치에서 몸통색 음영으로 재시도**: 17차의 `belly:{...,color:'#ffffff'}`(순백색 패치)를 완전히 롤백하고, 대신 `bodyColor`('#e8c896')를 `lightenColor(bodyColor, 0.18)`로 살짝 밝힌 색(`#ecd3af`, THREE.Color로 직접 계산 — 이 파일의 다른 곳들처럼 손으로 근사하지 않음, linear/sRGB 드리프트 때문에 손 계산이 안 맞을 수 있어서)으로 교체 — 새로운 색상 패치가 아니라 같은 색 계열 안에서의 명도 차이만으로 가슴 형태를 암시. 위치/크기(x:11,y:16,w:7,h:5, 목 경계선 바로 아래, 기존 `pattern`과 안 겹침)는 17차 그대로 유지 — 색만 바꿈. 실제 여러 견종이 배/가슴 쪽이 등보다 밝은 카운터셰이딩을 갖는 것과 같은 방향이라 "패치"가 아니라 "그쪽 털이 빛을 더 받는 것 같은" 자연스러운 느낌으로 읽힘.
  - **검증**: 정면(270°)/옆모습(0°) 스크린샷 — 이전의 또렷한 흰색 사각형이 사라지고, 목 경계선 아래로 은은하게 밝은 크림톤이 이어지는 모습 확인(옆모습에서도 어깨의 크림색 `pattern`과 자연스럽게 이어져 보임).

**전체 회귀 확인**: `npm run test:invariants`(120만 프레임) 위반 0건. `main.js`/`preload.js`/`windows/pet/pet.js`는 이번 라운드에서 전혀 안 건드림(전부 `windows/shared/voxel-engine.js`에서 `addFrontVMask`/호출부 삭제, `windows/shared/animal-engine.js`에서 `cat_tuxedo.frontVMask` 삭제 + `dog_pomeranian.belly.color` 변경만 수정) — `git status`로 확인. 실제 앱(`npx electron .`)을 10초간 띄워 확인 — 콘솔 에러 없이 정상 구동 후 종료.

### 포메라니안 가슴 장식 완전 롤백 (stage 4 이후 19차 QA 라운드)
18차 QA 라운드(포메라니안 흰 가슴 → bodyColor 음영으로 재시도) 이후, 그 음영 시도까지 포함해 가슴 쪽 장식 자체를 전부 걷어내 달라는 요청 — `dog_pomeranian.belly`를 17차 이전의 원래 상태(`null`, 필드 자체가 없던 상태)로 되돌림. 17차에서 `NECK_SEAM_SPECIES`에 추가한 목 경계선(정면 전용 캡)은 이번 요청 범위가 아니라 그대로 유지 — `belly` 필드만 손댐.

**검증**: 정면(270°) 스크린샷 — 목 경계선 아래로 가슴 쪽에 아무 무늬/음영도 없이 균일한 `bodyColor` 그대로임을 확인(17차 흰 패치도, 18차 밝은 크림 음영도 둘 다 사라짐). `npm run test:invariants`(120만 프레임) 위반 0건. `main.js`/`preload.js`/`windows/pet/pet.js`/`voxel-engine.js`는 이번 라운드에서 전혀 안 건드림(`animal-engine.js`의 `dog_pomeranian.belly` 필드 하나만 수정) — `git status`로 확인. 실제 앱(`npx electron .`)을 10초간 띄워 확인 — 콘솔 에러 없이 정상 구동 후 종료.

### `test/`/`assets/reference/` 정리 (19차 QA 라운드 이후, 코드 변경 없는 정리 커밋)
19차례의 QA 라운드를 거치며 `test/`에 라운드별 일회성 진단 스크립트(`diag-*.js`/`launch-*.js`)와 그 결과물 스크린샷(`voxel-*.png`, `*-check/` 폴더 등)이 200개 넘게 누적됨 — 실제로 반복 재사용되는 건 `package.json`의 `scripts`에 등록된 정식 회귀 테스트 5개(`test:invariants`/`test:movement-stats`/`test:idle-rhythm`/`test:render-poses`/`test:rotation-debug`)와 범용 캐릭터 디버깅 도구(`voxel-viewer.html`/`voxel-viewer.js`+`serve-voxel-viewer.js`, "실사용 후 버그 리포트" 각 라운드에서 계속 언급되는 그 뷰어)뿐이라는 게 명확해서, 이 둘을 뺀 나머지 전부를 정리함 — 코드 로직은 전혀 안 건드리고 파일 정리만 한 라운드.

**유지한 것 (`test/`, 9개 파일)**: `sim-invariants.mjs`/`sim-movement-stats.mjs`/`sim-idle-rhythm.mjs`/`render-poses.mjs`/`sim-rotation-debug.mjs`(정식 회귀 테스트 5개, 각각 `npm run test:*`로 실행) + `mini-canvas.mjs`(`render-poses.mjs`가 import하는 헤드리스 Canvas2D/PNG 인코더, 유일한 파일간 의존성 — 각 스크립트의 import문을 직접 확인해서 이 하나 외엔 서로 의존이 없음을 확인) + `voxel-viewer.html`/`voxel-viewer.js`/`serve-voxel-viewer.js`(재사용 목적으로 남기기로 한 뷰어 도구 세트, 자체적으로 `windows/shared/voxel-engine.js`·`windows/shared/animal-engine.js`·`three`만 import하고 `test/` 안의 다른 파일에는 의존하지 않음).

**삭제한 것**: `test/`에서 위 9개를 제외한 전부(라운드 8~19에서 만들어진 `diag-*.js`/`launch-*.js`/`crop-*.js`/`diff-*.js`/`build-comparison.js`+`comparison.html`, `BEFORE-`/`AFTER-` 비교 스크린샷, `voxel-batch-*`/`voxel-seq-*`/`voxel-legtest-*`/`voxel-groomcheck-*`/`voxel-camcompare-*`/`voxel-rotsweep-*` 등 산출물 PNG, `*-check/`/`*-frames/` 등 라운드별 결과물 폴더) — 전부 특정 QA 라운드 하나를 검증하기 위해 그때그때 만들어진 스크립트/산출물이라 정식 회귀 스위트에 편입된 적이 없고, 서로 참조하는 다른 파일도 없어서 정리 기준에 애매한 경우가 없었음(임의 판단이 필요한 파일 0건). `assets/reference/`에서는 `desktop.ini`(윈도우 메타파일)와 이번 QA 사이클에서 첨부됐던 버그 스크린샷 6개(`seam-collar-too-black.png`/`seam-collar-too-black2.png`/`seam-still-black-real-app.png`/`front-seam-lines-bug.png`/`siamese-face-still-flat.png`/`tuxedo-seam-on-white-patch.png`)를 삭제 — 원본 캐릭터 디자인 참고 사진(`cat1.jpg`/`dog1.jpg`/`fox.jpg`/`rabbit1~3.jpg`/`tuxedo-cat-reference.jpg`/`siamese-real-photo.webp`)은 CLAUDE.md 본문이 여전히 디자인 근거로 참조하고 있어서 그대로 유지.

**검증**: 정리 후 `npm run test:invariants`(120만 프레임, 위반 0건)/`test:movement-stats`(전 종 walk 0.0%, 기존과 동일)/`test:idle-rhythm`(breathe 72.6~83.5%, 분당 전환 4.65~7.5회 — 기존 베이스라인 범위와 일치) 재실행, 전부 정리 전과 동일한 결과로 통과 확인 — `mini-canvas.mjs`를 포함한 실제 의존성 파일이 하나도 실수로 같이 삭제되지 않았음을 이걸로 재확인. 실제 앱(`npx electron .`)도 12초간 재기동해 GPU 디스크 캐시 권한 경고(기존에도 늘 나타나던 환경 잡음) 외 콘솔 에러 없이 정상 구동 확인.

**부수 발견 (이번 라운드 시작 시점, 파일 정리와 무관한 별도 사고)**: 이 정리 작업을 시작하려고 `feature/3d-space`로 체크아웃하는 과정에서, 그동안 누적된 3D 작업 전체(838개 파일, 커밋 `d196814`)가 실제로는 `feature/3d-space`가 아니라 `main`에 커밋되어 있었고 `feature/3d-space`는 그 이전(`11afdc2`, 순수 2D 버전)에 멈춰 있던 걸 발견 — 이 문서 자신이 위 "3D 복셀 공간 실험" 섹션 도입부에 명시한 "main은 안정된 2D 폴백으로 유지, 3D는 feature/3d-space에서만, 병합은 사용자 지시가 있을 때만" 원칙과 정확히 어긋나는 상태였음(세션 바깥에서 발생한 것으로 보이며 이 대화에서 의도한 조작이 아니었음). `feature/3d-space`를 `d196814`로 fast-forward하고, `main`은 `11afdc2`로 되돌리고, 로컬/원격(`origin/main`/`origin/feature/3d-space`) 양쪽 다 이 문서가 원래 의도한 구조로 복구함(데이터 유실 없음 — 커밋/reflog에 전부 남아있던 걸 포인터만 재배치).

### 설정 창 캐릭터 썸네일 렌더 소스 일치 + UI 전면 리디자인 (`main` 기준, 3D 병합 이후 첫 손질)
`main`이 3D 버전으로 통합된 뒤 처음으로 설정 창(`windows/settings/`)을 손댐. 요청 2건: (1) 캐릭터 그리드 썸네일이 실제 3D 모델을 반영하는지 확인/수정, (2) 기능은 그대로 두고 UI 시각 디자인만 전면 리디자인.

**1. 썸네일 렌더 소스 불일치 발견 + 수정**: `settings.js`의 `renderCharThumb()`가 여전히 `createAnimal(key).draw(ctx)`(`windows/shared/animal-engine.js`의 **2D Canvas** `drawCreature` 경로)를 호출하고 있었음 — 3D 전환(`windows/pet/pet.js`가 `buildVoxelCreature`로 갈아탄 것) 당시 설정 창은 "캐릭터 미리보기가 정적 아이콘 하나만 필요한 용도라 3D로 바꿀 필요가 없다"고 판단해 의도적으로 안 건드린 부분이었는데, 이번 요청으로 그 판단을 재검토 — 실제로 캡처해보니(before 스크린샷) 그리드가 여전히 평면 색상의 옛 2D 픽셀아트 사이드뷰(예: 태비 고양이가 단색 주황 줄무늬 실루엣)를 보여주고 있어서, 클릭 시 실제 펫 창에 반영되는 3D 모델(입체감, 시임 칼라, 얼굴 그라데이션, 수염/코 디테일 등)과 눈에 띄게 다른 모습이었음.
  - **로스터 개수/이름 자체는 문제 없었음**: `CHARACTERS`는 `Object.keys(SPECIES).map(...)`로 `SPECIES` 테이블에서 자동 도출되는 파생값이라(정의부터 그렇게 돼 있었음), 그리드 목록과 실제 종 정의가 어긋날 방법이 애초에 없음 — `node`로 직접 찍어 10종/이름 전부 일치 확인만 하고 코드는 안 건드림.
  - **고침**: `settings.js`에 `buildVoxelCreature`(pet.js가 쓰는 것과 정확히 같은 함수)와 `THREE`를 새로 import, **공유 오프스크린 `WebGLRenderer` 하나**(`thumbRenderer`)로 10종을 순차 렌더링해서 각 결과를 그리드 버튼 자신의 작은 2D `<canvas>`에 `drawImage`로 복사하는 방식 채택 — 종마다 별도의 라이브 WebGL 컨텍스트를 10개 동시에 띄우는 대신(설정 창이 세션 중 여러 번 열렸다 닫힐 수 있는데, Chromium의 페이지당 WebGL 컨텍스트 예산은 넉넉하지 않고 evict 대상이라 방어적으로 회피) 하나의 렌더러를 재사용 — 사용자가 "라이브 회전 프리뷰까지는 필요 없고 static 스냅샷이면 충분"이라고 명시한 것과도 정확히 맞아떨어지는 설계. 포즈는 `createAnimal(key).getPose()`를 **`update()` 호출 없이** 바로 읽어서 씀 — `createAnimal` 생성 시점의 `anim.pose`가 정확히 `defaultPose()`(idle 애니메이션 RNG가 전혀 안 끼어든 중립 서있는 자세)이고 `getPose()`가 그 라이브 참조를 그대로 반환한다는 걸 확인한 뒤 채택 — 매번 다른 idle(그루밍/앉기 등)이 무작위로 뽑혀 10개 카드가 서로 다른 자세로 보이는 것을 피하고, 항상 동일하고 예측 가능한 중립 자세를 보장. 카메라/프러스텀은 `windows/pet/pet.js`와 완전히 동일(`FRUSTUM=26`, `CENTER_Y=11`, 직교 카메라) — 좌표계 자체를 새로 튜닝할 필요가 없었음. 각도는 `-0.62rad`(약 -35.5°)의 3/4뷰로 설정 — 기존 옆모습(0rad, pet.js의 기본 대기 자세와 동일 각도)은 예전 2D 썸네일과 실루엣이 거의 똑같아서 "이게 3D 모델이다"라는 게 잘 안 드러나므로, 깊이감이 실제로 보이는 각도를 의도적으로 선택(얼굴이 회전을 리드하는 `pet.js`의 `FACING_LEFT_Y` 부호 컨벤션과 같은 방향).
  - **리소스 정리**: `disposeGroup()`을 새로 추가해 각 종을 렌더링한 뒤 `geometry.dispose()`/`material.dispose()`/(있다면) `material.map.dispose()`까지 순회 — 샴 고양이의 `faceShade` 그라데이션이 `THREE.CanvasTexture`를 쓰는데(5차 QA 라운드 참고) 이건 `material.dispose()`만으로는 안 풀리는 별도 GPU 리소스라 빠뜨리면 설정 창을 열 때마다(캐릭터 10개를 매번 새로 build하므로) 텍스처가 누적돼 새는 구조였음 — 명시적으로 같이 처분.
  - **미리보기(previewCharacter)와 그리드가 같은 렌더 소스를 쓰는지 확인**: `settings.js`의 그리드 클릭 핸들러 → `previewCharacter` IPC → `main.js`의 `preview-character` 핸들러 → `pet.js`의 `onPreviewCharacter` → `loadCharacter()` → `buildVoxelCreature()` 경로를 다시 추적 — 이제 그리드 썸네일(`renderCharThumb`)과 라이브 미리보기(`loadCharacter`) **둘 다 `buildVoxelCreature`를 직접 호출**해서 완전히 같은 소스를 공유함(둘 사이에 렌더러 자체는 별개 인스턴스지만, 지오메트리/색/포즈 계산 로직은 100% 동일 함수). 이전엔 그리드가 2D `drawCreature`, 프리뷰가 3D `buildVoxelCreature`로 아예 다른 두 렌더러였던 것과 대조됨 — 이번 수정으로 이 불일치 자체가 구조적으로 해소됨.
  - **검증**: 실제 Electron 앱을 띄워 캡처 — 10종 전부 실제 3D 모델(태비 줄무늬, 턱시도 흰 블레이즈, 삼색이 좌우 다른 귀 색, 허스키 파란 눈+흰 얼굴 등)이 3/4 각도로 또렷하게 보임을 확인. 콘솔 에러 없음(`three` bare specifier를 해석하려면 `windows/pet/index.html`과 동일한 `<script type="importmap">`이 필요해서 `windows/settings/index.html`에도 추가함 — 빠뜨렸다면 모듈 로드 자체가 실패했을 부분, 실제로 에러 없이 로드되는 것으로 확인).

**2. 설정 창 UI 전면 리디자인 (기능 변경 없음, 시각적 요소만)**: 갈색/베이지 계열의 밋밋한 배색과 기본 브라우저 컨트롤(체크박스/슬라이더 그대로) 대신, 뉴트럴 톤 + 코랄-앰버 포인트 컬러 하나로 정리:
  - **색 토큰화**: `settings.css` 최상단에 CSS custom property로 팔레트 정의(`--bg`/`--surface`/`--border`/`--text`/`--text-secondary`/`--accent`/`--accent-soft` 등) — 배경은 아주 옅은 웜 그레이(`#f6f4f0`), 카드는 흰색, 텍스트는 다크 뉴트럴, 포인트 컬러는 `#e8763a`(코랄-앰버, 기존의 탁한 `#e8a95a`보다 채도를 올려 하나의 색이 "선택됨/주요 동작"을 일관되게 나타내도록) 하나로 통일 — 캐릭터 선택 카드의 선택 테두리, 라디오 선택 행 배경, 토글 스위치 on 색, 슬라이더 채움, Primary 버튼이 전부 이 한 색만 씀.
  - **타이포그래피 위계**: 헤더(16px/700), 섹션 타이틀(11.5px/700/대문자/회색), 필드 라벨(12.5px/600), 라디오 타이틀(13px/600)+힌트(11.5px/400/회색) 2단으로 분리 — 기존엔 "고정 간격 (뽀모도로 스타일 — ...)"처럼 제목과 설명이 한 줄 텍스트로 뭉쳐 있던 걸, `index.html`에서 `<span class="radio-title">`/`<span class="radio-hint">`로 마크업만 쪼개서(문구 자체는 그대로) 굵기/크기 차등을 줌.
  - **카드 그룹핑**: 기존 `<section>`들을 전부 `.card`(흰 배경, 둥근 모서리, 옅은 테두리+그림자)로 감싸고, 서로 관련된 "언제 알려줄까요" 라디오 + "알림 간격"/"입력 없음 감지 기준" 숫자 입력을 하나의 카드 안에 묶음(기존엔 3개의 독립된 `<section>`이었음) — `interval-section`/`idle-section`의 **id는 그대로 유지**했으므로 `settings.js`의 `updateModeVisibility()`(`.hidden` 클래스 토글) 로직은 손 안 댐.
  - **토글 스위치**: `#soundEnabled` 체크박스는 그대로 두고(`settings.js`가 `.checked`를 읽고 쓰는 로직 자체는 무변경), 시각적으로만 `opacity:0`로 숨긴 뒤 인접한 `.toggle-track` span을 `input:checked + .toggle-track` 형제 선택자로 스타일링하는 표준 CSS 토글 패턴 채택 — 순수 CSS 리스킨이라 JS 변경 없이도 동작.
  - **선택 상태 강조**: 캐릭터 카드는 선택 시 포인트 컬러 테두리+연한 배경+우측 상단 체크마크 배지(`::after`)로 표시(기존엔 테두리 색만 살짝 바뀌는 정도). 라디오 행은 Chromium이 지원하는 `:has()` 선택자로 `.radio-row:has(input:checked)` 규칙을 걸어 체크된 라디오가 속한 행 전체에 배경/테두리를 입힘 — 별도 JS 없이 순수 CSS만으로 "선택된 행 전체가 카드처럼 강조되는" 효과.
    - **검증 중 발견한 헛다리(실제로는 버그 아님)**: 자동화 스모크 테스트로 `radio.checked=true` 직후 곧바로 `getComputedStyle(row).backgroundColor`를 읽었더니 `rgba(0,0,0,0)`(투명)이 나와서 `:has()`가 안 먹히는 줄 알았음 — `.radio-row`에 `transition: background 0.15s ease`가 걸려 있어서, 상태 변경 직후 읽은 값이 트랜지션 시작 지점(거의 투명)이었을 뿐이었음. `CSS.supports('selector(:has(*))')`로 브라우저 지원 자체를 확인하고, 트랜지션이 끝난 뒤(250ms 대기) 다시 읽으니 정확히 `rgb(253,239,226)`(`--accent-soft`)로 정착 — 실제로는 버그가 아니라 "읽는 타이밍이 CSS 애니메이션 중이었다"는, 이 문서 전반에서 반복돼온 "짧은 순간의 상태를 정적으로 판단하면 오판할 수 있다"는 교훈의 또 다른 사례. 별도 코드 수정 없이 검증 스크립트만 대기 시간을 늘려 재확인함.
  - **커스텀 슬라이더**: `input[type=range]`에 `-webkit-appearance:none` 후 `background`를 `linear-gradient(...)`로 직접 걸어 "채워진 부분/안 채워진 부분"을 표현 — 값이 바뀔 때마다 `settings.js`의 새 `updateVolumeFill()`(기존 `soundVolumeInput`의 `input` 이벤트 리스너와 `fillForm()` 양쪽에서 호출)이 `--fill` CSS 변수를 퍼센트로 갱신. Thumb는 `::-webkit-slider-thumb`로 흰 배경+포인트 컬러 테두리의 원형.
  - **단일 스크롤 영역으로 정리**: 기존엔 `body`가 명시적 높이 제약이 없어 콘텐츠가 460x640(구 420x560) 창보다 커지면 `main`의 자체 `overflow-y:auto` 스크롤바 **더불어** OS 레벨 바깥 스크롤바까지 같이 나타나(스크린샷으로 서로 몇 px 떨어진 두 개의 스크롤바를 확인) 지저분했음 — `html,body{height:100%}` + `body{display:flex;flex-direction:column;overflow:hidden}` + `header/footer{flex:0 0 auto}` + `main{flex:1 1 auto;overflow-y:auto}`로 레이아웃을 고쳐서 `main` 하나만 스크롤되도록 정리.
  - **창 크기**: `main.js`의 `createSettingsWindow()` 크기를 420×560 → 460×640으로 소폭 확대 — 카드 패딩/여백이 늘어난 새 레이아웃이 이전 크기에선 스크롤이 과하게 필요했을 것이라 판단, 기능과 무관한 순수 수치 변경.
  - **버튼 통일**: Primary(저장, 포인트 컬러 배경+흰 텍스트)/Secondary(기본값으로, 흰 배경+테두리) 스타일을 하나의 규칙 세트로 정리하고 hover/active 트랜지션 추가.
  - **범위 밖으로 둔 것**: 다크 모드/테마 전환은 요청에 없었고 네이티브 Electron 창이라 OS 다크모드 감지 자체가 이 앱의 다른 어디에도 없어서 이번에도 안 넣음(라이트 테마 하나로 통일). 라이브 회전 프리뷰(그리드 안에서 캐릭터가 계속 도는 것)도 사용자가 명시적으로 "필요 없다"고 한 부분이라 넣지 않음 — 클릭하면 실제 pet 창에서 바로 확인 가능한 기존 `previewCharacter` 플로우가 그 역할을 대신함.

**검증 (기능 회귀 없음, 실제 앱 경로 둘 다 확인)**:
- **자동화 스모크 테스트**(진짜 `preload.js`/`settings.js`/`main.js`의 IPC 계약을 그대로 재구현한 스크래치 하네스로, DOM 이벤트를 실제로 디스패치): 캐릭터 클릭 → `.selected` 클래스 적용 + `previewCharacter` IPC 발사, 모드 라디오 전환 → `interval-section`/`idle-section`의 `.hidden` 토글이 정확히 반대로 바뀜, 토글 스위치 클릭 → 내부 체크박스 `.checked` 실제로 반전, 볼륨 슬라이더 조작 → 라벨 텍스트+`--fill` 갱신, 기본값 버튼 → 폼 리필+미리보기 재발사, 저장 버튼 → `save-settings` IPC에 올바른 최종 설정 객체 전달 — 전부 기대대로 동작 확인(마크업을 라디오 힌트 텍스트 분리/토글 스위치/카드 그룹핑으로 상당히 재구성했음에도 `id` 기반 로직은 전부 그대로 유지했기 때문에 무리 없이 통과).
- **실제 `main.js` 진입점으로도 재확인**: 격리된 스크래치 하네스 대신, `FOCUSPET_DEBUG_SETTINGS` 환경변수로 시작 시 `createSettingsWindow()`를 자동 호출하는 한 줄을 `main.js`에 임시로 추가해 진짜 앱(트레이/타이머/폴링 전부 같이 도는 상태)에서 뜬 설정 창을 캡처 — 460×640(OS 프레임 포함 461×640) 크기로 정상 생성, 콘솔 에러 없음, 그리드 3D 썸네일도 동일하게 보임 확인 후 이 임시 훅은 즉시 제거(`git diff main.js`로 실제 남은 변경이 창 크기 수치 하나뿐임을 재확인).
- `npm run test:invariants`(120만 프레임) 재실행 — 위반 0건(이번 라운드는 `animal-engine.js`/`voxel-engine.js`의 애니메이션·지오메트리 로직을 전혀 안 건드렸으므로 예상대로).

### 설정 창 커스텀 스크롤바 + 알림 기본값 조정 + "둘 다 사용" 모드 제거
직전 라운드(설정 창 리디자인) 직후 요청 3건 — 스크롤바를 새 팔레트에 맞게, 알림 기본값 변경(고정 간격 25→30분, 활동 감지 임계값을 초 단위에서 분 단위 UI로+기본 15분), "둘 다 사용" 모드가 실제로 중복 알림을 유발하는지 코드로 확인 후 판단.

**1. 커스텀 스크롤바**: `settings.css`의 `main`(유일한 스크롤 영역, 직전 라운드에서 단일 스크롤로 정리해둔 그 요소)에 `scrollbar-width:thin; scrollbar-color: var(--border-strong) transparent;`(표준 스펙, 폴백 성격)와 `::-webkit-scrollbar`/`-track`/`-thumb`/`-thumb:hover`(Electron/Chromium이 실제로 렌더링에 쓰는 쪽, 더 세밀한 제어 가능)를 같이 추가 — 트랙은 투명, 썸은 `--border-strong`(옅은 뉴트럴 톤)에 `border:2px solid transparent`+`background-clip:padding-box`로 트랙 가장자리에서 안쪽으로 살짝 띄운 "떠 있는 얇은 알약" 모양, hover 시 `--accent-border`로 살짝 진해짐. 기본 상태는 포인트 컬러가 아니라 뉴트럴 톤을 써서 "은은하게"라는 요청을 그대로 반영(hover에서만 브랜드 컬러 힌트).

**2. 알림 기본값 변경**: `settingsStore.js`의 `DEFAULTS.intervalMinutes`를 25→30으로, `DEFAULTS.idleThresholdSeconds`를 90→900(15분)으로 변경 — 필드 자체는 사용자가 명시한 대로 초 단위 저장을 그대로 유지. UI 쪽만 분 단위로 전환: `index.html`의 입력 필드 id를 `idleThresholdSeconds` → `idleThresholdMinutes`로 바꾸고(더 이상 초가 아닌 값을 담는데 옛 id를 그대로 두면 나중에 헷갈릴 수 있어서) 라벨을 "입력 없음 감지 기준 (분)"으로, `min/max`를 `intervalMinutes` 필드와 동일하게 `1~180`으로 맞춤(두 분 단위 입력의 허용 범위를 통일 — 임의의 새 숫자를 만들어내는 대신 이미 있는 필드의 범위를 그대로 재사용). `settings.js`의 `fillForm()`/저장 핸들러에서 `Math.round(settings.idleThresholdSeconds/60)`(불러올 때)·`(Number(idleInput.value)||15)*60`(저장할 때)로 경계에서만 초↔분 변환 — 그 외 로직(모드별 표시/숨김 등)은 안 건드림.
  - **검증**: 새 창을 열었을 때 두 필드가 각각 30/15로 뜨는지, 값을 22분으로 바꿔 저장하면 실제로 `idleThresholdSeconds:1320`(22×60)으로 저장되는지, 그 값으로 창을 다시 열면 다시 22로 정확히 역변환되어 보이는지를 실제 `preload.js`/`settings.js`/`save-settings` IPC 계약을 그대로 태운 스크립트로 확인 — 전부 일치.

**3. "둘 다 사용" 모드 조사 — 실제로 문제 있음을 코드로 확인 후 제거**: `main.js`의 `applyTimers()`를 직접 읽어 확인 — `mode==='interval'||mode==='both'`일 때 `setInterval(fireReminder, intervalMinutes*60*1000)`를, `mode==='idle'||mode==='both'`일 때 별도의 5초 폴링 `setInterval`(유휴 시간이 임계값을 넘는 "새로운" 순간마다 `fireReminder()`)을 **완전히 독립적으로** 등록하고 있었고, `fireReminder()` 자신도 `settings.paused` 체크 외에는 아무 쿨다운/중복 억제 로직이 없었음 — 즉 두 타이머 사이에 서로를 아는 코드가 전혀 없음. 사용자가 우려한 시나리오(30분 고정 간격 + 15분 활동 감지를 같이 켜면, 간헐적으로 움직이는 사용자는 "15분 넘게 가만히 있었다가 살짝 움직이고 다시 15분 넘게 가만히" 패턴을 반복할 때마다 매번 새로 알림이 발동돼, 의도한 30분보다 훨씬 잦아질 수 있음)가 코드상 실제로 그대로 재현 가능한 구조임을 확인 — 이를 막는 로직은 어디에도 없었음.
  - **결정**: 사용자가 제시한 조건("정말 중복/과다로 이어진다면 제거")에 해당해 **완전히 제거**함. `windows/settings/index.html`에서 `value="both"` 라디오 행 삭제(이제 라디오는 "고정 간격"/"활동 감지" 2개만). `main.js`의 두 조건에서 `|| settings.mode === 'both'`를 제거해 각자 자기 모드만 체크하도록 단순화. `settingsStore.js`의 `load()`에 마이그레이션 추가 — 기존에 저장된 `settings.json`이 `mode:'both'`를 갖고 있으면 `'interval'`로 되돌림(기존 `LEGACY_CHARACTER_MAP`과 같은 패턴) — `'idle'`이 아니라 `'interval'`을 고른 이유는 사용자가 마지막으로 명시적으로 골랐던 동작과 무관하게 더 예측 가능한 쪽(고정 주기)으로 떨어지게 하기 위함. `DEFAULTS.mode`는 원래도 `'interval'`이라 안 건드림.
  - **검증**: `store.load()`를 직접 호출해 `mode:'both'`로 저장된 `settings.json`을 읽으면 `mode:'interval'`로(나머지 필드는 그대로) 정규화되는 것을 확인. 실제 창에서 라디오가 2개뿐인 것, "고정 간격"/"활동 감지" 각각 선택했을 때 라벨/필드가 정상적으로 바뀌는 것을 스크린샷으로 확인.

**전체 회귀 확인**: `npm run test:invariants`(120만 프레임) 위반 0건(이 로직과 무관). 실제 앱(`npx electron .`)을 12초간 띄워 확인 — 기존에 이미 알려진 GPU 디스크 캐시 경고 외 콘솔 에러 없이 정상 구동 후 종료. `git status`로 확인한 결과 `assets/app-icon.png`/`assets/tray-icon.png`와 신규 `assets/app-icon-256.png`/`assets/tray-icon-16.png`가 이 세션 시작 전부터 이미 워킹트리에 변경/추가돼 있었음(이번 라운드가 만든 변경이 아님, 손 안 댐) — 사용자가 인지하지 못한 상태일 수 있어 그대로 남겨두고 알림.

### 새 앱/트레이 아이콘 반영 확인 (코드 변경 없음 — 이미 올바르게 연결돼 있었음을 검증만)
직전 라운드 끝에서 발견했던 `assets/app-icon.png`/`assets/tray-icon.png`(내용 교체, 파일명 유지)와 신규 `assets/app-icon-256.png`(256×256)/`assets/tray-icon-16.png`(16×16, 둘 다 아직 어디서도 참조 안 됨)에 대해, 사용자가 실제로 새 아이콘으로 교체했다고 확인 — 이게 트레이/설정창 타이틀바/`package.json` 빌드 설정/실행파일 아이콘까지 전부 실제로 반영되는지 점검.

**코드 레벨 확인 (전부 이미 정확한 경로를 가리키고 있었음, 수정 불필요)**: `grep`으로 `app-icon`/`tray-icon` 참조 4곳을 전수 확인 — `main.js:101`(설정 창 `BrowserWindow`의 `icon` 옵션) → `assets/app-icon.png`, `main.js:233`(`nativeImage.createFromPath` → `new Tray(icon)`) → `assets/tray-icon.png`, `package.json`의 `build.win.icon`/`build.mac.icon` → `assets/app-icon.png` — 전부 파일명이 그대로라 사용자가 내용만 교체한 것으로 이미 새 이미지를 가리키고 있었음. 코드 변경이 필요 없었던 이유: 아이콘 자체를 다른 파일로 바꾼 게 아니라 "같은 파일명, 새 내용"으로 교체했기 때문 — 파일 경로를 참조하는 코드 입장에서는 아무 차이가 없음.

**PNG→ICO 변환이 필요한지 실측으로 확인**: electron-builder(`app-builder-lib`)의 `WinPackager`가 실제로 뭘 하는지 소스(`node_modules/app-builder-lib/out/winPackager.js`/`platformPackager.js`)를 읽어 확인 — `getOrConvertIcon("ico")`가 내부적으로 자기가 번들한 네이티브 바이너리(`app-builder-bin`)를 `app-builder icon --format ico --input <경로>`로 호출해서 PNG를 ICO로 **항상 자동 변환**하는 구조였음 — 즉 `build.win.icon`에 ICO가 아니라 PNG를 줘도 빌드 타임에 자동으로 변환됨. 문서/기억에만 의존하지 않고, 이 바이너리(`node_modules/app-builder-bin/win/x64/app-builder.exe`)를 실제로 직접 실행해서 `assets/app-icon.png`(512×512)을 ICO로 변환해봄 — 에러 없이 성공, ICO 헤더를 직접 파싱해서 유효한 파일임을 확인. 결과 ICO는 256×256 단일 프레임 하나만 담고 있었음(입력으로 `app-icon.png`+`app-icon-256.png`+`tray-icon.png`+`tray-icon-16.png`를 다양한 조합으로 같이 줘봐도 항상 "가장 적합한 소스 하나"만 골라 256×256 한 장으로 리사이즈할 뿐, 여러 해상도를 조합해 진짜 멀티 레졸루션 ICO를 만들어주지는 않음 — 이 도구/electron-builder의 실제 동작 방식으로 확인) — 다만 단일 256×256 프레임 ICO는 Windows가 타이틀바/작업표시줄 등 더 작은 크기에서 자동으로 다운스케일해 렌더링하는, electron-builder로 빌드되는 앱 대부분이 실제로 쓰는 흔하고 정상적인 방식이라 문제 삼지 않음. **결론: PNG만으로 충분, 저장소에 별도 변환 스텝을 추가할 필요 없음** — mac 쪽(`build.mac.icon`→ICNS)도 같은 `resolveIcon` 경로를 타므로 원리는 동일할 것으로 보이나, ICNS 변환 바이너리는 macOS 전용이라 이 Windows 환경에서 직접 실행해 확인하지는 못함(정직하게 기록).

**실제 렌더링 확인 (스크린샷)**: `webContents.capturePage()`는 창의 **웹 콘텐츠 영역만** 캡처하고 OS가 그리는 네이티브 타이틀바/프레임은 안 담기는 걸 알게 돼서(설정 창 타이틀바 아이콘은 이 방법으로 확인 불가), 대신 `desktopCapturer.getSources({types:['window']})`로 창 전체(네이티브 프레임 포함)의 썸네일을 캡처하는 방식으로 전환.
  - **자기 자신의 창을 못 캡처하는 문제 발견**: 처음엔 설정 창을 띄운 바로 그 프로세스 안에서 `desktopCapturer.getSources()`를 호출했는데, `win.getTitle()`은 정확히 "FocusPet 설정"을 반환하고 창도 `isVisible()=true`인데도 반환된 window source 목록에 그 창 자체가 아예 없었음(Windows에서 desktopCapturer가 호출자 자신의 프로세스가 소유한 창은 열거하지 않는 것으로 보임 — Chromium의 알려진 동작). **고침**: 진짜 앱(`npx electron .`, `FOCUSPET_DEBUG_SETTINGS` 임시 env var로 시작 시 설정 창도 같이 뜨게 함)을 백그라운드 프로세스로 띄워두고, **완전히 별개의 두 번째 Electron 프로세스**에서 `desktopCapturer`를 호출해 그 창을 캡처 — 이번엔 "FocusPet"(펫 창)과 "FocusPet 설정"(설정 창) 둘 다 정상적으로 목록에 잡히고 캡처됨.
  - **설정 창 타이틀바 아이콘**: 캡처된 창 전체 스크린샷의 좌상단에 새 아이콘(고양이 발자국이 그려진 둥근 배지 모양)이 "FocusPet 설정" 텍스트 옆에 또렷하게 보임 — 확대 크롭으로도 재확인.
  - **트레이 아이콘**: 전체 화면 캡처(`desktopCapturer`의 `screen` 소스)로 작업표시줄 시스템 트레이 영역을 크롭해봤지만 눈에 보이는 아이콘 목록엔 FocusPet이 없었음 — Windows가 새로 등록되는 트레이 아이콘을 기본적으로 "숨겨진 아이콘"(오버플로우) 영역에 넣는 기본 동작 때문일 가능성이 높다고 보고, 스크린샷만으로 결론 내리지 않고 코드/API 레벨로 추가 확인: (1) `nativeImage.createFromPath(tray-icon.png)`가 `isEmpty():false`, `size:{32,32}`, 중심 픽셀이 불투명한 실제 색상(`isEmpty`도 아니고 완전 투명도 아님)을 반환 — 이미지 자체가 정상 로드됨. (2) 실제 앱에서 `tray.getBounds()`를 찍어보니 `{x:1160,y:816,width:33,height:48}`라는 유효한(0이 아닌) 사각형을 반환 — OS가 이 트레이 아이콘을 실제로 인식하고 좌표를 배정하고 있다는 뜻(단, 보이는 작업표시줄 트레이 줄의 y좌표와는 다른 값이라, "숨겨진 아이콘" 플라이아웃 내부 좌표로 추정). 즉 트레이 아이콘 자체는 정상적으로 로드되고 등록되고 있음을 코드 레벨로 확인했지만, 스크린샷 상 "눈에 보이는 트레이 줄"에 나타나는 것까지는 확인하지 못함(Windows의 "새 트레이 아이콘은 기본적으로 숨김" 동작 때문으로 추정 — 우리 코드/이미지 파일 문제가 아니라 OS 기본값 문제일 가능성이 큼, 정직하게 기록. 사용자가 작업표시줄 설정에서 "숨겨진 아이콘 표시"로 수동 고정하면 바로 보일 것).
  - **검증 중 발견한 부수 문제**: 검증 과정에서 이전 세션들에서 남겨진 electron.exe 프로세스가 11개나 백그라운드에 떠 있는 걸 발견(`app.quit()`/`timeout` 킬이 항상 프로세스 트리를 완전히 정리하지는 않았던 것으로 보임) — 전부 이 세션이 만든 스크래치 검증용 프로세스라 안전하게 `taskkill`로 정리. 검증에 쓴 임시 훅(`FOCUSPET_DEBUG_SETTINGS` 자동 설정창 오픈, `tray.getBounds()` 로그)은 확인 후 전부 `main.js`에서 제거 — `git diff main.js`가 이 라운드 시작 전과 정확히 동일함을 재확인(이번 라운드는 실제로 코드를 하나도 안 고쳤음).
  - **범위 밖으로 둔 것**: `app-icon-256.png`/`tray-icon-16.png`는 여전히 어디서도 참조되지 않는 미사용 파일 — 트레이 아이콘을 여러 DPI에 맞게 크리스프하게 보이려면 `nativeImage.addRepresentation()`으로 이 16px 소스를 명시적으로 추가하는 개선이 가능하지만, 이번 요청 범위(기존 참조가 새 파일을 반영하는지 확인)를 벗어나는 별도 개선이라 손 안 댐 — 필요하면 다음에 요청.

### 기본 알림 메시지 교체 + "기본값으로" 버튼이 메시지 textarea를 채운 적이 없던 버그 발견/수정
`settingsStore.js`의 `DEFAULTS.messages`(기존 5개: "집중할 시간이에요! 🧐" 등)를 사용자가 준 새 12개 문구로 교체.

**기존 사용자에게 언제 반영되는지 확인**: `load()`가 `{...DEFAULTS, ...parsed}`로 병합하는 구조라, 이미 `settings.json`이 있는 기존 사용자는 그 파일에 저장된 `messages` 배열(과거에 저장된 시점의 기본값이든 직접 편집한 문구든)이 새 `DEFAULTS.messages`를 그대로 덮어써서 **자동으로는 안 바뀜** — 사용자가 예상한 그대로. 새로 설치하는 경우(`settings.json` 자체가 없음)는 `load()`가 `{...DEFAULTS}`를 그대로 반환하므로 새 12개가 바로 적용됨 — 둘 다 직접 시뮬레이션해서 수치로 확인(기존 사용자 시나리오는 예전 5개 문구가 든 `settings.json`을 미리 써두고 `load()` 호출, 신규 시나리오는 `settings.json` 자체를 지우고 `load()` 호출).

**검증 중 발견한 버그**: "기존 사용자는 설정창에서 '기본값으로'를 눌러야 반영된다"는 걸 실제로 확인하려고 그 버튼을 실제 UI로 눌러봤는데, **메시지 textarea가 처음부터 끝까지 한 번도 채워지지 않는** 걸 발견 — `windows/settings/settings.js`의 `fillForm(settings)`가 캐릭터/모드/간격/위치/소리는 전부 폼에 반영하면서 `messagesTextarea.value`를 세팅하는 줄 자체가 없었음(설정 창 UI 리디자인 라운드에서 `fillForm`을 상당히 손댔는데 그때부터 빠져있었던 것으로 보임). 그 결과:
  - 설정 창을 열어도 지금 저장된 메시지가 뭔지 화면에서 전혀 안 보임(빈 칸).
  - "기본값으로"를 눌러도(내부적으로 `fillForm(defaults)`를 호출하지만 메시지는 그 함수가 손을 안 대므로) textarea는 여전히 빈 칸 그대로.
  - 이 상태로 "저장"을 누르면 `saveBtn` 핸들러가 빈 textarea를 `''.split('\n').filter(Boolean) = []`로 파싱해 `messages: undefined`를 보내고, `main.js`의 `save-settings` 핸들러가 `{...settings, ...newSettings}`로 병합하면서 `settings.messages`를 명시적으로 `undefined`로 덮어씀 — `JSON.stringify`가 `undefined` 값을 가진 키는 아예 안 쓰므로 `settings.json`에서 `messages` 키 자체가 사라짐. 다행히 `main.js`의 `pickMessage()`가 `settings.messages && settings.messages.length` 체크로 빈/누락 시 `store.DEFAULTS.messages`로 폴백하는 방어 코드가 있어서 알림 자체가 깨지진 않았지만(그래서 지금까지 겉으로 드러나는 문제 없이 지나갔던 것), 사용자가 커스텀 메시지를 넣어뒀었다면 저장 버튼 한 번에 조용히 지워지는 셈이었고, "기본값으로 되돌리기" 버튼도 메시지 항목에 한해서는 사실상 눈으로 확인할 방법도 실제로 되돌리는 방법도 없었음.
  - **고침**: `fillForm()` 끝에 `messagesTextarea.value = (settings.messages || []).join('\n');` 한 줄 추가 — `fillForm`은 항상 `messages` 배열이 이미 채워진 객체(로드된 `settings` 또는 `store.DEFAULTS` 자체)만 받으므로 이 한 줄로 충분.
  - **검증**: 예전 5개 문구가 저장된 것처럼 꾸민 설정으로 창을 열어 textarea에 그 5개가 정확히 보이는지, "기본값으로"를 누르면 textarea가 새 12개 문구로 바뀌는지, 그 상태로 "저장"을 누르면 실제로 그 12개가 `save-settings` IPC에 그대로 실려가는지를 실제 `preload.js`/`settings.js`/`main.js` IPC 계약을 그대로 태운 스크립트로 3단계 다 확인 — 전부 기대대로 동작.

**전체 회귀 확인**: 실제 앱(`npx electron .`)을 10초간 띄워 확인 — 콘솔 에러 없이 정상 구동 후 종료.

### "기본값이 여전히 25분/2분(120초)으로 보인다" 재조사 — 코드는 이미 정확했음, 원인은 사용자의 실제 `settings.json`이 변경 이전에 저장됐던 것 + 커스텀 셀렉트 드롭다운 신규 추가
사용자가 "지난번 기본값 변경이 반영이 안 된 것 같다"고 재확인 요청 — `settingsStore.js`를 다시 읽어보니 `intervalMinutes:30`/`idleThresholdSeconds:900`가 이미 정확히 들어있었음(코드 자체는 문제 없었음). 실제 원인을 찾으려고 이 머신의 진짜 `%APPDATA%\focus-pet\settings.json`(dev 모드에서 `app.getPath('userData')`가 실제로 쓰는 그 경로, 이 세션들이 만든 스크래치 테스트와는 별개의 진짜 파일)을 직접 열어봄 — `intervalMinutes:25`, `idleThresholdSeconds:90`가 그대로 저장돼 있었음(이 값 변경 이전, 이전 세션에서 실제 앱을 처음 켰을 때 저장된 파일로 추정). `load()`가 `{...DEFAULTS, ...parsed}`로 병합하는 구조라 이 저장된 값이 새 `DEFAULTS`를 그대로 덮어쓰고 있었던 것 — 지난 라운드에서 이미 설명했던 "기존 사용자는 자동으로 안 바뀐다"는 그 케이스가 사용자 본인의 실제 로컬 파일에서 정확히 일어나고 있었음. "2분(120초)"이라고 보였던 부분도 계산해보니 실제 저장값은 90초였고, `Math.round(90/60)=Math.round(1.5)=2`(자바스크립트가 .5를 반올림할 때 올림)라 "2분"으로 표시된 것 — 사용자가 2분을 120초로 역산했지만 실제 저장값은 90초였던 것도 확인.
  - **조치**: 이번엔 코드 검증에서 그치지 않고, 사용자의 실제 `settings.json`에서 `intervalMinutes`/`idleThresholdSeconds` 두 필드만 30/900으로 직접 패치(다른 필드 — `character:"dog_pomeranian"`, `soundVolume:1` 등 — 는 사용자가 실제로 커스터마이징한 값이라 그대로 유지, 전체 파일을 기본값으로 덮어쓰지 않음). 다음에 설정 창을 열면 바로 30분/15분이 보임.
  - **검증**: `store.DEFAULTS`가 정확히 30/900인지 재확인, 새 창을 열었을 때(기존 `settings.json` 없는 상태를 스크립트로 재현) 필드가 30/15로 채워지는지, "활동 감지" 모드로 전환했을 때도 15가 보이는지 스크린샷 2장으로 확인.

**"둘 다 사용" 모드 결론 재확인 (지난 라운드 요약)**: 코드로 직접 확인한 결과 `interval` 타이머와 `idle` 폴링 타이머가 서로를 전혀 모르는 채 각자 `fireReminder()`를 독립적으로 호출하고 있었고, 쿨다운/중복 억제 로직이 어디에도 없어서 사용자가 우려한 "간헐적 활동 패턴에서 의도한 것보다 훨씬 잦은 알림"이 실제로 재현 가능한 구조였음 — 그래서 **"둘 다 사용" 옵션 자체를 설정 화면에서 완전히 제거**했고(`windows/settings/index.html`의 `value="both"` 라디오 삭제), `main.js`의 `applyTimers()`도 각 조건에서 `|| mode==='both'`를 제거해 단순화했으며, `settingsStore.js`의 `load()`에 기존에 `mode:'both'`로 저장된 설정을 `'interval'`로 되돌리는 마이그레이션을 추가했음 — 지금 이 저장소에는 `'both'`를 선택할 방법 자체가 없음.

### (신규) 드롭다운(`<select>`) 커스텀 스타일링
설정 창의 "펫 위치" 셀렉트가 지난 리디자인 라운드에서 빠져 있던 걸 발견 — `input[type="number"], select, textarea`로 테두리/패딩만 공유하고 있었을 뿐, 화살표 아이콘과 옵션 목록은 여전히 브라우저/OS 기본 스타일 그대로였음.

**구현**: 네이티브 `<select id="position">`는 DOM에 그대로 남겨두되(`settings.js`의 `positionSelect.value` 읽기/쓰기 로직은 전혀 안 건드림 — 여전히 실제 값의 소스) `.native-select-hidden`(절대위치+1px 크기+opacity:0)으로 시각적으로만 숨기고, 그 위에 커스텀 트리거 버튼(`#positionTrigger`, 선택된 라벨 + 회전하는 SVG 셰브런)과 커스텀 리스트박스(`#positionListbox`, `role="listbox"`/`role="option"`, 카드와 같은 흰 배경+그림자, 선택된 옵션은 라디오 행과 동일한 accent-soft 배경+강조색 텍스트)를 얹음. `settings.js`에 `syncPositionTrigger()`(네이티브 select 값 → 트리거 라벨/`aria-selected` 동기화)/`setPosition()`/`open·closePositionMenu()`를 추가하고 `fillForm()` 끝에 `syncPositionTrigger()` 호출을 끼워 넣는 것만으로 기존 로드/저장 흐름과 완전히 호환.
  - **발견한 버그 (직접 스크린샷 비교하다 발견) — 드롭다운이 카드 밖으로 못 나가고 잘림**: 처음엔 `.select-menu`를 `position:absolute`(트리거 기준)로 만들었는데, "펫 위치" 카드가 페이지 중간쯤(스크롤되는 `<main>`의 `overflow-y:auto` 안)에 있을 때 열어보니 옵션 4개 중 2개(오른쪽 위/왼쪽 위)가 안 보였음 — `getBoundingClientRect().height`로 확인해보니 실제로 0(렌더링은 됐지만 시각적으로 잘림). 원인: `position:absolute` 요소라도 조상에 `overflow:auto`가 있으면 그 조상의 클리핑 박스를 벗어난 부분은 그대로 잘림(널리 알려진 CSS 동작, `absolute`가 "겹쳐서 그린다"는 것과 "스크롤 컨테이너 밖으로 삐져나갈 수 있다"는 건 별개). **고침**: `.select-menu`를 `position:fixed`로 바꾸고, 열릴 때마다 `positionMenuToTrigger()`가 `positionTrigger.getBoundingClientRect()`로 뷰포트 기준 좌표를 계산해 인라인으로 `left/top/width`를 세팅 — 스크롤 컨테이너의 클리핑을 아예 벗어남(팝업 라이브러리들이 "포탈링"이라 부르는 방식과 같은 원리). `<main>`이 스크롤되면(트리거가 화면에서 움직이는데 `fixed` 팝업은 안 따라가므로) 메뉴를 닫도록 스크롤 리스너도 추가 — 네이티브 `<select>`의 드롭다운도 트리거가 스크롤로 가려지면 스스로 닫히는 것과 같은 동작.
  - **검증 스크립트 자체의 버그 (진짜 앱 버그와 착각할 뻔함)**: 고친 뒤 재검증하다가 스크린샷에 드롭다운 자체가 아예 안 보이는(캐릭터 그리드가 찍히는) 이상한 결과가 한 번 나왔는데, `getBoundingClientRect()`로 실제로는 옵션 4개가 전부 정상 렌더링되고 있다는 게 확인돼서 앱 버그가 아니라고 판단 — 원인을 파보니 스크린샷 스크립트가 `#position`(이번에 시각적으로 숨긴 네이티브 select, 1px 크기)을 `scrollIntoView`하고 있었던 것 — 숨겨진 1px 요소를 스크롤 대상으로 삼으니 스크롤 자체가 거의 안 일어나(`scrollTop`이 0으로 남음) 트리거가 화면 아래(뷰포트 640px보다 훨씬 아래)에 있는 채로 열렸던 것뿐, 실제 렌더링 로직과는 무관한 스크립트 버그였음 — `#positionTrigger`(보이는 실제 요소)를 스크롤 대상으로 바꿔서 해결. 이 프로젝트에서 반복돼온 "이상해 보이는 결과부터 실제 원인을 수치로 확인하고, 테스트 스크립트 자체의 버그일 가능성도 배제하지 않는다"는 습관이 이번에도 유효했음.
  - **검증**: 실제 IPC 계약을 태운 스크립트로 트리거 클릭→열림, 옵션 클릭→네이티브 select 값과 트리거 라벨 동시 갱신+메뉴 닫힘, 저장 시 올바른 값 전달까지 확인. 클리핑 수정 후에는 카드가 페이지 중간에 있는 스크롤 위치에서도 4개 옵션 전부(`getBoundingClientRect().height>0`) 렌더링되고 4번째 옵션도 실제로 클릭 가능함을 재확인. 스크린샷으로 닫힌 상태(새 회색조 셰브런 아이콘)와 열린 상태(카드형 리스트박스, 선택 항목 강조) 둘 다 확인.

**전체 회귀 확인**: `npm run test:invariants`(120만 프레임) 위반 0건(이번 라운드는 `animal-engine.js`/`voxel-engine.js`를 전혀 안 건드림). 실제 앱(`npx electron .`)을 10초간 띄워 확인 — 콘솔 에러 없이 정상 구동 후 종료. 사용자의 실제 `%APPDATA%\focus-pet\settings.json`을 직접 패치한 것 외에는 코드 변경만 있었고, 검증용 임시 `node_modules` 디렉터리 정션(스크래치 "before" 스냅샷이 `three` 모듈을 올바른 상대 경로에서 찾게 하려고 만든 것)은 확인 후 정리함.

### 알림 메시지 textarea를 진짜 "직접 쓰는 칸"으로 + 펫 창 모서리 자석 스냅
**1. 메시지 textarea 기본값 미리보기 제거**: 직전 라운드에서 "textarea가 항상 비어있던 버그"를 고치면서 `fillForm()`에 `messagesTextarea.value = (settings.messages || []).join('\n')`을 추가했는데, 이 자체가 이번 요청 기준으로는 또 다른 문제였음 — `settingsStore.js`의 `load()`가 `{...DEFAULTS, ...parsed}`로 병합하는 구조라 사용자가 한 번도 커스텀 메시지를 저장한 적 없으면 `settings.messages`는 항상 `DEFAULTS.messages`(12개 유머 대사) 그 자체이고, 그 결과 textarea가 매번 이 12줄을 전부 미리 채운 채로 열렸음 — "이 칸은 사용자가 직접 쓰는 곳"이라는 원래 의도와 어긋남.
  - **핵심 문제**: `settings.messages`만 봐서는 "정말 커스텀한 것"과 "그냥 기본값을 물려받은 것"을 구분할 방법이 없음(둘 다 배열이 채워져서 옴). **해결**: `init()`에서 `getDefaults()`/`getSettings()`를 `Promise.all`로 같이 가져와 `cachedDefaultMessages`에 캐싱해두고, 새 `isDefaultMessages(messages)`(길이/내용이 `cachedDefaultMessages`와 완전히 같으면 true, 즉 "사용자가 손 안 댄 상태"로 판단)로 `fillForm()`의 textarea 채움 여부를 결정 — 진짜 커스텀 목록일 때만 그 내용을 보여주고, 기본값을 그대로 물려받은 상태면 빈 칸으로 둠(이 로직 하나로 최초 로드/"기본값으로" 리셋 둘 다 자연히 커버됨 — `resetBtn`이 `fillForm(defaults)`를 호출하면 `defaults.messages`가 `cachedDefaultMessages`와 내용까지 완전히 같으므로 역시 빈 칸이 됨, 별도 분기 불필요). `index.html`의 `<textarea>`에 `placeholder="비워두면 기본 유머 대사가 자동으로 사용돼요"` 추가, `settings.css`에 `textarea::placeholder` 색상(`--text-tertiary`)만 살짝 추가.
  - **알림에 실제로 쓰이는 로직은 그대로 유지**: `settingsStore.js`/`main.js`의 `pickMessage()`(빈/누락 시 `DEFAULTS.messages`로 폴백)는 **한 글자도 안 건드림** — 이번 변경은 순전히 "설정창이 뭘 보여주는가"에 관한 것이고, "알림이 실제로 뭘 쓰는가"는 기존 그대로.
  - **검증 (실제 IPC 계약 + 실제 main.js 양쪽으로)**: (1) 스크래치 하네스로 최초 오픈 시 textarea가 정확히 빈 문자열인지, placeholder 텍스트가 맞는지, 커스텀 2줄을 입력 후 저장하면 그 값이 저장되는지, 재오픈 시 그 커스텀 값이 그대로 보이는지("기본값을 물려받은 것"이 아니므로), "기본값으로"를 누르면 다시 빈 칸으로 돌아가는지 5단계 전부 확인. (2) **진짜 `main.js`를 그대로 띄워서(`require('main.js')`) 실제 설정 창에 커스텀 대사 2개를 입력→저장하고, `fireReminder()`를 임시 디버그 IPC로 10번 반복 호출해 펫 창의 말풍선 텍스트를 매번 읽어봄 — 10번 전부 그 커스텀 2개 중 하나만 나오고 기본 12개 중 어느 것도 안 나옴을 확인**(검증 중엔 실제 `%APPDATA%\focus-pet\settings.json`을 건드리게 되므로 사전에 백업해뒀다가 검증 후 원래 내용으로 복원, 임시 디버그 훅(`FOCUSPET_DEBUG_MESSAGES`)도 검증 직후 `main.js`에서 제거 — `git diff main.js`로 흔적 없음 재확인).

**2. (신규) 펫 창 모서리 자석 스냅**: 드래그해서 화면 네 모서리 근처에 놓으면 그 모서리로 부드럽게 미끄러져 붙는 기능 추가.
  - **구현**: 기존 `getPetPosition()`(설정의 "펫 위치" 4개 좌표를 계산하던 함수)에서 좌표 계산 부분만 `getCornerPositions(display)`로 분리해 재사용 가능하게 만듦 — "펫 위치" 드롭다운이 스냅되는 지점과 드래그 스냅 지점이 정확히 같은 좌표(마진 24px 포함)를 쓰게 되어 둘 사이에 어긋남이 생길 여지가 없음. `drag-end` 핸들러(기존엔 스프링-팔로우 타이머만 멈추고 끝이었음)에 `findNearestCornerSnap(win, display)`를 추가 — 4개 화면 모서리 각각에 대해 "그 모서리와 대응하는 윈도우 자신의 모서리"(예: top-left 쪽은 창의 top-left 점, bottom-right 쪽은 창의 (x+width, y+height) 점) 사이의 유클리드 거리를 계산하고, 가장 가까운 모서리가 반경(`CORNER_SNAP_RADIUS=50px`, 요청받은 40~60px 범위의 중간값) 이내면 그 모서리의 목표 좌표를 반환 — 창의 top-left 좌표만 목표 좌표와 비교하지 않은 이유는, 예를 들어 우상단 스냅 판정에서 창의 top-left를 화면 top-right와 비교하면 창 너비만큼 항상 어긋난 값이 나와 판정 자체가 무의미해지기 때문(창의 "우상단에 해당하는 점"과 화면 우상단을 비교해야 함). 반경 이내면 `animateCornerSnap()`이 기존 드래그 스프링-팔로우와 같은 "고정 width/height로 매 틱 `setBounds()` 재적용" 패턴(위 드래그 관련 CLAUDE.md 항목 참고 — `setPosition()` 대신 `setBounds()`를 쓰는 이유가 이 창 특유의 창 높이 계속 자라나는 버그를 우회하기 위함이었던 것과 동일한 이유로 여기서도 그대로 적용)을 16ms 간격으로 반복하되, easeOutCubic(`1-(1-t)^3`)으로 목표 지점까지 `CORNER_SNAP_DURATION_MS=180ms`(요청받은 0.15~0.2초의 중간값)에 걸쳐 부드럽게 감속하며 도착 — "순간이동이 아니라 짧게 미끄러지듯"이라는 요청 그대로. `drag-start`가 시작될 때 혹시 이전 스냅 애니메이션이 아직 도는 중이면 취소하도록 방어 코드도 추가(같은 창에 두 타이머가 동시에 `setBounds()`를 호출하는 충돌 방지). `settings.position`/`settings.json`은 전혀 안 건드림 — 순수하게 "이번에 드래그로 어디 놓았는지"에 대한 즉흥적 반응일 뿐, "펫 위치" 설정 자체를 바꾸는 게 아님.
  - **검증 (수치)**: 실제 `main.js`를 그대로 띄워서(`require`) `drag-end` IPC를 직접 호출하는 방식으로 4개 모서리 전부(15px 이내에서 드롭) → 정확히 목표 좌표로 수렴(허용 오차 3px 이내 — 이 디스플레이 자체가 정수가 아닌 배율을 쓰는지 프레임리스 창의 실측 크기가 `PET_SIZE` 상수(340×210)보다 살짝 크게(343×213) 보고되는 걸 발견, `setBounds()`/`getBounds()`가 논리 픽셀 기준으로 반올림되며 1~2px 정도의 잔차가 남는 것도 확인 — 둘 다 이번 기능과 무관한 이 디스플레이의 기존 특성), 화면 중앙에 드롭 → 스냅 없이 그 자리 그대로, 반경(50px) 바로 밖(약 68px 거리)에 드롭 → 역시 스냅 없이 그 자리 그대로, 이렇게 6가지 케이스 전부 기대대로 동작.
    - **1차 시도에서 나온 "실패"는 검증 스크립트 자체의 버그였음**: 처음엔 목표 좌표를 창의 **실측** 너비/높이(343×213)로 계산해서 4개 모서리 중 3개가 어긋나 보였는데, 실제로는 앱 자신의 `getCornerPositions()`가 `PET_SIZE` **상수**(340×210)로 계산한다는 걸 놓치고 검증 스크립트가 다른 값으로 "정답"을 계산했던 것 — 상수로 다시 계산하니 정확히 일치. 이 프로젝트에서 반복돼온 "이상해 보이는 결과는 앱 버그로 단정하기 전에 검증 스크립트 자체부터 의심"이라는 습관이 이번에도 유효했음.
  - **검증 (시각)**: `desktopCapturer`의 `screen` 소스로 실제 데스크톱 전체를 캡처해 좌상단/우하단 스냅 후 펫이 실제로 그 모서리에 꼭 붙어있는 모습, 화면 중앙에 놓았을 때 모서리와 무관하게 그 자리에 그대로 있는 모습까지 3장의 스크린샷으로 육안 확인.

**전체 회귀 확인**: `npm run test:invariants`(120만 프레임) 위반 0건(둘 다 `animal-engine.js`/`voxel-engine.js`와 무관). 실제 앱을 여러 차례 재기동해 확인 — 콘솔 에러 없이 정상 구동. 이번 라운드에서 `main.js`에 추가했던 두 임시 디버그 훅(`FOCUSPET_DEBUG_MESSAGES` 게이트의 자동 설정창 오픈 + `debug-fire-reminder` IPC)은 검증 직후 전부 제거, `git diff main.js`로 최종적으로 코드에 남은 변경이 정확히 모서리 스냅 기능(`getCornerPositions`/`findNearestCornerSnap`/`animateCornerSnap`/`drag-start`·`drag-end` 수정)뿐임을 재확인.

### 모서리 자석 스냅 재조사 — 지난 검증이 실제로는 재현 불가능한 상태를 테스트하고 있었음, 진짜 원인 발견 + 수정
사용자가 "실제로 써보니 전혀 안 먹힌다"고 재확인 요청 — 지난 라운드의 검증(`ipcMain.emit('drag-end', ...)`을 직접 호출하기 전에 `win.setBounds()`로 창을 모서리 근처에 직접 놓는 방식)이 **진짜 드래그로는 절대 나올 수 없는 창 위치를 인위적으로 만들어놓고 테스트한 것**이었음이 이번에 밝혀짐 — 사용자가 지목한 3가지 의심(좌표계 불일치/mouseup 경로 우회/테스트 하네스 전용 검증) 중 정확히 세 번째가 맞았고, 그 안에 숨어있던 원인은 사용자가 제시한 첫 번째 의심(좌표계)과도 맞닿아 있었음.

**진짜 원인**: 실제 마우스 하드웨어 대신 `webContents.sendInputEvent()`(실제 OS 마우스 이벤트와 같은 네이티브 입력 파이프라인을 타는 합성 이벤트 — JS 함수를 직접 호출하는 것과 다름)로 진짜 `mousedown`을 쏘고, PowerShell로 **실제 OS 커서**를 여러 단계에 걸쳐 화면 모서리 쪽으로 이동시켜(실제 경과 시간 동안 `main.js`의 진짜 16ms 드래그 폴링이 진짜 `screen.getCursorScreenPoint()` 값을 읽게 함) `mouseup`까지 재현해보니, 드래그 종료 시점 창의 실제 좌표가 예상과 크게 다름을 발견 — `#pet-wrap`(실제 보이는 96×96 스프라이트)이 창(340×210, 예전 패트롤 이동 범위 때문에 넓게 잡혔다가 패트롤 자체는 사라진 뒤에도 안 줄어든 크기)의 **한가운데 안쪽에** 떠 있고, 창 자신의 가장자리와는 전혀 안 붙어있음. 그래서 사용자가 화면 모서리에 스프라이트를 딱 붙여놔도 **창의 실제 raw 모서리는 여전히 화면 모서리에서 120~190px(창 크기의 절반)나 떨어져 있음** — `findNearestCornerSnap()`이 그동안 비교해온 게 바로 이 "창의 raw 모서리"였으므로, 반경 50px는 실제 드래그 상황에서는 사실상 영원히 도달 불가능한 조건이었음. 반면 지난 검증은 `win.setBounds()`로 **창의 raw 모서리 자체**를 직접 화면 모서리 근처에 갖다 놓고 테스트했기 때문에(스프라이트가 어디 있는지는 전혀 상관없이), 그 좁은 조건 안에서만 성공했던 것 — 실제 드래그로는 결코 만들어낼 수 없는 인위적 상태를 검증하고 있었던 셈.
  - **고침**: `preload.js`의 `dragEnd()`가 이제 인자(스프라이트 rect)를 받아 그대로 IPC에 실어 보내도록 변경, `pet.js`의 `mouseup` 핸들러가 `dragEnd()`를 호출할 때 `petWrapEl.getBoundingClientRect()`(창 로컬 좌표 기준, 실제 지금 보이는 스프라이트의 위치)를 같이 실어 보냄. `main.js`의 `findNearestCornerSnap(win, display, spriteRect)`가 이제 창의 raw 모서리 대신 **`spriteRect`를 창의 screen 좌표로 환산한 스프라이트 자신의 4개 모서리**를 화면 모서리와 비교 — "드래그해서 화면 모서리 근처로 가져가면"이라는 원래 요청을 문자 그대로, 사용자 눈에 보이는 그 스프라이트 기준으로 판정하게 됨. 창을 실제로 옮기는 스냅 목표 좌표 자체(`getCornerPositions()`)는 안 건드림 — "펫 위치" 설정과 동일한 좌표를 계속 재사용.
  - **사용자가 제시한 3가지 의심에 대한 답**: (1) 좌표계 — 근본 원인이 정확히 이것의 변종이었음: `screen` 절대좌표와 `window` 상대좌표를 혼동한 건 아니었지만, "창(window) 전체의 경계"와 "실제로 보이는 스프라이트의 경계"라는 **서로 다른 두 사각형**을 혼동하고 있었음. (2) mouseup 경로 — `drag-end` 핸들러는 `main.js`에 단 한 곳뿐이고 `pet.js`의 `mouseup`도 무조건 `dragEnd()`를 호출하고 있어서, 우회 경로는 없었음(코드 검색으로 확인). (3) 테스트 하네스 문제 — 정확히 이것이었음, 위에서 설명한 그대로.

**검증 (진짜 물리적 드래그와 최대한 동일한 경로로, 4개 모서리 전부 + 모서리 아닌 곳)**: `sendInputEvent`로 실제 `mousedown`/`mouseup`을 쏘고, PowerShell(`[System.Windows.Forms.Cursor]::Position`)로 실제 OS 커서를 실제 경과 시간에 걸쳐 이동시키는 방식 — 실제 `main.js`(`require`로 그대로 로드)의 진짜 `drag-start`/`drag-end` IPC, 진짜 16ms 스프링-팔로우 폴링, 진짜 corner-snap 판정/애니메이션을 전부 통과하는 경로. 4개 모서리 전부 독립된 새 프로세스(앱을 매번 새로 켜서 "사용자의 첫 드래그"와 동일한 조건)에서 검증 — 전부 `getCornerPositions()`가 계산하는 정확한 목표 좌표로(오차 5px 이내) 수렴 확인. 화면 중앙에서 짧게 드래그한 경우는 4개 모서리 좌표 전부에서 최소 461px 이상 떨어진 곳에 그대로 남아(스냅 안 됨) 정상 작동 확인. `desktopCapturer`로 실제 데스크톱을 캡처해 우하단으로 드래그한 펫이 실제로 그 모서리에 붙어있는 모습도 스크린샷으로 재확인.
  - **검증 중 발견한, 별개의 사전 존재 특성 (정직하게 기록, 이번 요청 범위 밖이라 손 안 댐)**: 같은 세션 안에서 드래그를 연달아 여러 번 재현하려던 중, 두 번째 이후의 드래그가 이따금 아예 시작되지 않는(mousedown이 무시되는) 현상을 발견 — 원인을 추적해보니 스프라이트의 alpha 클릭 통과 히트테스트(`hitState`)가 그 순간의 idle 포즈에 따라 "그리드 중앙"이 우연히 투명한 픽셀일 수 있어서(3D 복셀 모델의 자세가 계속 미세하게 바뀜, 위 "클릭 통과 히트테스트" 항목에 이미 문서화된 `HIT_RADIUS=2` 취약점과 같은 종류) 생기는 것으로 확인 — 실제 사람은 눈으로 보고 "지금 보이는 스프라이트 부분"에 맞춰 커서를 대지만, 자동화 스크립트가 고정된 기하학적 중심점을 맹목적으로 노리면 가끔 빗나갈 수 있다는, 클릭 통과 시스템 자체의 사전부터 있던 성질이었음(코너 스냅 기능과 무관 — 코너 스냅을 아예 안 쓰는 일반 드래그에도 똑같이 적용되는 특성). 이번 요청(모서리 스냅 재현/수정)과는 별개의 사안이라 코드는 안 건드리고, 검증 스크립트만 "화면에 실제로 보이는 스프라이트 근처 여러 지점을 확인해서 그 순간 불투명한 지점을 찾아 거기를 클릭"하도록 만들어 우회 — 이 자체가 "사람은 눈으로 보고 맞춰 클릭한다"는 실제 사용 방식과 더 가까운 검증 방법이기도 함.

**전체 회귀 확인**: `npm run test:invariants`(120만 프레임) 위반 0건. 검증에 썼던 임시 디버그 훅(`pet.js`에 추가했던 `window.__dragDebug`/`__rectDebug`/`__pixelDebug`)은 확인 후 전부 제거 — `git diff`로 `main.js`/`preload.js`/`pet.js`에 남은 변경이 정확히 스프라이트 rect를 스냅 판정에 쓰도록 고친 부분뿐임을 재확인. 실제 앱(`npx electron .`)을 재기동해 콘솔 에러 없이 정상 구동 확인.

### 펫 창 크기를 실제 필요한 만큼으로 축소 (340x210 → 230x200) + 모서리 자석 스냅 "반대 방향으로 튕겨나감" 버그 근본 수정
사용자가 두 가지를 요청 — (1) 340x210 창 크기 자체가 예전에 삭제된 자율 patrol 기능 시절의 유물이니 "실제 필요한 만큼만"(96x96 스프라이트 + 말풍선 뜰 공간 + 최소 여백)으로 줄여서, 창 경계와 스프라이트 경계 사이의 괴리 자체를 구조적으로 줄이면 좌표 관련 버그가 덜 날 것이라는 근본 대책. (2) 위 라운드에서 고쳤다던 모서리 자석 스냅이 여전히 안 되는데, 이번엔 "안 붙는다" 수준이 아니라 "모서리 쪽으로 이동시키면 오히려 반대 방향으로 밀려난다(튕겨낸다)"는 더 구체적인 증상 — 사용자가 부호 반전이나 클램프 로직과의 충돌을 직접 의심함. 두 요청 다 좌표 문제라 순서대로(창 크기부터) 진행.

**1. 창 크기 축소**: 새 크기를 감으로 정하지 않고 실측 — 스크래치 스크립트로 실제 340x210 창을 띄워 `settingsStore.js`의 12개 기본 알림 메시지 전부를 실제 `#bubble` CSS로 렌더링해 `getBoundingClientRect()`를 읽어봄 — 가장 긴(3줄) 메시지가 창 바닥에서 169.6px 위까지 올라감. **높이 200**: 그 169.6px 위에 ~30px 여유(그보다 살짝 긴 커스텀 메시지에 대비, 완전히 임의의 긴 메시지까지는 어떤 고정 크기 창도 못 막음 - 340x210이던 이전에도 마찬가지였던 한계)를 더한 값. **너비 230**: 스프라이트 자체는 96px면 충분하지만 `#bubble`의 CSS `max-width`가 190px이고 `positionBubble()`의 `BUBBLE_SAFE_MARGIN`(20px, 좌우 각각)이 항상 화면 안에 여유를 두려면 190+20+20=230이 정확히 그 클램프가 항상 작동할 수 있는 최소 너비. `main.js`의 `PET_SIZE`를 `{340,210}`→`{230,200}`으로, `pet.js`의 4곳(`x` 초기값, `bounds()`, `positionBubble()`, `updateCursorHint()`)에 있던 fallback 상수(`||340`/`||210`)를 `||230`/`||200`으로 맞춤 — `pet.css`는 `#stage{width:100%;height:100%}`가 창 크기에 자동으로 맞춰지는 구조라 전혀 안 건드림. `WRAP_WIDTH`(96)/`MARGIN`(16) 등 스프라이트 자체 크기 상수는 그대로.
  - **드래그/클릭 통과 로직 창 크기 의존성 점검 (사용자가 명시적으로 요청)**: 드래그 스프링-팔로우(`main.js`의 `drag-start` 폴링)는 `screen.getCursorScreenPoint()`(절대 화면 좌표)와 드래그 시작 시점의 grab offset만 쓰고, `setBounds()`에 넘기는 `width`/`height`도 `win.getBounds()`로 그때그때 읽은 실제 값이라 `PET_SIZE`를 코드에 하드코딩해 참조하는 곳이 없음 - 창 크기와 무관하게 그대로 작동. 클릭 통과 히트테스트(`sampleHit`)도 `canvas.getBoundingClientRect()`/`canvas.width`/`canvas.height`(캔버스 자체의 크기, 96x96 백킹 픽셀 고정)만 사용해 창 크기와 무관 - 96x96 캔버스는 안 바뀌었으므로 그대로 안전. 결론: 두 로직 다 애초에 `PET_SIZE`를 직접 참조하지 않는 구조라 크기 변경의 영향이 없었음(그래서 코드 수정은 안 하고 확인만).
  - **검증**: 실제 `main.js`를 그대로 띄워 새 창이 정확히 235x200(230+DPI 스케일 오차 5px, 이 개발 환경에서 프레임리스 창이 지정값보다 몇 px 크게 보고되는 기존에 이미 알려진 특성 - 버그 아님)으로 생성되는지, `#pet-wrap`이 그 안에 잘 들어앉는지, 가장 긴 실제 알림 메시지를 띄웠을 때 말풍선이 상/좌/우 어느 쪽으로도 안 잘리는지(`top`/`left`/`right` 좌표를 직접 읽어 확인) 스크린샷과 함께 확인 - 스프라이트가 이전처럼 텅 빈 사각형 한가운데 파묻힌 모습이 아니라 창을 거의 꽉 채우는 자연스러운 비율로 보임.

**2. 모서리 자석 스냅 "반대 방향 튕겨남" — 실제로는 완전히 다른, 훨씬 단순한 버그였음**: 창을 줄이고 나니(사용자 예상대로) 좌표가 훨씬 단순해져서 문제를 파고들기 쉬워짐 — 그런데 실제로 찾은 원인은 사용자가 의심한 "부호 반전"이나 "클램프 충돌"이 아니라, 이번 라운드에서 손댄 코드 자체의 **아주 단순한 오탈자성 실수**였음. 정직하게 기록하면 이번 조사는 두 단계로 나뉨 — (A) 설계 자체의 진짜 결함(사용자가 예상한 방향과 비슷한 종류) 하나와, (B) 그 결함을 고치다가 직접 만들어 넣은 새 버그 하나, 총 두 번의 수정을 거쳐서야 실제로 동작함.

  - **(A) 설계 결함 — 트리거와 목표가 서로 다른 좌표계를 쓰고 있었음**: `findNearestCornerSnap`은 "스냅해야 하는가"를 스프라이트 자신의 모서리(`spriteRect`, 창 안에서 96x96 스프라이트가 실제로 있는 자리)를 화면 모서리와 비교해서 판정하는데(정확함), 정작 스냅 **목표 좌표**는 `getCornerPositions()`(펫 위치 설정용 - 창 자신의 모서리를 화면에서 24px 마진에 두는 함수)를 그대로 재사용하고 있었음. 스프라이트는 창 안에서 안쪽으로 들어가 있어서(230x200 창 기준 좌우 ~65-70px, 하단 14px, `bottom:14px` CSS 앵커) 창을 "24px 마진"에 두면 스프라이트는 그보다 훨씬 더(65~89px) 화면 모서리에서 떨어진 곳에 놓이게 됨 — 즉 스프라이트가 트리거 반경(50px) 안까지 들어와서 스냅이 발동하는 바로 그 순간, 목표는 그보다 더 먼 곳(65~89px)을 가리키고 있어서, 스냅이 걸릴 때마다 오히려 스프라이트를 화면 모서리에서 **더 멀리** 밀어내는 모양이 됨 — 정확히 사용자가 본 "모서리로 이동시키면 반대 방향으로 튕겨난다"는 증상. **고침**: `getCornerPositions()`를 그대로 쓰는 대신, `findNearestCornerSnap`이 `spriteRect`로부터 직접 "스프라이트의 해당 모서리가 화면에서 정확히 24px(`CORNER_SNAP_MARGIN`, 새 상수) 떨어지려면 창 좌표가 얼마여야 하는가"를 역산 — 트리거와 목표가 이제 같은(스프라이트 기준) 좌표계를 쓰므로, 트리거된 상태(반경 50px 이내)에서 목표(24px)로 가는 건 항상 "마저 들어가는" 방향의 작은 이동이지 "튕겨나가는" 큰 역방향 이동일 수 없음. "펫 위치" 드롭다운 설정 자체(`getCornerPositions`/`getPetPosition`)는 안 건드림 — 그쪽은 트리거 반경이라는 개념이 아예 없어서 이 문제가 발생할 수 없는 별개의 기능.
  - **(B) 새로 만들어 넣은 버그 — `best.dist`를 실제로는 한 번도 저장 안 함**: (A)를 고친 직후 실제 드래그로 재현해보니 스냅이 여전히 **전혀 발동을 안 함**(반대 방향 튕김도, 정상 스냅도 아닌, 그냥 무반응) — 원인은 후보 목록을 순회하며 최솟값을 고르는 코드에 있었음: `if (!best || dist < best.dist) best = c;`처럼 매번 후보 객체 `c`를 그대로 `best`에 대입했는데, `c` 자체에는 `dist` 필드가 없어서(따로 계산만 하고 객체에 붙이는 걸 빠뜨림) `best.dist`는 항상 `undefined`로 남음 — 두 번째 후보부터는 `dist < undefined`가 항상 `false`라 첫 후보에서 `best`가 고정되고, 최종적으로 `best.dist <= CORNER_SNAP_RADIUS`(`undefined <= 50`)도 항상 `false`라 **함수가 무조건 `null`을 반환**하고 있었음 — (A)의 수정 여부와 무관하게 존재했을 문제였는데, (A)를 고치는 과정에서 새로 짠 코드에 처음 심어진 실수. **발견 경위**: 실제 `drag-end` IPC 핸들러에 임시로 `console.log`를 심어(스프라이트 거리를 직접 손으로 재계산해보니 4px로 트리거 조건을 명백히 만족하는데도) `findNearestCornerSnap`이 `null`을 반환하는 걸 로그로 직접 확인 → 코드를 다시 읽다가 `best.dist`가 애초에 안 채워지고 있다는 걸 발견. **고침**: `best = { ...c, dist };`로 한 글자 수정.
  - **검증 (실제 sendInputEvent + 실제 OS 커서 드래그, 4개 모서리 전부, 반복)**: main.js에 잠깐 추가했던 로깅으로 먼저 (B)가 진짜 원인임을 수치로 확인(트리거 거리 4px인데도 `target: null` → 한 글자 수정 후 같은 조건에서 `target: {x,y}` 정상 산출, 스프라이트가 실측 마진 25px/23px로 정확히 안착) 한 뒤, 로그를 지우고 본 검증 진행. 4개 모서리 × 2번씩(스프라이트가 트리거 반경에 거의 붙어있는 "flush" 케이스와 반경 안쪽 다른 거리의 "near" 케이스) = 8회 실제 드래그(`sendInputEvent`로 진짜 `mousedown`/`mouseup`, PowerShell `[System.Windows.Forms.Cursor]::Position`으로 진짜 OS 커서를 매 프레임 이동 - 이전 라운드에서 확립된, JS 함수 직접 호출이 아닌 진짜 입력 파이프라인 경로) 전부 성공 — 8/8 전부 스프라이트가 화면 모서리에서 정확히 23~26px(의도한 24px 마진과 거의 정확히 일치) 떨어진 곳에 안착, 반경(50px)을 넘어서는 역방향 이동은 단 한 번도 없음. 이후 별도로 4개 모서리 전부(desktopCapturer로 실제 데스크톱을 캡처, PowerShell `CopyFromScreen`은 이 환경에서 GDI/DWM 합성 문제로 자기 자신의 콘솔 창을 찍는 등 신뢰할 수 없어서 대신 사용 - 이전 라운드에 이미 검증된 방법) 스크린샷으로 스프라이트가 실제 화면 모서리에 자연스럽게 붙어있는 모습을 육안으로도 재확인 — 반경 밖(약 30-40px 더 먼 지점)에서 놓았을 때는 스냅이 발동하지 않고(트리거 조건 자체가 정상적으로 걸러짐) 그 자리 그대로 남는 것도 함께 확인.
  - **정직하게 기록**: 검증 과정에서 클릭 통과 히트테스트의 알파 샘플링이 idle 포즈 미세 변화 때문에 가끔(4번 중 1번 정도) `mousedown`을 못 잡는 걸 관찰 - 위 "모서리 자석 스냅 재조사" 항목에서 이미 발견/기록된 것과 정확히 같은, 클릭 통과 시스템 자체의 사전부터 있던 성질(코너 스냅과 무관)이라 재확인만 하고 코드는 안 건드림.

**전체 회귀 확인**: `npm run test:invariants`(120만 프레임) 위반 0건(이 로직은 `animal-engine.js`/`voxel-engine.js`와 무관). 검증에 썼던 임시 디버그 훅(`main.js`의 `[TEST]` 로그, `pet.js`의 `window.__sampleHitForTest__`/`[TEST]` 로그)은 확인 후 전부 제거 — `git diff`로 `main.js`/`pet.js`에 남은 변경이 창 크기 축소와 corner-snap 좌표계/버그 수정뿐임을 재확인. 실제 앱(`npx electron .`)을 재기동해 확인 — 이 환경에서 항상 나타나는 Electron 자체의 CSP 개발 경고(코드와 무관, dev 모드 전용) 외에는 콘솔 에러 없이 정상 구동.

### 창 크기가 위치마다 달라 보이는 문제 + 스프라이트 좌우 비대칭 — 둘 다 이전 라운드의 부작용이었음
사용자가 스크린샷 두 장(`assets/reference/window-size-center.png`, `window-size-corner-snapped.png`)을 첨부하며 "중앙에 있을 때랑 모서리에 스냅됐을 때 실제 창 크기/비율이 달라 보인다"고 재확인 요청 + "스프라이트가 창 안에서 살짝 한쪽으로 치우쳐 있다"는 두 번째 문제도 같이 제기. 두 스크린샷의 PNG 픽셀 크기 자체(322×283 vs 237×282)는 폭만 크게 다르고 높이는 거의 같아서 어느 한 DPI 배율로는 설명이 안 되는 수치였음 — 사용자가 어떤 방식으로 캡처했는지(전체화면에서 영역을 수동으로 지정했는지, 창 자체를 정밀하게 캡처했는지)를 알 수 없어 이 두 PNG의 정확한 픽셀 값 자체를 근거로 삼진 않고, 대신 실제 앱을 직접 구동해 `getBounds()`/`getBoundingClientRect()`로 진짜 창 크기·스프라이트 위치를 직접 재는 방식으로 처음부터 다시 조사함.

**원인 1 — 지난 corner-snap 수정 라운드 자체가 남긴, 그 라운드에선 못 잡았던 크리프**: 이전 세션에 저장해둔 실제 드래그 로그(8회 연속 드래그, `drag-end` 시점 `win.getBounds()`)를 다시 들여다보니 `width`가 237→241→244→248→251→255→257→260으로, `height`가 201→205→...→225로 **드래그할 때마다 꾸준히 커지고 있었음** — 예전에 이미 문서화된 "창 크기가 계속 자라나는 버그"(`setPosition()` 대신 `setBounds()`로 우회했던 그 버그)와 정확히 같은 증상 패턴인데, 그 우회 자체가 불완전했다는 뜻. 원인: `drag-start`의 스프링-팔로우 poll과 `animateCornerSnap` 둘 다 `width`/`height`를 매 틱 고정해서 넘기긴 하지만, 그 고정값 자체를 **`PET_SIZE` 상수가 아니라 그 순간의 `win.getBounds()`에서 한 번 읽어와** 썼음 — 이 창 특유의(예전부터 원인 불명으로 남아있던) OS/Electron 레벨의 미세한 크기 튐이 단일 드래그/스냅 세션 안에서 완전히 0으로 억제되진 않는데, 그 세션이 끝나면서 살짝 커진 값이 그대로 다음 드래그가 "고정할" 기준값으로 다시 읽혀서, 세션을 거듭할수록 랫칫처럼 계속 불어났던 것. **고침**: `drag-start`/`animateCornerSnap`/`save-settings`(여기는 아예 `setPosition()`을 쓰고 있어서 이번에 `setBounds()`로 통일) 세 곳 전부 `win.getBounds()`에서 읽은 값 대신 `PET_SIZE.width`/`PET_SIZE.height`를 직접 참조하도록 변경 — 이제 창 크기를 건드리는 모든 경로가 정확히 같은 하나의 상수를 매 틱 재적용하므로, OS가 한 틱에서 몇 px 튀어도 바로 다음 틱에 다시 정정되고, 세션을 넘어 누적될 방법 자체가 없어짐(사용자가 요청한 "한 곳에서만 정의하고 모든 경로가 참조" 그대로).
  - **부수 발견 — 생성 시점과 런타임 setBounds 시점의 크기가 서로 다름**: 위 수정을 적용하고 재검증하니 창 생성 직후(드래그 한 번도 없이) 측정한 크기는 235×200인데, 드래그를 한 번이라도 거치면 231×201로 안정되는 걸 발견 — `new BrowserWindow({width,height,...})` 생성자와 이후의 `setBounds()` 호출이 이 플랫폼에서 서로 다른 실제 크기로 정착하는 것으로 보임(어느 쪽이든 요청값 230/200과 몇 px 정도 어긋나는 것 자체는 DPI 반올림으로 이해할 수 있지만, 두 경로가 **서로 다른** 값으로 어긋나는 건 "창이 위치/시점에 따라 달라 보인다"는 이번 리포트의 정의 그 자체라 방치하지 않음). `createPetWindow()`에서 생성 직후 곧바로 `petWindow.setBounds({x,y,width:PET_SIZE.width,height:PET_SIZE.height})`를 한 번 더 호출해, 다른 모든 크기 변경 경로가 쓰는 것과 동일한 코드 경로를 첫 프레임부터 타도록 통일 — 이후 첫 프레임부터 231×201로 일관됨.

**원인 2 — 스프라이트가 창 안에서 좌우로 살짝 치우침, 실제로는 원인 1의 직접적인 결과였음**: `#pet-wrap`의 가로 위치(`x`, pet.js)는 "이동 정책 변경" 이후 patrol이 완전히 죽으면서 **pet.js 로드 시점에 딱 한 번만** `(stageEl.clientWidth - WRAP_WIDTH)/2`로 계산되고 그 이후로는 절대 재계산되지 않는 값이었음 — 로드 시점엔 `stageEl.clientWidth`가 234였는데(원인 1의 "생성 시점 235×200" 크기에서 나온 CSS 폭), 이후 첫 드래그가 끝나자마자 창의 실제 폭이 231로 바뀌면서(원인 1) 스프라이트의 절대 픽셀 위치(`x`=69)는 그대로 남아있는데 담고 있는 캔버스 폭만 3px 줄어들어, 그 3px 전부가 오른쪽 여백에서만 빠짐 — 실측 결과 `leftMargin:69` vs `rightMargin:66`으로 정확히 3px 비대칭이 드래그 이후 모든 상태(중앙/모서리 4개 전부)에서 재현됨, 앱을 켠 직후(드래그 전) 단 한 번만 정확히 대칭(69/69)이었음 — 사용자가 본 "치우침"이 우연이나 별개 버그가 아니라 원인 1과 정확히 같은 근본 원인(창 크기가 시점마다 달랐던 것)의 또 다른 증상이었다는 뜻. **고침**: `x`가 마지막으로 중앙 정렬됐던 폭(`xCenteredForWidth`, 신규 변수)을 기억해뒀다가, `tick()`이 매 프레임(요청받은 다른 이동 로직과 마찬가지로 이미 매 프레임 도는 루프 안) 살아있는 `stageEl.clientWidth`와 비교해서 달라졌으면 그 자리에서 다시 중앙으로 재계산 — 이 파일의 다른 poll들(`startAlwaysOnTopPoll` 등)이 이미 쓰고 있는 "원인을 낱낱이 추적하는 대신 몇 틱마다 통째로 재적용해서 자연 복구시킨다"는 철학을 그대로 재사용. 폭이 실제로 안 바뀌는 절대다수의 프레임에서는 비교 한 번만 하고 끝나는 사실상 공짜 비용이고, "나중에 커서 쪽으로 다가가기 같은 진짜 이동 기능이 부활할 때를 대비해 죽은 채로 남겨둔" `advance` 기반 이동 블록과도 안 부딪힘(그 블록이 실제로 `x`를 이동시키는 중에 폭까지 우연히 바뀌는 경우에만 이 보정이 끼어드는데, 그 정도는 "창이 갑자기 리사이즈됐으면 다시 중앙 쪽으로 맞추는 게 낫다"는 합리적인 기본 동작으로 판단).

**검증 (실제 앱, 연속된 많은 실제 드래그로 스트레스 테스트)**: 이전에 크리프를 처음 노출시켰던 것과 같은 "많은 드래그를 연달아" 조건을 그대로 재현 — 실제 `main.js`를 그대로 띄우고 `sendInputEvent`+실제 OS 커서로 초기 실행 → 화면 중앙으로 드래그(코너 스냅 반경 밖) → 4개 모서리 각각 2번씩(총 8회, 예전 크리프를 처음 드러냈던 것과 동일한 반복 횟수) → 다시 중앙으로, 총 12개 지점에서 매번 `win.getBounds()`(창 크기)와 `#pet-wrap`의 `getBoundingClientRect()`(좌우 여백)를 동시에 측정. 결과: **12개 지점 전부** 너비 231~232px·높이 200~201px 범위 안(추세 없이 안정, 예전의 237→260 같은 증가 패턴 완전히 사라짐), **12개 지점 전부** `leftMargin === rightMargin`(67.5px, 소수점까지 정확히 일치, 크리프가 해소되면서 폭 자체도 234가 아니라 231로 한 번 더 안정된 값 기준) — 완전 대칭 확인. `desktopCapturer` 대신 이번엔 창 자체의 `capturePage()`(OS 캡처 방식의 모호함 없이 브라우저 콘텐츠 영역만 정확히 캡처, 이번 검증엔 이쪽이 더 적합)로 초기 실행/드래그 중/중앙/4개 모서리 스냅 후 각각 스크린샷을 찍어 육안으로도 확인 — 창의 비율/여백이 어느 캡처에서나 시각적으로 동일하고, 고양이가 항상 좌우 중앙에 놓여 있음. `npm run test:invariants`(120만 프레임) 위반 0건.
  - **정직하게 기록**: 이번 검증 중에도 클릭 통과 히트테스트가 몇몇 시도에서 `mousedown`을 못 잡는 걸 관찰(재시도 로직으로 우회) — 위 두 라운드에서 이미 발견/기록된 것과 같은, 이 기능과 무관한 사전부터 있던 특성이라 재확인만 하고 코드는 안 건드림.

**전체 회귀 확인**: `npm run test:invariants` 위반 0건. 검증에 썼던 임시 디버그 훅(`pet.js`의 `window.__sampleHitForTest__`)은 확인 후 제거 — `git diff`로 `main.js`/`pet.js`에 남은 변경이 이번에 설명한 세 곳(창 크기 통일 3곳, 스프라이트 재중앙 로직)뿐임을 재확인. 실제 앱(`npx electron .`)을 재기동해 확인 — 기존과 동일한 CSP 개발 경고 외에는 콘솔 에러 없이 정상 구동.

### 모서리에서 말풍선이 잘리는 문제 — 창을 일시적으로 넓혔다 좁히는 방식으로 해결 (스프라이트 위치는 고정)
사용자가 "스프라이트가 모서리에 붙으면 좁아진 여백 때문에 말풍선이 매번 잘린다"고 요청 — 대책으로 (1) 평소엔 지금처럼 작은 창 유지, (2) 말풍선이 뜨는 순간에만 필요한 만큼 창을 일시적으로 넓히되 스프라이트가 있는 모서리 반대 방향(예: 우하단이면 좌/위)으로 넓혀서 스프라이트의 화면상 위치 자체는 전혀 안 움직이게, (3) 말풍선이 사라지면 다시 원래 크기로 — 를 제시하고, 대안으로 "말풍선을 별도 오버레이 창으로 분리"도 고려해보되 어느 쪽이 나을지는 판단해서 진행하라고 위임함.

**먼저 실측 — "지금도 정말 잘리는가"**: 코드 분석 결과 `#bubble`은 `#pet-wrap`의 자식이고(`index.html`), `#pet-wrap`은 `bottom:14px`로 창 자체의 바닥에 고정되므로, 말풍선이 창의 위쪽 경계에 클리핑되는지 여부는 **오직 (창 높이, 메시지 길이)에만 좌우되고 창이 화면의 어느 위치(중앙이든 모서리든)에 있는지와는 수학적으로 무관**하다는 걸 먼저 확인함(`#pet-wrap`의 창-로컬 좌표는 창의 화면상 위치와 독립적이므로) — 즉 "모서리라서" 여백이 좁아지는 게 아니라는 뜻. 실제로 12개 기본 메시지 전부를 현재 코드(직전 라운드에 창 크기/스프라이트 중앙 정렬을 고친 뒤)로 중앙·4개 모서리 전부에서 실제로 띄워 측정해보니 **전부 안 잘림**(가장 긴 메시지도 상단 여백 29.2px, 위치 무관하게 완전히 동일) — 사용자의 실제 `%APPDATA%\focus-pet\settings.json`에도 커스텀 `messages`가 없어(기본 12개 그대로 사용 중) 이 리포트는 십중팔구 창 크기가 위치마다 들쭉날쭉했던(그래서 어떤 순간엔 200px보다 작게 정착했을 수 있는) **직전 라운드 이전의 버그를 겪었던 경험**을 얘기한 것으로 보임 — 다만 그 버그가 이미 고쳐졌다고 해도, 사용자가 요청한 아키텍처(고정 크기 대신 필요할 때만 늘어나는 창) 자체는 "이 12개는 우연히 다 맞았을 뿐, 조금만 더 긴 커스텀 메시지가 오면 다시 잘린다"는 구조적 취약점을 근본적으로 없애는 더 견고한 설계라 판단 — 실측 결과와 무관하게 요청대로 구현 진행.

**오버레이 창 vs 메인 창 리사이즈 — 메인 창 리사이즈를 선택**: 오버레이 창 방식은 말풍선 위치/클램프/꼬리 방향(`--tail-shift`) 로직을 창 두 개 사이의 좌표 변환을 새로 설계해야 하고, z-order·always-on-top·click-through를 창 하나 더 관리해야 함 — 반면 메인 창을 일시적으로 키우는 방식은 이미 검증된 `setBounds()` 패턴(드래그/코너스냅이 이미 매 틱 쓰고 있는 것과 동일)과 `positionBubble()`의 기존 가로 클램프 로직을 거의 그대로 재사용 가능. 후자로 결정.

**설계**: `pet.js`의 `showBubble()`이 말풍선을 실제로 보여주기 직전에 `bubbleEl.offsetWidth`/`getBoundingClientRect().top`(둘 다 아직 `positionBubble()`을 호출하기 전이어도 정확함 — 가로 클램프가 결정하는 `left`와 무관한 값들이라)으로 "지금 창 크기로 이 메시지가 들어가는가"를 계산 — 가로는 `BUBBLE_SAFE_MARGIN(20)*2 + 말풍선 실제 폭`이 현재 `stageWidth`를 넘는 만큼(`extraWidth`), 세로는 새 `BUBBLE_TOP_SLACK(12, 그림자 blur 상단 여백만큼만 - 가로 마진 20보다 작게 잡음, 리사이즈 자체가 가로 클램프와 달리 눈에 보이는 동작이라 꼭 필요할 때만 트리거하려는 의도)를 상단 여백이 못 채우는 만큼(`extraHeight`)을 계산해서, 필요할 때만(둘 다 0이면 아무것도 안 함 - 실측대로면 지금 있는 12개 메시지는 전부 이 경로를 아예 안 탐) `resizeForBubble({extraWidth,extraHeight})`를 `main.js`에 IPC로 요청.
  - **가로는 이론상 거의 트리거 안 됨**: `PET_SIZE.width(230)`가 이미 `BUBBLE_SAFE_MARGIN*2(40) + 말풍선 CSS max-width(190)`와 정확히 같아서(직전 라운드 PET_SIZE 코멘트), `positionBubble()`의 기존 클램프가 창을 키우지 않고도 항상 이 폭 안에서 처리 가능 — 그래도 향후 이 CSS 상수들이 바뀔 경우에 대비해 가로도 똑같이 늘어나도록 일반적인 메커니즘으로 구현(사용자가 예시로 가로/세로 둘 다 언급하기도 했음).
  - **main.js의 `computeAnchoredBounds(baseBounds, targetWidth, targetHeight, workArea)`**: 가로는 "현재 창이 화면 왼쪽/오른쪽 중 어느 쪽에 더 가까운가"를 매번 계산해서(4개 코너에 한정되지 않고 자유 위치에서도 일반적으로 동작) 더 가까운 쪽 모서리를 고정하고 반대쪽으로 확장 — 우하단에 있으면 오른쪽이 가깝다고 판정되어 왼쪽으로 확장(사용자 예시와 일치). 세로는 **항상 아래쪽 모서리를 고정하고 위로만 확장** — 좌우와 달리 대칭적인 "더 가까운 쪽" 판단을 안 쓴 이유: 펫의 세로 위치는 `pet.js`의 JS 변수가 아니라 `pet.css`의 `bottom:14px`라는 순수 CSS 앵커라서(가로 `x`처럼 프레임마다 보정할 JS 오프셋이 없음), 창의 바닥 모서리가 화면상에서 안 움직이는 한 스프라이트도 자동으로 안 움직임 — 반대로 창의 위쪽을 고정하고 아래로 확장하면 바닥 모서리 자체가 움직여서 스프라이트가 딸려 내려감. 말풍선도 항상 펫 위로만 뜨므로("더 필요한 방향"이 항상 위쪽) 이 비대칭이 정확히 맞아떨어짐.
  - **`preGrowBounds`**: 말풍선 세션이 시작될 때(처음 커진 순간)의 창 좌표를 기억해뒀다가, 늘어난 상태에서 그대로 복원(재계산 없이 바이트 그대로 `setBounds()`) — 뒤이어 다른 길이의 메시지가 연달아 뜨는 경우(예: poke 반응이 리마인더 직후에 뜨는 등)도 항상 이 안정된 기준점에서부터 다시 계산해서 커졌다 줄었다를 반복해도 창이 계속 불어나지 않게 함.
  - **드래그와의 상호작용**: 드래그의 스프링-팔로우 poll은 매 틱 `PET_SIZE`(커진 크기 아님)로 `setBounds()`를 강제하므로, 말풍선이 뜬 채로 드래그를 시작하면 그 순간 창이 강제로 원래 크기로 줄어듦 — `pet.js`는 이 경우를 감지해(`dragging` 플래그) 애초에 `resizeForBubble` 요청 자체를 건너뛰어(드래그 poll과 싸워봐야 항상 짐) 리마인더 타이머가 드래그와 우연히 겹치는 드문 경우엔 그냥 "기존처럼 안 늘어난 채로 보여준다"는 예전 동작으로 자연히 폴백. `drag-start` 핸들러도 `preGrowBounds`를 초기화해서 나중에 엉뚱한(드래그로 이미 옮겨진) 위치로 복원 시도하는 걸 방지.
  - **스프라이트 위치 고정 (사용자의 핵심 요구사항)**: `resizeForBubble`이 반환하는 `windowDeltaX`(창의 왼쪽 모서리가 실제로 얼마나 움직였는지)만큼 `pet.js`가 자기 `x`(창-로컬 스프라이트 좌표)를 반대로 보정(`x -= windowDeltaX`) — 창이 왼쪽으로 10px 늘어났으면(`windowDeltaX=-10`) `x`를 10px 늘려서 절대 화면 좌표를 상쇄. 세로는 위에서 설명한 CSS 바닥-앵커 덕분에 이 보정이 아예 필요 없음. **직전 라운드에서 만든 "창 폭이 예상과 달라지면 자동으로 다시 중앙 정렬하는" `tick()`의 드리프트 보정(`xCenteredForWidth`)과 정면으로 충돌할 뻔함** — 그 로직은 "창 폭이 바뀌면 무조건 재중앙"이 원칙인데, 이번 리사이즈는 의도적으로 창 폭을 바꾸면서 스프라이트는 "중앙"이 아니라 "고정된 절대 위치"를 유지해야 함. 해결: 리사이즈 직후 `xCenteredForWidth`를 새 폭으로 즉시 갱신해서, 다음 프레임에 그 드리프트 보정 로직이 "폭이 달라졌다"고 오판해 방금 계산한 보정값을 다시 덮어쓰지 않게 함(재중앙 로직 자체는 안 건드림 — "의도된 변경 후엔 새 기준점을 갱신해서 그 로직이 개입할 이유 자체를 없앤다"는, 그 로직이 원래 방지하려던 것과는 다른 종류의 폭 변화라는 차이를 그렇게 구분).
  - **`positionBubble(overrideStageWidth)`**: 리사이즈 응답으로 받은 `newWidth`를 직접 넘겨서 가로 클램프 계산 — `await` 이후 `stageEl.clientWidth`를 다시 읽으면 main→renderer IPC 응답과 Chromium 자체의 창-크기-변경 알림이 서로 다른 채널이라 아주 짧은 순간 아직 안 갱신된 값을 읽을 이론적 위험이 있어서, 그 값 대신 `main.js`가 실제로 적용한 값을 그대로 신뢰.

**버그 발견 (실제 4개 모서리 + 실제 리마인더로 검증하다가) — 세로 확장 시 화면 끝에서 앵커가 깨짐**: 처음 구현한 `computeAnchoredBounds`는 목표 높이로 늘렸을 때 위쪽(`y`)이 화면 밖(음수)으로 나가면 `Math.max(workArea.y, ...)`로 **위치(y)만** 재클램프하고 있었음 — 8줄짜리 일부러 아주 길게 만든 테스트 메시지로 상단 모서리(화면 꼭대기에서 겨우 24px)에서 검증했더니, 스프라이트가 세로로 72px이나 아래로 튀는 걸 실제 좌표 비교로 발견(`spriteMoved` 체크가 걸림) — 원인: 목표 높이(296px)가 그 지점에서 실제로 확보 가능한 높이(224px, 화면 꼭대기까지의 거리)보다 커서 "바닥 고정, 위로 확장" 계산이 화면 밖 y를 냈고, 그 y를 사후에 화면 안으로 강제로 끌어내리면서 **정작 고정되어야 했던 바닥 모서리 자체가 같이 밀려 내려감**(높이는 그대로 296인데 y만 밀렸으니 바닥=y+height도 같이 이동) — "확장 방향으로 최대한 늘리되 화면을 벗어나면 안 된다"를 "먼저 늘리고 나중에 위치를 화면 안으로 밀어넣는다" 순서로 구현한 게 문제였음. **고침**: 순서를 뒤집어서 "고정된 모서리부터 반대쪽 화면 끝까지의 실제 가용 거리"를 먼저 계산하고 **목표 높이/폭 자체를 그 가용치로 clamp**한 뒤, 그 clamp된 크기로부터 위치를 역산 — 이렇게 하면 위치가 사후 보정으로 밀릴 일 자체가 없어서 고정 모서리가 항상 정확히 보존됨(화면 전체를 다 써도 메시지가 안 들어갈 만큼 극단적인 경우엔 말풍선이 살짝 잘릴 순 있지만, 스프라이트가 움직이는 것보다는 훨씬 나은 트레이드오프 — 사용자의 "스프라이트는 절대 안 움직여야 한다"가 "말풍선이 어떤 경우에도 절대 안 잘려야 한다"보다 명시적으로 우선순위가 높은 요구사항이었음).
  - **재검증**: 같은 8줄 테스트 메시지로 4개 모서리 재검증 — 아래쪽 두 모서리(화면 아래쪽이라 위로 확장할 공간이 충분함, 296px 요청대로 다 커짐)는 스프라이트 이동 없음+말풍선도 안 잘림. 위쪽 두 모서리(화면 꼭대기까지 224px밖에 없음)는 창이 가용한 224px까지만 커지고(요청한 296엔 못 미침) 스프라이트는 **완전히 고정**(`spriteMoved: false`) — 대신 이 일부러 극단적으로 길게 만든 메시지만 상단이 살짝 잘림(설계상 감수하기로 한 트레이드오프, 아래 참고). 창을 원래 크기로 되돌리는 것도 4곳 전부 확인(±2px, 이 창이 이미 갖고 있던 DPI/OS 반올림 오차 범위 - 직전 라운드에서 이미 확인/수용된 것과 동일).

**"극단적인 경우 살짝 잘릴 수 있다"는 게 실제로 문제가 되는 범위인지 재확인**: 위 8줄 메시지는 메커니즘 자체를 스트레스 테스트하려고 일부러 비현실적으로 길게 만든 것 — 실제로 어느 길이부터 문제가 되는지 상단 여유가 가장 좁은(24px) 상단 모서리에서 길이를 늘려가며 확인: 기본 메시지(1x, 31자) → 리사이즈 자체가 발동 안 함(원래도 안 잘림), 약 3배 길이(81자, 4~5줄) → 창이 가용한 최대(224px)까지 커지면서 상단 여백 0.4px로 **거의 정확히 안 잘리는 선까지 성공적으로 구제**, 약 5배 길이(136자, 7줄) → 그제서야 52px 정도 잘림 시작. 즉 이 트레이드오프가 실제로 발동하려면 상단 모서리에 붙은 채로 기본 메시지의 5배쯤 되는, 상당히 비현실적으로 긴 커스텀 메시지가 필요 — 지금 있는 12개 메시지는 물론 어지간히 긴 커스텀 메시지까지도 이 한계에 안 걸림.

**최종 검증 (실제 4개 모서리, 실제 IPC 'reminder' 이벤트, 3배 길이의 현실적인 긴 메시지)**: `desktopCapturer`로 실제 데스크톱을 캡처해 (1) 평소 대기 상태 - 작은 창, 말풍선 없음, (2) 리마인더가 뜬 상태 - 창이 커지고 말풍선 전체가 안 잘리고 보임, (3) 시간 초과 후 - 다시 원래 작은 크기로 — 3단계를 4개 모서리 전부에서 스크린샷으로 확인. 4곳 모두 (2)에서 `clipped:false`(말풍선의 top/left/right가 전부 새 창 경계 안), 스크린샷상 캐릭터가 (1)과 (2)에서 완전히 같은 화면 위치에 있음(육안 확인), (3)에서 창이 다시 작아지고 캐릭터 위치는 그대로 유지됨을 확인.

**전체 회귀 확인**: `npm run test:invariants`(120만 프레임) 위반 0건(이 로직은 `animal-engine.js`/`voxel-engine.js`와 무관). 이번 라운드는 외부 테스트 스크립트만으로 전부 검증(실제 `main.js`/`pet.js` 소스에 임시 로그/훅을 전혀 안 남김 - `git diff`로 재확인), 실제 앱(`npx electron .`)을 재기동해 기존과 동일한 CSP 개발 경고 외 콘솔 에러 없이 정상 구동 확인.

### 위치/이동 시스템 전면 재설계 — "자유 드래그 + 4모서리 자석 + 동적 리사이즈"에서 "항상 작업표시줄 위, 좌우로만" + 정적 창 + 페이싱 모드로
직전 여러 라운드(모서리 자석 스냅, 창 크기 통일, 스프라이트 재중앙, 말풍선 동적 리사이즈)를 거치며 자유 드래그+4모서리 스냅 구조 위에 계속 패치를 얹었는데도 말풍선 클리핑 버그가 반복 재발했던 근본 원인 — 펫이 있을 수 있는 위치(4개 모서리 + 그 사이 자유 위치)마다 창의 Y좌표가 달라서, 말풍선이 위로 뻗을 수 있는 실제 여유 공간 자체가 위치마다 달랐던 것. 사용자가 이번엔 그 변수 자체를 구조적으로 없애자고 요청 — Y를 완전히 고정하고, 그 전제 위에서 정적 크기 창 하나로 모든 메시지를 커버하고, 좌우 자석 스냅으로 단순화하고, 신규 "이동 방식" 설정(가만히/좌우 이동)을 추가.

**1. Y 고정 (`groundWindowY`)**: `screen.getPrimaryDisplay().workArea`(작업표시줄 제외 영역)의 `y+height`가 정확히 작업표시줄의 윗변 — 창의 아랫변을 여기 맞추면 펫이 항상 작업표시줄 바로 위에 섬. `pet.css`의 `#pet-wrap{bottom:14px}`가 그 안에서 발-창 경계 사이 자연스러운 여백을 그대로 제공(구석 마진이던 걸 작업표시줄 마진으로 의미만 바뀜, 코드는 안 건드림). 다중 모니터 미지원은 기존과 동일(`getPrimaryDisplay()` 고정).

**2. 정적 크기 창으로 전환 (동적 리사이즈 완전 폐기)**: 직전 라운드의 `resizeForBubble`/`releaseBubbleRoom` IPC, `computeAnchoredBounds`, `preGrowBounds` 전부 삭제 — Y가 항상 고정이니 "메시지가 얼마나 위로 뻗을 여유가 있는지"도 항상 동일해져서, 애초에 매 알림마다 창을 늘렸다 줄일 필요 자체가 없어짐. 대신 `PET_SIZE`를 한 번만 넉넉하게 잡아 항상 그 크기 유지:
  - **높이(300)**: 실측으로 결정 — 이번엔 다이나믹 세이프티넷이 없으므로 기본 12개 메시지뿐 아니라 그보다 긴 커스텀 메시지까지 커버해야 해서, 가장 긴 기본 메시지의 1/2/3/4/5/7배 길이 합성 메시지를 실제 `#bubble` CSS로 렌더링해 필요한 높이를 측정 — 5배(136자, 사실적으로 긴 커스텀 메시지 수준)에서도 41px 여유, 3배(81자)는 89px 여유, 일부러 극단적인 7배(199자)조차 ~38px 정도만 잘리는(파국적이지 않게 우아하게 저하) 지점인 300으로 채택.
  - **너비(230)**: 이전 라운드 그대로(`#bubble`의 CSS `max-width`(190) + `positionBubble()`의 `BUBBLE_SAFE_MARGIN`(좌우 각 20)의 합 = 230, 메시지가 아무리 길어도 줄바꿈이 캡을 걸어주므로 높이처럼 "더 넉넉하게" 여지를 둘 이유가 없음).
  - **화면에 항상 보이는 빈 공간에 대한 우려**: 평소(말풍선 없을 때) 창 위쪽 대부분이 투명+클릭통과라, 스프라이트가 이미 WIDTH 방향으로 여백 안에 들어앉아 있던 것과 마찬가지로 시각적으로 "빈 상자"처럼 안 보임 — 실제로 실행해서 확인.

**3. 좌우 자석 스냅으로 단순화 (`findNearestEdgeSnap`/`animateEdgeSnap`)**: 옛 4모서리 버전(`findNearestCornerSnap`/`animateCornerSnap`)에서 세로 방향 후보/계산을 전부 들어내고 좌/우 2방향만 남김 — 스프라이트 자신의 화면상 가장자리(창의 가장자리가 아니라, 창-창 안에서 96px 스프라이트가 실제로 있는 자리)를 화면 가장자리와 비교하는 기존 방식은 그대로 재사용(값도 `EDGE_SNAP_RADIUS=50`/`EDGE_SNAP_DURATION_MS=180`으로 이전과 동일). `edgeWindowX(side, display)`가 "스프라이트가 그 가장자리에서 `EDGE_MARGIN`(24px, 옛 코너 마진과 동일)만큼 떨어지는 창 X좌표"를 계산 — 창 생성 시 초기 위치, 드래그 종료 시 스냅 목표, 페이싱의 좌우 반환점(아래 참고) 셋 다 이 하나의 함수를 공유해서 "스냅되는 자리"가 어디서나 정확히 같은 좌표가 되도록 보장.

**4. 신규 "이동 방식" 설정 (가만히 / 좌우 이동)**: `settingsStore.js`의 옛 4방향 `position` 필드(`bottom-right` 등)를 완전히 제거하고 `movementMode: 'stationary'|'pacing'`으로 교체 — Y가 항상 고정된 지금은 "어느 구석"이라는 개념 자체가 의미가 없어졌으므로, 그 자리를 대신하는 게 "가만히 있을지 좌우로 걸어다닐지"라는 새로운 축. `windows/settings/index.html`의 "펫 위치" 커스텀 드롭다운 섹션(트리거+리스트박스+포탈링 로직 전부) 통째로 삭제하고, 기존 "언제 알려줄까요" 라디오와 같은 `.radio-row` 패턴의 "이동 방식" 라디오 2개(가만히/좌우 이동, 각각 짧은 설명 힌트 포함)로 교체 — 새 마크업 패턴을 발명하지 않고 이미 검증된 카드/라디오 스타일을 그대로 재사용.

- **엔진 쪽 (`animal-engine.js`) — 죽어있던 `walk` 상태를 외부에서 안전하게 깨우는 진입점 추가**: "이동 정책 변경 2단계"(자율 이동 완전 제거) 이후 아무도 `enterWalk()`를 안 불러서 죽은 채로 남아있던 `walk` behaviorState/gait 렌더링 코드(당시 "나중에 커서 쪽으로 다가가기 같은 기능이 돌아오면 그 자리에 다시 연결하면 된다"고 명시적으로 남겨뒀던 부분) — 이번이 바로 그 "돌아온 경우"라 판단해, `createAnimal()`이 반환하는 객체에 `startWalking()`/`stopWalking()` 두 메서드만 추가하고 상태머신 내부(`updateBehaviorState`/`enterWalk`/`GAIT_PRESETS`/idle 풀/`WALKING_IDLE_NAMES`/`circleStep` 등) 전체는 단 한 줄도 안 건드림.
  - `startWalking()`: `anim.behaviorState==='walk'`면 즉시 리턴(idempotent), 아니면 `enterWalk(anim)` 호출 후 `anim.walkDuration=1e9`로 설정 — `pinnedSleep`이 쓰는 것과 같은 "사실상 무한"패턴, 외부(pet.js)가 걷기를 끝내고 싶을 때만 명시적으로 `stopWalking()`을 부르는 구조. **idempotent(멱등)해야 하는 이유**: pet.js의 `updatePacing()`은 페이싱 중인 매 프레임 이 메서드를 계속 호출하는데(아래 참고), 매번 무조건 `enterWalk()`(→`resetPose()`)를 다시 태우면 정상적으로 걷는 도중에도 포즈 크로스페이드가 계속 팝(pop)돼서 걷기 애니메이션 자체가 깨짐 — 이미 걷는 중이면 조용히 무시하는 이 멱등성이 그 사고를 원천 차단.
  - `stopWalking()`: `anim.behaviorState!=='walk'`면 즉시 리턴, 아니면 `enterIdle(anim, pickIdleBehavior(anim.spec))` — 실제 `walkDuration` 타임아웃이 걸렸을 때와 정확히 같은 방식으로 idle 풀에 넘김. `prowlcircle`/`chasetail`/`hopspin`/`wheelrun`을 포함한 idle 풀 자체는 이 함수가 전혀 안 건드리므로, 페이싱 leg가 끝난 뒤에도 이 Z축 원형 idle들이 평소와 똑같이 뽑힐 수 있음(아래 검증 항목 참고).
  - `npm run test:invariants`(120만 프레임) 재실행 — 위반 0건, 이 추가가 기존 2D 회귀에 영향 없음 확인.

- **`pet.js` 쪽 — 페이싱 상태머신 (타이밍/방향 결정은 렌더러가, 실행/경계감지는 main이)**: 새 모듈 스코프 상태 `movementMode`(설정에서), `pacing`(현재 실제로 걷는 leg 중인지), `pacingRestUntil`(다음 leg를 시작할 시각) — `PACING_REST_MIN_MS=30000`/`PACING_REST_MAX_MS=60000`(요청받은 "수십 초 걷고 수십 초~1분 정도 쉬기"를 그대로 반영, 방향 전환이 드물고 서두르지 않게). `updatePacing(nowMs, allowedToMove)`이 매 프레임(`tick()` 맨 앞, `animal.update()`보다 먼저 — 이번 프레임에 시작된 leg라도 그 프레임의 advance가 바로 반영되도록, `updateCursorAlertTrigger`가 쓰는 것과 같은 순서 원칙) 호출돼 leg 시작/유지/휴식을 결정하고 `animal.startWalking()`을 부름(멱등이므로 매 프레임 불러도 안전). `tick()`은 `animal.update()`가 반환한 `advance`(그 프레임의 실제 걸음 거리, 종의 gait 함수가 계산한 값 그대로)를 `pacing`일 때만 `window.focusPetAPI.paceMove(advance*direction)`로 main에 IPC 전송 — main 쪽이 "얼마나 걸었는지" 자체를 독자적으로 추측하는 대신, 다리 애니메이션과 창 이동이 항상 완전히 동기화되도록.
  - **main.js가 실제로 창을 옮김 (main.js의 `pace-move` 핸들러)**: `paceFloatX`(반올림 없는 float 누적값, 드래그 후에는 `null`로 리셋돼 다음 이동의 첫 델타에서 창의 실제 현재 위치로 다시 시딩됨)에 델타를 누적하고, `edgeWindowX('left'/'right', display)` 범위를 벗어나면 그 경계로 clamp하고 `hitSide`를 렌더러에 `'pace-hit-edge'` IPC로 통보 — main이 화면 경계를 아는 유일한 쪽이라 경계 판정은 main이, 그 외 모든 타이밍/방향 결정은 렌더러가 갖는 역할 분담.
  - **`onPaceHitEdge`**: 현재 leg를 끝내고(`pacing=false`, `animal.stopWalking()`), 반대 방향으로 `direction`을 뒤집고, `pacingRestUntil`을 새 무작위 휴식 구간(30~60초)으로 재설정 — 다음 leg는 반대편을 향해, 충분히 쉬고 나서 시작됨.
  - **모드 전환/드래그 중 interruption**: `onSettingsUpdated`가 `movementMode`가 실제로 바뀐 걸 감지하면(페이싱→가만히로 바뀔 때만) `pacing=false`+`animal.stopWalking()`으로 즉시 멈춤 — 경계에 부딪히는 것 말고는 leg가 끝날 다른 계기가 없어서 명시적으로 필요. 드래그 시작 시에도 `mousedown` 핸들러가 진행 중이던 leg를 완전히 끝냄(main.js의 `paceFloatX=null` 리셋과 짝) — `animal-engine.js`의 `pinnedHeld`/`allowedToMove=false` 우선순위 자체가 이미 드래그/말풍선 중엔 걷기를 강제로 멈추게 하지만, `updatePacing`은 그 인터럽션 동안 `pacing`/`pacingRestUntil` 자체는 안 건드리고 그냥 기다리기만 함 — 인터럽션이 풀리면 `startWalking()`의 멱등 재확인이 같은 leg를 자연히 이어받음(원래 상태로 되돌아가야 할 이유가 없는, "인터럽션이 걷혔으니 하던 거 계속" 시나리오).

**검증 (다섯 가지, 사용자가 명시적으로 요구한 항목 그대로, 전부 실제 구동 중인 앱으로 확인 — 코드 리뷰만으로 안 끝냄)**:

1. **작업표시줄 바로 위 배치**: 실제 앱을 띄워 `desktopCapturer`로 실제 작업표시줄이 보이는 데스크톱 영역을 캡처 — 펫이 정확히 작업표시줄 바로 위에 서 있는 걸 확인. `win.getBounds()`로 계산한 Y도 `groundWindowY()`의 기대값과 일치.
2. **좌우 자석 스냅**: `sendInputEvent`(실제 마우스다운/업)+PowerShell `[System.Windows.Forms.Cursor]::Position`(실제 OS 커서 이동)로 진짜 드래그를 재현해 왼쪽/오른쪽 각각으로 드래그 — 처음엔 커서를 화면 가장자리로만 겨냥해서 왼쪽 스냅이 실패했는데(스프라이트를 잡은 지점이 스프라이트 중앙 근처라, 가장자리를 겨냥하면 실제 스프라이트 가장자리는 여전히 트리거 반경(50px) 밖에 남는 게 원인 — 오른쪽 드래그는 우연히 성공해서 처음엔 "왼쪽만 이상하다"로 보였음), grab-point-to-sprite-edge 오프셋을 반영해 커서 목표를 다시 계산하는 이전 라운드(구 4모서리 스냅 검증)의 검증된 공식을 그대로 재적용해 해결 — 재검증 결과 좌우 둘 다 기대 좌표와 1px 이내로 일치.
3. **말풍선 안 잘림**: 실제 알림 메시지의 5배 길이 합성 메시지로 좌/우 양쪽 끝에서 각각 알림을 띄워 캡처 — 양쪽 다 완전히 동일한(23.6px) 넉넉한 상단 여백으로 안 잘림, 위치와 무관하게 항상 같은 여유라는 재설계의 핵심 전제가 실측으로 확인됨(이전 여러 라운드가 반복적으로 겪었던 "위치마다 클리핑 여유가 달라지는" 문제 자체가 구조적으로 사라졌음).
4. **가만히 vs 좌우 이동, 차분한 리듬**: 실제 설정 창을 열어 실제 라디오/저장 버튼을 클릭해(진짜 DOM 이벤트+진짜 `save-settings` IPC) 모드를 전환, 창 x좌표를 표본화해 비교 — `stationary`는 20초 넘게 드리프트 0px(그루밍/앉기 등 idle만 재생, 위치 불변). `pacing`은 화면 중앙에서 시작해 약 20초 동안 691px를 꾸준히, 방향 반전 0회로(요청한 "산만하지 않고 차분하게"를 만족), 초당 약 26px의 일정한 속도로 걸어가 실제로 오른쪽 경계에 도달하는 것까지 한 번에 확인.
   - **검증 중 겪은 삽질 3건, 전부 앱 버그가 아니라 헤드리스 테스트 환경 특유의 문제였음**(정직하게 기록): (a) 이 개발 머신의 실제 OS idle 시간이 사람이 실제로 입력을 안 한 지 39분 가까이 지난 상태였는데, `main.js`의 `startAwayPoll()`(3분 이상 무입력이면 펫을 재움 — 알림 모드와 완전히 별개로 항상 도는 코스메틱 기능)이 이를 그대로 반영해 펫을 재워버려 `updatePacing`의 `asleep` 체크에 걸려 못 걷던 것 → 실제 `pet-away-state` IPC를 직접 눌러 깨움. (b) 그 깨움이 main.js 자신의 첫 5초 폴 틱(그 시점에야 `petAway` 기본값 `false`가 실제 유휴시간과 처음 동기화됨)에 곧바로 재-오버라이드돼서 다시 잠들어버림 → 첫 폴 틱이 지나간 뒤(스크립트 시작 후 5초 이상 지난 시점에) 두 번째로 깨움. (c) **가장 헷갈렸던 것**: 매번 "페이싱이 즉시 경계에 부딪혀 멈추는" 것처럼 보였는데, 알고 보니 두 가지가 겹쳐 있었음 — 먼저 펫이 처음부터 오른쪽 경계에 서 있는 상태에서 페이싱을 켰더니 첫 델타 한 번 만에 이미 경계였던 것(실제로는 정상 동작, 진짜 버그 아님) → 화면 중앙으로 옮긴 뒤 재시도했는데도 여전히 "즉시 멈춤"처럼 보여서 더 팠더니, 진짜 원인은 `npx electron <레포 바깥의 스크립트 경로>`로 테스트를 실행하면 Electron이 앱 이름을 `package.json`의 `"focus-pet"`이 아니라 자기 자신의 기본값 `"Electron"`으로 인식해서, `app.getPath('userData')`가 진짜 사용자 설정 파일(`%APPDATA%\focus-pet\settings.json`, 이 세션 내내 한 번도 안 건드려짐 — 별도로 재확인함)이 아니라 완전히 별개의 `%APPDATA%\Electron\settings.json`으로 갔던 것 — 그 파일에 이 세션의 이전 테스트 실행이 저장해둔 `movementMode:"pacing"`이 그대로 남아있어서, 매 새 테스트 프로세스가 시작부터 이미 페이싱 모드(그리고 오른쪽 경계에 서 있는 위치)였던 것. 앱 코드/실제 사용자 설정과는 무관한, 순수 테스트 하네스 아티팩트 — 해당 샌드박스 파일을 지우고 나서야 진짜 "중앙에서 시작해 천천히 끝까지 걷는" 정상적인 페이싱을 관찰할 수 있었음. (앞으로 이 프로젝트에서 `npx electron`으로 레포 바깥의 스크립트를 직접 실행하는 검증을 또 하게 되면, `%APPDATA%\focus-pet\`가 아니라 `%APPDATA%\Electron\`이 그 세션의 실제 설정 파일 경로라는 걸 기억해둘 것 — 실제 앱을 `npx electron .`로 띄우는 최종 확인에는 해당 안 됨, 진짜 유저 데이터는 항상 `%APPDATA%\focus-pet\`.)
5. **Z축 원형 걷기 idle(prowlcircle/chasetail/hopspin/wheelrun)이 재설계 후에도 정상 작동**: `windows/shared/animal-engine.js`가 순수 ES 모듈이라 Electron 없이 Node에서 직접 `createAnimal`을 import해 헤드리스로 검증 — (A) `startWalking()`을 전혀 안 부르는(가만히 모드와 동등한) 순수 idle 풀 순환만으로 4종 전부 각자의 원형 idle이 뽑히고, 반경/스핀 각도/다리 위상/hop 높이가 전부 실제로 움직이는 것 확인(이 프로젝트가 8차 QA 라운드부터 계속 써온 것과 같은 판정 기준). (B) `startWalking()`→2초 실제 걷기→`stopWalking()`(페이싱 leg 하나가 끝나는 걸 흉내) 이후에도 같은 원형 idle이 다시 뽑히는지 확인 — 고양이/토끼/햄스터는 바로 통과했지만 허스키의 `chasetail`(가중치 1, `earflick`(3.5) 등에 밀려 뽑힐 확률 자체가 낮음)만 첫 600초 시도에서 한 번도 안 뽑힘 — 이 프로젝트 자체가 이미 문서화해 둔 "낮은 가중치 idle 한 번 안 뽑힌 건 버그가 아니라 그냥 변동성일 수 있다"(햄스터 `wheelrun` 전례)는 선례를 그대로 따라 곧바로 버그로 단정하지 않고 1200초 상한으로 6회 재시도 — 6/6 전부 성공(평균 대기시간도 이론적 기댓값과 크게 안 어긋남)해서 첫 결과가 순수 변동성이었음을 확인, 코드는 전혀 안 고침.

**전체 회귀 확인 + 정리**: `npm run test:invariants`(120만 프레임) 위반 0건. 검증 과정에서 임시로 추가했던 디버그 계측(`pet.js`의 `__sampleHitForTest__`/`__paceDebugRead__`, `main.js`의 `pace-move` 핸들러 안 `[PACE-DEBUG]` 콘솔 로그) 전부 제거 확인 — `git diff`로 `main.js`/`preload.js`/`windows/pet/pet.js`/`settingsStore.js`/`windows/settings/`에 남은 변경이 이 재설계 자체(그리고 이번 세션 이전부터 있던, 관련 없는 다른 변경들)뿐이고 디버그 흔적이 없음을 재확인. 이 검증 과정에서 우연히 발견한, 이 세션 이전 라운드들에서 남겨진 채 여전히 떠 있던 orphan `electron.exe` 프로세스 9개(이 프로젝트가 이미 여러 번 문서화한, `app.exit(0)`/타임아웃 킬이 항상 프로세스 트리를 완전히 정리하진 않는다는 그 현상과 동일)는 안전하게 종료 정리함. 실제 앱(`npx electron .`)을 재기동해 정상 구동(콘솔 에러 없음) 확인 후 종료.

### 말풍선 완전 분리(별도 창) + 작업표시줄 간격 최소화 + 자유 드래그·낙하 애니메이션
위 재설계 라운드 이후 사용자가 세 가지를 추가 요청 — 이번 라운드의 핵심은 특히 1번: "말풍선이 펫 창 안의 DOM 요소로 있는 한 이 문제가 계속 반복된다"는 사용자 자신의 진단이 정확했음 — 위 재설계(Y 고정+정적 창)도 결국 "펫 창 크기/여백을 다시 계산"하는 접근의 연장선이었을 뿐, 근본적으로는 여전히 말풍선이 펫 창에 종속된 구조였음. 이번엔 패치 대신 아키텍처 자체를 바꿈.

**1. 말풍선을 완전히 별도 `BrowserWindow`로 분리 (`windows/bubble/`)**
- **새 창**: `main.js`의 `createBubbleWindow()`가 앱 시작 시 펫 창과 함께 딱 한 번 생성해 숨겨둠(`show:false`) — 알림마다 새로 만들지 않고 `show()`/`hide()`로 재사용(`createSettingsWindow()`의 "이미 열려 있으면 focus()만" 패턴과 같은 철학, 창 생성 오버헤드/깜빡임 회피). `frame:false`/`transparent:true`/`skipTaskbar:true`/`focusable:false`/`alwaysOnTop:true`(`setAlwaysOnTop(true,'screen-saver')`+`setVisibleOnAllWorkspaces`, 펫 창과 동일 — 전체화면 앱 위에도 떠야 하는 이 앱의 핵심 요구사항은 알림에도 그대로 적용됨), `setIgnoreMouseEvents(true)`(펫 창과 달리 `{forward:true}` 불필요 — 말풍선은 어디를 클릭해도 인터랙션이 없는 순수 장식이라 픽셀 단위 판정 자체가 필요 없음, 통째로 클릭 통과). Preload 없음 — 이 창은 자기 자신의 어떤 Electron/IPC API도 안 쓰고, main.js가 `webContents.executeJavaScript()`로 직접 구동(contextIsolation과 무관하게 항상 페이지의 main world에서 실행되는 API라 preload/contextBridge 없이도 안전하게 동작).
- **크기 = 실제 콘텐츠 크기**: `windows/bubble/bubble.js`의 `window.__setBubbleMessage(message)`가 텍스트를 채운 뒤 `#page`(패딩만 있고 나머지는 콘텐츠에 맞춰 줄어드는 `display:inline-block` 래퍼 — 그림자 여백 20px 전방향 + 꼬리 삼각형 여백 28px 하단)의 `getBoundingClientRect()`를 반환 — main.js가 이 값을 그대로 `BrowserWindow.setBounds()`의 width/height로 씀. 메시지 길이와 무관하게 창 크기 자체가 항상 콘텐츠에 정확히 맞으므로, 이전 라운드처럼 "이 메시지가 이 고정 크기 안에 들어가는가"를 걱정할 필요 자체가 없어짐 — 클리핑이라는 개념이 창 크기 계산 쪽에서는 원천적으로 발생할 수 없는 구조.
- **위치 = 화면 좌표, 펫의 실제 현재 위치 기준**: `showBubbleWindow(message, durationMs)`가 펫 창의 `getBounds()`+`SPRITE_INSET_X`+`SPRITE_WIDTH`로 스프라이트의 실제 화면 중심 X를 구하고, 그 중심에 말풍선을 맞추되(`idealX`) 화면 왼쪽/오른쪽 경계에서 `BUBBLE_SCREEN_MARGIN`(8px)만큼만 떨어지도록 클램프(`clampedX`) — 사용자가 요청한 그대로 "말풍선 창 하나의 x좌표만 화면 폭 안으로 클램프"가 정확히 이 두 줄. Y는 스프라이트 머리 위 `BUBBLE_GAP`(6px)만큼 띄운 고정 위치(`petSpriteTopY - height - BUBBLE_GAP`) — 세로 클램프가 아예 필요 없는 이유는 펫 창 자체가 이미 항상 작업표시줄 위 고정 Y에 있으므로(위 재설계 라운드) 말풍선의 Y도 자동으로 항상 화면 안에 들어옴.
- **꼬리(tail)**: 클램프로 말풍선 박스가 스프라이트 중심에서 벗어나도(예: 스프라이트가 화면 맨 왼쪽에 있을 때), 꼬리는 항상 스프라이트의 진짜 화면 X(`petSpriteCenterX`)를 가리키도록 `tailX = clamp(20, width-20, petSpriteCenterX - clampedX)`를 계산해 `bubble.js`의 `window.__showBubble(tailX)`(CSS 변수 `--tail-x`)에 그대로 전달 — 이전 라운드의 "박스를 밀고 꼬리를 반대로 되민다"는 아이디어를 그대로 계승하되, 이번엔 스테이지 하나 안의 상대좌표가 아니라 완전히 독립된 두 창 사이의 화면좌표로 계산한다는 점만 다름.
- **페이드 인/아웃**: 기존 CSS 트랜지션(`opacity`+`scale`, 0.25s) 그대로 유지 — `showInactive()`로 창을 보이자마자(포커스는 안 뺏음, `focusable:false`라 애초에 뺏을 수도 없지만 의미상 더 정확한 API) `requestAnimationFrame`으로 `show` 클래스를 붙여 트랜지션 시작, `durationMs` 후 `show` 클래스 제거(페이드아웃 시작) → 250ms 뒤(CSS 트랜지션 시간과 동일) `hide()`.
- **pet.js 쪽 축소**: `showBubble(message, durationMs=6000)`가 이제 `window.focusPetAPI.showBubble(message, durationMs)`(신규 `preload.js` 브릿지 → `'show-bubble'` IPC) 한 줄 + 기존 `paused` idle-정지 타이머 로직만 남음 — DOM 조작(`bubbleEl`/`bubbleTextEl`/`positionBubble()`/`BUBBLE_SAFE_MARGIN`) 전부 삭제. 리마인더(`onReminder`)와 poke 반응(클릭 핸들러) 둘 다 이 하나의 `showBubble()`을 그대로 호출하므로, main.js의 `'show-bubble'` 핸들러 하나가 두 경로를 전부 커버 — 별도 분기 불필요.
- **`windows/pet/index.html`/`pet.css`**: `#bubble`/`#bubble-text` 마크업과 `#bubble`/`#bubble::after`/`.show`/`.hidden` 스타일 전부 삭제(그대로 `windows/bubble/bubble.css`로 이전, 세부 값은 동일하게 유지 — 흰 배경/둥근모서리/그림자/꼬리 삼각형 전부 픽셀 단위로 같음, 다만 `left:50%` 절대위치 트릭은 필요 없어짐 — 별도 창의 콘텐츠는 그 자체로 이미 중앙정렬된 유일한 자식이라 그냥 패딩 박스 안에 자연스럽게 위치함).

**2. 작업표시줄 간격 최소화**: `pet.css`의 `#pet-wrap{bottom:14px}` → `bottom:3px` — "발이 거의 닿아있는 수준"을 그대로 반영. `main.js`도 이 값을 `SPRITE_BOTTOM_INSET=3`으로 동일하게 들고 있음(말풍선의 Y 앵커 계산에 스프라이트 머리 위치가 필요해서 — 렌더러와 모듈 경계가 없는 이 프로젝트 기존 관례대로 `SPRITE_WIDTH`처럼 중복 정의, 값이 바뀌면 두 곳 다 갱신).
  - **덤으로 펫 창 자체도 확 작아짐**: 말풍선이 별도 창으로 완전히 빠지면서, 기존 `PET_SIZE`(230×300 — 전부 말풍선을 위한 여유 공간이었음)가 더 이상 존재할 이유가 없어짐 — "펫 창의 크기/여백 계산과 말풍선이 완전히 무관해지고"라는 사용자의 예상되는 결과를 그대로 반영해, 펫 창도 스프라이트(96)+그림자 여백(`PET_MARGIN=8`, 캔버스의 CSS `drop-shadow` 필터가 `overflow:hidden`에 안 잘리게 하는 최소 여유)만 남긴 112×107로 축소. 클릭 통과/드래그/커서 추적은 전부 캔버스 자체(96×96 고정)나 실시간 `PET_SIZE` 상수/`spriteRect` 값을 그대로 참조하는 구조라 창이 작아져도 전혀 영향 없음(사각형이 작아졌을 뿐 좌표 계산 방식 자체는 안 바뀜) — 다만 `EDGE_MARGIN`/`SPRITE_INSET_X`가 파생되는 `PET_MARGIN`이 옛 값과 달라지므로, `pet.js`의 대응하는 fallback 상수(`||230`→`||112`, `||200`→`||107`)와 `updateCursorHint()`의 히트박스 중심 계산에 하드코딩돼 있던 옛 바닥 인셋(`-14`)도 `-3`으로 같이 갱신 — 놓쳤으면 클릭 히트박스 중심이 실제 스프라이트와 어긋났을 부분.

**3. 자유 2D 드래그 + 작업표시줄로 낙하하는 릴리즈 애니메이션**
- **드래그 자체는 완전히 자유로운 2D로**: 직전 라운드는 `drag-start`의 스프링-팔로우에서 Y를 매 틱 `groundWindowY()`로 고정하고 X만 커서를 쫓게 했었는데, 이번 요청("화면 어디든 자유롭게, 위아래 포함")에 따라 Y도 X와 완전히 동일한 독립 스프링(같은 `SPRING_STIFFNESS`/`SPRING_DAMPING`, 각자 자기 target/velocity)으로 커서를 따라가도록 변경 — `offset.offsetY`는 사실 이전 라운드부터 이미 `pet.js`의 `mousedown` 핸들러가 보내주고 있었지만(`{offsetX, offsetY}`) main.js가 그동안 안 쓰고 있었을 뿐이라, 실제로 새로 배선한 건 main.js가 이 값을 읽어서 Y 스프링의 target으로 쓰는 부분뿐. 드래그 도중엔 화면 맨 위로 끌고 올라가 화면 밖으로도 나갈 수 있음(의도된 동작 — "자유롭게").
- **릴리즈하면 항상 낙하**: `drag-end`가 더 이상 "가장자리 근처면 스냅, 아니면 아무것도 안 함"이 아니라, 항상(무조건) 애니메이션을 트리거하도록 바뀜 — Y는 예외 없이 `groundWindowY()`로 떨어지고, X는 기존처럼 좌우 가장자리 `EDGE_SNAP_RADIUS`(50px) 안이면 그 가장자리로, 아니면 놓인 자리 그대로 유지. 두 축을 하나의 `animateDrop()` 타이머가 함께 구동(`DROP_DURATION_MS=350ms`) — X는 기존 자석 스냅과 같은 easeOutCubic("슥 미끄러져 들어감"), Y는 easeInCubic(`t³`, 처음엔 느리다가 갈수록 빨라지는 "떨어지는" 느낌 — "부드럽게 아래로 슝 떨어지는 느낌"이라는 요청을 반영해 X와 의도적으로 다른 커브를 씀). 놓인 위치가 두 가장자리 어디에도 안 가까우면 X는 전혀 안 움직이고 Y만 떨어짐 — "놓은 위치가 좌우 끝 근처면 거기 붙고, 아니면 그 x좌표 그대로 떨어지면 된다"는 요청 그대로.
  - 기존 `findNearestEdgeSnap`(null 반환 시 스냅 자체를 안 하던 함수)을 `findDropTargetX`(항상 유효한 X를 반환 — 스냅 대상이 없으면 그냥 현재 X)로, `animateEdgeSnap`을 `animateDrop`(Y를 무조건 포함)으로 이름 그대로 재정의 — "스냅 여부를 판단하는 함수"에서 "낙하 목표 X를 계산하는 함수"로 역할 자체가 바뀐 것을 반영.

**검증 (사용자가 명시한 세 가지, 전부 실제 구동 중인 앱 + 실제 OS 입력으로 확인)**:
1. **말풍선 안 잘림 (중앙/좌측 끝/우측 끝)**: 기본 메시지보다 훨씬 긴(약 3~4배) 합성 테스트 메시지를 실제 `ipcMain.emit('show-bubble', ...)`로 세 위치(오른쪽 끝 시작 위치, 드래그로 옮긴 화면 정중앙, 드래그로 옮긴 왼쪽 끝) 각각에서 띄워 `desktopCapturer`로 캡처 — 세 곳 모두 말풍선 창의 실제 `getBounds()`가 `clipped left: false, clipped right: false`로 확인됨(오른쪽 끝: x=1297~1528, 화면폭 1536에서 정확히 `BUBBLE_SCREEN_MARGIN`(8px) 남기고 클램프; 왼쪽 끝: x=8~239, 역시 정확히 8px 클램프; 중앙: x=648~880, 클램프 없이 스프라이트 중심에 정확히 맞음) — 스크린샷으로도 메시지 전체가 여러 줄에 걸쳐 잘림 없이 보이고 꼬리가 매번 펫을 정확히 가리킴을 확인. 추가로 실제 클릭→poke 반응(`click` 핸들러의 진짜 `showBubble()`)까지 완전한 실제 경로(진짜 클릭 → 진짜 IPC → 진짜 말풍선 창)로 한 번 더 확인 — 코드 리뷰로만 확인하고 넘어가지 않음.
2. **작업표시줄 간격**: `desktopCapturer`로 펫-작업표시줄 경계 스크린샷 — 발이 작업표시줄 바로 위 몇 px 안쪽까지 붙어 보임(이전 라운드의 눈에 띄게 뜬 간격과 확연히 다름), `win.getBounds()`의 window bottom(816)이 `workArea` 바닥(816)과 정확히 일치함도 수치로 재확인(창 자체는 항상 작업표시줄에 붙어있고, 이번에 좁힌 건 그 창 **안에서** 스프라이트가 앉는 위치일 뿐이라는 구분을 그대로 확인).
3. **자유 드래그 + 낙하**: 실제 `sendInputEvent`(mousedown/mouseup)+PowerShell 실제 OS 커서 이동으로, 펫을 화면 정중앙 근처의 **화면 맨 위**(`workArea.y+60`, 가장자리에서 한참 먼 위치라 X 스냅이 안 걸리는 지점)까지 실제로 드래그해서 릴리즈 — 릴리즈 직후 `getBounds().y`가 `-16`~`-20`(화면 상단 바깥까지 나가 있음, "위아래 포함 자유 이동"이 실제로 동작함을 직접 증명)이었다가, 이후 `bounds()` 폴링(스크린샷 없이 순수 `getBounds()`만 15ms 간격으로 샘플링해 애니메이션 자체의 타이밍 왜곡을 제거)으로 낙하 곡선을 그대로 추적 — t=0~100ms까지는 거의 안 움직이다가(`y: -20→-19→-12`) 이후 급격히 가속해(`y: 16→57→178→364→596`) t≈351~367ms에 정확히 `groundY`(708)에 도달, 그 뒤로는 계속 708 고정 — 설계한 `DROP_DURATION_MS=350`·easeInCubic 곡선과 수치로 정확히 일치. X는 스냅 대상이 없는 위치였으므로 낙하 내내 완전히 고정(704, 변화 없음)도 함께 확인. 별도로 왼쪽 가장자리 근처로 드래그해 릴리즈했을 때는 X가 `edgeWindowX('left')`(16, `EDGE_MARGIN(24)-SPRITE_INSET_X(8)`)로 정확히 스냅되는 것도 재확인 — "가장자리 근처면 스냅, 아니면 그 자리에서 떨어짐" 두 경로 모두 실제 앱에서 검증.
  - **1차 시도에서의 착시(정직하게 기록)**: 처음엔 "릴리즈 직후"와 "150ms 후" 두 지점 사이에 스크린샷 캡처(`desktopCapturer.getSources()`, 실측 수백ms 소요)를 끼워 넣었더니 두 번째 샘플이 이미 `y=708`(착지 완료)로 나와서 "떨어지는 게 아니라 순간이동하나?"로 잠깐 오판할 뻔했음 — 원인은 애니메이션이 아니라 스크린샷 캡처 자체가 실제로 걸리는 시간이 150ms보다 커서 두 번째 측정 시점이 이미 350ms 애니메이션 구간을 넘어서 있었던 것(테스트 스크립트 자신의 타이밍 오차, 앱 버그 아님) — 스크린샷을 완전히 빼고 순수 `getBounds()` 폴링만으로 재측정해서 실제 낙하 곡선(위 항목)을 정확히 잡아냄.

**전체 회귀 확인 + 정리**: `npm run test:invariants`(120만 프레임) 위반 0건(이 로직은 `animal-engine.js`/`voxel-engine.js`와 무관, 예상대로 영향 없음). 검증 과정에서 임시로 추가했던 디버그 훅(`pet.js`의 `__sampleHitForTest__`)은 검증 직후 제거 확인 — `grep`으로 `main.js`/`preload.js`/`windows/pet/pet.js`/`windows/bubble/*.js` 전체에 디버그 흔적이 하나도 안 남아있음을 재확인. 실제 앱(`npx electron .`)을 재기동해 정상 구동 확인 — 강제 종료 시점의 `startCursorTrackPoll` 타이머 경합으로 인한 통상적인 셧다운 로그(이전 여러 라운드에서도 나타났던, 이 라운드의 변경과 무관한 종료 아티팩트) 외 콘솔 에러 없음.

### 말풍선-펫 간격 좁히기 + 이동 중 실시간 추적(라이브 팔로우)
위 "말풍선 완전 분리" 라운드 직후 사용자가 두 가지 빠진 연동을 지적 — 둘 다 "따로 떨어진 두 창을 하나처럼 보이게 만드는" 마무리 작업.

**1. 말풍선-펫 거리 — Y 공식이 CSS 패딩을 이중으로 반영하고 있었음**: `showBubbleWindow`의 `y = petSpriteTopY - height - BUBBLE_GAP`(`BUBBLE_GAP=6`)이 창의 **raw 바닥 모서리**를 기준으로 계산하고 있었는데, `bubble.css`의 `#page{padding-bottom:28px}`(그림자/꼬리 여유 공간)와 꼬리 자신의 높이(`#bubble::after`가 `bottom:-8px`로 박스 아래 8px만큼 튀어나옴)를 감안하면, **꼬리의 실제 시각적 끝**은 창 바닥에서 `28-8=20px`만큼 위에 떠 있음 — 즉 실제 눈에 보이는 간격은 `BUBBLE_GAP(6)`이 아니라 `BUBBLE_GAP+20=26px`였음(창의 여유 패딩을 "펫과의 거리"로 착각하고 그 위에 또 여백을 얹은 셈). **고침**: `BUBBLE_BOTTOM_PADDING=28`/`BUBBLE_TAIL_HEIGHT=8`(둘 다 `bubble.css`를 그대로 미러링하는 명명된 상수 — 값이 바뀌면 두 곳 다 갱신)을 도입해 `y = petSpriteTopY - height - BUBBLE_GAP + (BUBBLE_BOTTOM_PADDING - BUBBLE_TAIL_HEIGHT)`로 정정 — `BUBBLE_GAP` 자체는 6 그대로 유지(원래 의도했던 값이 맞았고, 계산이 그 값을 반영 못 하고 있었을 뿐이므로 상수를 건드릴 필요는 없었음).

**2. 펫 이동 중 말풍선 실시간 추적 — "숨겼다 다시 보여주기"보다 "매 프레임 같이 이동"을 선택**: 사용자가 두 방식(라이브 팔로우 vs 이동 시작 시 숨기고 종료 후 재표시) 중 판단을 맡겼는데, 말풍선을 "펫 머리에 붙어있는 것"으로 보이게 만드는 게 이번 라운드 1번 항목의 취지와 같은 방향이라, 이동 중 사라졌다 나타나는 것보다 계속 붙어서 따라오는 쪽이 더 자연스럽다고 판단 — 특히 기본 알림(6초)·포크(1.4초) 둘 다 순간적인 드래그보다 오래 떠 있는 경우가 흔해서, 잠깐의 드래그 때마다 매번 사라졌다 나타나면 오히려 더 산만해 보일 수 있음.
  - **구현**: 기존 위치 계산 로직을 `repositionBubbleWindow()`라는 별도 함수로 뽑아내(펫의 "현재" 위치를 다시 읽어 클램프/꼬리 위치까지 전부 재계산하고 `setBounds()`+`__showBubble(tailX)`로 반영) `showBubbleWindow()`(최초 표시)와 펫 창이 움직일 수 있는 **세 곳 전부**(드래그의 스프링-팔로우 tick, 낙하 애니메이션 tick, 페이싱의 `pace-move` 핸들러)에서 공통으로 호출 — 사용자가 명시한 "드래그 중 실시간 이동/낙하 애니메이션/좌우 자석 스냅"(낙하와 자석 스냅은 이제 같은 `animateDrop` 애니메이션 하나이므로 사실상 두 지점)에 더해, 명시되진 않았지만 같은 원리("펫 창 위치가 바뀌는 모든 경로")가 그대로 적용되는 **페이싱**(좌우로 걸어다니는 중에도 펫 창이 계속 움직임)까지 포함 — 알림이 페이싱 도중 우연히 겹쳐 뜨는 경우도 실제로 있을 수 있어 빠뜨리면 같은 문제가 재발했을 자리. `repositionBubbleWindow()` 자신은 `bubbleWindow.isVisible()`이 아니면 아무것도 안 하는 가벼운 no-op이라, 세 tick 모두에서 매 프레임 무조건 호출해도(말풍선이 안 떠 있는 게 보통이므로 대부분의 프레임에서 정말 아무 일도 안 함) 별도로 "지금 말풍선이 떠 있나"를 호출부가 추적할 필요가 없음.
  - **버그 발견 + 고침 — 말풍선 창도 똑같은 "크기가 계속 자라나는" 플랫폼 버그를 겪고 있었음**: 1차 구현은 `repositionBubbleWindow()`가 매 tick `bubbleWindow.getBounds()`로 width/height를 다시 읽어서 그대로 `setBounds()`에 되먹였는데, 이게 정확히 이 프로젝트가 펫 창 자신에게서 이미 겪고 고쳤던(`PET_SIZE` 상수로 고정, 절대 `getBounds()`를 되먹이지 않기) 그 크기 누적 버그를 말풍선 창에서도 그대로 재현시켰음 — 실제 10초 가까이 이어진 긴 드래그(60fps로 600회 이상 tick)로 검증하다가 발견: 드래그가 끝난 뒤 정지 상태의 말풍선 x좌표가 손으로 계산한 기대값과 40px 가까이 어긋나 있었음(펫이 최종적으로 멈춘 위치 기준 `petSpriteCenterX - width/2`로 역산한 기대 X와, 실측된 최종 X가 안 맞음 — width 자체가 조용히 커져 있었다는 뜻). **고침**: 펫 창의 `PET_SIZE`와 완전히 같은 패턴 — `showBubbleWindow`가 메시지를 측정한 실제 width/height를 `bubbleContentWidth`/`bubbleContentHeight`라는 모듈 스코프 변수에 딱 한 번 캐싱해두고, `repositionBubbleWindow()`는 이후 `bubbleWindow.getBounds()`를 절대 다시 안 읽고 이 캐싱된 값만 `setBounds()`에 씀 — 재검증 결과 10초짜리 드래그+낙하 전체에서 최종 위치가 손계산 기대값과 1px 이내로 일치(이전엔 40px 어긋났던 것과 대조).

**검증 (둘 다 실제 앱 + 실제 OS 입력)**:
1. **거리**: `ipcMain.emit('show-bubble', ...)`로 실제 말풍선을 띄운 뒤, 말풍선의 실제 꼬리 끝 Y(`bubbleBounds.y+height-(28-8)`로 역산)와 펫 스프라이트 머리 위치 Y의 차이를 계산 — 6~7px(설계한 `BUBBLE_GAP=6`과 거의 정확히 일치, 반올림 오차 수준). 스크린샷으로도 꼬리가 머리에 거의 닿을 듯 가까이 붙어 보임 확인(이전엔 눈에 띄게 떠 있었던 것과 대비).
2. **실시간 추적**: 말풍선을 띄운 채로 실제 `sendInputEvent`+PowerShell 커서 이동으로 펫을 화면 우측 하단에서 좌측 상단 쪽으로 실제로 드래그(24스텝) — 매 스텝마다 펫/말풍선 좌표를 함께 로깅해 말풍선이 스텝마다 펫을 따라 계속 이동하는 것을 수치로 확인(중간에 멈추거나 뒤처지는 구간 없음), 이어지는 낙하 애니메이션 동안에도(15ms 간격 순수 `getBounds()` 폴링, 스크린샷 오버헤드 없이) 펫의 Y가 떨어지는 것과 정확히 같은 리듬으로 말풍선의 Y도 같이 떨어지다가 펫이 착지하는 바로 그 tick에 말풍선도 함께 멈춤을 확인. 스크린샷으로도 드래그 중간 지점과 최종 정착 지점 둘 다에서 말풍선이 펫 머리 바로 위에 붙어 있는 것을 확인 — 뒤에 남겨지는 모습은 어디에도 없음.
  - **1차 시도에서 나온 "즉시 멈춤" 현상은 앱 버그가 아니라 테스트 자체의 지속시간 문제였음(정직하게 기록)**: 처음엔 드래그 스텝을 18~19번째쯤 진행하다가 말풍선이 갑자기 그 자리에 얼어붙는 것처럼 보였는데, `Date.now()`로 실측한 실제 경과 시간을 찍어보니 PowerShell을 매 스텝 `execSync`로 새로 띄우는 오버헤드(각 호출이 수백 ms) 때문에 24스텝을 도는 데 실제로는 10초 가까이 걸리고 있었음 — 그 시점 사용하던 말풍선의 `durationMs=8000`이 테스트 도중에 자연 만료돼 `hideBubbleImmediately()`가 호출된 것일 뿐, `repositionBubbleWindow()`의 `isVisible()` 가드가 정확히 설계대로 그 시점부터 위치 갱신을 멈춘 것이었음(진짜 버그처럼 보였지만 사실은 의도된 동작) — `durationMs`를 60초로 늘려 재검증해서 "얼어붙음"이 사라지는 것으로 확인, 그 재검증 과정에서 위에 적은 진짜 버그(크기 누적)를 발견함.

**전체 회귀 확인 + 정리**: `npm run test:invariants`(120만 프레임) 위반 0건. 검증 과정에서 임시로 추가했던 디버그 훅(`pet.js`의 `__sampleHitForTest__`)은 검증 직후 제거 확인 — `grep`으로 관련 파일 전체에 디버그 흔적이 없음을 재확인. 실제 앱(`npx electron .`)을 재기동해 정상 구동 확인 — 기존에 이미 알려진 `startCursorTrackPoll` 강제종료 셧다운 로그 외 콘솔 에러 없음.

### 말풍선 간격 한 번 더 좁히기 + 페이싱 중 알림 시 "멈췄다 이어걷기"
직전 라운드 직후 사용자가 두 가지를 추가 요청 — 1번은 파라미터 조정, 2번은 이번 라운드에서 가장 시간이 오래 걸린 부분으로, 검증 스크립트 자체가 네 번 연속으로 잘못된 결론을 낼 뻔했던 라운드(전부 정직하게 기록).

**1. 간격을 한 번 더 좁힘**: `BUBBLE_GAP`을 `6`→`1`로 — "머리 바로 위, 거의 맞닿는 수준" 요청 그대로, 0으로 완전히 붙이지 않은 이유는 꼬리가 스프라이트 실루엣에 그대로 파묻히는 것보다 머리카락 한 올 두께의 여백이 "머리 위에 얹혀있다"는 인상을 유지하는 데 낫다고 판단해서. 나머지 계산식(`BUBBLE_BOTTOM_PADDING`/`BUBBLE_TAIL_HEIGHT`)은 직전 라운드에서 이미 올바르게 고쳐져 있어서 안 건드림.

**2. 페이싱 중 알림 — 실제로는 이미 대부분 구현돼 있었음**: 코드를 다시 읽어보니 `updatePacing()`의 기존 주석(페이싱을 처음 도입한 훨씬 이전 라운드부터)에 이미 "Doesn't touch `pacing` at all while interrupted (bubble/drag/sleep) - just waits... the SAME leg picks back up once the interruption clears via the idempotent startWalking() re-assertion"라고 명시돼 있었음 — 말풍선이 뜨면 `paused=true`가 되고, 이게 `allowedToMove=false`로 이어져 `updatePacing`이 `startWalking()` 호출 자체를 멈추고, `animal-engine.js`의 `updateBehaviorState`도 `!allowedToMove`일 때 무조건 idle로 강제 전환하므로 `advance`가 0이 되어 `pace-move`가 애초에 안 나감 — 즉 "말풍선이 뜨면 자동으로 멈추고, 사라지면 같은 `pacing=true` 플래그를 그대로 두고 있다가 다시 걷기 시작"하는 구조가 이미 처음부터 있었음. 코드 변경은 필요 없었고, 이걸 **실제로 검증**하는 과정에서 오히려 테스트 스크립트 쪽 문제를 네 번 연속으로 만났음(전부 기록):
  - **1차 삽질 — `ipcMain.emit('show-bubble',...)`은 `paused`를 안 건드림**: main.js의 `'show-bubble'` 핸들러를 직접 호출하는 지난 라운드의 검증 방식은 말풍선 창을 띄우는 것까지만 하고 렌더러의 `paused` 플래그는 전혀 안 건드림(그 플래그는 `pet.js`의 `showBubble()` 안에만 있음, main.js는 몰라도 됨) — 이 방식으로 테스트하니 페이싱이 전혀 안 멈추는 것처럼 보였는데, 실은 테스트 자체가 진짜 정지 메커니즘을 건드리지도 않은 것이었음.
  - **2차 삽질 — 실제 클릭(poke)으로 테스트했더니 이번엔 다시 안 걸어감**: 렌더러 실제 경로를 타려고 진짜 클릭(mousedown+mouseup)으로 바꿨는데, `mousedown` 핸들러 자체가 "펫을 손으로 집었다"는 별개의 기존 로직을 갖고 있어서(`if(pacing){pacing=false; animal.stopWalking(); pacingRestUntil=지금+30~60초 랜덤;}`) — 클릭 한 번에도 이게 걸려서 pacing 자체가 완전히 끝나고 30~60초짜리 새 휴식이 걸려버림. 사용자가 요청한 "알림이 뜨면"은 **사용자가 직접 만지는 게 아니라 자동으로 뜨는 리마인더**를 말하는 것이므로, 클릭(poke)은 애초에 이 라운드가 테스트해야 할 경로가 아니었음 — main.js의 `fireReminder()`가 실제로 보내는 것과 정확히 같은 `'reminder'` IPC(`{message,soundEnabled,soundVolume}`)를 직접 펫 창에 보내는 방식으로 다시 바꿈(마우스다운 자체가 아예 없는 경로).
  - **3차 삽질 — "즉시 재개"처럼 보였던 것은 테스트의 샘플링 창 계산 오류**: 이 방식으로 재검증했더니 `paused`가 진짜 6초(기본 `showBubble` duration) 다 채우기 전에(관찰상 약 4.7~4.8초 지점) 풀리는 것처럼 보여서 처음엔 진짜 버그로 의심 — 렌더러의 `performance.now()`를 알림을 보낸 시점과 `paused`가 실제로 풀리는 시점 둘 다에서 정밀하게 찍어보니, **알림을 보낸 시점 + 6000ms**와 **실제로 풀린 시점**이 100~200ms 오차 내로 정확히 일치했음 — "너무 일찍 풀린다"는 인상은 테스트 스크립트가 "알림을 보낸 후 여러 딜레이(스크린샷 캡처 등)를 거친 뒤"부터 6초짜리 샘플링 창을 다시 시작해서 생긴, 창 자체가 원래보다 짧아진 착시였을 뿐(진짜 타이머는 처음부터 끝까지 정확히 6초).
  - **4차(작은) 삽질 — 페이싱 자체의 30~60초 랜덤 휴식이 테스트를 가끔 타임아웃시킴**: 페이싱을 켜고 "걷기 시작할 때까지 최대 30초 대기"하는 방식으로 테스트했는데 한 번은 30초 안에 안 걸어서 실패 — 진짜 버그가 아니라, 마침 그 순간 펫이 (최대 60초까지 갈 수 있는) 랜덤 휴식 중이었을 가능성이 높음(휴식 자체가 정상 설계). 매번 랜덤 휴식이 끝나길 수동적으로 기다리는 대신, 임시 디버그 훅(`__forcePacingNow__`, `pacingRestUntil`을 즉시 지금 시각으로 앞당김)으로 다음 leg를 강제로 즉시 시작시켜 이 무작위성 자체를 테스트에서 제거.
  - 이 과정에서 실제로 코드를 고친 곳은 없음(`main.js`/`pet.js`의 실제 페이싱·일시정지 로직은 이번 라운드에서 단 한 줄도 안 바뀜) — 전부 검증 스크립트 쪽에서 발견/수정한 네 가지 서로 다른 문제.

**최종 검증 결과 (정밀 타임스탬프, 실제 앱)**: `__forcePacingNow__`로 즉시 걷기 시작 → 실제 걷는 동안 x좌표가 꾸준히 변함(1400→1372) 확인 → 실제 `'reminder'` IPC 전송(렌더러 `performance.now()`=17930.5 기록) → `paused`가 정확히 `t=23793`(아직 true)와 `t=23997`(false로 전환) 사이에서 풀림(예측값 23930.5와 100~200ms 이내로 정확히 일치) → 그 사이(약 6초) x좌표는 단 한 픽셀도 안 바뀜(고정값 1321 반복) → `paused`가 풀리자마자 그 다음 샘플부터 다시 x가 움직이기 시작(1321→1320→1314→...) → 말풍선이 완전히 사라진 뒤에도 계속 걸음(범위 61px) → 재개 후 방향(-1)이 멈추기 전 방향(-1)과 정확히 같음(새 leg가 아니라 같은 leg를 이어서 걷는 것 확인). 스크린샷으로도 알림이 뜬 순간 펫이 멈춘 자리 그대로 말풍선이 뜨고, 말풍선이 사라진 뒤엔 다리 자세가 바뀐 채(걷는 중) 다른 위치에 있는 것을 확인.

**전체 회귀 확인 + 정리**: `npm run test:invariants`(120만 프레임) 위반 0건(이번 라운드는 애니메이션/포즈 로직을 전혀 안 건드림). 검증 과정에서 추가했던 임시 디버그 훅 세 개(`pet.js`의 `__sampleHitForTest__`/`__pacingDebugRead__`/`__forcePacingNow__`) 전부 검증 직후 제거 확인 — `grep`으로 관련 파일 전체에 흔적 없음 재확인. 실제 앱(`npx electron .`)을 재기동해 정상 구동 확인 — 기존에 이미 알려진 `startCursorTrackPoll` 강제종료 셧다운 로그 외 콘솔 에러 없음.

### 말풍선 간격이 여전히 떠 보임(근본 원인 발견 — Y 공식 자체가 잘못된 값에 의존) + 알림 소리가 한 번만 재생되는 버그 수정
사용자가 스크린샷(`assets/reference/bubble-gap-still-visible.png`)을 첨부하며 지난 라운드의 `BUBBLE_GAP=1`로도 여전히 눈에 띄게 떠 보인다고 재확인 요청(1~2px 이내로 더 좁혀달라) + 별개로 `playBeep()`이 호출마다 `new AudioContext()`를 만들고 절대 `close()`하지 않아 Chromium의 페이지당 AudioContext 개수 제한(~6개)에 걸려 몇 번째 알림부터 소리가 조용히 안 나는 버그를 사용자가 직접 코드로 진단해 옴(재사용+`resume()` 방식으로 고쳐달라고 요청).

**순환 검증이었다는 걸 먼저 인정**: 지난 라운드의 "간격 확인됨(~2px)" 검증 자체가 순환 논리였음 — 그 검증 스크립트가 "기대값"을 계산할 때 main.js가 Y를 **설정하는 데 쓰는 바로 그** `BUBBLE_BOTTOM_PADDING`/`BUBBLE_TAIL_HEIGHT` 상수를 그대로 재사용하고 있었음 — 같은 공식으로 값을 설정하고 같은 공식으로 검증하면 그 공식 자체에 있는 오차는 절대 못 잡음. 이번엔 두 렌더러에서 실제 `getBoundingClientRect()`/`getComputedStyle(el,'::after')`를 직접 읽어(main.js의 어떤 상수도 참조하지 않고) 화면 좌표계에서 독립적으로 간격을 재는 진단 스크립트를 새로 작성 — 최초 측정 결과 3.4px(디스플레이 `scaleFactor:1.25`, `#page`의 실측 CSS 높이 85.6px vs 창의 실제 `getBounds().height` 88px — 요청한 크기와 OS가 실제로 승인하는 크기가 어긋나는, 이 프로젝트가 펫 창에서 이미 여러 번 문서화한 것과 같은 종류의 DPI 반올림이 말풍선 창에도 있음을 확인).

**1차 시도(읽어서-보정하기) — 오히려 간격이 더 벌어짐**: `repositionBubbleWindow()`가 `setBounds()` 직후 `getBounds()`로 실제 승인된 높이를 다시 읽어 그 값으로 Y를 재계산·재적용하는 보정 패스를 추가했는데, 실측 결과 간격이 3.4px → 5.4px/4.2px로 오히려 나빠짐. **원인**: Y 공식 자체(`petSpriteTopY - height - GAP + (BOTTOM_PADDING - TAIL_HEIGHT)`)에서 `height - (BOTTOM_PADDING-TAIL_HEIGHT)`가 "창 위쪽 가장자리부터 꼬리 끝까지의 거리"와 대수적으로 같아지려면 `height`가 **콘텐츠의 실제 측정 높이**(85.6)여야 하는데, 보정 패스가 그 대신 **창의 승인된 bounds 높이**(88, OS 반올림값)를 넣고 있었음 — 이 둘은 다른 양이라(`#page`는 `top:0`에서 흘러내리는 블록이라 콘텐츠의 세로 위치는 창의 총 높이와 무관하게 고정, 창이 콘텐츠보다 커도 남는 공간은 그냥 창 바닥에 빈 채로 남음), 창 bounds 높이를 대신 넣는 순간 그 차이(88-85.6=2.4px)만큼 꼬리가 창 위쪽에서 더 멀어지는 방향으로 계산이 틀어짐 — 정확히 관측된 악화폭과 일치.
**근본 수정 — Y 공식에서 "창 높이"라는 개념 자체를 제거**: 창 높이(요청이든 승인이든)에 의존하는 공식 대신, `windows/bubble/bubble.js`의 `__setBubbleMessage`가 **꼬리의 실제 시각적 끝이 창 위쪽 가장자리로부터 얼마나 떨어져 있는지**(`bubbleEl.getBoundingClientRect().bottom + tailBorderPx`, 실제 라이브 DOM에서 직접 측정 — `#bubble`의 세로 위치는 `padding-top`으로만 결정되고 창의 총 높이와 무관하므로, 창 높이가 요청과 다르게 승인돼도 이 값 자체는 절대 안 변함)를 같이 반환하도록 확장 — main.js는 이 `bubbleTailOffsetFromTop` 값 하나만으로 `y = petSpriteTopY - offset - GAP`를 계산. 창 높이가 몇 px로 승인되든 이 공식 자체에 안 들어가므로 "보정 패스"가 필요한 지점 자체가 없어짐(`BUBBLE_BOTTOM_PADDING`/`BUBBLE_TAIL_HEIGHT` 상수와 보정 분기 코드는 전부 삭제).
**2차로 발견한 별개의 1px 오차 — 펫 스프라이트 쪽에도 같은 종류의 버그가 있었음**: 위 수정 후에도 메시지별로 gap이 1.4px/-0.8px로 들쭉날쭉(2.2px 스프레드) — 원인은 말풍선이 아니라 `petSpriteTopY = petBounds.y + (PET_SIZE.height - SPRITE_BOTTOM_INSET - SPRITE_WIDTH)`가 펫 창의 **요청** 높이 상수(`PET_SIZE.height=107`)를 쓰고 있었던 것 — 실측해보니 펫 창의 **승인된** 높이는 108(+1, 같은 DPI 반올림)이고, 스프라이트는 `#pet-wrap{bottom:3px}`로 창 **바닥**에 고정돼 있어서, 창이 요청보다 1px 더 크게 승인되면 스프라이트 자체가 그만큼 아래로 밀림(독립 측정한 라이브 `#pet-wrap` top이 717인데 공식은 716을 냄, 정확히 1px 차이). **고침**: `petBounds.height`(이미 `getBounds()`로 갖고 있는 값)를 상수 대신 그대로 사용 — `petSpriteTopY = petBounds.y + (petBounds.height - SPRITE_BOTTOM_INSET - SPRITE_WIDTH)`. 이 수정 후 `petSpriteTopY`가 두 메시지 모두 정확히 717(독립 측정값과 완전히 일치)로 안정됨.
**잔여 지터는 그대로 인정 — 값을 살짝 올려서 흡수**: 위 두 수정 후에도 요청 Y와 승인 Y 사이에 호출마다 0~1px 정도의 잔여 반올림 지터가 남음(이 디스플레이의 125% 배율이 만드는 물리 픽셀 격자가 논리 픽셀과 딱 안 맞아떨어져서 생기는, 근본적으로 없앨 수 없는 양자화 노이즈로 판단 — 여러 번 재측정해도 매번 조금씩 다른 승인 Y가 나옴). `BUBBLE_GAP`을 `1`→`3`으로 올려 이 지터가 음수(꼬리가 스프라이트에 파고드는 쪽)로 안 넘어가게 함 — 재측정 결과 두 메시지 모두 0.2px/2.4px로 수렴(둘 다 "거의 맞닿는" 범위 안, 이전의 4~8px대와는 확연히 다름). 스크린샷으로도 육안 확인 — 꼬리가 고양이 머리 바로 위에 거의 닿아 있음.

**알림 소리 — 재사용 방식 채택**: `pet.js`에 모듈 스코프 `let sharedAudioContext = null`과 `getSharedAudioContext()`(없으면 한 번만 생성, 있으면 그대로 반환)를 추가 — `playBeep()`이 매번 `new AudioContext()` 대신 이 함수를 호출하고, `ctxA.resume().catch(()=>{})`를 무조건 같이 호출(이미 `running`이면 안전한 no-op, 탭이 백그라운드로 갔다 오는 등으로 `suspended`가 됐을 때만 실제로 의미 있음 — `await` 안 해도 무방한 이유는 그 아래 오실레이터 스케줄링이 `currentTime` 기준으로 걸리는 것이라 컨텍스트가 아직 resume 중이어도 정상적으로 큐잉되고 resume 완료 즉시 재생되기 때문).

**검증**: main.js를 그대로 `require`해서 실제 IPC 계약(`show-bubble`/`reminder`)만으로 구동하는 스크립트로 — (1) 간격: 두 개의 서로 다른 메시지(짧은 테스트 문구, 사용자가 첨부한 스크린샷과 정확히 같은 2줄 문구)로 각각 독립 측정, 0.2px/2.4px로 확인, 스크린샷도 저장해 육안 확인. (2) 소리: 실제 `'reminder'` IPC를 6회 연속(1.75초 간격) 전송하면서, `pet.js` 소스에 영구 훅을 남기는 대신 **외부에서** `executeJavaScript`로 `window.AudioContext`를 `Proxy`로 감싸 생성자 호출 횟수만 세는 임시 계측을 주입(테스트 스크립트 쪽 코드일 뿐, pet.js는 무변경) — 6번 전부 construct count가 정확히 1로 유지됨을 확인(재사용이 실제로 되고 있다는 뜻). 클린업 전 단계에서는 pet.js에 임시로 `window.__audioDebugRead__()`(construct count/`state`/`currentTime`)를 노출해 6회 모두 `state:'running'`, `currentTime`이 단조 증가(0.000→1.520→3.285→5.040→6.821→8.600)하는 것까지 먼저 확인해두었음 — 검증 후 이 훅은 pet.js에서 완전히 제거.
**전체 회귀 확인**: `npm run test:invariants`(120만 프레임) 위반 0건(애니메이션/포즈 로직과 무관). 이번 라운드에서 추가했던 임시 디버그 로그(`main.js`의 `FOCUSPET_DEBUG_GAP` 게이트)와 훅(`pet.js`의 `__audioDebugRead__`/`audioContextConstructCount`)은 전부 제거 확인 — `grep`으로 관련 파일 전체에 흔적 없음 재확인. 실제 앱(`npx electron .`)을 재기동해 정상 구동 확인 — 기존에 이미 알려진 CSP 개발 경고 외 콘솔 에러 없음.

### 말풍선 간격을 특정 DPI 배율에 의존하지 않게 재수정 + 알림 소리 "최초 1회만 재생" 재조사 (코드만 수정, 이번 라운드는 검증 스킵)
사용자가 두 가지를 다시 요청 — 1번: 지난 라운드의 `BUBBLE_GAP=3`이 이 개발 머신의 125% 배율 하나에서 실측한 잔여 지터를 흡수하려고 고른 값이라, 100%/150%/그 외 배율에서는 오히려 어긋날 수 있는 "특정 배율 전용 보정"이었음을 스스로 인정하고, 배율 자체에 의존하지 않는 구조로 다시 고쳐달라고 요청. 2번: 지난 라운드의 AudioContext 재사용 수정이 실사용에서는 여전히 첫 알림 이후 소리가 안 난다고 재확인 — 이번엔 AudioContext 문제가 아닐 수도 있다고 보고 이벤트 리스너 중복/조건 체크 등 처음부터 다시 보라고 요청. **이번 라운드는 사용자 지시대로 재현 테스트/스크린샷 등 실제 검증을 스킵하고 코드 수정만 완료함** — 아래 두 수정 모두 실기 확인 없이 코드 리뷰/논리적 추론만으로 반영된 것으로, 다음에 실사용/스크린샷으로 재확인이 필요함.

**1. 말풍선 간격 — 배율 전용 상수 대신 "요청한 위치와 실제 승인된 위치의 차이를 그때그때 재보정"하는 구조로 교체**: `BUBBLE_GAP`을 다시 순수한 디자인 의도값(3→2)으로 되돌리고, 그 위에 얹혀 있던 "125%에서 실측한 지터를 흡수하기 위한 편향"이라는 역할은 `repositionBubbleWindow()`의 새 보정 패스로 옮김 — `setBounds({x,y,width,height})` 직후 `bubbleWindow.getBounds()`로 **실제로 승인된 y**를 다시 읽어, 그 값 기준으로 실제 꼬리 끝 위치(`granted.y + bubbleTailOffsetFromTop`)를 재계산하고, 원래 의도했던 위치(`desiredTailTipY = petSpriteTopY - BUBBLE_GAP`)와의 차이(`errorPx`)만큼만 y를 다시 보정해 **한 번 더** `setBounds()`를 호출 — 배율이 얼마든(100%든 125%든 150%든) 그때그때 실제로 벌어진 오차를 그 자리에서 되갚는 구조라, 특정 배율 하나에 맞춘 상수를 하드코딩할 필요 자체가 없어짐(100%처럼 반올림 오차가 애초에 없는 배율에서는 `errorPx===0`이라 이 보정 패스가 그냥 같은 값으로 다시 `setBounds()`하는 무해한 no-op이 됨). **9차/13차 라운드의 "읽어서 보정" 실패를 반복하지 않도록 두 가지를 의도적으로 지킴**: (a) 창 **크기**(width/height)는 절대 `granted`에서 다시 읽지 않고 항상 처음 계산해둔 고정값(`bubbleContentWidth`/`Height`)만 재사용 — 이전 라운드에 겪은 "getBounds()로 읽은 값을 다시 setBounds()에 먹이면 펫 창에서 이미 문서화된 창 크기 무한 증식 버그가 재현된다"는 패턴을 피하기 위해 **오직 y 위치만** 보정 대상으로 삼음(size creep 버그의 핵심이 "매 틱 크기를 읽어서 다시 먹이는" 구조였지, "위치를 한 번 보정하는" 것과는 다른 문제라는 걸 이번엔 명확히 구분). (b) 예전(9차 라운드) 실패 원인이었던 "창 높이(height)를 Y 공식에 다시 섞어 넣는" 실수를 반복하지 않도록, Y 공식 자체는 여전히 (이전 라운드에서 이미 고쳐둔 대로) `bubbleTailOffsetFromTop`(창 높이와 무관하게 라이브 DOM에서 측정한, 창 위쪽 가장자리부터 꼬리 끝까지의 순수 거리)만 쓰고, 이번 보정 패스는 오직 "요청한 y와 승인된 y의 차이"만 다룸 — 창 높이라는 변수 자체가 이 보정 로직 어디에도 안 들어감. 검증은 이번 라운드에서 스킵했으므로, 다음에 실사용 시(특히 이 개발 머신이 아닌 다른 배율의 환경에서) 간격이 실제로 배율과 무관하게 일정한지 재확인 필요.

**2. 알림 소리 — AudioContext 재사용 자체가 아니라 "resume()을 기다리지 않고 바로 재생을 스케줄한 것"이 진짜 원인일 가능성이 높다고 재판단**: 사용자 요청대로 이벤트 리스너 중복 등록/해제부터 재점검 — `preload.js`의 `onReminder: (callback) => ipcRenderer.on('reminder', ...)`는 `pet.js` 모듈 로드 시 **딱 한 번만** 등록되고 어디서도 `removeListener`/`once`를 안 씀(중복 등록·조기 해제 둘 다 코드상 없음), `main.js`의 `fireReminder()`도 `settings.paused`(사용자가 트레이에서 수동으로 끄지 않는 한 항상 `false`)만 체크할 뿐 반복 호출을 막는 별도 조건이 없고, `soundEnabled`/`soundVolume`도 매 호출 `settings` 객체에서 그대로 읽어올 뿐 첫 호출 후 값이 바뀌는 코드 경로가 없음 — 즉 사용자가 의심한 "리스너 중복/조건 체크에서 조용히 막히는 부분"은 이 세 경로(리스너, `paused`, `soundEnabled`) 어디에서도 못 찾음. 대신 `playBeep()` 자신의 로직으로 시선을 돌림: 지난 라운드의 수정은 `ctxA.resume()`을 **기다리지 않고**(`.catch(()=>{})`만 걸고 바로 다음 줄에서 `knockNote()`를 스케줄) "언젠가 resume이 끝나면 이미 예약해둔 오실레이터가 알아서 재생될 것"이라는 가정에 기대고 있었는데, 이 가정은 Web Audio 스펙이 명시적으로 보장하는 동작이 아님 — 그리고 이 프로젝트의 기본 알림 간격(30분)은, 포커스를 절대 못 받는(`focusable:false`) 이 창이 Chromium의 백그라운드 전력 관리에 의해 AudioContext가 다시 `suspended`로 넘어가기에 충분히 긴 유휴 시간임(빠른 연속 테스트로는 이 상태 전이 자체가 재현이 안 됨 — 지난 라운드의 "6회 연속, 1.75초 간격" 검증이 실제로는 이 문제를 노출할 수 없는 조건이었다는 뜻). "1회차는 되고 2회차부터 조용히 안 됨"이라는 사용자의 실사용 증상과, "첫 재생 이후 오래 쉬었다가 다시 재생을 시도할 때만 실패할 수 있는" 이 메커니즘이 정확히 들어맞음. **고침**: `playViaContext(ctxA, peak)`를 새로 분리해 `if (ctxA.state !== 'running') await ctxA.resume();`로 **실제로 resume이 끝날 때까지 기다린 뒤에야** `knockNote()`를 스케줄하도록 순서를 바꿈(기존의 "예약해두고 나중에 resume이 따라잡을 것"이라는 fire-and-forget 가정 자체를 제거). 여기에 더해 자가 치유 경로 두 개를 추가: (a) `playBeep()`이 매번 `sharedAudioContext.state==='closed'`인지 먼저 확인해, 만약 어떤 이유로든(이 앱이 직접 `close()`를 호출하는 곳은 없지만, 포커스를 못 받는 창에 대한 OS/Chromium 쪽 전력 관리가 컨텍스트 자체를 닫아버리는 경로가 있을 가능성을 배제할 수 없어서) 닫혀 있으면 캐시를 비우고 새로 만듦. (b) `playViaContext` 호출 자체(resume 포함)가 예외를 던지면 catch에서 **공유 컨텍스트를 한 번 더 버리고 새로 만들어 딱 한 번 재시도** — 기존엔 이 지점에서 조용히 삼키고 끝났던 실패를, 재시도 한 번으로 스스로 복구를 시도하게 함. 두 경로 다 "매번 새 컨텍스트를 만드는" 예전 방식으로 되돌아가는 게 아니라(그러면 9차 라운드 이전의 AudioContext 개수 제한 버그가 재발함), 정상 상황에서는 항상 하나의 컨텍스트를 계속 재사용하되 진짜로 망가졌을 때만 예외적으로 다시 만드는 구조. 검증은 스킵했으므로, 실제 30분 이상 간격을 두고 최소 2~3회 알림을 실제로 받아보며 매번 소리가 나는지 다음에 확인 필요 — 이번 수정이 진짜 원인을 맞혔는지는 그 실사용 확인 전까지는 확정된 게 아님(정직하게 기록).

**코드 변경만 하고 회귀 테스트/실제 앱 구동 확인은 사용자 지시대로 스킵함** — `npm run test:invariants` 등 정식 검증은 다음 라운드에서(또는 사용자가 재확인을 요청하면) 실시.

### 알림 소리 재확인 완료(조치 불필요) + 말풍선 간격 재조사(로그 기반) + 꼬리가 몸통 중심을 안 가리키던 문제(새 메커니즘으로 수정) — 이번 라운드도 코드 수정만, 재현 검증은 사용자가 직접
사용자가 세 가지 요청 — (1) 알림 소리는 실제로 간격을 두고 발생하는 알림에서는 정상 재생됨을 확인했다며 **조치 불필요**로 종료. (2) 말풍선 간격이 125% 환경에서도 여전히 벌어져 보인다며(`assets/reference/bubble-gap-still-125percent.png` 첨부) 이번엔 명시적으로 "scaleFactor가 실제로 1.25로 잡히는 환경에서 로그를 찍어서... 원인을 찾아서 고쳐줘"라고 요청 — 검증은 스킵하되 이 진단 자체는 요청받은 작업이라 실제로 수행함. (3) 말풍선 꼬리가 펫의 "특정 부위(꼬리 쪽에 가까운 지점)"를 가리키는 문제(`bubble-old-embedded-reference.png`의 옛 임베디드 버전과 비교) — "스프라이트 전체 가로 폭의 정중앙"을 기준으로 삼도록 요청.

**1. 알림 소리**: 사용자가 이미 확인 완료라고 명시 — 코드 변경 없음.

**2. 말풍선 간격 — 실제 로그로 원인을 찾았지만 "재현 자체가 안 됨" (정직하게 기록)**: `repositionBubbleWindow()`에 `FOCUSPET_DEBUG_GAP` 게이트 임시 로그(요청/승인 y, errorPx, 최종 위치)를 추가하고, 실제 `main.js`를 그대로 `require`해서 scaleFactor 1.25가 실제로 잡히는 이 개발 환경에서 진짜 `show-bubble` IPC를 여러 번(5개 종: `cat_a`/`dog_pomeranian`/`hamster`/`rabbit_b`/`dog_dachshund`)에 걸쳐 트리거해 로그를 확인 — **모든 케이스에서 지난 라운드의 보정 패스가 정상 작동해 최종 간격이 -0.2~1.8px로 수렴**함을 확인(`granted.y`가 요청값과 다를 때도 `errorPx`만큼 정확히 재보정됨). 즉 **현재 코드(main.js에 저장된 그대로)를 깨끗한 프로세스에서 실행하면 사용자가 겪은 것 같은 큰 간격이 전혀 재현되지 않음** — 5종 전부, 사용자 스크린샷과 동일한 메시지 텍스트로도 마찬가지. 스크린샷과 실측 결과가 이렇게 어긋나는 이유로 가장 유력한 설명: **Electron 메인 프로세스 코드는 파일을 저장해도 핫리로드되지 않고, 앱을 완전히 종료 후 재시작해야 반영됨** — 만약 사용자의 펫이 지난 라운드(보정 패스를 추가한 라운드) 이전부터 계속 켜져 있던 프로세스였다면, 그 오래된 인스턴스는 여전히 예전(더 심하게 어긋났던) 코드로 동작 중이었을 것이고, 이는 스크린샷에서 보이는 큰 간격과 정확히 들어맞음. 이 가설을 검증할 방법이 없어 확정은 못 하지만(다른 가능성 — DWM 컴포지팅 지연으로 인한 시각적 지연 등 — 도 완전히 배제 못함), 코드 자체에서 재현 가능한 버그를 못 찾았으므로 **추가 코드 수정은 하지 않되**, 아래처럼 견고성만 보강함: 기존 "요청 후 딱 한 번만 재보정"하던 패스를 **최대 3회까지 반복하는 루프**로 바꿔(`errorPx===0`이면 즉시 종료, 사실상 대부분 1회 안에 끝남) OS의 반올림이 단 한 번의 보정으로 항상 완전히 수렴한다는 보장이 없는 경우에 대한 여유를 더 둠 — 창 **크기**는 여전히 절대 `granted`에서 다시 안 읽음(size creep 재발 방지, 기존 원칙 그대로). `BUBBLE_GAP`은 3→2로 되돌린 지난 라운드 값 그대로 유지(순수 디자인 의도값, 배율별 보정은 여전히 재보정 루프가 전담). **사용자에게 전달할 것**: 재테스트 전에 앱을 (설정 창 닫기가 아니라) 완전히 종료 후 재시작했는지 확인해달라고 안내 필요 — main.js 변경은 항상 전체 재시작이 있어야 반영됨.

**3. 말풍선 꼬리가 몸통 중심이 아니라 다른 지점을 가리키던 문제 — 실제 원인을 알파 채널 스캔으로 찾음**: `main.js`의 `petSpriteCenterX = petBounds.x + SPRITE_INSET_X + SPRITE_WIDTH/2`는 "96px CSS 캔버스 박스 자체의 기하학적 중심"을 계산하는 것이었는데, 이게 항상 "캐릭터가 실제로 보이는 위치의 중심"과 같다고 가정하고 있었음 — 이 가정이 실제로 맞는지 확인하려고, pet.js에 임시 진단 훅(`window.__debugSpriteVisualCenter__`, 캔버스 전체를 `gl.readPixels`로 스캔해 불투명 픽셀이 있는 최좌/최우 열의 중간값을 반환)을 추가해 실제 렌더링 결과를 종별로 측정: `cat_a`/`dog_dachshund`는 assumed center(48px)와 실측이 거의 일치(±1.5px 이내)했지만, **`dog_pomeranian`은 실측 중심이 43~44px로 assumed 48px보다 일관되게 4~5px 왼쪽에 위치**(idle 애니메이션이 계속 도는 6번의 샘플 모두 동일한 방향/크기로 벗어남 — 순간적인 포즈 노이즈가 아니라 이 종 특유의 지속적인 편향임을 확인) — 사용자가 정확히 이런 형태(꼬리 쪽에 가까운 지점을 가리킴)의 통통한/말린 꼬리 강아지 스크린샷을 첨부한 것과 정확히 들어맞음. 회전각을 강제로 스윕해서 확인한 결과(`cat_a` 기준 7개 각도) 회전 자체가 주는 영향은 작았음(±1px 이내) — 즉 이건 회전에 따른 투영 문제가 아니라, 특정 종의 3D 모델 자체(포메라니안처럼 몸통이 둥글고 꼬리가 등 위로 말린 형태)가 자기 96px 캔버스 안에서 애초에 비대칭하게 자리잡는, **종마다 다른, main.js의 고정 상수 하나로는 절대 못 맞추는 문제**임을 확인.
  - **고침 (라이브 리포팅 방식으로 전환)**: 정적 상수 대신, pet.js가 매 프레임 렌더링 직후(`processPendingMouse()`와 같은 자리, 클릭 통과 히트테스트가 이미 쓰고 있는 것과 똑같은 `gl.readPixels` 메커니즘 재사용) 캔버스 전체를 스캔해 실제 불투명 픽셀의 좌우 경계 중간값을 계산하고, **150ms 간격으로 스로틀**(매 프레임 하면 안 됨 — `readPixels`는 GPU 파이프라인을 강제로 동기화시키는 호출이라 그 정도 빈도로도 충분하고, 이미 이 앱의 다른 백그라운드 폴들(main.js의 커서 추적 폴 등)이 쓰는 것과 비슷한 "느리게 갱신해도 되는 값"에 맞는 주기)해 `preload.js`의 새 `reportSpriteCenter(centerX)` → `'sprite-center-report'` IPC로 main.js에 보고. main.js는 `liveSpriteCenterOffsetX`(기본값 `SPRITE_WIDTH/2`=48, 즉 첫 보고가 오기 전까지는 기존 동작과 완전히 동일)로 캐싱하고, `repositionBubbleWindow()`가 이 값을 `SPRITE_WIDTH/2` 대신 사용 — 창을 화면 가장자리에 붙이는 `edgeWindowX`/`SPRITE_INSET_X`(고정 CSS 박스 기준, 창 배치 관심사)는 전혀 안 건드림 — 오직 말풍선 꼬리가 가리키는 지점(시각적 캐릭터 중심, 별개의 관심사)만 바꿈.
  - **새 배선이 실제로 도는지 최소 확인 (재현 검증은 아님)**: 완전히 새로 짠 IPC 배선(코드는 검증됐지만 배선 자체는 미검증)이라, "고쳐졌다"는 걸 스크린샷으로 다시 확인하는 건 스킵했지만 "크래시 없이 돌고 종별로 다른 값을 실제로 만들어내는지"만 가볍게 확인 — `cat_a`와 `dog_pomeranian`으로 각각 말풍선을 띄워 실제 `--tail-x` CSS 값을 읽어보니 4px 차이(84.5px vs 80.5px)가 나서, 앞서 알파 스캔으로 확인한 실측 오프셋 차이(4~5px)와 방향·크기가 일치 — 배선이 최소한 "작동은 한다"는 것까지는 확인. 눈으로 실제 붙어있는지 보는 최종 확인은 사용자 몫으로 남김.
  - **임시 진단 훅(`__debugSpriteVisualCenter__`) 제거 완료** — `grep`으로 pet.js/main.js/preload.js 전체에 흔적 없음 재확인.

**코드 변경만 하고 회귀 테스트/실제 앱 구동 확인은 사용자 지시대로 스킵함** — `npm run test:invariants` 등 정식 검증, 그리고 특히 "완전 재시작 후에도 간격이 여전히 벌어지는지" 확인은 다음에 사용자가 직접.

### 말풍선을 별도 창에서 다시 펫 창 내장 방식으로 되돌림 (구조 단순화, 코드만 수정 — 이번 라운드도 검증 스킵)
사용자가 지금까지의 접근 자체를 재검토 — "말풍선을 별도 `BrowserWindow`로 분리"했던 아키텍처(위 "말풍선을 별도 창으로 분리" 라운드부터, 그 뒤로 간격/중심 정렬 버그가 여러 차례 반복됨)를 포기하고, 펫 창 안에 DOM 요소로 다시 합치기로 결정 — "두 개의 독립적으로 DPI 반올림되는 창이 서로 화면 좌표에 합의해야 하는 구조 자체가, 분리하기 전보다 버그가 줄기는커녕 같은 종류의 버그(간격/중심 어긋남)를 계속 재생산하는 근본 원인이었다"는 판단. 클리핑 방지는 (1) 펫 창 자체를 가장 긴 메시지 기준으로 넉넉하게 고정 크기로 잡고, (2) 펫이 화면 모서리에 가까울수록 창의 여백을 비대칭으로 조정(펫 자신의 화면상 위치는 그대로 유지)하는 방식으로 해결하라는 요청 — 별도 창 관련 코드(`bubbleWindow`, `repositionBubbleWindow`, `sprite-center-report` IPC 등)는 전부 제거, 말풍선 자체의 표시 로직(텍스트/타이밍/페이드)은 유지. **이번 라운드도 사용자 지시대로 실제 검증(재현 테스트/스크린샷)은 스킵 — 코드 수정만 완료.**

**"여백" 방향 해석에 대한 정직한 기록**: 사용자 문구를 그대로 읽으면 "펫이 왼쪽 모서리에 붙어있을 때는 창의 왼쪽 여백을 크게"였는데, 같은 문장의 목적("말풍선이 그 여백 공간 안에서 펼쳐지며 화면 밖으로 안 나가게")과는 기하학적으로 반대 방향임을 확인 — 펫이 왼쪽 screen edge 근처에 있으면 말풍선(펫 중심으로 좌우 대칭하게 뜸)은 왼쪽으로 넘칠 위험이 있으므로, 여유 공간은 펫의 **오른쪽**(화면 안쪽)에 있어야 말풍선이 그 안에서 펼쳐질 수 있음 — 왼쪽에 여백을 더 주면 창 자체가 화면 밖(왼쪽)으로 더 확장될 뿐이고, 그 확장된 영역은 화면 밖이라 OS가 그리지도 않으므로 말풍선이 거기 놓일 수도 없음. 목적 문장과 정면으로 모순되는 문자 그대로의 해석을 따르면 클리핑을 막기는커녕 그대로 재현하거나 악화시키는 결과가 되므로, **목적("펼쳐지며 화면 밖으로 안 나가게")을 기준으로 구현** — 펫이 왼쪽 모서리에 가까울수록 창의 **오른쪽**(펫 기준 화면 안쪽) 여백이 커지고, 오른쪽 모서리에 가까울수록 **왼쪽** 여백이 커지는 방향으로 구현함. 사용자가 실제로 의도한 것과 다르면 즉시 알려주면 반대로 뒤집는 건 한 함수(`spriteWindowLayout`)의 부호 하나만 바꾸면 되는 간단한 수정.

**설계 (main.js)**:
- **`PET_SIZE`를 96x107(스프라이트에 꼭 맞는 크기)에서 420x400(고정, 넉넉한 크기)으로 확대**: 너비는 `#bubble{max-width:190px}`(pet.css, CSS로 항상 상한이 걸려 있어 메시지가 아무리 길어도 폭 자체는 안 늘어남 - 줄바꿈만 늘어남) + 패딩/그림자 여유를 더한 최대 말풍선 폭(~230px) 기준으로, 스프라이트(96px)+최소 여백(16px, 아래)+여유 있는 반대쪽 공간(~230px+여유)을 계산해 필요한 최소치(~366px)보다 넉넉하게(420px) 잡음. 높이는 스프라이트(96)+바닥 여백(3)+말풍선이 아주 긴 메시지(여러 줄 줄바꿈)로 늘어나도 여유 있게 커버할 수 있는 높이(~290px)를 더해 400px로 설정 — 100%/125% 두 배율 모두에서 이 프로젝트가 반복적으로 관측해온 몇 px 수준의 DPI 반올림 오차를 가볍게 흡수하고도 남는 수준의 여유.
- **`spriteWindowLayout(spriteScreenX, display)`(신규)**: `PET_SIZE.width`의 여유폭(width-96)을 스프라이트의 좌/우 여백으로 "비대칭 분배"하는 함수 — 스프라이트가 화면 왼쪽 끝에 가까울수록(t→0) 왼쪽 여백은 최소값(`MIN_SPRITE_MARGIN`=16px, 그림자/DPI 안전 여유)까지 줄고 나머지 전부가 오른쪽으로, 오른쪽 끝에 가까울수록(t→1) 그 반대가 되도록 **연속적으로**(하드 2단계 스위치가 아니라 화면 안에서의 상대 위치 `t=(spriteScreenX-workArea.x)/(workArea.width-96)`에 대한 선형 보간) 계산 — 화면 중앙 부근(어느 모서리에도 안 붙어있는, 자유 드래그로 떨어뜨린 위치)에서는 자연히 거의 대칭(기존 동작과 거의 동일)으로 수렴. **창의 origin(`winX`)과 스프라이트의 window-local 오프셋(`localX`)을 함께 반환** — 이 둘은 `winX+localX=spriteScreenX`라는 불변식으로 묶여 있어, 이 함수를 쓰는 모든 호출부는 반드시 둘을 같이 적용(따로 쓰면 스프라이트 화면 위치가 어긋남).
- **`edgeSpriteX(side, display)`(구 `edgeWindowX`를 대체)**: "스프라이트"가 화면 모서리에서 `EDGE_MARGIN`만큼 떨어진 지점의 화면 X — 창 레이아웃과 완전히 독립적인 순수한 "스프라이트가 어디 있어야 하는가" 계산으로 분리(기존 `edgeWindowX`는 이 둘이 뒤섞여 있었음 — 창이 항상 스프라이트에 꼭 맞는 크기였을 때는 구분할 필요가 없었지만, 이제 창이 스프라이트보다 훨씬 크므로 반드시 분리해야 함).
- **`currentSpriteLocalX`(모듈 전역) + `pushSpriteLocalX(win, localX)`**: main.js가 마지막으로 pet.js에 알려준 로컬 오프셋을 기억해두는 캐시(펫 창 자신의 `getBounds()`를 다시 읽어서 재활용하는 방식은 절대 안 씀 — 이 프로젝트가 이미 문서화한 "읽은 값을 다시 setBounds에 먹이면 창 크기가 무한 증식하는 버그"와 같은 패턴을 피하기 위해, 항상 "우리가 마지막으로 보낸 값"만 신뢰) — pacing이 "스프라이트가 지금 화면 어디 있는지"를 매 프레임 알아야 하고(엣지 도달 판정), drop 애니메이션이 "드래그 종료 시점에 어떤 로컬 오프셋에서 시작해야 하는지" 알아야 하는 두 곳에서 재사용.
- **"움직이는 동안은 로컬 오프셋 고정, 정지할 때만 비대칭 레이아웃 재계산" 원칙**: 드래그 스프링-팔로우/페이싱 도중의 매 틱은 `localX`를 **절대 안 건드림**(창의 `winX`만 이동) — 만약 움직이는 도중에 `localX`를 계속 재계산했다면, (a) 드래그의 "잡은 지점이 커서를 계속 따라간다"는 불변식(`offset.offsetX`가 mousedown 시점의 로컬 좌표를 그대로 재사용하는 구조라, 그 로컬 좌표계 자체가 도중에 바뀌면 잡은 지점이 커서에서 이탈함)이 깨지고, (b) drop 애니메이션 도중 `localX`를 매 프레임 목표값으로 갑자기 바꾸면 스프라이트의 실제 화면 위치(`winX+localX`)가 순간적으로 크게 튀는(최대 ~150px) 점프가 생김(직접 계산으로 확인 후 회피) — 대신 drop 애니메이션은 X와 함께 `localX`도 **같은 이징 곡선으로 함께 보간**(시작 로컬값 → 목표 로컬값)해서 두 값의 합(=진짜 화면 위치)이 애니메이션 내내 매끄럽게 움직이도록 함(스냅 대상이 없는 "그 자리에 그대로 두기" 케이스는 시작=목표라 이 보간 자체가 자동으로 no-op). 페이싱의 엣지 도달은 반대로 순간적 이벤트(매 프레임 몇 px씩 다가가다가, "도달했다" 판정이 나는 바로 그 틱에 `winX`/`localX`를 동시에 최종값으로 맞춤)라 별도 보간 없이도 자연스럽게 이어짐(도달 직전 프레임의 화면 위치가 이미 거의 그 지점 근처였으므로).

**설계 (pet.js) — 말풍선을 다시 순수 렌더러 로컬 기능으로**:
- `#bubble`/`#bubble-text`가 다시 `pet/index.html`(`#stage`의 직계 자식, `#pet-wrap`의 96px 폭에 갇히지 않도록 - 말풍선이 창의 훨씬 넓은 범위까지 이동해야 하므로)로 돌아옴, 스타일은 예전 `windows/bubble/bubble.css`를 그대로 `pet.css`에 이식(꼬리 삼각형/그림자/페이드 트랜지션 등 시각적으로 동일).
- **`showBubble()`이 더 이상 IPC를 보내지 않고** 텍스트 설정 → `positionBubble()`(아래) → `.show` 클래스 트랜지션까지 전부 로컬에서 처리 — `main.js`를 거칠 이유 자체가 없어짐(별도 창이 없으므로). 리마인더 경로(`onReminder`)와 클릭 반응(poke) 경로 둘 다 이 하나의 로컬 함수를 그대로 호출(호출부 자체는 무변경).
- **`positionBubble()`**: 예전 `repositionBubbleWindow()`/그보다 더 예전의 최초 임베디드 버전이 쓰던 것과 똑같은 "박스를 밀고 꼬리를 반대로 되민다" 클램프 로직을 창-로컬 좌표계로 그대로 재구현 — 스프라이트 중심에 기본적으로 정렬하되, 창의 (넉넉한) 경계를 벗어나면 클램프. `bubbleEl.offsetWidth`로 실제 laid-out 폭을 매번 다시 읽어(메시지 길이가 바뀔 때마다 자동 반영) 계산.
- **`x`(스프라이트의 창-로컬 X)가 더 이상 pet.js 자체적으로 "항상 중앙 정렬"되지 않음** — main.js의 `sprite-local-x` IPC 푸시(`onSpriteLocalX`)로만 갱신됨. 예전에 있던 "라이브 스테이지 폭이 드리프트하면 재중앙"(`xCenteredForWidth`) 로직은 이제 무의미해져 삭제 — 애초에 "중앙"이 목표가 아니게 됐으므로.
- **제거한 것**: `sprite-center-report`/`reportSpriteVisualCenter`(캔버스 알파 스캔으로 종별 시각적 중심을 찾아 말풍선 꼬리를 정렬하던, 직전 라운드에 추가한 기능) — 사용자가 이번 라운드에서 명시적으로 제거 대상에 포함시킴. 이제 말풍선 꼬리는 다시 (전 라운드 이전과 마찬가지로) 스프라이트의 96px CSS 박스 자체의 기하학적 중심을 가리킴 — 포메라니안처럼 박스 안에서 실제 캐릭터가 비대칭하게 자리잡는 종은 다시 미세하게 어긋나 보일 수 있음(알려진 한계로 아래 기록, 되살리려면 그 라운드의 메커니즘을 그대로 복원하면 됨 - 삭제됐을 뿐 설계 자체는 CLAUDE.md 히스토리에 남아있음).

**제거된 것 전체 목록**: main.js의 `bubbleWindow`/`createBubbleWindow()`/`showBubbleWindow()`/`repositionBubbleWindow()`/`hideBubbleImmediately()`/`BUBBLE_GAP`/`BUBBLE_SCREEN_MARGIN`/`bubbleContentWidth`/`bubbleContentHeight`/`bubbleTailOffsetFromTop`/`liveSpriteCenterOffsetX`/`'show-bubble'` 핸들러/`'sprite-center-report'` 핸들러, preload.js의 `showBubble`/`reportSpriteCenter` 브릿지, `windows/bubble/` 디렉터리 전체(`index.html`/`bubble.css`/`bubble.js`) 삭제.

**코드 수정만 완료, 검증은 사용자 지시대로 스킵** — 문법 체크(`node --check`)와 잔여 참조 `grep`(별도 창/구 IPC 채널 관련 문자열이 저장소 어디에도 안 남아있음 확인)만 했고, 실제 앱 구동/스크린샷/`npm run test:invariants`는 실시하지 않음.

### 트레이 메뉴 기능 설정창 이중화 + 펫 숨김-말풍선만 표시 + 편의 기능 4종 (사용자 지시로 검증 스킵 — 코드 수정만)
사용자가 세 가지를 한 번에 요청: (1) 트레이 우클릭 메뉴 전용이던 기능(알림 일시정지/재개, 펫 숨기기/보이기, 지금 바로 알림 테스트)을 설정 창에서도 켜고 끌 수 있게, 양방향 동기화, (2) "펫 숨기기"를 해도 알림 트리거는 계속 정상 작동하고 그 순간엔 스프라이트 없이 말풍선만 작업표시줄 위 중앙에 뜨도록, 다시 보이기 하면 원래 위치로 복귀, (3) 편의 기능 4종(Windows 시작 시 자동 실행 / 말풍선 스누즈 버튼 / 오늘 하루 알림 끄기 / 전체 설정 초기화) — 이번 프로젝트 구조상 넷 다 무리 없이 들어맞는다고 판단해 전부 구현(뺀 것 없음). **사용자가 명시적으로 검증(테스트 실행/실제 앱 구동/스크린샷)을 생략하라고 지시** — `node --check` 문법 검사와 IPC 채널/DOM id 전수 대조(`grep`)만 하고 종료.

**단일 소스 오브 트루스로 양방향 동기화 (`main.js`)**: 트레이 클릭 핸들러와 설정창의 새 IPC 핸들러 모두 상태를 직접 건드리지 않고, 딱 하나씩만 있는 공유 함수(`setPaused(paused)`/`setPetVisible(visible)`)를 통해서만 상태를 바꾸도록 리팩터링 — 두 함수 다 끝에서 `refreshTrayMenu()`(트레이 라벨 갱신)와 `broadcastStatus()`(설정창이 열려 있으면 `'status-updated'` IPC로 현재 상태 푸시)를 호출하므로, 트레이에서 바꾸든 설정창에서 바꾸든 두 표면이 절대 어긋날 수 없음. `getStatus()`(`{paused, petVisible, offForToday}`)를 새 `'get-status'`(invoke)/`'status-updated'`(push) 채널로 노출 — 설정창은 `init()`에서 한 번 가져오고 이후 이 push로 계속 최신 상태를 반영.

**"펫 숨기기" — OS 레벨 `hide()` 대신 논리적 플래그로 재설계**: 기존엔 `petWindow.hide()`로 창 자체를 OS 레벨에서 숨겼는데, 말풍선(`#bubble`)이 그 창의 DOM 자식으로 내장되어 있어서(직전 라운드 "말풍선을 다시 펫 창에 내장" 참고) 창을 숨기면 말풍선도 통째로 안 뜨는 구조적 문제가 있었음 — 요구사항("숨긴 상태에서도 알림은 계속, 그 순간엔 말풍선만") 자체가 이 구조와 충돌. **해결**: 창은 계속 OS-visible 상태로 두고, 순수 로직 플래그 `petVisible`(main.js)만 토글 — `pet-hidden-state` IPC로 렌더러에 알리면 `pet.js`가 `#pet-wrap`에 `.pet-hidden`(`display:none`) 클래스를 토글해 스프라이트만 안 그림(`display:none`이 `getBoundingClientRect()`를 0-rect로 만들어서 클릭 통과 히트테스트도 별도 분기 없이 자동으로 완전 통과 상태가 됨 — 기존 `sampleHit()`의 zero-rect 가드를 그대로 재사용). `tick()`의 gait/포즈/커서추적/페이싱 블록 전체를 `if (animal && !petHidden)`로 감싸 스킵(안 보이는 스프라이트를 애니메이션할 이유가 없음), `render()`도 `petHidden`이면 렌더 자체를 건너뜀 — 단 말풍선 포지셔닝(`positionBubble()`)과 클릭 통과 판정(`processPendingMouse()`)은 **무조건 계속 실행**해서 숨김 중에도 말풍선이 정상 동작.
- **숨길 때**: `main.js`가 창을 화면 중앙(`centerSpriteX(display)` — "작업표시줄 위 중앙") 위치로 옮기기 **전에** 현재 위치(`{x, y, localX: currentSpriteLocalX}`)를 `preHideState`에 저장.
- **다시 보이기**: `preHideState`를 그대로 복원(재계산 없이 바이트 그대로) — "원래 있던 위치로 정확히 복귀"를 좌표 재계산 없이 보장.

**말풍선 스누즈 버튼**: `#bubble`에 `<button id="bubble-snooze">10분 뒤 다시</button>` 추가(`pet/index.html`) — `#bubble` 자체는 여전히 `pointer-events:none`(장식용)이지만 이 버튼만 `pointer-events:auto`로 오버라이드, `pet.js`의 클릭 통과 히트테스트(`processPendingMouse`)에 `isOverSnoozeButton()`(버튼 `getBoundingClientRect()` 체크)을 OR로 추가해 실제 OS 클릭이 버튼까지 전달되게 함. `showBubble(message, durationMs, showSnooze)`에 세 번째 인자 추가 — 리마인더(`onReminder`)만 `showSnooze:true`로 호출, poke 반응은 그대로 `false`(기본값)라 버튼 자체가 안 보임. 클릭 시 `fadeOutBubble()`(기존 타임아웃 경로와 동일한 페이드아웃)로 즉시 닫고 `window.focusPetAPI.snoozeReminder(message)` 전송 — `main.js`의 `'snooze-reminder'` 핸들러가 `setTimeout(() => sendReminderMessage(msg), 10*60*1000)`로 10분 뒤 같은 메시지를 재발송. `sendReminderMessage()`는 일반 타이머 경로(`fireReminder`)와 스누즈 경로가 공유하는 단일 게이트 함수로 분리해, 스누즈로 예약된 알림도 그 사이에 일시정지/오늘-하루-끄기가 켜지면 정상적으로 억제됨(우회 불가).

**"오늘 하루 알림 끄기"**: `settingsStore.js`에 `offForTodayDate`(YYYY-MM-DD 문자열 또는 null) 추가 — `paused`와 별개로, 자정이 지나면 자동으로 풀림. 스케줄 타이머(`setTimeout(msUntilMidnight)`) 대신 "기회주의적 재확인" 패턴 채택: `clearStaleOffForToday()`가 저장된 날짜와 오늘 날짜를 비교해 다르면 지움 — 이미 항상 돌고 있는 `startAlwaysOnTopPoll()`(3초 간격)에 얹어서 별도 타이머 없이 몇 초 안에 자연 갱신됨(랩톱 절전모드로 자정을 넘기는 경우에도 예약 타이머보다 안전 — 이 프로젝트의 다른 poll들과 같은 "정확한 스케줄보다 짧은 주기 재확인" 철학).

**Windows 시작 시 자동 실행**: `settingsStore.js`에 `launchAtStartup`(boolean) 추가, `main.js`의 `applyLoginItemSettings()`가 `app.setLoginItemSettings({openAtLogin: !!settings.launchAtStartup})`를 앱 시작 시 1회 + 설정 저장(`save-settings`) 때마다 재적용(멱등이라 매번 불러도 무해).

**전체 설정 초기화**: `settingsStore.js`의 `load()`가 이미 `{...DEFAULTS, ...parsed}` 병합 패턴이라, `saveSettings(fullDefaultsObject)`를 그대로 호출하는 것만으로 완전한 초기화가 성립 — 별도 main.js/preload.js 배선 없이 `settings.js`에서 클라이언트 사이드로 구현(`confirm()` 다이얼로그로 되돌릴 수 없는 동작임을 확인받은 뒤 `getDefaults()`→`saveSettings(defaults)`→`fillForm(defaults)`→`previewCharacter()`→`getStatus()`→`fillStatus()` 순으로 처리). 기존 "기본값으로"(`resetBtn`, 드래프트 폼만 채우고 저장은 안 함 — 캐릭터별 항목 미리보기 용도)와 footer에서 시각적으로 분리(`resetAllBtn`은 `button.danger-link` 스타일로 좌측에, 저장/기본값으로는 `.footer-actions`로 묶어 우측에 — `justify-content:space-between`).

**설정창 "빠른 제어" 카드**: 캐릭터 카드보다 앞, 맨 위에 배치 — 일시정지/펫 표시/오늘 하루 끄기 토글 3개(`.toggle-row` 기존 패턴 재사용) + "지금 바로 알림 테스트" 버튼. 이 토글들은 드래프트 폼 상태(`fillForm`)가 아니라 **라이브 상태**(`fillStatus`, `getStatus`/`onStatusUpdated`)를 반영 — 클릭 즉시 반영(저장 버튼 불필요), main.js가 push하는 `status-updated`로 트레이 쪽 변경도 실시간 반영됨. "일반" 카드(Windows 자동 실행)는 알림 소리 카드와 메시지 카드 사이에 배치.

**검증 (사용자 지시로 최소화)**: `node --check`를 `main.js`/`preload.js`/`settingsStore.js`/`windows/pet/pet.js`/`windows/settings/settings.js` 전부에 실행 — 전부 통과. `ipcMain.on/handle` ↔ `ipcRenderer.send/invoke`/`preload.js`의 `focusPetAPI` 노출 목록 ↔ `pet.js`/`settings.js`의 실제 사용처, 그리고 `main.js`의 `webContents.send` push 채널 ↔ `preload.js`의 `ipcRenderer.on` 전부를 `grep`으로 전수 대조 — 어긋남 0건. `settings/index.html`/`pet/index.html`의 DOM id ↔ 각 JS의 `getElementById()` 호출도 전수 대조 — 어긋남 0건. **실제 앱 구동/스크린샷/`npm run test:invariants`는 이번 라운드에서 실시하지 않음**(사용자 지시).

### 리셋 버튼 두 개 정리 + 펫 숨김 전환 플리커/말풍선 위치 수정 4건 (사용자 지시로 검증 스킵 — 코드 수정만)
직전 라운드(트레이 기능 설정창 이중화 + 펫 숨김-말풍선 + 편의 기능 4종) 이후 실사용 리포트 4건. 이번에도 사용자가 명시적으로 검증(테스트 실행/실제 앱 구동/스크린샷)을 생략하라고 지시 — `node --check`와 IPC/DOM 전수 대조만 하고 종료.

**1. "전체 설정 초기화" vs "기본값으로" — 실제로 다른 동작이었음, 라벨/설명으로 구분**: 코드를 직접 대조해본 결과 완전히 같은 동작이 아니었음 — `resetBtn`("기본값으로")는 `fillForm(defaults)`로 **드래프트 폼만** 기본값으로 채울 뿐 `saveSettings()`를 전혀 호출하지 않아서, 사용자가 뒤이어 "저장"을 누르지 않으면 아무 것도 실제로 안 바뀜(설정창을 그냥 닫으면 `settingsWindow`의 `'closed'` 핸들러가 마지막으로 실제 저장된 값을 다시 보내 원상복구됨). `resetAllBtn`("전체 설정 초기화")는 `confirm()` 확인 후 `saveSettings(defaults)`를 **즉시 호출해 곧바로 영구 저장**함 — "캐릭터별 항목만 vs 전체"라는 스코프 차이는 아니었음(`fillForm`도 launchAtStartup/movementMode 등 전 필드를 다 채움, 스코프 자체는 동일) — 진짜 차이는 순전히 **"언제 실제로 적용되는가"**(저장을 눌러야 하는 미리보기 vs 확인만 하면 즉시 영구 반영)였음. 이 차이가 실사용에서 의미 있는 기능(미리보기 후 손으로 몇 개 더 조정한 뒤 저장 vs 원클릭 완전 초기화)이라 둘 다 유지하되, 사용자에게 이 시점 차이가 라벨만 봐서는 안 보였던 게 진짜 문제라 판단 — 라벨/설명을 명확히 구분:
  - `resetBtn` 라벨을 "기본값으로" → **"기본값으로 채우기"**로 변경("채우기"가 "즉시 적용되는 액션"이 아니라 "폼에 값을 채워 넣는 것"이라는 인상을 줌) + `title` 툴팁 "폼에 기본값을 채워 넣을 뿐, 저장을 눌러야 실제로 적용돼요".
  - `resetAllBtn`은 라벨 그대로("전체 설정 초기화") + `title` 툴팁 "확인 후 모든 설정을 즉시 기본값으로 되돌리고 저장까지 끝내요 (되돌릴 수 없어요)".
  - 툴팁은 호버해야만 보이므로 발견성이 낮다고 판단해, footer 자체에 상시 노출되는 캡션 한 줄도 추가: `<p class="footer-hint">기본값으로 채우기는 저장을 눌러야 적용돼요 · 전체 설정 초기화는 확인 후 바로 저장까지 끝나요</p>` — 이걸 넣기 위해 `footer`를 `flex-direction:column`(캡션 줄 + 버튼 줄 2단)으로 재구성, 기존 버튼 줄은 `.footer-row`로 감싸서 그대로 유지(`justify-content:space-between`은 그 안으로 이동, `resetAllBtn`이 여전히 좌측 danger-link, 나머지 두 버튼이 여전히 우측 `.footer-actions`).

**2. 펫 표시 끄기 시 화면 중앙으로 순간 점멸 후 사라짐 — opacity 0으로 전환 구간 전체를 감쌈**: 원인은 `setPetVisible`의 hide 분기가 `setBounds()`로 창을 먼저 옮기고 **그 다음에** `'pet-hidden-state'` IPC를 보내던 순서 — `setBounds()`는 그 순간 창에 이미 그려져 있던 내용(아직 스프라이트가 그대로 보이는 프레임)을 그대로 들고 새 위치로 동기적으로 옮기는데, 렌더러가 IPC를 받아 `.pet-hidden`을 적용하고 실제로 리페인트하는 건 그보다 나중(비동기)이라 — 그 사이의 프레임이 "새 위치에서 잠깐 스프라이트가 보였다가 사라짐"으로 관측된 것. IPC를 먼저 보내는 순서 교체만으로는 안 고쳐짐(IPC 전달+렌더러 리페인트가 비동기라 뒤이은 동기 `setBounds()` 호출보다 먼저 끝난다는 보장이 없음, 순서를 바꿔도 여전히 경쟁 상태). **고침**: 순서로 경쟁을 이기려 하는 대신, 전환 구간 전체를 `petWindow.setOpacity(0)`로 완전히 안 보이게 만든 뒤, 렌더러가 실제로 적용 완료를 확인해준 뒤에만(`'pet-hidden-applied'` ack) `setOpacity(1)`로 복원 — opacity 0이면 그 순간 창에 뭐가 그려져 있든(옛 스프라이트든, 옮겨진 새 위치든) 사용자 눈에 아예 안 보이므로 타이밍 경쟁 자체가 무의미해짐. `armPetHiddenOpacityRestore()`(main.js, 신규)가 `ipcMain.once('pet-hidden-applied', ...)`로 ack를 기다리되, 렌더러가 죽거나 IPC가 씹히는 경우에 대비해 300ms 타임아웃 세이프티넷도 같이 걺(먼저 도착하는 쪽이 처리, 나머지는 `removeListener`로 정리해 중복/누수 방지). `pet.js`의 `onPetHiddenState` 핸들러는 `.pet-hidden` 클래스를 토글한 뒤 **더블 `requestAnimationFrame`**(한 번이 아니라 두 번 — 첫 rAF는 "다음 페인트에 이 변경이 반영될 차례"만 보장하고, 실제로 그 페인트가 끝났다는 보장은 두 번째 rAF에서야 나옴)으로 실제 페인트가 반영된 뒤에야 `notifyHiddenApplied()`로 ack를 보냄. 이 opacity 가드는 숨기기/보이기 양방향 다 적용(보이기 쪽은 원래 투명한 창을 옮기는 것이라 이론상 깜빡임이 없어야 하지만, 만약 그 순간 말풍선이 떠 있는 상태였다면 같은 종류의 점프가 말풍선에도 보일 수 있어서 — 별도 요청받은 범위는 아니지만 opacity 가드가 이미 양방향에 걸리는 공용 함수라 자연히 같이 커버됨).

**3/4. 펫 숨김 상태의 말풍선 위치 — 우측 하단 고정 + 작업표시줄에 밀착**: 두 리포트가 사실상 하나의 근본 원인(숨김 상태 전용 말풍선 위치 로직이 아예 없었음)의 두 측면이라 같이 고침.
  - **가로(우측 고정)**: 기존엔 hide 시 창을 화면 가로 중앙(`centerSpriteX`)으로 옮기고 있었음 — `centerSpriteX` 함수를 완전히 삭제하고, 대신 드래그/페이싱이 이미 쓰는 것과 같은 **우측 엣지 좌표**(`edgeSpriteX('right', display)`)로 `spriteWindowLayout()`을 계산해 창을 옮기도록 변경 — `positionBubble()`의 기존 "스프라이트 중심에 맞춰 말풍선을 정렬" 로직이 그대로 이 새 우측 위치를 따라가므로, 별도의 "숨김 모드 전용 가로 위치 계산"을 새로 안 짜도 우측에 자연히 정렬됨(이미 검증된 우측-엣지 정렬/클램프 로직 재사용). Y는 원래도 항상 `groundWindowY`(작업표시줄에 항상 붙어있음)라 안 건드림 — 우측(가로)+ground(세로)가 합쳐져서 "우측 하단"이 됨.
  - **세로(작업표시줄 밀착)**: `#bubble{bottom:101px}`(펫이 보일 때 스프라이트 머리 위 여백)는 "96px짜리 보이는 스프라이트 위로 여백을 확보한다"는 값이라, 스프라이트가 숨겨진 상태에선 그 96px이 통째로 빈 공간이 되어 "작업표시줄에서 한참 떠 보임"으로 관측된 것(픽셀 오프셋 자체는 안 보이는-보이는 상태 사이에 전혀 안 바뀌지만, 그 공간을 채워주던 스프라이트 실루엣이 없어지니 시각적으로 훨씬 크게 느껴짐). **고침**: `pet.css`의 `#bubble`이 `bottom: var(--bubble-bottom, 101px)`을 쓰도록 바꾸고, `pet.js`가 `onPetHiddenState`에서 숨김 진입 시 `--bubble-bottom`을 `HIDDEN_BUBBLE_BOTTOM`(16px — 스프라이트가 없을 때 "발이 작업표시줄에 거의 닿는" 수준의 여백)으로, 숨김 해제 시 `removeProperty`로 되돌려(기본값 101px 복원) 스왑.

**전체 변경 파일**: `main.js`(`centerSpriteX` 삭제, `armPetHiddenOpacityRestore` 신규, `setPetVisible`이 opacity 가드+우측 앵커 사용하도록 재작성), `preload.js`(`notifyHiddenApplied` 브릿지 추가), `windows/pet/pet.js`(`HIDDEN_BUBBLE_BOTTOM` 신규, `onPetHiddenState`가 `--bubble-bottom` 스왑 + 더블 rAF ack 전송), `windows/pet/pet.css`(`#bubble{bottom}`이 커스텀 프로퍼티 참조하도록), `windows/settings/index.html`/`settings.css`/`settings.js`(footer 2단 재구성 + 두 리셋 버튼 라벨/툴팁 구분, `resetBtn` 핸들러 주석 정리).

**검증 (사용자 지시로 최소화)**: `node --check`를 수정한 5개 JS 파일 전부에 실행 — 전부 통과. `ipcMain.on/handle/once` ↔ `ipcRenderer.send/invoke` 전수 대조 — `pet-hidden-applied`(신규, renderer→main `.send`/main `ipcMain.once`) 포함 전부 일치, 어긋남 0건. `settings/index.html`의 DOM id ↔ `settings.js`의 `getElementById()` 호출 전수 대조(footer 재구성 이후에도) — 어긋남 0건. `centerSpriteX` 삭제 후 잔여 참조 `grep` — 0건. **실제 앱 구동/스크린샷/`npm run test:invariants`는 이번 라운드에서 실시하지 않음**(사용자 지시) — 특히 opacity 가드의 실제 타이밍(더블 rAF + 300ms 세이프티넷이 실사용에서 진짜로 깜빡임을 없애는지)과 우측 하단 말풍선 위치의 실측 확인이 다음에 필요.

### GitHub Actions로 Windows/Mac 동시 배포 (GitHub Releases, 사용자 지시로 실제 태그 push/빌드 실행은 안 함 — 설정 파일 작성 + 문법 검증까지만)
개발 환경이 Windows뿐이라 Mac 빌드(macOS에서만 가능)를 로컬로 만들 수 없어서, 버전 태그를 push하면 GitHub Actions가 `windows-latest`/`macos-latest` 두 러너에서 각각 빌드해 같은 GitHub Release에 자동 첨부하도록 구성. 사용자가 실제 태그 push/빌드 실행은 직접 하겠다고 명시 — 이 라운드는 설정 파일 작성과 문법 검증(`package.json`은 `require()`로 JSON 파싱, `.github/workflows/release.yml`은 `js-yaml`로 파싱)까지만 하고 실제 워크플로우 실행/릴리즈 생성은 하지 않음.

**`package.json`의 `build` 설정**:
- `win`: 기존 `nsis` 타겟 유지, `artifactName: "${productName}-${version}-Setup.${ext}"` 추가 — 버전이 파일명에 명시적으로 들어가도록(electron-builder의 nsis 기본 artifactName에도 보통 버전이 들어가지만, "명시적으로"라는 요청에 맞춰 기본값에 기대지 않고 직접 지정).
- `mac`: 기존 `dmg` 타겟 유지, `artifactName: "${productName}-${version}.${ext}"`(win과 대칭으로 동일한 이유 — 이번 요청에서 win에만 명시됐지만 mac 쪽도 명시해두는 게 두 플랫폼 파일명 규칙을 일관되게 유지하는 데 도움이 돼서 같이 넣음) + **`identity: null`** 추가 — 이 저장소엔 애플 개발자 서명 인증서가 없어서, `identity`를 명시적으로 `null`로 주면 electron-builder가 키체인에서 인증서를 자동 탐색하려다 실패해 CI 빌드가 죽는 상황 자체를 원천적으로 피함(electron-builder 공식 문서가 "서명을 명시적으로 끄는" 방법으로 문서화해둔 값). 워크플로우 쪽 `CSC_IDENTITY_AUTO_DISCOVERY=false` 환경변수(아래)와 이중으로 겹쳐 걺 — 사용자가 "identity: null 또는 CSC_IDENTITY_AUTO_DISCOVERY=false" 둘 중 하나를 제안했는데, 서로 다른 레이어(빌드 설정 파일 vs CI 환경변수)라 겹쳐도 해될 게 없어서 벨트-앤-서스펜더로 둘 다 적용.
- `publish: {provider: "github"}` 추가 — electron-builder의 GitHub publish provider가 owner/repo를 어디서 읽어오는지 확인하려고 `git remote -v`로 이 저장소의 실제 origin(`https://github.com/yoobilee/focus-pet.git`)을 확인한 뒤, 최상위에 표준 npm `"repository"` 필드(`{type:"git", url:"https://github.com/yoobilee/focus-pet.git"}`)를 새로 추가 — electron-builder가 `publish.owner`/`publish.repo`를 명시적으로 안 줘도 이 `repository` 필드에서 자동으로 읽어가므로(공식적으로 지원되는 동작), 워크플로우 안에서 조차 owner/repo를 하드코딩할 필요가 없어짐. `repository` 필드가 없었다면 electron-builder가 git 원격(origin)에서 추론을 시도하긴 하지만, 명시적으로 필드를 채워두는 쪽이 더 확실함.

**`.github/workflows/release.yml`(신규)**:
- **트리거**: `on.push.tags: ['v*.*.*']` — `v1.0.0`, `v1.2.3`류 태그 push에만 반응, 일반 커밋 push로는 절대 안 켜짐.
- **`permissions: contents: write`**: GitHub Actions의 기본 제공 `GITHUB_TOKEN`이 저장소/조직 설정에 따라 기본값이 read-only일 수 있어서(2023년 이후 신규 저장소는 보통 read-only가 기본) — Release 생성/에셋 업로드에 필요한 write 권한을 워크플로우 레벨에서 명시적으로 켜둠(요청엔 없었지만 이게 없으면 electron-builder의 publish 단계 자체가 권한 부족으로 실패할 수 있어서 필수로 판단해 추가).
- **`build-windows`(`windows-latest`)/`build-mac`(`macos-latest`) 두 개의 독립된 job**: 서로 `needs:`로 안 엮여 있어서 GitHub Actions가 기본적으로 병렬 실행 — "두 OS 빌드가 서로 독립적으로(병렬로) 실행되게"를 별도 설정 없이 "그냥 needs를 안 넣는다"로 충족. 각 job: `actions/checkout@v4` → `actions/setup-node@v4`(Node 20, `cache: npm`으로 `package-lock.json` 기반 캐싱) → `npm ci` → `npm run build -- --publish always`. 마지막 스텝이 요청의 "npm ci && npm run build"와 "electron-builder의 --publish always 옵션 활용"을 동시에 충족하도록, 기존 `package.json`의 `"build": "electron-builder"` 스크립트를 그대로 쓰되(수정 안 함) `npm run build -- --publish always`로 `--publish always`를 CLI에 추가로 전달 — `npm run <script> -- <args>`는 `--` 뒤 인자를 그대로 밑단 커맨드(`electron-builder`)에 전달하는 표준 npm 문법.
  - Windows job: `GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}`만 필요 — electron-builder의 GitHub publish 로직은 (GitHub Actions가 표준 제공하는) `GITHUB_TOKEN`이 아니라 자기만의 `GH_TOKEN`이라는 이름의 환경변수를 읽으므로, `secrets.GITHUB_TOKEN`(별도 시크릿 등록 없이 모든 워크플로우 실행마다 자동 발급되는 값)의 값을 그 이름으로 노출만 해줌 — 사용자가 "GITHUB_TOKEN 시크릿 사용"이라고 요청한 것과 정확히 일치(추가로 발급받아야 할 PAT 없음).
  - Mac job: 같은 `GH_TOKEN`에 더해 `CSC_IDENTITY_AUTO_DISCOVERY: false`도 같이 줌(위 `package.json`의 `identity: null`과 이중 방어).
- **병렬 publish가 같은 GitHub Release에 안전하게 같이 올라가는지**: electron-builder의 GitHub publish provider는 대상 태그에 이미 Release가 있으면 재사용하고 없으면 새로 만드는 방식이라, 두 job이 거의 동시에 `--publish always`를 실행해도 (전자가 먼저 Release를 만들면 후자는 거기에 에셋만 추가하는 식으로) 안전하게 수렴함 — electron-builder + GitHub Actions 멀티 OS 릴리즈에서 이미 널리 쓰이는 표준 패턴이라 이 프로젝트에 특별한 추가 조치는 필요 없다고 판단.

**YAML 문법 검증**: 이 저장소의 `node_modules/js-yaml`(electron-builder의 전이 의존성으로 이미 설치돼 있던 걸 발견, 별도 설치 없이 재사용)로 `.github/workflows/release.yml`을 직접 파싱해 JS 객체로 확인 — 에러 없이 파싱되고, `on`/`jobs`/`permissions`/각 job의 `steps` 배열까지 의도한 구조 그대로 나옴을 확인. (참고: `on:`이라는 키가 YAML 1.1 스펙 하의 일부 파서에서는 boolean `true`로 잘못 해석되는 유명한 함정이 있는데 — 이 저장소의 js-yaml 버전(4.3.0)은 기본적으로 YAML 1.2 계열 스키마를 써서 `on`을 문자열 키 그대로 파싱함, 그리고 GitHub 자체의 워크플로우 파서도 `on:`을 컨텍스트상 정확히 트리거 키로 처리하도록 특별히 다뤄서 실사용에서는 애초에 문제되지 않는 부분 — 확인 차 짚어둠.) `package.json`도 `require()`로 다시 로드해 문법 오류 없음 + `build` 섹션 내용이 의도한 그대로인지 확인.

**README.md 업데이트**: 기존 "배포용 설치파일 만들기" 섹션(로컬에서 `npm run build`, 지금 빌드 중인 OS 하나만 만들어짐)은 그대로 두되 "Mac 빌드는 macOS에서만 가능하니 두 OS 다 배포하려면 아래 GitHub Actions를 쓰라"는 안내를 추가. 그 아래 새 "새 버전 배포하기 (GitHub Releases, 자동 빌드)" 섹션에 (1) `package.json`의 `version` 올리고 커밋 → 버전 태그(`v1.0.1`류) 생성 → push, 이 3단계만 하면 된다는 절차, (2) Windows 사용자용 SmartScreen 경고 안내("추가 정보" → "실행"), (3) Mac 사용자용 "손상되었다" 경고 해결법(`xattr -cr /Applications/FocusPet.app`, productName이 `FocusPet`이라 `.app` 이름도 그대로 일치) — 요청받은 3가지 안내 문구를 전부 포함. README의 나머지 부분(캐릭터 로스터가 "고양이/강아지/토끼/판다/햄스터"로, 톱니바퀴 버튼이 있는 것처럼 등 예전 프로토타입 시절 그대로 남아있는 오래된 서술)은 이번 요청 범위 밖이라 손 안 댐 — 정직하게 기록.

### GitHub Actions 릴리즈 워크플로우 — draft 릴리즈 + 커밋 로그 기반 자동 릴리즈 노트 + 두 OS job 레이스 컨디션 제거 (사용자 지시로 실제 태그 push/빌드 실행은 안 함 — YAML 문법 검증까지만)
직전 라운드(GitHub Actions로 Windows/Mac 동시 배포)의 워크플로우를 세 가지로 개선: (1) 태그와 바로 이전 태그 사이 커밋 로그로 릴리즈 설명을 자동 채움, (2) 릴리즈를 처음엔 draft로 생성해서 사용자가 GitHub Releases 페이지에서 검토 후 직접 Publish하도록, (3) Windows/Mac 두 job이 같은 draft 릴리즈에 안전하게 첨부되도록(레이스 컨디션 제거). 이번에도 사용자가 실제 태그 push/빌드 실행은 직접 하겠다고 명시 — YAML 문법 검증(`js-yaml` 파싱)과 워크플로우 안에 박힌 bash 스크립트 문법 검증(`bash -n`, 스크립트를 워크플로우에서 추출해 별도 파일로 확인)까지만 하고 실제 실행은 안 함.

**electron-builder의 실제 동작을 소스코드로 직접 확인 (짐작 대신)**: 설계를 시작하기 전에 `node_modules/electron-publish/out/gitHubPublisher.js`(electron-builder의 실제 전이 의존성, 로컬에 이미 설치돼 있어서 바로 읽을 수 있었음)를 직접 읽어서 GitHub publish 단계가 기존 release를 어떻게 찾고 재사용하는지 확인 — `getOrCreateRelease()`가 태그가 일치하는 release 목록을 조회한 뒤 `if (release.draft) { return release; }`로, **일치하는 release가 draft이기만 하면 조건 없이 그대로 재사용**한다는 걸 확인(이 라운드 설계 전체의 핵심 전제 — 아래 참고). 또한 `node_modules/builder-util-runtime/out/publishOptions.d.ts`의 `GithubOptions` 타입 정의를 읽다가 **직전 라운드가 시도했던 `package.json`의 `publish.draft: true` 필드 자체가 애초에 존재하지 않는 필드**라는 걸 발견(`GithubOptions`엔 `draft`가 없고 `releaseType?: "draft"|"prerelease"|"release"`만 있음, 문서 주석에 "By default `draft` release will be created"라고 명시돼 있어 기본값 자체가 이미 draft라는 것도 재확인) — JSON에 없는 필드를 넣어도 JS는 조용히 무시할 뿐 에러를 안 내서, 실제로 켜보기 전엔 절대 못 잡았을 실수. `"draft": true` → **`"releaseType": "draft"`**로 정정.

**1. draft 릴리즈 + 레이스 컨디션 제거를 한 번에 — `create-release` job 신설**: 위에서 확인한 `getOrCreateRelease()`의 동작(draft release가 이미 있으면 무조건 재사용) 자체가 레이스 컨디션을 없애는 열쇠라고 판단 — Windows/Mac 두 job이 각자 독립적으로 `--publish always`를 실행하다가 **둘 다 release가 아직 없는 순간**을 동시에 관측하면(거의 동시에 시작하는 CI job 두 개가 이 좁은 창을 피해간다는 보장이 없음) 각자 새 release를 만들어버려 태그당 release가 2개 생기는 게 진짜 레이스 — 이걸 "먼저 실행되는 쪽이 만들고 나중 쪽은 기존 걸 쓰길 바라는" 확률적 해결 대신, **release 생성 자체를 전담하는 job을 하나 새로 만들어 win/mac 둘 다 그 job이 끝난 뒤에만 시작**하도록(`needs: create-release`) 구조적으로 막음 — release를 만드는 코드 경로가 전체 워크플로우에 물리적으로 한 곳(`create-release` job)밖에 없으니 동시 생성이라는 상황 자체가 발생할 수 없음. `build-windows`/`build-mac`은 서로에게는 여전히 `needs`가 없어서(공통 선행 job 하나를 공유할 뿐) 요청받은 "두 OS 빌드가 서로 독립적으로(병렬로)"는 그대로 유지됨.
- **`create-release`(`ubuntu-latest`, 가장 가볍고 빠른 러너 — win/mac 어느 쪽 OS 특화 작업도 아니라서 선택)**: `actions/checkout@v4`에 `fetch-depth: 0`(전체 히스토리+모든 태그 — 기본 얕은 클론(depth 1)으로는 태그 목록도 커밋 범위도 못 구함) → 커밋 로그로 릴리즈 노트 생성 → `gh release create --draft`.
- **재실행 안전성(idempotent)**: `gh release view "$CURRENT_TAG"`로 이미 있으면 생성을 건너뜀 — GitHub Actions의 "Re-run all jobs"로 실패한 워크플로우를 통째로 다시 돌려도(`build-mac`만 실패했던 경우 등) "이미 존재하는 태그의 release" 에러로 안 죽고 그냥 있는 걸 그대로 씀.

**2. 릴리즈 노트 — GitHub 자동생성 대신 커밋 로그 diff를 선택**: 사용자가 GitHub의 `--generate-notes` 기능(PR 기반 자동 생성)도 허용했지만, 이 저장소의 실제 커밋 히스토리를 먼저 확인(`git log --merges` — merge 커밋이 하나도 없음, 지금까지 전부 main에 바로 커밋되고 PR을 거친 적이 없음)한 뒤 PR 기반 자동생성 기능을 쓰면 이 저장소에서는 "비교 링크 하나만 덜렁 있는 거의 빈 본문"이 나올 가능성이 높다고 판단해 **손으로 커밋 로그를 diff하는 쪽을 채택**(사용자가 첫 번째로 제시한 방법이기도 함). `git tag --sort=-v:refname`(문자열 정렬이 아니라 git의 버전 인식 정렬 — v1.10.0을 v1.9.0보다 앞에 잘못 놓는 실수를 피함)으로 태그를 최신순 정렬한 뒤 현재 태그를 제외한 첫 번째를 "바로 이전 태그"로 잡고, `git log --pretty=format:'- %s (%h)' prevTag..currentTag`로 그 사이 커밋들을 불릿 목록으로, 맨 아래에 `https://github.com/{repo}/compare/prevTag...currentTag` 비교 링크까지 붙여서 `release-notes.md`로 저장 → `gh release create --notes-file release-notes.md`. 이전 태그가 없는 경우(최초 릴리즈)는 전체 로그를 대신 사용하고 그 사실을 본문에 명시.
- **`gh release view`/`gh release create`에 `--repo` 안 넘김**: `create-release` job이 `actions/checkout`을 이미 했으므로(위 1번), 그 디렉터리가 진짜 git 저장소이고 origin이 올바른 GitHub 레포를 가리키고 있어서 `gh` CLI가 자동으로 저장소 컨텍스트를 잡음 — 명시적 하드코딩 불필요.

**3. Windows/Mac job에 `needs: create-release` 추가**: 그 외 스텝(`checkout`/`setup-node`/`npm ci`/`npm run build -- --publish always`)은 무변경 — 두 job이 실행되는 시점엔 draft release가 이미 존재함이 보장되므로, electron-builder의 `--publish always`가 (위에서 확인한 동작대로) 그 draft를 그대로 찾아서 exe/dmg만 업로드함.

**YAML/bash 문법 검증**: `.github/workflows/release.yml`을 `js-yaml`로 파싱해 `jobs` 3개(`create-release`/`build-windows`/`build-mac`)와 `needs` 관계(win/mac→create-release, create-release→없음)가 의도대로인지 JS 객체로 직접 확인. `create-release` job의 두 `run:` 블록(멀티라인 bash)은 워크플로우에서 텍스트 그대로 추출해 스크래치 디렉터리에 `.sh` 파일로 저장한 뒤 `bash -n`(구문 검사 전용, 실행 안 함)으로 각각 검증 — 둘 다 통과. `${{ github.repository }}`/`${{ github.ref_name }}` 같은 GitHub Actions 표현식이 스크립트 본문에 섞여 있어도 `bash -n`이 문제없이 통과하는 걸 확인 — 실제 실행 시엔 Actions 러너가 `${{ }}` 부분을 bash에 넘기기 전에 텍스트로 먼저 치환하므로 이중 해석 걱정이 없다는 것도 재확인. `package.json`도 `require()`로 다시 로드해 문법 오류 없음 + `releaseType: "draft"` 반영 확인.

### 첫 실제 태그 push(v1.0.1) 후 — build job은 둘 다 성공했는데 draft 릴리즈에 exe/dmg가 안 붙던 버그 (실제 워크플로우 로그로 원인 특정)
사용자가 실제로 `v1.0.1` 태그를 push해서 워크플로우를 처음 돌려봄 — `create-release`/`build-windows`/`build-mac` 세 job 전부 초록 체크(성공)로 끝났는데도, GitHub Releases의 `v1.0.1` draft에는 GitHub이 자동으로 붙이는 Source code(zip/tar.gz)만 있고 실제 빌드 결과물(exe/dmg)이 안 보인다는 리포트 — `gh run list`/`gh run view --log`로 실제 실행 로그를 가져와 원인을 특정해달라는 요청.

**조사 방법**: 이 세션에 `gh` CLI가 이미 인증된 상태로 설치돼 있어서(`gh auth status` 확인) 실제로 `gh run list --repo yoobilee/focus-pet`로 v1.0.1 워크플로우 실행(`30522134467`)을 찾고, `gh run view --job=<id> --log`로 `build-windows`/`build-mac` 두 job의 전체 로그를 각각 받아와 `publish`/`upload`/`GH_TOKEN`/`version` 관련 줄을 직접 훑음.

**사용자가 의심했던 세 가지 중 둘은 로그로 명확히 기각**:
- **GH_TOKEN이 실제로 전달됐는지**: 로그의 `##[group]Run npm run build -- --publish always` 바로 아래 `env:` 섹션에 `GH_TOKEN: ***`가 두 job 모두 정확히 찍혀 있음 — 문제 없음.
- **`npm run build -- --publish always`가 electron-builder에 인자를 제대로 넘기는지**: npm 자신이 실행 직전에 찍는 `> focus-pet@1.0.0 build` / `> electron-builder --publish always` 로그로 `--publish always`가 정확히 electron-builder까지 전달된 걸 직접 확인 — 문제 없음.

**세 번째 의심(태그/버전 매칭 실패)이 정답이었음, 로그에 그대로 찍혀 있었음**: 두 job의 로그 모두에 `publishing publisher=Github (owner: yoobilee, project: focus-pet, **version: 1.0.0**)`와 `uploading file=FocusPet-1.0.0-Setup.exe` / `FocusPet-1.0.0.dmg`가 그대로 남아있었음 — **이 실행은 `v1.0.1` 태그를 빌드하는 중인데 electron-builder는 "1.0.0"으로 퍼블리시하고 있었음**. 원인: `node_modules/electron-publish/out/gitHubPublisher.js`(직전 라운드에 이미 한 번 읽어서 익숙했던 파일)의 생성자가 `this.tag = info.vPrefixedTagName === false ? version : \`v${version}\`` — electron-builder는 자기가 퍼블리시할 release의 태그를 **`package.json`의 `"version"` 필드에서 직접 계산**하지, 이 워크플로우를 트리거한 실제 git 태그(`github.ref_name`)를 전혀 참조하지 않음. `package.json`의 `version`이 (v1.0.1 태그를 push하기 전에) `1.0.0`에서 안 올라가 있어서, electron-builder는 스스로 "v1.0.0"을 타겟으로 계산했고, `getOrCreateRelease()`가 (직전 라운드 검증 때 확인한 그대로) 태그가 일치하는 draft release를 찾아 **무조건 재사용**하는 로직대로 — 마침 `v1.0.0` draft release가 이미 존재했으므로(직전 라운드에서 워크플로우 자체를 처음 검증할 때 만들어진 테스트 release) 그걸 그대로 찾아서 조용히 재사용, 새로 만든 `v1.0.1` draft(`create-release` job이 정확히 만들어뒀던 바로 그것)는 완전히 건드리지도 않은 채 지나감. **job이 실패하거나 뭔가를 건너뛴 게 아니라, 처음부터 끝까지 정상적으로 실행됐는데 그 대상이 잘못된 release였던 것** — 그래서 둘 다 초록 체크였던 것. `gh release view v1.0.0`/`gh release view v1.0.1`로 실제 GitHub 상태까지 대조해 확정: `v1.0.0`엔 exe/dmg/blockmap/latest.yml 6개 에셋이 전부 있고, `v1.0.1`엔 (본문은 `create-release`가 정확히 채워뒀지만) 에셋이 0개.

**고침 — package.json을 미리 올려두는 걸 신뢰하는 대신, CI가 태그에서 버전을 직접 만들어 쓰도록**: `build-windows`/`build-mac` 두 job 각각에 `npm ci` 직후, 빌드 스텝 직전에 새 스텝 추가:
```yaml
- name: Sync package.json version to the pushed tag
  shell: bash
  run: |
    TAG="${{ github.ref_name }}"
    npm pkg set version="${TAG#v}"
```
`npm pkg set`(npm 7+, git을 안 건드리는 순수 설정 조작 커맨드 — `npm version`과 달리 커밋/태그를 새로 만들려는 시도가 없어서 CI에서 쓰기에 정확히 맞음)으로 `package.json`의 `version`을 태그에서 `v` 접두어만 뗀 값으로 그 자리에서 덮어씀 — 이후 실행되는 `npm run build -- --publish always`(electron-builder)는 이 갱신된 `package.json`을 읽으므로, `vPrefixedTagName` 기본값(true)과 합쳐져 항상 `create-release`가 쓴 것과 똑같은 `github.ref_name`으로 수렴함. "태그를 push하기 전에 `package.json`의 version을 미리 올려둬야 한다"는 사람의 기억에 의존하는 대신(README에 여전히 그렇게 하라고 안내는 남겨둠 — 로컬 `npm start`/`npm run build`가 정확한 버전을 보여주는 데는 여전히 의미 있음, 다만 이제 CI 결과의 정합성이 그 기억에 의존하지 않게 된 것), 실제로 퍼블리시되는 버전의 최종 근거를 **git 태그 하나**로 통일 — `create-release`도, 이제 `build-windows`/`build-mac`도 전부 `github.ref_name`에서 파생됨. `shell: bash`를 명시한 이유: `build-windows`의 `run:` 기본 셸은 pwsh(로그의 `shell: C:\Program Files\PowerShell\7\pwsh.EXE`에서 확인)라, 명시 안 하면 이 짧은 스크립트를 PowerShell 문법으로 다시 써야 해서 mac 쪽과 스크립트 내용이 갈라짐 — `shell: bash`로 강제해서 두 job이 완전히 동일한 스크립트를 쓰게 통일(Windows 러너에도 Git for Windows가 기본 포함돼 있어 `shell: bash`가 항상 사용 가능).

**검증**: `npm pkg set version=...`을 실제로 격리된 스크래치 사본(`package.json`을 복사해서, 진짜 저장소 파일은 안 건드림)에서 실행해 `"1.0.0"` → `"1.0.1"`로 정확히 바뀌고 나머지 필드(`build.publish` 등)는 그대로인 걸 확인. 워크플로우 YAML은 `js-yaml`로 재파싱해 두 job의 스텝 순서(`checkout`→`setup-node`→`npm ci`→**버전 동기화(신규)**→빌드)가 의도대로 들어갔는지 확인, 8개 `run:` 스텝 전부(신규 2개 포함) `bash -n`으로 구문 검증 — 전부 통과. **실제 재태깅/재push는 안 함(사용자 지시)** — 이 고침이 실제로 효과가 있는지는 다음 실제 태그 push(예: `v1.0.2`, 이 고침이 반영된 커밋을 포함하는 새 태그) 때 확인 필요. `v1.0.1` 태그 자체를 재실행("Re-run jobs")해도 이 고침을 못 잡음(태그가 가리키는 커밋 시점의 워크플로우 YAML을 그대로 다시 쓰므로) — 새 태그를 push해야 이번에 고친 워크플로우가 실제로 실행됨, 사용자에게 이 부분 명확히 안내.

**정리 안 한 것 (사용자 승인 없이 손대지 않음)**: `v1.0.0` draft release엔 이번 v1.0.1 실행이 실수로 덮어쓴 에셋이 최신 상태로 남아있고(내용 자체는 두 태그 사이 커밋이 워크플로우 YAML 변경뿐이라 앱 코드상 차이는 없을 것), `v1.0.1` draft는 여전히 에셋 0개인 채로 남아있음 — 기존 GitHub Release 상태를 삭제/수정하는 건 요청받지 않은 파괴적 변경이라 손 안 대고 현재 상태만 보고, 정리 여부는 사용자 판단에 맡김.

### GitHub Pages 랜딩 페이지 추가 (`docs/`)
FocusPet 소개용 정적 페이지 — 순수 HTML/CSS, 빌드 스텝 없이 GitHub Pages(저장소 설정에서 Pages 소스를 `main` 브랜치의 `/docs` 폴더로 지정하면)로 바로 서빙 가능. `windows/settings/settings.css`의 뉴트럴+코랄 팔레트(`--bg`/`--accent` 등 CSS 커스텀 프로퍼티)를 그대로 재사용해 앱 UI와 랜딩 페이지가 같은 제품처럼 보이게 함 — 새 팔레트를 따로 만들지 않고 값 자체를 그대로 복사.

**구성**: `docs/index.html`(전체 내용), `docs/styles.css`(별도 스타일시트), `docs/assets/app-icon.png`(`assets/app-icon-256.png`를 복사 — 파비콘 + 헤더 로고로 사용).
- **히어로**: 앱 이름 + 한 줄 소개 + Windows/Mac 다운로드 버튼(둘 다 `https://github.com/yoobilee/focus-pet/releases/latest`로 연결 — 특정 버전 파일을 직접 링크하지 않아서 새 릴리즈가 나와도 이 페이지를 따로 안 고쳐도 됨, GitHub이 자체 제공하는 리다이렉트 기능) + 스크린샷/GIF 자리.
- **기능 6개**: CLAUDE.md 본문(10종 캐릭터 로스터, 3D voxel 렌더링, 커서 반응형 상호작용, 알림 트리거 방식 선택, 항상-위-표시, 드래그+자석 스냅)을 참고해서 정리 — 실제 구현과 어긋나지 않게 정확한 기능만 나열(예: "3D 렌더링"을 "픽셀아트가 아니라 진짜 3D"라고 명시한 건 CLAUDE.md의 "왜 3D를 접고 픽셀아트로 돌아왔나" → 이후 "3D 복셀 공간 실험" 라운드에서 다시 3D로 전환된 실제 히스토리를 반영한 것).
- **캐릭터 로스터**: 이미지 대신 색 점 + 이름의 칩(chip) 목록으로 — 실제 캐릭터 렌더는 Three.js로 런타임에 그려지는 것이라 정적 페이지에 이미지로 미리 구울 방법이 마땅치 않았고, 그렇다고 랜딩 페이지에 Three.js를 통째로 들여오는 건 "순수 HTML/CSS, 빌드 없이"라는 요청과 정면으로 배치되는 규모의 작업이라 하지 않음 — 대신 `windows/shared/animal-engine.js`의 `SPECIES` 테이블에서 각 종의 실제 `bodyColor`를 직접 읽어와(고양이A `#e8935a`, 턱시도 `#161616` 등) 칩의 점 색으로 그대로 사용, 최소한 "이 종은 대충 이런 색"이라는 정보는 정확하게 전달되도록 함.
- **다운로드 섹션**: Windows SmartScreen("추가 정보" → "실행")과 Mac `xattr -cr /Applications/FocusPet.app` 안내 — 문구는 직전 라운드에 README.md에 이미 써둔 것과 동일(같은 배포를 다루는 두 문서가 서로 다른 설명을 하면 안 되니 그대로 재사용).
- **스크린샷/GIF 자리**: `<img>` placeholder 대신(빈 src로 깨진 이미지 아이콘이 보이는 걸 피하려고) 점선 테두리 CSS 박스로 — 실제 이미지 파일을 안 만들어도 페이지 자체가 깨져 보이지 않음. 나중에 교체하는 방법(`assets/screenshot.png`로 저장 후 `<img>` 태그로 바꾸기)을 HTML 주석으로 정확한 위치에 남겨둠.

**검증 — 이 프로젝트의 기존 devDependency(Electron)를 그대로 재사용한 헤드리스 브라우저 체크**: "로컬에서 브라우저로 열어서 레이아웃이 깨지지 않는지 확인"이라는 요청을 문자 그대로("직접 브라우저로 열기") + 실질적으로(실제로 뭐가 보이는지 눈으로 판정) 둘 다 만족시키려고, 별도 툴 설치 없이 이미 `devDependencies`에 있는 `electron`으로 headless `BrowserWindow`를 띄워 `docs/index.html`을 로드하고 (1) `document.documentElement.scrollWidth`가 `clientWidth`를 넘는지(가로 스크롤/깨짐의 결정적 지표)를 desktop(1440px)/tablet(820px)/mobile(375px) 세 폭에서 측정, (2) `webContents.on('console-message'/'did-fail-load')`로 실제 콘솔 에러/리소스 로드 실패를 수집, (3) `capturePage()`로 실제 렌더링 결과를 스크린샷으로 남겨 직접 봄. 순수 "코드가 문법적으로 맞다"가 아니라 실제 Chromium이 그린 픽셀을 확인하는 검증 — 이 방식으로 실제 버그 2개를 발견/수정함(아래).
- **버그 1 — 모바일(375px)에서 가로 스크롤 발생**: `scrollWidth=361 > clientWidth=345`로 실측 확인(스크린샷으로도 재확인 — nav의 4개 한글 링크(기능/캐릭터/다운로드/GitHub)가 `flex-wrap` 없이 한 줄에 억지로 들어가려다 각 링크 텍스트가 한 글자씩 세로로 쌓이는 방식으로 깨져 있었음). **고침**: 모바일 브레이크포인트에서 부차적인 텍스트 링크를 아예 숨기고(`.nav-links a:not(.btn){display:none}`) 브랜드 로고 + 다운로드 버튼만 남김 — 단일 페이지라 앵커 링크가 없어도 스크롤로 다 접근 가능하니 허용 가능한 단순화로 판단.
- **버그 1 고쳤는데도 같은 크기(16px)로 오버플로가 그대로 남음** — nav가 원인이 아니었다는 뜻이라 별도 진단 스크립트(`document.querySelectorAll('*')`를 순회해 `getBoundingClientRect().right`가 뷰포트를 넘는 요소를 전부 찾아내는 헬퍼)로 정확한 원인을 재특정: `.download-card`(그리드 아이템)가 범인이었음 — 원인은 CSS Grid의 잘 알려진 함정인 `min-width:auto` 기본값: `<code>xattr -cr /Applications/FocusPet.app</code>`가 `white-space:pre`(줄바꿈 없이 한 줄 유지, 내부적으로 `overflow-x:auto`로 스크롤시키려던 의도)라서, 그 한 줄의 고정 너비가 grid 트랙이 줄어들 수 있는 최소 폭의 바닥으로 작동해 카드(그리고 페이지 전체)를 뷰포트보다 넓게 밀어냄. **고침**: `.feature-card`/`.download-card`(그리드 아이템)에 `min-width:0` 추가 — 재측정 결과 `scrollWidth=345=clientWidth`로 완전히 일치, offenders 0건.
- **부수 발견 (버그는 아니지만 실제 깨짐) — Mac 아이콘이 안 보임**: 히어로 버튼/다운로드 카드에 애플 로고 대신 macOS 전용 프라이빗 유즈 영역 문자(U+F8FF, Apple 자체 폰트에서만 렌더링됨)를 실수로 넣어서 Windows에서 빈 칸으로 보이고 있었음 — 스크린샷으로 직접 보고서야 발견(코드만 봐서는 " " 처럼 보여서 못 잡았을 것). 크로스플랫폼에서 항상 렌더링되는 표준 유니코드 🍎(사과 이모지)로 교체.
- **타이포그래피 다듬기**: 스크린샷을 보다가 "작은 창 하나에 생각보다 많은 게 들어있어요"라는 섹션 제목이 데스크톱 폭에서 "요" 한 글자만 마지막 줄에 남는 어색한 줄바꿈을 발견 — `text-wrap:balance`(최신 Chromium 지원, Electron도 포함)를 `.section-title`에 추가해 줄 길이를 균등하게 재분배, 재확인 결과 "작은 창 하나에 생각보/다 많은 게 들어있어요"로 자연스럽게 개선.
- **스크롤 캡처 관련 삽질(참고용 기록)**: 페이지 전체를 한 번에 보려고 시도한 두 가지 방법이 각각 다른 이유로 실패했음 — (1) `show:false` 창에서 `scrollTo()` 후 `capturePage()`를 여러 번 호출하는 방식은 스크롤 위치가 전혀 반영 안 된 동일한(맨 위) 프레임만 반복해서 나옴(숨겨진 창의 컴포지터가 스크롤에 따른 재페인트를 하지 않는 것으로 추정), (2) 창 높이를 페이지 전체 높이(3000px+)로 키워서 스크롤 자체를 없애려던 방식은 이 화면의 실제 workArea(816px)에 의해 창 높이가 조용히 잘림(Electron이 `show:false`여도 화면 크기를 넘는 요청은 클램프함)을 `getBounds()`로 직접 확인. 최종적으로 `show:true`(실제로 화면에 잠깐 뜨는 창)로 바꾸고 `html{scroll-behavior:smooth}`(styles.css 자체에 있음)를 캡처 직전에 `'auto'`로 강제 전환한 뒤 스크롤해야 정확한 프레임을 얻을 수 있었음(짧은 거리는 스무스 스크롤 애니메이션이 대기시간 안에 우연히 끝나서 문제가 안 드러났었는데, 맨 아래로 한 번에 점프하는 큰 거리에서는 애니메이션이 채 안 끝난 상태로 캡처돼 엉뚱한 위치가 찍혔음).
- **실제 기본 브라우저로도 오픈**: 위 검증 후 `start docs/index.html`로 사용자 기본 브라우저에도 실제로 띄워둠(사용자가 직접 훑어볼 수 있도록) — "브라우저로 열어서" 요청의 문자 그대로도 충족.

## 알려진 한계 / 다음에 다듬을 것
- **(신규)** `docs/`의 스크린샷/GIF 자리는 여전히 플레이스홀더 상태(요청대로 — 실제 캡처 이미지는 사용자가 직접 넣을 예정) — `docs/index.html`의 `.media-placeholder` 블록을 지우고 `<img src="assets/screenshot.png">`(또는 GIF)로 교체하면 됨, HTML 안에 정확한 안내 주석을 남겨둠.
- **(신규)** GitHub Pages 자체를 활성화(저장소 Settings → Pages → Source를 `main`/`docs` 폴더로 지정)하는 절차는 이번에 안 밟음 — 파일만 만들어뒀고, 실제로 `https://yoobilee.github.io/focus-pet/`(또는 커스텀 도메인)로 서빙되게 하려면 저장소 설정에서 한 번 켜야 함, 사용자 몫으로 남김.
- **(신규)** README.md의 "특징"/"실행 방법" 섹션이 여전히 초기 이모지 프로토타입 시절 서술(캐릭터가 "고양이/강아지/토끼/판다/햄스터", 톱니바퀴 버튼으로 설정 열기, "네 모서리 중 선택" 위치 등)로 남아있음 — 실제로는 10종 로스터 3D voxel 렌더링, 더블클릭으로 설정 열기, 항상 작업표시줄 위 고정+좌우 페이싱/드래그로 다 바뀐 지 오래됨. 이번 배포 자동화 라운드에서 새로 추가한 배포 관련 섹션들은 정확하지만, 그 위의 기존 섹션들은 안 건드림 — 다음에 README 전반을 실제 기능과 맞게 갱신할 필요.
- **(갱신)** GitHub Actions 워크플로우 — 이번 라운드에서 실제 태그 push(v1.0.1) 한 번을 실제 로그로 진단해 "package.json version이 태그와 안 맞으면 electron-builder가 엉뚱한(오래된) release로 조용히 퍼블리시한다"는 구체적 버그를 하나 찾아 고침(버전 동기화 스텝 추가) — 그 외에도 여전히 실측 확인이 안 된 부분들: (1) `mac.identity: null` + `CSC_IDENTITY_AUTO_DISCOVERY=false` 조합이 실제 macos-latest 러너에서 서명 없이 dmg를 만들어내는지(이번 v1.0.1 로그에선 dmg 빌드/업로드 자체는 정상 진행됐던 것으로 보임 — 서명 실패로 죽지는 않았음 — 다만 "서명이 진짜로 안 걸려 있는지"까지는 별도 확인 안 함), (2) `release-notes.md`의 한글 커밋 메시지가 실제 GitHub Releases 페이지에서 인코딩 깨짐 없이 표시되는지(v1.0.1 draft를 `gh release view`로 다시 읽어봤을 땐 한글이 정상 표시됐음 — 꽤 긍정적인 신호지만 웹 UI에서 직접 본 건 아님), (3) 이번에 추가한 버전 동기화 스텝이 실제로 문제를 고치는지는 다음 태그 push 전까진 확정 못 함(재현 확인 미완료) — 전부 다음 실제 태그 push 때 확인 필요.
- **(신규, 검증 미완료)** 위 "말풍선을 다시 펫 창 내장 방식으로" 라운드 전체가 코드만 작성하고 실제 검증(재현 테스트/스크린샷/`test:invariants`)을 스킵한 상태 — 100%/125% 배율에서 실제로 안 잘리는지, 비대칭 여백 방향이 사용자 의도와 맞는지(위 "여백 방향 해석" 항목 참고 — 반대로 읽었을 가능성이 있어 명시적으로 기록해둠), 드래그/페이싱/드롭 애니메이션 도중 스프라이트가 매끄럽게 움직이는지(특히 로컬 오프셋이 고정되는 동안과 애니메이션 중 보간되는 구간의 경계) 전부 다음에 실제로 켜서 확인 필요.
- **(신규)** `sprite-center-report`(캔버스 알파 스캔 기반 종별 시각적 중심 보정) 제거로, 포메라니안처럼 96px 박스 안에서 실제 캐릭터가 비대칭하게 자리잡는 종은 말풍선 꼬리가 다시 살짝 어긋나 보일 수 있음(4~5px 수준, 직전 라운드에서 실측) — 사용자가 이번 라운드에서 명시적으로 제거를 요청한 트레이드오프.
- **(폐기됨 — 더 이상 유효하지 않은 옛 한계)** "말풍선 창 리사이즈"/"별도 BrowserWindow" 관련 항목들은 이번 라운드로 아키텍처 자체가 다시 바뀌면서 전부 무의미해짐 — 말풍선은 다시 펫 창에 내장된 DOM 요소이고, 별도 창 자체가 더 이상 존재하지 않음.
- **(3D 전용, 17차 QA 라운드에서 발견)** `belly`/`snout`/`pattern` 같은 skin 데칼은 정면 포즈에서 보이려면 그 데칼이 얹히는 부위(머리/몸통) 자신의 앞쪽(+X) 가장자리와 같거나 넘게 폭을 잡아야 한다는 걸 턱시도 사례로 발견(안 그러면 부위 자신의 앞면에 영구히 가려짐 - 코 위치 계산이 이미 지키고 있던 원칙과 동일). 턱시도의 `belly`/`snout`만 이번에 확인/수정했고, **나머지 9종의 기존 belly/pattern 필드는 이 기준으로 전수 재검사하지 않음** - 다른 종에서도 같은 이유로 정면에서 무늬가 안 보이는 경우가 있을 수 있음(신고된 적은 없음, 옆모습 위주로만 지금까지 검증돼왔기 때문일 가능성).
- **(3D 전용, 17차 QA 라운드에서 확인 → 18차 QA 라운드에서 우회책도 좌절)** Z축(depth) 폭을 좁혀 정면에서 더 가늘게 보이게 하는 것은, 그 감소폭이 부위 자신의 절반 깊이(대략 3유닛) 밑으로 내려가면 옆모습에서 아예 안 보이게 되는 구조적 상충이 있음(정면=로컬X가 카메라축, 옆모습=로컬Z가 카메라축이라 정반대 조건이 걸림). 17차 라운드는 `depthTest:false` 방식의 정면 전용 평면 오버레이(`frontVMask`)로 이를 우회하려 했지만, 18차 라운드에서 **평면과 그 아래 원본 데칼(박스)의 회전에 따른 겉보기 폭 변화 방식이 서로 달라서**(평면=코사인 감쇠, 박스=깊이 면이 추가로 드러나며 다르게 변함) 완전한 정면이 아닌 각도(225°/315° 등)에서 마스크가 원본과 안 맞물려 얼룩/찢김으로 보이는 문제가 발견돼 롤백됨(`frontVMask` 메커니즘 자체가 코드에서 완전히 삭제됨) — 진짜 Z-테이퍼가 필요한 경우, 옆모습 가시성과 정면 테이퍼를 동시에 만족하는 알려진 방법이 현재 이 파일 안에는 없음. 대안(같은 도형 종류로 마스킹, 커스텀 지오메트리 등)은 시도되지 않음.
- **(3D 전용)** 원근(perspective) 카메라가 직교보다 확실히 더 입체감 있는 회전을 준다는 게 확인됐지만(`windows/pet3d/pet3d.js`의 `?perspective=1&camDist=NN` 토글로 재확인 가능), 로스터에서 가장 긴 종(닥스훈트)이 가까운 카메라 거리에서 여러 각도(특히 90도)에 화면 잘림을 일으켜서 실제 pet 창에는 아직 적용 안 함(직교 유지) — 도입하려면 종별 최대 길이에 맞춘 프레이밍 여유(FOV 패딩 또는 종별 camDist 보정) 튜닝이 추가로 필요.
- **(3D 전용)** 그루밍 앞발이 얼굴 표면 밖으로 튀어나오도록 Z축을 밀어내는 수정(`GROOM_PROTRUDE_Z`) 이후, 정면에 가까운 회전 각도(90도 근처)에서는 근접/반대쪽 앞다리 두 개가 목 양옆에 대칭으로 튀어나와 살짝 부자연스러워 보임 — 그루밍이 방향 전환(회전 애니메이션)과 정확히 겹치는 드문 순간에만 스치듯 보이는 경우라 감수하기로 함.
- **(3D 전용)** 샴 고양이의 얼굴 그라데이션(`addFaceShadedHeadBox`)은 0°/180°(옆모습, 이 엔진의 기본 휴식 각도)에서만 캔버스 텍스처로 픽셀 단위 정밀하게 그려짐 — 90°/270°(정면에 가까운 각도, 4차 QA 라운드의 연속 회전으로 실사용 중 꽤 자주 지나감)에서는 세분화된 vertex color 폴백을 대신 씀(BoxGeometry의 +x/-x 면은 구조적으로 (x2d,y2d) 중 하나가 고정값이라 텍스처를 붙여도 2D 방사형 패턴 전체를 담을 방법이 없음 - 박스라는 도형 자체의 한계, 5차 QA 라운드에서 확인). 실사용 확인 결과 폴백도 눈/코 주변에 그럴듯한 어두운 마스크를 만들어 크게 위화감은 없지만, 텍스처 버전만큼 정밀하지는 않음 - 더 완벽하게 만들려면 +x/-x 면에도 별도 UV 매핑을 가진 두 번째 텍스처를 씌우는 작업이 추가로 필요(시도 안 함, 텍스처 하나로 커버되는 주 시야각에서 이미 목표를 달성했다고 판단).
- **(3D 전용)** Z축 idle 4종(`prowlcircle`/`chasetail`/`hopspin`/`wheelrun`)은 7차 QA 라운드에서 "제자리 회전+스케일"에서 "이 종의 실제 gait로 걸으며 원형 경로를 도는" 방식으로 재설계됨(`circleStep`+`WALKING_IDLE_NAMES`, `animal-engine.js`). 8차 라운드에서 `prowlcircle`(고양이)만 "그래도 제자리처럼 보인다"는 후속 리포트가 있었는데, dispatch/코드 경로 자체는 처음부터 정상이었고 반지름 2.6·각속도 0.9rad/s·지속시간 5-7.5초 조합이 활성화 한 번에 한 바퀴도 다 못 돌 정도로 느려서(직접 화면 bbox를 측정해 확인) 생긴 문제 - 반지름 2.2·각속도 1.8rad/s·지속시간 4-5.5초로 재조정, `chasetail`과 비슷한 수준(~1.3바퀴/활성화)으로 맞춤. **10차 QA 라운드에서 실제 Electron 앱(시뮬레이션 아님)으로 재검증** - 병렬로 여러 창을 띄우면 리소스 경합으로 idle 순환 자체가 멈추는 테스트 하네스 버그를 먼저 걷어낸 뒤 순차 검증한 결과, 고양이 4종 전부 실제 화면 알파채널 바운딩박스 기준 X/Y/스케일이 비슷한 크기로 함께 움직이는 진짜 원형 이동을 확인 - 코드 버그를 못 찾음(8차 라운드의 재조정이 실제로 잘 작동하고 있었다는 뜻). `approach`(원형 경로가 아니라 단순 전후 이동만 하던, 다리 애니메이션 없이 `bodyBob`만 있는 "밋밋한 스텝" idle)는 원형 걷기 4종과 겹치는 느낌이라는 피드백에 따라 **11차 QA 라운드에서 완전히 삭제됨** - 더 이상 어떤 종의 idlePool에도 없음.
- **(3D 전용)** idle 상태머신이 아주 드물게(재현 시도 3번 중 1번) 특정 idle(관찰된 사례: cat_calico의 `tailtwitch`)에 멈춰 수십 초간 전환이 안 되는 현상을 10차 QA 라운드 검증 중 목격 - `tailtwitch` 자체의 지속시간은 1.2~2초로 코드상 정상이라 훨씬 일찍 넘어갔어야 함. `window.onerror`/`unhandledrejection` 훅으로도 에러를 못 잡았고, 같은 종을 다시 띄우면 재현이 안 되며, 공유 상태머신 로직 자체(`pet3d.js`의 구동 시뮬레이션)는 별도로 100초 재생해봐도 문제없이 순환함 - 원인 불명, 우연일 가능성이 높지만 확정 못함. 재현되는 리포트가 실사용 중 반복되면 `test/diag-calico-crash-capture.js`(`window.onerror` 훅 패턴)와 `test/diag-single-window-idle-check.js`(격리된 단일 창 관찰)를 재사용해 더 볼 것.
- **(3D 전용)** cat_a의 줄무늬(`pattern`)-몸통 z-fighting: 8차 QA 라운드가 도입한 `PATTERN_OVER_BODY_NUDGE`/`BELLY_OVER_PATTERN_NUDGE`가 실제로는 박스 위치를 미는 방식(`zOffset`)이라 한쪽 면 마진을 다른 쪽 면 마진과 맞바꾸는 부작용이 있었고(9차 QA 라운드에서 발견 - 왼쪽 줄무늬가 통째로 안 보이는 회귀), 박스 깊이를 양쪽으로 균등하게 키우는 방식(`extraZNudge*2`를 `depth`에 더함)으로 고쳐서 양쪽 면 다 해소됨. 이제 8개 체크포인트 전부에서 패턴이 보이지만, 이전엔 안 보여서 드러나지 않았던 135°/180°/225° 지점에 8차 라운드가 이미 특정해둔 것과 같은 종류(넛지를 키워도 안 줄어드는, `antialias:false` 렌더러의 정상적인 픽셀 경계 반올림)의 아주 미세한 하드엣지 지터가 새로 드러남 - z-fighting이 아니라는 건 넛지 크기에 무관함으로 재확인했으므로 더 쫓지 않음. `pattern`이 있는 종은 현재 cat_a 하나뿐이라 다른 종에서의 검증은 안 함.
- **(3D 전용, 10차 QA 라운드에서 해소 → 11차 QA 라운드에서 메커니즘 자체가 다시 바뀜)** `HEAD_Z_NUDGE`(머리)는 10차 라운드에서 pattern/belly와 같은 위치-이동 버그였음이 확인돼 깊이 대칭 성장 방식으로 고쳐진 뒤 지금도 그대로(머리 자신의 z-fighting 방지용, 안 바뀜). 반면 부위 경계 칼라(`addSeamCollar`, 목/어깨)는 10차 라운드가 "몸통-머리가 코-꼬리 축에서 동심이 아니라 작은 마진으로는 못 이긴다"는 이유로 깊이를 크게 부풀렸던 `SEAM_Z_NUDGE_STANDALONE`/`SEAM_Z_NUDGE_INHERITED` 값 자체가, 정면 각도에서 몸통보다 넓게 튀어나오는 새 버그(11차 라운드 항목 참고)의 직접 원인이었음이 드러나 — z-테스트 경쟁 자체를 없애는 `depthTest:false` 방식으로 교체되면서 두 상수는 완전히 삭제됨, 이제 접합부 깊이는 그 부위 자신의 실제 깊이와 정확히 같은 값을 씀. 10종×8각도 전수 스윕(가시성 + 튀어나옴 여부 둘 다) 통과.
- **(3D 전용)** 수염 자동 대비(`relativeLuminance`, 8차 QA 라운드)는 임계값 0.5 하나로 현재 고양이 4종(0.09/0.29/0.63/0.96)을 정확히 나누지만, 임계값 자체가 4개의 실측값으로만 보정된 것이라 앞으로 얼굴색이 임계값에 아주 가까운(0.4~0.6대) 새 고양이 종이 추가되면 재검토가 필요할 수 있음 - Rec.709 공식을 sRGB 채널값에 직접 적용한 근사치라(완전한 감마 보정 없이) 엄밀한 WCAG 대비비 계산과는 다름, 지금은 "밝다/어둡다 이분류"만 필요해서 근사식으로 충분하다고 판단.
- **(3D 전용, 16차 QA 라운드에서 판정 방식이 자동→명시적 목록으로 다시 바뀜)** 목 경계선(`addNeckSeamCollar`)은 이제 **`NECK_SEAM_SPECIES`에 직접 나열된 7종**(`cat_a`/`cat_tuxedo`/`cat_calico`/`dog_dachshund`/`dog_husky`/`rabbit_b`/`hamster`)에만 적용됨 — 15차 라운드의 자동 판정(`headBodyColorsMatch`, headColor/bodyColor 값 비교)은 16차 라운드에서 완전히 삭제됨(자동 규칙이 산출한 분류가 원하는 것과 안 맞았음 — 특히 허스키: headColor가 bodyColor와 실제로 다른데도 유지 목록에 포함되길 원했음). 나머지 3종(`cat_siamese`/`dog_corgi`/`dog_pomeranian`)은 목 경계선 메시 자체가 씬그래프에 없음. 지오메트리도 16차 라운드에서 박스(모든 각도에서 보이는 "링")에서 `THREE.PlaneGeometry` 기반 평면("정면 캡", 로컬 법선이 코 방향인 +X를 향하도록 90° 회전)으로 바뀜 — 정면(회전 -90°/270°)에서만 카메라를 정면으로 마주보고, 옆모습(0°/180°)에서는 진짜 두께 0이라 화면 면적이 정확히 0, 후면(90°)에서는 `MeshBasicMaterial` 기본 단면 컬링에 걸려 안 그려짐 - 세 경우 다 별도 각도 판정 코드 없이 지오메트리만으로 해결됨(단, 225°/315°처럼 정면에서 살짝 벗어난 각도에서는 평면이 비스듬히 보여 폭이 줄어든 채로는 보임 - 진짜 평면인 한 피할 수 없는 자연스러운 현상, 16차 라운드에서 의도적으로 받아들임). 어깨/엉덩이(다리-몸통) 경계선은 15차 라운드에서 색 일치 여부와 무관하게 **완전히 삭제됨**(`addSeamCollar` 함수 자체가 없어짐) — 이 부분은 16차 라운드에서도 안 바뀜. 귀-머리·꼬리-몸통 경계는 애초에 손댄 적 없음(귀는 대부분 종에서 이미 색이 다르고, 꼬리는 접합부 칼라 자체가 없음) - 여전히 미구현 상태.
- **(3D 전용)** 정면 디테일 보강(6차 QA 라운드)의 수염/콧구멍 버그 둘 다 "이 축이 90°/270° 회전에서 화면 가로폭이 된다"는 같은 종류의 축 착각에서 나왔음(수염은 긴 축을 반대로 잡음, 콧구멍은 얇은 축을 하필 그 축으로 잡음) — `voxel-engine.js`에 좌우 대칭 디테일(Z=±오프셋)을 새로 추가할 일이 있으면, 옆모습(0°)뿐 아니라 반드시 정면(270°) 스크린샷까지 같이 확인할 것(이번에도 옆모습만 먼저 확인했다가 두 버그 다 뒤늦게 발견함).
- 다중 모니터 처리 안 됨 (항상 `getPrimaryDisplay()` 기준)
- 펫 창 위치가 화면 크기 바뀌거나 모니터 구성 바뀔 때 재계산 안 됨
- 24x24(도입 당시 24x20이었다가 토끼 귀 클리핑 때문에 세로로 확장 — 위 항목 참고) 픽셀 그리드가 작아서 표현할 수 있는 디테일에 한계가 있음 — 다리는 이제 hip→knee→paw 2본 관절 체인으로 실제로 굽지만(`legJointAngles`/`pxJoint`), 각 세그먼트 길이가 종별 고정값(도출 공식이 아니라 손으로 튜닝)이라 IK처럼 목표 지점에 정확히 닿도록 풀리는 건 아니고 각도만 맞춰서 "그럴듯하게" 굽음 — 토끼처럼 몸통이 크고 다리가 짧은 종은 다리 힌지 위치(`legY`)가 몸통 아래쪽 경계와 딱 맞아떨어지지 않으면 다리가 몸통 사각형에 그대로 가려져 안 보이는 문제가 있었음(당시 토끼 3종 모두 `legY`가 몸통 바닥보다 위에 있어서 발생 — `legY`를 몸통 바닥과 맞춰서 고침, 이후 새로 추가한 종은 전부 처음부터 이 규칙을 지켜서 authored함). 그루밍은 앞다리를 관절째로 얼굴 쪽으로 접어 올린 채 좌우로 반복 스윕하는 "와이퍼" 동작으로 바뀜(`legJointAngles`의 `frontLegRaise` 분기, 기준각 -150°+`groomWipe`, `-50°` 무릎 고정 — 3D는 여기에 더해 `GROOM_PROTRUDE_Z`로 다리를 얼굴 표면 밖까지 Z축으로 밀어내 가려짐 자체를 없앰) — 더 튜닝하려면 좌표를 직접 찍어보는 스크립트(`legJointAngles` 결과를 `pxJoint`가 반환하는 끝점으로 역추적)로 확인하는 게 눈으로 보는 것보다 정확함
- 캐릭터 외형·색상·걸음걸이·idle 행동 파라미터가 `windows/shared/animal-engine.js`의 `SPECIES` 테이블에 하드코딩되어 있음 (사전 생성 이미지 파일이 없어서 — 매 프레임 이 파라미터로 캔버스에 직접 그림). 캐릭터 추가/수정은 이 파일의 종별 객체와 `idlePool`만 고치면 됨, 별도 빌드/이미지 생성 스텝 없음
- 클릭 통과 판정은 `mousemove` 이벤트가 실제로 발생해야 갱신됨 — 앱이 막 켜졌거나 창이 재배치된 순간 커서가 이미(움직이지 않은 채로) 캐릭터 위에 있으면, 그 시점엔 `mousemove`가 안 뜨므로 main.js가 걸어둔 기본값(클릭 통과)이 그대로 유지되다가 커서를 살짝 움직여야 반영됨 — 실사용에서 거의 못 느낄 수준이지만 완벽한 즉시성은 아님
- 여우(`assets/reference/fox.jpg`)는 참고용으로만 있고 아직 선택 목록에 없음 — 같은 패턴으로 추가 가능
- 자율 이동(patrol)은 여전히 커서를 향해 다가가는 종류로는 없음(위 "이동 정책 변경 2단계" 참고 — 관련 죽은 코드/gait 서브시스템은 그대로 남아있음) — 하지만 위 "위치/이동 시스템 전면 재설계" 라운드에서 완전히 새로운 종류의 자율 이동인 **페이싱**(`movementMode:'pacing'`)이 추가됨: 화면(정확히는 작업표시줄) 좌우 끝을 종의 실제 gait로 왕복, 각 끝에서 30~60초 무작위 휴식 — 옛 patrol과 달리 자유로운 소요 위치가 아니라 항상 화면의 두 확정된 끝점(`edgeWindowX`)만 목적지이고, 커서 반응이 아니라 시간 기반 스케줄. 기본값(`stationary`)은 여전히 patrol 제거 이후의 동작(idle 풀만 재생, 위치 불변) 그대로. 시선 방향 추적(고개/눈)과 몸 전체 방향(`direction`, 위 "커서 방향에 따라 몸 전체가 좌우로 방향을 바꿈" 참고)은 `stationary` 모드에서는 여전히 화면 전체 기준으로 상시 커서를 따라가지만(위 "이동 정책 변경 3단계" 참고, `main.js`의 `startCursorTrackPoll()`이 100ms마다 전역 커서 위치를 폴링), `pacing` 중에는 걷는 방향을 향하도록 `pet.js`가 오버라이드함(위 재설계 라운드 참고 — 머리/눈의 커서 추적 자체는 안 건드리고 몸 전체 방향만); 근접 alert 반응(귀 쫑긋 등)은 두 모드 모두 여전히 `CURSOR_CLOSE_RADIUS`(55px) 기준의 국소 반응임
- 알림 소리(`knockNote`/`playBeep`, pet.js)는 Web Audio API 코드라 이 headless 환경에서 직접 재생해 들어볼 방법이 없음 — 엔벨로프/주파수 수식 검토 + 실제 앱에서 트리거해 콘솔에 오디오 에러 없이 실행되는 것까지만 확인함, "묵직하게 들리는지"라는 실제 청감 판단은 사용자가 직접 들어보고 확인 필요 — 이미 두 번 피드백을 반영해 튜닝했으므로(마림바→우드박스넉) 세 번째 조정이 필요하면 이번에도 방향(음역/배음 비율/디케이 길이) 위주로 구체적으로 알려주면 반영하기 수월함
- 강아지 4종 중 포메라니안은 `bellyUp`(배 하늘로 향한 채 쉬는 포즈)에서 다리가 몸통 실루엣을 완전히 벗어나지 못함 — 옛 rollover 시절부터 있던 `reachScale` 유도 공식(종별 몸높이 대비 다리 길이 비율에서 필요한 만큼만 늘리되 4.2배로 상한)을 그대로 재사용했기 때문(몸높이 대비 다리가 로스터에서 가장 짧은 조합이라 상한을 넘어야 완전히 벗어나는데, 그러면 막대기처럼 보임 - 이전 rollover 세션에서 이미 검증된 트레이드오프, `bellyUp`으로 교체되면서도 이 기하학적 제약 자체는 안 바뀌었으므로 그대로 승계)
- 커서 방향에 따른 몸 전체 방향 전환은 4차 QA 라운드에서 `updateBodyDirection`(이진 좌/우)이 `updateBodyRotationTarget`(연속 각도)으로 교체됨 — 합성 `mousemove` 이벤트로 여러 dx 값을 직접 주입해 목표각이 설계값과 소수점 단위로 일치함을 수치로, 스크린샷 시퀀스로 시각적으로도 확인함(이전엔 "코드 경로가 에러 없이 도는 것까지만" 확인했던 항목이었는데 이번엔 실제 동작 자체를 검증함). 다만 이것도 합성 이벤트 기반 검증이라 실제 마우스로 화면을 이리저리 움직였을 때의 체감(반응 속도·이징 느낌)까지는 headless 환경의 한계로 못 함 - 실사용 중 확인 필요
- 강아지 `sit`(`haunches`)이 `pose.waddleTilt`로 몸 전체를 살짝 뒤로 기울이는데, `drawShadow`는 `pose`를 전혀 안 읽는 고정 위치 타원이라 이 기울임을 따라가지 않아 그림자가 몸통 아래에서 살짝 벗어나 보임 — `rollover`가 이미 최대 70°까지 훨씬 크게 같은 회전을 써왔는데도 지금까지 지적된 적 없던 것과 같은 종류의(사전부터 있던) 단순화라 이번에도 안 건드림. 고치려면 `drawShadow`가 `pose.rollAngle+pose.waddleTilt`를 받아 타원 중심을 같이 회전시키는 정도로 충분할 것
- 시작 프로그램 자동 등록 기능 없음 (수동으로 `npm start` 또는 빌드 후 실행)
- **(2D 자유 드래그 라운드에서 다시 바뀜)** 드래그는 이제 다시 완전히 자유로운 2D(위아래 포함, 위 "자유 2D 드래그 + 낙하 애니메이션" 라운드 참고)로, 릴리즈하면 Y는 항상 작업표시줄로 떨어지고 X는 가장자리 근처면 스냅·아니면 그 자리 그대로 유지됨 — 드래그 **도중**에는 여전히 두 축 모두 clamp가 없어서(스프링-팔로우가 커서를 그대로 쫓아가는 동안) 화면 경계를 얼마든지 넘어갈 수 있고, 화면 맨 위로 끌어올리면 실제로 화면 밖까지 나감(의도된 동작 - "자유롭게" 요청 그대로). 손을 뗀 뒤에는 Y는 항상 화면 안(작업표시줄 바로 위)으로 정정되지만, X는 가장자리 `EDGE_SNAP_RADIUS`(50px) 밖에서 놓으면 여전히 clamp 없이 놓인 자리 그대로 유지 — 화면 경계를 살짝 넘긴 채로 릴리즈하면(예: 드래그 중 커서가 화면 오른쪽 끝을 살짝 넘어간 상태에서 놓임) 창이 화면 밖에 걸친 채로 남을 수 있음(요청받은 범위가 아니라 일부러 안 넣음, 다중 모니터 미지원과 마찬가지로 향후 다듬을 거리)
- 페이싱 모드로 화면 끝까지 걸어가서 멈춘 위치(옛 4방향 `position`처럼 저장되던 값과 달리)나, 드래그로 옮긴 자유 X 위치는 재시작 시 유지되지 않음 — 앱이 켜질 때마다 항상 오른쪽 끝(`edgeWindowX('right', display)`)에서 시작(`createPetWindow()`의 의도적 선택, 옛 자유 X 위치도 원래 저장 안 됐던 것과 같은 선례를 그대로 적용 — 위 재설계 라운드 참고). 설정 창에서 "이동 방식"을 바꿔 저장해도 더 이상 특정 구석으로 다시 스냅시키는 동작은 없음(그 메커니즘 자체가 옛 `position` 필드와 함께 삭제됨) — `movementMode` 저장은 오직 가만히/페이싱 전환만 트리거함.
- idle 감지에서 화면보호기/잠금 상태와의 상호작용 미검증 (수면 연출용 idle 폴링도 동일 API를 쓰므로 같은 사각지대)
- Shimeji류 기능 중 커서 따라 시선 이동은 이제 들어옴(위 "이동 정책 변경 2/3단계" 참고, 화면 전체 기준 상시 추적), 화면 가장자리 매달리기/중력, 다른 창에 올라타기는 여전히 일부러 안 넣음 — 이 앱은 "화면 구석에 늘 떠 있는 가벼운 집중 알림 오버레이"가 목적이라 그런 기능들은 무겁거나(전역 마우스/창 위치 폴링) 목적에 안 맞는다고 판단함. "손으로 집어서 원하는 데로 옮기기"(드래그, 스퀴시+매달림 포즈+스프링 지연 추적으로 "잡힌 느낌" 보강 — 위 드래그 항목 참고)는 클릭 반응·시스템 idle 연동 수면·드래그 이동·커서 반응, 이렇게 네 가지가 목적에 맞는 선으로 들어와 있음
- 드래그 스프링-댐퍼 물리(`main.js`)는 매 틱 실제 OS 커서 위치(`screen.getCursorScreenPoint()`)를 목표로 추적하므로, 자동화 테스트에서 합성 mousemove로는 이 목표 위치를 흉내낼 수 없음(실제 사람 손으로 드래그할 때는 문제 없음) — 헤드리스 검증 스크립트로 스프링 자체의 수렴성(오버슈트 없이 목표에 도달)은 별도 시뮬레이션으로, 창 크기 불변/커서 스타일 전환/스퀴시 발생 여부는 실제 앱 코드로 확인했지만 "합성 이벤트로 실제 커서를 이 시점에 이 위치로 이동시켜 스프링이 정확히 그 좌표를 쫓아가는지"까지는 자동 검증 범위 밖임

## 파일 구조
```
focus-pet/
├── main.js
├── preload.js
├── settingsStore.js
├── package.json          (dependencies 없음 — 순수 Canvas 2D)
├── assets/
│   ├── tray-icon.png, app-icon.png
│   └── reference/ (스타일 레퍼런스 이미지, 앱에서 사용 안 함 — 캐릭터 디자인 참고용)
├── test/
│   ├── sim-invariants.mjs        (npm run test:invariants — 종x시드 대량 시뮬레이션 + checkPoseInvariants 회귀 스위트, 좌표가 올바른지만 검증 - 실제 렌더링 결과는 아래 render-*.mjs들 몫)
│   ├── sim-movement-stats.mjs    (npm run test:movement-stats — 이제는 이동 정책 2단계 후 전 종 walk 비율이 0.0%임을 보여주는 회귀 확인용)
│   ├── sim-idle-rhythm.mjs       (npm run test:idle-rhythm — breathe idle의 시간 점유율/분당 전환 횟수를 측정해 "차분한 리듬"이 유지되는지 보는 회귀 확인용)
│   ├── mini-canvas.mjs           (Node 내장 zlib만으로 만든 헤드리스 Canvas2D 서브셋 + PNG 인코더 - node-canvas 등 네이티브 의존성 없음, 실제 drawCreature를 그대로 실행해 눈으로 볼 PNG를 뽑기 위한 용도)
│   ├── render-poses.mjs          (npm run test:render-poses — sit 포즈/커서 반응 스크린샷을 실제 drawCreature로 렌더링해 PNG로 저장)
│   ├── render-zoom.mjs           (특정 영역을 크게 확대해 크롭하는 헬퍼 - 귀-머리 경계 같은 몇 픽셀짜리 이음매 문제를 96x96 전체 그림에서는 못 알아볼 때 씀)
│   ├── render-bellyup.mjs        (rollover가 삭제되고 대체된 정적 idle `bellyUp`을 강아지 4종 × 3회 반복 렌더링해 eyesClosed/bodySquash/bellyUp 값이 매번 동일하게 정착하는지 확인 - rollover 삭제 후 새 포즈의 안정성 검증용)
│   ├── render-cursor-sweep.mjs   (커서를 화면 좌표 전체에 걸쳐 쓸며 hint.dx/lookDX 수렴값을 숫자로, 스크린샷도 같이 남김)
│   ├── render-cursor-sequence.mjs (같은 애니멀 인스턴스 하나로 커서를 좌→중앙→우로 실시간으로 옮기며 렌더링 - 독립된 인스턴스 비교보다 실제 사용에 더 가까운 시퀀스)
│   ├── render-full-check.mjs     (여러 종의 sit/sleep 전신 렌더 - 패턴/벨리가 몸통 경계를 넘는지 같은 전신 단위 확인용)
│   └── sim-rotation-debug.mjs    (npm run test:rotation-debug — setRotationDebug 훅으로 |rollAngle+waddleTilt| 임계값 초과 프레임을 대량 시뮬레이션에서 탐지)
└── windows/
    ├── shared/
    │   ├── package.json  ({"type":"module"} — animal-engine.js를 Node에서 그대로 import해 테스트하기 위한 스코프 지정일 뿐, Electron 렌더러는 index.html의 <script type="module">을 쓰므로 이 파일과 무관하게 그대로 동작함)
    │   └── animal-engine.js  (픽셀아트 엔진 + 행동 상태머신 — pet/settings 두 창이 공유)
    ├── pet/ (index.html, pet.css, pet.js — 알림/포크 반응 말풍선(#bubble)도 이 창 안에 DOM으로 내장됨, 별도 창 아님 — "말풍선을 다시 펫 창 내장 방식으로" 라운드 참고. 한때 windows/bubble/에 별도 BrowserWindow로 분리됐었지만 그 디렉터리는 완전히 삭제됨)
    └── settings/ (index.html, settings.css, settings.js)
```
