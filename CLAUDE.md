# FocusPet — 설계 스펙 (Claude Code 핸드오프용)

## 목적
작업/공부 중 컴퓨터 화면 한쪽에 떠 있다가, 정해진 방식으로 "집중하세요" 알림을 주는 개인용 데스크탑 펫.
포트폴리오용 아님 — 일상 사용 목적. 브라우저 안이 아니라 **다른 어떤 프로그램을 켜놔도** 위에 떠 있어야 해서 웹앱이 아닌 Electron 데스크탑 앱으로 선택함.

## 핵심 요구사항 (바뀌면 안 되는 것)
1. 다른 프로그램(전체화면 강의 영상 포함) 위에 항상 표시되어야 함
2. 캐릭터는 여러 종류 중 사용자가 설정에서 선택
3. 알림 트리거 방식이 설정 가능해야 함
   - 고정 간격 (강의 시청처럼 입력이 적은 상황용)
   - 활동(키보드/마우스) 감지 — 입력이 끊기면 알림 (문서 작업 중 딴짓 감지용)
   - 위 두 개 동시 사용도 가능해야 함

## 현재 구현 방식
- **메인 프로세스** (`main.js`): 트레이 아이콘, 펫 창 생성, 타이머/idle 폴링, 설정 파일 저장/로드
  - `petWindow.setAlwaysOnTop(true, 'screen-saver')` + `setVisibleOnAllWorkspaces({visibleOnFullScreen:true})`로 전체화면 위 표시 구현
  - 고정 간격: `setInterval`
  - 활동 감지: `powerMonitor.getSystemIdleTime()`을 5초마다 폴링, threshold 넘으면 1회 알림 (재알림은 다시 활동 후 idle 재진입 시)
  - `both` 모드는 두 타이머를 동시에 돌림
  - 이거와 별개로 `startAwayPoll()`이 항상(알림 모드 설정과 무관하게) 5초마다 idle time을 확인해서 3분(`AWAY_IDLE_THRESHOLD_SEC`) 이상 입력이 없으면 `pet-away-state` IPC로 펫 창에 알림 → 펫이 잠들었다가 활동 재개 시 깨어남 (순전히 연출용, 알림 로직과는 독립적)
  - `startCursorTrackPoll()`도 마찬가지로 항상 돌아감 — 100ms마다 `screen.getCursorScreenPoint()`와 `petWindow.getBounds()`를 읽어 커서를 이 창의 로컬 좌표로 환산한 값을 `'cursor-track'` IPC로 펫 창에 보냄. `pet.js`의 로컬 `mousemove`는 커서가 이 340x210 창 위에 있을 때만 발생하는데(드래그-따라가기가 겪는 것과 같은 사각지대), 커서 반응형 인터랙션(아래 "이동 정책 변경 3단계" 참고)은 창 밖에 있는 커서에 대해서도 "어느 쪽에 있는지"를 알아야 해서 추가함 - 드래그-따라가기의 16ms 폴링과 달리 물리적으로 커서에 붙어있을 필요가 없어 100ms로 충분히 가벼움.
  - 펫 창 크기 460x210 → 340x210으로 축소 (패트롤 가능 범위가 너무 넓다는 피드백 반영)
  - **항상 최상단 유지 (`startAlwaysOnTopPoll()`)**: `setAlwaysOnTop(true,'screen-saver')`는 창 생성 시 딱 한 번만 호출돼 있었음 — Windows에서는 다른 앱이 자기도 topmost/screen-saver 레벨을 요청하거나, 특정 전체화면 독점 앱/게임, UAC 프롬프트 등의 상황에서 이 창이 다른 창 뒤로 밀릴 수 있는데(Electron에 이런 상황을 알려주는 이벤트가 따로 없음), 한 번 밀리면 다시 앱을 재시작하기 전까진 계속 뒤에 숨어있게 됨. `startAwayPoll`/`startCursorTrackPoll`과 같은 패턴으로 `startAlwaysOnTopPoll()`을 추가 — 3초마다 `petWindow.setAlwaysOnTop(true,'screen-saver')`를 **무조건** 다시 호출(`isAlwaysOnTop()`으로 먼저 확인 후 필요할 때만 부르는 방식은 일부러 안 씀 — 플래그 자체는 살아있는데 다른 topmost 창이 나중에 그 위로 올라와서 순서만 밀린 경우는 `isAlwaysOnTop()`이 여전히 true를 반환하므로 그 케이스를 못 잡음, 무조건 다시 호출해야 topmost 그룹 안에서도 맨 앞으로 다시 끌어올림). 원인을 개별적으로 감지/분기하는 대신 몇 초마다 통째로 재적용해서 어떤 원인이든 자연 복구되게 하는, 기존 poll들과 같은 접근.
  - **클릭 통과(click-through)**: `createPetWindow()`에서 `petWindow.setIgnoreMouseEvents(true, {forward:true})`를 기본값으로 걸어둠 — 펫 창은 항상 340x210 사각형을 통째로 차지하므로, 이게 없으면 캐릭터가 그려지지 않은 투명한 나머지 영역까지 뒤에 있는 다른 프로그램 클릭을 다 막아버림. `{forward:true}`가 핵심: ignore 상태에서도 `mousemove`만은 렌더러로 계속 전달되게 해서, pet.js가 커서 위치의 실제 알파값을 보고 판단할 수 있게 함(아래 pet.js 항목 참고). 렌더러는 `set-ignore-mouse-events` IPC(`preload.js`의 `setIgnoreMouseEvents`)로 `ignore`를 켰다 껐다 전환 — 판정 로직 자체는 메인 프로세스가 아니라 pet.js에 있고, main.js는 그 결과를 그대로 `BrowserWindow.setIgnoreMouseEvents`에 전달하는 역할만 함.
  - **드래그로 펫 옮기기**: pet.js가 스프라이트 픽셀 위에서 `mousedown`을 받으면(클릭 통과 히트테스트가 이미 `hitState`로 판정해둔 걸 재사용, 다시 샘플링 안 함) `drag-start` IPC를 보내고, `mouseup`에서 `drag-end`를 보냄. 실제로 창을 옮기는 건 메인 프로세스 쪽 — 렌더러의 `mousemove`만으로는 커서가 이미 이 창의 (아직 안 움직인) 영역 밖으로 나가버리면 못 쫓아가므로, 대신 `drag-start`를 받으면 `setInterval`로 16ms마다 `screen.getCursorScreenPoint()`를 읽어서 grab offset(드래그 시작 시점에 커서가 창 안 어디를 잡았는지, `pet.js`가 `mousedown` 이벤트의 `clientX/clientY`로 계산해 IPC에 실어 보냄)만큼 뺀 자리로 창을 옮김. `drag-end`에서 `clearInterval`.
    - **크기가 계속 자라나는 버그와 그 우회**: 처음엔 `win.setPosition(x,y)`로 옮겼는데, 이 특정 창(투명+frameless+alwaysOnTop이면서 canvas가 매 프레임 다시 그려지는 중)에다 `screen.getCursorScreenPoint()`를 16ms마다 같이 호출하면서 `setPosition`을 반복 호출하면 창 높이가 폴링이 계속되는 동안 끝없이 커짐(재현 스크립트로 최소 조합을 좁혀봄 — `setPosition`만 반복 호출, 이 창인데 드래그 상태 아님, 빈 창에 커서+setPosition 조합, 이 창+드래그 상태인데 poll이 고정값을 쓰는 경우는 전부 안정적이었고, "이 창 + 실제로 mousedown이 눌린 상태 + `getCursorScreenPoint()`로 읽은 값으로 매 틱 반복 호출"이라는 조합에서만 재현됨 — 정확한 근본 원인까지는 못 밝혔지만 재현 스크립트로 조건은 확인함). `setPosition(x,y)` 대신 `setBounds({x,y,width,height})`로 바꾸고 `width`/`height`를 드래그 시작 시점의 `win.getBounds()`에서 딱 한 번만 읽어서 매 틱 명시적으로 다시 못박아주는 걸로 우회함 — 원인은 불명확해도 결과적으로 크기가 절대 안 변하는 걸 반복 확인함.
    - **클릭 통과와의 상호작용**: 드래그 중에는 `pet.js`가 알파 히트테스트를 아예 건너뛰고 `hitState`(=ignore 상태)를 드래그 시작 시점 값으로 고정 — 안 그러면 창이 커서를 쫓아가는 도중 한두 프레임 뒤처질 때 그 순간의 커서 좌표가 (아직 안 움직인) 창의 투명한 부분이나 창 밖으로 샘플링돼서 클릭 통과로 되돌아갈 수 있고, 그러면 이후 `mouseup`을 이 창이 아예 못 받아서 드래그가 영원히 안 끝남.
    - **patrol/걷기 애니메이션 일시정지**: 드래그 중엔 리마인더 말풍선이 떠 있을 때와 똑같이 `paused` 플래그로 처리 — `animal.update(dt, false, ...)`가 강제로 idle을 유지(완전히 얼어붙는 건 아니고 idle 애니메이션은 계속 돎, 기존 말풍선 동작과 동일한 컨벤션). 드래그 시작~끝 동안 `x`(창 안에서의 로컬 패트롤 좌표)는 안 건드리므로, `mouseup` 시점에 창만 새 위치로 옮겨진 채로 그 자리에서 그대로 patrol이 재개됨 — 원래 구석으로 돌아가는 로직이 따로 없어도 자연히 "놓은 자리에서 계속"이 됨.
    - **`mousedown`+`mouseup`이 같은 엘리먼트에서 끝나면 `click`도 같이 발동**해서 드래그를 끝낼 때마다 `poke()`가 한 번 더 불릴 뻔했음 — `mouseup` 핸들러에서 `suppressNextClick` 플래그를 세팅해 그 다음 `click` 한 번을 소비하도록 처리.
    - **"잡힌 느낌" 연출**: "그냥 순간이동하듯 옮겨지는" 느낌이 아니라 실제로 손에 집힌 것처럼 보이도록 4가지 후보(스퀴시, 매달림 포즈, 스프링 지연 추적, grab/grabbing 커서)를 전부 시도해보고 넷 다 자연스럽게 읽혀서 그대로 유지함 — 하나씩 제거해보는 대신 각각을 독립적으로 검증(스퀴시는 눈으로는 안 보일 만큼 미묘해서 수치 트레이스로, 스프링은 물리 시뮬레이션으로, 나머지는 스크린샷/`getComputedStyle`로)한 뒤 넷 다 통과해서 뺄 이유가 없었음.
      - **스퀴시 + 매달림 포즈**: `createAnimal()`에 `setHeld(held)`를 추가 — `mousedown`에서 `animal.setHeld(true)`, `mouseup`에서 `animal.setHeld(false)`를 호출(`pet.js`). 내부적으로는 `pinnedSleep`과 같은 우선순위 구조의 새 오버라이드 상태 `pinnedHeld`를 두고, `updateBehaviorState()`에서 `pinnedSleep`보다도 먼저 체크해서 `held`라는 새 idle behavior로 강제 진입시킴(잡혀 있는 동안은 걷기/기존 idle 전부 무시). `held.apply()`는 `Math.exp(-t*8)*0.5`로 감쇠하는 스퀴시 펄스(순간적으로 눌렸다가 `bodySquash 0.12`로 잦아듦), `dangle=1`(다리를 축 늘어뜨림 — 아래 참고), 머리/귀도 처짐 방향으로 오프셋을 준다. 스퀴시는 96px 화면에서 체고 변화가 약 4px(그리드 1.25유닛)뿐이라 스크린샷 두 장을 눈으로 비교해서는 차이를 못 느꼈음 — 매트릭스 추적 mock ctx로 몸통 사각형의 실제 world-space 높이를 프레임별로 찍어보고서야(6→4.75유닛으로 즉시 줄었다가 ~0.5초에 걸쳐 5.75로 이완) 실제로 동작한다는 걸 확인함(이전 턴의 "투명 배경이 흰색으로 보여서 안 그려진 줄 알았다"는 교훈과 같은 맥락 — 눈으로 안 보인다고 안 되는 게 아니라 수치로 확인해야 함).
      - **다리 축 늘어짐(dangle)**: `legJointAngles()` 맨 끝에서 `pose.dangle`이 0보다 크면 hip/knee 각도를 `pose.dangleSway`(잡힌 채로 살짝 좌우로 흔들리는 sine)와 고정된 무릎각(12°) 쪽으로 `lerp`해서 덮어씀 — 걷기/tuck 등 기존 다리 로직 위에 얹는 최종 오버라이드라 다른 종별 관절 파라미터를 하나도 안 건드리고 전 종에 동일하게 적용됨. 꼬리도 같은 문제(다른 idle이 세팅 안 하는 필드가 남는 문제, 위 "포즈 전환 글리치" 항목과 동일한 종류)를 피하려고, `updateTailFlavor()`가 매 프레임 종별 기본 흔들림으로 덮어쓰기 전에 `pose.tailOverride` 플래그를 체크해서 세팅돼 있으면 건너뛰게 만듦(`held`와 아래 `tailtwitch` 둘 다 이 플래그를 씀) — 처음엔 `held.apply()`에서 `tailPhase`/`tailAmp`를 직접 세팅했는데 `updateTailFlavor()`가 그 다음에 실행되면서 매번 종별 기본값으로 도로 덮어써버려서 꼬리만 안 늘어지는 버그가 있었음, 이 플래그로 고침.
      - **스프링 지연 추적**: `main.js`의 `drag-start` 핸들러를 "커서 - offset 자리로 즉시 `setBounds`" 방식에서 스프링-댐퍼 물리 시뮬레이션으로 교체 — 매 16ms 틱마다 `targetX/Y`(커서 기준 목표 위치)와 현재 위치 `curX/Y`의 차이에 `SPRING_STIFFNESS(180)`를 곱하고 현재 속도에 `SPRING_DAMPING(16)`을 곱해 뺀 걸 가속도로 삼아 `velX/Y`, `curX/Y`를 적분(오일러 적분, `dt`는 실제 틱 간격을 측정해서 사용, 최대 50ms로 clamp). 일부러 underdamped(진동이 완전히 안 죽고 살짝 오버슈트하는 영역)로 튜닝해서 "뻣뻣하게 따라온다"가 아니라 "느슨하게 매달려 흔들리며 따라온다"는 느낌을 냄 — 별도 스크립트로 150px 스텝 입력에 대한 반응을 시뮬레이션해서 목표치에 수렴은 하되(발산하지 않고) 과도한 오버슈트는 없는 걸 확인한 뒤 실제 값 채택. 이전 턴에 고친 "창 크기가 계속 자라나는 버그" 우회(=`setBounds`에 `width`/`height`를 매 틱 명시적으로 다시 넘기기)는 그대로 유지 — 물리 시뮬레이션으로 바뀐 건 x/y를 계산하는 방식뿐, `setBounds` 자체의 사용법은 안 건드림.
      - **grab/grabbing 커서**: `pet.css`의 `#pet-canvas` 기본 커서를 `pointer`에서 `grab`으로, `pet.js`가 `mousedown`/`mouseup`에서 `canvas.style.cursor`를 `grabbing`/`grab`으로 직접 토글.
- **설정 저장**: `electron-store` 같은 외부 라이브러리 없이 `app.getPath('userData')/settings.json`에 직접 JSON 읽기/쓰기 (`settingsStore.js`). `LEGACY_CHARACTER_MAP`이 예전 로스터의 캐릭터 키(`cat_b`, `rabbit_a`, `rabbit_c`, `panda`, 재분류된 `dog`, 삭제된 `dog_pug` 등)를 로드 시점에 살아있는 키로 자동 치환 — 이 매핑에도 없고 현재 `VALID_CHARACTERS`에도 없는 값이면 `DEFAULTS.character`(`cat_a`)로 조용히 폴백. 저장된 `settings.json`을 수동으로 마이그레이션할 필요 없이 로스터를 계속 정리/확장할 수 있게 하려는 장치.
- **펫 창** (`windows/pet/`): frameless, transparent, `focusable:false`. 캐릭터는 **픽셀아트(도트) 스프라이트**로 렌더링 — 한 차례 Three.js 기반 실제 3D 로우폴리로 시도했다가 다시 픽셀아트로 되돌림.
  - **왜 3D를 접고 픽셀아트로 돌아왔나**: 이 펫은 96x96px 정도의 작은 크기로 화면 구석에 떠 있으면서 캐릭터 10종이 실시간으로 서로 구분되어야 함. 이 크기에서는 3D의 "정확한 비율과 조명"보다 픽셀아트의 "과장된 실루엣과 평면 색상"이 훨씬 잘 통함 — Mac Pet 같은 실제로 성공한 메뉴바/노치 데스크탑 펫들이 전부 이 방식을 쓰는 이유이기도 함. 3D 버전은 목-몸통 이음새 문제는 깔끔히 해결했지만, 그 정교함 자체가 작은 크기에서는 잘 안 보이고 오히려 캐릭터 간 차별점(귀 모양 등)이 실루엣에 묻혀버리는 문제가 있었음.
  - **엔진** (`windows/shared/animal-engine.js`, pet/settings 두 창이 공유, Three.js 종속성 완전히 제거함 — `package.json`에 dependencies 없음, 순수 Canvas 2D): 24x24 논리 픽셀 그리드에 PX=4로 그려서 정확히 96x96px 캔버스가 됨(스케일 단계 없음, 별도 이미지 파일도 없음 — 매번 코드로 그림). 세로 그리드는 원래 20이었다가 24로 늘림 — 토끼 hop 애니메이션 피크에서 `bodyBob`이 크게 음수로 튀면서 귀(`earStyle:'long'`)까지 딸려 올라가는데, 20에서는 그 위로 남는 여백이 없어서 귀 끝이 캔버스 상단 경계에 그대로 잘려나갔음(아래 "토끼 귀 클리핑" 항목 참고). 늘어난 4줄은 전부 기존 콘텐츠 "위쪽"에 여백으로만 추가한 것 — 모든 종의 y좌표를 기존 값에서 +4 해서 authored했고(`GROUND_Y`도 18→22로 같이 이동), 바닥 기준 상대 위치/비율 자체는 그대로 유지됨. 몸통/머리/귀는 사각형(모서리 1px씩 깎은 `pxSoft`로 살짝 둥글게, 단 5px 미만인 작은 도형은 모서리를 깎으면 오히려 십자 모양으로 뾰족해 보여서 그냥 각지게 둠 — 처음에 판다 귀가 뾰족한 십자 모양으로 나온 버그를 이렇게 고침)로 그리는 방식.
    - **토끼 귀 클리핑 버그 고침**: 위 그리드 확장(20→24)에 더해 hop 궤적 자체의 정점 높이(`GAIT_PRESETS.hop`의 `height`)도 5→4로 살짝 줄여 여유를 더 뒀음 — 두 조치를 합쳐 hop 정점에서 귀 끝까지 캔버스 상단까지 최소 2그리드유닛(8px) 이상 남도록 확인함(계산으로 도출한 값이 아니라 실제로 200프레임을 그려서 화면에 찍힌 모든 도형의 최소 y좌표를 추적하는 스크립트로 검증 — 눈으로 스크린샷 몇 장 보고 "괜찮아 보인다"로 끝내지 않고, 한 사이클 전체를 훑어서 한 번도 0 밑으로 안 내려가는지 확인).
    - **관절 체인 (`pxJoint`/`drawLegChain`/`legJointAngles`)**: 다리는 더 이상 통짜 사각형이 아니라 어깨/고관절→무릎→발끝으로 이어지는 2본(upper+lower) 체인 — `pxJoint(ctx, hingeX, hingeY, angleDeg, length, width, color)`가 힌지에서 `angleDeg` 방향으로 `length`만큼 뻗은 사각형 하나를 그리고 그 끝점을 반환, `drawLegChain`이 이걸 두 번(hip→knee, knee→paw) 이어 붙임. 목도 같은 방식으로 몸통 상단 고정점과 머리 사이를 잇는 1본 `neck` — 각도는 매 프레임 `headDX/headDY`(기존 idle behavior들이 그대로 쓰는 필드)로부터 `atan2`로 역산해서 그리므로, groom/sleep처럼 머리가 크게 움직이는 idle을 새로 손대지 않아도 목이 따라 굽음(길이는 원래 종별 고정값이었다가 이후 실제 거리 기반 계산값으로 바뀜 — 아래 "sit 포즈가 몸통이 겹쳐 보이는" 항목 참고). 꼬리는 배열의 첫 세그먼트(몸통에 가장 가까운 것)만 고정 위치에 rigid하게 그리고, 그 다음 세그먼트부터는 이전 세그먼트 끝점에서 이어지는 체인 + 세그먼트별로 위상이 지연된(phase-lagged) sine 파동을 누적해서 마디마디 휘어지는 스윙을 만듦(진폭은 꼬리 끝으로 갈수록 커짐) — 정지 상태(파동 진폭 0)에서는 원래 하드코딩된 좌표를 그대로 복원하도록 세그먼트 간 각도/길이를 원래 좌표에서 역산해서 씀. 각도 컨벤션: 0°=아래, 양수=시계방향(즉 -x/꼬리쪽으로 스윙) — `pxJoint`의 회전행렬에서 직접 유도한 값이라 `legJointAngles`의 다른 계산도 전부 이 부호를 기준으로 맞춤. `drawLegChain`은 선택적으로 `pawColor`/`pawFraction`도 받음 — 아래 다리뼈를 `color`로 그린 뒤 그 끝쪽 일부(기본 35%)만 `pawColor`로 같은 각도/폭으로 겹쳐 그려서 발끝만 다른 색인 2톤 다리를 표현(예: 턱시도 고양이의 검정 다리+흰 양말 — 아래 관련 버그 항목 참고). 안 쓰는 종은 완전히 무해.
    - **종별 관절 차별화** (`SPECIES[...].joints`): 다른 파라미터들과 같은 철학으로(파일 상단 코멘트에 있는 "10종 정도뿐이니 도출 공식보다 종별로 손으로 명시하는 게 더 안전하다"는 원칙) 종마다 `front`/`back` 다리의 길이·스윙폭·무릎 굽힘폭·tuck(웅크림) 목표각을 전부 따로 명시. 특히 토끼(`rabbit_b`)는 뒷다리가 hock(발목)을 크게 접는 실제 토끼 해부 구조를 반영해 뒷다리 `tuckHip/tuckKnee`를 훨씬 크게(95°/130°) 잡고 앞다리는 거의 안 움직이게(`swingAmp` 작음) 해서 "뒷다리가 일을 다 하는" 실루엣을 냄 — 고양이/강아지는 앞뒤가 더 대칭적. 강아지 중 `floppyRollover:true`가 설정된 종(`dog_dachshund`)은 `rollover` 중 무릎 각도에 sine 흔들림을 얹어 헐렁한 다리 느낌을 추가. `dog_corgi`/다리 짧은 품종은 스윙폭/무릎폭을 작게 잡아 뭉툭하고 덜 굽는 다리로, 햄스터는 반대로 스윙폭을 크게 잡아 짧고 빠른 종종걸음으로 차별화.
    - **고양이 `sit` 포즈가 뭉개져 보이던 버그**: 원인은 `tuckHip`/`tuckKnee` 수치 자체 — 고양이 4종 전부 `tuckHip`이 55~60°(고관절이 크게 뒤로/옆으로 스윙)인데 `tuckKnee`는 75~85°(무릎은 상대적으로 덜 굽음)로 잡혀 있어서, 앉았을 때 발끝이 몸통 아래로 깔끔하게 접히는 게 아니라 다리 전체가 뒤로 크게 휙 젖혀지면서 발이 허공에 어정쩡하게 걸리는 모양이 나왔음(스크린샷을 직접 렌더링해서 확인 — 무릎 각도만 눈으로 훑어서는 안 보이던 문제라 실제로 sit 포즈를 그려서 눈으로 봐야 알 수 있었음). 실제 고양이가 식빵 자세로 앉을 때는 반대 — 고관절 스윙은 작게, 무릎을 크게 접어서 발끝이 몸통 바로 아래 붙게 되는 쪽. 그래서 고양이 4종의 `tuckHip`/`tuckKnee`를 앞다리 `15°/125°`, 뒷다리 `20°/135°`로(스윙 최소화, 무릎 폴딩 최대화) 다시 잡음 — 강아지/토끼/햄스터의 sit도 같이 렌더링해서 확인했는데 이쪽은 원래도 문제없었어서(하체 비율이 다르고 tuck 각도 자체도 덜 극단적) 건드리지 않음, 즉 이 버그는 고양이 4종 한정.
    - **sit 포즈가 "몸통이 겹쳐 보이는" 것처럼 남아있던 진짜 원인 (목 앵커/길이 버그)**: 위 tuckHip/tuckKnee 수정으로 다리는 고쳐졌지만, 이후에도 턱시도 등 일부 종은 sit 상태에서 목 부근에 몸통 색과 다른 덩어리가 붙어있는 것처럼 보이는 문제가 남아있었음. 각도 숫자를 또 조정하는 대신 목 관절의 앵커/회전 기준점 자체를 다시 짚어봄 — `neck`은 `pxJoint(ctx, neckBaseX, neckBaseY, neckAngle, neckLen, ...)`로 그리는데, `neckLen`이 `cfg.joints.neckLen`(종별 고정값)이었음. 고정 길이 뼈는 머리 "방향"만 가리킬 뿐 머리에 실제로 "닿지는" 않음 — 평소(걷기)엔 머리와 몸통이 authored 위치상 우연히 거의 붙어 있어서 이 차이가 안 보였지만, `sit`/`sleep`처럼 `bodyBob`이 커서 몸통 상단이 머리보다 훨씬 아래로 내려가는 포즈에서는 목 베이스와 머리 부착점 사이의 실제 거리가 `neckLen`보다 훨씬 커짐 — 그런데도 뼈는 여전히 `neckLen`만큼만 그려지니, 두 점을 잇지 못하는 짧고 두꺼운 대각선 막대가 허공에 떠 있는 것처럼 보였고, 이게 몸통색(`cfg.bodyColor`)이라 몸통과 안 붙어있으면 "몸통 조각이 삐져나온 것"처럼 읽혔음(같은 색 종은 우연히 안 보였고, 흰 패치와 인접한 턱시도/허스키 같은 종에서 두드러짐). **고침**: `neckLen`을 종별 고정값 대신 `Math.max(0.3, Math.hypot(headAttachX-neckBaseX, headAttachY-neckBaseY))`로 매 프레임 실제 거리를 계산해서 사용 — 이제 목은 항상 몸통과 머리를 정확히 잇는다. 이것만으론 부족해서(폭이 여전히 두꺼워 짧은 거리에서도 눈에 띔) 10종 전부 `neckW`도 대략 절반으로 줄임(예: `cat_a` 2.2→1.2, `dog_husky` 2.6→1.5). `cfg.joints.neckLen` 필드 자체는 이제 안 쓰이지만(길이는 항상 계산값), 종별 설정에는 그대로 남아있음 — 지우는 게 맞지만 이번엔 렌더링 로직만 고치는 데 집중함. 10종 전부 sit/sleep 포즈로 진입시켜 스크린샷으로 확인해서 해결 확인함.
    - **다리 4개 표현 (`drawLegPair`)**: 옆모습에서 앞다리1+뒷다리1(사실상 좌우 다리가 완전히 겹쳐 보임)로 뭉툭해 보이던 걸, 각 앞/뒤 다리 자리에 "먼 쪽"과 "가까운 쪽" 두 개를 살짝 어긋나게 그려서 4개 다리가 다 있는 것처럼 보이게 함 — `drawLegPair(ctx, cfg, pose, isFront, legY)`가 `legJointAngles()`로 구한 hip/knee 각도를 그대로 쓰되, "먼 쪽" 다리는 x를 `Math.max(0.9, w*0.7)`만큼 뒤(왼쪽)로 밀고 폭을 1px 얇게, hip 각도도 `*0.85`로 살짝 덜 벌어지게 그려서 원근감 있는 실루엣을 냄(각도까지 다르게 한 건 두 다리가 완전히 평행하게 겹쳐 보이면 "그냥 두꺼운 다리 하나"로 다시 읽힐 수 있어서). 처음엔 오프셋을 `Math.max(0.4, w*0.35)`로 작게 잡았는데 확대 스크린샷에서 "다리가 4개"가 아니라 "다리가 살짝 삐뚤빼뚤한 하나"로 보여서, 오프셋을 눈에 띄게 키운 지금 값으로 재조정 — 걷기/앉기 각 포즈에서 다시 확인한 뒤 이 방식을 그대로 채택함(사전에 사용자가 허락한 "지저분해 보이면 2다리 방식으로" 대안은 필요 없었음).
    - **몸통에서 안 떨어지게 하기**: 목/꼬리 앵커는 몸통의 실제 그려지는 상단 가장자리(`bodyBob`뿐 아니라 `bodySquash`로 인한 이동분까지)를 그대로 따라감 — 처음엔 꼬리가 `-bodyBob*0.4`만 반영해서 걷기 중 살짝 처지는 정도로만 튜닝돼 있었는데, `sleep`/`sit`처럼 `bodyBob`이 크고 양수인 데다 `bodySquash`까지 겹치는 포즈에서는 몸통 상단이 훨씬 크게 내려가는데 꼬리는 그만큼 안 따라가서 몸통과 꼬리 사이에 눈에 띄는 빈 사각형 틈이 생기는 걸 스크린샷으로 확인하고 고침(아래 "포즈 전환 글리치" 항목 참고) — 이제는 몸통 rect가 쓰는 것과 동일한 `bodyBob + squashY*0.5` 오프셋을 목/꼬리 둘 다에 그대로 적용.
    - **캐릭터 구분 원칙**: 사실적 비율보다 "실루엣과 색을 확실히 다르게"가 우선. `assets/reference/`의 cat1·cat2·dog1·rabbit1·2·3 사진에서 "그 이미지를 봤을 때 떠오르는 핵심 특징"(귀 모양, 털 색, 무늬)만 뽑아 단순화한 것이지 사실적 재현이 목표가 아님. `fox.jpg`는 참고용으로만 있고 목록엔 없음.
    - **2차 로스터 개편**: 초기 8종(고양이 A/B, 강아지, 토끼 A/B/C, 판다, 햄스터) 중 고양이 B(회색+흰 패치), 토끼 A(보라), 토끼 C(분홍, 귀 벌어짐), 판다 4종을 완전히 삭제하고(`SPECIES`에서 키 자체를 제거 — `CHARACTERS`가 `Object.keys(SPECIES)`로 자동 도출되므로 설정 화면 목록도 같이 빠짐) 고양이 3종·강아지 4종을 실제 품종/무늬 특징을 반영해 새로 추가함. 기존 `dog`(탄 색+처진 귀) 항목은 별도로 남기지 않고 `dog_dachshund`로 재분류(귀 스타일은 그대로 `floppy` 유지, 몸통만 길고 낮게 재작업) — 그래서 강아지가 5종이 아니라 4종. 최종 로스터 10종:
      - **고양이 A** (유지): 주황 태비(세로 줄무늬)
      - **턱시도** (`cat_tuxedo`): `assets/reference/tuxedo-cat-reference.jpg`(정면 사진) 기준으로 재설계 — 이마/귀/눈 주변은 검정, 눈 사이~코~입 주변으로 흰 블레이즈가 세로로 내려오는 좌우대칭 무늬, 넓은 흰 가슴, 발끝만 흰색, 분홍 코, 초록빛 눈. 정면 사진의 "얼굴 중앙 세로 블레이즈"를 옆모습 스프라이트로 옮기면 "머리 앞쪽 가장자리를 따라 눈높이~턱까지 밝은 패치"가 되는 셈이라(이 종은 항상 옆에서 본 모습만 그리므로), 기존에 하나의 사각형이던 `snout`을 배열로 받을 수 있게 확장(위쪽은 좁고 아래쪽은 넓어지는 패치 2개를 쌓음 — `drawCreature`가 `Array.isArray(cfg.snout)`이면 순서대로 다 그림, 기존처럼 객체 하나만 주는 종들은 그대로 동작). 코 색도 새로 추가한 `nose.color`(없으면 기존처럼 `darkColor`)로 분홍으로 오버라이드. 꼬리는 마지막 세그먼트를 다른 것보다 크게 authored해서 끝이 풍성해 보이게 함(별도 렌더링 기법 없이 세그먼트 크기만으로). *(디버깅 메모: 스크린샷을 처음 봤을 때 흰 블레이즈가 안 보여서 렌더링이 깨진 줄 알았는데, `getImageData`로 좌표를 직접 찍어보니 정확히 흰색(255,255,255,255)으로 그려지고 있었음 — 캔버스 투명 배경이 스크린샷 뷰어에서 흰색으로 보이는 바람에 흰 블레이즈가 배경과 구분이 안 됐던 것뿐이었음. 캔버스에 임의의 진한 배경색을 깔고 다시 스크린샷을 떠서 확인함. 흰색/밝은 색 패치를 새로 디자인할 때는 이 함정을 기억해둘 것 — 투명 배경 스크린샷만 보고 "안 그려진다"고 판단하면 안 됨.)*
      - **삼색이** (`cat_calico`): 흰 바탕에 검정+주황 패치. 실제 칼리코 특유의 "패치가 좌우 비대칭"을 살리려고 양쪽 귀 색을 다르게 줌(`ear.color`/`ear.color2` — 이 종만 쓰는, `drawEarPointy`에 새로 추가한 필드). 초록 눈.
      - **샴** (`cat_siamese`): 몸통은 크림색인데 얼굴·귀·다리·꼬리만 진한 seal-brown "포인트" 컬러. 얼굴이 몸통과 아예 다른 색이어야 해서 `headColor`(새 필드, 없으면 기존처럼 `bodyColor`를 그대로 씀 — `drawCreature`의 머리 그리는 줄만 수정) 도입 계기가 된 종. 파란 눈.
      - **닥스훈트** (`dog_dachshund`, 기존 `dog` 재활용): 몸통을 grid 폭의 80%(19/24유닛)까지 늘리고 높이는 5유닛으로 낮춤 — 이 로스터에서 가장 극단적인 비율. 적갈색, 처진 귀.
      - **코기** (`dog_corgi`): 다리가 아주 짧고(관절 `upperLen`/`lowerLen` 0.6) 몸통이 김. 귀는 `pointy` 스타일을 다른 종보다 크게 키워서 사용(같은 그리기 함수를 재사용하되 파라미터만 다르게 하는 원칙 그대로). **꼬리 없음**(`hasTail:false`) — 실제 펨브록 웰시코기가 선천적 단미인 특징을 그대로 반영, 다른 강아지들과 구분되는 포인트.
      - **허스키** (`dog_husky`): 회색 몸통 + 흰 얼굴(`headColor`)/가슴, 파란 눈, 선 귀. 꼬리를 몸통 뒤가 아니라 등 위로 말아 올라가는 모양으로 authored — 꼬리 체인은 세그먼트 rest 좌표를 어떻게 잡느냐의 문제일 뿐이라(아래 tail-chain 설명 참고) 코드 변경 없이 좌표만으로 표현됨. `swishTail:false`라 거의 고정된 채로 살짝만 흔들림.
      - **포메라니안** (`dog_pomeranian`, 퍼그 대체 — 아래 "3차 로스터 조정" 참고): 허스키와 같은 어휘(선 귀 + 등 위로 말린 꼬리)를 쓰면서도 한눈에 갈리도록, 허스키는 "날렵한 스피츠" 느낌인 반면 이쪽은 몸통 폭/높이를 거의 1:1로 잡아서(다른 강아지는 전부 옆으로 긴 비율) "동그란 털뭉치" 실루엣으로 차별화. 크림/골드 색 — 기존 오렌지 계열(고양이A, 닥스훈트)과도 채도/톤을 다르게 잡음. 등 쪽에 dither 하이라이트(아래 "그라데이션/디더링" 항목)를 얹어 "복슬복슬함"을 텍스처로 흉내.
      - **토끼** (`rabbit_b`, 라벨에서 A/B/C 구분 뗌 — 이제 한 종뿐이라): 크림+분홍 귀끝, 유지.
      - **햄스터**: 더 동글동글하게 재설계 — 아래 "3차 로스터 조정" 참고.
    - **3차 로스터 조정**: 퍼그(`dog_pug`)를 삭제하고 **포메라니안**(`dog_pomeranian`)으로 교체 — 허스키 추가 이후로 "쫑긋한 귀 + 등 위로 말린 꼬리" 어휘를 이미 쓰고 있어서, 치와와(초소형+큰 귀)와 포메라니안(동그란 털뭉치) 중 실루엣이 기존 로스터(특히 몸통이 짧고 낮은 코기/퍼그 계열)와 가장 안 겹치는 쪽으로 포메라니안을 골랐음 — 자세한 차별화 근거는 위 포메라니안 항목 참고. 햄스터도 재설계 — 기존 버전은 몸통이 머리보다 별로 안 커서 "그냥 작은 설치류" 정도로 밖에 안 읽혔는데, 실제 햄스터 특징(거의 목이 안 보일 정도로 동글동글한 몸통, 짧고 통통한 다리, 작은 귀, 꼬리 없음, 볼주머니)을 살려 몸통을 더 크고 둥글게(`bodyW`/`bodyH` 확대), 다리 관절 길이를 더 줄이고(`upperLen`/`lowerLen` 0.5~0.6), 볼주머니(`cheeks`)와 배 무늬를 더 키움.
    - **그라데이션/디더링 실험** (`pxDither`, `SPECIES[...].shade`): 픽셀아트 느낌을 유지하면서 입체감을 줄 수 있는지 시도 — 매끈한 알파 블렌딩 대신 물리 픽셀 단위 체커보드 디더링(`pxDither(ctx,x,y,w,h,colorA,colorB)`, 논리 그리드 유닛이 아니라 실제 화면 픽셀 하나하나를 번갈아 칠함)으로 두 색 사이 중간 톤을 흉내내는, 레트로 픽셀아트에서 실제로 쓰이던 기법. 몸통을 그린 직후 상단에 얇은 띠(1.5~2유닛) 하나만 밝은 색으로 디더링해서 "위에서 빛을 받는 둥근 몸통" 느낌을 냄 — `cfg.shade = {highlight, bandH}`가 있는 종만 적용, 없으면 기존처럼 단색 그대로. 96x96px 실제 크기로 스크린샷을 떠서 확인해보니(확대한 스크린샷만 보면 체커판이 그대로 보여서 안 어울려 보일 수 있음 — 실제 크기에서 판단해야 함) 거슬리는 노이즈가 아니라 은은한 하이라이트 정도로 읽혀서, 샴 고양이 하나만 시도해보고 끝내지 않고 **닥스훈트**·**포메라니안**에도 같은 걸 적용함(닥스훈트는 매끈한 긴 몸통이라 하이라이트가 자연스럽게 어울리고, 포메라니안은 "복슬복슬함"을 텍스처로 흉내내는 용도를 겸함). 나머지 7종은 안 건드림 — 전부 다 입히면 효과가 특별해 보이지 않고, 무늬가 있는 종(고양이A 줄무늬, 삼색이 패치)은 패턴과 디더링이 겹쳐서 지저분해질 위험이 있다고 판단.
    - **샴 고양이 그라데이션 위치/방향 수정 (몸통 → 얼굴, 평평한 밴드 → 방사형)**: 위 `shade`는 몸통에 넣은 것이었는데, 실제로 원했던 건 샴 특유의 콜러포인트(눈코입 주변 진하고 이마/뺨 바깥쪽으로 갈수록 밝아짐) — 그래서 `cat_siamese`에서 `shade`를 떼고 얼굴 전용 `faceShade`를 새로 만듦(`headColor`가 이미 있는 종에서 얼굴을 몸과 다른 색으로 다루는 것과 같은 맥락, `faceShade`는 그 `headColor` 위에 한 겹 더 얹는 하이라이트). **1차 시도**: `pxDither`를 그대로 재사용해서 머리 상단 가장자리(이마 쪽)에 가로 밴드 하나, 머리 왼쪽(뒤통수/귀 쪽) 가장자리에 세로 밴드 하나, 이렇게 두 개의 평평한 50/50 체커 밴드를 겹쳐 발랐음 — 렌더링 자체는 됐지만(`getImageData`로도 확인) 크롭 스크린샷으로 다시 보니 "그라데이션"이 아니라 머리의 절반 정도를 뒤덮는 진한 체커보드 블록과 나머지 절반의 단색 영역이 딱 붙어있는 L자 모양 이색 분할로 읽혔음 — 두 밴드가 각자 독립적으로 하드 엣지에서 뚝 끊기다 보니 서로 안 섞이고, 폭(2.5유닛, 머리 폭 7유닛의 1/3 이상)도 너무 넓어서 "은은한 하이라이트"가 아니라 "칠하다 만 것"처럼 보였음. **2차 수정(현재)**: `pxDither` 대신 새로 만든 `pxDitherRadial(ctx,x,y,w,h,colorA,colorB,focusX,focusY,radius)`로 교체 — 4x4 Bayer 행렬(`BAYER4`, 정렬 디더링에서 쓰는 표준 임계값 행렬)을 이용해 각 픽셀의 임계값과, 그 픽셀이 `focusX/focusY`(기본값: 눈 중심)로부터 얼마나 떨어져 있는지(`radius`로 정규화한 0~1 density)를 비교해서 색을 결정 — focus에 가까울수록(density→0) 진한 `headColor`가, 멀수록(density→1) 밝은 `highlight`가 우세해지는 진짜 방사형 낙차를 만듦. 두 개의 독립된 사각 밴드 대신 머리 전체 영역에 하나의 연속된 함수를 적용하는 방식이라 하드 엣지 없이 자연스럽게 이어짐. 눈으로 보는 판단이 이전 턴들에서 몇 번 틀렸던 전례가 있어서(투명 배경/미묘한 스퀴시 등) 이번에도 시각 확인에만 기대지 않고, 머리 영역을 focus로부터의 거리 3구간(가까움/중간/멈)으로 나눠 각 구간의 평균 RGB를 스크립트로 뽑아 `[115,88,66] → [128,98,73] → [149,114,84]`로(다크 headColor `[92,70,54]`에서 라이트 highlight `[169,130,95]` 쪽으로) 세 채널 모두 단조증가하는 걸 수치로 확인 — 진짜 "중심에서 바깥으로 밝아지는" 낙차가 있는 걸 확인 후 확정. `focusX/focusY`는 기본적으로 `cfg.eye` 위치(대부분의 종에게 자연스러운 기본값)를 쓰고, `radius`는 `cat_siamese`가 `6.5`로 오버라이드 — 다른 종이 `faceShade`를 쓰고 싶으면 `highlight`만 지정해도 되고 필요하면 `focusX/focusY/radius`로 세밀 조정 가능.
  - **행동 상태머신** (3D 버전에서 그대로 가져옴 — 렌더링 방식과 무관하게 잘 만든 부분이라 로직은 유지하고 "포즈를 어떻게 그리는가"만 바꿈): 각 동물은 `walk`(또는 hop 등 자기 gait) ↔ `idle`(종별 행동 하나) 사이를 자동으로 오감 — 원래는 `walkMin~walkMax`초 걷다가 랜덤하게 하나의 idle 행동을 골라 재생하고, 그 행동의 지속시간이 끝나면 곧장 다시 걷기로 복귀하는 식이어서 사실상 항상 patrol 중이었음. 이후 "대부분 제자리, 가끔 짧게 이동"으로 한 차례 완화했다가, 지금은 아래 "커서 반응형 인터랙션으로 전환" 항목에서 자율 이동 자체를 완전히 없앰 — `walk` 상태/`enterWalk()`/gait 관련 코드는 지워지지 않고 그대로 남아있지만(이하 서술 참고) 현재는 아무도 `enterWalk()`를 호출하지 않아 사실상 항상 `idle`. idle 행동 풀: `groom`(그루밍), `stretch`(기지개), `sit`(앉기), `sleep`(수면, 다리 접고 눈 감김+숨쉬기), `earflick`(귀 실룩), `rollover`(옆으로 뒹굴기 — 픽셀아트에서는 캔버스 자체를 몸통 중심으로 회전시켜서 구현), `cheekpuff`(볼주머니 부풀리기, 햄스터), 그리고 아래 "종별 모션 리서치" 항목에서 새로 추가한 `tailtwitch`/`earalert`/`nosetwitch`(+ 그랩 전용 `held`, 위 드래그 항목 참고). 3D의 THREE.js transform 대신, 각 idle behavior의 `apply(pose,t,duration)`이 `pose` 객체(다리 위상, 몸통 bob/squash, 머리 오프셋, 귀 flick, 꼬리 swing, 눈감김, 볼펌프, roll각도 등 순수 숫자 필드)를 채워 넣고, 매 프레임 이 `pose`를 읽어서 사각형을 다시 그림 — 2D/3D 어느 쪽이든 상태머신 코드 구조 자체는 거의 그대로.
    - **종별 모션 리서치**: 기존 가중치 기반 랜덤 idle 풀 구조(`idlePool: [{name, weight}, ...]`, `pickIdleBehavior`)는 그대로 두고, 각 동물이 실제로 하는 특징적 행동을 조사해서 idle 풀 구성/가중치와 새 idle behavior 몇 개로 반영함. 완전히 새로운 애니메이션 시스템을 만들지 않고 기존 `apply(pose,t,duration)` 패턴 안에서 3개만 추가한 이유: 이미 있는 `earflick`(귀 실룩) 자체가 "쫑긋한 귀가 반응하듯 움찔거림" 요구를 그대로 충족해서 재사용하고 가중치만 올리면 됐고, `sit`/`groom`/`stretch`도 이미 종별로 tuck 각도가 달라 "식빵굽기"/"그루밍"/"몸 늘리기"를 이미 표현하고 있었음.
      - **`tailtwitch`(고양이 전용, "꼬리 끝만 까딱거리기")**: `tailPhase = t*14`(빠른 주기), `tailAmp = Math.exp(-t*2.2)*1.3`(시간이 지날수록 감쇠)를 직접 세팅하고 `pose.tailOverride = true`로 `updateTailFlavor()`의 매 프레임 기본 흔들림 덮어쓰기를 막음(위 "다리 축 늘어짐" 항목의 `tailOverride` 메커니즘 재사용) — 걷기 중의 크고 느린 스윙과 뚜렷이 다른, 짧고 빠르게 튕겼다 잦아드는 움직임이라 "몸은 가만히 있는데 꼬리 끝만 신경질적으로 까딱거림"으로 읽힘. 고양이 4종의 idlePool에 비교적 높은 가중치(2~2.5)로 추가.
      - **`earalert`(토끼 전용, "귀 방향 바꾸기/경계")**: `earFlick`을 0→1로 올렸다가(rise) 잠깐 유지(hold)하고 다시 0으로 내리는(fall) 3단계 구간을 `clamp01`로 나눠 만듦 — 기존 `earflick`(짧은 sine-decay 움찔)과 달리 "귀를 세우고 한동안 그 자세를 유지"하는 형태라 별도 behavior로 분리. 토끼 idlePool에서 가장 높은 가중치(3)로, 토끼의 대표 행동으로 설정.
      - **`nosetwitch`(토끼 전용, "코 씰룩거리기")**: `pose.noseTwitch = clamp01(0.5 + Math.sin(t*13)*0.5)`로 빠른 진동을 만들고, 코 렌더링 쪽(`drawCreature`의 nose 그리는 부분)에서 `noseScale = 1 + pose.noseTwitch*0.6`로 코 사각형을 자기 중심 기준으로 살짝씩 키웠다 줄였다 함(좌상단 기준으로 그냥 키우면 코가 옆으로 밀리는 것처럼 보여서, `x/y`에서 `(noseW-cfg.nose.w)/2`만큼 빼서 중심을 고정) — 은은한 귀 떨림도 곁들임. `noseTwitch` 필드는 `defaultPose()`에 새로 추가.
      - **강아지 품종별 gait 차별화 (`gaitOpts`, `SPECIES[...].gaitTuning`)**: "코기는 짧은 다리로 종종거림"은 다리 관절 파라미터만으론 부족해서(관절이 짧아도 걸음 자체의 리듬이 다른 개와 같으면 "그냥 다리 짧은 개"로만 보임) gait 프리셋에 `gaitOpts(anim, defaults) => ({...defaults, ...(anim.spec.gaitTuning||{}), speed: anim.spec.speed})`를 얹어 종별로 `bob`(상하 바운스 폭) 등을 오버라이드할 수 있게 함. `dog_corgi`는 gait 자체를 `trot`에서 (햄스터가 쓰던) `scamper`로 바꿔서 종종걸음 리듬을 냄. `dog_dachshund`는 `gaitTuning:{bob:0.35}`로 바운스를 확 줄여 "낮은 자세로 스윽 이동"하는 글라이드 느낌을. `dog_pomeranian`은 반대로 `gaitTuning:{bob:2.2}`로 바운스를 키워 "통통 튀듯 걷기"를 표현.
      - **허스키 idlePool 재정렬**: 시바/허스키/진돗개류의 "쫑긋한 귀가 소리에 반응하듯 움찔거림"은 이미 있는 `earflick`이 정확히 그 모양이라 새로 안 만들고, `dog_husky`의 `earflick` 가중치를 3.5(다른 idle보다 확연히 높게)로 올려서 이 종의 시그니처 행동으로 만듦.
      - **햄스터**: "볼주머니 채우기/비우기"는 기존 `cheekpuff`를 유지하되 `pose.cheekPuff = Math.sin(clamp01(t/duration)*Math.PI)*0.9`로 한 번 부풀었다 가라앉는 아치형 곡선으로 다듬음(비대칭적으로 훅 부풀었다 뚝 끊기던 것보다 채우고-비우는 왕복이 더 자연스럽게 읽힘). "짧은 다리로 종종걸음"은 이미 `gait:'scamper'` + 짧은 관절 길이로 반영돼 있어서 추가 변경 없음.
    - **포즈 전환 시 리셋 버그 고침**: 예전엔 `idle→walk` 전환 때만 `anim.pose`를 새로 초기화했고, 그 외 모든 전환(walk→idle, idle→idle 재추첨, `poke()`/`forceSleep()`/`wakeUp()`으로 인한 강제 중단)은 이전 pose 객체를 그대로 재사용 — 새 idle behavior의 `apply()`는 자기가 쓰는 필드만 세팅하므로, 직전 behavior가 건드려놓고 자기는 더 이상 안 건드리는 필드(대표적으로 `rollover`의 `rollAngle`, `sit`/`sleep`의 `legsTucked`)가 그대로 남아 다음 behavior와 뒤섞여 그려짐. 캔버스가 96x96px로 여백이 거의 없다 보니(당시엔 96x80px), 예를 들어 `rollover` 도중 3분 idle로 `forceSleep()`이 걸리면 몸 전체가 기울어진 각도(`rollAngle`)가 리셋 안 된 채 `sleep`(다리 접힘+눈 감김) 포즈와 겹쳐져서, 꼬리가 캔버스 위쪽 경계 밖으로 회전해 잘리거나(`legsTucked` 잔존값 때문에 다리 높이가 0 이하로 계산돼 사라지는 것도 같은 원인) 하는 식으로 보였을 것 — `enterIdle()`/`enterWalk()` 헬퍼로 모든 전환 지점을 통일하고 매번 `anim.pose = defaultPose()`로 완전히 초기화하도록 고침. (`assets/reference/bug-tail-ear-glitch.png`가 저장돼 있다고 했는데 실제로는 폴더에 없어서 스크린샷 자체는 못 봤음 — 위 진단은 코드 분석 + 직접 재현한 케이스로 검증한 것.)
    - **클릭 반응 / 잠들기**: 그대로 유지 — 펫을 클릭하면 귀 실룩임 + 짧은 반응 말풍선(`animal.poke()`), 시스템 3분 이상 idle이면 `sleep`에 고정되어 잠들고 활동 재개 시 기지개(`animal.forceSleep()`/`wakeUp()`, main.js의 `startAwayPoll()`과 연동).
    - **"산만하다" 피드백 — 가만히 숨쉬는 `breathe` idle 추가 + idle 리듬 차분화**: `pickIdleBehavior`는 원래 항상 idlePool에서 이름 있는 행동(groom/stretch/sit/...) 하나를 뽑아 재생하는 구조라 — 진짜 "아무것도 안 하고 가만히 서있는" 상태 자체가 없었음, 매 순간 뭔가는 하고 있었던 셈. `breathe`라는 가장 단순한 idle behavior를 새로 추가 — `pose.bodyBob`/`headDY`에 아주 낮은 진폭(0.3/0.15유닛)의 느린 사인(1.1rad/s, 서로 살짝 위상차)만 줘서 "가슴이 오르내리는 숨쉬기"만 표현하고 그 외엔 아무것도 안 건드림. 지속시간을 다른 idle(대부분 0.8~4초)보다 훨씬 길게(7~13초) 잡고, 10종 전부의 idlePool에 가중치 12(다른 항목들 합계가 종별로 8~11.5 정도이므로 대략 절반 이상의 뽑힐 확률)로 추가 — 뽑힐 확률에 더해 지속시간 자체도 훨씬 기니 시간 기준 점유율은 그보다도 높아짐.
      - **수치 검증**: `test/sim-idle-rhythm.mjs`(`npm run test:idle-rhythm`)를 새로 만들어 종별 20분 시뮬레이션에서 `breathe`의 시간 점유율과 분당 전환 횟수를 측정 — 전 종 77~86% 시간을 `breathe`가 차지하고, 분당 전환 횟수는 4.9~7.35회(약 8~12초에 한 번 꼴)로 나옴. `breathe` 추가 전이었다면 개별 idle 지속시간이 1~4초대였으니 분당 전환이 20~40회 이상이었을 것 — 수치로 "훨씬 차분해짐"을 확인. 회귀 테스트로 남겨둠(`test:movement-stats`와 같은 성격 — 나중에 idlePool 가중치를 또 조정하다가 이 리듬이 도로 산만해지면 잡아낼 수 있게).
    - **포즈 전환이 뚝뚝 끊기는 문제 — 크로스페이드 레이어 추가**: `enterIdle`/`enterWalk`가 호출하는 `resetPose()`는 매번 `anim.pose = defaultPose()`로 전체를 즉시 초기화해왔음(위 "포즈 전환 시 리셋 버그 고침" 항목 참고 — 이 자체는 잔존 필드가 다음 behavior와 섞이는 걸 막기 위한 올바른 설계였음) — 문제는 이 초기화가 순간적이라, 예를 들어 `sit`(bodyBob=1.6)에서 `earflick`(bodyBob 안 건드림, 그래서 0)으로 전환되는 순간 화면에 그려지는 값이 한 프레임 만에 1.6→0으로 뚝 떨어져 보였을 것. **고침**: `resetPose()`가 초기화 직전의 pose를 `anim.transitionFrom`으로 스냅샷해두고 `anim.transitionElapsed=0`으로 크로스페이드 타이머를 리셋 — `update()` 맨 마지막(`applyCursorLookEasing` 다음)에서 `applyPoseCrossfade(anim,dt)`가 그 프레임에 새로 계산된 목표 pose 값들을 스냅샷 값에서부터 0.25초(`POSE_TRANSITION_DURATION`)에 걸쳐 숫자 필드마다 `lerp`로 블렌딩. 전환이 0.25초 안에 또 일어나도(가장 짧은 idle인 earflick조차 0.8초는 되므로 실제로는 거의 안 겹치지만) 그 시점의 "지금 블렌딩 중인" pose를 다시 스냅샷하고 타이머만 리셋하는 방식이라 자연히 이어짐 — 별도의 "이미 전환 중이면 무시" 같은 분기 없이도 안전함. `tailOverride` 같은 boolean 필드는 `typeof`체크로 건너뛰고, 숫자 필드만 블렌딩.
      - **수치 검증**: `sit`(bodyBob=1.6) 도중 `poke()`로 강제 전환시켜 이후 20프레임의 `bodyBob`을 추적 — 전환된 첫 프레임은 직전 값(1.6)과 완전히 동일(블렌딩 비율 0에서 시작하므로 팝 없음), 이후 서서히 감소해 최종적으로 목표값(earflick은 bodyBob을 안 건드리므로 0)에 수렴. 크로스페이드가 공식 종료된 뒤에도 값이 계속 줄어드는 게 보였는데 — 이건 버그가 아니라 `updateContinuousGait`의 idle 분기(`else { anim.pose.bodyBob *= 0.8; ... }`, 걷기 관성이 남아있을 때 idle로 전환되면 서서히 감쇠시키던, 이번 작업 전부터 있던 기존 메커니즘)가 매 프레임 `gaitFn(anim,dt,false)`으로 항상 같이 돌고 있어서 크로스페이드와 겹쳐 이중으로 감쇠되는 것 — 결과적으로 팝 없이 더 매끄럽게 수렴하는 방향이라 문제 삼지 않음. `npm run test:invariants`(120만 프레임) 재실행 — 위반 0건.
    - **`sit`을 종별로 분화 (식빵/하체앉기/토끼로프/햄스터정착)**: 예전엔 전 종이 똑같은 `sit`(다리 전부 tuck + 몸통 스쿼시 + 머리 좌우 흔들림)을 썼는데, 실제 동물마다 앉는 자세가 다름. `legJointAngles`가 쓰던 단일 `pose.legsTucked` 스칼라를 `legsTuckedFront`/`legsTuckedBack` 두 필드로 쪼갬(`legJointAngles(cfg,pose,isFront)`가 `isFront`에 따라 둘 중 하나를 읽음) — 앞뒤 다리가 독립적으로 접혀야(강아지는 뒷다리만) 하는데 스칼라 하나로는 둘을 구분할 수 없었기 때문. `sleep`/`rollover`/걷기·hop gait의 리셋 분기는 그대로 앞뒤에 같은 값을 넣도록만 고쳐서(대칭적으로 웅크리는 건 여전히 대칭) 기존 동작을 안 건드림. `sit.apply(pose,t,duration,cfg)`에 4번째 인자로 종 config를 넘기도록 시그니처를 넓히고(다른 idle behavior는 다 무시, `sit`만 읽음), `cfg.sitStyle`(SPECIES 테이블에 종별로 명시 — "10종뿐이니 공식보다 손으로" 원칙 그대로)로 분기:
      - **`loaf`(고양이 4종, 그대로 유지)**: 기존과 동일 — 앞뒤 다리 전부 tuck, 몸통 bob 크게, 머리 좌우로 살짝 흔들림.
      - **`haunches`(강아지 4종)**: 실제 "앉아"는 뒷다리만 접고 엉덩이를 바닥에 붙이고 앞다리는 세운 채 상체를 드는 자세라, `legsTuckedBack`만 크게 올리고(0.95) `legsTuckedFront`는 0으로 둬서(다른 tuck 안 하는 idle들이 이미 렌더링하는 "서 있을 때의 기본 무릎 각"을 그대로 씀) 앞다리가 안 접히게 함. **1차 시도가 화면으로는 거의 안 보였음**: 다리 각도 수치 자체는 정확했지만(`legAngles:back hip=47.5° knee=72.2°` vs `legAngles:front hip=0° knee=16°` — 손으로 확인), 다리가 몸통보다 먼저 그려지고(z-order) 접힌 뒷다리의 발끝이 `legY`(몸통 아랫변) 바로 위/근처에서 끝나 몸통 사각형에 거의 다 가려지는 데다, 코기/닥스훈트처럼 다리 자체가 짧은 종은 세그먼트 길이(`upperLen/lowerLen` 0.6~0.7)가 작아서 각도 차이가 나도 실제 픽셀 이동량이 작아 `test/render-poses.mjs` 스크린샷으로 그냥 서 있는 것처럼 보였음(수치는 맞는데 육안 판정과 어긋난 사례 — 이전 턴들의 교훈대로 스크린샷을 직접 떠서 확인했기 때문에 잡을 수 있었음). **수정**: 원래 아무 종도 안 쓰던(죽은 필드였던) `pose.waddleTilt`(각도, `drawCreature`가 `rollAngle`과 합쳐서 캔버스 전체를 회전시키는 데 씀)를 재활용해 `-s*9`도(음수 — `waddleTilt`가 양수면 머리 쪽(고 x좌표)이 아래로 돌아가므로, 앉을 때 원하는 "가슴 쪽이 들리고 엉덩이가 내려가는" 방향은 반대) 만큼 살짝 뒤로 기울임, `bodyBob`도 0.7→1.0으로 소폭 키움 — 다리 각도 비대칭은 그대로 두고 위에 시각적으로 더 뚜렷한 신호를 얹은 것. 재렌더링해서 4종(코기/닥스훈트/허스키/포메라니안) 전부 확인 — 몸 전체가 기울고 앞다리 쪽만 바닥에 닿아 보이는 게 뚜렷해짐. (부수 효과: 그림자(`drawShadow`)는 `pose`를 전혀 안 읽는 고정 타원이라 이 기울임을 따라가지 않아 살짝 어긋나 보이는데, `rollover`가 이미 최대 70°까지 훨씬 크게 같은 회전을 써왔고 그 정도 어긋남은 지금까지 문제로 지적된 적이 없어서 — 이번 9°는 그보다 한참 작음 — 같은 기준으로 안 고치고 남겨둠, 아래 알려진 한계 참고.)
      - **`loafNoLegs`(토끼)**: 뒷다리(이미 가장 크게 접히는 종 — `tuckHip:95/tuckKnee:130`)는 `legsTuckedBack=1`(거의 완전히 접어 몸통 아래로 숨김), 앞다리(원래 작음)는 `0.7`만 접어 완전히 안 사라지게. 몸통 bob은 1.8로 고양이보다 살짝 큼. 좌표 확인 결과 뒷다리 발끝이 `legY` 근처(몸통 바로 아래)로 접혀 들어가 시각적으로 몸통에 가려지는 것 확인.
      - **`roundPuff`(햄스터)**: 다리가 이미 아주 짧아 tuck을 크게 해도 티가 잘 안 나서 앞뒤 각각 0.5/0.6로 적당히만, 대신 `cheekPuff`를 0.25만큼 같이 올려서 "자리 잡고 앉으며 볼도 살짝 부푼" 느낌을 냄 — 기존 `cheekpuff` idle과 겹치지 않게(그쪽은 훨씬 크게 0→0.9 왕복) 은은한 수준으로만.
      - **검증**: `test/render-poses.mjs`가 10종 중 8종(강아지 4 + 고양이 2 + 토끼 + 햄스터)을 실제로 `sit`에 진입시켜 스크린샷으로 저장 — 강아지 4종 모두 기울어진 자세 확인, 고양이는 기존과 동일한 식빵 자세 유지 확인.
    - **`bodySquash` 중 pattern/belly/cheeks 장식이 몸통 밖으로 튀어나오는 문제 재확인 + 이번엔 실제로 고침**: 지난 턴에 "알려진 한계"로 남겨뒀던 항목("풀 스쿼시일 때 패턴/배 무늬가 튀어나와 보임")을 이번 sit 분화 작업 후 다시 확인해달라는 요청에 따라 재점검 — 참고로 이 문제는 `sleep`(모든 종이 `bodySquash`를 쓰는 유일한 idle)에서만 발생하고 `sit`(이번에 손댄 것)은 애초에 `bodySquash`를 전혀 안 쓰므로 sit 분화 자체와는 무관하지만, 요청대로 재확인 후 그대로 있길래 이번엔 제대로 고침. **원인**: `pattern`은 `y`에 `pose.bodyBob`만 더하고 스쿼시 오프셋이 아예 없었고(몸통 윗변은 스쿼시로 내려가는데 패턴은 안 따라가 위로 튀어나옴), `belly`는 `+squashY*0.3`/`-squashY*0.5`라는, 몸통 자체가 쓰는 비율(윗변 `+squashY*0.5`, 높이 `-squashY`)과 다른 임의의 상수를 쓰고 있었음 — 둘 다 "그때그때 봐서 튜닝한" 근사치였지 몸통의 실제 축소 비율과 연동된 게 아니었음. **고침**: `bodyScaleY = bodyH/cfg.bodyH`(몸통이 스�쿼시로 줄어든 실제 비율)를 구해서, `pattern`/`belly` rect를 "몸통의 authored 좌표계 기준 상대 위치"를 그대로 유지한 채 몸통과 동일한 비율로 이 함수(`toBodySquashed`)를 통해 리맵 — 스쿼시가 0이면 정확히 기존과 같은 결과(`bodyScaleY=1`)가 나오고, 스쿼시가 커질수록 몸통이 줄어드는 만큼 패턴/배 무늬도 같이 줄어들어 몸통 경계를 벗어나지 않음. 같은 자리에서 `cheeks`(햄스터 볼주머니)도 점검해보니 `pose.bodyBob`을 아예 안 더하고 있었던 걸 발견(머리는 `headY`에 `+pose.bodyBob`을 더하는데 볼주머니는 `headDX/headDY`만 썼음) — 걷기 bounce나 이번에 손댄 `sit`/`roundPuff`처럼 `bodyBob`이 있는 모든 상황에서 볼주머니가 머리에서 미세하게 떨어져 보였을 문제라 `+pose.bodyBob`을 추가해 같이 고침. 패턴/벨리와 달리 정확한 비례 관계가 필요한 게 아니라 그냥 빠진 오프셋 하나를 더한 것이라 별도 스케일 로직 없이 바로 해결.
    - **이동 정책 변경 1단계 (상시 patrol → 대부분 제자리, 가끔 짧게 이동, 이후 2단계에서 완전히 대체됨)**: 예전엔 `walk`가 끝나면 곧장 idle 하나 재생 후 곧장 다시 `walk`로 돌아가는 구조라, idle 지속시간(대략 1~9초)과 walk 지속시간(`walkMin~walkMax`, 종별로 2~7.5초)이 거의 반반씩 번갈아 — 즉 펫이 깨어있는 시간의 절반 가까이를 항상 좌우로 걷고 있었음. 한 차례는 `updateBehaviorState`의 idle 분기에 idle 누적 시간과 다음 이동까지의 랜덤 목표치를 추가해 "평소엔 제자리, 가끔 짧게 이동"으로 완화(이동 길이는 `spec.walkMin/walkMax`에 축소 계수를 곱해 재사용, 종별 상대적 보폭감 유지)했었음 — `test/sim-movement-stats.mjs`(`npm run test:movement-stats`)로 walk 비율이 종별 추정 약 43~65%(원래) → 실측 약 2~5%(1단계)로 떨어진 걸 수치로 확인했었음. 아래 2단계에서 이 누적 타이머/burst 트리거 자체를 완전히 걷어내고 커서 반응형으로 대체했으므로, `test:movement-stats`를 다시 돌리면 이제 전 종 walk 비율이 정확히 0.0%로 나옴(더 이상 자율 이동이 전혀 없다는 걸 보여주는 회귀 확인용으로 남겨둠).
    - **이동 정책 변경 2단계 (자율 이동 완전 제거 → 커서 반응형 인터랙션)**: 사용자가 연속으로 두 번째 "더 차분하게" 요청 — 이번엔 "가끔 짧게 이동"조차 빼고 자체적으로는 아예 안 움직이되, 커서 움직임에 반응하도록 바꿔달라는 것. 두 파일에 걸쳐 구현:
      - **엔진 쪽 (`animal-engine.js`)**: `updateBehaviorState`의 idle 분기에서 "다음 이동까지 누적 시간을 세다가 `enterWalk()` 호출" 로직 자체를 제거하고 원래의 "idle 하나 끝나면 다음 idle 하나 더" 단순 반복으로 되돌림 — `MOVE_INTERVAL_MIN/MAX`/`MOVE_BURST_SCALE`/`idleElapsed`/`nextMoveDelay`는 완전히 삭제. 다만 `enterWalk()` 함수 자체와 `walk` 상태 분기, `GAIT_PRESETS`/걷기 애니메이션, 종별 `gait`/`speed`/`walkMin`/`walkMax` 필드, `pet.js`의 `x`/`direction`/`bounds()` 좌표 로직은 지우지 않고 그대로 남겨둠 — 지금은 아무도 `enterWalk()`를 호출하지 않아 사실상 죽은 코드지만, 완전히 잘 작동하고 검증까지 끝난 걷기 렌더링 하나를 "지금 아무도 안 부른다"는 이유만으로 통째로 뜯어내는 건 이번 요청(자율 이동을 멈춰달라는 것이지 걷기 렌더링 자체를 없애달라는 게 아님) 대비 과한 변경이라 판단 — `cfg.joints.neckLen`을 지오메트리용으로는 안 쓰지만 지우지 않고 남겨둔 것과 같은 판단 기준. 나중에 "가끔 커서 쪽으로 몇 걸음 다가가기" 같은 걸 다시 넣는다면 이 자리에 그대로 다시 연결하면 됨.
      - **커서 신호 처리**: `createAnimal()`이 반환하는 객체에 `setCursorHint(hint)`를 추가 — `hint`는 `null`(커서가 멀리 있거나 없음) 또는 `{dx, dy, close}`(펫 중심 기준 커서 방향과, 더 가까운 "경계" 반경 안인지 여부 — 실제 화면/윈도우 좌표 계산은 엔진이 모르는 영역이라 호출자인 `pet.js`가 계산해서 넘김). 매 프레임 두 함수가 이걸 소비: `updateCursorAlertTrigger`(커서가 "멀리→가까이"로 바뀌는 순간에만, 종의 idlePool에 있는 것 중 가장 "경계 반응"다운 행동 하나로 갈아탐 — `ALERT_BEHAVIOR_PRIORITY = ['earalert','tailtwitch','nosetwitch','earflick']` 순서로 종의 idlePool을 뒤져서 있는 것 채택, 전부 없으면 모든 종이 가진 `earflick`으로 폴백 — "품종별 idle 행동 풀에 이미 있는 반응 활용"이라는 요청을 그대로 반영)와 `applyCursorLookEasing`(고개/눈이 커서 쪽을 향하도록 `pose.lookDX/lookDY`를 목표치 쪽으로 매 프레임 부드럽게 보간, `LOOK_MAX=0.8`그리드유닛/`LOOK_EASE_RATE=6`). `lookDX/lookDY`는 `headDX/headDY`와 별개 필드로 둬서 groom/sleep/sit 등 idle behavior 자체의 머리 오프셋과 안 겹치고 그 위에 얹히도록 함(`drawCreature`가 머리/눈/귀/코/주둥이/볼주머니를 그릴 때 전부 `pose.headDX+pose.lookDX`, `pose.headDY+pose.lookDY`를 합친 `hdx`/`hdy`를 씀 — 목도 `headX`/`headY`를 통해 자동으로 따라 굽음). `pinnedSleep`/`pinnedHeld`(자는 중/드래그 중)일 땐 완전히 무시.
      - **호출 순서 버그와 수정**: 처음엔 `updateCursorAlertTrigger`와 이징 로직을 하나의 `updateCursorLook()` 함수로 합쳐서 `updateBehaviorState`보다 *먼저* 불렀는데, 렌더링 스크린샷 확인 중(아래 검증 항목 참고) 같은 커서 힌트를 줘도 실행마다 `lookDX`가 다르게 수렴하는 걸 발견 — 원인은 `enterIdle()`/`enterWalk()`가 전환마다 `anim.pose`를 통째로 `defaultPose()`로 리셋하는데(포즈 전환 리셋 컨벤션, 위 항목 참고) 이게 `lookDX/lookDY`도 매번 0으로 되돌려버려서, 흔한 idle→idle 재추첨(수초마다 랜덤하게 발생)이 일어날 때마다 커서를 향한 시선이 다시 0부터 수렴해야 했던 것 — `updateTailFlavor`가 `tailPhase`/`tailAmp`를 매 프레임 자기가 다시 세팅해서 이 문제를 안 겪는 것과 같은 이치인데, 커서-룩 로직은 반대 순서(리셋되기 전에 세팅)로 불러서 이 이점을 못 받고 있었던 것. `updateCursorAlertTrigger`(전환을 일으킬 수 있는 부분, 여전히 `updateBehaviorState` 이전)와 `applyCursorLookEasing`(그냥 보간만, `updateTailFlavor`와 같은 자리인 `updateBehaviorState`/idle `apply()` *이후*)로 분리해서 고침 — 이제 어떤 전환이 일어나든 그 프레임 안에서 바로 보정됨. 5번의 반복 실행으로 `lookDX`가 매번 동일하게(0.78~0.80) 수렴하는 걸 확인.
      - **`pet.js` 쪽**: 기존 클릭 통과 히트테스트용 `pendingMouse`(샘플링마다 `null`로 비워짐)와는 별개로 `lastMouseClient`/`lastMouseMoveAt`을 새로 둬서 "커서가 가만히 멈춰있는 동안에도" 마지막 위치를 유지 — 매 프레임 `updateCursorHint()`가 펫-랩의 화면상 중심(`x + WRAP_WIDTH/2`, `stageHeight - 14 - WRAP_WIDTH/2`)과 마지막 커서 위치의 거리를 계산해 `CURSOR_NEAR_RADIUS`(130px, 이 안이면 시선이 따라감) / `CURSOR_CLOSE_RADIUS`(55px, 이 안이면 경계 반응 트리거) 두 반경과 비교해 `animal.setCursorHint(...)`를 호출. `mouseleave`는 클릭 통과 창의 `setIgnoreMouseEvents({forward:true})` 오버라이드를 통해 안정적으로 전달된다는 보장이 없어서(`mousemove`는 명시적으로 forward되지만 `mouseleave`도 그런지는 불확실), 대신 `CURSOR_STALE_MS`(400ms) 동안 `mousemove`가 안 왔으면 "커서가 없어짐"으로 간주. `direction`/`x`/`bounds()`는 위에서 설명한 대로 죽은 코드가 아니라 "지금은 항상 0인 advance를 소비하는 자리"로 그대로 둠 — 실제 이동이 없으니 `x`는 초기값(`MARGIN`)에서 절대 안 바뀜.
      - **선택 사항으로 남겨둔 것**: 요청에 "커서가 계속 가까이 머무르면 아주 가끔 한두 걸음 다가가는 정도는 판단해서 넣어도 된다"는 여지가 있었지만, 이번이 "더 차분하게" 요청의 두 번째 연속(1단계 때 이미 상시 patrol을 줄였는데 이번엔 그 마지막 남은 이동까지 빼달라는 것)이라 "자체적으로는 전혀 안 움직임"을 무조건적인 제약으로 지키는 쪽을 택함 — 접근 이동은 넣지 않음. 이미 있는 `enterWalk()`/gait 서브시스템이 그대로 남아있으므로 나중에 필요하면 붙이기만 하면 됨.
      - **검증**: `test/render-poses.mjs`(`npm run test:render-poses` — 직접 만든 헤드리스 Canvas2D 서브셋 구현체 `test/mini-canvas.mjs` 위에서 실제 `drawCreature`/`drawShadow`를 그대로 실행해 PNG로 저장, node-canvas 같은 네이티브 의존성 없이 Node 내장 zlib만으로 PNG를 직접 인코딩)로 커서 힌트별 스크린샷(`out-cat-look-none/right/left-down.png`)을 뜨고, 상태 전이는 스크립트 로그로 확인: `rabbit_b`는 idlePool에 있는 `earalert`로, `cat_a`는 `tailtwitch`로, `earalert`/`tailtwitch`/`nosetwitch`가 전부 없는 `dog_corgi`는 폴백인 `earflick`으로 정확히 반응(모두 기대값과 일치). 커서가 사라진 뒤 5초 넘게 지나도 정상적인 idle 순환으로 돌아와 있는 것도 확인(멈춰있지 않음). `test/sim-invariants.mjs`(120만 프레임)도 이 변경 전후로 재실행해 위반 0건 유지 확인.
    - **이동 정책 변경 3단계 (근접 트리거 → 상시 방향 추적)**: 2단계는 "커서가 `CURSOR_NEAR_RADIUS`(130px) 안에 들어와야만" 시선이 반응했는데, 원했던 건 "화면 어디에 있든 커서 쪽을 상시로" — 즉 트리거가 아니라 항상 켜져 있는 추적. 두 가지를 바꿈:
      - **화면 전체 커서 위치 확보**: `pet.js`의 로컬 `mousemove`는 커서가 이 340x210 창 위에 있을 때만 발생(드래그-따라가기가 같은 문제를 이미 겪었던 것과 동일한 사각지대 — `main.js`의 `drag-start` 주석 참고)하므로, 창 밖 커서 위치는 알 방법이 없었음. `main.js`에 `startCursorTrackPoll()`을 추가 — `setInterval`로 100ms마다(드래그-따라가기의 16ms보다 훨씬 낮은 빈도로 충분 — "어느 쪽에 있는지"만 필요하지 커서에 물리적으로 붙어있어야 하는 게 아님) `screen.getCursorScreenPoint()`와 `petWindow.getBounds()`를 읽어 이미 창 로컬 좌표로 변환한 값(`cursor.x-bounds.x, cursor.y-bounds.y` — 실제 `mousemove` 이벤트의 `clientX/clientY`와 같은 좌표계)을 `'cursor-track'` IPC로 렌더러에 보냄. `preload.js`에 `onCursorTrack` 추가.
      - **`pet.js`**: 기존 로컬 `mousemove`와 새 `onCursorTrack` 둘 다 같은 `lastMouseClient`를 갱신(마지막에 온 쪽이 이김 — 창 안에 있을 때는 `mousemove`가 더 정밀/고빈도라 자연히 우세하고, 창 밖에서는 poll이 유일한 소스라 문제 없음). `updateCursorHint()`에서 "`CURSOR_NEAR_RADIUS`보다 멀면 `null`" 컷오프를 제거 — 이제 `dx`/`dy`는 항상 계산되고 clamp만 됨(상수 이름도 `CURSOR_DIRECTION_RANGE`로 바꿔서 "이 거리부터는 완전히 그쪽으로 봄"이라는, 컷오프가 아니라 포화 거리라는 새 의미를 반영). `close`(근접 alert 트리거)는 그대로 `CURSOR_CLOSE_RADIUS`(55px) 기준 유지 — 상시 추적은 "추가"이지 근접 반응을 대체하는 게 아니라는 요청 그대로.
      - **부수로 발견한 버그: lookDX/lookDY가 idle 전환마다 리셋됨**: 스크린샷 검증 중 같은 커서 위치를 몇 초간 유지시켰는데 `lookDX`가 매번 다른 값(-0.13 ~ -0.80 사이 아무 값)으로 멈추는 걸 발견 — 지난 턴에 "같은 프레임 안에서 보정되도록 순서를 바꿔서" 고쳤다고 생각한 버그가 실은 절반만 고쳐져 있었음. 원인: `enterIdle`/`enterWalk`는 전환마다 `anim.pose`를 통째로 `defaultPose()`로 초기화하는데(포즈 전환 리셋 컨벤션, 위 항목 참고) 이게 `lookDX`/`lookDY`도 0으로 되돌림 — 순서를 고쳐서 "리셋된 그 프레임 안에서" 다시 목표치로 보간을 시작하게는 됐지만, `LOOK_EASE_RATE=6`이면 95% 수렴에 ~0.5초가 걸리는데 idle 지속시간이 짧은 종(`earflick` 혼자만 있어도 0.8~1.3초)은 그 사이 또 전환이 일어나 처음부터 다시 시작하는 일이 잦았음 — 결과적으로 "항상 부드럽게 커서를 향함"이 아니라 idle이 바뀔 때마다 살짝 흔들리다 다시 감기는 톱니파 패턴이었던 것. **고침**: `lookDX`/`lookDY`는애초에 어떤 idle behavior의 `apply()`도 건드리지 않는, 완전히 별개 시스템(`updateCursorAlertTrigger`/`applyCursorLookEasing`) 소유인데 `defaultPose()` 리셋 대상에 왜 포함돼 있었는지가 진짜 문제 — `resetPose(anim)` 헬퍼를 새로 만들어 `enterIdle`/`enterWalk`가 pose를 초기화할 때 `lookDX`/`lookDY`만은 이전 값을 그대로 옮겨 보존하도록 고침(다른 필드는 여전히 전부 리셋 — `rollAngle`/`legsTucked` 등이 새는 걸 막던 원래 컨벤션은 그대로 유지). 재검증: 같은 커서 힌트로 5번 반복 실행 결과 `lookDX`가 매번 정확히 동일한 값(-0.702/0.714)으로 수렴 — 전환 타이밍에 더 이상 안 흔들림.
      - **검증**: `test/render-cursor-sweep.mjs`/`test/render-cursor-sequence.mjs`로 `pet.js`의 `updateCursorHint()`와 동일한 수식을 그대로 재현해 커서를 화면 좌측 멀리(오프스크린, clientX=-600 — 로컬 `mousemove`만으로는 절대 못 얻는 값) → 좌측 창 안 → 중앙 → 우측 창 안 → 우측 멀리(오프스크린)로 쓸어가며 각 지점의 `hint.dx`/수렴한 `lookDX` 값을 숫자로 확인(오프스크린 좌우 모두 정확히 ±1.0/±0.80으로 포화, 창 안 좌우는 각각 0.877/-0.877, 0.892/0.892 등 방향에 맞게 부호 일치) 하고, `cat_a`/`dog_husky` 양쪽에 대해 스크린샷도 저장(`sweep-cat-*.png`, `sequence-*.png`) — 머리/눈 부위가 커서 방향에 따라 실제로 이동한 걸 육안으로도 확인. `test/sim-invariants.mjs`(120만 프레임)도 재실행해 위반 0건 유지 확인.
    - **squash 중 다리 힌지(legY)가 몸통에서 떨어지는 버그 발견 + invariant 자동 탐지 도입**: "간헐적으로, 특정 종/상태에 안 묶여서 몸이 찌그러지거나 뭉개져 보인다"는 리포트에 대해, 스크린샷으로 순간을 잡아 눈으로 판단하는 대신(이전 sit 버그 때도 4000프레임 회귀 테스트로 못 잡았던 전례가 있어 이번엔 접근을 바꿈) `windows/shared/animal-engine.js`에 기하 계측 훅을 추가함 — `px`/`pxSoft`/`pxJoint`가 그리기 직전에 자기가 그릴 정확한 논리 그리드 좌표(회전/PX 배율 적용 *전*, 즉 SPECIES 테이블이 authored된 것과 같은 좌표계)를 `reportPart(tag, ...)`로 보고(`setGeometrySink()`로 등록한 콜백이 있을 때만 — 실제 앱에서는 아무도 등록 안 하므로 `if` 한 줄 비용만 남는 순수 테스트 훅), 몸통/머리/목/다리 8세그먼트/꼬리 N세그먼트에 태그를 답. 이 데이터를 받아 프레임 하나의 유효성을 판정하는 `checkPoseInvariants(cfg, pose, geometry)`를 export하고, `test/sim-invariants.mjs`(`npm run test:invariants`)가 10종 × 6개 시드 구성(시드 4개는 갓 켠 상태, 2개는 각각 앱이 27.8시간/57.9일째 켜져 있던 것처럼 `elapsed` 오프셋을 줘서 오래 켜둔 경우의 오실레이터 누적치도 커버) × 종당 20,000프레임을 돌리면서 `poke()`/`forceSleep()`/`wakeUp()`/`setHeld(true/false)`/말풍선 일시정지를 서로 겹치게(자는 중에 드래그, 말풍선 뜬 채로 잠듦 등) 랜덤하게 섞어 매 프레임 invariant를 체크 — 걸리면 문제 종류/프레임/직전 40프레임의 상태 전환 이력을 `test/invariant-violations.log`에 남김.
      - **1차 시도는 허탕**: 처음엔 각 pose 필드가 finite인지/대략적인 범위 안인지, 몸통·머리 rect 크기가 authored 값 근처인지 정도의 느슨한 sanity check만 넣고 120만 프레임을 돌렸는데 위반 0건 — 이전 sit 버그의 교훈("각도 문제인 줄 알았는데 사실 다른 동적 계산값 문제") 그대로, 헐거운 범위 체크로는 안 걸리는 종류였다는 뜻이라 판단하고 직접 수식을 다시 짚어봄.
      - **손 계산으로 찾은 것**: `drawCreature`에서 `bodyY = cfg.bodyY + pose.bodyBob + squashY*0.5`, `bodyH = cfg.bodyH - squashY`인데(`squashY = pose.bodySquash * cfg.bodyH * 0.35`) 다리 힌지 `legY`는 `cfg.legY + pose.bodyBob`뿐 — 몸통의 실제 렌더링된 아랫변(`bodyY+bodyH = cfg.bodyY+cfg.bodyH+pose.bodyBob - squashY*0.5`, 스쿼시가 몸통을 세로 중심 쪽으로 대칭 압축하므로 아랫변은 `squashY*0.5`만큼 위로 올라옴)을 `legY`가 전혀 따라가지 않고 있었음 — 정확히 목/꼬리를 몸통에 붙게 고쳤던(위 "몸통에서 안 떨어지게 하기" 항목) 것과 같은 종류의 버그가 다리에는 적용 안 돼 있었던 것. `bodySquash`는 `sleep`(전 종의 idlePool에 존재)과 드래그 `held` 둘 다에서 0보다 커지므로, 이 버그는 특정 종/특정 idle 하나에 묶이지 않고 "언제든 잠들거나 집힐 때마다"라 사용자 입장에서는 "아무 때나 랜덤하게"로 보였을 만함.
      - **invariant를 정밀화해서 실제로 잡음**: 위 손 계산을 바탕으로 `checkPoseInvariants`에 "`leg:*:*:upper` 힌지의 y좌표는 `body` 태그로 보고된 렌더링된 아랫변(`body.y+body.h`)과 (오차 0.15유닛 이내로) 정확히 같아야 한다"는 하드 invariant를 추가 — 이건 느슨한 sanity bound가 아니라 "legY는 bodyY+bodyH와 정확히 같아야 한다"는 SPECIES 테이블 위 코멘트에 이미 명시돼 있던 authored-time 컨벤션을 프레임 단위로도 그대로 검증한 것. 재실행하자 즉시 10종 전부, 120만 프레임 중 316,438프레임(약 26%)에서 위반 검출 — `sleep`/`held`로 `bodySquash`가 조금이라도 올라간 프레임마다 100% 재현.
      - **첫 수정은 부호가 틀림**: `legY`에 `+ squashY*0.5`를 더하는 걸로 처음 고쳤는데 재실행하니 위반이 316,438 → 329,834건으로 오히려 늘어남 — 목/꼬리가 따라가는 건 몸통 *윗변*(스쿼시로 아래로 내려감, `+squashY*0.5`가 맞음)인데 `legY`는 몸통 *아랫변*(스쿼시로 위로 올라감, `-squashY*0.5`가 맞음)을 따라가야 하는 거라 부호가 반대였던 것 — 검증 없이 "그럴듯해 보이는 첫 수정"을 그대로 믿었으면 놓쳤을 실수라, invariant 스위트를 고치고 나서도 매번 재실행해 확인하는 이 절차 자체가 그대로 값어치를 함. `legY = cfg.legY + pose.bodyBob - squashY*0.5`로 정정.
      - **최종 확인**: 같은 10종 × 6시드 × 20,000프레임(총 120만 프레임)을 동일하게 재실행 — 위반 0건. `test/sim-invariants.mjs`/`checkPoseInvariants`는 회귀 테스트로 남겨둠(`npm run test:invariants`), 앞으로 pose 계산을 건드릴 때마다 같은 방식으로 재검증 가능.
    - **귀-머리 경계 "베어 문 자국" 노치 버그 (좌표 invariant는 통과해도 실제 렌더링은 깨져 보이는 별개 문제)**: 사용자가 "여전히 웅크릴 때 뭉개져 보인다"며 `assets/reference/glitch-ear-head-seam.png`를 언급했는데 실제로는 폴더에 저장돼 있지 않았음(`bug-tail-ear-glitch.png`가 같은 식으로 없었던 전례와 동일 — 텍스트 설명만으로 재현/진단 진행). 지난 turn의 `checkPoseInvariants`는 "각 파츠가 올바른 좌표에 있는지"만 검증하지 "실제로 그려진 픽셀이 매끈한지"는 전혀 다른 질문이라 이런 종류의 버그는 애초에 잡을 수 없는 범위 밖이었음 — 이번엔 실제 렌더링 결과를 봐야 해서, Node에서 실제 `drawCreature`를 그대로 실행해 PNG로 뽑는 헤드리스 Canvas2D 서브셋(`test/mini-canvas.mjs` — `fillRect`/`save`/`translate`/`rotate`/경로 fill을 자체 구현, PNG 인코딩은 Node 내장 `zlib`만 사용, node-canvas 등 네이티브 의존성 없음)을 새로 만듦. 여기서 처음에 좌표 변환을 잘못해서(`animal-engine.js`의 `px()`/`pxJoint()`가 이미 자체적으로 `*PX(4)`를 하는데 그 위에 또 `ctx.scale()`을 얹어서 이중 배율) 크롭한 이미지가 전부 배경색으로만 나오는 삽질이 있었음 — `test/render-zoom.mjs`의 주석에 원인을 남겨둠, 실제 배율은 `PX*SCALE`이어야 함.
      - **원인**: `drawEarPointy`(고양이 4종 + 코기/허스키/포메라니안, 7종이 씀)는 삼각형이고, 머리는 `pxSoft`로 그려져서 네 모서리가 1유닛씩 깎여 있음(둥글게 보이려고 — `pxSoft` 주석 참고). 귀의 밑동이 머리 위쪽 모서리 바로 근처에 붙는데, 삼각형은 밑동에서 끝(tip)으로 갈수록 좁아지므로 머리의 모서리-컷이 있는 바로 그 행(`y=headY`)에서는 아직 밑동 폭(100%)에 도달하지 못한 상태(약 75% 폭) — 그 결과 머리의 깎인 모서리와 귀의 아직 안 넓어진 가장자리 사이에 어느 쪽도 안 채우는 작은 삼각형 틈이 남음. 배경이 반대쪽에서 그대로 보여서 지그재그(계단형) 노치로 읽힘 — 정확히 사용자가 설명한 "한 입 베어 문" 모양. 직접 좌표 계산(`legJointAngles`류 함수를 손으로 재현)으로 종별 부족량을 구해봄: `cat_a`/`cat_tuxedo`/`cat_calico`/`cat_siamese` 각 0.63유닛, `dog_husky` 0.70유닛, `dog_pomeranian` 1.00유닛(가장 심함) 부족, `dog_corgi`만 우연히 귀 폭/각도가 넉넉해서 커버됨(7종 중 6종에서 재현). 고정밀 줌 크롭(`test/render-zoom.mjs`, `PX*SCALE`=40배)과 한 픽셀씩 배경/전경을 스캔하는 스크립트로 실제 존재를 확인 — `귀-머리 경계`는 육안 확인 자체도 처음엔 96x96 전체 그림에서는 너무 작아 못 알아챘고(초반 크롭 좌표도 여러 번 재조정), 종 좌표를 정확히 읽어 크롭 범위를 계산한 뒤에야 뚜렷하게 보임(다른 종은 육안으로 안 보인다고 결론 내리지 말고 숫자로 재확인하라는 이전 턴들의 교훈 그대로).
      - **고침**: 머리를 그리는 줄을 `pxSoft(...)`에서 `px(...)`(모서리 안 깎는 일반 사각형)로 교체 — 귀 하나하나의 폭/각도를 종별로 다시 튜닝해서 모서리-컷을 커버하게 만드는 대신, 애초에 모서리-컷 자체를 없애서 6종을 한 번에 해결(귀 스타일이 바뀌어도 다시 안 깨짐). 대가는 머리 모서리가 아주 살짝(1유닛) 덜 둥글어 보이는 것뿐인데, 어차피 대부분 귀에 가려지는 부분이라 손실이 작다고 판단.
      - **부수 확인 (사용자 체크리스트 나머지 항목)**: (1) z-order — 노치의 진짜 원인은 순서 문제가 아니라 좌표 부족이었음(귀가 머리보다 나중에/위에 그려지는 건 원래도 맞는 순서). (2) 무늬(턱시도 흰 배/양말)가 웅크린 몸통 경계를 넘는지 — 지난 턴에 이미 고친 `bodyScaleY` 비례 리맵이 여전히 유효한지 `sit`/`sleep` 양쪽으로 재렌더링해 재확인, 문제 없음. (3) 다른 종 — 위에서 이미 7종 전부(귀가 삼각형인 종) 확인함; 토끼(`long`, 직사각형이라 밑동까지 폭이 안 좁아짐)·햄스터(`round`, `pxSoft`도 5유닛 미만이라 애초에 통짜로 그려짐)는 귀 모양 자체가 다르므로 같은 버그가 구조적으로 발생할 수 없음, 확인차 같이 렌더링해봤지만 문제 없었음.
      - **검증**: 고치기 전 상태로 잠깐 되돌려 `test/BEFORE-seam-{tuxedo,husky,pomeranian}.png`를 뜨고, 고친 뒤 같은 좌표로 `test/AFTER-seam-{tuxedo,husky,pomeranian}.png`를 떠서 나란히 비교 — BEFORE는 머리 경계선이 계단형으로 끊기고 배경이 삐져나오는 게 보이고, AFTER는 귀의 대각선 가장자리가 머리의 수평 가장자리로 매끄럽게 이어짐(끊김 없음). 한 픽셀씩 스캔하는 스크립트로도 BEFORE에서 보이던 "배경 열 개수"가 AFTER에서 0으로 떨어지는 것 확인. `test/sim-invariants.mjs`(120만 프레임)도 재실행해 위반 0건 유지 확인(머리를 `px`로 바꿔도 보고되는 geometry 자체는 동일 — `pxSoft`도 작은 도형이 아니면 전체 rect를 한 번에 report하므로 invariant 체크와는 무관).
    - **강아지가 가끔 물구나무 서듯 뒤집히는 버그 — 원인은 `rollover`, 상태 전환 로직은 무관함**: `anim.behaviorState`/`anim.currentIdle`을 직접 대입하는 곳이 `enterIdle`/`enterWalk` 두 곳뿐임을 확인(grep으로 전수 확인) — 즉 모든 전환이 pose 리셋을 거치므로 `rollAngle`이 전환 사이에 새서 쌓이는 종류의 버그는 아님. 진짜 원인을 찾으려고 `test/render-rollover.mjs`로 `rollover` idle의 실제 회전 곡선(0→70°→0, `Math.sin` 아치)을 프레임별로 렌더링해서 직접 봄 — `dog_dachshund`(bodyW:19/bodyH:5, 로스터에서 가장 극단적으로 길고 낮은 비율)가 `rollAngle`이 최대치(70°) 근처일 때 옆으로 구르는 게 아니라 몸 전체가 거의 수직으로 서서 판자를 세워놓은 것처럼 보임 — 회전 피벗이 몸통 중앙 근처에 고정돼 있어서, 몸이 짧고 통통하면(고양이 등) 그 각도가 "옆으로 살짝 구르기"로 읽히지만 몸이 길고 낮으면 같은 각도가 "긴 축이 거의 수직이 되는" 결과를 냄 — `dog_husky`(bodyW:14/bodyH:8)·`dog_pomeranian`(bodyW:14/bodyH:10)도 정도는 약하지만 같은 방향으로 어색함. 상태머신 버그가 아니라 "의도된 행동(장난스럽게 옆으로 구르기)이 특정 체형에서 과하게 나타나는" 케이스로 판단 — 사용자 지침대로 삭제 대신 자연스럽게 보강함.
      - **고침**: `rollover.apply(pose,t,duration,cfg)`(다른 idle과 마찬가지로 4번째 인자로 종 config를 받도록 확장)에서 회전 최대각을 고정 70° 대신 `cfg.rolloverMaxAngle ?? 70`으로 — `rollover`를 실제로 쓰는 3종(`dog_dachshund`/`dog_husky`/`dog_pomeranian`, 코기는 `rollover` 자체가 idlePool에 없음)에만 몸통 비율에 맞춰 손으로 값을 낮춤: 닥스훈트 22°(가장 극단적이라 가장 크게 낮춤), 허스키 42°, 포메라니안 58°(제일 정사각형에 가까워 원래 각도에서 덜 깎음) — 10종 전부에 공식을 적용하는 대신 이 3종만 손튜닝(이 파일 전반의 "종 몇 개뿐이니 공식보다 손으로" 원칙 그대로).
      - **검증**: `test/render-rollover.mjs`로 3종 모두 회전 곡선을 다시 렌더링 — 닥스훈트는 최대각에서도 몸이 완만하게 기울어진 "장난스러운 구르기"로 보이고(물구나무 서던 것과 확연히 다름), 허스키/포메라니안도 덜 극단적으로 개선. `test/sim-invariants.mjs` 재실행, 위반 0건(수치만 바뀐 것이라 애초에 영향 없을 걸로 예상했고 확인도 그렇게 나옴).
    - **턱시도 고양이 다리 전체가 흰색으로 보이는 버그 (사용자가 직접 코드에서 원인 특정)**: `cat_tuxedo`의 `legColor:'#ffffff'`가 다리 전체(허벅지+종아리)를 흰색으로 칠하고 있었는데, 위 tuxedo 항목의 참고 사진 설명 자체가 "white only at the very end of each leg (paws)"라고 이미 명시하고 있었음 — 발끝만 흰 양말이어야 하는데 종 재설계 당시 다리 전체를 흰색 하나로 퉁친 채 넘어갔던 것. `drawLegChain`/`drawLegPair`가 다리 하나당 색을 하나만 받는 구조라 애초에 2톤 다리를 표현할 방법이 없었음(사용자가 코드까지 읽고 구조적 원인을 짚어줌).
      - **고침**: `drawLegChain(ctx,...,color,tagPrefix,pawColor,pawFraction)`에 선택 인자 두 개를 추가 — 아래 다리뼈(무릎→발끝)를 `color`로 그린 다음, `pawColor`가 있으면 그 위에 같은 각도/폭으로 아래 다리뼈 끝부분(길이의 `pawFraction`만큼, 기본값 `PAW_FRACTION_DEFAULT=0.35`)만 한 번 더 겹쳐 그림 — 같은 각도/폭이라 이음매 없이 딱 맞물림. `pawColor`/`pawFraction`을 안 주는 종은 이 블록 자체가 완전히 건너뛰어져 기존 동작과 100% 동일. `cat_tuxedo`는 `legColor`를 몸통색(`#161616`)으로, `pawColor`를 흰색(`#ffffff`)으로 설정.
      - **다른 종 점검**: 요청대로 `cat_calico`도 확인 — `legColor:'#faf3e6'`가 `bodyColor:'#faf3e6'`와 정확히 같아서 애초에 문제 없음(원래도 몸 색 그대로인 크림색 다리, 단색 의도 그대로 맞음). 샴(`legColor:'#5c4636'`)은 몸통이 아니라 포인트 색과 일치 — 실제 샴 고양이가 다리/발끝까지 진한 포인트 색인 게 맞는 특징이라(위 샴 항목 코멘트 "dark seal-brown points on ears/face/paws/tail" 참고) 이건 흰 양말이 아니라 의도된 단색이므로 안 건드림. 허스키(`legColor:'#e8e6e0'`)는 `headColor`(흰 얼굴/가슴)와 같은 색 — 이미 확립된 "흰 얼굴+가슴" 마킹의 연장으로 다리도 통짜 흰색인 게 자연스러워 안 건드림. 발끝만 다른 색이어야 하는 종은 턱시도 하나뿐이었음.
      - **검증**: `cat_tuxedo`를 실제 `drawCreature`로 렌더링 — 고치기 전(`test/BEFORE-legs-tuxedo.png`, 다리 전체가 흰색)과 고친 뒤(`test/AFTER-legs-tuxedo.png`, 다리는 검정이고 발끝에만 작은 흰 양말)를 나란히 비교해 눈으로 확인. `test/sim-invariants.mjs` 재실행 — 위반 0건(`leg:*:*:paw` 태그는 길이 검사 정규식 `^leg:(front|back):(near|far):(upper|lower)$`에 안 걸려 그 특정 체크는 건너뛰지만, finite/캔버스 밖 이탈 등 일반 geometry 체크는 다른 모든 파츠와 동일하게 받음).
    - **닥스훈트 처진 귀가 머리보다 커서 목 아래로 늘어지는 버그 (사용자가 직접 원인 특정)**: `dog_dachshund`의 `ear:{h:7}`인데 `headH:6`이라 귀가 머리보다 크고, `drawEarFloppy`가 머리 위쪽에서 그 길이만큼 그대로 아래로 그리다 보니(`headY+1`에서 시작해 `e.h`만큼 아래로) 실제로 몸통/앞다리 영역(`bodyY:16`~`legY:21`)까지 침범해서 머리에서 뚝 떨어진 진한 색 블록처럼 보였음(사용자가 좌표까지 대조해서 정확히 짚어줌).
      - **고침**: 종별 수치를 손으로 낮추는 대신 `drawEarFloppy` 자체에 상한을 걸어서 앞으로 추가될 floppy 귀 종에도 똑같이 적용되게 함 — `effectiveH = Math.min(e.h, cfg.headH - 0.5)`로 그리는 높이를 제한(머리 아랫변에서 최대 0.5유닛까지만 처짐 허용, 그 이상은 잘림). 닥스훈트는 `e.h=7`이 `effectiveH≈5.5`로 잘려서 귀 아랫변이 머리 아랫변(`grid y=19`)에서 `19.47`(약 0.5유닛 처짐)로 끝남 — 고치기 전엔 `20.97`(약 2유닛 처짐, 몸통/다리 영역까지 침범)이었던 것과 비교해 숫자로 확인.
      - **검증**: `test/BEFORE-ear-dachshund.png`/`AFTER-ear-dachshund.png`(전신)과 더 확대한 `BEFORE/AFTER-ear-dachshund-crop.png`(귀-몸통 경계만 크게)를 나란히 비교 — AFTER에서는 귀 아래로 몸통/다리 쪽 실루엣의 원래 계단형 윤곽이 다시 드러나 보이는 반면(귀가 그 위를 안 덮음), BEFORE는 그 계단을 귀 색이 그대로 덮어버려 하나의 이어진 블록처럼 보였음. 픽셀 스캔으로 귀 색(`#8a3f1f`)이 나타나는 가장 아래쪽 행의 grid y좌표를 직접 재서 20.97 → 19.47로 줄어든 것 확인. `test/sim-invariants.mjs` 재실행 — 위반 0건(귀는 애초에 invariant가 태깅/검사하는 대상이 아니라 이 변경과는 무관 — 눈으로 보는 렌더링 검증이 유일한 확인 수단).
    - **sleep 포즈에서 귀-머리 사이에 배경색 틈이 생기는 버그 (사용자가 직접 원인 특정)**: `drawCreature`에서 머리 사각형은 `pose.bodySquash`가 클 때(`sleep`/`held`) `headY + (cfg.headH - headH)`로 윗변이 아래로 밀려 그려지는데(스쿼시가 헤드 위쪽만 깎아내는 방식이라 — 위 "귀-머리 경계 노치" 항목의 헤드 렌더링 코멘트 참고), 귀를 그릴 때 넘기는 `earCfg`는 밀리기 전의 `headY`를 그대로 쓰고 있었음 — `cat_a`로 확인해보니 `bodySquash=1`(sleep 완전히 진입)일 때 `cfg.headH - headH_actual = 1.75`유닛만큼 귀 밑동이 머리 실제 윗변보다 위에 붕 떠서, 그 사이로 배경색이 드러나 보임(사용자가 코드에서 정확히 짚어줌).
      - **고침**: 머리의 실제로 그려지는 윗변을 `headTopY = headY + (cfg.headH - headH)`라는 이름으로 명시적인 변수로 뽑아내고, 머리 `px()` 호출과 `earCfg.headY` 둘 다 이걸 쓰도록 통일. `bodySquash=0`인 다른 모든 포즈(sit/걷기/기타 idle)에서는 `cfg.headH-headH = cfg.headH-cfg.headH = 0`이라 `headTopY`가 `headY`와 정확히 같아지므로(대수적으로 완전히 동일), 이 변경이 그 포즈들의 렌더링 결과를 단 1픽셀도 바꿀 수 없음 — 그래도 사용자 요청대로 실제로 렌더링해서 확인함(아래 검증 항목).
      - **부수 발견 (faceShade도 같은 버그)**: 같은 자리를 고치다가 `faceShade`(샴 고양이의 콜러포인트 그라데이션)도 `pxDitherRadial(ctx, headX, headY, ...)`로 밀리기 전 `headY`를 그대로 쓰고 있는 걸 발견 — 요청받은 범위(귀)는 아니지만 정확히 같은 원인(헤드 실제 윗변과 안 맞는 좌표)이라 같이 고침(`headTopY`로 교체). 시각적 증상은 귀만큼 뚜렷하진 않았음(배경색 대신 그라데이션이 살짝 위로 치우치고 아래쪽이 짧아지는 정도) — 샴 sleep 렌더링(`test/BEFORE/AFTER-faceshade-sleep-siamese.png`)으로 그라데이션이 머리 아래쪽까지 더 고르게 이어지는 걸 확인. `headAttachY`(목 부착점 계산에 쓰는, 머리 내부 55% 지점)는 손대지 않음 — 목 길이가 매 프레임 실제 거리로 재계산돼 어차피 시각적 끊김이 안 생기고, 이번에 보고된 범위도 아니라서 더 깊이 들어가지 않음.
      - **검증**: `cat_a` sleep 포즈를 고치기 전/후로 렌더링해 확대 크롭 비교(`test/BEFORE-earhead-sleep-cat.png` vs `AFTER-earhead-sleep-cat.png`) — BEFORE는 귀 삼각형과 머리 사각형 사이에 뚜렷한 초록(배경) 띠가 있었는데 AFTER는 완전히 사라지고 귀가 머리에 매끈하게 이어짐. 요청대로 다른 포즈 영향 없음도 확인: `test/render-poses.mjs`로 만든 `out-cat-sit.png` 등 sit 포즈 스크린샷이 이전과 시각적으로 동일(애초에 `bodySquash=0`이라 대수적으로 변경 자체가 불가능함을 위에서 확인했지만 눈으로도 재확인). `test/sim-invariants.mjs` 재실행 — 위반 0건.
    - **강아지 "물구나무" 재현 시도 - 실제 임계값 초과 프레임을 못 찾음 (정직한 보고, 숫자 조정 없음)**: 사용자가 `rolloverMaxAngle`(22°)과 `waddleTilt`(-9°)를 정적으로 렌더링해서는 handstand가 안 나온다고 확인하고, 실행 중 순간을 잡는 계측을 요청 — `animal-engine.js`에 `setRotationDebug(sink, thresholdDeg=25)`를 새로 추가(기존 `setGeometrySink`와 동일한 옵트인 패턴, 아무도 안 부르면 완전 무비용). `createAnimal()`의 `update()` 맨 끝에서 매 프레임 `recordRotationDebug(anim, elapsed)` 호출 — `|pose.rollAngle+pose.waddleTilt|`(정확히 `drawCreature`가 `ctx.rotate()`에 넘기는 그 합)가 임계값을 넘으면 pose 전체값 + 직전 20개 상태 전환(behaviorState/currentIdleName이 실제로 바뀐 시점만 기록, 값 변화 없는 매 프레임을 다 안 남겨서 로그가 안 부풀어오름) 이력을 sink로 보고.
      - **대규모 시뮬레이션**: `test/sim-rotation-debug.mjs`가 `test/sim-invariants.mjs`와 같은 방식(dt 지터 + poke/forceSleep/wakeUp/setHeld/pause를 서로 겹치게 랜덤 주입, 이번엔 커서 힌트 랜덤 주입도 추가 — cursor-alert 트리거가 `rollover` 도중에도 끼어들 수 있는 경로라 빠뜨리면 커버리지 구멍이었음)으로 `rollover`나 `sit`(`haunches`)이 있는 종(닥스훈트/허스키/포메라니안/코기) + 대조군으로 고양이 2종을, 종당 8개 시드 × 300,000프레임(약 83분 상당) 총 1,440만 프레임 실행. 결과: 25° 초과 243,299프레임 전부 `currentIdleName:'rollover'`에서만 발생, `rollAngle`/`waddleTilt`가 동시에 유의미하게 0이 아닌 경우·60°(로스터에서 설정된 가장 큰 `rolloverMaxAngle`인 포메라니안 58°보다도 큰 값) 초과·`rollover`/`sit` 외 다른 idle에서 발생, 이 셋 중 어느 것도 0건 — 즉 전부 이미 튜닝하고 스크린샷으로 확인해둔 정상적인 rollover 피크였고 "이상 현상"은 하나도 안 잡힘.
      - **실제 Electron 앱으로도 확인**: `FOCUSPET_DEBUG_ROTATION=1`로 실행하면 main.js가 `did-finish-load` 이후 IPC로 렌더러에 계측을 켜라고 알리고, 렌더러는 `setRotationDebug`의 sink에서 다시 IPC로 main 프로세스에 이벤트를 보내 파일로 기록(콘솔이 아니라 파일인 이유: 이 창은 frameless+click-through라 devtools를 붙이기 까다로움, main.js는 진짜 Node 프로세스라 `fs` 접근이 그냥 됨). **삽질 기록**: (1) 처음엔 `loadFile(file, {query:{...}})`로 플래그를 넘기려 했는데 렌더러에서 전혀 안 잡혀서(원인은 깊이 안 팜 — IPC push가 이미 이 코드베이스에서 검증된 패턴이라 그쪽으로 바로 전환) `did-finish-load` 후 IPC push로 바꿈. (2) `Start-Process -FilePath "npm"`으로 백그라운드 실행했는데 프로세스 자체가 안 뜬 적이 있었음 — `npm`이 Windows에서 `.cmd` 셸 래퍼라 `-RedirectStandardOutput`을 쓰면 "%1 is not a valid Win32 application" 에러로 즉시 실패(리다이렉션 없이 부를 땐 방식이 달라서 되긴 했었음) — `cmd.exe /c npm start > log 2>&1`로 감싸서 해결, 이걸로 실제 콘솔 출력을 처음 볼 수 있었음. (3) 그 콘솔 출력에서 `settingsStore.js`가 `JSON.parse` 실패로 기본값(`cat_a`)에 폴백하고 있는 걸 발견 — 캐릭터를 `dog_husky`로 바꾸려고 PowerShell `ConvertTo-Json | Set-Content -Encoding utf8`로 유저의 실제 `settings.json`(`%APPDATA%\focus-pet\settings.json`)을 수정했는데, 그 방식이 UTF-8 BOM을 넣고 메시지 문자열의 한글을 깨뜨려버려서 `JSON.parse`가 터지고 있었음 — **유저의 실제 데이터 파일을 손상시킨 것**이라 Node `fs.writeFileSync(...,'utf8')`(BOM 없음)로 원래 한글 메시지를 복원하고 캐릭터를 `dog_husky`로 다시 설정, 계측이 끝난 뒤엔 원래 값(`dog_dachshund`)으로 되돌려놓음.
      - **결과**: `dog_husky`로 약 4~5분 실제 구동 — `rollover`가 1회 발생, `combinedAngle`이 25.07°~42.00°(허스키의 `rolloverMaxAngle`과 정확히 일치) 범위에서 매끄럽게 오르내렸고 출처는 전부 `rollover`, 이상 케이스 0건 — 시뮬레이션 결과와 완전히 일치.
      - **결론 (정직하게 보고)**: 라이브 실행(실제 앱 ~5분) + 대규모 시뮬레이션(1,440만 프레임, 커서/포크/수면/드래그/일시정지 상호 중첩 포함) 양쪽 다 25°를 넘는 프레임은 전부 이미 알고 있고 스크린샷으로 "장난스러운 기울임"으로 확인해둔 정상적인 `rollover` 피크였음 — `rollAngle`/`waddleTilt` 조합이나 예상 밖 출처에서 나오는 진짜 "물구나무"에 해당하는 프레임은 못 찾음. 코드를 하나도 안 고쳤음(숫자 조정 없음) — 이 현재 코드베이스에서는 재현 실패로 결론. 재발한다면 이번에 만든 계측(아래 참고)을 다시 켜서 그 시점의 `history`를 보는 게 다음 단서가 될 것.
      - **계측 중 남긴 것/치운 것**: `animal-engine.js`의 `setRotationDebug`/`recordRotationDebug`와 `test/sim-rotation-debug.mjs`(`npm run test:rotation-debug`)는 `setGeometrySink`/`test/sim-invariants.mjs`와 같은 성격(옵트인, 무비용, 재사용 가능한 회귀 도구)이라 그대로 남겨둠 — 이 조사에서 이미 한 번 실제로 값어치를 증명했으니 다음에 비슷한 리포트가 오면 바로 재사용 가능. 반면 `main.js`/`preload.js`/`pet.js`의 실제 앱 전용 배선(env var 릴레이, IPC 핸들러, `reportRotationDebug`/`onEnableRotationDebug`)은 이번 조사 한정 용도라 전부 제거함 — 원인 진단용 `env-check.log` 남긴 것도 삭제. `test/rotation-debug.log`(시뮬레이션 쪽 요약)만 남아있고 실앱 로그(`rotation-debug-live.log`)는 정리함.
    - **"물구나무" 진짜 원인 발견 — 회전각이 아니라 다리가 몸통과 같이 뻣뻣하게 도는 것 (사용자가 화면 녹화를 프레임 단위로 분석해서 특정)**: 위 조사는 "`rollAngle`/`waddleTilt` 합이 튀는 순간"을 찾는 가설이었고 그건 못 찾았음(정확한 결론이었음, 아래 내용이 그걸 틀렸다고 뒤집는 게 아님) — 사용자가 실제 리포트된 녹화 396프레임을 직접 프레임별로 분석해 다른 각도로 짚어줌: 실루엣의 가로:세로 비율이 전체 영상에서 한 번도 1.7:1 밑으로(수직에 가깝게) 안 떨어짐 — 즉 `rolloverMaxAngle`로 이미 깎아둔 회전각 자체는 튄 적이 없다는 뜻. 대신 `legJointAngles`를 다시 읽어보니: `rollover.apply()`는 `pose.rollAngle`(몸통 전체를 `drawCreature` 맨 위 `ctx.rotate()`로 회전시키는 값)과 `legsTuckedFront/Back`(최대 0.4까지만 tuck 방향으로 블렌드)만 세팅하고, 다리의 hip/knee 각도 자체는 그 tuck 블렌드 외엔 평소 서 있을 때 계산(`phase*swingAmp` 등, idle 중엔 `legPhase=0`이라 사실상 각 종의 기본 서 있는 각도)에서 별로 안 움직임 — 다리가 몸통 로컬 좌표계 안에서 "힘이 빠진" 각도로 바뀌는 게 전혀 없이 몸통과 같은 `ctx.rotate()` 안에서 그대로 같이 돔. 그 결과 다리의 **월드(화면) 공간 각도 = 로컬 각도 + rollAngle**로 그냥 더해지는데, 로컬 각도가 원래 서 있는 각도(수직에서 15~20° 정도)라 rollAngle이 커질수록 월드 각도가 더 커져서(예: 포메라니안 피크에서 15.9°+57.6°=73.5°, 거의 수평) 몸통이 기울어질수록 다리가 오히려 더 "뻗대며 버티는" 것처럼 보였던 것 — 각도 자체는 정상 범위인데 다리 렌더링이 그 회전을 안 따라가는 게 진짜 문제였음. (닥스훈트에 이미 있던 `cfg.joints.floppyRollover` 플래그+`knee += Math.sin(...)*18` 블록은 무릎에 코스메틱 웨이브만 얹는 것뿐이라 — 자기 주석에도 "purely cosmetic extra wiggle on top of the tuck already driven by legsTucked"라고 명시돼 있었음 — hip 각도의 기본값 자체를 안 바꾸므로 이 문제를 해결 못 했고, 애초에 허스키/포메라니안엔 설정도 안 돼 있었음.)
      - **고침**: `held`(드래그로 집었을 때)가 이미 쓰던 `pose.dangle`/`pose.dangleSway` 메커니즘(`legJointAngles` 맨 끝, hip/knee를 무조건 덮어씀 — 위 "잡힌 느낌" 항목 참고)을 `rollover.apply()`에서도 같이 세팅하도록 확장 — `rollAngle`과 같은 `s`(0→1→0 사인 아치) 엔벨로프로 `pose.dangle = s`, 그리고 `pose.dangleSway = -pose.rollAngle`. `pxJoint`의 자체 `ctx.rotate(rad)`와 `drawCreature` 바깥쪽의 `ctx.rotate(angle)`가 같은 Canvas2D 회전이라 그냥 더해진다는 걸 이용 — 다리 로컬 각도를 `-rollAngle`로 주면 바깥쪽 `+rollAngle`과 정확히 상쇄돼서, 몸통이 얼마나 기울든 다리의 최종 월드 각도가 항상 (거의) 0°(수직 아래, 중력 방향)로 유지됨 — "몸은 굴러도 다리는 중력 따라 축 늘어진 채 그대로"라는, `held`와 정확히 같은 물리적 직관. `legAngle` 자체는 안 건드리고 렌더링(다리가 몸통 회전을 따라가는 방식)만 바꿨으므로 `rolloverMaxAngle`(22°/42°/58°) 수치는 요청대로 그대로 둠. 닥스훈트 전용이던 기존 `floppyRollover` 플래그+무릎 웨이브 블록은 이제 3종 모두를 포괄하는 이 메커니즘이 완전히 대체하므로(부분적으로만 잔존해봐야 dangle 블렌드에 일부 섞여 들어가는 것 말고는 득이 없음) 제거 — `legJointAngles`에서 그 블록과 `dog_dachshund.joints.floppyRollover:true`를 삭제.
      - **수치 검증 (육안보다 우선 — 이전 턴들의 교훈 그대로)**: `setGeometrySink`로 `leg:front:near:upper` 태그의 `hingeX/Y`·`tipX/Y`를 읽어 로컬 hip 각도를 역산(`atan2`)하고, 고치기 전 코드(임시 사본)와 나란히 각 종 rollover 피크 프레임에서 비교 — **로컬 hip각+rollAngle(=월드 각)**이 고치기 전엔 닥스훈트 41.7°/허스키 60.8°/포메라니안 73.5°(전부 수직에서 크게 벗어남, 회전이 클수록 더 벌어짐)였던 게, 고친 뒤엔 닥스훈트 0.4°/허스키 0.5°/포메라니안 0.6°(3종 모두 수직에서 1° 안쪽)로 수렴 — 의도한 "몸이 얼마나 기울든 다리는 중력 방향으로 늘어짐"이 수치로 정확히 확인됨. 스크린샷 비교(96x96 전체 뷰, 확대 크롭 둘 다 시도)는 포메라니안·허스키처럼 다리 세그먼트 길이(`upperLen`/`lowerLen`)가 짧은 종에서는 각도 변화폭(60~70°)에 비해 실제 픽셀 이동량이 크지 않아 육안으로는 뚜렷이 안 갈렸음(이전 턴의 스퀴시 펄스가 "체고 변화 4px라 눈으론 안 보였다"던 것과 같은 종류 — 수치 트레이스로만 확실히 확인됨). `npm run test:invariants`(120만 프레임) 재실행 — 위반 0건.
      - **다음에 참고할 것**: `test/render-rollover.mjs`(이미 있던 스크립트, 그대로 재사용 — 각 종 rollover 사이클을 5개 지점으로 나눠 렌더링)는 이번에도 1차 육안 확인용으로 다시 돌려서 최신 스프라이트로 갱신해둠(`test/rollover-{dachshund,husky,pomeranian}-f{15,30,45,60,75}.png`). 진단에 썼던 "before 엔진 사본 + 수치 트레이스 스크립트"는 이번 조사 전용이라 정리함 — 비슷한 회전/포즈 버그가 다시 리포트되면 `setGeometrySink` + `atan2`로 로컬 각도를 역산해 before/after를 수치로 비교하는 이 방법이 육안 판단보다 먼저 믿을 수 있는 절차라는 걸 기억해둘 것.
    - **rollover 애니메이션 재설계 — dangle 수정으로도 안 고쳐졌던 진짜 원인 (사용자가 화면 녹화를 다시 프레임 단위로 분석해서 특정)**: 위 dangle 수정(다리를 world-vertical로 늘어뜨림) 이후에도 "앞다리로 버티고 뒷다리만 붕 뜬" 모양이 그대로 남아있다는 리포트 — 사용자가 이번엔 회전 피벗 자체(`drawCreature` 맨 위 `cx = (cfg.bodyX + cfg.headX + cfg.headW) / 2`)가 몸통 정중앙이 아니라 머리 쪽에 가깝다는 가설을 제시. 좌표로 직접 검산해봄: 닥스훈트는 `cx=12`인데 꼬리(`tail[0].x=0`)까지 포함한 전체 실루엣의 시각적 경계는 대략 x=0(꼬리)~x=24(코끝)라 중점은 12 — 즉 피벗이 "머리 쪽으로 치우쳤다"기보다는 이미 전체 실루엣의 기하학적 중점에 상당히 가까움(허스키도 마찬가지로 계산상 1 grid unit 이내 오차). **그런데도 사용자가 관찰한 증상 자체는 100% 정확함** — 진짜 원인은 피벗의 위치가 아니라 **애초에 "긴 막대를 하나의 피벗으로 강체 회전시키는" 접근 자체**였음: 몸통 폭(닥스훈트 19유닛)에 비해 몸높이(5유닛)가 극단적으로 얇은 형태에서는, 피벗을 아무리 정확히 중앙에 맞춰도 다리가 붙어있는 피벗 근처 영역은 회전각과 무관하게 화면상 이동량이 작고(반지름이 작으므로), 피벗에서 먼 꼬리 끝은 반지름이 커서 같은 각도에도 훨씬 큰 호를 그리며 휙 움직임 — 이게 바로 "다리는 거의 안 움직이는데(=버티고 서 있는 것처럼 보임) 꼬리만 붕 뜬다"는 정확히 그 인상을 만듦, 피벗을 어디로 옮겨도 한쪽 끝은 항상 이렇게 큰 호를 그릴 수밖에 없는 구조적 문제. 사용자도 "회전축 미세조정 대신 다른 방식으로" 요청했는데, 위 검산이 바로 그 판단이 옳았던 이유를 뒷받침함 — 회전축을 어디로 옮겨도 근본적으로 안 고쳐졌을 것.
      - **재설계 방향 결정 (스크린샷 실측 비교)**: 사용자가 제시한 두 방향 — A) 큰 회전 대신 `bodySquash`를 크게 줘서 옆으로 누운 듯 납작하게 만들고 거기에 짧은 주기로 여러 번 오가는 작은 흔들림을 얹는 "누운 채로 좌우로 뒹구는" 방식, B) rollover 자체를 다른(더 차분한) 행동으로 교체 — 둘 다 실제로 구현해서(B는 `test/_candidateB-engine.mjs`라는 임시 사본에, A는 실제 엔진에) 닥스훈트/포메라니안 rollover 사이클을 스크린샷으로 렌더링해 비교함. **B(차분한 lean, 오실레이션 없이 한 번만 살짝 기울고 유지)** 결과: 닥스훈트는 몸높이(5유닛)가 워낙 얇아서 `bodySquash` 공식(`squashY = bodySquash * bodyH * 0.35`)이 절대 픽셀 변화량으로 환산되면 거의 안 보이는 수준(스크린샷상 거의 평소 서 있는 자세와 구분이 안 감) — 반대로 포메라니안(몸높이 10유닛, 로스터에서 가장 정사각형에 가까움)은 같은 정규화 수치인데도 절대 변화량이 훨씬 커서 과하게 기울어 보임 — 즉 B는 종별 체형에 따라 효과 크기가 들쭉날쭉하고, 대표적으로 가장 문제였던 닥스훈트에서는 사실상 "뒹구는" 느낌이 거의 안 남. **A(적극적 wallow)**는 `bodySquash` 상한을 훨씬 높게(0.9) 잡아 닥스훈트에서도 눈에 띄게 납작해지고, 다리도 명확히 다른 위치로 벌어져 보여 "뒹굴거리는" 인상이 뚜렷함 — 4종 전부(코기 포함, 아래 참고) 스크린샷으로 확인해 채택.
      - **최종 구현 (A 채택)**: `rollover.apply(pose, t, duration)`을 전면 재작성 — 더 이상 `cfg`(`rolloverMaxAngle`)를 안 받음. `envelope = Math.sin(u*Math.PI)`(behavior 진행률 0→1→0의 부드러운 인/아웃)로 `bodySquash`를 `0.15~0.9`까지 올려(옆으로 누운 듯 납작하게 — `sleep`이 이미 전 종에서 문제없이 쓰는 것과 같은 메커니즘, 회전이 전혀 없어서 이 버그 자체가 구조적으로 발생할 수 없음), `bodyBob`을 `envelope*1.6`만큼 내려서(`sleep`과 같은 부호 — 스쿼시되며 바닥 쪽으로 가라앉는 방향) 땅에 눕는 느낌을 보강. 큰 단일 회전 대신 `pose.rollAngle = Math.sin(t*5.5)*14*envelope` — 진폭을 예전 최소치(닥스훈트 22°)보다도 작은 ±14°로, 대신 behavior 지속시간(2.6~3.4초) 동안 여러 번(약 2~3사이클) 오가게 해서 "한 번 크게 구른다"가 아니라 "누운 채로 좌우로 흔들거린다"는 인상을 냄 — 이미 `bodySquash`로 납작해진 실루엣이라 이 정도 진폭으로는 절대 "서 있는 것처럼" 보일 수 없음(아래 검증 참고). 다리는 `held`가 쓰는 `pose.dangle`/`dangleSway` 메커니즘을 재사용하되(위 dangle 수정 항목 참고) `dangleSway = Math.sin(t*7+1.3)*22`로 `rollAngle`과 **독립된 위상**을 줌 — 몸의 흔들림과 다리가 1:1로 같이 움직이면 "그냥 뻣뻣하게 딸려간다"로 보이는데, 위상이 다르면 "몸은 몸대로 흔들리고 다리는 허공에서 따로 파닥거린다"로 읽혀 뒹구는 느낌이 강해짐. `cfg.rolloverMaxAngle`은 이 설계에서 의미가 없어져서 3종(`dog_dachshund`/`dog_husky`/`dog_pomeranian`) 전부에서 필드와 관련 주석을 삭제.
      - **코기에 처음으로 rollover 추가**: 기존엔 코기(`hasTail:false`)의 idlePool에 `rollover` 자체가 없었음(옛 설계가 꼬리 유무와 무관하게도 시도된 적이 없었을 뿐) — 새 설계는 `bodySquash`/`rollAngle`(작은 흔들림)/`dangle`(다리 파닥임) 중 어느 것도 꼬리를 참조하지 않으므로 꼬리 없는 종에도 구조적으로 아무 문제 없이 적용됨, 실제로 스크린샷 확인 결과도 문제 없어서 `dog_corgi`의 `idlePool`에 `{ name: 'rollover', weight: 1 }`을 다른 3종과 같은 가중치로 추가 — 이제 강아지 4종 전부가 rollover를 가짐.
      - **수치 검증 (사용자의 원래 진단 방법을 그대로 재사용)**: 사용자가 최초 버그를 "396프레임 동안 실루엣 가로:세로 비율이 1.7:1 밑으로 안 떨어짐(=한 번도 수직에 가깝게 안 섬)"으로 검증했던 것과 같은 방법을 재검증 스크립트(`test/render-rollover.mjs`)에 그대로 프로그램화 — `setGeometrySink`로 매 프레임 몸통/머리/다리/꼬리 파츠의 bounding box를 모아 가로:세로 비율을 계산하고, rollover 사이클 전체(약 150~190프레임)에서 최솟값을 추적. 결과: 닥스훈트 2.42, 코기 1.72, 허스키 1.54, 포메라니안 1.45 — 4종 전부 1.0을 여유 있게 웃돌아(가로가 세로보다 항상 더 큼) 한 번도 "수직에 가깝게 서는" 순간이 없음을 확인. `npm run test:invariants`(120만 프레임) 재실행 — 위반 0건.
      - **최종 스크린샷 검증**: `test/render-rollover.mjs`로 4종 전부 rollover 사이클을 촘촘히(12프레임 간격) 렌더링해 육안으로 훑고, 대표 5프레임(`f000/036/072/108/144`)을 큐레이션해 `test/rollover-dog_{dachshund,corgi,husky,pomeranian}-f*.png`로 남김 — 전 종에서 몸이 납작하게 눌린 채 좌우로 흔들리고 다리가 각 프레임마다 다른 각도로 벌어져 있는 게 보이며, 예전처럼 몸이 좁고 길게 세워지거나 앞다리만 뻗대고 있는 프레임은 4종 어디에도 없음. 정리: candidate B 비교용 임시 사본(`test/_candidateB-engine.mjs`, `test/_render-candidateB.mjs`, `candB-*.png`)과 예전 단일-arc 설계 전용이었던 구 `render-rollover.mjs`(5프레임 고정 샘플링, 새 설계의 여러 흔들림 사이클을 못 잡음)는 정리하고, 촘촘한 샘플링+수치 검증을 겸하는 새 버전으로 대체.
    - **rollover 재설계 2차 — "시소처럼 보인다" 피드백, rollAngle 오실레이션 자체를 포기하고 "등 대고 누워 다리 버둥거림"으로 전환**: 위 재설계(스쿼시+`rollAngle` 소진폭 다회 흔들림)는 "서 있는 것처럼 보이는" 문제는 확실히 없앴지만, 사용자가 실제로 보니 몸 전체가 규칙적으로 좌우로 왔다갔다하는 게 시소/그네처럼 보인다는 새 피드백 — `Math.sin(t*5.5)*14`가 진폭은 작아도 "한 주파수로 깨끗하게 오가는 단일 진동"이라는 형태 자체가 문제였음(진폭을 더 줄이는 미세조정이 아니라 사용자가 아예 "rollAngle 오가는 접근 자체를 그만두자"고 명시). 사용자 진단: 실제로 뒹구는 강아지의 핵심은 "몸이 기울어짐"이 아니라 "등을 바닥에 대고 누워서 다리를 든 채 버둥거림" — 몸통 회전은 완전히 부차적이어야 하고 다리 자체의 자세/움직임이 주가 되어야 함.
      - **몸통**: `rollAngle`을 몸 전체를 흔드는 용도에서 빼고, 대신 두 개의 서로 다른(맞아떨어지지 않는) 주파수 사인을 합친 미세한 "불규칙한 씰룩거림"으로 교체 — `(Math.sin(t*11) + Math.sin(t*17.3+0.8)*0.6) * 2.2 * envelope`(피크 진폭 약 ±3.5°, 사용자가 제시한 "정 필요하면 ±5도 이내" 안에 여유 있게 들어옴) — 단일 주기가 아니라 서로 안 맞물리는 두 주기를 더해서 "일정한 리듬으로 오간다"는 인상 자체가 안 생기게 함(시소는 규칙적인 왕복이라 시소로 보였던 것이므로, 규칙성을 깨는 게 핵심이라 판단). `bodySquash`(최대 0.9)와 `bodyBob`(가라앉음)은 이전 설계 그대로 유지 — 납작하게 눕는 부분 자체는 문제 없었음.
      - **다리 뒤집기 (`legJointAngles`의 새 `legsUp` 오버라이드)**: 기존 `dangle`(hip/knee를 `dangleSway`/12°로 고정 — "축 늘어짐", `held`에서 쓰는 늘어진-아래-방향 다리)와는 반대로 다리가 **위쪽**(pxJoint 각도 컨벤션상 0°=아래이므로 180°=위)을 향하게 하는 완전히 새 메커니즘을 추가 — `pose.legsUp`(0..1 블렌드 팩터, `dangle`과 같은 자리에서 마지막에 적용되어 항상 우선함)과 `pose.paddleHipFront/Back`·`paddleKneeFront/Back`(앞/뒤 다리 쌍이 서로 다른 주파수·위상으로 `180°` 중심으로 오가는, 매 프레임 완전히 계산된 목표각). `dangle`과 `legsUp`을 별도 필드로 분리한 이유: 하나는 "무게가 안 실려 축 늘어짐"(느린 진자), 하나는 "뒤집혀서 허공에 버둥거림"(빠르고 진폭 큰 파닥임)으로 물리적 의도 자체가 달라 같은 메커니즘을 억지로 겹쳐 쓰면 나중에 헷갈릴 여지가 있다고 판단(이 파일 전반의 "의미가 다르면 필드도 분리" 컨벤션 그대로). `rollover.apply()`는 이제 `dangle`/`dangleSway`를 전혀 안 건드리므로(오직 `held`만 여전히 씀) 실질적 충돌은 없음.
      - **z-order 문제 발견 + 고침 (렌더링해보기 전엔 몰랐을 것)**: 다리를 위로 향하게만 하고 처음 렌더링해보니 다리가 전혀 안 보이거나 몸통에 파묻혀 보임 — 원인은 `drawCreature`가 원래 다리를 몸통보다 **먼저**(뒤에) 그리는 순서였기 때문(서 있는/걷는 다리는 힌지가 몸통 아랫변에 있고 아래로 뻗으므로, 몸통에 가려지는 건 원래 의도된 정상 동작 — 벨리 밑에서 다리가 시작되는 것처럼 보이려는 것). 다리가 **위로** 향하면 같은 힌지(`legY`, 몸통 아랫변)에서 몸통의 세로 범위(스쿼시된 높이) 안쪽으로 뻗어 들어가는 셈이라, 뒤에 그려지면 몸통에 완전히 덮여버림. **고침**: `pose.legsUp>0`일 때는 다리를 원래 자리(몸통보다 먼저)에서 안 그리고, `drawCreature` 맨 끝(머리/눈/코까지 다 그린 뒤, `ctx.restore()` 직전)으로 미뤄서 다시 그리게 함 — "등 대고 누운 개는 버둥거리는 다리가 몸보다 화면에 더 가까운(앞에 있는) 요소"라는 실제 원근과도 맞아떨어져서 단순 z-order 땜빵이 아니라 타당한 순서.
      - **다리 길이가 몸에 파묻히는 문제 (두 번째 렌더링에서 발견) + 종별 파생 공식으로 고침**: z-order를 고쳐도 다리가 짧아서 스쿼시된 몸통 자체의 세로 범위를 못 벗어나 여전히 몸통 위에 얹힌 작은 표식 정도로만 보임(`setGeometrySink`로 다리 끝 y좌표와 몸통 윗변 y좌표를 직접 비교하는 진단 스크립트로 확인 — 최초 시도는 4종 중 3종에서 다리 끝이 몸통보다 여전히 아래(=몸통에 파묻힘)였음). 다리 길이(`j.upperLen+j.lowerLen`, 종별 고정값)에 `reachScale`을 곱해 늘리는 방식으로 대응하되, **고정 배율 하나로는 안 됨** — 포메라니안(몸높이 10, 로스터에서 가장 크면서 다리는 가장 짧은 0.6+0.6=1.2)과 허스키(몸높이 8, 다리 1.1+1.1=2.2)는 몸-다리 비율이 몇 배씩 차이나서, 허스키 기준으로 맞춘 배율을 포메라니안에 그대로 쓰면 전혀 안 벗어나고, 포메라니안 기준으로 맞추면 허스키 다리가 막대기처럼 과하게 길어짐(실제로 배율 6까지 시도해봤다가 스크린샷에서 다리가 몸통 절반 길이만 한 긴 사선 막대로 튀어나와 보여 되돌림). **고침**: `drawLegPair`에서 종별로 "이 종이 실제로 필요한 만큼만" 유도 — `neededReach = cfg.bodyH * 0.685 + 2`(0.685 = rollover의 최대 `bodySquash`인 0.9에서 살아남는 몸높이 비율 `1-0.9*0.35`, +2는 몸통 경계를 눈에 띄게 벗어나라는 여유분), `maxReachScale = min(4.2, max(1, neededReach / (j.upperLen+j.lowerLen)))`(상한 4.2는 포메라니안처럼 극단적으로 짧은 다리에 억지로 큰 배율을 줘서 막대기처럼 보이는 걸 막는 안전판), `reachScale = 1 + pose.legsUp * (maxReachScale - 1)`. `checkPoseInvariants`의 다리 길이 검증(`leg:*:*:upper/lower` 길이가 종별 authored 값과 맞는지 보는 하드 invariant, 앞선 세션에서 만든 것)도 같은 공식으로 업데이트해서 이 의도된 변화를 오탐하지 않게 함.
      - **수치로 확인한 최종 상태 (정직한 보고 — 4종 전부 완전히 몸통을 벗어나지는 않음)**: 위 공식으로 튜닝한 뒤, 다리 끝 y좌표가 몸통 윗변보다 위로 올라가는 정도("clearance", 양수면 몸통을 벗어남)를 rollover 사이클 전체(약 150~200프레임)에서 추적: 닥스훈트 최대 +1.96유닛(172프레임 중 84프레임에서 벗어남), 코기 최대 +0.81유닛(155개 중 24개), 허스키 최대 +1.95유닛(170개 중 61개), **포메라니안은 사이클 전체에서 한 번도 몸통 윗변을 넘지 못함(최대 -1.89, 즉 항상 몸통 안쪽)** — 포메라니안은 몸높이 대비 다리가 로스터에서 가장 짧고(0.6+0.6) 몸통은 가장 큰(10) 조합이라, 위 상한(4.2배)까지 늘려도 물리적으로 부족함. 상한을 6까지 올려서 시험해봤을 때는 수치상 아주 살짝 벗어나긴 했지만(+0.24, 200프레임 중 9프레임만) 실제 스크린샷에서는 다리 각도가 수직에서 먼 순간에 몸 절반 길이만 한 얇고 긴 대각선 막대로 튀어나와 보여 다른 프레임들에서 오히려 더 부자연스러웠음 — 그래서 "가끔 아주 살짝 벗어나지만 나머지 프레임에서 막대기처럼 안 보이는" 6배 대신 "전혀 안 벗어나지만 비율은 자연스러운" 4.2배 상한을 최종으로 선택함(스크린샷으로 둘 다 직접 비교해서 판단 — 수치 하나만 보고 정하지 않음). 포메라니안은 다리가 몸통 실루엣을 완전히 벗어나 허공에 뜨는 것처럼 보이진 않지만, 다리 방향이 위쪽으로 뒤집혀 있고 다른 세 다리와 함께 제각각 다른 각도로 벌어져 있는 건(옅은 다리색이 몸통색과 가까운 것도 겹쳐 눈에 덜 띄는 요인 — `legColor`가 `bodyColor`와 동일) 뚜렷이 보임 — "완벽한 클리어런스" 대신 "비율이 안 깨지는 선에서 최대한"으로 타협한 것을 정직하게 기록. 이후 이 종을 더 다듬으려면 다리 길이보다(이미 한계까지 늘림) `legColor`를 몸통과 다르게 주는 쪽이 다음으로 시도해볼 만한 방향.
      - **꼬리**: 사용자 요청대로 `pose.tailOverride=true` + `tailPhase=t*8`/`tailAmp=1.3*envelope`로 `held`/`tailtwitch`와 같은 메커니즘을 재사용해 꼬리도 같이 흔들리게 함 — 꼬리 없는 코기(`hasTail:false`)는 이 필드들이 설정돼도 애초에 그릴 꼬리 자체가 없어 완전히 무해.
      - **회귀 검증**: `npm run test:invariants`(120만 프레임, 다리 길이 invariant 공식 갱신 포함) 위반 0건, `npm run test:movement-stats` 전 종 walk 0.0% 유지(무관한 변경이라 예상대로). `test/render-rollover.mjs`로 4종 전부 재렌더링해 대표 5프레임(`f000/036/072/108/144`)으로 교체 확인 — 몸 전체가 좌우로 왕복하는 시소 형태의 움직임은 어디에도 없고(작은 불규칙 씰룩임만 있음), 다리가 위쪽을 향한 채 프레임마다 다른 각도로 벌어져 있어 "버둥거림"으로 읽힘(허스키·코기·닥스훈트는 다리가 몸통 윤곽 밖으로 뚜렷이 보이는 프레임이 있고, 포메라니안은 위 항목대로 부분적). 진단에 썼던 스크래치 스크립트(`test/_diag-legreach.mjs` 등)는 정리.
    - **rollover 최종 수정 — "노 젓는 것처럼 보인다" 피드백, 다리 움직임을 파닥임에서 떨림으로 축소 (마지막 시도, 실패하면 삭제하기로 사전 합의)**: 위 paddle 설계(`paddleHipFront/Back`가 180°를 중심으로 ±42~48°, `paddleKneeFront/Back`가 서로 다른 위상/주파수로 오가는 것)에 대해 "다리가 360도로 계속 돌아가며 노 젓는 것처럼 보인다"는 피드백 — 원인은 hip과 knee가 같은 다리 안에서 서로 다른 주파수(예: 앞다리 hip 6.3rad/s, knee도 6.3rad/s인데 위상이 1.9만큼 어긋남)로 움직이면 두 관절이 합쳐진 발끝 궤적이 리사주 도형처럼 루프를 그리게 되고, 진폭도 커서(±35~48°) 이 루프가 눈에 확 띄는 크기로 나왔던 것 — "떨림"이 아니라 실제로 "휘젓는" 움직임을 그리고 있었던 셈. **고침**: 사용자가 제시한 "마지막으로 한 번 더 시도, 안 되면 삭제"에 따라 hip/knee 진폭을 대폭 줄이고(180±9°, 무릎도 14±8°) **같은 다리 안에서는 hip과 knee가 완전히 같은 위상/주파수**를 쓰도록 통일(앞다리 `t*15`, 뒷다리는 `t*13.5+1.1`로 앞/뒤 쌍끼리는 여전히 다르게 — "제각각 움직이는 느낌"은 앞/뒤 쌍 사이에서만 남기고, 한 다리 안에서의 루프는 없앰) — 두 관절이 항상 같은 위상으로 같이 움직이므로 발끝이 그리는 궤적이 루프가 아니라 거의 직선에 가까운 왕복이 되어 "짧게 떨림"으로 읽힘. `bodySquash`/`bodyBob`/몸통의 미세한 불규칙 흔들림(`rollAngle`)은 이전 설계에서 이미 검증됐던 부분이라 손대지 않음.
      - **검증**: 정적 스크린샷 비교로는 "떨림 vs 회전"을 구분하기 어려워서(진폭이 작아 한 프레임만 봐서는 잘 안 보임) `test/_diag-tremble-seq.mjs`(스크래치, 검증 후 삭제)로 rollover 피크 구간에서 3프레임(0.05s) 간격의 연속 스크린샷 6장을 4종 전부에 대해 렌더링 — 다리가 매 프레임 조금씩만 위치를 바꾸며 위쪽을 향한 자세를 유지하는 걸 눈으로 확인(허스키/코기는 특히 또렷하게 다리 모양이 몸통 위로 드러남, 닥스훈트/포메라니안은 이전과 같은 이유로 덜 두드러지지만 회전/루프처럼 보이는 프레임은 없음). `npm run test:invariants`(120만 프레임) 재실행 — 위반 0건. 결과가 충분히 만족스러워서(더 이상 "노 젓기"로 안 보임) 삭제 대신 유지하기로 결정 — `rollover`는 강아지 4종 idlePool에 그대로 남음.
    - **rollover 완전 삭제 + `bellyUp`(배 하늘로 향한 채 가만히 쉬기)으로 교체 — 계속 붙잡지 않기로 결정**: 위 "떨림" 수정 이후에도 "어정쩡하게 엎드린 채 눈이 감긴 것도 뜬 것도 아닌 애매한 자세로 멈추는 것 같다"는 새 피드백 — 사용자가 "전환 크로스페이드나 눈 감김 상태가 중간에 꼬였을 가능성"을 직접 지목. 이번엔 사용자가 명시적으로 "더 붙잡지 말고 완전히 삭제, 대신 훨씬 단순한 정적 포즈로 교체"를 지시 — rollover는 이번까지 포함해 총 4번의 서로 다른 설계(①큰 단일 회전 → 핸드스탠드처럼 보임, ②스쿼시+`rollAngle` 소진폭 다회 흔들림 → 시소처럼 보임, ③다리를 `legsUp`/`paddleHip·Knee`로 파닥이게 → 노 젓는 것처럼 보임, ④파닥임을 떨림으로 축소 → 이번 리포트의 "어중간한 정지 자세")를 거쳤고, 매번 겉으로 드러난 증상은 달랐지만 공통점은 전부 **시간에 따라 변하는 값(회전각, 흔들림, 파닥임/떨림)이 있었다는 것** — 사용자 지침대로 근본 원인을 더 파고드는 대신, "동적인 요소를 아예 없애면 이 종류의 버그 자체가 구조적으로 생길 수 없다"는 판단으로 완전 삭제 + 무(無)-애니메이션 재설계를 선택.
      - **삭제한 것**: `IDLE_BEHAVIORS.rollover` 전체, `defaultPose()`의 `legsUp`/`paddleHipFront`/`paddleKneeFront`/`paddleHipBack`/`paddleKneeBack` 필드, `POSE_UNIT_FIELDS`의 `legsUp` 항목, `POSE_FIELD_BOUNDS`의 `paddleHipFront/Back`·`paddleKneeFront/Back` 항목, `legJointAngles`의 `pose.legsUp>0` 오버라이드 블록, 강아지 4종(`dog_dachshund`/`dog_corgi`/`dog_husky`/`dog_pomeranian`) idlePool의 `rollover` 항목, `test/render-rollover.mjs`와 `test/rollover-dog_*.png`(더 이상 존재하지 않는 behavior를 테스트하던 스크립트라 그대로 두면 무한루프/에러만 남음).
      - **유지한 것 (버그와 무관했던 부분)**: 다리가 몸통에 가려지지 않게 몸통보다 나중에 그리는 z-order 우회(`drawCreature`의 `bellyUp` 분기)와, 종별 몸높이/다리길이 비율에서 필요한 만큼만 다리 길이를 늘리는 `reachScale` 유도 공식(`drawLegPair`) — 둘 다 "다리를 위로 향하게 해서 보이게 하는" 정적인 기하 문제이지, 지금까지 문제였던 "다리가 시간에 따라 어떻게 움직이는가"와는 무관한 부분이라 그대로 재사용(단, 필드명은 `legsUp`→`bellyUp`, 곱셈 상수는 새 `bodySquash` 피크(0.9→0.85)에 맞춰 0.685→0.15로 갱신).
      - **새 `bellyUp`**: `duration: randRange(4,7)`, `apply(pose,t)`가 `s=clamp01(t/0.6)`(그 뒤로는 안 바뀌고 유지 — `sleep`이 이미 오래 검증해온 것과 똑같이 "한 번 램프하고 계속 유지"하는 패턴, `rollover`가 매번 새로 만들었던 `sin(u*Math.PI)` 형태의 "다시 0으로 돌아오는" 엔벨로프 자체를 안 씀)로 `bodySquash=s*0.85`, `bodyBob=s*1.6`, `bellyUp=s`(다리가 위를 향하는 정도), `headDY=s*0.5`, `headDX=-s*0.3`, 그리고 **`eyesClosed=0`을 명시적으로 세팅**. 다리 목표각 자체도 `legJointAngles`에서 `hip=lerp(hip,180,pose.bellyUp)`, `knee=lerp(knee,15,pose.bellyUp)`로 **시간(`t`)이 전혀 안 들어가는 상수** — 예전 `paddleHip/KneeFront/Back`처럼 매 프레임 다시 계산되는 오실레이터가 아예 없음. 꼬리도 `tailOverride`를 안 건드려서 종별 평소 idle 흔들림이 그대로 나감(예전 rollover처럼 별도 흔들기 로직 없음). 몸통 흔들림(`rollAngle`)도 아예 안 건드림(0으로 유지) — "동적인 움직임을 최소화"라는 요청을 문자 그대로, 렌더링에 필요한 최소한(스쿼시+다리각+약간의 머리 처짐, 전부 한 번 램프하고 고정)만 남김.
      - **`eyesClosed=0`을 명시한 이유**: 리포트된 "눈이 감긴 것도 뜬 것도 아닌" 증상의 유력한 후보가 크로스페이드였음 — `sleep`(eyesClosed를 1까지 올림)에서 곧장 전환되면, 옛 rollover의 `apply()`는 `eyesClosed`를 아예 안 건드려서 `defaultPose()`의 기본값(0)에 맡기고 있었는데, 이 경우 크로스페이드가 "직전 값(최대 1) → 이번 프레임의 목표값(0)"을 0.25초에 걸쳐 블렌딩하는 것 자체는 정상 동작이지만, 그 사이(전환 직후 몇 프레임)에 화면을 캡처하면 눈이 부분적으로만 감긴 어중간한 순간을 그대로 보게 됨 — 이게 사용자가 본 "애매한 자세"였을 가능성이 있음(정확한 재현은 못 했지만, 개연성 있는 메커니즘). 새 `bellyUp`은 매 프레임 `pose.eyesClosed=0`을 **명시적으로** 세팅하므로, 크로스페이드가 매 프레임 블렌딩하는 목표값 자체가 항상 정확히 0으로 고정 — 크로스페이드 도중에는 여전히 잠깐 부분적으로 감긴 값을 거치겠지만(0.25초 동안 1→0으로 자연스럽게 줄어드는 것 자체는 의도된 동작), 그 이후로는 다시는 안 올라가고 계속 0을 유지 — "눈 상태가 어중간한 데서 멈춰버리는" 것과 "잠깐 스쳐가는 자연스러운 전환"의 차이.
      - **수치 검증 (반복 렌더링으로 안정성 확인)**: `test/_diag-bellyup-stability.mjs`(스크래치, 검증 후 삭제)로 강아지 4종 각각 90분 시뮬레이션(닥스훈트 42회, 코기 55회, 허스키 39회, 포메라니안 26회 `bellyUp` 진입 — 무작위 `poke()`/`forceSleep()`/`wakeUp()`을 자주 섞어 특히 `sleep`(eyesClosed=1)에서 곧장 전환되는 경우를 의도적으로 많이 만듦) — `bellyUp`이 충분히 정착한 뒤(진입 후 1초 이상 지난 프레임만 집계, 즉 0.6초 램프업+0.25초 크로스페이드 둘 다 끝난 시점) `eyesClosed`가 정확히 0이 아닌 경우, `bodySquash`/`bellyUp` 값이 인접 프레임끼리 0.01 이상 차이 나는(=아직도 드리프트 중인) 경우를 각각 셌음 — **4종 전부 세 가지 지표 모두 0건**, 즉 정착 후에는 눈도 다리도 완전히 고정된 채 안 흔들림을 확인.
      - **스크린샷 검증**: `test/render-bellyup.mjs`(npm 스크립트는 아니고 `node test/render-bellyup.mjs`로 직접 실행 — 매번 새 `createAnimal()`로 독립된 인스턴스를 만들어 서로 다른 난수 상태에서 종별 3번씩 렌더링)로 4종 × 3회 반복 렌더링, 정착된 프레임(`behaviorTimer>1.2초`)에서 `eyesClosed`/`bodySquash`/`bellyUp` 값을 매번 출력 — 12번의 렌더링 전부 정확히 동일한 값(`eyesClosed=0, bodySquash=0.85, bellyUp=1.00`)으로 수렴, 스크린샷으로도 눈이 뚜렷이 뜬 채(감긴 가로선 아님) 납작하게 누워 다리가 몸통 위로 드러난 일관된 포즈를 4종 전부에서 확인(`test/bellyup-dog_{dachshund,corgi,husky,pomeranian}-v0.png`로 대표 1장씩 남김). `npm run test:invariants`(120만 프레임) 재실행 — 위반 0건.
  - **pet.js**: 3D 카메라/투영 다 걷어내고 원래(도트 시절) 방식으로 복귀 — `#pet-wrap`(96x96 div)을 `translateX`로 좌우 이동시키고, 그 안의 `<canvas>`에 `image-rendering:pixelated`로 크리스프하게 그림. 방향 전환은 canvas에 `scaleX(-1)`(래퍼 전체가 아니라 canvas만 뒤집어야 말풍선 글자가 안 뒤집힘). 말풍선은 pet-wrap의 자식 DOM이라 좌표 투영 없이 그냥 따라다님. **좌표 안전장치**: x가 매 프레임 `Number.isFinite` 체크를 통과하지 못하면 안전값으로 폴백한 뒤 clamp — `Math.min/max`만으로는 NaN을 못 거르므로(NaN과의 비교는 항상 false) 이 체크를 따로 둠. 걸음 속도(`spec.speed`)는 이제 3D의 world-unit이 아니라 실제 CSS px/sec. 패트롤 가능 범위는 여전히 좁은 창(340px) 기준 그대로 유지. 효과음은 외부 mp3 없이 Web Audio API 오실레이터로 생성(구체적인 소리 자체는 아래 "알림 소리를 부드럽게" 항목에서 교체됨).
    - **커서 방향에 따라 몸 전체가 좌우로 방향을 바꿈**: 기존엔 머리/눈(`lookDX/lookDY`, animal-engine.js)만 커서를 향했고 몸 전체가 보는 방향(`direction`, canvas의 `scaleX(±1)`을 좌우하는 값)은 그대로였음 — patrol이 아직 있던 시절 걷다가 경계에 부딪히면 뒤집던 것 말고는 `direction`을 바꾸는 경로가 없었음(patrol 자체가 이동 정책 변경으로 완전히 제거된 뒤로는 그 경계-반사 코드조차 죽은 코드). `updateBodyDirection()`을 새로 추가 — `lastMouseClient`(이미 커서 추적에 쓰는 값)의 `dx` 부호로 `direction`을 정하되, 정확히 중앙 근처(±`DIRECTION_FLIP_HYSTERESIS`=20px)에서는 안 바꾸는 데드존을 둬서 커서가 펫 바로 위/아래를 지나갈 때 좌우로 파르르 떨리듯 뒤집히는 걸 막음. 눈동자를 따로 움직이는 건(사용자가 명시적으로 "과할 것 같다"며 제외 요청) 넣지 않음 — 머리 전체가 커서 쪽으로 기우는 기존 `lookDX`만으로 시선 반응은 이미 충분하다고 판단.
      - **호출 순서 문제 (죽은 코드였던 patrol 경계-반사 로직과의 충돌)**: `updateCursorHint()`(머리/눈 커서 추적, `tick()` 맨 앞에서 호출) 안에 `direction` 설정도 같이 넣으려 했는데, `tick()` 뒷부분의 이동/경계 처리 블록(`if(x<=min){x=min;direction=1;}...`)이 그 뒤에 또 실행됨 — `advance`가 항상 0이라(자율 이동 완전 제거 이후) `x`가 영원히 `MARGIN`(=`bounds().min`)에 고정돼 있어서 `x<=min`이 매 프레임 항상 참이 되고, 그 결과 이 블록이 `direction=1`로 매 프레임 강제로 되돌려버려서 커서 기반 설정이 그 프레임 안에서 바로 무효화됐음. 이 patrol 경계-반사 코드 자체는(향후 "커서에게 다가가기" 같은 자율 이동 기능이 돌아올 때를 대비해 일부러 안 지운 죽은 코드 — 위 이동 정책 히스토리 참고) 지우지 않고, 대신 `updateBodyDirection()` 호출을 그 이동/경계 블록 **뒤**(즉 `render()` 직전)로 옮겨서 매 프레임 최종 결정권을 갖게 함 — 죽은 코드를 안 건드리면서 문제만 해결.
      - **검증**: 실제 Electron 앱을 띄워 `FOCUSPET_DEBUG_FINAL` 임시 훅(스크린샷 캡처용, 검증 후 제거)으로 콘솔에 관련 에러 없이 정상 렌더링되는 것 확인(코드 경로가 실행되는지 확인하는 용도 — 실제 커서를 이 자동화된 캡처 흐름 안에서 움직여보는 것까지는 안 됨, 아래 "알려진 한계" 참고).
    - **말풍선이 왼쪽 끝에서 잘리는 버그 — 실제로는 클램프 자체가 없었음**: "전에 클램프를 넣었다"는 기억과 달리, `pet.js`/`pet.css` 전체를 검색해도 말풍선 위치에 클램프 로직은 전혀 없었음(있던 건 펫 자기 자신의 `x` 좌표를 창 안에 두는 `bounds()`뿐, 말풍선 전용 로직은 처음부터 없었음) — `#bubble`이 `#pet-wrap`(96px)의 자식으로 `left:50%`인 채 그 부모가 340px 스테이지의 어디에 있는지는 전혀 신경 안 쓰는 순수 CSS 중앙정렬이었던 것. 펫이 왼쪽 끝(`x=MARGIN=16`)에 있으면 펫 중심(`x+48=64`)을 기준으로 말풍선(최대 190px 너비, 중심에서 최대 95px)이 좌우로 뻗어나가는데, 64-95=-31이라 스테이지 왼쪽 경계(x=0)를 31px 넘어가 `overflow:hidden`에 잘림 — 정확히 리포트된 증상. **실제 재현**: `main.js`에 `FOCUSPET_DEBUG_BUBBLE` 임시 env 훅(검증 후 제거)을 추가해 실제 앱을 띄우고 진짜 알림을 띄운 뒤 `petWindow.webContents.capturePage()`로 실제 렌더링을 캡처 — 고치기 전 스크린샷(`test/BEFORE-AFTER-bubble-clamp-before.png`)에서 메시지 앞부분("물")이 실제로 잘려 나가 "...를 한 잔 마시고 와요"로 보이는 걸 확인, 가설이 아니라 실물로 재현함.
      - **고침**: `positionBubble()`을 새로 추가 — 펫 중심(`x+WRAP_WIDTH/2`)과 말풍선의 실제 레이아웃 폭(`bubbleEl.offsetWidth`, 메시지 길이에 따라 매번 다름 — `hidden` 클래스를 뗀 직후에 읽어야 정확한 값이 나옴)으로 말풍선이 허용 가능한 중심 범위(`[반폭+4px, 스테이지폭-반폭-4px]`)를 계산하고, 펫 중심이 그 범위를 벗어나면 딱 그만큼만 `bubbleEl.style.left`(인라인 스타일로 CSS의 `left:50%` 오버라이드)를 밀어서 화면 안에 붙잡아둠. 말풍선 꼬리(`#bubble::after`)는 그대로 두면 밀려난 말풍선의 새 중심을 가리키게 되므로, `--tail-shift` CSS 변수(`calc(50% + var(--tail-shift,0px))`)를 반대 부호로 걸어서 꼬리는 계속 펫의 실제 위치를 가리키게 보정. `showBubble()`에서 `hidden` 클래스를 뗀 직후, `show` 클래스를 붙이기 전에 호출.
      - **검증**: 고친 뒤 같은 훅으로 다시 캡처(`test/BEFORE-AFTER-bubble-clamp-after.png`) — 메시지 전체("허리 좀 펴볼까요?")가 잘림 없이 온전히 보이고 창 왼쪽 경계에서 적절한 여백을 두고 있음을 확인. 클램프 공식 자체도 순수 수치 스크립트로 좌/우 극단(x=16, x=228) 양쪽 다 재확인 — 양쪽 다 정확히 4px(`BUBBLE_SAFE_MARGIN`) 여유를 두고 딱 맞게 계산됨.
      - **왼쪽에 그림자 같은 아티팩트 — 사용자 가설(꼬리 동기화 실패)은 틀렸고, 진짜 원인은 여백 부족이었음**: 클램프는 잘 되는데 밀려날 때 왼쪽에 그림자처럼 보이는 게 남는다는 리포트 — 사용자는 "꼬리가 여전히 예전 CSS 위치 기준으로 그려지는 것 같다"고 추측. **먼저 그 가설부터 직접 확인**: `#bubble::after`의 `border-color`를 일시적으로 빨간색으로 바꾸고 실제 앱을 띄워 `capturePage()`+`nativeImage.crop()/resize()`로 꼬리 부분만 4배 확대 캡처 — 빨간 꼬리가 정확히 말풍선 박스 아래 중앙 근처(펫을 향해)에 있는 걸 확인, **꼬리 동기화는 이미 정상이었음**(지난 세션에 만든 `--tail-shift` 메커니즘이 실제로 잘 작동하고 있었다는 뜻 — 사용자의 가설을 그대로 믿고 꼬리 쪽만 고쳤다면 헛수고했을 뻔함). 흰 꼬리로 되돌리고 같은 위치를 다시 확대 캡처해보니, 꼬리와는 별개로 말풍선 박스의 **둥근 왼쪽 아래 모서리**에 회색 얼룩이 있는 걸 발견 — 이게 진짜 리포트된 아티팩트. 원인 규명: `#bubble`의 `box-shadow: 0 6px 16px rgba(0,0,0,0.18)`는 박스 경계선에서 blur-radius(16px)만큼 시각적으로 더 퍼져나가는데, 기존 `BUBBLE_SAFE_MARGIN=4`는 말풍선 **박스 자체**만 스테이지 안에 들어오게 여유를 뒀을 뿐 이 그림자의 퍼짐까지는 고려 안 함 — 펫이 왼쪽 끝에 있어 말풍선이 최대로 밀려나면 박스 왼쪽 경계가 스테이지 x=4에 딱 붙는데, 그림자는 거기서 최대 16px 더 왼쪽(x=-12)까지 퍼지려다가 스테이지의 `overflow:hidden`(x=0)에서 뚝 잘림 — 3면은 자연스럽게 흐려지는데 왼쪽 한 면만 칼로 자른 듯 뚝 끊기니 "그림자 같은 아티팩트"로 보였던 것.
      - **검증 (margin=4 vs 20 스크린샷 비교)**: `BUBBLE_SAFE_MARGIN`을 20(박스+그림자 blur-radius를 합쳐도 여유 있게 들어오는 값)으로 올리고 같은 위치를 다시 확대 캡처 — 회색 얼룩이 완전히 사라지고 모서리가 깨끗하게 둥글게 마감됨을 확인(`test/BEFORE-AFTER-bubble-shadow-fix-{before,after}.png`). 클램프 계산식 자체(펫 중심/말풍선 폭 기반 로직)는 안 건드리고 상수 하나(4→20)만 바꿈 — 좌우 극단에서 재계산해보면 말풍선 박스 경계가 스테이지 경계에서 20px 여유를 두게 됨(이전엔 4px). `positionBubble()`/`--tail-shift` 메커니즘 자체는 이미 정상이었으므로 그대로 유지.
    - **알림 소리를 부드럽게**: 기존 `playBeep()`은 880Hz 순수 사인파에 공격적인 exponential 엔벨로프(20ms 만에 피크, 그 뒤 급격히 감쇠)라 전자음처럼 쨍하게 들린다는 피드백 — 마림바/우드블록 같은 "도록" 소리를 요청받음. `pluckNote(ctxA, freq, startAt, duration, peak)`를 새로 만들어 기본음(사인) 하나에 4배음(조용한 배음, gain 0.18 — 실제 마림바 음판의 1차 배음이 기본음의 대략 4배 근처인 것에서 착안)을 살짝 섞고, 공격을 `linearRampToValueAtTime`으로 12ms에 걸쳐 부드럽게, 감쇠는 `exponentialRampToValueAtTime`으로 자연스럽게(320ms) — 급격한 attack/cutoff 대신 "튕기고 서서히 잦아드는" pluck 느낌을 냄. `playBeep()`은 이 `pluckNote`를 C5(523.25Hz)와 E5(659.25Hz, 0.13초 뒤) 두 번 연속으로 낮은음→높은음 순서로 호출해 "도록" 하는 두 음 리듬을 만듦. Web Audio API 특성상 Node에서 직접 소리를 들어보며 검증할 수는 없어서(마이크/스피커 없는 headless 환경) 엔벨로프 수식 자체(공격/감쇠 값이 유효 범위인지, `exponentialRampToValueAtTime`이 0이 아닌 양수 시작/끝값을 받는지 등)를 검토하고, 실제 Electron 앱을 띄워 알림을 실제로 트리거해 콘솔에 오디오 관련 에러 없이 재생 경로가 정상 실행되는 것까지 확인 — 소리 자체의 "느낌"은 사용자가 직접 들어보고 판단해야 함(알려진 한계 참고).
      - **더 묵직하게 — "아이폰 무음 스위치 소리 같다"는 후속 피드백**: 위 마림바 pluck이 여전히 가볍고 딸깍거리게 들린다는 피드백 — "손등으로 나무 상자를 두드리는" 묵직한 "도독" 소리를 요청받음. 원인으로 짚이는 두 가지를 손봄: (1) 음역대 자체가 너무 높았음(C5/E5, 523/659Hz는 작고 밝은 타악기 음역) — 기본음을 두 옥타브 가까이 낮춰 185Hz(F#3 근처)로. (2) 배음 구성이 저음 보강 없이 밝은 배음(4배음)만 있었음 — `pluckNote`를 `knockNote`로 교체하면서 배음을 4배에서 2배로 낮추고(더 둥글고 덜 밝게) gain도 낮춤(0.18→0.14), 그리고 **기본음의 절반 주파수(한 옥타브 아래)에 새 서브 오실레이터를 추가**(gain 0.55, 세 소스 중 가장 비중 있게) — "무게감"의 실체는 기본음을 낮추는 것보다 이 저음 보강 자체에서 나온다고 보고 추가함("배음을 더 낮은 쪽으로 옮기고... 저음 성분 비중을 높이는 식으로"라는 요청을 그대로 반영). 어택은 여전히 부드럽게(`linearRampToValueAtTime`, 12ms→18ms로 살짝 더 늘림), 디케이는 0.32초→0.5초로 늘려서 "치고 바로 끊기는" 느낌 대신 "치고 나서 여운이 남는" 느낌으로. 두 번의 노크도 기존엔 낮은음→높은음으로 올라가는 작은 멜로디(C5→E5)였는데, 이번엔 **완전히 같은 낮은 음(185Hz)을 두 번** — 사용자가 비유한 "두드리는" 동작 자체가 오르내리는 가락이 아니라 같은 강도로 반복되는 물리적 타격이라 판단해서, 멜로디 대신 반복 타격으로 성격을 바꿈.
      - **검증**: 이전과 마찬가지로 이 headless 환경에서 직접 들어볼 수는 없어서(알려진 한계 참고) 실제 Electron 앱을 띄워 알림을 트리거해 콘솔에 오디오 에러 없이 정상 실행되는 것까지 확인. 엔벨로프/주파수 값 자체도 코드 리뷰로 재검토(서브 오실레이터 gain 0.55가 기본음 대비 과하게 커서 클리핑 위험은 없는지 — `masterGain`의 피크 자체가 0.17로 이미 작아서 세 소스를 합쳐도 Web Audio의 정상 진폭 범위 안에 여유 있게 들어옴).
    - **클릭 통과 히트테스트**: `window`의 `mousemove`마다 좌표를 저장해뒀다가, 매 프레임(`tick()`의 `render()` 직후) `processPendingMouse()`가 한 번만 처리 — mousemove가 프레임보다 훨씬 자주 오므로 rAF 주기로 코얼레싱. canvas의 `getBoundingClientRect()`로 커서를 캔버스 백킹 픽셀 좌표로 환산하고(96x96 backing == 96x96 CSS라 배율 계산 불필요, `scaleX(-1)` 뒤집힘만 `canvas.width-1-x`로 보정) `ctx.getImageData(x,y,1,1).data[3]`로 그 지점의 실제 알파값을 읽어 임계치(128, 그림자의 ~46/255보다는 높고 스프라이트의 불투명 채움 255보다는 낮게)와 비교. 히트 여부가 바뀔 때만(`hitState`와 다를 때만) `focusPetAPI.setIgnoreMouseEvents(!hit, {forward:true})`를 호출해서 불필요한 IPC 왕복을 줄임. 바운딩박스 근사 대신 알파 샘플링을 택한 이유: 스프라이트가 매 프레임 자세가 바뀌는 작고 불규칙한 실루엣이라 고정 사각형으로는 정확도가 안 나오고, 1x1 `getImageData` 한 번은 mousemove당 비용으로 무시할 만함 (canvas는 `{willReadFrequently:true}`로 얻어서 반복 readback을 브라우저가 최적화하게 함). `main.js`가 창 생성 시 기본으로 `setIgnoreMouseEvents(true,{forward:true})`를 걸어두므로, 첫 mousemove가 오기 전까지는 안전하게 클릭 통과 상태. (예전엔 여기에 "기어 버튼 영역은 알파값과 무관하게 무조건 히트" 분기가 있었는데, 기어 버튼 자체가 없어져서 — 바로 아래 항목 — 지금은 전부 알파 샘플링 한 경로로만 판정함.)
    - **설정 버튼 제거 / 더블클릭으로 대체**: 펫 창의 톱니바퀴(⚙️) 버튼(`#gear`)을 완전히 삭제 — `index.html`에서 엘리먼트 자체를 뺐고, `pet.css`의 `#gear` 규칙과 `pet.js`의 관련 코드(클릭 리스너, 히트테스트 특례)도 같이 정리함. 설정을 여는 경로는 이제 두 가지: (1) 트레이 아이콘 메뉴의 "설정 열기"(기존 그대로, `main.js`의 `open-settings` IPC), (2) 펫을 더블클릭.
    - **더블클릭이 실제로는 안 열리던 버그**: 처음엔 `canvas`에 네이티브 `dblclick` 리스너를 달아뒀는데, 이 창이 `focusable:false`라 그런지 실제로는 잘 안 잡혔음(자동화 테스트에서 `dispatchEvent(new MouseEvent('dblclick'))`로 직접 합성 이벤트를 쏴서 확인했을 때는 통과했지만, 이건 브라우저의 실제 더블클릭 판정 로직 — 두 번의 real mousedown/mouseup을 시간差 보고 하나의 dblclick으로 묶어주는 것 — 을 건너뛰고 dblclick 이벤트 자체를 직접 만들어 쏜 것이었어서 가짜 통과였음). 네이티브 이벤트에 의존하지 않고 직접 타임스탬프를 재는 방식으로 교체 — 이미 있던 `click` 리스너(`poke()` 처리하는 곳)에서 `performance.now() - lastClickAt < 300`이면 더블클릭으로 판정해 `openSettings()`를 부르고 `poke()`는 건너뜀(리셋을 위해 `lastClickAt`을 0으로 되돌려서, 짧은 시간 안에 세 번째 클릭이 들어와도 그건 또 다른 더블클릭의 시작으로 안 잡히게 함). 테스트도 이제 실제 `click` 이벤트를 두 번 시간差 두고 보내는 식으로 재검증(합성 `dblclick` 한 방으로 넘어가지 않도록).
- **설정 창** (`windows/settings/`): 캐릭터 선택 그리드(4열 × 3행, 10종) — 각 칸의 작은 `<canvas>`에 동일 엔진으로 정지 포즈를 그려서 보여줌(정적 이미지 파일 아님, 실제 스프라이트 그대로). `.char-btn canvas`의 CSS `aspect-ratio`가 예전엔 그리드 비율(24x20 → 6:5)에 맞춰 하드코딩돼 있었는데, 그리드가 24x24(1:1)로 바뀌면서 이걸 안 고쳤으면 썸네일이 가로로 눌린 채 렌더링될 뻔했음 — `aspect-ratio: 1/1`로 같이 수정.

## 알려진 한계 / 다음에 다듬을 것
- 다중 모니터 처리 안 됨 (항상 `getPrimaryDisplay()` 기준)
- 펫 창 위치가 화면 크기 바뀌거나 모니터 구성 바뀔 때 재계산 안 됨
- 24x24(도입 당시 24x20이었다가 토끼 귀 클리핑 때문에 세로로 확장 — 위 항목 참고) 픽셀 그리드가 작아서 표현할 수 있는 디테일에 한계가 있음 — 다리는 이제 hip→knee→paw 2본 관절 체인으로 실제로 굽지만(`legJointAngles`/`pxJoint`), 각 세그먼트 길이가 종별 고정값(도출 공식이 아니라 손으로 튜닝)이라 IK처럼 목표 지점에 정확히 닿도록 풀리는 건 아니고 각도만 맞춰서 "그럴듯하게" 굽음 — 토끼처럼 몸통이 크고 다리가 짧은 종은 다리 힌지 위치(`legY`)가 몸통 아래쪽 경계와 딱 맞아떨어지지 않으면 다리가 몸통 사각형에 그대로 가려져 안 보이는 문제가 있었음(당시 토끼 3종 모두 `legY`가 몸통 바닥보다 위에 있어서 발생 — `legY`를 몸통 바닥과 맞춰서 고침, 이후 새로 추가한 종은 전부 처음부터 이 규칙을 지켜서 authored함). 그루밍도 앞다리를 관절째로 얼굴 쪽으로 접어 올리는 식으로 바뀌었지만 목표 각도(`legJointAngles`의 `frontLegRaise` 분기, -160°/-50°)는 좌표 계산으로 검증했을 뿐 96x96px에서 육안으로는 다리 색이 머리/몸통 색과 비슷해 잘 안 보일 수 있음 — 더 튜닝하려면 좌표를 직접 찍어보는 스크립트(`legJointAngles` 결과를 `pxJoint`가 반환하는 끝점으로 역추적)로 확인하는 게 눈으로 보는 것보다 정확함
- 캐릭터 외형·색상·걸음걸이·idle 행동 파라미터가 `windows/shared/animal-engine.js`의 `SPECIES` 테이블에 하드코딩되어 있음 (사전 생성 이미지 파일이 없어서 — 매 프레임 이 파라미터로 캔버스에 직접 그림). 캐릭터 추가/수정은 이 파일의 종별 객체와 `idlePool`만 고치면 됨, 별도 빌드/이미지 생성 스텝 없음
- 클릭 통과 판정은 `mousemove` 이벤트가 실제로 발생해야 갱신됨 — 앱이 막 켜졌거나 창이 재배치된 순간 커서가 이미(움직이지 않은 채로) 캐릭터 위에 있으면, 그 시점엔 `mousemove`가 안 뜨므로 main.js가 걸어둔 기본값(클릭 통과)이 그대로 유지되다가 커서를 살짝 움직여야 반영됨 — 실사용에서 거의 못 느낄 수준이지만 완벽한 즉시성은 아님
- 여우(`assets/reference/fox.jpg`)는 참고용으로만 있고 아직 선택 목록에 없음 — 같은 패턴으로 추가 가능
- 이동(patrol)은 완전히 제거됨(위 "이동 정책 변경 2단계" 참고) — `x`는 이제 초기값에서 전혀 안 바뀌고 드래그로만 움직임. 시선 방향 추적(고개/눈)과 몸 전체 방향(`direction`, 위 "커서 방향에 따라 몸 전체가 좌우로 방향을 바꿈" 참고)은 이제 화면 전체 기준으로 상시 동작(위 "이동 정책 변경 3단계" 참고, `main.js`의 `startCursorTrackPoll()`이 100ms마다 전역 커서 위치를 폴링); 근접 alert 반응(귀 쫑긋 등)만 여전히 `CURSOR_CLOSE_RADIUS`(55px) 기준의 국소 반응임
- 알림 소리(`knockNote`/`playBeep`, pet.js)는 Web Audio API 코드라 이 headless 환경에서 직접 재생해 들어볼 방법이 없음 — 엔벨로프/주파수 수식 검토 + 실제 앱에서 트리거해 콘솔에 오디오 에러 없이 실행되는 것까지만 확인함, "묵직하게 들리는지"라는 실제 청감 판단은 사용자가 직접 들어보고 확인 필요 — 이미 두 번 피드백을 반영해 튜닝했으므로(마림바→우드박스넉) 세 번째 조정이 필요하면 이번에도 방향(음역/배음 비율/디케이 길이) 위주로 구체적으로 알려주면 반영하기 수월함
- 강아지 4종 중 포메라니안은 `bellyUp`(배 하늘로 향한 채 쉬는 포즈)에서 다리가 몸통 실루엣을 완전히 벗어나지 못함 — 옛 rollover 시절부터 있던 `reachScale` 유도 공식(종별 몸높이 대비 다리 길이 비율에서 필요한 만큼만 늘리되 4.2배로 상한)을 그대로 재사용했기 때문(몸높이 대비 다리가 로스터에서 가장 짧은 조합이라 상한을 넘어야 완전히 벗어나는데, 그러면 막대기처럼 보임 - 이전 rollover 세션에서 이미 검증된 트레이드오프, `bellyUp`으로 교체되면서도 이 기하학적 제약 자체는 안 바뀌었으므로 그대로 승계)
- 커서 방향에 따른 몸 전체 방향 전환(`updateBodyDirection`)은 실제 앱을 띄워 코드 경로가 에러 없이 도는 것까지는 확인했지만, 이 headless 환경에서는 실제 마우스 커서를 이리저리 움직이며 방향이 실시간으로 바뀌는 모습까지는 직접 확인하지 못함 — 사용자가 실사용 중 확인 필요
- 강아지 `sit`(`haunches`)이 `pose.waddleTilt`로 몸 전체를 살짝 뒤로 기울이는데, `drawShadow`는 `pose`를 전혀 안 읽는 고정 위치 타원이라 이 기울임을 따라가지 않아 그림자가 몸통 아래에서 살짝 벗어나 보임 — `rollover`가 이미 최대 70°까지 훨씬 크게 같은 회전을 써왔는데도 지금까지 지적된 적 없던 것과 같은 종류의(사전부터 있던) 단순화라 이번에도 안 건드림. 고치려면 `drawShadow`가 `pose.rollAngle+pose.waddleTilt`를 받아 타원 중심을 같이 회전시키는 정도로 충분할 것
- 시작 프로그램 자동 등록 기능 없음 (수동으로 `npm start` 또는 빌드 후 실행)
- 드래그로 창을 화면 어디로 옮기든 화면 경계 밖으로 나가는 걸 막는 clamp가 없음 — 모니터 가장자리까지 세게 끌고 가면 창이 화면 밖으로 일부/전부 나갈 수 있음(요청받은 범위가 아니라 일부러 안 넣음, 다중 모니터 미지원과 마찬가지로 향후 다듬을 거리)
- 드래그로 창을 옮긴 뒤 설정에서 알림 위치(`position`)를 바꾸거나 저장하면 `save-settings` 핸들러가 무조건 그 설정값 기준 구석 좌표로 다시 스냅함(기존 동작 그대로 유지 — 의도적인 설정 기반 재배치라 건드리지 않음, 드래그로 옮긴 위치가 설정 저장 전까지만 유지된다는 뜻)
- idle 감지에서 화면보호기/잠금 상태와의 상호작용 미검증 (수면 연출용 idle 폴링도 동일 API를 쓰므로 같은 사각지대)
- Shimeji류 기능 중 커서 따라 시선 이동은 이제 들어옴(위 "이동 정책 변경 2/3단계" 참고, 화면 전체 기준 상시 추적), 화면 가장자리 매달리기/중력, 다른 창에 올라타기는 여전히 일부러 안 넣음 — 이 앱은 "화면 구석에 늘 떠 있는 가벼운 집중 알림 오버레이"가 목적이라 그런 기능들은 무겁거나(전역 마우스/창 위치 폴링) 목적에 안 맞는다고 판단함. "손으로 집어서 원하는 데로 옮기기"(드래그, 스퀴시+매달림 포즈+스프링 지연 추적으로 "잡힌 느낌" 보강 — 위 드래그 항목 참고)는 클릭 반응·시스템 idle 연동 수면·드래그 이동·커서 반응, 이렇게 네 가지가 목적에 맞는 선으로 들어와 있음
- 드래그 스프링-댐퍼 물리(`main.js`)는 매 틱 실제 OS 커서 위치(`screen.getCursorScreenPoint()`)를 목표로 추적하므로, 자동화 테스트에서 합성 mousemove로는 이 목표 위치를 흉내낼 수 없음(실제 사람 손으로 드래그할 때는 문제 없음) — 헤드리스 검증 스크립트로 스프링 자체의 수렴성(오버슈트 없이 목표에 도달)은 별도 시뮬레이션으로, 창 크기 불변/커서 스타일 전환/스퀴시 발생 여부는 실제 앱 코드로 확인했지만 "합성 이벤트로 실제 커서를 이 시점에 이 위치로 이동시켜 스프링이 정확히 그 좌표를 쫓아가는지"까지는 자동 검증 범위 밖임

## 파일 구조
```
focus-pet/
├── main.js
├── preload.js
├── settingsStore.js
├── package.json          (dependencies 없음 — 순수 Canvas 2D)
├── assets/
│   ├── tray-icon.png, app-icon.png
│   └── reference/ (스타일 레퍼런스 이미지, 앱에서 사용 안 함 — 캐릭터 디자인 참고용)
├── test/
│   ├── sim-invariants.mjs        (npm run test:invariants — 종x시드 대량 시뮬레이션 + checkPoseInvariants 회귀 스위트, 좌표가 올바른지만 검증 - 실제 렌더링 결과는 아래 render-*.mjs들 몫)
│   ├── sim-movement-stats.mjs    (npm run test:movement-stats — 이제는 이동 정책 2단계 후 전 종 walk 비율이 0.0%임을 보여주는 회귀 확인용)
│   ├── sim-idle-rhythm.mjs       (npm run test:idle-rhythm — breathe idle의 시간 점유율/분당 전환 횟수를 측정해 "차분한 리듬"이 유지되는지 보는 회귀 확인용)
│   ├── mini-canvas.mjs           (Node 내장 zlib만으로 만든 헤드리스 Canvas2D 서브셋 + PNG 인코더 - node-canvas 등 네이티브 의존성 없음, 실제 drawCreature를 그대로 실행해 눈으로 볼 PNG를 뽑기 위한 용도)
│   ├── render-poses.mjs          (npm run test:render-poses — sit 포즈/커서 반응 스크린샷을 실제 drawCreature로 렌더링해 PNG로 저장)
│   ├── render-zoom.mjs           (특정 영역을 크게 확대해 크롭하는 헬퍼 - 귀-머리 경계 같은 몇 픽셀짜리 이음매 문제를 96x96 전체 그림에서는 못 알아볼 때 씀)
│   ├── render-bellyup.mjs        (rollover가 삭제되고 대체된 정적 idle `bellyUp`을 강아지 4종 × 3회 반복 렌더링해 eyesClosed/bodySquash/bellyUp 값이 매번 동일하게 정착하는지 확인 - rollover 삭제 후 새 포즈의 안정성 검증용)
│   ├── render-cursor-sweep.mjs   (커서를 화면 좌표 전체에 걸쳐 쓸며 hint.dx/lookDX 수렴값을 숫자로, 스크린샷도 같이 남김)
│   ├── render-cursor-sequence.mjs (같은 애니멀 인스턴스 하나로 커서를 좌→중앙→우로 실시간으로 옮기며 렌더링 - 독립된 인스턴스 비교보다 실제 사용에 더 가까운 시퀀스)
│   ├── render-full-check.mjs     (여러 종의 sit/sleep 전신 렌더 - 패턴/벨리가 몸통 경계를 넘는지 같은 전신 단위 확인용)
│   └── sim-rotation-debug.mjs    (npm run test:rotation-debug — setRotationDebug 훅으로 |rollAngle+waddleTilt| 임계값 초과 프레임을 대량 시뮬레이션에서 탐지)
└── windows/
    ├── shared/
    │   ├── package.json  ({"type":"module"} — animal-engine.js를 Node에서 그대로 import해 테스트하기 위한 스코프 지정일 뿐, Electron 렌더러는 index.html의 <script type="module">을 쓰므로 이 파일과 무관하게 그대로 동작함)
    │   └── animal-engine.js  (픽셀아트 엔진 + 행동 상태머신 — pet/settings 두 창이 공유)
    ├── pet/ (index.html, pet.css, pet.js)
    └── settings/ (index.html, settings.css, settings.js)
```
